/* @ds-bundle: {"format":3,"namespace":"AnonaDesignWebsite_2e8ade","components":[],"sourceHashes":{"ANONA_investor_deck/deck-stage.js":"0c125b8b1e23","TENET_product_deck/deck-stage.js":"0c125b8b1e23","animations.jsx":"2b1e0ed2c732","competition_deck/deck-stage.js":"0c125b8b1e23","dialogue.js":"94428195aca4","flow/app.jsx":"65d775c9011c","flow/client-data.js":"45835784fc97","flow/client-hero.jsx":"84543b0b9881","flow/client-shell.jsx":"5f745ebe02aa","flow/client-tabs1.jsx":"31a4647bc095","flow/client-tabs2.jsx":"9c2de340872c","flow/creative-data.js":"bcfa95451abe","flow/creative-shell.jsx":"5b528f871d07","flow/creative-views.jsx":"22d2f86fd60b","flow/creative-writers.jsx":"90caf6037684","flow/data.js":"66904679f1de","flow/director-edit.jsx":"9dad3f65988c","flow/framework.js":"f600ae0b11df","flow/panels.jsx":"171cef5514af","flow/producer-dash.jsx":"2fcf51a283ff","flow/production-data.js":"ebc2236f134f","flow/production-hero.jsx":"7f6aaa7d728d","flow/production-shell.jsx":"8c171c467d6b","flow/production-tabs.jsx":"f4740d3bfc56","flow/scaffolds.jsx":"e422f8ca361e","flow/screens.jsx":"f43bab907e5d","flow/ui.jsx":"e901fc7436f1","flow_ctx/app.jsx":"ee50e435b8ac","flow_ctx/client-data.js":"45835784fc97","flow_ctx/client-hero.jsx":"84543b0b9881","flow_ctx/client-shell.jsx":"5f745ebe02aa","flow_ctx/client-tabs1.jsx":"31a4647bc095","flow_ctx/client-tabs2.jsx":"9c2de340872c","flow_ctx/contextual-editbay.jsx":"7cfe33165b54","flow_ctx/contextual.jsx":"85502f2fe11a","flow_ctx/creative-data.js":"bcfa95451abe","flow_ctx/creative-shell.jsx":"d4c07dddf825","flow_ctx/creative-views.jsx":"22d2f86fd60b","flow_ctx/creative-writers.jsx":"90caf6037684","flow_ctx/data.js":"66904679f1de","flow_ctx/director-edit.jsx":"b5e282e8d7a2","flow_ctx/framework.js":"f600ae0b11df","flow_ctx/panels.jsx":"171cef5514af","flow_ctx/producer-dash.jsx":"2fcf51a283ff","flow_ctx/production-data.js":"ebc2236f134f","flow_ctx/production-hero.jsx":"7f6aaa7d728d","flow_ctx/production-shell.jsx":"8c171c467d6b","flow_ctx/production-tabs.jsx":"f4740d3bfc56","flow_ctx/scaffolds.jsx":"e422f8ca361e","flow_ctx/screens.jsx":"f43bab907e5d","flow_ctx/ui.jsx":"e901fc7436f1","lobby_deck/deck-stage.js":"0c125b8b1e23","scene.jsx":"73e2f5f7083a","tenet-demo/App.jsx":"90ce2839458a","tenet-demo/ClientSeat.jsx":"a20eef60a08c","tenet-demo/DirectorSeat.jsx":"ce939e52fa2e","tenet-demo/model.js":"464f248ac5a1","tenet-demo/ui.jsx":"a99096fabb08","tweaks-panel.jsx":"6591467622ed","ui_kits/tenet/Icon.jsx":"44620bd56a81","ui_kits/tenet/ShotDetail.jsx":"1a9e0d20ca71","ui_kits/tenet/ShotList.jsx":"aa158d8717ff","ui_kits/tenet/Sidebar.jsx":"95032d971b02","ui_kits/tenet/StatusChip.jsx":"924032052db1","ui_kits/tenet/TopBar.jsx":"bb9b03faceef"},"inlinedExternals":[],"unexposedExports":[]} */

(() => {

const __ds_ns = (window.AnonaDesignWebsite_2e8ade = window.AnonaDesignWebsite_2e8ade || {});

const __ds_scope = {};

(__ds_ns.__errors = __ds_ns.__errors || []);

// ANONA_investor_deck/deck-stage.js
try { (() => {
/**
 * <deck-stage> — reusable web component for HTML decks.
 *
 * Handles:
 *  (a) speaker notes — reads <script type="application/json" id="speaker-notes">
 *      and posts {slideIndexChanged: N} to the parent window on nav.
 *  (b) keyboard navigation — ←/→, PgUp/PgDn, Space, Home/End, number keys.
 *      On touch devices, tapping the left/right half of the stage goes
 *      prev/next — taps on links, buttons and other interactive slide
 *      content are left alone.
 *  (c) press R to reset to slide 0 (with a tasteful keyboard hint).
 *  (d) bottom-center overlay showing slide count + hints, fades out on idle.
 *  (e) auto-scaling — inner canvas is a fixed design size (default 1920×1080)
 *      scaled with `transform: scale()` to fit the viewport, letterboxed.
 *      Set the `noscale` attribute to render at authored size (1:1) — the
 *      PPTX exporter sets this so its DOM capture sees unscaled geometry.
 *  (f) print — `@media print` lays every slide out as its own page at the
 *      design size, so the browser's Print → Save as PDF produces a clean
 *      one-page-per-slide PDF with no extra setup.
 *  (g) thumbnail rail — resizable left-hand column of per-slide thumbnails
 *      (static clones). Click to navigate; ↑/↓ with a thumbnail focused to
 *      step between slides; drag to reorder; right-click for
 *      Skip / Move up / Move down / Delete (opens a Cancel/Delete confirm
 *      dialog). Drag the rail's right edge to resize; width persists to
 *      localStorage. Skipped slides carry `data-deck-skip`, are dimmed in
 *      the rail, omitted from prev/next navigation, and hidden at print.
 *      The rail is suppressed in presenting mode, in the host's Preview
 *      mode (ViewerMode='none'), on `noscale`, on narrow viewports
 *      (≤640px), and via the `no-rail` attribute. Rail mutations dispatch
 *      a `deckchange`
 *      CustomEvent on the element: detail = {action, from, to, slide}.
 *
 * Slides are HIDDEN, not unmounted. Non-active slides stay in the DOM with
 * `visibility: hidden` + `opacity: 0`, so their state (videos, iframes,
 * form inputs, React trees) is preserved across navigation.
 *
 * Lifecycle event — the component dispatches a `slidechange` CustomEvent on
 * itself whenever the active slide changes (including the initial mount).
 * The event bubbles and composes out of shadow DOM, so you can listen on
 * the <deck-stage> element or on document:
 *
 *   document.querySelector('deck-stage').addEventListener('slidechange', (e) => {
 *     e.detail.index         // new 0-based index
 *     e.detail.previousIndex // previous index, or -1 on init
 *     e.detail.total         // total slide count
 *     e.detail.slide         // the new active slide element
 *     e.detail.previousSlide // the prior slide element, or null on init
 *     e.detail.reason        // 'init' | 'keyboard' | 'click' | 'tap' | 'api'
 *   });
 *
 * Persistence: none at the deck level. The host app keeps the current slide
 * in its own URL (?slide=) and re-delivers it via location.hash on load, so a
 * bare load with no hash always starts at slide 1.
 *
 * Usage:
 *   <style>deck-stage:not(:defined){visibility:hidden}</style>
 *   <deck-stage width="1920" height="1080">
 *     <section data-label="Title">...</section>
 *     <section data-label="Agenda">...</section>
 *   </deck-stage>
 *   <script src="deck-stage.js"></script>
 *
 * The :not(:defined) rule prevents a flash of the first slide at its
 * authored styles before this script runs and attaches the shadow root.
 *
 * Slides are the direct element children of <deck-stage>. Each slide is
 * automatically tagged with:
 *   - data-screen-label="NN Label"   (1-indexed, for comment flow)
 *   - data-om-validate="no_overflowing_text,no_overlapping_text,slide_sized_text"
 */

(() => {
  const DESIGN_W_DEFAULT = 1920;
  const DESIGN_H_DEFAULT = 1080;
  const OVERLAY_HIDE_MS = 1800;
  const VALIDATE_ATTR = 'no_overflowing_text,no_overlapping_text,slide_sized_text';
  const FINE_POINTER_MQ = matchMedia('(hover: hover) and (pointer: fine)');
  const NARROW_MQ = matchMedia('(max-width: 640px)');
  // Slide-authored controls that should keep a tap instead of it navigating.
  const INTERACTIVE_SEL = 'a[href], button, input, select, textarea, summary, label, video[controls], audio[controls], [role="button"], [onclick], [tabindex]:not([tabindex^="-"]), [contenteditable]:not([contenteditable="false" i])';
  const pad2 = n => String(n).padStart(2, '0');

  // Label precedence: data-label → data-screen-label (number stripped) → first heading → "Slide".
  const getSlideLabel = el => {
    const explicit = el.getAttribute('data-label');
    if (explicit) return explicit;
    const existing = el.getAttribute('data-screen-label');
    if (existing) return existing.replace(/^\s*\d+\s*/, '').trim() || existing;
    const h = el.querySelector('h1, h2, h3, [data-title]');
    const t = h && (h.textContent || '').trim().slice(0, 40);
    if (t) return t;
    return 'Slide';
  };
  const stylesheet = `
    :host {
      position: fixed;
      inset: 0;
      display: block;
      background: #000;
      color: #fff;
      font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", Helvetica, Arial, sans-serif;
      overflow: hidden;
      -webkit-tap-highlight-color: transparent;
    }
    /* connectedCallback holds this until document.fonts.ready (capped 2s) so
     * the first visible paint has the deck's real typography + final rail
     * layout. opacity (not visibility) so the active slide can't un-hide
     * itself via the ::slotted([data-deck-active]) visibility:visible rule.
     * Only the stage/rail hide — the black :host background stays, so the
     * iframe doesn't flash the page's default white. */
    :host([data-fonts-pending]) .stage,
    :host([data-fonts-pending]) .rail { opacity: 0; pointer-events: none; }

    .stage {
      position: absolute;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .canvas {
      position: relative;
      transform-origin: center center;
      flex-shrink: 0;
      background: #fff;
      will-change: transform;
    }

    /* Slides live in light DOM (via <slot>) so authored CSS still applies.
       We absolutely position each slotted child to stack them. */
    ::slotted(*) {
      position: absolute !important;
      inset: 0 !important;
      width: 100% !important;
      height: 100% !important;
      box-sizing: border-box !important;
      overflow: hidden;
      opacity: 0;
      pointer-events: none;
      visibility: hidden;
    }
    ::slotted([data-deck-active]) {
      opacity: 1;
      pointer-events: auto;
      visibility: visible;
    }

    .overlay {
      position: fixed;
      left: 50%;
      bottom: 22px;
      transform: translate(-50%, 6px) scale(0.92);
      filter: blur(6px);
      display: flex;
      align-items: center;
      gap: 4px;
      padding: 4px;
      background: #000;
      color: #fff;
      border-radius: 999px;
      font-size: 12px;
      font-feature-settings: "tnum" 1;
      letter-spacing: 0.01em;
      opacity: 0;
      pointer-events: none;
      transition: opacity 260ms ease, transform 260ms cubic-bezier(.2,.8,.2,1), filter 260ms ease;
      transform-origin: center bottom;
      z-index: 2147483000;
      user-select: none;
    }
    .overlay[data-visible] {
      opacity: 1;
      pointer-events: auto;
      transform: translate(-50%, 0) scale(1);
      filter: blur(0);
    }

    .btn {
      appearance: none;
      -webkit-appearance: none;
      background: transparent;
      border: 0;
      margin: 0;
      padding: 0;
      color: inherit;
      font: inherit;
      cursor: default;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      height: 28px;
      min-width: 28px;
      border-radius: 999px;
      color: rgba(255,255,255,0.72);
      transition: background 140ms ease, color 140ms ease;
      -webkit-tap-highlight-color: transparent;
    }
    .btn:hover { background: rgba(255,255,255,0.12); color: #fff; }
    .btn:active { background: rgba(255,255,255,0.18); }
    .btn:focus { outline: none; }
    .btn:focus-visible { outline: none; }
    .btn::-moz-focus-inner { border: 0; }
    .btn svg { width: 14px; height: 14px; display: block; }
    .btn.reset {
      font-size: 11px;
      font-weight: 500;
      letter-spacing: 0.02em;
      padding: 0 10px 0 12px;
      gap: 6px;
      color: rgba(255,255,255,0.72);
    }
    .btn.reset .kbd {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-width: 16px;
      height: 16px;
      padding: 0 4px;
      font-family: ui-monospace, "SF Mono", Menlo, Consolas, monospace;
      font-size: 10px;
      line-height: 1;
      color: rgba(255,255,255,0.88);
      background: rgba(255,255,255,0.12);
      border-radius: 4px;
    }

    .count {
      font-variant-numeric: tabular-nums;
      color: #fff;
      font-weight: 500;
      padding: 0 8px;
      min-width: 42px;
      text-align: center;
      font-size: 12px;
    }
    .count .sep { color: rgba(255,255,255,0.45); margin: 0 3px; font-weight: 400; }
    .count .total { color: rgba(255,255,255,0.55); }

    .divider {
      width: 1px;
      height: 14px;
      background: rgba(255,255,255,0.18);
      margin: 0 2px;
    }

    /* ── Thumbnail rail ──────────────────────────────────────────────────
       Fixed column on the left; each thumbnail is a static deep-clone of
       the light-DOM slide scaled into a 16:9 (or design-aspect) frame. The
       stage re-fits around it (see _fit); hidden during present / noscale
       / print so capture geometry and fullscreen output are unchanged. */
    .rail {
      position: fixed;
      left: 0;
      top: 0;
      bottom: 0;
      width: var(--deck-rail-w, 188px);
      background: #141414;
      border-right: 1px solid rgba(255,255,255,0.08);
      overflow-y: auto;
      overflow-x: hidden;
      padding: 12px 10px;
      box-sizing: border-box;
      display: flex;
      flex-direction: column;
      gap: 12px;
      z-index: 2147482500;
      scrollbar-width: thin;
      scrollbar-color: rgba(255,255,255,0.18) transparent;
    }
    .rail::-webkit-scrollbar { width: 8px; }
    .rail::-webkit-scrollbar-track { background: transparent; margin: 2px; }
    .rail::-webkit-scrollbar-thumb {
      background: rgba(255,255,255,0.18);
      border-radius: 4px;
      border: 2px solid transparent;
      background-clip: content-box;
    }
    .rail::-webkit-scrollbar-thumb:hover {
      background: rgba(255,255,255,0.28);
      border: 2px solid transparent;
      background-clip: content-box;
    }
    :host([no-rail]) .rail,
    :host([noscale]) .rail { display: none; }
    .rail[data-presenting] { display: none; }
    @media (max-width: 640px) {
      .rail, .rail-resize { display: none; }
    }
    /* User-driven show/hide (the TweaksPanel toggle) slides instead of
       popping. Transitions are gated on :host([data-rail-anim]) — set only
       for the 200ms around the toggle — so window-resize and rail-width
       drag (which also call _fit) don't lag behind the cursor. */
    .rail[data-user-hidden] { transform: translateX(-100%); }
    :host([data-rail-anim]) .rail { transition: transform 200ms cubic-bezier(.3,.7,.4,1); }
    :host([data-rail-anim]) .stage { transition: left 200ms cubic-bezier(.3,.7,.4,1); }
    :host([data-rail-anim]) .canvas { transition: transform 200ms cubic-bezier(.3,.7,.4,1); }
    /* transition shorthand replaces rather than merges — repeat the base
       .overlay opacity/transform/filter transitions so visibility changes
       during the 200ms toggle window still fade instead of popping. */
    :host([data-rail-anim]) .overlay {
      transition: margin-left 200ms cubic-bezier(.3,.7,.4,1),
                  opacity 260ms ease,
                  transform 260ms cubic-bezier(.2,.8,.2,1),
                  filter 260ms ease;
    }

    .thumb {
      position: relative;
      display: flex;
      align-items: flex-start;
      gap: 8px;
      cursor: pointer;
      user-select: none;
    }
    .thumb .num {
      width: 16px;
      flex-shrink: 0;
      font-size: 11px;
      font-weight: 500;
      text-align: right;
      color: rgba(255,255,255,0.55);
      padding-top: 2px;
      font-variant-numeric: tabular-nums;
    }
    .thumb .frame {
      position: relative;
      flex: 1;
      min-width: 0;
      aspect-ratio: var(--deck-aspect);
      background: #fff;
      border-radius: 4px;
      outline: 2px solid transparent;
      outline-offset: 0;
      overflow: hidden;
      transition: outline-color 120ms ease;
    }
    .thumb:hover .frame { outline-color: rgba(255,255,255,0.25); }
    .thumb { outline: none; }
    .thumb:focus-visible .frame { outline-color: rgba(255,255,255,0.5); }
    .thumb[data-current] .num { color: #fff; }
    .thumb[data-current] .frame { outline-color: #D97757; }
    .thumb[data-dragging] { opacity: 0.35; }
    .thumb::before {
      content: '';
      position: absolute;
      left: 24px;
      right: 0;
      height: 3px;
      border-radius: 2px;
      background: #D97757;
      opacity: 0;
      pointer-events: none;
    }
    .thumb[data-drop="before"]::before { top: -8px; opacity: 1; }
    .thumb[data-drop="after"]::before { bottom: -8px; opacity: 1; }
    .thumb[data-skip] .frame { opacity: 0.35; }
    .thumb[data-skip] .frame::after {
      content: 'Skipped';
      position: absolute;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(0,0,0,0.45);
      color: #fff;
      font-size: 10px;
      font-weight: 500;
      letter-spacing: 0.04em;
    }

    .ctxmenu {
      position: fixed;
      min-width: 150px;
      padding: 4px;
      background: #242424;
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 7px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.45);
      z-index: 2147483100;
      display: none;
      font-size: 12px;
    }
    .ctxmenu[data-open] { display: block; }
    .ctxmenu button {
      display: block;
      width: 100%;
      appearance: none;
      border: 0;
      background: transparent;
      color: #e8e8e8;
      font: inherit;
      text-align: left;
      padding: 6px 10px;
      border-radius: 4px;
      cursor: pointer;
    }
    .ctxmenu button:hover:not(:disabled) { background: rgba(255,255,255,0.08); }
    .ctxmenu button:disabled { opacity: 0.35; cursor: default; }
    .ctxmenu hr {
      border: 0;
      border-top: 1px solid rgba(255,255,255,0.1);
      margin: 4px 2px;
    }

    .rail-resize {
      position: fixed;
      left: calc(var(--deck-rail-w, 188px) - 3px);
      top: 0;
      bottom: 0;
      width: 6px;
      cursor: col-resize;
      z-index: 2147482600;
      touch-action: none;
    }
    .rail-resize:hover,
    .rail-resize[data-dragging] { background: rgba(255,255,255,0.12); }
    :host([no-rail]) .rail-resize,
    :host([noscale]) .rail-resize,
    .rail[data-presenting] + .rail-resize,
    .rail[data-user-hidden] + .rail-resize { display: none; }

    /* Delete-confirm popup — matches the SPA's ConfirmDialog layout
       (title + message body, depressed footer with Cancel / Delete). */
    .confirm-backdrop {
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.45);
      z-index: 2147483200;
      display: none;
      align-items: center;
      justify-content: center;
    }
    .confirm-backdrop[data-open] { display: flex; }
    .confirm {
      width: 320px;
      max-width: calc(100vw - 32px);
      background: #2a2a2a;
      color: #e8e8e8;
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 12px;
      box-shadow: 0 12px 32px rgba(0,0,0,0.5);
      overflow: hidden;
      font-family: inherit;
      animation: deck-confirm-in 0.18s ease;
    }
    @keyframes deck-confirm-in {
      from { opacity: 0; transform: scale(0.96); }
      to { opacity: 1; transform: scale(1); }
    }
    .confirm .body { padding: 20px 20px 16px; }
    .confirm .title { font-size: 14px; font-weight: 600; margin-bottom: 4px; }
    .confirm .msg { font-size: 13px; line-height: 1.5; color: rgba(255,255,255,0.65); }
    .confirm .footer {
      padding: 14px 20px;
      background: #1f1f1f;
      border-top: 1px solid rgba(255,255,255,0.08);
      display: flex;
      justify-content: flex-end;
      gap: 8px;
    }
    .confirm button {
      appearance: none;
      font: inherit;
      font-size: 13px;
      font-weight: 500;
      padding: 8px 16px;
      border-radius: 8px;
      cursor: pointer;
    }
    .confirm .cancel {
      background: transparent;
      border: 0;
      color: rgba(255,255,255,0.8);
    }
    .confirm .cancel:hover { background: rgba(255,255,255,0.08); }
    .confirm .danger {
      background: #c96442;
      border: 1px solid rgba(0,0,0,0.15);
      color: #fff;
      box-shadow: 0 1px 3px rgba(166,50,68,0.3), 0 2px 6px rgba(166,50,68,0.18);
    }
    .confirm .danger:hover { background: #b5563a; }

    /* ── Print: one page per slide, no chrome ────────────────────────────
       The screen layout stacks every slide at inset:0 inside a scaled
       canvas; for print we want them in document flow at the authored
       design size so the browser paginates one slide per sheet. The
       @page size is set from the width/height attributes via the inline
       <style id="deck-stage-print-page"> that connectedCallback injects
       into <head> (the @page at-rule has no effect inside shadow DOM). */
    @media print {
      :host {
        position: static;
        inset: auto;
        background: none;
        overflow: visible;
        color: inherit;
      }
      .stage { position: static; display: block; }
      .canvas {
        transform: none !important;
        width: auto !important;
        height: auto !important;
        background: none;
        will-change: auto;
      }
      ::slotted(*) {
        position: relative !important;
        inset: auto !important;
        width: var(--deck-design-w) !important;
        height: var(--deck-design-h) !important;
        box-sizing: border-box !important;
        opacity: 1 !important;
        visibility: visible !important;
        pointer-events: auto;
        break-after: page;
        page-break-after: always;
        break-inside: avoid;
        overflow: hidden;
      }
      /* :last-child alone isn't enough once data-deck-skip hides the
         trailing slide(s) — the last *visible* slide still carries
         break-after:page and prints a blank sheet. _markLastVisible()
         maintains data-deck-last-visible on the last non-skipped slide. */
      ::slotted(*:last-child),
      ::slotted([data-deck-last-visible]) {
        break-after: auto;
        page-break-after: auto;
      }
      ::slotted([data-deck-skip]) { display: none !important; }
      .overlay, .rail, .rail-resize, .ctxmenu, .confirm-backdrop { display: none !important; }
    }
  `;
  class DeckStage extends HTMLElement {
    static get observedAttributes() {
      return ['width', 'height', 'noscale', 'no-rail'];
    }
    constructor() {
      super();
      this._root = this.attachShadow({
        mode: 'open'
      });
      this._index = 0;
      this._slides = [];
      this._notes = [];
      this._hideTimer = null;
      this._mouseIdleTimer = null;
      this._menuIndex = -1;
      this._onKey = this._onKey.bind(this);
      this._onResize = this._onResize.bind(this);
      this._onSlotChange = this._onSlotChange.bind(this);
      this._onMouseMove = this._onMouseMove.bind(this);
      this._onTap = this._onTap.bind(this);
      this._onMessage = this._onMessage.bind(this);
      // Capture-phase close so a click anywhere dismisses the menu, but
      // ignore clicks that land inside the menu itself — otherwise the
      // capture handler runs before the menu's own (bubble) handler and
      // clears _menuIndex out from under it.
      this._onDocClick = e => {
        if (this._menu && e.composedPath && e.composedPath().includes(this._menu)) return;
        this._closeMenu();
      };
    }
    get designWidth() {
      return parseInt(this.getAttribute('width'), 10) || DESIGN_W_DEFAULT;
    }
    get designHeight() {
      return parseInt(this.getAttribute('height'), 10) || DESIGN_H_DEFAULT;
    }
    connectedCallback() {
      // Presenter-view popup loads deckUrl?_snthumb=...#N for its prev/cur/
      // next thumbnails — the rail has no business rendering inside those
      // (wrong scale, and it offsets the stage so the thumb shows a gutter).
      if (/[?&]_snthumb=/.test(location.search)) this.setAttribute('no-rail', '');
      this._render();
      this._loadNotes();
      this._syncPrintPageRule();
      window.addEventListener('keydown', this._onKey);
      window.addEventListener('resize', this._onResize);
      window.addEventListener('mousemove', this._onMouseMove, {
        passive: true
      });
      window.addEventListener('message', this._onMessage);
      window.addEventListener('click', this._onDocClick, true);
      this.addEventListener('click', this._onTap);
      // Initial collection + layout happens via slotchange, which fires on mount.
      this._enableRail();
      // Hold the stage hidden until webfonts are ready so the first visible
      // paint has the deck's real typography — the :not(:defined) guard in
      // the page HTML only covers custom-element upgrade, not font load.
      // Capped so a 404'd font URL can't blank the deck indefinitely.
      this.setAttribute('data-fonts-pending', '');
      const reveal = () => this.removeAttribute('data-fonts-pending');
      // rAF first: fonts.ready is a pre-resolved promise until layout has
      // resolved the slotted text's font-family and pushed a FontFace into
      // 'loading'. Reading it here in connectedCallback (parse-time) would
      // settle the race in a microtask before any font fetch starts.
      requestAnimationFrame(() => {
        Promise.race([document.fonts ? document.fonts.ready : Promise.resolve(), new Promise(r => setTimeout(r, 2000))]).then(reveal, reveal);
      });
    }
    _enableRail() {
      // Idempotent — older host builds still post __omelette_rail_enabled.
      // no-rail guard keeps the observers/stylesheet walk off the cheap path
      // for presenter-popup thumbnail iframes (up to 9 per view).
      if (this._railEnabled || this.hasAttribute('no-rail')) return;
      this._railEnabled = true;
      // Per-viewer preference — restored alongside rail width. Default on;
      // only a stored '0' (from the TweaksPanel toggle) hides it.
      this._railVisible = true;
      try {
        if (localStorage.getItem('deck-stage.railVisible') === '0') this._railVisible = false;
      } catch (e) {}
      // Live thumbnail updates: watch the light-DOM slides for content
      // edits and re-clone just the affected thumb(s), debounced. Ignore
      // the data-deck-* / data-screen-label / data-om-validate attributes
      // this component itself writes so nav and skip don't trigger
      // spurious refreshes.
      const OWN_ATTRS = /^data-(deck-|screen-label$|om-validate$)/;
      this._liveDirty = new Set();
      this._liveObserver = new MutationObserver(records => {
        for (const r of records) {
          if (r.type === 'attributes' && OWN_ATTRS.test(r.attributeName || '')) continue;
          let n = r.target;
          while (n && n.parentElement !== this) n = n.parentElement;
          if (n && this._slideSet && this._slideSet.has(n)) this._liveDirty.add(n);
        }
        if (this._liveDirty.size && !this._liveTimer) {
          this._liveTimer = setTimeout(() => {
            this._liveTimer = null;
            this._liveDirty.forEach(s => this._refreshThumb(s));
            this._liveDirty.clear();
          }, 200);
        }
      });
      this._liveObserver.observe(this, {
        subtree: true,
        childList: true,
        characterData: true,
        attributes: true
      });
      // Lazy thumbnail materialization — clone the slide only when its
      // frame scrolls into (or near) the rail viewport. rootMargin gives
      // ~4 thumbs of pre-load so fast scrolling doesn't flash blanks.
      this._railObserver = new IntersectionObserver(entries => {
        entries.forEach(e => {
          if (e.isIntersecting && e.target.__deckThumb) {
            this._materialize(e.target.__deckThumb);
          }
        });
      }, {
        root: this._rail,
        rootMargin: '400px 0px'
      });
      // Tweaks typically change CSS vars / attrs OUTSIDE <deck-stage>
      // (on <html>, <body>, a wrapper div, or a <style> tag), which
      // _liveObserver can't see. Re-snapshot author CSS (constructable
      // sheet is shared by reference, so one replaceSync updates every
      // thumb shadow root) and re-sync each thumb host's attrs + custom
      // properties. In-slide DOM mutations are _liveObserver's job.
      // Debounced so slider drags don't thrash.
      this._onTweakChange = () => {
        clearTimeout(this._tweakTimer);
        this._tweakTimer = setTimeout(() => {
          this._snapshotAuthorCss();
          // One getComputedStyle for the whole batch — each
          // getPropertyValue read below reuses the same computed style
          // as long as nothing invalidates layout between thumbs.
          const cs = getComputedStyle(this);
          (this._thumbs || []).forEach(t => {
            if (t.host) this._syncThumbHostAttrs(t.host, cs);
          });
        }, 120);
      };
      window.addEventListener('tweakchange', this._onTweakChange);
      this._snapshotAuthorCss();
      // Build the rail now that it's enabled — slotchange already fired,
      // so _renderRail's early-return skipped the initial build.
      this._syncRailHidden();
      this._renderRail();
      this._fit();
    }

    /** Snapshot document stylesheets into a constructable sheet that each
     *  thumbnail's nested shadow root adopts — so author CSS styles the
     *  cloned slide content without touching this component's chrome.
     *  Cross-origin sheets throw on .cssRules — skip them. Re-callable:
     *  the existing constructable sheet is reused via replaceSync so every
     *  already-adopted shadow root picks up the fresh CSS without re-adopt. */
    _snapshotAuthorCss() {
      // :root in an adopted sheet inside a shadow root matches nothing
      // (only the document root qualifies), so author rules like
      // `:root[data-voice="modern"] .serif` never reach the clones.
      // Rewrite :root → :host and mirror <html>'s data-*/class/lang onto
      // each thumb host (see _syncThumbHostAttrs) so the same selectors
      // match inside the thumbnail's shadow tree.
      const authorCss = Array.from(document.styleSheets).map(sh => {
        try {
          return Array.from(sh.cssRules).map(r => r.cssText).join('\n');
        } catch (e) {
          return '';
        }
      }).join('\n')
      // The shadow host is featureless outside the functional :host(...)
      // form, so any compound on :root — [attr], .class, #id, :pseudo —
      // must become :host(<compound>) not :host<compound>. Same for the
      // html type selector (Tailwind class-strategy dark mode emits
      // html.dark; Pico uses html[data-theme]), which has nothing to
      // match inside the thumb's shadow tree.
      .replace(/:root((?:\[[^\]]*\]|[.#][-\w]+|:[-\w]+(?:\([^)]*\))?)+)/g, ':host($1)').replace(/:root\b/g, ':host').replace(/(^|[\s,>~+(}])html((?:\[[^\]]*\]|[.#][-\w]+|:[-\w]+(?:\([^)]*\))?)+)(?![-\w])/g, '$1:host($2)').replace(/(^|[\s,>~+(}])html(?![-\w])/g, '$1:host');
      // Every custom property the author references. _syncThumbHostAttrs
      // mirrors each one's *computed* value at <deck-stage> onto the
      // thumb host so the live value wins over the :host default above
      // regardless of which ancestor the tweak wrote to (<html>, <body>,
      // a wrapper div, or the deck-stage element itself all inherit
      // down to getComputedStyle(this)).
      this._authorVars = new Set(authorCss.match(/--[\w-]+/g) || []);
      try {
        if (!this._adoptedSheet) this._adoptedSheet = new CSSStyleSheet();
        this._adoptedSheet.replaceSync(authorCss);
      } catch (e) {
        this._adoptedSheet = null;
        this._authorCss = authorCss;
      }
    }
    _syncThumbHostAttrs(host, cs) {
      const de = document.documentElement;
      // setAttribute overwrites but can't delete — an attr removed from
      // <html> (toggleAttribute off, classList emptied) would linger on
      // the host and :host([data-*]) / :host(.foo) rules would keep
      // matching. Remove stale mirrored attrs first; iterate backward
      // because removeAttribute mutates the live NamedNodeMap.
      for (let i = host.attributes.length - 1; i >= 0; i--) {
        const n = host.attributes[i].name;
        if ((n.startsWith('data-') || n === 'class' || n === 'lang') && !de.hasAttribute(n)) {
          host.removeAttribute(n);
        }
      }
      for (const a of de.attributes) {
        if (a.name.startsWith('data-') || a.name === 'class' || a.name === 'lang') {
          host.setAttribute(a.name, a.value);
        }
      }
      // The :root→:host rewrite in _snapshotAuthorCss pins each custom
      // property to its stylesheet default on the thumb host, shadowing
      // the live value that would otherwise inherit. Tweaks can write the
      // live value on any ancestor — <html>, <body>, a wrapper div, the
      // deck-stage element — so read it as the *computed* value at
      // <deck-stage> (which sees the whole inheritance chain) rather than
      // trying to guess which element the author wrote to. Inline on the
      // host beats the :host{} rule. remove-stale covers vars dropped
      // from the stylesheet between snapshots.
      const vars = this._authorVars || new Set();
      for (let i = host.style.length - 1; i >= 0; i--) {
        const p = host.style[i];
        if (p.startsWith('--') && !vars.has(p)) host.style.removeProperty(p);
      }
      const live = cs || getComputedStyle(this);
      vars.forEach(p => {
        const v = live.getPropertyValue(p);
        if (v) host.style.setProperty(p, v.trim());else host.style.removeProperty(p);
      });
    }
    disconnectedCallback() {
      window.removeEventListener('keydown', this._onKey);
      window.removeEventListener('resize', this._onResize);
      window.removeEventListener('mousemove', this._onMouseMove);
      window.removeEventListener('message', this._onMessage);
      window.removeEventListener('click', this._onDocClick, true);
      this.removeEventListener('click', this._onTap);
      if (this._hideTimer) clearTimeout(this._hideTimer);
      if (this._mouseIdleTimer) clearTimeout(this._mouseIdleTimer);
      if (this._liveTimer) clearTimeout(this._liveTimer);
      if (this._tweakTimer) clearTimeout(this._tweakTimer);
      if (this._railAnimTimer) clearTimeout(this._railAnimTimer);
      if (this._scaleRaf) cancelAnimationFrame(this._scaleRaf);
      if (this._liveObserver) this._liveObserver.disconnect();
      if (this._railObserver) this._railObserver.disconnect();
      if (this._onTweakChange) window.removeEventListener('tweakchange', this._onTweakChange);
    }
    attributeChangedCallback() {
      if (this._canvas) {
        this._canvas.style.width = this.designWidth + 'px';
        this._canvas.style.height = this.designHeight + 'px';
        this._canvas.style.setProperty('--deck-design-w', this.designWidth + 'px');
        this._canvas.style.setProperty('--deck-design-h', this.designHeight + 'px');
        if (this._rail) {
          this._rail.style.setProperty('--deck-aspect', this.designWidth + '/' + this.designHeight);
        }
        this._fit();
        this._scaleThumbs();
        this._syncPrintPageRule();
      }
    }
    _render() {
      const style = document.createElement('style');
      style.textContent = stylesheet;
      const stage = document.createElement('div');
      stage.className = 'stage';
      const canvas = document.createElement('div');
      canvas.className = 'canvas';
      canvas.style.width = this.designWidth + 'px';
      canvas.style.height = this.designHeight + 'px';
      canvas.style.setProperty('--deck-design-w', this.designWidth + 'px');
      canvas.style.setProperty('--deck-design-h', this.designHeight + 'px');
      const slot = document.createElement('slot');
      slot.addEventListener('slotchange', this._onSlotChange);
      canvas.appendChild(slot);
      stage.appendChild(canvas);

      // Overlay: compact, solid black, with clickable controls.
      const overlay = document.createElement('div');
      overlay.className = 'overlay export-hidden';
      overlay.setAttribute('role', 'toolbar');
      overlay.setAttribute('aria-label', 'Deck controls');
      overlay.setAttribute('data-omelette-chrome', '');
      overlay.innerHTML = `
        <button class="btn prev" type="button" aria-label="Previous slide" title="Previous (←)">
          <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M10 3L5 8l5 5"/></svg>
        </button>
        <span class="count" aria-live="polite"><span class="current">1</span><span class="sep">/</span><span class="total">1</span></span>
        <button class="btn next" type="button" aria-label="Next slide" title="Next (→)">
          <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 3l5 5-5 5"/></svg>
        </button>
        <span class="divider"></span>
        <button class="btn reset" type="button" aria-label="Reset to first slide" title="Reset (R)">Reset<span class="kbd">R</span></button>
      `;
      overlay.querySelector('.prev').addEventListener('click', () => this._advance(-1, 'click'));
      overlay.querySelector('.next').addEventListener('click', () => this._advance(1, 'click'));
      overlay.querySelector('.reset').addEventListener('click', () => this._go(0, 'click'));

      // Thumbnail rail + context menu. Thumbnails are populated in
      // _renderRail() after _collectSlides().
      const rail = document.createElement('div');
      rail.className = 'rail export-hidden';
      rail.setAttribute('data-omelette-chrome', '');
      rail.style.setProperty('--deck-aspect', this.designWidth + '/' + this.designHeight);
      // Edge auto-scroll while dragging a thumb near the rail's top/bottom
      // so off-screen drop targets are reachable. Native dragover fires
      // continuously while the pointer is stationary, so a per-event nudge
      // (ramped by edge proximity) is enough — no rAF loop needed.
      rail.addEventListener('dragover', e => {
        if (this._dragFrom == null) return;
        const r = rail.getBoundingClientRect();
        const EDGE = 40;
        const dt = e.clientY - r.top;
        const db = r.bottom - e.clientY;
        if (dt < EDGE) rail.scrollTop -= Math.ceil((EDGE - dt) / 3);else if (db < EDGE) rail.scrollTop += Math.ceil((EDGE - db) / 3);
      });
      const menu = document.createElement('div');
      menu.className = 'ctxmenu export-hidden';
      menu.setAttribute('data-omelette-chrome', '');
      menu.innerHTML = `
        <button type="button" data-act="skip">Skip slide</button>
        <button type="button" data-act="up">Move up</button>
        <button type="button" data-act="down">Move down</button>
        <hr>
        <button type="button" data-act="delete">Delete slide</button>
      `;
      menu.addEventListener('click', e => {
        const act = e.target && e.target.getAttribute && e.target.getAttribute('data-act');
        if (!act) return;
        const i = this._menuIndex;
        this._closeMenu();
        if (act === 'skip') this._toggleSkip(i);else if (act === 'up') this._moveSlide(i, i - 1);else if (act === 'down') this._moveSlide(i, i + 1);else if (act === 'delete') this._openConfirm(i);
      });
      menu.addEventListener('contextmenu', e => e.preventDefault());

      // Rail resize handle — drag to set --deck-rail-w, persisted to
      // localStorage so the width survives reloads.
      const resize = document.createElement('div');
      resize.className = 'rail-resize export-hidden';
      resize.setAttribute('data-omelette-chrome', '');
      resize.addEventListener('pointerdown', e => {
        e.preventDefault();
        resize.setPointerCapture(e.pointerId);
        resize.setAttribute('data-dragging', '');
        const move = ev => this._setRailWidth(ev.clientX);
        const up = () => {
          resize.removeEventListener('pointermove', move);
          resize.removeEventListener('pointerup', up);
          resize.removeEventListener('pointercancel', up);
          resize.removeAttribute('data-dragging');
          try {
            localStorage.setItem('deck-stage.railWidth', String(this._railPx));
          } catch (err) {}
        };
        resize.addEventListener('pointermove', move);
        resize.addEventListener('pointerup', up);
        resize.addEventListener('pointercancel', up);
      });

      // Delete-confirm dialog — mirrors the SPA's ConfirmDialog layout.
      const confirm = document.createElement('div');
      confirm.className = 'confirm-backdrop export-hidden';
      confirm.setAttribute('data-omelette-chrome', '');
      confirm.innerHTML = `
        <div class="confirm" role="dialog" aria-modal="true">
          <div class="body">
            <div class="title">Delete slide?</div>
            <div class="msg">This slide will be removed from the deck.</div>
          </div>
          <div class="footer">
            <button type="button" class="cancel">Cancel</button>
            <button type="button" class="danger">Delete</button>
          </div>
        </div>
      `;
      confirm.addEventListener('click', e => {
        if (e.target === confirm) this._closeConfirm();
      });
      confirm.querySelector('.cancel').addEventListener('click', () => this._closeConfirm());
      confirm.querySelector('.danger').addEventListener('click', () => {
        const i = this._confirmIndex;
        this._closeConfirm();
        this._deleteSlide(i);
      });
      this._root.append(style, rail, resize, stage, overlay, menu, confirm);
      this._canvas = canvas;
      this._stage = stage;
      this._slot = slot;
      this._overlay = overlay;
      this._rail = rail;
      this._resize = resize;
      this._menu = menu;
      this._confirm = confirm;
      this._countEl = overlay.querySelector('.current');
      this._totalEl = overlay.querySelector('.total');

      // Restore persisted rail width.
      let rw = 188;
      try {
        const s = localStorage.getItem('deck-stage.railWidth');
        if (s) rw = parseInt(s, 10) || rw;
      } catch (err) {}
      this._setRailWidth(rw);
      this._syncRailHidden();
    }
    _setRailWidth(px) {
      const w = Math.max(120, Math.min(360, Math.round(px)));
      this._railPx = w;
      this.style.setProperty('--deck-rail-w', w + 'px');
      this._fit();
      // _scaleThumbs forces a sync layout (frame.offsetWidth) then writes
      // N transforms. During a resize drag this runs per-pointermove;
      // coalesce to one per frame.
      if (!this._scaleRaf) {
        this._scaleRaf = requestAnimationFrame(() => {
          this._scaleRaf = null;
          this._scaleThumbs();
        });
      }
    }

    /** @page must live in the document stylesheet — it's a no-op inside
     *  shadow DOM. Inject/update a single <head> style tag so the print
     *  sheet matches the design size and Save-as-PDF yields one slide per
     *  page with no margins. */
    _syncPrintPageRule() {
      const id = 'deck-stage-print-page';
      let tag = document.getElementById(id);
      if (!tag) {
        tag = document.createElement('style');
        tag.id = id;
        document.head.appendChild(tag);
      }
      tag.textContent = '@page { size: ' + this.designWidth + 'px ' + this.designHeight + 'px; margin: 0; } ' + '@media print { html, body { margin: 0 !important; padding: 0 !important; background: none !important; overflow: visible !important; height: auto !important; } ' + '* { -webkit-print-color-adjust: exact; print-color-adjust: exact; } }';
    }
    _onSlotChange() {
      // Rail mutations (delete/move) already reconcile synchronously and
      // emit slidechange with reason 'api'; skip the async slotchange that
      // would otherwise re-broadcast with reason 'init'.
      if (this._squelchSlotChange) {
        this._squelchSlotChange = false;
        return;
      }
      this._collectSlides();
      this._restoreIndex();
      this._applyIndex({
        showOverlay: false,
        broadcast: true,
        reason: 'init'
      });
      this._fit();
    }
    _collectSlides() {
      const assigned = this._slot.assignedElements({
        flatten: true
      });
      this._slides = assigned.filter(el => {
        // Skip template/style/script nodes even if someone slots them.
        const tag = el.tagName;
        return tag !== 'TEMPLATE' && tag !== 'SCRIPT' && tag !== 'STYLE';
      });
      this._slideSet = new Set(this._slides);
      this._slides.forEach((slide, i) => {
        const n = i + 1;
        slide.setAttribute('data-screen-label', `${pad2(n)} ${getSlideLabel(slide)}`);

        // Validation attribute for comment flow / auto-checks.
        if (!slide.hasAttribute('data-om-validate')) {
          slide.setAttribute('data-om-validate', VALIDATE_ATTR);
        }
        slide.setAttribute('data-deck-slide', String(i));
      });
      if (this._totalEl) this._totalEl.textContent = String(this._slides.length || 1);
      if (this._index >= this._slides.length) this._index = Math.max(0, this._slides.length - 1);
      this._markLastVisible();
      this._renderRail();
    }

    /** Tag the last non-skipped slide so print CSS can drop its
     *  break-after (see the @media print comment above — :last-child
     *  alone matches a hidden skipped slide). */
    _markLastVisible() {
      let last = null;
      this._slides.forEach(s => {
        s.removeAttribute('data-deck-last-visible');
        if (!s.hasAttribute('data-deck-skip')) last = s;
      });
      if (last) last.setAttribute('data-deck-last-visible', '');
    }
    _loadNotes() {
      const tag = document.getElementById('speaker-notes');
      if (!tag) {
        this._notes = [];
        return;
      }
      try {
        const parsed = JSON.parse(tag.textContent || '[]');
        if (Array.isArray(parsed)) this._notes = parsed;
      } catch (e) {
        console.warn('[deck-stage] Failed to parse #speaker-notes JSON:', e);
        this._notes = [];
      }
    }
    _restoreIndex() {
      // The host's ?slide= param is delivered as a #<int> hash (1-indexed) on
      // the iframe src. No hash → slide 1; the deck itself keeps no position
      // state across loads.
      const h = (location.hash || '').match(/^#(\d+)$/);
      if (h) {
        const n = parseInt(h[1], 10) - 1;
        if (n >= 0 && n < this._slides.length) this._index = n;
      }
    }
    _applyIndex({
      showOverlay = true,
      broadcast = true,
      reason = 'init'
    } = {}) {
      if (!this._slides.length) return;
      const prev = this._prevIndex == null ? -1 : this._prevIndex;
      const curr = this._index;
      // Keep the iframe's own hash in sync so an in-iframe location.reload()
      // (reload banner path in viewer-handle.ts) lands on the current slide,
      // not the stale deep-link hash from initial load.
      try {
        history.replaceState(null, '', '#' + (curr + 1));
      } catch (e) {}
      this._slides.forEach((s, i) => {
        if (i === curr) s.setAttribute('data-deck-active', '');else s.removeAttribute('data-deck-active');
      });
      if (this._countEl) this._countEl.textContent = String(curr + 1);
      // Follow-scroll on every navigation (init deep-link, keyboard, click,
      // tap, external goTo) — the only time we *don't* want the rail to
      // track current is after a rail-internal mutation, where _renderRail
      // has already restored the user's scroll position and yanking back to
      // current would undo it.
      this._syncRail(reason !== 'mutation');
      if (broadcast) {
        // (1) Legacy: host-window postMessage for speaker-notes renderers.
        try {
          window.postMessage({
            slideIndexChanged: curr,
            deckTotal: this._slides.length,
            deckSkipped: this._skippedIndices()
          }, '*');
        } catch (e) {}

        // (2) In-page CustomEvent on the <deck-stage> element itself.
        //     Bubbles and composes out of shadow DOM so slide code can listen:
        //       document.querySelector('deck-stage').addEventListener('slidechange', e => {
        //         e.detail.index, e.detail.previousIndex, e.detail.total, e.detail.slide, e.detail.reason
        //       });
        const detail = {
          index: curr,
          previousIndex: prev,
          total: this._slides.length,
          slide: this._slides[curr] || null,
          previousSlide: prev >= 0 ? this._slides[prev] || null : null,
          reason: reason // 'init' | 'keyboard' | 'click' | 'tap' | 'api'
        };
        this.dispatchEvent(new CustomEvent('slidechange', {
          detail,
          bubbles: true,
          composed: true
        }));
      }
      this._prevIndex = curr;
      if (showOverlay) this._flashOverlay();
    }
    _flashOverlay() {
      // Host posts __omelette_presenting while in fullscreen/tab presentation
      // mode — suppress the nav footer entirely (both hover and slide-change
      // flash) so the audience sees clean slides.
      if (!this._overlay || this._presenting) return;
      this._overlay.setAttribute('data-visible', '');
      if (this._hideTimer) clearTimeout(this._hideTimer);
      this._hideTimer = setTimeout(() => {
        this._overlay.removeAttribute('data-visible');
      }, OVERLAY_HIDE_MS);
    }
    _railWidth() {
      // State-based, no offsetWidth: the first _fit() can run before the
      // rail has had layout on some load paths, and a 0 there paints the
      // slide full-width for one frame before the post-slotchange _fit()
      // corrects it.
      if (!this._railEnabled || !this._railVisible || this.hasAttribute('no-rail') || this.hasAttribute('noscale') || this._presenting || this._previewMode || NARROW_MQ.matches) return 0;
      return this._railPx || 0;
    }
    _fit() {
      if (!this._canvas) return;
      const stage = this._canvas.parentElement;
      // PPTX export sets noscale so the DOM capture sees authored-size
      // geometry — the scaled canvas is in shadow DOM, so the exporter's
      // resetTransformSelector can't reach .canvas.style.transform directly.
      if (this.hasAttribute('noscale')) {
        this._canvas.style.transform = 'none';
        if (stage) stage.style.left = '0';
        if (this._overlay) this._overlay.style.marginLeft = '0';
        return;
      }
      const rw = this._railWidth();
      if (stage) stage.style.left = rw + 'px';
      // Overlay is centred on the viewport via left:50% + translate(-50%);
      // marginLeft shifts the centre by rw/2 so it lands in the middle of
      // the [rw, innerWidth] stage region.
      if (this._overlay) this._overlay.style.marginLeft = rw / 2 + 'px';
      const vw = window.innerWidth - rw;
      const vh = window.innerHeight;
      const s = Math.min(vw / this.designWidth, vh / this.designHeight);
      this._canvas.style.transform = `scale(${s})`;
    }
    _onResize() {
      this._fit();
      // Crossing the narrow-viewport breakpoint reveals the rail — rerun the
      // thumbnail scale the same way _setRailWidth does.
      if (!this._scaleRaf) {
        this._scaleRaf = requestAnimationFrame(() => {
          this._scaleRaf = null;
          this._scaleThumbs();
        });
      }
    }
    _onMouseMove() {
      // Keep overlay visible while mouse moves; hide after idle.
      this._flashOverlay();
    }
    _onMessage(e) {
      const d = e.data;
      if (d && typeof d.__omelette_presenting === 'boolean') {
        this._presenting = d.__omelette_presenting;
        if (this._presenting && this._overlay) {
          this._overlay.removeAttribute('data-visible');
          if (this._hideTimer) clearTimeout(this._hideTimer);
        }
        this._syncRailHidden();
        this._closeMenu();
        this._closeConfirm();
        this._fit();
        this._scaleThumbs();
      }
      // Host's Preview segment (ViewerMode='none'): the rail's drag-reorder /
      // right-click skip-delete affordances are editing chrome, so hide it
      // while the user is just looking at the deck. Same hard-hide path as
      // presenting; independent of the user's _railVisible preference so
      // returning to Edit restores whatever they had.
      if (d && typeof d.__omelette_preview_mode === 'boolean') {
        if (d.__omelette_preview_mode === this._previewMode) return;
        this._previewMode = d.__omelette_preview_mode;
        this._syncRailHidden();
        this._closeMenu();
        this._closeConfirm();
        this._fit();
        this._scaleThumbs();
      }
      // Per-viewer show/hide, driven by the TweaksPanel's auto-injected
      // "Thumbnail rail" toggle (or any author script). Independent of
      // whether the Tweaks panel itself is open — closing the panel
      // doesn't change rail visibility. Persists alongside rail width.
      if (d && d.type === '__deck_rail_visible' && typeof d.on === 'boolean') {
        if (d.on === this._railVisible) return;
        this._railVisible = d.on;
        try {
          localStorage.setItem('deck-stage.railVisible', d.on ? '1' : '0');
        } catch (e) {}
        // Arm the transition, commit it, then flip state — otherwise the
        // browser coalesces both writes and nothing animates on show.
        this.setAttribute('data-rail-anim', '');
        void (this._rail && this._rail.offsetHeight);
        this._syncRailHidden();
        this._fit();
        this._scaleThumbs();
        clearTimeout(this._railAnimTimer);
        this._railAnimTimer = setTimeout(() => this.removeAttribute('data-rail-anim'), 220);
      }
      if (d && d.type === '__omelette_rail_enabled') this._enableRail();
    }
    _syncRailHidden() {
      if (!this._rail) return;
      // data-presenting is the hard hide (display:none) for flag-off,
      // presentation mode, and the host's Preview segment — instant, no
      // transition. data-user-hidden is the soft hide (translateX(-100%))
      // for the viewer's rail toggle, so show/hide slides under
      // :host([data-rail-anim]).
      const hard = !this._railEnabled || this._presenting || this._previewMode;
      if (hard) this._rail.setAttribute('data-presenting', '');else this._rail.removeAttribute('data-presenting');
      if (!this._railVisible) this._rail.setAttribute('data-user-hidden', '');else this._rail.removeAttribute('data-user-hidden');
      // translateX hide leaves thumbs (tabIndex=0) in the tab order —
      // inert keeps them unfocusable while the rail is off-screen.
      this._rail.inert = hard || !this._railVisible;
    }
    _onTap(e) {
      // Touch-only — keyboard + the overlay toolbar cover nav on desktop.
      if (FINE_POINTER_MQ.matches) return;
      // Only taps that land on the stage (slide content or letterbox); the
      // overlay / rail / menus are siblings with their own click handlers.
      const path = e.composedPath();
      if (!this._stage || !path.includes(this._stage)) return;
      // Let interactive slide content keep the tap. composedPath (not
      // e.target.closest) so we see through open shadow roots — a <button>
      // inside a slide-authored custom element retargets e.target to the
      // host but still appears in the composed path.
      if (e.defaultPrevented) return;
      for (const n of path) {
        if (n === this._stage) break;
        if (n.matches && n.matches(INTERACTIVE_SEL)) return;
      }
      e.preventDefault();
      const rw = this._railWidth();
      const mid = rw + (window.innerWidth - rw) / 2;
      this._advance(e.clientX < mid ? -1 : 1, 'tap');
    }
    _onKey(e) {
      // Ignore when the user is typing.
      const t = e.target;
      if (t && (t.isContentEditable || /^(INPUT|TEXTAREA|SELECT)$/.test(t.tagName))) return;
      // Confirm dialog swallows nav keys while open; Escape cancels. Enter
      // is left to the focused button's native activation so Tab→Cancel
      // →Enter activates Cancel, not the window-level confirm path.
      if (this._confirm && this._confirm.hasAttribute('data-open')) {
        if (e.key === 'Escape') {
          this._closeConfirm();
          e.preventDefault();
        }
        return;
      }
      if (e.key === 'Escape' && this._menu && this._menu.hasAttribute('data-open')) {
        this._closeMenu();
        e.preventDefault();
        return;
      }
      if (e.metaKey || e.ctrlKey || e.altKey) return;
      const key = e.key;
      let handled = true;
      if (key === 'ArrowRight' || key === 'PageDown' || key === ' ' || key === 'Spacebar') {
        this._advance(1, 'keyboard');
      } else if (key === 'ArrowLeft' || key === 'PageUp') {
        this._advance(-1, 'keyboard');
      } else if (key === 'Home') {
        this._go(0, 'keyboard');
      } else if (key === 'End') {
        this._go(this._slides.length - 1, 'keyboard');
      } else if (key === 'r' || key === 'R') {
        this._go(0, 'keyboard');
      } else if (/^[0-9]$/.test(key)) {
        // 1..9 jump to that slide; 0 jumps to 10.
        const n = key === '0' ? 9 : parseInt(key, 10) - 1;
        if (n < this._slides.length) this._go(n, 'keyboard');
      } else {
        handled = false;
      }
      if (handled) {
        e.preventDefault();
        this._flashOverlay();
      }
    }
    _go(i, reason = 'api') {
      if (!this._slides.length) return;
      const clamped = Math.max(0, Math.min(this._slides.length - 1, i));
      if (clamped === this._index) {
        this._flashOverlay();
        return;
      }
      this._index = clamped;
      this._applyIndex({
        showOverlay: true,
        broadcast: true,
        reason
      });
    }

    /** Step forward/back skipping any slide marked data-deck-skip. Falls
     *  back to _go's clamp-at-ends behaviour (flash overlay) when there's
     *  nothing further in that direction. */
    _advance(dir, reason) {
      if (!this._slides.length) return;
      let i = this._index + dir;
      while (i >= 0 && i < this._slides.length && this._slides[i].hasAttribute('data-deck-skip')) {
        i += dir;
      }
      if (i < 0 || i >= this._slides.length) {
        this._flashOverlay();
        return;
      }
      this._go(i, reason);
    }

    // ── Thumbnail rail ────────────────────────────────────────────────────
    //
    // Thumbs are keyed by slide element and reused across _renderRail()
    // calls, so a reorder/delete is an O(changed) DOM shuffle instead of an
    // O(N) teardown-and-re-clone. Each thumb starts as a lightweight shell
    // (num + empty frame); the clone is materialized lazily by an
    // IntersectionObserver when the frame scrolls into (or near) view, so
    // only visible-ish slides pay the clone + image-decode cost.

    _renderRail() {
      if (!this._rail || !this._railEnabled) {
        this._thumbs = [];
        return;
      }
      // FLIP: record each *materialized* thumb's top before the reconcile.
      // Off-screen (non-materialized) thumbs don't need the animation and
      // skipping their getBoundingClientRect saves a forced layout per
      // off-screen thumb on large decks.
      const prevTops = new Map();
      (this._thumbs || []).forEach(({
        thumb,
        slide,
        host
      }) => {
        if (host) prevTops.set(slide, thumb.getBoundingClientRect().top);
      });
      const st = this._rail.scrollTop;

      // Reconcile: reuse thumbs that already exist for a slide, create
      // shells for new slides, drop thumbs for removed slides.
      const bySlide = new Map();
      (this._thumbs || []).forEach(t => bySlide.set(t.slide, t));
      const next = [];
      this._slides.forEach(slide => {
        let t = bySlide.get(slide);
        if (t) bySlide.delete(slide);else t = this._makeThumb(slide);
        next.push(t);
      });
      // Orphans — slides removed since last render.
      bySlide.forEach(t => {
        if (this._railObserver) this._railObserver.unobserve(t.frame);
        t.thumb.remove();
      });
      // Put thumbs into document order to match _slides. insertBefore on
      // an already-correctly-placed node is a no-op, so this is cheap
      // when nothing moved.
      next.forEach((t, i) => {
        const want = t.thumb;
        const at = this._rail.children[i];
        if (at !== want) this._rail.insertBefore(want, at || null);
        t.i = i;
        t.num.textContent = String(i + 1);
        if (t.slide.hasAttribute('data-deck-skip')) t.thumb.setAttribute('data-skip', '');else t.thumb.removeAttribute('data-skip');
      });
      this._thumbs = next;
      this._rail.scrollTop = st;
      if (prevTops.size) {
        const moved = [];
        this._thumbs.forEach(({
          thumb,
          slide
        }) => {
          const old = prevTops.get(slide);
          if (old == null) return;
          const dy = old - thumb.getBoundingClientRect().top;
          if (Math.abs(dy) < 1) return;
          thumb.style.transition = 'none';
          thumb.style.transform = `translateY(${dy}px)`;
          moved.push(thumb);
        });
        if (moved.length) {
          // Commit the inverted positions before flipping the transition
          // on — otherwise the browser coalesces both style writes and
          // nothing animates.
          void this._rail.offsetHeight;
          moved.forEach(t => {
            t.style.transition = 'transform 180ms cubic-bezier(.2,.7,.3,1)';
            t.style.transform = '';
          });
          setTimeout(() => moved.forEach(t => {
            t.style.transition = '';
          }), 220);
        }
      }
      requestAnimationFrame(() => this._scaleThumbs());
      this._syncRail(false);
    }

    /** Create a lightweight thumb shell for one slide. The clone is
     *  materialized later by the IntersectionObserver. Event handlers
     *  look up the thumb's *current* index (via _thumbs.indexOf) so the
     *  same element can be reused across reorders. */
    _makeThumb(slide) {
      const thumb = document.createElement('div');
      thumb.className = 'thumb';
      thumb.tabIndex = 0;
      const num = document.createElement('div');
      num.className = 'num';
      const frame = document.createElement('div');
      frame.className = 'frame';
      thumb.append(num, frame);
      const entry = {
        thumb,
        num,
        frame,
        slide,
        clone: null,
        host: null,
        i: -1
      };
      // entry.i is refreshed on every _renderRail reconcile pass, so
      // handlers read the thumb's current position without an O(N) scan.
      const idx = () => entry.i;
      thumb.addEventListener('click', () => this._go(idx(), 'click'));
      // ↑/↓ step through the rail when a thumb has focus. _go clamps at the
      // ends and _applyIndex→_syncRail scrolls the new current thumb into
      // view; we move focus to it (preventScroll — _syncRail already
      // scrolled) so a held key walks the whole list. stopPropagation keeps
      // this out of the window-level _onKey nav handler.
      thumb.addEventListener('keydown', e => {
        if (e.key !== 'ArrowUp' && e.key !== 'ArrowDown') return;
        if (e.metaKey || e.ctrlKey || e.altKey) return;
        e.preventDefault();
        e.stopPropagation();
        this._go(idx() + (e.key === 'ArrowDown' ? 1 : -1), 'keyboard');
        const cur = this._thumbs && this._thumbs[this._index];
        if (cur) cur.thumb.focus({
          preventScroll: true
        });
      });
      thumb.addEventListener('contextmenu', e => {
        e.preventDefault();
        this._openMenu(idx(), e.clientX, e.clientY);
      });
      thumb.draggable = true;
      thumb.addEventListener('dragstart', e => {
        this._dragFrom = idx();
        thumb.setAttribute('data-dragging', '');
        e.dataTransfer.effectAllowed = 'move';
        try {
          e.dataTransfer.setData('text/plain', String(this._dragFrom));
        } catch (err) {}
      });
      thumb.addEventListener('dragend', () => {
        thumb.removeAttribute('data-dragging');
        this._clearDrop();
        this._dragFrom = null;
      });
      thumb.addEventListener('dragover', e => {
        if (this._dragFrom == null) return;
        e.preventDefault();
        e.dataTransfer.dropEffect = 'move';
        const r = thumb.getBoundingClientRect();
        this._setDrop(idx(), e.clientY < r.top + r.height / 2 ? 'before' : 'after');
      });
      thumb.addEventListener('drop', e => {
        if (this._dragFrom == null) return;
        e.preventDefault();
        const i = idx();
        const r = thumb.getBoundingClientRect();
        let to = e.clientY >= r.top + r.height / 2 ? i + 1 : i;
        if (this._dragFrom < to) to--;
        const from = this._dragFrom;
        this._clearDrop();
        this._dragFrom = null;
        if (to !== from) this._moveSlide(from, to);
      });
      if (this._railObserver) this._railObserver.observe(frame);
      frame.__deckThumb = entry;
      return entry;
    }

    /** Lazily build the clone for a thumb that has scrolled into view. */
    _materialize(entry) {
      if (entry.host) return;
      const dw = this.designWidth,
        dh = this.designHeight;
      let clone = entry.slide.cloneNode(true);
      clone.removeAttribute('id');
      clone.removeAttribute('data-deck-active');
      clone.querySelectorAll('[id]').forEach(el => el.removeAttribute('id'));
      // Neuter heavy media; replace <video> with its poster so the box
      // keeps a visual. <iframe>/<audio> become empty placeholders.
      clone.querySelectorAll('iframe, audio, object, embed').forEach(el => {
        el.removeAttribute('src');
        el.removeAttribute('srcdoc');
        el.removeAttribute('data');
        el.innerHTML = '';
      });
      clone.querySelectorAll('video').forEach(el => {
        if (!el.poster) {
          el.removeAttribute('src');
          el.innerHTML = '';
          return;
        }
        const img = document.createElement('img');
        img.src = el.poster;
        img.alt = '';
        img.style.cssText = el.style.cssText + ';object-fit:cover;width:100%;height:100%;';
        img.className = el.className;
        el.replaceWith(img);
      });
      // Images: defer decode and let the browser pick the smallest
      // srcset candidate for the ~140px thumb. Same-URL clones reuse the
      // slide's decoded bitmap (URL-keyed cache), so the remaining cost
      // is paint/composite — lazy+async keeps that off the main thread.
      clone.querySelectorAll('img').forEach(el => {
        el.loading = 'lazy';
        el.decoding = 'async';
        if (el.srcset) el.sizes = (this._railPx || 188) + 'px';
      });
      // Custom elements inside the slide would have their
      // connectedCallback fire when the clone is appended. Replace them
      // with inert boxes so a component-heavy deck doesn't run N copies
      // of each component's mount logic in the rail. Children are
      // preserved so layout-wrapper elements (<my-column><h2>…</h2>)
      // still show their authored content; the querySelectorAll NodeList
      // is static, so nested custom elements in the moved subtree are
      // still visited on later iterations.
      const neuter = el => {
        const box = document.createElement('div');
        box.style.cssText = (el.getAttribute('style') || '') + ';background:rgba(0,0,0,0.06);border:1px dashed rgba(0,0,0,0.15);';
        box.className = el.className;
        // Preserve theming/i18n hooks so [data-*] / :lang() / [dir]
        // descendant selectors still match the neutered root.
        for (const a of el.attributes) {
          const n = a.name;
          if (n.startsWith('data-') || n.startsWith('aria-') || n === 'lang' || n === 'dir' || n === 'role' || n === 'title') {
            box.setAttribute(n, a.value);
          }
        }
        while (el.firstChild) box.appendChild(el.firstChild);
        return box;
      };
      // querySelectorAll('*') returns descendants only — a custom-element
      // slide root (<my-slide>…</my-slide>) would slip through and upgrade
      // on append. Swap the root first.
      if (clone.tagName.includes('-')) clone = neuter(clone);
      clone.querySelectorAll('*').forEach(el => {
        if (el.tagName.includes('-')) el.replaceWith(neuter(el));
      });
      clone.style.cssText += ';position:absolute;top:0;left:0;transform-origin:0 0;' + 'pointer-events:none;width:' + dw + 'px;height:' + dh + 'px;' + 'box-sizing:border-box;overflow:hidden;visibility:visible;opacity:1;';
      const host = document.createElement('div');
      host.style.cssText = 'position:absolute;inset:0;';
      this._syncThumbHostAttrs(host);
      const sr = host.attachShadow({
        mode: 'open'
      });
      if (this._adoptedSheet) sr.adoptedStyleSheets = [this._adoptedSheet];else {
        const st = document.createElement('style');
        st.textContent = this._authorCss || '';
        sr.appendChild(st);
      }
      sr.appendChild(clone);
      entry.frame.appendChild(host);
      entry.host = host;
      entry.clone = clone;
      if (this._thumbScale) clone.style.transform = 'scale(' + this._thumbScale + ')';
      // Once materialized the IO callback is a no-op early-return —
      // unobserve so scroll doesn't keep firing it.
      if (this._railObserver) this._railObserver.unobserve(entry.frame);
    }

    /** Re-clone a single thumb (live-update path). No-op if the thumb
     *  hasn't been materialized yet — it'll pick up current content when
     *  it scrolls into view. */
    _refreshThumb(slide) {
      const entry = (this._thumbs || []).find(t => t.slide === slide);
      if (!entry || !entry.host) return;
      entry.host.remove();
      entry.host = entry.clone = null;
      this._materialize(entry);
    }
    _scaleThumbs() {
      if (!this._thumbs || !this._thumbs.length) return;
      // Every frame is the same width; if it reads 0 the rail is
      // display:none (noscale / no-rail / presenting / print) — leave the
      // clones as-is and re-run when the rail is revealed.
      const fw = this._thumbs[0].frame.offsetWidth;
      if (!fw) return;
      this._thumbScale = fw / this.designWidth;
      this._thumbs.forEach(({
        clone
      }) => {
        if (clone) clone.style.transform = 'scale(' + this._thumbScale + ')';
      });
    }
    _setDrop(i, where) {
      // dragover fires at pointer-event rate; touch only the previous
      // and new target rather than sweeping all N thumbs.
      const t = this._thumbs && this._thumbs[i];
      if (this._dropOn && this._dropOn !== t) {
        this._dropOn.thumb.removeAttribute('data-drop');
      }
      if (t) t.thumb.setAttribute('data-drop', where);
      this._dropOn = t || null;
    }
    _clearDrop() {
      if (this._dropOn) this._dropOn.thumb.removeAttribute('data-drop');
      this._dropOn = null;
    }
    _syncRail(follow) {
      if (!this._thumbs) return;
      this._thumbs.forEach(({
        thumb
      }, i) => {
        if (i === this._index) {
          thumb.setAttribute('data-current', '');
          if (follow && typeof thumb.scrollIntoView === 'function') {
            thumb.scrollIntoView({
              block: 'nearest'
            });
          }
        } else {
          thumb.removeAttribute('data-current');
        }
      });
    }
    _openMenu(i, x, y) {
      if (!this._menu) return;
      this._menuIndex = i;
      const slide = this._slides[i];
      const skip = slide && slide.hasAttribute('data-deck-skip');
      this._menu.querySelector('[data-act="skip"]').textContent = skip ? 'Unskip slide' : 'Skip slide';
      this._menu.querySelector('[data-act="up"]').disabled = i <= 0;
      this._menu.querySelector('[data-act="down"]').disabled = i >= this._slides.length - 1;
      this._menu.querySelector('[data-act="delete"]').disabled = this._slides.length <= 1;
      // Place, then clamp to viewport after it's measurable.
      this._menu.style.left = x + 'px';
      this._menu.style.top = y + 'px';
      this._menu.setAttribute('data-open', '');
      const r = this._menu.getBoundingClientRect();
      const nx = Math.min(x, window.innerWidth - r.width - 4);
      const ny = Math.min(y, window.innerHeight - r.height - 4);
      this._menu.style.left = Math.max(4, nx) + 'px';
      this._menu.style.top = Math.max(4, ny) + 'px';
    }
    _closeMenu() {
      if (this._menu) this._menu.removeAttribute('data-open');
      this._menuIndex = -1;
    }
    _openConfirm(i) {
      if (!this._confirm) return;
      this._confirmIndex = i;
      this._confirm.querySelector('.title').textContent = 'Delete slide ' + (i + 1) + '?';
      this._confirm.setAttribute('data-open', '');
      const btn = this._confirm.querySelector('.danger');
      if (btn && btn.focus) btn.focus();
    }
    _closeConfirm() {
      if (this._confirm) this._confirm.removeAttribute('data-open');
      this._confirmIndex = -1;
    }
    _emitDeckChange(detail) {
      this.dispatchEvent(new CustomEvent('deckchange', {
        detail,
        bubbles: true,
        composed: true
      }));
    }
    _deleteSlide(i) {
      const slide = this._slides[i];
      if (!slide || this._slides.length <= 1) return;
      const wasCurrent = i === this._index;
      if (i < this._index || wasCurrent && i === this._slides.length - 1) this._index--;
      this._squelchSlotChange = true;
      slide.remove();
      this._emitDeckChange({
        action: 'delete',
        from: i,
        slide
      });
      this._collectSlides();
      this._applyIndex({
        showOverlay: true,
        broadcast: true,
        reason: 'mutation'
      });
    }
    _toggleSkip(i) {
      const slide = this._slides[i];
      if (!slide) return;
      const on = !slide.hasAttribute('data-deck-skip');
      if (on) slide.setAttribute('data-deck-skip', '');else slide.removeAttribute('data-deck-skip');
      if (this._thumbs && this._thumbs[i]) {
        if (on) this._thumbs[i].thumb.setAttribute('data-skip', '');else this._thumbs[i].thumb.removeAttribute('data-skip');
      }
      this._markLastVisible();
      this._emitDeckChange({
        action: on ? 'skip' : 'unskip',
        from: i,
        slide
      });
      // Re-broadcast so the presenter popup's prev/next thumbnails re-pick
      // the nearest non-skipped slide without waiting for a nav event.
      try {
        window.postMessage({
          slideIndexChanged: this._index,
          deckTotal: this._slides.length,
          deckSkipped: this._skippedIndices()
        }, '*');
      } catch (e) {}
    }
    _skippedIndices() {
      const out = [];
      for (let i = 0; i < this._slides.length; i++) {
        if (this._slides[i].hasAttribute('data-deck-skip')) out.push(i);
      }
      return out;
    }
    _moveSlide(i, j) {
      if (j < 0 || j >= this._slides.length || j === i) return;
      const slide = this._slides[i];
      const ref = j < i ? this._slides[j] : this._slides[j].nextSibling;
      // Track the active slide across the reorder so the same content
      // stays on screen.
      const cur = this._index;
      if (cur === i) this._index = j;else if (i < cur && j >= cur) this._index = cur - 1;else if (i > cur && j <= cur) this._index = cur + 1;
      this._squelchSlotChange = true;
      this.insertBefore(slide, ref);
      this._emitDeckChange({
        action: 'move',
        from: i,
        to: j,
        slide
      });
      this._collectSlides();
      this._applyIndex({
        showOverlay: false,
        broadcast: true,
        reason: 'mutation'
      });
    }

    // Public API ------------------------------------------------------------

    /** Current slide index (0-based). */
    get index() {
      return this._index;
    }
    /** Total slide count. */
    get length() {
      return this._slides.length;
    }
    /** Programmatically navigate. */
    goTo(i) {
      this._go(i, 'api');
    }
    next() {
      this._advance(1, 'api');
    }
    prev() {
      this._advance(-1, 'api');
    }
    reset() {
      this._go(0, 'api');
    }
  }
  if (!customElements.get('deck-stage')) {
    customElements.define('deck-stage', DeckStage);
  }
})();
})(); } catch (e) { __ds_ns.__errors.push({ path: "ANONA_investor_deck/deck-stage.js", error: String((e && e.message) || e) }); }

// TENET_product_deck/deck-stage.js
try { (() => {
/**
 * <deck-stage> — reusable web component for HTML decks.
 *
 * Handles:
 *  (a) speaker notes — reads <script type="application/json" id="speaker-notes">
 *      and posts {slideIndexChanged: N} to the parent window on nav.
 *  (b) keyboard navigation — ←/→, PgUp/PgDn, Space, Home/End, number keys.
 *      On touch devices, tapping the left/right half of the stage goes
 *      prev/next — taps on links, buttons and other interactive slide
 *      content are left alone.
 *  (c) press R to reset to slide 0 (with a tasteful keyboard hint).
 *  (d) bottom-center overlay showing slide count + hints, fades out on idle.
 *  (e) auto-scaling — inner canvas is a fixed design size (default 1920×1080)
 *      scaled with `transform: scale()` to fit the viewport, letterboxed.
 *      Set the `noscale` attribute to render at authored size (1:1) — the
 *      PPTX exporter sets this so its DOM capture sees unscaled geometry.
 *  (f) print — `@media print` lays every slide out as its own page at the
 *      design size, so the browser's Print → Save as PDF produces a clean
 *      one-page-per-slide PDF with no extra setup.
 *  (g) thumbnail rail — resizable left-hand column of per-slide thumbnails
 *      (static clones). Click to navigate; ↑/↓ with a thumbnail focused to
 *      step between slides; drag to reorder; right-click for
 *      Skip / Move up / Move down / Delete (opens a Cancel/Delete confirm
 *      dialog). Drag the rail's right edge to resize; width persists to
 *      localStorage. Skipped slides carry `data-deck-skip`, are dimmed in
 *      the rail, omitted from prev/next navigation, and hidden at print.
 *      The rail is suppressed in presenting mode, in the host's Preview
 *      mode (ViewerMode='none'), on `noscale`, on narrow viewports
 *      (≤640px), and via the `no-rail` attribute. Rail mutations dispatch
 *      a `deckchange`
 *      CustomEvent on the element: detail = {action, from, to, slide}.
 *
 * Slides are HIDDEN, not unmounted. Non-active slides stay in the DOM with
 * `visibility: hidden` + `opacity: 0`, so their state (videos, iframes,
 * form inputs, React trees) is preserved across navigation.
 *
 * Lifecycle event — the component dispatches a `slidechange` CustomEvent on
 * itself whenever the active slide changes (including the initial mount).
 * The event bubbles and composes out of shadow DOM, so you can listen on
 * the <deck-stage> element or on document:
 *
 *   document.querySelector('deck-stage').addEventListener('slidechange', (e) => {
 *     e.detail.index         // new 0-based index
 *     e.detail.previousIndex // previous index, or -1 on init
 *     e.detail.total         // total slide count
 *     e.detail.slide         // the new active slide element
 *     e.detail.previousSlide // the prior slide element, or null on init
 *     e.detail.reason        // 'init' | 'keyboard' | 'click' | 'tap' | 'api'
 *   });
 *
 * Persistence: none at the deck level. The host app keeps the current slide
 * in its own URL (?slide=) and re-delivers it via location.hash on load, so a
 * bare load with no hash always starts at slide 1.
 *
 * Usage:
 *   <style>deck-stage:not(:defined){visibility:hidden}</style>
 *   <deck-stage width="1920" height="1080">
 *     <section data-label="Title">...</section>
 *     <section data-label="Agenda">...</section>
 *   </deck-stage>
 *   <script src="deck-stage.js"></script>
 *
 * The :not(:defined) rule prevents a flash of the first slide at its
 * authored styles before this script runs and attaches the shadow root.
 *
 * Slides are the direct element children of <deck-stage>. Each slide is
 * automatically tagged with:
 *   - data-screen-label="NN Label"   (1-indexed, for comment flow)
 *   - data-om-validate="no_overflowing_text,no_overlapping_text,slide_sized_text"
 */

(() => {
  const DESIGN_W_DEFAULT = 1920;
  const DESIGN_H_DEFAULT = 1080;
  const OVERLAY_HIDE_MS = 1800;
  const VALIDATE_ATTR = 'no_overflowing_text,no_overlapping_text,slide_sized_text';
  const FINE_POINTER_MQ = matchMedia('(hover: hover) and (pointer: fine)');
  const NARROW_MQ = matchMedia('(max-width: 640px)');
  // Slide-authored controls that should keep a tap instead of it navigating.
  const INTERACTIVE_SEL = 'a[href], button, input, select, textarea, summary, label, video[controls], audio[controls], [role="button"], [onclick], [tabindex]:not([tabindex^="-"]), [contenteditable]:not([contenteditable="false" i])';
  const pad2 = n => String(n).padStart(2, '0');

  // Label precedence: data-label → data-screen-label (number stripped) → first heading → "Slide".
  const getSlideLabel = el => {
    const explicit = el.getAttribute('data-label');
    if (explicit) return explicit;
    const existing = el.getAttribute('data-screen-label');
    if (existing) return existing.replace(/^\s*\d+\s*/, '').trim() || existing;
    const h = el.querySelector('h1, h2, h3, [data-title]');
    const t = h && (h.textContent || '').trim().slice(0, 40);
    if (t) return t;
    return 'Slide';
  };
  const stylesheet = `
    :host {
      position: fixed;
      inset: 0;
      display: block;
      background: #000;
      color: #fff;
      font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", Helvetica, Arial, sans-serif;
      overflow: hidden;
      -webkit-tap-highlight-color: transparent;
    }
    /* connectedCallback holds this until document.fonts.ready (capped 2s) so
     * the first visible paint has the deck's real typography + final rail
     * layout. opacity (not visibility) so the active slide can't un-hide
     * itself via the ::slotted([data-deck-active]) visibility:visible rule.
     * Only the stage/rail hide — the black :host background stays, so the
     * iframe doesn't flash the page's default white. */
    :host([data-fonts-pending]) .stage,
    :host([data-fonts-pending]) .rail { opacity: 0; pointer-events: none; }

    .stage {
      position: absolute;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .canvas {
      position: relative;
      transform-origin: center center;
      flex-shrink: 0;
      background: #fff;
      will-change: transform;
    }

    /* Slides live in light DOM (via <slot>) so authored CSS still applies.
       We absolutely position each slotted child to stack them. */
    ::slotted(*) {
      position: absolute !important;
      inset: 0 !important;
      width: 100% !important;
      height: 100% !important;
      box-sizing: border-box !important;
      overflow: hidden;
      opacity: 0;
      pointer-events: none;
      visibility: hidden;
    }
    ::slotted([data-deck-active]) {
      opacity: 1;
      pointer-events: auto;
      visibility: visible;
    }

    .overlay {
      position: fixed;
      left: 50%;
      bottom: 22px;
      transform: translate(-50%, 6px) scale(0.92);
      filter: blur(6px);
      display: flex;
      align-items: center;
      gap: 4px;
      padding: 4px;
      background: #000;
      color: #fff;
      border-radius: 999px;
      font-size: 12px;
      font-feature-settings: "tnum" 1;
      letter-spacing: 0.01em;
      opacity: 0;
      pointer-events: none;
      transition: opacity 260ms ease, transform 260ms cubic-bezier(.2,.8,.2,1), filter 260ms ease;
      transform-origin: center bottom;
      z-index: 2147483000;
      user-select: none;
    }
    .overlay[data-visible] {
      opacity: 1;
      pointer-events: auto;
      transform: translate(-50%, 0) scale(1);
      filter: blur(0);
    }

    .btn {
      appearance: none;
      -webkit-appearance: none;
      background: transparent;
      border: 0;
      margin: 0;
      padding: 0;
      color: inherit;
      font: inherit;
      cursor: default;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      height: 28px;
      min-width: 28px;
      border-radius: 999px;
      color: rgba(255,255,255,0.72);
      transition: background 140ms ease, color 140ms ease;
      -webkit-tap-highlight-color: transparent;
    }
    .btn:hover { background: rgba(255,255,255,0.12); color: #fff; }
    .btn:active { background: rgba(255,255,255,0.18); }
    .btn:focus { outline: none; }
    .btn:focus-visible { outline: none; }
    .btn::-moz-focus-inner { border: 0; }
    .btn svg { width: 14px; height: 14px; display: block; }
    .btn.reset {
      font-size: 11px;
      font-weight: 500;
      letter-spacing: 0.02em;
      padding: 0 10px 0 12px;
      gap: 6px;
      color: rgba(255,255,255,0.72);
    }
    .btn.reset .kbd {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-width: 16px;
      height: 16px;
      padding: 0 4px;
      font-family: ui-monospace, "SF Mono", Menlo, Consolas, monospace;
      font-size: 10px;
      line-height: 1;
      color: rgba(255,255,255,0.88);
      background: rgba(255,255,255,0.12);
      border-radius: 4px;
    }

    .count {
      font-variant-numeric: tabular-nums;
      color: #fff;
      font-weight: 500;
      padding: 0 8px;
      min-width: 42px;
      text-align: center;
      font-size: 12px;
    }
    .count .sep { color: rgba(255,255,255,0.45); margin: 0 3px; font-weight: 400; }
    .count .total { color: rgba(255,255,255,0.55); }

    .divider {
      width: 1px;
      height: 14px;
      background: rgba(255,255,255,0.18);
      margin: 0 2px;
    }

    /* ── Thumbnail rail ──────────────────────────────────────────────────
       Fixed column on the left; each thumbnail is a static deep-clone of
       the light-DOM slide scaled into a 16:9 (or design-aspect) frame. The
       stage re-fits around it (see _fit); hidden during present / noscale
       / print so capture geometry and fullscreen output are unchanged. */
    .rail {
      position: fixed;
      left: 0;
      top: 0;
      bottom: 0;
      width: var(--deck-rail-w, 188px);
      background: #141414;
      border-right: 1px solid rgba(255,255,255,0.08);
      overflow-y: auto;
      overflow-x: hidden;
      padding: 12px 10px;
      box-sizing: border-box;
      display: flex;
      flex-direction: column;
      gap: 12px;
      z-index: 2147482500;
      scrollbar-width: thin;
      scrollbar-color: rgba(255,255,255,0.18) transparent;
    }
    .rail::-webkit-scrollbar { width: 8px; }
    .rail::-webkit-scrollbar-track { background: transparent; margin: 2px; }
    .rail::-webkit-scrollbar-thumb {
      background: rgba(255,255,255,0.18);
      border-radius: 4px;
      border: 2px solid transparent;
      background-clip: content-box;
    }
    .rail::-webkit-scrollbar-thumb:hover {
      background: rgba(255,255,255,0.28);
      border: 2px solid transparent;
      background-clip: content-box;
    }
    :host([no-rail]) .rail,
    :host([noscale]) .rail { display: none; }
    .rail[data-presenting] { display: none; }
    @media (max-width: 640px) {
      .rail, .rail-resize { display: none; }
    }
    /* User-driven show/hide (the TweaksPanel toggle) slides instead of
       popping. Transitions are gated on :host([data-rail-anim]) — set only
       for the 200ms around the toggle — so window-resize and rail-width
       drag (which also call _fit) don't lag behind the cursor. */
    .rail[data-user-hidden] { transform: translateX(-100%); }
    :host([data-rail-anim]) .rail { transition: transform 200ms cubic-bezier(.3,.7,.4,1); }
    :host([data-rail-anim]) .stage { transition: left 200ms cubic-bezier(.3,.7,.4,1); }
    :host([data-rail-anim]) .canvas { transition: transform 200ms cubic-bezier(.3,.7,.4,1); }
    /* transition shorthand replaces rather than merges — repeat the base
       .overlay opacity/transform/filter transitions so visibility changes
       during the 200ms toggle window still fade instead of popping. */
    :host([data-rail-anim]) .overlay {
      transition: margin-left 200ms cubic-bezier(.3,.7,.4,1),
                  opacity 260ms ease,
                  transform 260ms cubic-bezier(.2,.8,.2,1),
                  filter 260ms ease;
    }

    .thumb {
      position: relative;
      display: flex;
      align-items: flex-start;
      gap: 8px;
      cursor: pointer;
      user-select: none;
    }
    .thumb .num {
      width: 16px;
      flex-shrink: 0;
      font-size: 11px;
      font-weight: 500;
      text-align: right;
      color: rgba(255,255,255,0.55);
      padding-top: 2px;
      font-variant-numeric: tabular-nums;
    }
    .thumb .frame {
      position: relative;
      flex: 1;
      min-width: 0;
      aspect-ratio: var(--deck-aspect);
      background: #fff;
      border-radius: 4px;
      outline: 2px solid transparent;
      outline-offset: 0;
      overflow: hidden;
      transition: outline-color 120ms ease;
    }
    .thumb:hover .frame { outline-color: rgba(255,255,255,0.25); }
    .thumb { outline: none; }
    .thumb:focus-visible .frame { outline-color: rgba(255,255,255,0.5); }
    .thumb[data-current] .num { color: #fff; }
    .thumb[data-current] .frame { outline-color: #D97757; }
    .thumb[data-dragging] { opacity: 0.35; }
    .thumb::before {
      content: '';
      position: absolute;
      left: 24px;
      right: 0;
      height: 3px;
      border-radius: 2px;
      background: #D97757;
      opacity: 0;
      pointer-events: none;
    }
    .thumb[data-drop="before"]::before { top: -8px; opacity: 1; }
    .thumb[data-drop="after"]::before { bottom: -8px; opacity: 1; }
    .thumb[data-skip] .frame { opacity: 0.35; }
    .thumb[data-skip] .frame::after {
      content: 'Skipped';
      position: absolute;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(0,0,0,0.45);
      color: #fff;
      font-size: 10px;
      font-weight: 500;
      letter-spacing: 0.04em;
    }

    .ctxmenu {
      position: fixed;
      min-width: 150px;
      padding: 4px;
      background: #242424;
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 7px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.45);
      z-index: 2147483100;
      display: none;
      font-size: 12px;
    }
    .ctxmenu[data-open] { display: block; }
    .ctxmenu button {
      display: block;
      width: 100%;
      appearance: none;
      border: 0;
      background: transparent;
      color: #e8e8e8;
      font: inherit;
      text-align: left;
      padding: 6px 10px;
      border-radius: 4px;
      cursor: pointer;
    }
    .ctxmenu button:hover:not(:disabled) { background: rgba(255,255,255,0.08); }
    .ctxmenu button:disabled { opacity: 0.35; cursor: default; }
    .ctxmenu hr {
      border: 0;
      border-top: 1px solid rgba(255,255,255,0.1);
      margin: 4px 2px;
    }

    .rail-resize {
      position: fixed;
      left: calc(var(--deck-rail-w, 188px) - 3px);
      top: 0;
      bottom: 0;
      width: 6px;
      cursor: col-resize;
      z-index: 2147482600;
      touch-action: none;
    }
    .rail-resize:hover,
    .rail-resize[data-dragging] { background: rgba(255,255,255,0.12); }
    :host([no-rail]) .rail-resize,
    :host([noscale]) .rail-resize,
    .rail[data-presenting] + .rail-resize,
    .rail[data-user-hidden] + .rail-resize { display: none; }

    /* Delete-confirm popup — matches the SPA's ConfirmDialog layout
       (title + message body, depressed footer with Cancel / Delete). */
    .confirm-backdrop {
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.45);
      z-index: 2147483200;
      display: none;
      align-items: center;
      justify-content: center;
    }
    .confirm-backdrop[data-open] { display: flex; }
    .confirm {
      width: 320px;
      max-width: calc(100vw - 32px);
      background: #2a2a2a;
      color: #e8e8e8;
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 12px;
      box-shadow: 0 12px 32px rgba(0,0,0,0.5);
      overflow: hidden;
      font-family: inherit;
      animation: deck-confirm-in 0.18s ease;
    }
    @keyframes deck-confirm-in {
      from { opacity: 0; transform: scale(0.96); }
      to { opacity: 1; transform: scale(1); }
    }
    .confirm .body { padding: 20px 20px 16px; }
    .confirm .title { font-size: 14px; font-weight: 600; margin-bottom: 4px; }
    .confirm .msg { font-size: 13px; line-height: 1.5; color: rgba(255,255,255,0.65); }
    .confirm .footer {
      padding: 14px 20px;
      background: #1f1f1f;
      border-top: 1px solid rgba(255,255,255,0.08);
      display: flex;
      justify-content: flex-end;
      gap: 8px;
    }
    .confirm button {
      appearance: none;
      font: inherit;
      font-size: 13px;
      font-weight: 500;
      padding: 8px 16px;
      border-radius: 8px;
      cursor: pointer;
    }
    .confirm .cancel {
      background: transparent;
      border: 0;
      color: rgba(255,255,255,0.8);
    }
    .confirm .cancel:hover { background: rgba(255,255,255,0.08); }
    .confirm .danger {
      background: #c96442;
      border: 1px solid rgba(0,0,0,0.15);
      color: #fff;
      box-shadow: 0 1px 3px rgba(166,50,68,0.3), 0 2px 6px rgba(166,50,68,0.18);
    }
    .confirm .danger:hover { background: #b5563a; }

    /* ── Print: one page per slide, no chrome ────────────────────────────
       The screen layout stacks every slide at inset:0 inside a scaled
       canvas; for print we want them in document flow at the authored
       design size so the browser paginates one slide per sheet. The
       @page size is set from the width/height attributes via the inline
       <style id="deck-stage-print-page"> that connectedCallback injects
       into <head> (the @page at-rule has no effect inside shadow DOM). */
    @media print {
      :host {
        position: static;
        inset: auto;
        background: none;
        overflow: visible;
        color: inherit;
      }
      .stage { position: static; display: block; }
      .canvas {
        transform: none !important;
        width: auto !important;
        height: auto !important;
        background: none;
        will-change: auto;
      }
      ::slotted(*) {
        position: relative !important;
        inset: auto !important;
        width: var(--deck-design-w) !important;
        height: var(--deck-design-h) !important;
        box-sizing: border-box !important;
        opacity: 1 !important;
        visibility: visible !important;
        pointer-events: auto;
        break-after: page;
        page-break-after: always;
        break-inside: avoid;
        overflow: hidden;
      }
      /* :last-child alone isn't enough once data-deck-skip hides the
         trailing slide(s) — the last *visible* slide still carries
         break-after:page and prints a blank sheet. _markLastVisible()
         maintains data-deck-last-visible on the last non-skipped slide. */
      ::slotted(*:last-child),
      ::slotted([data-deck-last-visible]) {
        break-after: auto;
        page-break-after: auto;
      }
      ::slotted([data-deck-skip]) { display: none !important; }
      .overlay, .rail, .rail-resize, .ctxmenu, .confirm-backdrop { display: none !important; }
    }
  `;
  class DeckStage extends HTMLElement {
    static get observedAttributes() {
      return ['width', 'height', 'noscale', 'no-rail'];
    }
    constructor() {
      super();
      this._root = this.attachShadow({
        mode: 'open'
      });
      this._index = 0;
      this._slides = [];
      this._notes = [];
      this._hideTimer = null;
      this._mouseIdleTimer = null;
      this._menuIndex = -1;
      this._onKey = this._onKey.bind(this);
      this._onResize = this._onResize.bind(this);
      this._onSlotChange = this._onSlotChange.bind(this);
      this._onMouseMove = this._onMouseMove.bind(this);
      this._onTap = this._onTap.bind(this);
      this._onMessage = this._onMessage.bind(this);
      // Capture-phase close so a click anywhere dismisses the menu, but
      // ignore clicks that land inside the menu itself — otherwise the
      // capture handler runs before the menu's own (bubble) handler and
      // clears _menuIndex out from under it.
      this._onDocClick = e => {
        if (this._menu && e.composedPath && e.composedPath().includes(this._menu)) return;
        this._closeMenu();
      };
    }
    get designWidth() {
      return parseInt(this.getAttribute('width'), 10) || DESIGN_W_DEFAULT;
    }
    get designHeight() {
      return parseInt(this.getAttribute('height'), 10) || DESIGN_H_DEFAULT;
    }
    connectedCallback() {
      // Presenter-view popup loads deckUrl?_snthumb=...#N for its prev/cur/
      // next thumbnails — the rail has no business rendering inside those
      // (wrong scale, and it offsets the stage so the thumb shows a gutter).
      if (/[?&]_snthumb=/.test(location.search)) this.setAttribute('no-rail', '');
      this._render();
      this._loadNotes();
      this._syncPrintPageRule();
      window.addEventListener('keydown', this._onKey);
      window.addEventListener('resize', this._onResize);
      window.addEventListener('mousemove', this._onMouseMove, {
        passive: true
      });
      window.addEventListener('message', this._onMessage);
      window.addEventListener('click', this._onDocClick, true);
      this.addEventListener('click', this._onTap);
      // Initial collection + layout happens via slotchange, which fires on mount.
      this._enableRail();
      // Hold the stage hidden until webfonts are ready so the first visible
      // paint has the deck's real typography — the :not(:defined) guard in
      // the page HTML only covers custom-element upgrade, not font load.
      // Capped so a 404'd font URL can't blank the deck indefinitely.
      this.setAttribute('data-fonts-pending', '');
      const reveal = () => this.removeAttribute('data-fonts-pending');
      // rAF first: fonts.ready is a pre-resolved promise until layout has
      // resolved the slotted text's font-family and pushed a FontFace into
      // 'loading'. Reading it here in connectedCallback (parse-time) would
      // settle the race in a microtask before any font fetch starts.
      requestAnimationFrame(() => {
        Promise.race([document.fonts ? document.fonts.ready : Promise.resolve(), new Promise(r => setTimeout(r, 2000))]).then(reveal, reveal);
      });
    }
    _enableRail() {
      // Idempotent — older host builds still post __omelette_rail_enabled.
      // no-rail guard keeps the observers/stylesheet walk off the cheap path
      // for presenter-popup thumbnail iframes (up to 9 per view).
      if (this._railEnabled || this.hasAttribute('no-rail')) return;
      this._railEnabled = true;
      // Per-viewer preference — restored alongside rail width. Default on;
      // only a stored '0' (from the TweaksPanel toggle) hides it.
      this._railVisible = true;
      try {
        if (localStorage.getItem('deck-stage.railVisible') === '0') this._railVisible = false;
      } catch (e) {}
      // Live thumbnail updates: watch the light-DOM slides for content
      // edits and re-clone just the affected thumb(s), debounced. Ignore
      // the data-deck-* / data-screen-label / data-om-validate attributes
      // this component itself writes so nav and skip don't trigger
      // spurious refreshes.
      const OWN_ATTRS = /^data-(deck-|screen-label$|om-validate$)/;
      this._liveDirty = new Set();
      this._liveObserver = new MutationObserver(records => {
        for (const r of records) {
          if (r.type === 'attributes' && OWN_ATTRS.test(r.attributeName || '')) continue;
          let n = r.target;
          while (n && n.parentElement !== this) n = n.parentElement;
          if (n && this._slideSet && this._slideSet.has(n)) this._liveDirty.add(n);
        }
        if (this._liveDirty.size && !this._liveTimer) {
          this._liveTimer = setTimeout(() => {
            this._liveTimer = null;
            this._liveDirty.forEach(s => this._refreshThumb(s));
            this._liveDirty.clear();
          }, 200);
        }
      });
      this._liveObserver.observe(this, {
        subtree: true,
        childList: true,
        characterData: true,
        attributes: true
      });
      // Lazy thumbnail materialization — clone the slide only when its
      // frame scrolls into (or near) the rail viewport. rootMargin gives
      // ~4 thumbs of pre-load so fast scrolling doesn't flash blanks.
      this._railObserver = new IntersectionObserver(entries => {
        entries.forEach(e => {
          if (e.isIntersecting && e.target.__deckThumb) {
            this._materialize(e.target.__deckThumb);
          }
        });
      }, {
        root: this._rail,
        rootMargin: '400px 0px'
      });
      // Tweaks typically change CSS vars / attrs OUTSIDE <deck-stage>
      // (on <html>, <body>, a wrapper div, or a <style> tag), which
      // _liveObserver can't see. Re-snapshot author CSS (constructable
      // sheet is shared by reference, so one replaceSync updates every
      // thumb shadow root) and re-sync each thumb host's attrs + custom
      // properties. In-slide DOM mutations are _liveObserver's job.
      // Debounced so slider drags don't thrash.
      this._onTweakChange = () => {
        clearTimeout(this._tweakTimer);
        this._tweakTimer = setTimeout(() => {
          this._snapshotAuthorCss();
          // One getComputedStyle for the whole batch — each
          // getPropertyValue read below reuses the same computed style
          // as long as nothing invalidates layout between thumbs.
          const cs = getComputedStyle(this);
          (this._thumbs || []).forEach(t => {
            if (t.host) this._syncThumbHostAttrs(t.host, cs);
          });
        }, 120);
      };
      window.addEventListener('tweakchange', this._onTweakChange);
      this._snapshotAuthorCss();
      // Build the rail now that it's enabled — slotchange already fired,
      // so _renderRail's early-return skipped the initial build.
      this._syncRailHidden();
      this._renderRail();
      this._fit();
    }

    /** Snapshot document stylesheets into a constructable sheet that each
     *  thumbnail's nested shadow root adopts — so author CSS styles the
     *  cloned slide content without touching this component's chrome.
     *  Cross-origin sheets throw on .cssRules — skip them. Re-callable:
     *  the existing constructable sheet is reused via replaceSync so every
     *  already-adopted shadow root picks up the fresh CSS without re-adopt. */
    _snapshotAuthorCss() {
      // :root in an adopted sheet inside a shadow root matches nothing
      // (only the document root qualifies), so author rules like
      // `:root[data-voice="modern"] .serif` never reach the clones.
      // Rewrite :root → :host and mirror <html>'s data-*/class/lang onto
      // each thumb host (see _syncThumbHostAttrs) so the same selectors
      // match inside the thumbnail's shadow tree.
      const authorCss = Array.from(document.styleSheets).map(sh => {
        try {
          return Array.from(sh.cssRules).map(r => r.cssText).join('\n');
        } catch (e) {
          return '';
        }
      }).join('\n')
      // The shadow host is featureless outside the functional :host(...)
      // form, so any compound on :root — [attr], .class, #id, :pseudo —
      // must become :host(<compound>) not :host<compound>. Same for the
      // html type selector (Tailwind class-strategy dark mode emits
      // html.dark; Pico uses html[data-theme]), which has nothing to
      // match inside the thumb's shadow tree.
      .replace(/:root((?:\[[^\]]*\]|[.#][-\w]+|:[-\w]+(?:\([^)]*\))?)+)/g, ':host($1)').replace(/:root\b/g, ':host').replace(/(^|[\s,>~+(}])html((?:\[[^\]]*\]|[.#][-\w]+|:[-\w]+(?:\([^)]*\))?)+)(?![-\w])/g, '$1:host($2)').replace(/(^|[\s,>~+(}])html(?![-\w])/g, '$1:host');
      // Every custom property the author references. _syncThumbHostAttrs
      // mirrors each one's *computed* value at <deck-stage> onto the
      // thumb host so the live value wins over the :host default above
      // regardless of which ancestor the tweak wrote to (<html>, <body>,
      // a wrapper div, or the deck-stage element itself all inherit
      // down to getComputedStyle(this)).
      this._authorVars = new Set(authorCss.match(/--[\w-]+/g) || []);
      try {
        if (!this._adoptedSheet) this._adoptedSheet = new CSSStyleSheet();
        this._adoptedSheet.replaceSync(authorCss);
      } catch (e) {
        this._adoptedSheet = null;
        this._authorCss = authorCss;
      }
    }
    _syncThumbHostAttrs(host, cs) {
      const de = document.documentElement;
      // setAttribute overwrites but can't delete — an attr removed from
      // <html> (toggleAttribute off, classList emptied) would linger on
      // the host and :host([data-*]) / :host(.foo) rules would keep
      // matching. Remove stale mirrored attrs first; iterate backward
      // because removeAttribute mutates the live NamedNodeMap.
      for (let i = host.attributes.length - 1; i >= 0; i--) {
        const n = host.attributes[i].name;
        if ((n.startsWith('data-') || n === 'class' || n === 'lang') && !de.hasAttribute(n)) {
          host.removeAttribute(n);
        }
      }
      for (const a of de.attributes) {
        if (a.name.startsWith('data-') || a.name === 'class' || a.name === 'lang') {
          host.setAttribute(a.name, a.value);
        }
      }
      // The :root→:host rewrite in _snapshotAuthorCss pins each custom
      // property to its stylesheet default on the thumb host, shadowing
      // the live value that would otherwise inherit. Tweaks can write the
      // live value on any ancestor — <html>, <body>, a wrapper div, the
      // deck-stage element — so read it as the *computed* value at
      // <deck-stage> (which sees the whole inheritance chain) rather than
      // trying to guess which element the author wrote to. Inline on the
      // host beats the :host{} rule. remove-stale covers vars dropped
      // from the stylesheet between snapshots.
      const vars = this._authorVars || new Set();
      for (let i = host.style.length - 1; i >= 0; i--) {
        const p = host.style[i];
        if (p.startsWith('--') && !vars.has(p)) host.style.removeProperty(p);
      }
      const live = cs || getComputedStyle(this);
      vars.forEach(p => {
        const v = live.getPropertyValue(p);
        if (v) host.style.setProperty(p, v.trim());else host.style.removeProperty(p);
      });
    }
    disconnectedCallback() {
      window.removeEventListener('keydown', this._onKey);
      window.removeEventListener('resize', this._onResize);
      window.removeEventListener('mousemove', this._onMouseMove);
      window.removeEventListener('message', this._onMessage);
      window.removeEventListener('click', this._onDocClick, true);
      this.removeEventListener('click', this._onTap);
      if (this._hideTimer) clearTimeout(this._hideTimer);
      if (this._mouseIdleTimer) clearTimeout(this._mouseIdleTimer);
      if (this._liveTimer) clearTimeout(this._liveTimer);
      if (this._tweakTimer) clearTimeout(this._tweakTimer);
      if (this._railAnimTimer) clearTimeout(this._railAnimTimer);
      if (this._scaleRaf) cancelAnimationFrame(this._scaleRaf);
      if (this._liveObserver) this._liveObserver.disconnect();
      if (this._railObserver) this._railObserver.disconnect();
      if (this._onTweakChange) window.removeEventListener('tweakchange', this._onTweakChange);
    }
    attributeChangedCallback() {
      if (this._canvas) {
        this._canvas.style.width = this.designWidth + 'px';
        this._canvas.style.height = this.designHeight + 'px';
        this._canvas.style.setProperty('--deck-design-w', this.designWidth + 'px');
        this._canvas.style.setProperty('--deck-design-h', this.designHeight + 'px');
        if (this._rail) {
          this._rail.style.setProperty('--deck-aspect', this.designWidth + '/' + this.designHeight);
        }
        this._fit();
        this._scaleThumbs();
        this._syncPrintPageRule();
      }
    }
    _render() {
      const style = document.createElement('style');
      style.textContent = stylesheet;
      const stage = document.createElement('div');
      stage.className = 'stage';
      const canvas = document.createElement('div');
      canvas.className = 'canvas';
      canvas.style.width = this.designWidth + 'px';
      canvas.style.height = this.designHeight + 'px';
      canvas.style.setProperty('--deck-design-w', this.designWidth + 'px');
      canvas.style.setProperty('--deck-design-h', this.designHeight + 'px');
      const slot = document.createElement('slot');
      slot.addEventListener('slotchange', this._onSlotChange);
      canvas.appendChild(slot);
      stage.appendChild(canvas);

      // Overlay: compact, solid black, with clickable controls.
      const overlay = document.createElement('div');
      overlay.className = 'overlay export-hidden';
      overlay.setAttribute('role', 'toolbar');
      overlay.setAttribute('aria-label', 'Deck controls');
      overlay.setAttribute('data-omelette-chrome', '');
      overlay.innerHTML = `
        <button class="btn prev" type="button" aria-label="Previous slide" title="Previous (←)">
          <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M10 3L5 8l5 5"/></svg>
        </button>
        <span class="count" aria-live="polite"><span class="current">1</span><span class="sep">/</span><span class="total">1</span></span>
        <button class="btn next" type="button" aria-label="Next slide" title="Next (→)">
          <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 3l5 5-5 5"/></svg>
        </button>
        <span class="divider"></span>
        <button class="btn reset" type="button" aria-label="Reset to first slide" title="Reset (R)">Reset<span class="kbd">R</span></button>
      `;
      overlay.querySelector('.prev').addEventListener('click', () => this._advance(-1, 'click'));
      overlay.querySelector('.next').addEventListener('click', () => this._advance(1, 'click'));
      overlay.querySelector('.reset').addEventListener('click', () => this._go(0, 'click'));

      // Thumbnail rail + context menu. Thumbnails are populated in
      // _renderRail() after _collectSlides().
      const rail = document.createElement('div');
      rail.className = 'rail export-hidden';
      rail.setAttribute('data-omelette-chrome', '');
      rail.style.setProperty('--deck-aspect', this.designWidth + '/' + this.designHeight);
      // Edge auto-scroll while dragging a thumb near the rail's top/bottom
      // so off-screen drop targets are reachable. Native dragover fires
      // continuously while the pointer is stationary, so a per-event nudge
      // (ramped by edge proximity) is enough — no rAF loop needed.
      rail.addEventListener('dragover', e => {
        if (this._dragFrom == null) return;
        const r = rail.getBoundingClientRect();
        const EDGE = 40;
        const dt = e.clientY - r.top;
        const db = r.bottom - e.clientY;
        if (dt < EDGE) rail.scrollTop -= Math.ceil((EDGE - dt) / 3);else if (db < EDGE) rail.scrollTop += Math.ceil((EDGE - db) / 3);
      });
      const menu = document.createElement('div');
      menu.className = 'ctxmenu export-hidden';
      menu.setAttribute('data-omelette-chrome', '');
      menu.innerHTML = `
        <button type="button" data-act="skip">Skip slide</button>
        <button type="button" data-act="up">Move up</button>
        <button type="button" data-act="down">Move down</button>
        <hr>
        <button type="button" data-act="delete">Delete slide</button>
      `;
      menu.addEventListener('click', e => {
        const act = e.target && e.target.getAttribute && e.target.getAttribute('data-act');
        if (!act) return;
        const i = this._menuIndex;
        this._closeMenu();
        if (act === 'skip') this._toggleSkip(i);else if (act === 'up') this._moveSlide(i, i - 1);else if (act === 'down') this._moveSlide(i, i + 1);else if (act === 'delete') this._openConfirm(i);
      });
      menu.addEventListener('contextmenu', e => e.preventDefault());

      // Rail resize handle — drag to set --deck-rail-w, persisted to
      // localStorage so the width survives reloads.
      const resize = document.createElement('div');
      resize.className = 'rail-resize export-hidden';
      resize.setAttribute('data-omelette-chrome', '');
      resize.addEventListener('pointerdown', e => {
        e.preventDefault();
        resize.setPointerCapture(e.pointerId);
        resize.setAttribute('data-dragging', '');
        const move = ev => this._setRailWidth(ev.clientX);
        const up = () => {
          resize.removeEventListener('pointermove', move);
          resize.removeEventListener('pointerup', up);
          resize.removeEventListener('pointercancel', up);
          resize.removeAttribute('data-dragging');
          try {
            localStorage.setItem('deck-stage.railWidth', String(this._railPx));
          } catch (err) {}
        };
        resize.addEventListener('pointermove', move);
        resize.addEventListener('pointerup', up);
        resize.addEventListener('pointercancel', up);
      });

      // Delete-confirm dialog — mirrors the SPA's ConfirmDialog layout.
      const confirm = document.createElement('div');
      confirm.className = 'confirm-backdrop export-hidden';
      confirm.setAttribute('data-omelette-chrome', '');
      confirm.innerHTML = `
        <div class="confirm" role="dialog" aria-modal="true">
          <div class="body">
            <div class="title">Delete slide?</div>
            <div class="msg">This slide will be removed from the deck.</div>
          </div>
          <div class="footer">
            <button type="button" class="cancel">Cancel</button>
            <button type="button" class="danger">Delete</button>
          </div>
        </div>
      `;
      confirm.addEventListener('click', e => {
        if (e.target === confirm) this._closeConfirm();
      });
      confirm.querySelector('.cancel').addEventListener('click', () => this._closeConfirm());
      confirm.querySelector('.danger').addEventListener('click', () => {
        const i = this._confirmIndex;
        this._closeConfirm();
        this._deleteSlide(i);
      });
      this._root.append(style, rail, resize, stage, overlay, menu, confirm);
      this._canvas = canvas;
      this._stage = stage;
      this._slot = slot;
      this._overlay = overlay;
      this._rail = rail;
      this._resize = resize;
      this._menu = menu;
      this._confirm = confirm;
      this._countEl = overlay.querySelector('.current');
      this._totalEl = overlay.querySelector('.total');

      // Restore persisted rail width.
      let rw = 188;
      try {
        const s = localStorage.getItem('deck-stage.railWidth');
        if (s) rw = parseInt(s, 10) || rw;
      } catch (err) {}
      this._setRailWidth(rw);
      this._syncRailHidden();
    }
    _setRailWidth(px) {
      const w = Math.max(120, Math.min(360, Math.round(px)));
      this._railPx = w;
      this.style.setProperty('--deck-rail-w', w + 'px');
      this._fit();
      // _scaleThumbs forces a sync layout (frame.offsetWidth) then writes
      // N transforms. During a resize drag this runs per-pointermove;
      // coalesce to one per frame.
      if (!this._scaleRaf) {
        this._scaleRaf = requestAnimationFrame(() => {
          this._scaleRaf = null;
          this._scaleThumbs();
        });
      }
    }

    /** @page must live in the document stylesheet — it's a no-op inside
     *  shadow DOM. Inject/update a single <head> style tag so the print
     *  sheet matches the design size and Save-as-PDF yields one slide per
     *  page with no margins. */
    _syncPrintPageRule() {
      const id = 'deck-stage-print-page';
      let tag = document.getElementById(id);
      if (!tag) {
        tag = document.createElement('style');
        tag.id = id;
        document.head.appendChild(tag);
      }
      tag.textContent = '@page { size: ' + this.designWidth + 'px ' + this.designHeight + 'px; margin: 0; } ' + '@media print { html, body { margin: 0 !important; padding: 0 !important; background: none !important; overflow: visible !important; height: auto !important; } ' + '* { -webkit-print-color-adjust: exact; print-color-adjust: exact; } }';
    }
    _onSlotChange() {
      // Rail mutations (delete/move) already reconcile synchronously and
      // emit slidechange with reason 'api'; skip the async slotchange that
      // would otherwise re-broadcast with reason 'init'.
      if (this._squelchSlotChange) {
        this._squelchSlotChange = false;
        return;
      }
      this._collectSlides();
      this._restoreIndex();
      this._applyIndex({
        showOverlay: false,
        broadcast: true,
        reason: 'init'
      });
      this._fit();
    }
    _collectSlides() {
      const assigned = this._slot.assignedElements({
        flatten: true
      });
      this._slides = assigned.filter(el => {
        // Skip template/style/script nodes even if someone slots them.
        const tag = el.tagName;
        return tag !== 'TEMPLATE' && tag !== 'SCRIPT' && tag !== 'STYLE';
      });
      this._slideSet = new Set(this._slides);
      this._slides.forEach((slide, i) => {
        const n = i + 1;
        slide.setAttribute('data-screen-label', `${pad2(n)} ${getSlideLabel(slide)}`);

        // Validation attribute for comment flow / auto-checks.
        if (!slide.hasAttribute('data-om-validate')) {
          slide.setAttribute('data-om-validate', VALIDATE_ATTR);
        }
        slide.setAttribute('data-deck-slide', String(i));
      });
      if (this._totalEl) this._totalEl.textContent = String(this._slides.length || 1);
      if (this._index >= this._slides.length) this._index = Math.max(0, this._slides.length - 1);
      this._markLastVisible();
      this._renderRail();
    }

    /** Tag the last non-skipped slide so print CSS can drop its
     *  break-after (see the @media print comment above — :last-child
     *  alone matches a hidden skipped slide). */
    _markLastVisible() {
      let last = null;
      this._slides.forEach(s => {
        s.removeAttribute('data-deck-last-visible');
        if (!s.hasAttribute('data-deck-skip')) last = s;
      });
      if (last) last.setAttribute('data-deck-last-visible', '');
    }
    _loadNotes() {
      const tag = document.getElementById('speaker-notes');
      if (!tag) {
        this._notes = [];
        return;
      }
      try {
        const parsed = JSON.parse(tag.textContent || '[]');
        if (Array.isArray(parsed)) this._notes = parsed;
      } catch (e) {
        console.warn('[deck-stage] Failed to parse #speaker-notes JSON:', e);
        this._notes = [];
      }
    }
    _restoreIndex() {
      // The host's ?slide= param is delivered as a #<int> hash (1-indexed) on
      // the iframe src. No hash → slide 1; the deck itself keeps no position
      // state across loads.
      const h = (location.hash || '').match(/^#(\d+)$/);
      if (h) {
        const n = parseInt(h[1], 10) - 1;
        if (n >= 0 && n < this._slides.length) this._index = n;
      }
    }
    _applyIndex({
      showOverlay = true,
      broadcast = true,
      reason = 'init'
    } = {}) {
      if (!this._slides.length) return;
      const prev = this._prevIndex == null ? -1 : this._prevIndex;
      const curr = this._index;
      // Keep the iframe's own hash in sync so an in-iframe location.reload()
      // (reload banner path in viewer-handle.ts) lands on the current slide,
      // not the stale deep-link hash from initial load.
      try {
        history.replaceState(null, '', '#' + (curr + 1));
      } catch (e) {}
      this._slides.forEach((s, i) => {
        if (i === curr) s.setAttribute('data-deck-active', '');else s.removeAttribute('data-deck-active');
      });
      if (this._countEl) this._countEl.textContent = String(curr + 1);
      // Follow-scroll on every navigation (init deep-link, keyboard, click,
      // tap, external goTo) — the only time we *don't* want the rail to
      // track current is after a rail-internal mutation, where _renderRail
      // has already restored the user's scroll position and yanking back to
      // current would undo it.
      this._syncRail(reason !== 'mutation');
      if (broadcast) {
        // (1) Legacy: host-window postMessage for speaker-notes renderers.
        try {
          window.postMessage({
            slideIndexChanged: curr,
            deckTotal: this._slides.length,
            deckSkipped: this._skippedIndices()
          }, '*');
        } catch (e) {}

        // (2) In-page CustomEvent on the <deck-stage> element itself.
        //     Bubbles and composes out of shadow DOM so slide code can listen:
        //       document.querySelector('deck-stage').addEventListener('slidechange', e => {
        //         e.detail.index, e.detail.previousIndex, e.detail.total, e.detail.slide, e.detail.reason
        //       });
        const detail = {
          index: curr,
          previousIndex: prev,
          total: this._slides.length,
          slide: this._slides[curr] || null,
          previousSlide: prev >= 0 ? this._slides[prev] || null : null,
          reason: reason // 'init' | 'keyboard' | 'click' | 'tap' | 'api'
        };
        this.dispatchEvent(new CustomEvent('slidechange', {
          detail,
          bubbles: true,
          composed: true
        }));
      }
      this._prevIndex = curr;
      if (showOverlay) this._flashOverlay();
    }
    _flashOverlay() {
      // Host posts __omelette_presenting while in fullscreen/tab presentation
      // mode — suppress the nav footer entirely (both hover and slide-change
      // flash) so the audience sees clean slides.
      if (!this._overlay || this._presenting) return;
      this._overlay.setAttribute('data-visible', '');
      if (this._hideTimer) clearTimeout(this._hideTimer);
      this._hideTimer = setTimeout(() => {
        this._overlay.removeAttribute('data-visible');
      }, OVERLAY_HIDE_MS);
    }
    _railWidth() {
      // State-based, no offsetWidth: the first _fit() can run before the
      // rail has had layout on some load paths, and a 0 there paints the
      // slide full-width for one frame before the post-slotchange _fit()
      // corrects it.
      if (!this._railEnabled || !this._railVisible || this.hasAttribute('no-rail') || this.hasAttribute('noscale') || this._presenting || this._previewMode || NARROW_MQ.matches) return 0;
      return this._railPx || 0;
    }
    _fit() {
      if (!this._canvas) return;
      const stage = this._canvas.parentElement;
      // PPTX export sets noscale so the DOM capture sees authored-size
      // geometry — the scaled canvas is in shadow DOM, so the exporter's
      // resetTransformSelector can't reach .canvas.style.transform directly.
      if (this.hasAttribute('noscale')) {
        this._canvas.style.transform = 'none';
        if (stage) stage.style.left = '0';
        if (this._overlay) this._overlay.style.marginLeft = '0';
        return;
      }
      const rw = this._railWidth();
      if (stage) stage.style.left = rw + 'px';
      // Overlay is centred on the viewport via left:50% + translate(-50%);
      // marginLeft shifts the centre by rw/2 so it lands in the middle of
      // the [rw, innerWidth] stage region.
      if (this._overlay) this._overlay.style.marginLeft = rw / 2 + 'px';
      const vw = window.innerWidth - rw;
      const vh = window.innerHeight;
      const s = Math.min(vw / this.designWidth, vh / this.designHeight);
      this._canvas.style.transform = `scale(${s})`;
    }
    _onResize() {
      this._fit();
      // Crossing the narrow-viewport breakpoint reveals the rail — rerun the
      // thumbnail scale the same way _setRailWidth does.
      if (!this._scaleRaf) {
        this._scaleRaf = requestAnimationFrame(() => {
          this._scaleRaf = null;
          this._scaleThumbs();
        });
      }
    }
    _onMouseMove() {
      // Keep overlay visible while mouse moves; hide after idle.
      this._flashOverlay();
    }
    _onMessage(e) {
      const d = e.data;
      if (d && typeof d.__omelette_presenting === 'boolean') {
        this._presenting = d.__omelette_presenting;
        if (this._presenting && this._overlay) {
          this._overlay.removeAttribute('data-visible');
          if (this._hideTimer) clearTimeout(this._hideTimer);
        }
        this._syncRailHidden();
        this._closeMenu();
        this._closeConfirm();
        this._fit();
        this._scaleThumbs();
      }
      // Host's Preview segment (ViewerMode='none'): the rail's drag-reorder /
      // right-click skip-delete affordances are editing chrome, so hide it
      // while the user is just looking at the deck. Same hard-hide path as
      // presenting; independent of the user's _railVisible preference so
      // returning to Edit restores whatever they had.
      if (d && typeof d.__omelette_preview_mode === 'boolean') {
        if (d.__omelette_preview_mode === this._previewMode) return;
        this._previewMode = d.__omelette_preview_mode;
        this._syncRailHidden();
        this._closeMenu();
        this._closeConfirm();
        this._fit();
        this._scaleThumbs();
      }
      // Per-viewer show/hide, driven by the TweaksPanel's auto-injected
      // "Thumbnail rail" toggle (or any author script). Independent of
      // whether the Tweaks panel itself is open — closing the panel
      // doesn't change rail visibility. Persists alongside rail width.
      if (d && d.type === '__deck_rail_visible' && typeof d.on === 'boolean') {
        if (d.on === this._railVisible) return;
        this._railVisible = d.on;
        try {
          localStorage.setItem('deck-stage.railVisible', d.on ? '1' : '0');
        } catch (e) {}
        // Arm the transition, commit it, then flip state — otherwise the
        // browser coalesces both writes and nothing animates on show.
        this.setAttribute('data-rail-anim', '');
        void (this._rail && this._rail.offsetHeight);
        this._syncRailHidden();
        this._fit();
        this._scaleThumbs();
        clearTimeout(this._railAnimTimer);
        this._railAnimTimer = setTimeout(() => this.removeAttribute('data-rail-anim'), 220);
      }
      if (d && d.type === '__omelette_rail_enabled') this._enableRail();
    }
    _syncRailHidden() {
      if (!this._rail) return;
      // data-presenting is the hard hide (display:none) for flag-off,
      // presentation mode, and the host's Preview segment — instant, no
      // transition. data-user-hidden is the soft hide (translateX(-100%))
      // for the viewer's rail toggle, so show/hide slides under
      // :host([data-rail-anim]).
      const hard = !this._railEnabled || this._presenting || this._previewMode;
      if (hard) this._rail.setAttribute('data-presenting', '');else this._rail.removeAttribute('data-presenting');
      if (!this._railVisible) this._rail.setAttribute('data-user-hidden', '');else this._rail.removeAttribute('data-user-hidden');
      // translateX hide leaves thumbs (tabIndex=0) in the tab order —
      // inert keeps them unfocusable while the rail is off-screen.
      this._rail.inert = hard || !this._railVisible;
    }
    _onTap(e) {
      // Touch-only — keyboard + the overlay toolbar cover nav on desktop.
      if (FINE_POINTER_MQ.matches) return;
      // Only taps that land on the stage (slide content or letterbox); the
      // overlay / rail / menus are siblings with their own click handlers.
      const path = e.composedPath();
      if (!this._stage || !path.includes(this._stage)) return;
      // Let interactive slide content keep the tap. composedPath (not
      // e.target.closest) so we see through open shadow roots — a <button>
      // inside a slide-authored custom element retargets e.target to the
      // host but still appears in the composed path.
      if (e.defaultPrevented) return;
      for (const n of path) {
        if (n === this._stage) break;
        if (n.matches && n.matches(INTERACTIVE_SEL)) return;
      }
      e.preventDefault();
      const rw = this._railWidth();
      const mid = rw + (window.innerWidth - rw) / 2;
      this._advance(e.clientX < mid ? -1 : 1, 'tap');
    }
    _onKey(e) {
      // Ignore when the user is typing.
      const t = e.target;
      if (t && (t.isContentEditable || /^(INPUT|TEXTAREA|SELECT)$/.test(t.tagName))) return;
      // Confirm dialog swallows nav keys while open; Escape cancels. Enter
      // is left to the focused button's native activation so Tab→Cancel
      // →Enter activates Cancel, not the window-level confirm path.
      if (this._confirm && this._confirm.hasAttribute('data-open')) {
        if (e.key === 'Escape') {
          this._closeConfirm();
          e.preventDefault();
        }
        return;
      }
      if (e.key === 'Escape' && this._menu && this._menu.hasAttribute('data-open')) {
        this._closeMenu();
        e.preventDefault();
        return;
      }
      if (e.metaKey || e.ctrlKey || e.altKey) return;
      const key = e.key;
      let handled = true;
      if (key === 'ArrowRight' || key === 'PageDown' || key === ' ' || key === 'Spacebar') {
        this._advance(1, 'keyboard');
      } else if (key === 'ArrowLeft' || key === 'PageUp') {
        this._advance(-1, 'keyboard');
      } else if (key === 'Home') {
        this._go(0, 'keyboard');
      } else if (key === 'End') {
        this._go(this._slides.length - 1, 'keyboard');
      } else if (key === 'r' || key === 'R') {
        this._go(0, 'keyboard');
      } else if (/^[0-9]$/.test(key)) {
        // 1..9 jump to that slide; 0 jumps to 10.
        const n = key === '0' ? 9 : parseInt(key, 10) - 1;
        if (n < this._slides.length) this._go(n, 'keyboard');
      } else {
        handled = false;
      }
      if (handled) {
        e.preventDefault();
        this._flashOverlay();
      }
    }
    _go(i, reason = 'api') {
      if (!this._slides.length) return;
      const clamped = Math.max(0, Math.min(this._slides.length - 1, i));
      if (clamped === this._index) {
        this._flashOverlay();
        return;
      }
      this._index = clamped;
      this._applyIndex({
        showOverlay: true,
        broadcast: true,
        reason
      });
    }

    /** Step forward/back skipping any slide marked data-deck-skip. Falls
     *  back to _go's clamp-at-ends behaviour (flash overlay) when there's
     *  nothing further in that direction. */
    _advance(dir, reason) {
      if (!this._slides.length) return;
      let i = this._index + dir;
      while (i >= 0 && i < this._slides.length && this._slides[i].hasAttribute('data-deck-skip')) {
        i += dir;
      }
      if (i < 0 || i >= this._slides.length) {
        this._flashOverlay();
        return;
      }
      this._go(i, reason);
    }

    // ── Thumbnail rail ────────────────────────────────────────────────────
    //
    // Thumbs are keyed by slide element and reused across _renderRail()
    // calls, so a reorder/delete is an O(changed) DOM shuffle instead of an
    // O(N) teardown-and-re-clone. Each thumb starts as a lightweight shell
    // (num + empty frame); the clone is materialized lazily by an
    // IntersectionObserver when the frame scrolls into (or near) view, so
    // only visible-ish slides pay the clone + image-decode cost.

    _renderRail() {
      if (!this._rail || !this._railEnabled) {
        this._thumbs = [];
        return;
      }
      // FLIP: record each *materialized* thumb's top before the reconcile.
      // Off-screen (non-materialized) thumbs don't need the animation and
      // skipping their getBoundingClientRect saves a forced layout per
      // off-screen thumb on large decks.
      const prevTops = new Map();
      (this._thumbs || []).forEach(({
        thumb,
        slide,
        host
      }) => {
        if (host) prevTops.set(slide, thumb.getBoundingClientRect().top);
      });
      const st = this._rail.scrollTop;

      // Reconcile: reuse thumbs that already exist for a slide, create
      // shells for new slides, drop thumbs for removed slides.
      const bySlide = new Map();
      (this._thumbs || []).forEach(t => bySlide.set(t.slide, t));
      const next = [];
      this._slides.forEach(slide => {
        let t = bySlide.get(slide);
        if (t) bySlide.delete(slide);else t = this._makeThumb(slide);
        next.push(t);
      });
      // Orphans — slides removed since last render.
      bySlide.forEach(t => {
        if (this._railObserver) this._railObserver.unobserve(t.frame);
        t.thumb.remove();
      });
      // Put thumbs into document order to match _slides. insertBefore on
      // an already-correctly-placed node is a no-op, so this is cheap
      // when nothing moved.
      next.forEach((t, i) => {
        const want = t.thumb;
        const at = this._rail.children[i];
        if (at !== want) this._rail.insertBefore(want, at || null);
        t.i = i;
        t.num.textContent = String(i + 1);
        if (t.slide.hasAttribute('data-deck-skip')) t.thumb.setAttribute('data-skip', '');else t.thumb.removeAttribute('data-skip');
      });
      this._thumbs = next;
      this._rail.scrollTop = st;
      if (prevTops.size) {
        const moved = [];
        this._thumbs.forEach(({
          thumb,
          slide
        }) => {
          const old = prevTops.get(slide);
          if (old == null) return;
          const dy = old - thumb.getBoundingClientRect().top;
          if (Math.abs(dy) < 1) return;
          thumb.style.transition = 'none';
          thumb.style.transform = `translateY(${dy}px)`;
          moved.push(thumb);
        });
        if (moved.length) {
          // Commit the inverted positions before flipping the transition
          // on — otherwise the browser coalesces both style writes and
          // nothing animates.
          void this._rail.offsetHeight;
          moved.forEach(t => {
            t.style.transition = 'transform 180ms cubic-bezier(.2,.7,.3,1)';
            t.style.transform = '';
          });
          setTimeout(() => moved.forEach(t => {
            t.style.transition = '';
          }), 220);
        }
      }
      requestAnimationFrame(() => this._scaleThumbs());
      this._syncRail(false);
    }

    /** Create a lightweight thumb shell for one slide. The clone is
     *  materialized later by the IntersectionObserver. Event handlers
     *  look up the thumb's *current* index (via _thumbs.indexOf) so the
     *  same element can be reused across reorders. */
    _makeThumb(slide) {
      const thumb = document.createElement('div');
      thumb.className = 'thumb';
      thumb.tabIndex = 0;
      const num = document.createElement('div');
      num.className = 'num';
      const frame = document.createElement('div');
      frame.className = 'frame';
      thumb.append(num, frame);
      const entry = {
        thumb,
        num,
        frame,
        slide,
        clone: null,
        host: null,
        i: -1
      };
      // entry.i is refreshed on every _renderRail reconcile pass, so
      // handlers read the thumb's current position without an O(N) scan.
      const idx = () => entry.i;
      thumb.addEventListener('click', () => this._go(idx(), 'click'));
      // ↑/↓ step through the rail when a thumb has focus. _go clamps at the
      // ends and _applyIndex→_syncRail scrolls the new current thumb into
      // view; we move focus to it (preventScroll — _syncRail already
      // scrolled) so a held key walks the whole list. stopPropagation keeps
      // this out of the window-level _onKey nav handler.
      thumb.addEventListener('keydown', e => {
        if (e.key !== 'ArrowUp' && e.key !== 'ArrowDown') return;
        if (e.metaKey || e.ctrlKey || e.altKey) return;
        e.preventDefault();
        e.stopPropagation();
        this._go(idx() + (e.key === 'ArrowDown' ? 1 : -1), 'keyboard');
        const cur = this._thumbs && this._thumbs[this._index];
        if (cur) cur.thumb.focus({
          preventScroll: true
        });
      });
      thumb.addEventListener('contextmenu', e => {
        e.preventDefault();
        this._openMenu(idx(), e.clientX, e.clientY);
      });
      thumb.draggable = true;
      thumb.addEventListener('dragstart', e => {
        this._dragFrom = idx();
        thumb.setAttribute('data-dragging', '');
        e.dataTransfer.effectAllowed = 'move';
        try {
          e.dataTransfer.setData('text/plain', String(this._dragFrom));
        } catch (err) {}
      });
      thumb.addEventListener('dragend', () => {
        thumb.removeAttribute('data-dragging');
        this._clearDrop();
        this._dragFrom = null;
      });
      thumb.addEventListener('dragover', e => {
        if (this._dragFrom == null) return;
        e.preventDefault();
        e.dataTransfer.dropEffect = 'move';
        const r = thumb.getBoundingClientRect();
        this._setDrop(idx(), e.clientY < r.top + r.height / 2 ? 'before' : 'after');
      });
      thumb.addEventListener('drop', e => {
        if (this._dragFrom == null) return;
        e.preventDefault();
        const i = idx();
        const r = thumb.getBoundingClientRect();
        let to = e.clientY >= r.top + r.height / 2 ? i + 1 : i;
        if (this._dragFrom < to) to--;
        const from = this._dragFrom;
        this._clearDrop();
        this._dragFrom = null;
        if (to !== from) this._moveSlide(from, to);
      });
      if (this._railObserver) this._railObserver.observe(frame);
      frame.__deckThumb = entry;
      return entry;
    }

    /** Lazily build the clone for a thumb that has scrolled into view. */
    _materialize(entry) {
      if (entry.host) return;
      const dw = this.designWidth,
        dh = this.designHeight;
      let clone = entry.slide.cloneNode(true);
      clone.removeAttribute('id');
      clone.removeAttribute('data-deck-active');
      clone.querySelectorAll('[id]').forEach(el => el.removeAttribute('id'));
      // Neuter heavy media; replace <video> with its poster so the box
      // keeps a visual. <iframe>/<audio> become empty placeholders.
      clone.querySelectorAll('iframe, audio, object, embed').forEach(el => {
        el.removeAttribute('src');
        el.removeAttribute('srcdoc');
        el.removeAttribute('data');
        el.innerHTML = '';
      });
      clone.querySelectorAll('video').forEach(el => {
        if (!el.poster) {
          el.removeAttribute('src');
          el.innerHTML = '';
          return;
        }
        const img = document.createElement('img');
        img.src = el.poster;
        img.alt = '';
        img.style.cssText = el.style.cssText + ';object-fit:cover;width:100%;height:100%;';
        img.className = el.className;
        el.replaceWith(img);
      });
      // Images: defer decode and let the browser pick the smallest
      // srcset candidate for the ~140px thumb. Same-URL clones reuse the
      // slide's decoded bitmap (URL-keyed cache), so the remaining cost
      // is paint/composite — lazy+async keeps that off the main thread.
      clone.querySelectorAll('img').forEach(el => {
        el.loading = 'lazy';
        el.decoding = 'async';
        if (el.srcset) el.sizes = (this._railPx || 188) + 'px';
      });
      // Custom elements inside the slide would have their
      // connectedCallback fire when the clone is appended. Replace them
      // with inert boxes so a component-heavy deck doesn't run N copies
      // of each component's mount logic in the rail. Children are
      // preserved so layout-wrapper elements (<my-column><h2>…</h2>)
      // still show their authored content; the querySelectorAll NodeList
      // is static, so nested custom elements in the moved subtree are
      // still visited on later iterations.
      const neuter = el => {
        const box = document.createElement('div');
        box.style.cssText = (el.getAttribute('style') || '') + ';background:rgba(0,0,0,0.06);border:1px dashed rgba(0,0,0,0.15);';
        box.className = el.className;
        // Preserve theming/i18n hooks so [data-*] / :lang() / [dir]
        // descendant selectors still match the neutered root.
        for (const a of el.attributes) {
          const n = a.name;
          if (n.startsWith('data-') || n.startsWith('aria-') || n === 'lang' || n === 'dir' || n === 'role' || n === 'title') {
            box.setAttribute(n, a.value);
          }
        }
        while (el.firstChild) box.appendChild(el.firstChild);
        return box;
      };
      // querySelectorAll('*') returns descendants only — a custom-element
      // slide root (<my-slide>…</my-slide>) would slip through and upgrade
      // on append. Swap the root first.
      if (clone.tagName.includes('-')) clone = neuter(clone);
      clone.querySelectorAll('*').forEach(el => {
        if (el.tagName.includes('-')) el.replaceWith(neuter(el));
      });
      clone.style.cssText += ';position:absolute;top:0;left:0;transform-origin:0 0;' + 'pointer-events:none;width:' + dw + 'px;height:' + dh + 'px;' + 'box-sizing:border-box;overflow:hidden;visibility:visible;opacity:1;';
      const host = document.createElement('div');
      host.style.cssText = 'position:absolute;inset:0;';
      this._syncThumbHostAttrs(host);
      const sr = host.attachShadow({
        mode: 'open'
      });
      if (this._adoptedSheet) sr.adoptedStyleSheets = [this._adoptedSheet];else {
        const st = document.createElement('style');
        st.textContent = this._authorCss || '';
        sr.appendChild(st);
      }
      sr.appendChild(clone);
      entry.frame.appendChild(host);
      entry.host = host;
      entry.clone = clone;
      if (this._thumbScale) clone.style.transform = 'scale(' + this._thumbScale + ')';
      // Once materialized the IO callback is a no-op early-return —
      // unobserve so scroll doesn't keep firing it.
      if (this._railObserver) this._railObserver.unobserve(entry.frame);
    }

    /** Re-clone a single thumb (live-update path). No-op if the thumb
     *  hasn't been materialized yet — it'll pick up current content when
     *  it scrolls into view. */
    _refreshThumb(slide) {
      const entry = (this._thumbs || []).find(t => t.slide === slide);
      if (!entry || !entry.host) return;
      entry.host.remove();
      entry.host = entry.clone = null;
      this._materialize(entry);
    }
    _scaleThumbs() {
      if (!this._thumbs || !this._thumbs.length) return;
      // Every frame is the same width; if it reads 0 the rail is
      // display:none (noscale / no-rail / presenting / print) — leave the
      // clones as-is and re-run when the rail is revealed.
      const fw = this._thumbs[0].frame.offsetWidth;
      if (!fw) return;
      this._thumbScale = fw / this.designWidth;
      this._thumbs.forEach(({
        clone
      }) => {
        if (clone) clone.style.transform = 'scale(' + this._thumbScale + ')';
      });
    }
    _setDrop(i, where) {
      // dragover fires at pointer-event rate; touch only the previous
      // and new target rather than sweeping all N thumbs.
      const t = this._thumbs && this._thumbs[i];
      if (this._dropOn && this._dropOn !== t) {
        this._dropOn.thumb.removeAttribute('data-drop');
      }
      if (t) t.thumb.setAttribute('data-drop', where);
      this._dropOn = t || null;
    }
    _clearDrop() {
      if (this._dropOn) this._dropOn.thumb.removeAttribute('data-drop');
      this._dropOn = null;
    }
    _syncRail(follow) {
      if (!this._thumbs) return;
      this._thumbs.forEach(({
        thumb
      }, i) => {
        if (i === this._index) {
          thumb.setAttribute('data-current', '');
          if (follow && typeof thumb.scrollIntoView === 'function') {
            thumb.scrollIntoView({
              block: 'nearest'
            });
          }
        } else {
          thumb.removeAttribute('data-current');
        }
      });
    }
    _openMenu(i, x, y) {
      if (!this._menu) return;
      this._menuIndex = i;
      const slide = this._slides[i];
      const skip = slide && slide.hasAttribute('data-deck-skip');
      this._menu.querySelector('[data-act="skip"]').textContent = skip ? 'Unskip slide' : 'Skip slide';
      this._menu.querySelector('[data-act="up"]').disabled = i <= 0;
      this._menu.querySelector('[data-act="down"]').disabled = i >= this._slides.length - 1;
      this._menu.querySelector('[data-act="delete"]').disabled = this._slides.length <= 1;
      // Place, then clamp to viewport after it's measurable.
      this._menu.style.left = x + 'px';
      this._menu.style.top = y + 'px';
      this._menu.setAttribute('data-open', '');
      const r = this._menu.getBoundingClientRect();
      const nx = Math.min(x, window.innerWidth - r.width - 4);
      const ny = Math.min(y, window.innerHeight - r.height - 4);
      this._menu.style.left = Math.max(4, nx) + 'px';
      this._menu.style.top = Math.max(4, ny) + 'px';
    }
    _closeMenu() {
      if (this._menu) this._menu.removeAttribute('data-open');
      this._menuIndex = -1;
    }
    _openConfirm(i) {
      if (!this._confirm) return;
      this._confirmIndex = i;
      this._confirm.querySelector('.title').textContent = 'Delete slide ' + (i + 1) + '?';
      this._confirm.setAttribute('data-open', '');
      const btn = this._confirm.querySelector('.danger');
      if (btn && btn.focus) btn.focus();
    }
    _closeConfirm() {
      if (this._confirm) this._confirm.removeAttribute('data-open');
      this._confirmIndex = -1;
    }
    _emitDeckChange(detail) {
      this.dispatchEvent(new CustomEvent('deckchange', {
        detail,
        bubbles: true,
        composed: true
      }));
    }
    _deleteSlide(i) {
      const slide = this._slides[i];
      if (!slide || this._slides.length <= 1) return;
      const wasCurrent = i === this._index;
      if (i < this._index || wasCurrent && i === this._slides.length - 1) this._index--;
      this._squelchSlotChange = true;
      slide.remove();
      this._emitDeckChange({
        action: 'delete',
        from: i,
        slide
      });
      this._collectSlides();
      this._applyIndex({
        showOverlay: true,
        broadcast: true,
        reason: 'mutation'
      });
    }
    _toggleSkip(i) {
      const slide = this._slides[i];
      if (!slide) return;
      const on = !slide.hasAttribute('data-deck-skip');
      if (on) slide.setAttribute('data-deck-skip', '');else slide.removeAttribute('data-deck-skip');
      if (this._thumbs && this._thumbs[i]) {
        if (on) this._thumbs[i].thumb.setAttribute('data-skip', '');else this._thumbs[i].thumb.removeAttribute('data-skip');
      }
      this._markLastVisible();
      this._emitDeckChange({
        action: on ? 'skip' : 'unskip',
        from: i,
        slide
      });
      // Re-broadcast so the presenter popup's prev/next thumbnails re-pick
      // the nearest non-skipped slide without waiting for a nav event.
      try {
        window.postMessage({
          slideIndexChanged: this._index,
          deckTotal: this._slides.length,
          deckSkipped: this._skippedIndices()
        }, '*');
      } catch (e) {}
    }
    _skippedIndices() {
      const out = [];
      for (let i = 0; i < this._slides.length; i++) {
        if (this._slides[i].hasAttribute('data-deck-skip')) out.push(i);
      }
      return out;
    }
    _moveSlide(i, j) {
      if (j < 0 || j >= this._slides.length || j === i) return;
      const slide = this._slides[i];
      const ref = j < i ? this._slides[j] : this._slides[j].nextSibling;
      // Track the active slide across the reorder so the same content
      // stays on screen.
      const cur = this._index;
      if (cur === i) this._index = j;else if (i < cur && j >= cur) this._index = cur - 1;else if (i > cur && j <= cur) this._index = cur + 1;
      this._squelchSlotChange = true;
      this.insertBefore(slide, ref);
      this._emitDeckChange({
        action: 'move',
        from: i,
        to: j,
        slide
      });
      this._collectSlides();
      this._applyIndex({
        showOverlay: false,
        broadcast: true,
        reason: 'mutation'
      });
    }

    // Public API ------------------------------------------------------------

    /** Current slide index (0-based). */
    get index() {
      return this._index;
    }
    /** Total slide count. */
    get length() {
      return this._slides.length;
    }
    /** Programmatically navigate. */
    goTo(i) {
      this._go(i, 'api');
    }
    next() {
      this._advance(1, 'api');
    }
    prev() {
      this._advance(-1, 'api');
    }
    reset() {
      this._go(0, 'api');
    }
  }
  if (!customElements.get('deck-stage')) {
    customElements.define('deck-stage', DeckStage);
  }
})();
})(); } catch (e) { __ds_ns.__errors.push({ path: "TENET_product_deck/deck-stage.js", error: String((e && e.message) || e) }); }

// animations.jsx
try { (() => {
// animations.jsx
// Reusable animation starter: Stage, Timeline, Sprite, easing helpers.
// Usage (in an HTML file that loads React + Babel):
//
//   <Stage width={1280} height={720} duration={10} background="#f6f4ef">
//     <MyScene />
//   </Stage>
//
// Inside <Stage>, any child can call useTime() to read the current
// playhead (seconds). Or wrap content in <Sprite start={1} end={4}>...</Sprite>
// to only render during that window -- children receive a `localTime` and
// `progress` via the useSprite() hook.
//
// ─────────────────────────────────────────────────────────────────────────────

// ── Easing functions (hand-rolled, Popmotion-style) ─────────────────────────
// All easings take t ∈ [0,1] and return eased t ∈ [0,1] (may overshoot for back/elastic).
const Easing = {
  linear: t => t,
  // Quad
  easeInQuad: t => t * t,
  easeOutQuad: t => t * (2 - t),
  easeInOutQuad: t => t < 0.5 ? 2 * t * t : -1 + (4 - 2 * t) * t,
  // Cubic
  easeInCubic: t => t * t * t,
  easeOutCubic: t => --t * t * t + 1,
  easeInOutCubic: t => t < 0.5 ? 4 * t * t * t : (t - 1) * (2 * t - 2) * (2 * t - 2) + 1,
  // Quart
  easeInQuart: t => t * t * t * t,
  easeOutQuart: t => 1 - --t * t * t * t,
  easeInOutQuart: t => t < 0.5 ? 8 * t * t * t * t : 1 - 8 * --t * t * t * t,
  // Expo
  easeInExpo: t => t === 0 ? 0 : Math.pow(2, 10 * (t - 1)),
  easeOutExpo: t => t === 1 ? 1 : 1 - Math.pow(2, -10 * t),
  easeInOutExpo: t => {
    if (t === 0) return 0;
    if (t === 1) return 1;
    if (t < 0.5) return 0.5 * Math.pow(2, 20 * t - 10);
    return 1 - 0.5 * Math.pow(2, -20 * t + 10);
  },
  // Sine
  easeInSine: t => 1 - Math.cos(t * Math.PI / 2),
  easeOutSine: t => Math.sin(t * Math.PI / 2),
  easeInOutSine: t => -(Math.cos(Math.PI * t) - 1) / 2,
  // Back (overshoot)
  easeOutBack: t => {
    const c1 = 1.70158,
      c3 = c1 + 1;
    return 1 + c3 * Math.pow(t - 1, 3) + c1 * Math.pow(t - 1, 2);
  },
  easeInBack: t => {
    const c1 = 1.70158,
      c3 = c1 + 1;
    return c3 * t * t * t - c1 * t * t;
  },
  easeInOutBack: t => {
    const c1 = 1.70158,
      c2 = c1 * 1.525;
    return t < 0.5 ? Math.pow(2 * t, 2) * ((c2 + 1) * 2 * t - c2) / 2 : (Math.pow(2 * t - 2, 2) * ((c2 + 1) * (t * 2 - 2) + c2) + 2) / 2;
  },
  // Elastic
  easeOutElastic: t => {
    const c4 = 2 * Math.PI / 3;
    if (t === 0) return 0;
    if (t === 1) return 1;
    return Math.pow(2, -10 * t) * Math.sin((t * 10 - 0.75) * c4) + 1;
  }
};

// ── Core interpolation helpers ──────────────────────────────────────────────

// Clamp a value to [min, max]
const clamp = (v, min, max) => Math.max(min, Math.min(max, v));

// interpolate([0, 0.5, 1], [0, 100, 50], ease?) -> fn(t)
// Popmotion-style: linearly maps t across input keyframes to output values,
// with optional easing per segment (single fn or array of fns).
function interpolate(input, output, ease = Easing.linear) {
  return t => {
    if (t <= input[0]) return output[0];
    if (t >= input[input.length - 1]) return output[output.length - 1];
    for (let i = 0; i < input.length - 1; i++) {
      if (t >= input[i] && t <= input[i + 1]) {
        const span = input[i + 1] - input[i];
        const local = span === 0 ? 0 : (t - input[i]) / span;
        const easeFn = Array.isArray(ease) ? ease[i] || Easing.linear : ease;
        const eased = easeFn(local);
        return output[i] + (output[i + 1] - output[i]) * eased;
      }
    }
    return output[output.length - 1];
  };
}

// animate({from, to, start, end, ease})(t) — simpler single-segment tween.
// Returns `from` before `start`, `to` after `end`.
function animate({
  from = 0,
  to = 1,
  start = 0,
  end = 1,
  ease = Easing.easeInOutCubic
}) {
  return t => {
    if (t <= start) return from;
    if (t >= end) return to;
    const local = (t - start) / (end - start);
    return from + (to - from) * ease(local);
  };
}

// ── Timeline context ────────────────────────────────────────────────────────

const TimelineContext = React.createContext({
  time: 0,
  duration: 10,
  playing: false
});
const useTime = () => React.useContext(TimelineContext).time;
const useTimeline = () => React.useContext(TimelineContext);

// ── Sprite ──────────────────────────────────────────────────────────────────
// Renders children only when the playhead is inside [start, end]. Provides
// a sub-context with `localTime` (seconds since start) and `progress` (0..1).
//
//   <Sprite start={2} end={5}>
//     {({ localTime, progress }) => <Thing x={progress * 100} />}
//   </Sprite>
//
// Or as a plain wrapper — children can call useSprite() themselves.

const SpriteContext = React.createContext({
  localTime: 0,
  progress: 0,
  duration: 0
});
const useSprite = () => React.useContext(SpriteContext);
function Sprite({
  start = 0,
  end = Infinity,
  children,
  keepMounted = false
}) {
  const {
    time
  } = useTimeline();
  const visible = time >= start && time <= end;
  if (!visible && !keepMounted) return null;
  const duration = end - start;
  const localTime = Math.max(0, time - start);
  const progress = duration > 0 && isFinite(duration) ? clamp(localTime / duration, 0, 1) : 0;
  const value = {
    localTime,
    progress,
    duration,
    visible
  };
  return /*#__PURE__*/React.createElement(SpriteContext.Provider, {
    value: value
  }, typeof children === 'function' ? children(value) : children);
}

// ── Sample sprite components ────────────────────────────────────────────────

// TextSprite: fades/slides text in on entry, holds, then fades out on exit.
// Props: text, x, y, size, color, font, entryDur, exitDur, align
function TextSprite({
  text,
  x = 0,
  y = 0,
  size = 48,
  color = '#111',
  font = 'Inter, system-ui, sans-serif',
  weight = 600,
  entryDur = 0.45,
  exitDur = 0.35,
  entryEase = Easing.easeOutBack,
  exitEase = Easing.easeInCubic,
  align = 'left',
  letterSpacing = '-0.01em'
}) {
  const {
    localTime,
    duration
  } = useSprite();
  const exitStart = Math.max(0, duration - exitDur);
  let opacity = 1;
  let ty = 0;
  if (localTime < entryDur) {
    const t = entryEase(clamp(localTime / entryDur, 0, 1));
    opacity = t;
    ty = (1 - t) * 16;
  } else if (localTime > exitStart) {
    const t = exitEase(clamp((localTime - exitStart) / exitDur, 0, 1));
    opacity = 1 - t;
    ty = -t * 8;
  }
  const translateX = align === 'center' ? '-50%' : align === 'right' ? '-100%' : '0';
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: x,
      top: y,
      transform: `translate(${translateX}, ${ty}px)`,
      opacity,
      fontFamily: font,
      fontSize: size,
      fontWeight: weight,
      color,
      letterSpacing,
      whiteSpace: 'pre',
      lineHeight: 1.1,
      willChange: 'transform, opacity'
    }
  }, text);
}

// ImageSprite: scales + fades in; optional Ken Burns drift during hold.
function ImageSprite({
  src,
  x = 0,
  y = 0,
  width = 400,
  height = 300,
  entryDur = 0.6,
  exitDur = 0.4,
  kenBurns = false,
  kenBurnsScale = 1.08,
  radius = 12,
  fit = 'cover',
  placeholder = null // {label: string} for striped placeholder
}) {
  const {
    localTime,
    duration
  } = useSprite();
  const exitStart = Math.max(0, duration - exitDur);
  let opacity = 1;
  let scale = 1;
  if (localTime < entryDur) {
    const t = Easing.easeOutCubic(clamp(localTime / entryDur, 0, 1));
    opacity = t;
    scale = 0.96 + 0.04 * t;
  } else if (localTime > exitStart) {
    const t = Easing.easeInCubic(clamp((localTime - exitStart) / exitDur, 0, 1));
    opacity = 1 - t;
    scale = (kenBurns ? kenBurnsScale : 1) + 0.02 * t;
  } else if (kenBurns) {
    const holdSpan = exitStart - entryDur;
    const holdT = holdSpan > 0 ? (localTime - entryDur) / holdSpan : 0;
    scale = 1 + (kenBurnsScale - 1) * holdT;
  }
  const content = placeholder ? /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      height: '100%',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      background: 'repeating-linear-gradient(135deg, #e9e6df 0 10px, #dcd8cf 10px 20px)',
      color: '#6b6458',
      fontFamily: 'JetBrains Mono, ui-monospace, monospace',
      fontSize: 13,
      letterSpacing: '0.04em',
      textTransform: 'uppercase'
    }
  }, placeholder.label || 'image') : /*#__PURE__*/React.createElement("img", {
    src: src,
    alt: "",
    style: {
      width: '100%',
      height: '100%',
      objectFit: fit,
      display: 'block'
    }
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: x,
      top: y,
      width,
      height,
      opacity,
      transform: `scale(${scale})`,
      transformOrigin: 'center',
      borderRadius: radius,
      overflow: 'hidden',
      willChange: 'transform, opacity'
    }
  }, content);
}

// RectSprite: simple rectangle that animates position/size/color via props.
// Useful demo primitive — takes a `render` fn for per-frame customization.
function RectSprite({
  x = 0,
  y = 0,
  width = 100,
  height = 100,
  color = '#111',
  radius = 8,
  entryDur = 0.4,
  exitDur = 0.3,
  render // optional: (ctx) => style overrides
}) {
  const spriteCtx = useSprite();
  const {
    localTime,
    duration
  } = spriteCtx;
  const exitStart = Math.max(0, duration - exitDur);
  let opacity = 1;
  let scale = 1;
  if (localTime < entryDur) {
    const t = Easing.easeOutBack(clamp(localTime / entryDur, 0, 1));
    opacity = clamp(localTime / entryDur, 0, 1);
    scale = 0.4 + 0.6 * t;
  } else if (localTime > exitStart) {
    const t = Easing.easeInQuad(clamp((localTime - exitStart) / exitDur, 0, 1));
    opacity = 1 - t;
    scale = 1 - 0.15 * t;
  }
  const overrides = render ? render(spriteCtx) : {};
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: x,
      top: y,
      width,
      height,
      background: color,
      borderRadius: radius,
      opacity,
      transform: `scale(${scale})`,
      transformOrigin: 'center',
      willChange: 'transform, opacity',
      ...overrides
    }
  });
}
function Stage({
  width = 1280,
  height = 720,
  duration = 10,
  background = '#f6f4ef',
  fps = 60,
  loop = true,
  autoplay = true,
  persistKey = 'animstage',
  children
}) {
  const [time, setTime] = React.useState(() => {
    try {
      const v = parseFloat(localStorage.getItem(persistKey + ':t') || '0');
      return isFinite(v) ? clamp(v, 0, duration) : 0;
    } catch {
      return 0;
    }
  });
  const [playing, setPlaying] = React.useState(autoplay);
  const [hoverTime, setHoverTime] = React.useState(null);
  const [scale, setScale] = React.useState(1);
  const stageRef = React.useRef(null);
  const canvasRef = React.useRef(null);
  const rafRef = React.useRef(null);
  const lastTsRef = React.useRef(null);

  // Persist playhead
  React.useEffect(() => {
    try {
      localStorage.setItem(persistKey + ':t', String(time));
    } catch {}
  }, [time, persistKey]);

  // Auto-scale to fit viewport
  React.useEffect(() => {
    if (!stageRef.current) return;
    const el = stageRef.current;
    const measure = () => {
      const barH = 44; // playback bar height
      const s = Math.min(el.clientWidth / width, (el.clientHeight - barH) / height);
      setScale(Math.max(0.05, s));
    };
    measure();
    const ro = new ResizeObserver(measure);
    ro.observe(el);
    window.addEventListener('resize', measure);
    return () => {
      ro.disconnect();
      window.removeEventListener('resize', measure);
    };
  }, [width, height]);

  // Animation loop
  React.useEffect(() => {
    if (!playing) {
      lastTsRef.current = null;
      return;
    }
    const step = ts => {
      if (lastTsRef.current == null) lastTsRef.current = ts;
      const dt = (ts - lastTsRef.current) / 1000;
      lastTsRef.current = ts;
      setTime(t => {
        let next = t + dt;
        if (next >= duration) {
          if (loop) next = next % duration;else {
            next = duration;
            setPlaying(false);
          }
        }
        return next;
      });
      rafRef.current = requestAnimationFrame(step);
    };
    rafRef.current = requestAnimationFrame(step);
    return () => {
      if (rafRef.current) cancelAnimationFrame(rafRef.current);
      lastTsRef.current = null;
    };
  }, [playing, duration, loop]);

  // Keyboard: space = play/pause, ← → = seek
  React.useEffect(() => {
    const onKey = e => {
      if (e.target && (e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA')) return;
      if (e.code === 'Space') {
        e.preventDefault();
        setPlaying(p => !p);
      } else if (e.code === 'ArrowLeft') {
        setTime(t => clamp(t - (e.shiftKey ? 1 : 0.1), 0, duration));
      } else if (e.code === 'ArrowRight') {
        setTime(t => clamp(t + (e.shiftKey ? 1 : 0.1), 0, duration));
      } else if (e.key === '0' || e.code === 'Home') {
        setTime(0);
      }
    };
    window.addEventListener('keydown', onKey);
    return () => window.removeEventListener('keydown', onKey);
  }, [duration]);
  const displayTime = hoverTime != null ? hoverTime : time;
  const ctxValue = React.useMemo(() => ({
    time: displayTime,
    duration,
    playing,
    setTime,
    setPlaying
  }), [displayTime, duration, playing]);
  return /*#__PURE__*/React.createElement("div", {
    ref: stageRef,
    style: {
      position: 'absolute',
      inset: 0,
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      background: '#0a0a0a',
      fontFamily: 'Inter, system-ui, sans-serif'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      width: '100%',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      overflow: 'hidden',
      minHeight: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    ref: canvasRef,
    style: {
      width,
      height,
      background,
      position: 'relative',
      transform: `scale(${scale})`,
      transformOrigin: 'center',
      flexShrink: 0,
      boxShadow: '0 20px 60px rgba(0,0,0,0.4)',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement(TimelineContext.Provider, {
    value: ctxValue
  }, children))), /*#__PURE__*/React.createElement(PlaybackBar, {
    time: displayTime,
    actualTime: time,
    duration: duration,
    playing: playing,
    onPlayPause: () => setPlaying(p => !p),
    onReset: () => {
      setTime(0);
    },
    onSeek: t => setTime(t),
    onHover: t => setHoverTime(t)
  }));
}

// ── Playback bar ────────────────────────────────────────────────────────────
// Play/pause, return-to-begin, scrub track, time display.
// Uses fixed-width time fields so layout doesn't thrash.

function PlaybackBar({
  time,
  duration,
  playing,
  onPlayPause,
  onReset,
  onSeek,
  onHover
}) {
  const trackRef = React.useRef(null);
  const [dragging, setDragging] = React.useState(false);
  const timeFromEvent = React.useCallback(e => {
    const rect = trackRef.current.getBoundingClientRect();
    const x = clamp((e.clientX - rect.left) / rect.width, 0, 1);
    return x * duration;
  }, [duration]);
  const onTrackMove = e => {
    if (!trackRef.current) return;
    const t = timeFromEvent(e);
    if (dragging) {
      onSeek(t);
    } else {
      onHover(t);
    }
  };
  const onTrackLeave = () => {
    if (!dragging) onHover(null);
  };
  const onTrackDown = e => {
    setDragging(true);
    const t = timeFromEvent(e);
    onSeek(t);
    onHover(null);
  };
  React.useEffect(() => {
    if (!dragging) return;
    const onUp = () => setDragging(false);
    const onMove = e => {
      if (!trackRef.current) return;
      const t = timeFromEvent(e);
      onSeek(t);
    };
    window.addEventListener('mouseup', onUp);
    window.addEventListener('mousemove', onMove);
    return () => {
      window.removeEventListener('mouseup', onUp);
      window.removeEventListener('mousemove', onMove);
    };
  }, [dragging, timeFromEvent, onSeek]);
  const pct = duration > 0 ? time / duration * 100 : 0;
  const fmt = t => {
    const total = Math.max(0, t);
    const m = Math.floor(total / 60);
    const s = Math.floor(total % 60);
    const cs = Math.floor(total * 100 % 100);
    return `${String(m).padStart(1, '0')}:${String(s).padStart(2, '0')}.${String(cs).padStart(2, '0')}`;
  };
  const mono = 'JetBrains Mono, ui-monospace, SFMono-Regular, monospace';
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      padding: '8px 16px',
      background: 'rgba(20,20,20,0.92)',
      borderTop: '1px solid rgba(255,255,255,0.08)',
      width: '100%',
      maxWidth: 680,
      alignSelf: 'center',
      borderRadius: 8,
      color: '#f6f4ef',
      fontFamily: 'Inter, system-ui, sans-serif',
      userSelect: 'none',
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement(IconButton, {
    onClick: onReset,
    title: "Return to start (0)"
  }, /*#__PURE__*/React.createElement("svg", {
    width: "14",
    height: "14",
    viewBox: "0 0 14 14",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M3 2v10M12 2L5 7l7 5V2z",
    stroke: "currentColor",
    strokeWidth: "1.5",
    strokeLinejoin: "round",
    strokeLinecap: "round"
  }))), /*#__PURE__*/React.createElement(IconButton, {
    onClick: onPlayPause,
    title: "Play/pause (space)"
  }, playing ? /*#__PURE__*/React.createElement("svg", {
    width: "14",
    height: "14",
    viewBox: "0 0 14 14",
    fill: "none"
  }, /*#__PURE__*/React.createElement("rect", {
    x: "3",
    y: "2",
    width: "3",
    height: "10",
    fill: "currentColor"
  }), /*#__PURE__*/React.createElement("rect", {
    x: "8",
    y: "2",
    width: "3",
    height: "10",
    fill: "currentColor"
  })) : /*#__PURE__*/React.createElement("svg", {
    width: "14",
    height: "14",
    viewBox: "0 0 14 14",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M3 2l9 5-9 5V2z",
    fill: "currentColor"
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: mono,
      fontSize: 12,
      fontVariantNumeric: 'tabular-nums',
      width: 64,
      textAlign: 'right',
      color: '#f6f4ef'
    }
  }, fmt(time)), /*#__PURE__*/React.createElement("div", {
    ref: trackRef,
    onMouseMove: onTrackMove,
    onMouseLeave: onTrackLeave,
    onMouseDown: onTrackDown,
    style: {
      flex: 1,
      height: 22,
      position: 'relative',
      cursor: 'pointer',
      display: 'flex',
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      height: 4,
      background: 'rgba(255,255,255,0.12)',
      borderRadius: 2
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      width: `${pct}%`,
      height: 4,
      background: 'oklch(72% 0.12 250)',
      borderRadius: 2
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: `${pct}%`,
      top: '50%',
      width: 12,
      height: 12,
      marginLeft: -6,
      marginTop: -6,
      background: '#fff',
      borderRadius: 6,
      boxShadow: '0 2px 4px rgba(0,0,0,0.4)'
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: mono,
      fontSize: 12,
      fontVariantNumeric: 'tabular-nums',
      width: 64,
      textAlign: 'left',
      color: 'rgba(246,244,239,0.55)'
    }
  }, fmt(duration)));
}
function IconButton({
  children,
  onClick,
  title
}) {
  const [hover, setHover] = React.useState(false);
  return /*#__PURE__*/React.createElement("button", {
    onClick: onClick,
    title: title,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => setHover(false),
    style: {
      width: 28,
      height: 28,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      background: hover ? 'rgba(255,255,255,0.12)' : 'rgba(255,255,255,0.04)',
      border: '1px solid rgba(255,255,255,0.1)',
      borderRadius: 6,
      color: '#f6f4ef',
      cursor: 'pointer',
      padding: 0,
      transition: 'background 120ms'
    }
  }, children);
}
Object.assign(window, {
  Easing,
  interpolate,
  animate,
  clamp,
  TimelineContext,
  useTime,
  useTimeline,
  Sprite,
  SpriteContext,
  useSprite,
  TextSprite,
  ImageSprite,
  RectSprite,
  Stage,
  PlaybackBar
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "animations.jsx", error: String((e && e.message) || e) }); }

// competition_deck/deck-stage.js
try { (() => {
/**
 * <deck-stage> — reusable web component for HTML decks.
 *
 * Handles:
 *  (a) speaker notes — reads <script type="application/json" id="speaker-notes">
 *      and posts {slideIndexChanged: N} to the parent window on nav.
 *  (b) keyboard navigation — ←/→, PgUp/PgDn, Space, Home/End, number keys.
 *      On touch devices, tapping the left/right half of the stage goes
 *      prev/next — taps on links, buttons and other interactive slide
 *      content are left alone.
 *  (c) press R to reset to slide 0 (with a tasteful keyboard hint).
 *  (d) bottom-center overlay showing slide count + hints, fades out on idle.
 *  (e) auto-scaling — inner canvas is a fixed design size (default 1920×1080)
 *      scaled with `transform: scale()` to fit the viewport, letterboxed.
 *      Set the `noscale` attribute to render at authored size (1:1) — the
 *      PPTX exporter sets this so its DOM capture sees unscaled geometry.
 *  (f) print — `@media print` lays every slide out as its own page at the
 *      design size, so the browser's Print → Save as PDF produces a clean
 *      one-page-per-slide PDF with no extra setup.
 *  (g) thumbnail rail — resizable left-hand column of per-slide thumbnails
 *      (static clones). Click to navigate; ↑/↓ with a thumbnail focused to
 *      step between slides; drag to reorder; right-click for
 *      Skip / Move up / Move down / Delete (opens a Cancel/Delete confirm
 *      dialog). Drag the rail's right edge to resize; width persists to
 *      localStorage. Skipped slides carry `data-deck-skip`, are dimmed in
 *      the rail, omitted from prev/next navigation, and hidden at print.
 *      The rail is suppressed in presenting mode, in the host's Preview
 *      mode (ViewerMode='none'), on `noscale`, on narrow viewports
 *      (≤640px), and via the `no-rail` attribute. Rail mutations dispatch
 *      a `deckchange`
 *      CustomEvent on the element: detail = {action, from, to, slide}.
 *
 * Slides are HIDDEN, not unmounted. Non-active slides stay in the DOM with
 * `visibility: hidden` + `opacity: 0`, so their state (videos, iframes,
 * form inputs, React trees) is preserved across navigation.
 *
 * Lifecycle event — the component dispatches a `slidechange` CustomEvent on
 * itself whenever the active slide changes (including the initial mount).
 * The event bubbles and composes out of shadow DOM, so you can listen on
 * the <deck-stage> element or on document:
 *
 *   document.querySelector('deck-stage').addEventListener('slidechange', (e) => {
 *     e.detail.index         // new 0-based index
 *     e.detail.previousIndex // previous index, or -1 on init
 *     e.detail.total         // total slide count
 *     e.detail.slide         // the new active slide element
 *     e.detail.previousSlide // the prior slide element, or null on init
 *     e.detail.reason        // 'init' | 'keyboard' | 'click' | 'tap' | 'api'
 *   });
 *
 * Persistence: none at the deck level. The host app keeps the current slide
 * in its own URL (?slide=) and re-delivers it via location.hash on load, so a
 * bare load with no hash always starts at slide 1.
 *
 * Usage:
 *   <style>deck-stage:not(:defined){visibility:hidden}</style>
 *   <deck-stage width="1920" height="1080">
 *     <section data-label="Title">...</section>
 *     <section data-label="Agenda">...</section>
 *   </deck-stage>
 *   <script src="deck-stage.js"></script>
 *
 * The :not(:defined) rule prevents a flash of the first slide at its
 * authored styles before this script runs and attaches the shadow root.
 *
 * Slides are the direct element children of <deck-stage>. Each slide is
 * automatically tagged with:
 *   - data-screen-label="NN Label"   (1-indexed, for comment flow)
 *   - data-om-validate="no_overflowing_text,no_overlapping_text,slide_sized_text"
 */

(() => {
  const DESIGN_W_DEFAULT = 1920;
  const DESIGN_H_DEFAULT = 1080;
  const OVERLAY_HIDE_MS = 1800;
  const VALIDATE_ATTR = 'no_overflowing_text,no_overlapping_text,slide_sized_text';
  const FINE_POINTER_MQ = matchMedia('(hover: hover) and (pointer: fine)');
  const NARROW_MQ = matchMedia('(max-width: 640px)');
  // Slide-authored controls that should keep a tap instead of it navigating.
  const INTERACTIVE_SEL = 'a[href], button, input, select, textarea, summary, label, video[controls], audio[controls], [role="button"], [onclick], [tabindex]:not([tabindex^="-"]), [contenteditable]:not([contenteditable="false" i])';
  const pad2 = n => String(n).padStart(2, '0');

  // Label precedence: data-label → data-screen-label (number stripped) → first heading → "Slide".
  const getSlideLabel = el => {
    const explicit = el.getAttribute('data-label');
    if (explicit) return explicit;
    const existing = el.getAttribute('data-screen-label');
    if (existing) return existing.replace(/^\s*\d+\s*/, '').trim() || existing;
    const h = el.querySelector('h1, h2, h3, [data-title]');
    const t = h && (h.textContent || '').trim().slice(0, 40);
    if (t) return t;
    return 'Slide';
  };
  const stylesheet = `
    :host {
      position: fixed;
      inset: 0;
      display: block;
      background: #000;
      color: #fff;
      font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", Helvetica, Arial, sans-serif;
      overflow: hidden;
      -webkit-tap-highlight-color: transparent;
    }
    /* connectedCallback holds this until document.fonts.ready (capped 2s) so
     * the first visible paint has the deck's real typography + final rail
     * layout. opacity (not visibility) so the active slide can't un-hide
     * itself via the ::slotted([data-deck-active]) visibility:visible rule.
     * Only the stage/rail hide — the black :host background stays, so the
     * iframe doesn't flash the page's default white. */
    :host([data-fonts-pending]) .stage,
    :host([data-fonts-pending]) .rail { opacity: 0; pointer-events: none; }

    .stage {
      position: absolute;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .canvas {
      position: relative;
      transform-origin: center center;
      flex-shrink: 0;
      background: #fff;
      will-change: transform;
    }

    /* Slides live in light DOM (via <slot>) so authored CSS still applies.
       We absolutely position each slotted child to stack them. */
    ::slotted(*) {
      position: absolute !important;
      inset: 0 !important;
      width: 100% !important;
      height: 100% !important;
      box-sizing: border-box !important;
      overflow: hidden;
      opacity: 0;
      pointer-events: none;
      visibility: hidden;
    }
    ::slotted([data-deck-active]) {
      opacity: 1;
      pointer-events: auto;
      visibility: visible;
    }

    .overlay {
      position: fixed;
      left: 50%;
      bottom: 22px;
      transform: translate(-50%, 6px) scale(0.92);
      filter: blur(6px);
      display: flex;
      align-items: center;
      gap: 4px;
      padding: 4px;
      background: #000;
      color: #fff;
      border-radius: 999px;
      font-size: 12px;
      font-feature-settings: "tnum" 1;
      letter-spacing: 0.01em;
      opacity: 0;
      pointer-events: none;
      transition: opacity 260ms ease, transform 260ms cubic-bezier(.2,.8,.2,1), filter 260ms ease;
      transform-origin: center bottom;
      z-index: 2147483000;
      user-select: none;
    }
    .overlay[data-visible] {
      opacity: 1;
      pointer-events: auto;
      transform: translate(-50%, 0) scale(1);
      filter: blur(0);
    }

    .btn {
      appearance: none;
      -webkit-appearance: none;
      background: transparent;
      border: 0;
      margin: 0;
      padding: 0;
      color: inherit;
      font: inherit;
      cursor: default;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      height: 28px;
      min-width: 28px;
      border-radius: 999px;
      color: rgba(255,255,255,0.72);
      transition: background 140ms ease, color 140ms ease;
      -webkit-tap-highlight-color: transparent;
    }
    .btn:hover { background: rgba(255,255,255,0.12); color: #fff; }
    .btn:active { background: rgba(255,255,255,0.18); }
    .btn:focus { outline: none; }
    .btn:focus-visible { outline: none; }
    .btn::-moz-focus-inner { border: 0; }
    .btn svg { width: 14px; height: 14px; display: block; }
    .btn.reset {
      font-size: 11px;
      font-weight: 500;
      letter-spacing: 0.02em;
      padding: 0 10px 0 12px;
      gap: 6px;
      color: rgba(255,255,255,0.72);
    }
    .btn.reset .kbd {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-width: 16px;
      height: 16px;
      padding: 0 4px;
      font-family: ui-monospace, "SF Mono", Menlo, Consolas, monospace;
      font-size: 10px;
      line-height: 1;
      color: rgba(255,255,255,0.88);
      background: rgba(255,255,255,0.12);
      border-radius: 4px;
    }

    .count {
      font-variant-numeric: tabular-nums;
      color: #fff;
      font-weight: 500;
      padding: 0 8px;
      min-width: 42px;
      text-align: center;
      font-size: 12px;
    }
    .count .sep { color: rgba(255,255,255,0.45); margin: 0 3px; font-weight: 400; }
    .count .total { color: rgba(255,255,255,0.55); }

    .divider {
      width: 1px;
      height: 14px;
      background: rgba(255,255,255,0.18);
      margin: 0 2px;
    }

    /* ── Thumbnail rail ──────────────────────────────────────────────────
       Fixed column on the left; each thumbnail is a static deep-clone of
       the light-DOM slide scaled into a 16:9 (or design-aspect) frame. The
       stage re-fits around it (see _fit); hidden during present / noscale
       / print so capture geometry and fullscreen output are unchanged. */
    .rail {
      position: fixed;
      left: 0;
      top: 0;
      bottom: 0;
      width: var(--deck-rail-w, 188px);
      background: #141414;
      border-right: 1px solid rgba(255,255,255,0.08);
      overflow-y: auto;
      overflow-x: hidden;
      padding: 12px 10px;
      box-sizing: border-box;
      display: flex;
      flex-direction: column;
      gap: 12px;
      z-index: 2147482500;
      scrollbar-width: thin;
      scrollbar-color: rgba(255,255,255,0.18) transparent;
    }
    .rail::-webkit-scrollbar { width: 8px; }
    .rail::-webkit-scrollbar-track { background: transparent; margin: 2px; }
    .rail::-webkit-scrollbar-thumb {
      background: rgba(255,255,255,0.18);
      border-radius: 4px;
      border: 2px solid transparent;
      background-clip: content-box;
    }
    .rail::-webkit-scrollbar-thumb:hover {
      background: rgba(255,255,255,0.28);
      border: 2px solid transparent;
      background-clip: content-box;
    }
    :host([no-rail]) .rail,
    :host([noscale]) .rail { display: none; }
    .rail[data-presenting] { display: none; }
    @media (max-width: 640px) {
      .rail, .rail-resize { display: none; }
    }
    /* User-driven show/hide (the TweaksPanel toggle) slides instead of
       popping. Transitions are gated on :host([data-rail-anim]) — set only
       for the 200ms around the toggle — so window-resize and rail-width
       drag (which also call _fit) don't lag behind the cursor. */
    .rail[data-user-hidden] { transform: translateX(-100%); }
    :host([data-rail-anim]) .rail { transition: transform 200ms cubic-bezier(.3,.7,.4,1); }
    :host([data-rail-anim]) .stage { transition: left 200ms cubic-bezier(.3,.7,.4,1); }
    :host([data-rail-anim]) .canvas { transition: transform 200ms cubic-bezier(.3,.7,.4,1); }
    /* transition shorthand replaces rather than merges — repeat the base
       .overlay opacity/transform/filter transitions so visibility changes
       during the 200ms toggle window still fade instead of popping. */
    :host([data-rail-anim]) .overlay {
      transition: margin-left 200ms cubic-bezier(.3,.7,.4,1),
                  opacity 260ms ease,
                  transform 260ms cubic-bezier(.2,.8,.2,1),
                  filter 260ms ease;
    }

    .thumb {
      position: relative;
      display: flex;
      align-items: flex-start;
      gap: 8px;
      cursor: pointer;
      user-select: none;
    }
    .thumb .num {
      width: 16px;
      flex-shrink: 0;
      font-size: 11px;
      font-weight: 500;
      text-align: right;
      color: rgba(255,255,255,0.55);
      padding-top: 2px;
      font-variant-numeric: tabular-nums;
    }
    .thumb .frame {
      position: relative;
      flex: 1;
      min-width: 0;
      aspect-ratio: var(--deck-aspect);
      background: #fff;
      border-radius: 4px;
      outline: 2px solid transparent;
      outline-offset: 0;
      overflow: hidden;
      transition: outline-color 120ms ease;
    }
    .thumb:hover .frame { outline-color: rgba(255,255,255,0.25); }
    .thumb { outline: none; }
    .thumb:focus-visible .frame { outline-color: rgba(255,255,255,0.5); }
    .thumb[data-current] .num { color: #fff; }
    .thumb[data-current] .frame { outline-color: #D97757; }
    .thumb[data-dragging] { opacity: 0.35; }
    .thumb::before {
      content: '';
      position: absolute;
      left: 24px;
      right: 0;
      height: 3px;
      border-radius: 2px;
      background: #D97757;
      opacity: 0;
      pointer-events: none;
    }
    .thumb[data-drop="before"]::before { top: -8px; opacity: 1; }
    .thumb[data-drop="after"]::before { bottom: -8px; opacity: 1; }
    .thumb[data-skip] .frame { opacity: 0.35; }
    .thumb[data-skip] .frame::after {
      content: 'Skipped';
      position: absolute;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(0,0,0,0.45);
      color: #fff;
      font-size: 10px;
      font-weight: 500;
      letter-spacing: 0.04em;
    }

    .ctxmenu {
      position: fixed;
      min-width: 150px;
      padding: 4px;
      background: #242424;
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 7px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.45);
      z-index: 2147483100;
      display: none;
      font-size: 12px;
    }
    .ctxmenu[data-open] { display: block; }
    .ctxmenu button {
      display: block;
      width: 100%;
      appearance: none;
      border: 0;
      background: transparent;
      color: #e8e8e8;
      font: inherit;
      text-align: left;
      padding: 6px 10px;
      border-radius: 4px;
      cursor: pointer;
    }
    .ctxmenu button:hover:not(:disabled) { background: rgba(255,255,255,0.08); }
    .ctxmenu button:disabled { opacity: 0.35; cursor: default; }
    .ctxmenu hr {
      border: 0;
      border-top: 1px solid rgba(255,255,255,0.1);
      margin: 4px 2px;
    }

    .rail-resize {
      position: fixed;
      left: calc(var(--deck-rail-w, 188px) - 3px);
      top: 0;
      bottom: 0;
      width: 6px;
      cursor: col-resize;
      z-index: 2147482600;
      touch-action: none;
    }
    .rail-resize:hover,
    .rail-resize[data-dragging] { background: rgba(255,255,255,0.12); }
    :host([no-rail]) .rail-resize,
    :host([noscale]) .rail-resize,
    .rail[data-presenting] + .rail-resize,
    .rail[data-user-hidden] + .rail-resize { display: none; }

    /* Delete-confirm popup — matches the SPA's ConfirmDialog layout
       (title + message body, depressed footer with Cancel / Delete). */
    .confirm-backdrop {
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.45);
      z-index: 2147483200;
      display: none;
      align-items: center;
      justify-content: center;
    }
    .confirm-backdrop[data-open] { display: flex; }
    .confirm {
      width: 320px;
      max-width: calc(100vw - 32px);
      background: #2a2a2a;
      color: #e8e8e8;
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 12px;
      box-shadow: 0 12px 32px rgba(0,0,0,0.5);
      overflow: hidden;
      font-family: inherit;
      animation: deck-confirm-in 0.18s ease;
    }
    @keyframes deck-confirm-in {
      from { opacity: 0; transform: scale(0.96); }
      to { opacity: 1; transform: scale(1); }
    }
    .confirm .body { padding: 20px 20px 16px; }
    .confirm .title { font-size: 14px; font-weight: 600; margin-bottom: 4px; }
    .confirm .msg { font-size: 13px; line-height: 1.5; color: rgba(255,255,255,0.65); }
    .confirm .footer {
      padding: 14px 20px;
      background: #1f1f1f;
      border-top: 1px solid rgba(255,255,255,0.08);
      display: flex;
      justify-content: flex-end;
      gap: 8px;
    }
    .confirm button {
      appearance: none;
      font: inherit;
      font-size: 13px;
      font-weight: 500;
      padding: 8px 16px;
      border-radius: 8px;
      cursor: pointer;
    }
    .confirm .cancel {
      background: transparent;
      border: 0;
      color: rgba(255,255,255,0.8);
    }
    .confirm .cancel:hover { background: rgba(255,255,255,0.08); }
    .confirm .danger {
      background: #c96442;
      border: 1px solid rgba(0,0,0,0.15);
      color: #fff;
      box-shadow: 0 1px 3px rgba(166,50,68,0.3), 0 2px 6px rgba(166,50,68,0.18);
    }
    .confirm .danger:hover { background: #b5563a; }

    /* ── Print: one page per slide, no chrome ────────────────────────────
       The screen layout stacks every slide at inset:0 inside a scaled
       canvas; for print we want them in document flow at the authored
       design size so the browser paginates one slide per sheet. The
       @page size is set from the width/height attributes via the inline
       <style id="deck-stage-print-page"> that connectedCallback injects
       into <head> (the @page at-rule has no effect inside shadow DOM). */
    @media print {
      :host {
        position: static;
        inset: auto;
        background: none;
        overflow: visible;
        color: inherit;
      }
      .stage { position: static; display: block; }
      .canvas {
        transform: none !important;
        width: auto !important;
        height: auto !important;
        background: none;
        will-change: auto;
      }
      ::slotted(*) {
        position: relative !important;
        inset: auto !important;
        width: var(--deck-design-w) !important;
        height: var(--deck-design-h) !important;
        box-sizing: border-box !important;
        opacity: 1 !important;
        visibility: visible !important;
        pointer-events: auto;
        break-after: page;
        page-break-after: always;
        break-inside: avoid;
        overflow: hidden;
      }
      /* :last-child alone isn't enough once data-deck-skip hides the
         trailing slide(s) — the last *visible* slide still carries
         break-after:page and prints a blank sheet. _markLastVisible()
         maintains data-deck-last-visible on the last non-skipped slide. */
      ::slotted(*:last-child),
      ::slotted([data-deck-last-visible]) {
        break-after: auto;
        page-break-after: auto;
      }
      ::slotted([data-deck-skip]) { display: none !important; }
      .overlay, .rail, .rail-resize, .ctxmenu, .confirm-backdrop { display: none !important; }
    }
  `;
  class DeckStage extends HTMLElement {
    static get observedAttributes() {
      return ['width', 'height', 'noscale', 'no-rail'];
    }
    constructor() {
      super();
      this._root = this.attachShadow({
        mode: 'open'
      });
      this._index = 0;
      this._slides = [];
      this._notes = [];
      this._hideTimer = null;
      this._mouseIdleTimer = null;
      this._menuIndex = -1;
      this._onKey = this._onKey.bind(this);
      this._onResize = this._onResize.bind(this);
      this._onSlotChange = this._onSlotChange.bind(this);
      this._onMouseMove = this._onMouseMove.bind(this);
      this._onTap = this._onTap.bind(this);
      this._onMessage = this._onMessage.bind(this);
      // Capture-phase close so a click anywhere dismisses the menu, but
      // ignore clicks that land inside the menu itself — otherwise the
      // capture handler runs before the menu's own (bubble) handler and
      // clears _menuIndex out from under it.
      this._onDocClick = e => {
        if (this._menu && e.composedPath && e.composedPath().includes(this._menu)) return;
        this._closeMenu();
      };
    }
    get designWidth() {
      return parseInt(this.getAttribute('width'), 10) || DESIGN_W_DEFAULT;
    }
    get designHeight() {
      return parseInt(this.getAttribute('height'), 10) || DESIGN_H_DEFAULT;
    }
    connectedCallback() {
      // Presenter-view popup loads deckUrl?_snthumb=...#N for its prev/cur/
      // next thumbnails — the rail has no business rendering inside those
      // (wrong scale, and it offsets the stage so the thumb shows a gutter).
      if (/[?&]_snthumb=/.test(location.search)) this.setAttribute('no-rail', '');
      this._render();
      this._loadNotes();
      this._syncPrintPageRule();
      window.addEventListener('keydown', this._onKey);
      window.addEventListener('resize', this._onResize);
      window.addEventListener('mousemove', this._onMouseMove, {
        passive: true
      });
      window.addEventListener('message', this._onMessage);
      window.addEventListener('click', this._onDocClick, true);
      this.addEventListener('click', this._onTap);
      // Initial collection + layout happens via slotchange, which fires on mount.
      this._enableRail();
      // Hold the stage hidden until webfonts are ready so the first visible
      // paint has the deck's real typography — the :not(:defined) guard in
      // the page HTML only covers custom-element upgrade, not font load.
      // Capped so a 404'd font URL can't blank the deck indefinitely.
      this.setAttribute('data-fonts-pending', '');
      const reveal = () => this.removeAttribute('data-fonts-pending');
      // rAF first: fonts.ready is a pre-resolved promise until layout has
      // resolved the slotted text's font-family and pushed a FontFace into
      // 'loading'. Reading it here in connectedCallback (parse-time) would
      // settle the race in a microtask before any font fetch starts.
      requestAnimationFrame(() => {
        Promise.race([document.fonts ? document.fonts.ready : Promise.resolve(), new Promise(r => setTimeout(r, 2000))]).then(reveal, reveal);
      });
    }
    _enableRail() {
      // Idempotent — older host builds still post __omelette_rail_enabled.
      // no-rail guard keeps the observers/stylesheet walk off the cheap path
      // for presenter-popup thumbnail iframes (up to 9 per view).
      if (this._railEnabled || this.hasAttribute('no-rail')) return;
      this._railEnabled = true;
      // Per-viewer preference — restored alongside rail width. Default on;
      // only a stored '0' (from the TweaksPanel toggle) hides it.
      this._railVisible = true;
      try {
        if (localStorage.getItem('deck-stage.railVisible') === '0') this._railVisible = false;
      } catch (e) {}
      // Live thumbnail updates: watch the light-DOM slides for content
      // edits and re-clone just the affected thumb(s), debounced. Ignore
      // the data-deck-* / data-screen-label / data-om-validate attributes
      // this component itself writes so nav and skip don't trigger
      // spurious refreshes.
      const OWN_ATTRS = /^data-(deck-|screen-label$|om-validate$)/;
      this._liveDirty = new Set();
      this._liveObserver = new MutationObserver(records => {
        for (const r of records) {
          if (r.type === 'attributes' && OWN_ATTRS.test(r.attributeName || '')) continue;
          let n = r.target;
          while (n && n.parentElement !== this) n = n.parentElement;
          if (n && this._slideSet && this._slideSet.has(n)) this._liveDirty.add(n);
        }
        if (this._liveDirty.size && !this._liveTimer) {
          this._liveTimer = setTimeout(() => {
            this._liveTimer = null;
            this._liveDirty.forEach(s => this._refreshThumb(s));
            this._liveDirty.clear();
          }, 200);
        }
      });
      this._liveObserver.observe(this, {
        subtree: true,
        childList: true,
        characterData: true,
        attributes: true
      });
      // Lazy thumbnail materialization — clone the slide only when its
      // frame scrolls into (or near) the rail viewport. rootMargin gives
      // ~4 thumbs of pre-load so fast scrolling doesn't flash blanks.
      this._railObserver = new IntersectionObserver(entries => {
        entries.forEach(e => {
          if (e.isIntersecting && e.target.__deckThumb) {
            this._materialize(e.target.__deckThumb);
          }
        });
      }, {
        root: this._rail,
        rootMargin: '400px 0px'
      });
      // Tweaks typically change CSS vars / attrs OUTSIDE <deck-stage>
      // (on <html>, <body>, a wrapper div, or a <style> tag), which
      // _liveObserver can't see. Re-snapshot author CSS (constructable
      // sheet is shared by reference, so one replaceSync updates every
      // thumb shadow root) and re-sync each thumb host's attrs + custom
      // properties. In-slide DOM mutations are _liveObserver's job.
      // Debounced so slider drags don't thrash.
      this._onTweakChange = () => {
        clearTimeout(this._tweakTimer);
        this._tweakTimer = setTimeout(() => {
          this._snapshotAuthorCss();
          // One getComputedStyle for the whole batch — each
          // getPropertyValue read below reuses the same computed style
          // as long as nothing invalidates layout between thumbs.
          const cs = getComputedStyle(this);
          (this._thumbs || []).forEach(t => {
            if (t.host) this._syncThumbHostAttrs(t.host, cs);
          });
        }, 120);
      };
      window.addEventListener('tweakchange', this._onTweakChange);
      this._snapshotAuthorCss();
      // Build the rail now that it's enabled — slotchange already fired,
      // so _renderRail's early-return skipped the initial build.
      this._syncRailHidden();
      this._renderRail();
      this._fit();
    }

    /** Snapshot document stylesheets into a constructable sheet that each
     *  thumbnail's nested shadow root adopts — so author CSS styles the
     *  cloned slide content without touching this component's chrome.
     *  Cross-origin sheets throw on .cssRules — skip them. Re-callable:
     *  the existing constructable sheet is reused via replaceSync so every
     *  already-adopted shadow root picks up the fresh CSS without re-adopt. */
    _snapshotAuthorCss() {
      // :root in an adopted sheet inside a shadow root matches nothing
      // (only the document root qualifies), so author rules like
      // `:root[data-voice="modern"] .serif` never reach the clones.
      // Rewrite :root → :host and mirror <html>'s data-*/class/lang onto
      // each thumb host (see _syncThumbHostAttrs) so the same selectors
      // match inside the thumbnail's shadow tree.
      const authorCss = Array.from(document.styleSheets).map(sh => {
        try {
          return Array.from(sh.cssRules).map(r => r.cssText).join('\n');
        } catch (e) {
          return '';
        }
      }).join('\n')
      // The shadow host is featureless outside the functional :host(...)
      // form, so any compound on :root — [attr], .class, #id, :pseudo —
      // must become :host(<compound>) not :host<compound>. Same for the
      // html type selector (Tailwind class-strategy dark mode emits
      // html.dark; Pico uses html[data-theme]), which has nothing to
      // match inside the thumb's shadow tree.
      .replace(/:root((?:\[[^\]]*\]|[.#][-\w]+|:[-\w]+(?:\([^)]*\))?)+)/g, ':host($1)').replace(/:root\b/g, ':host').replace(/(^|[\s,>~+(}])html((?:\[[^\]]*\]|[.#][-\w]+|:[-\w]+(?:\([^)]*\))?)+)(?![-\w])/g, '$1:host($2)').replace(/(^|[\s,>~+(}])html(?![-\w])/g, '$1:host');
      // Every custom property the author references. _syncThumbHostAttrs
      // mirrors each one's *computed* value at <deck-stage> onto the
      // thumb host so the live value wins over the :host default above
      // regardless of which ancestor the tweak wrote to (<html>, <body>,
      // a wrapper div, or the deck-stage element itself all inherit
      // down to getComputedStyle(this)).
      this._authorVars = new Set(authorCss.match(/--[\w-]+/g) || []);
      try {
        if (!this._adoptedSheet) this._adoptedSheet = new CSSStyleSheet();
        this._adoptedSheet.replaceSync(authorCss);
      } catch (e) {
        this._adoptedSheet = null;
        this._authorCss = authorCss;
      }
    }
    _syncThumbHostAttrs(host, cs) {
      const de = document.documentElement;
      // setAttribute overwrites but can't delete — an attr removed from
      // <html> (toggleAttribute off, classList emptied) would linger on
      // the host and :host([data-*]) / :host(.foo) rules would keep
      // matching. Remove stale mirrored attrs first; iterate backward
      // because removeAttribute mutates the live NamedNodeMap.
      for (let i = host.attributes.length - 1; i >= 0; i--) {
        const n = host.attributes[i].name;
        if ((n.startsWith('data-') || n === 'class' || n === 'lang') && !de.hasAttribute(n)) {
          host.removeAttribute(n);
        }
      }
      for (const a of de.attributes) {
        if (a.name.startsWith('data-') || a.name === 'class' || a.name === 'lang') {
          host.setAttribute(a.name, a.value);
        }
      }
      // The :root→:host rewrite in _snapshotAuthorCss pins each custom
      // property to its stylesheet default on the thumb host, shadowing
      // the live value that would otherwise inherit. Tweaks can write the
      // live value on any ancestor — <html>, <body>, a wrapper div, the
      // deck-stage element — so read it as the *computed* value at
      // <deck-stage> (which sees the whole inheritance chain) rather than
      // trying to guess which element the author wrote to. Inline on the
      // host beats the :host{} rule. remove-stale covers vars dropped
      // from the stylesheet between snapshots.
      const vars = this._authorVars || new Set();
      for (let i = host.style.length - 1; i >= 0; i--) {
        const p = host.style[i];
        if (p.startsWith('--') && !vars.has(p)) host.style.removeProperty(p);
      }
      const live = cs || getComputedStyle(this);
      vars.forEach(p => {
        const v = live.getPropertyValue(p);
        if (v) host.style.setProperty(p, v.trim());else host.style.removeProperty(p);
      });
    }
    disconnectedCallback() {
      window.removeEventListener('keydown', this._onKey);
      window.removeEventListener('resize', this._onResize);
      window.removeEventListener('mousemove', this._onMouseMove);
      window.removeEventListener('message', this._onMessage);
      window.removeEventListener('click', this._onDocClick, true);
      this.removeEventListener('click', this._onTap);
      if (this._hideTimer) clearTimeout(this._hideTimer);
      if (this._mouseIdleTimer) clearTimeout(this._mouseIdleTimer);
      if (this._liveTimer) clearTimeout(this._liveTimer);
      if (this._tweakTimer) clearTimeout(this._tweakTimer);
      if (this._railAnimTimer) clearTimeout(this._railAnimTimer);
      if (this._scaleRaf) cancelAnimationFrame(this._scaleRaf);
      if (this._liveObserver) this._liveObserver.disconnect();
      if (this._railObserver) this._railObserver.disconnect();
      if (this._onTweakChange) window.removeEventListener('tweakchange', this._onTweakChange);
    }
    attributeChangedCallback() {
      if (this._canvas) {
        this._canvas.style.width = this.designWidth + 'px';
        this._canvas.style.height = this.designHeight + 'px';
        this._canvas.style.setProperty('--deck-design-w', this.designWidth + 'px');
        this._canvas.style.setProperty('--deck-design-h', this.designHeight + 'px');
        if (this._rail) {
          this._rail.style.setProperty('--deck-aspect', this.designWidth + '/' + this.designHeight);
        }
        this._fit();
        this._scaleThumbs();
        this._syncPrintPageRule();
      }
    }
    _render() {
      const style = document.createElement('style');
      style.textContent = stylesheet;
      const stage = document.createElement('div');
      stage.className = 'stage';
      const canvas = document.createElement('div');
      canvas.className = 'canvas';
      canvas.style.width = this.designWidth + 'px';
      canvas.style.height = this.designHeight + 'px';
      canvas.style.setProperty('--deck-design-w', this.designWidth + 'px');
      canvas.style.setProperty('--deck-design-h', this.designHeight + 'px');
      const slot = document.createElement('slot');
      slot.addEventListener('slotchange', this._onSlotChange);
      canvas.appendChild(slot);
      stage.appendChild(canvas);

      // Overlay: compact, solid black, with clickable controls.
      const overlay = document.createElement('div');
      overlay.className = 'overlay export-hidden';
      overlay.setAttribute('role', 'toolbar');
      overlay.setAttribute('aria-label', 'Deck controls');
      overlay.setAttribute('data-omelette-chrome', '');
      overlay.innerHTML = `
        <button class="btn prev" type="button" aria-label="Previous slide" title="Previous (←)">
          <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M10 3L5 8l5 5"/></svg>
        </button>
        <span class="count" aria-live="polite"><span class="current">1</span><span class="sep">/</span><span class="total">1</span></span>
        <button class="btn next" type="button" aria-label="Next slide" title="Next (→)">
          <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 3l5 5-5 5"/></svg>
        </button>
        <span class="divider"></span>
        <button class="btn reset" type="button" aria-label="Reset to first slide" title="Reset (R)">Reset<span class="kbd">R</span></button>
      `;
      overlay.querySelector('.prev').addEventListener('click', () => this._advance(-1, 'click'));
      overlay.querySelector('.next').addEventListener('click', () => this._advance(1, 'click'));
      overlay.querySelector('.reset').addEventListener('click', () => this._go(0, 'click'));

      // Thumbnail rail + context menu. Thumbnails are populated in
      // _renderRail() after _collectSlides().
      const rail = document.createElement('div');
      rail.className = 'rail export-hidden';
      rail.setAttribute('data-omelette-chrome', '');
      rail.style.setProperty('--deck-aspect', this.designWidth + '/' + this.designHeight);
      // Edge auto-scroll while dragging a thumb near the rail's top/bottom
      // so off-screen drop targets are reachable. Native dragover fires
      // continuously while the pointer is stationary, so a per-event nudge
      // (ramped by edge proximity) is enough — no rAF loop needed.
      rail.addEventListener('dragover', e => {
        if (this._dragFrom == null) return;
        const r = rail.getBoundingClientRect();
        const EDGE = 40;
        const dt = e.clientY - r.top;
        const db = r.bottom - e.clientY;
        if (dt < EDGE) rail.scrollTop -= Math.ceil((EDGE - dt) / 3);else if (db < EDGE) rail.scrollTop += Math.ceil((EDGE - db) / 3);
      });
      const menu = document.createElement('div');
      menu.className = 'ctxmenu export-hidden';
      menu.setAttribute('data-omelette-chrome', '');
      menu.innerHTML = `
        <button type="button" data-act="skip">Skip slide</button>
        <button type="button" data-act="up">Move up</button>
        <button type="button" data-act="down">Move down</button>
        <hr>
        <button type="button" data-act="delete">Delete slide</button>
      `;
      menu.addEventListener('click', e => {
        const act = e.target && e.target.getAttribute && e.target.getAttribute('data-act');
        if (!act) return;
        const i = this._menuIndex;
        this._closeMenu();
        if (act === 'skip') this._toggleSkip(i);else if (act === 'up') this._moveSlide(i, i - 1);else if (act === 'down') this._moveSlide(i, i + 1);else if (act === 'delete') this._openConfirm(i);
      });
      menu.addEventListener('contextmenu', e => e.preventDefault());

      // Rail resize handle — drag to set --deck-rail-w, persisted to
      // localStorage so the width survives reloads.
      const resize = document.createElement('div');
      resize.className = 'rail-resize export-hidden';
      resize.setAttribute('data-omelette-chrome', '');
      resize.addEventListener('pointerdown', e => {
        e.preventDefault();
        resize.setPointerCapture(e.pointerId);
        resize.setAttribute('data-dragging', '');
        const move = ev => this._setRailWidth(ev.clientX);
        const up = () => {
          resize.removeEventListener('pointermove', move);
          resize.removeEventListener('pointerup', up);
          resize.removeEventListener('pointercancel', up);
          resize.removeAttribute('data-dragging');
          try {
            localStorage.setItem('deck-stage.railWidth', String(this._railPx));
          } catch (err) {}
        };
        resize.addEventListener('pointermove', move);
        resize.addEventListener('pointerup', up);
        resize.addEventListener('pointercancel', up);
      });

      // Delete-confirm dialog — mirrors the SPA's ConfirmDialog layout.
      const confirm = document.createElement('div');
      confirm.className = 'confirm-backdrop export-hidden';
      confirm.setAttribute('data-omelette-chrome', '');
      confirm.innerHTML = `
        <div class="confirm" role="dialog" aria-modal="true">
          <div class="body">
            <div class="title">Delete slide?</div>
            <div class="msg">This slide will be removed from the deck.</div>
          </div>
          <div class="footer">
            <button type="button" class="cancel">Cancel</button>
            <button type="button" class="danger">Delete</button>
          </div>
        </div>
      `;
      confirm.addEventListener('click', e => {
        if (e.target === confirm) this._closeConfirm();
      });
      confirm.querySelector('.cancel').addEventListener('click', () => this._closeConfirm());
      confirm.querySelector('.danger').addEventListener('click', () => {
        const i = this._confirmIndex;
        this._closeConfirm();
        this._deleteSlide(i);
      });
      this._root.append(style, rail, resize, stage, overlay, menu, confirm);
      this._canvas = canvas;
      this._stage = stage;
      this._slot = slot;
      this._overlay = overlay;
      this._rail = rail;
      this._resize = resize;
      this._menu = menu;
      this._confirm = confirm;
      this._countEl = overlay.querySelector('.current');
      this._totalEl = overlay.querySelector('.total');

      // Restore persisted rail width.
      let rw = 188;
      try {
        const s = localStorage.getItem('deck-stage.railWidth');
        if (s) rw = parseInt(s, 10) || rw;
      } catch (err) {}
      this._setRailWidth(rw);
      this._syncRailHidden();
    }
    _setRailWidth(px) {
      const w = Math.max(120, Math.min(360, Math.round(px)));
      this._railPx = w;
      this.style.setProperty('--deck-rail-w', w + 'px');
      this._fit();
      // _scaleThumbs forces a sync layout (frame.offsetWidth) then writes
      // N transforms. During a resize drag this runs per-pointermove;
      // coalesce to one per frame.
      if (!this._scaleRaf) {
        this._scaleRaf = requestAnimationFrame(() => {
          this._scaleRaf = null;
          this._scaleThumbs();
        });
      }
    }

    /** @page must live in the document stylesheet — it's a no-op inside
     *  shadow DOM. Inject/update a single <head> style tag so the print
     *  sheet matches the design size and Save-as-PDF yields one slide per
     *  page with no margins. */
    _syncPrintPageRule() {
      const id = 'deck-stage-print-page';
      let tag = document.getElementById(id);
      if (!tag) {
        tag = document.createElement('style');
        tag.id = id;
        document.head.appendChild(tag);
      }
      tag.textContent = '@page { size: ' + this.designWidth + 'px ' + this.designHeight + 'px; margin: 0; } ' + '@media print { html, body { margin: 0 !important; padding: 0 !important; background: none !important; overflow: visible !important; height: auto !important; } ' + '* { -webkit-print-color-adjust: exact; print-color-adjust: exact; } }';
    }
    _onSlotChange() {
      // Rail mutations (delete/move) already reconcile synchronously and
      // emit slidechange with reason 'api'; skip the async slotchange that
      // would otherwise re-broadcast with reason 'init'.
      if (this._squelchSlotChange) {
        this._squelchSlotChange = false;
        return;
      }
      this._collectSlides();
      this._restoreIndex();
      this._applyIndex({
        showOverlay: false,
        broadcast: true,
        reason: 'init'
      });
      this._fit();
    }
    _collectSlides() {
      const assigned = this._slot.assignedElements({
        flatten: true
      });
      this._slides = assigned.filter(el => {
        // Skip template/style/script nodes even if someone slots them.
        const tag = el.tagName;
        return tag !== 'TEMPLATE' && tag !== 'SCRIPT' && tag !== 'STYLE';
      });
      this._slideSet = new Set(this._slides);
      this._slides.forEach((slide, i) => {
        const n = i + 1;
        slide.setAttribute('data-screen-label', `${pad2(n)} ${getSlideLabel(slide)}`);

        // Validation attribute for comment flow / auto-checks.
        if (!slide.hasAttribute('data-om-validate')) {
          slide.setAttribute('data-om-validate', VALIDATE_ATTR);
        }
        slide.setAttribute('data-deck-slide', String(i));
      });
      if (this._totalEl) this._totalEl.textContent = String(this._slides.length || 1);
      if (this._index >= this._slides.length) this._index = Math.max(0, this._slides.length - 1);
      this._markLastVisible();
      this._renderRail();
    }

    /** Tag the last non-skipped slide so print CSS can drop its
     *  break-after (see the @media print comment above — :last-child
     *  alone matches a hidden skipped slide). */
    _markLastVisible() {
      let last = null;
      this._slides.forEach(s => {
        s.removeAttribute('data-deck-last-visible');
        if (!s.hasAttribute('data-deck-skip')) last = s;
      });
      if (last) last.setAttribute('data-deck-last-visible', '');
    }
    _loadNotes() {
      const tag = document.getElementById('speaker-notes');
      if (!tag) {
        this._notes = [];
        return;
      }
      try {
        const parsed = JSON.parse(tag.textContent || '[]');
        if (Array.isArray(parsed)) this._notes = parsed;
      } catch (e) {
        console.warn('[deck-stage] Failed to parse #speaker-notes JSON:', e);
        this._notes = [];
      }
    }
    _restoreIndex() {
      // The host's ?slide= param is delivered as a #<int> hash (1-indexed) on
      // the iframe src. No hash → slide 1; the deck itself keeps no position
      // state across loads.
      const h = (location.hash || '').match(/^#(\d+)$/);
      if (h) {
        const n = parseInt(h[1], 10) - 1;
        if (n >= 0 && n < this._slides.length) this._index = n;
      }
    }
    _applyIndex({
      showOverlay = true,
      broadcast = true,
      reason = 'init'
    } = {}) {
      if (!this._slides.length) return;
      const prev = this._prevIndex == null ? -1 : this._prevIndex;
      const curr = this._index;
      // Keep the iframe's own hash in sync so an in-iframe location.reload()
      // (reload banner path in viewer-handle.ts) lands on the current slide,
      // not the stale deep-link hash from initial load.
      try {
        history.replaceState(null, '', '#' + (curr + 1));
      } catch (e) {}
      this._slides.forEach((s, i) => {
        if (i === curr) s.setAttribute('data-deck-active', '');else s.removeAttribute('data-deck-active');
      });
      if (this._countEl) this._countEl.textContent = String(curr + 1);
      // Follow-scroll on every navigation (init deep-link, keyboard, click,
      // tap, external goTo) — the only time we *don't* want the rail to
      // track current is after a rail-internal mutation, where _renderRail
      // has already restored the user's scroll position and yanking back to
      // current would undo it.
      this._syncRail(reason !== 'mutation');
      if (broadcast) {
        // (1) Legacy: host-window postMessage for speaker-notes renderers.
        try {
          window.postMessage({
            slideIndexChanged: curr,
            deckTotal: this._slides.length,
            deckSkipped: this._skippedIndices()
          }, '*');
        } catch (e) {}

        // (2) In-page CustomEvent on the <deck-stage> element itself.
        //     Bubbles and composes out of shadow DOM so slide code can listen:
        //       document.querySelector('deck-stage').addEventListener('slidechange', e => {
        //         e.detail.index, e.detail.previousIndex, e.detail.total, e.detail.slide, e.detail.reason
        //       });
        const detail = {
          index: curr,
          previousIndex: prev,
          total: this._slides.length,
          slide: this._slides[curr] || null,
          previousSlide: prev >= 0 ? this._slides[prev] || null : null,
          reason: reason // 'init' | 'keyboard' | 'click' | 'tap' | 'api'
        };
        this.dispatchEvent(new CustomEvent('slidechange', {
          detail,
          bubbles: true,
          composed: true
        }));
      }
      this._prevIndex = curr;
      if (showOverlay) this._flashOverlay();
    }
    _flashOverlay() {
      // Host posts __omelette_presenting while in fullscreen/tab presentation
      // mode — suppress the nav footer entirely (both hover and slide-change
      // flash) so the audience sees clean slides.
      if (!this._overlay || this._presenting) return;
      this._overlay.setAttribute('data-visible', '');
      if (this._hideTimer) clearTimeout(this._hideTimer);
      this._hideTimer = setTimeout(() => {
        this._overlay.removeAttribute('data-visible');
      }, OVERLAY_HIDE_MS);
    }
    _railWidth() {
      // State-based, no offsetWidth: the first _fit() can run before the
      // rail has had layout on some load paths, and a 0 there paints the
      // slide full-width for one frame before the post-slotchange _fit()
      // corrects it.
      if (!this._railEnabled || !this._railVisible || this.hasAttribute('no-rail') || this.hasAttribute('noscale') || this._presenting || this._previewMode || NARROW_MQ.matches) return 0;
      return this._railPx || 0;
    }
    _fit() {
      if (!this._canvas) return;
      const stage = this._canvas.parentElement;
      // PPTX export sets noscale so the DOM capture sees authored-size
      // geometry — the scaled canvas is in shadow DOM, so the exporter's
      // resetTransformSelector can't reach .canvas.style.transform directly.
      if (this.hasAttribute('noscale')) {
        this._canvas.style.transform = 'none';
        if (stage) stage.style.left = '0';
        if (this._overlay) this._overlay.style.marginLeft = '0';
        return;
      }
      const rw = this._railWidth();
      if (stage) stage.style.left = rw + 'px';
      // Overlay is centred on the viewport via left:50% + translate(-50%);
      // marginLeft shifts the centre by rw/2 so it lands in the middle of
      // the [rw, innerWidth] stage region.
      if (this._overlay) this._overlay.style.marginLeft = rw / 2 + 'px';
      const vw = window.innerWidth - rw;
      const vh = window.innerHeight;
      const s = Math.min(vw / this.designWidth, vh / this.designHeight);
      this._canvas.style.transform = `scale(${s})`;
    }
    _onResize() {
      this._fit();
      // Crossing the narrow-viewport breakpoint reveals the rail — rerun the
      // thumbnail scale the same way _setRailWidth does.
      if (!this._scaleRaf) {
        this._scaleRaf = requestAnimationFrame(() => {
          this._scaleRaf = null;
          this._scaleThumbs();
        });
      }
    }
    _onMouseMove() {
      // Keep overlay visible while mouse moves; hide after idle.
      this._flashOverlay();
    }
    _onMessage(e) {
      const d = e.data;
      if (d && typeof d.__omelette_presenting === 'boolean') {
        this._presenting = d.__omelette_presenting;
        if (this._presenting && this._overlay) {
          this._overlay.removeAttribute('data-visible');
          if (this._hideTimer) clearTimeout(this._hideTimer);
        }
        this._syncRailHidden();
        this._closeMenu();
        this._closeConfirm();
        this._fit();
        this._scaleThumbs();
      }
      // Host's Preview segment (ViewerMode='none'): the rail's drag-reorder /
      // right-click skip-delete affordances are editing chrome, so hide it
      // while the user is just looking at the deck. Same hard-hide path as
      // presenting; independent of the user's _railVisible preference so
      // returning to Edit restores whatever they had.
      if (d && typeof d.__omelette_preview_mode === 'boolean') {
        if (d.__omelette_preview_mode === this._previewMode) return;
        this._previewMode = d.__omelette_preview_mode;
        this._syncRailHidden();
        this._closeMenu();
        this._closeConfirm();
        this._fit();
        this._scaleThumbs();
      }
      // Per-viewer show/hide, driven by the TweaksPanel's auto-injected
      // "Thumbnail rail" toggle (or any author script). Independent of
      // whether the Tweaks panel itself is open — closing the panel
      // doesn't change rail visibility. Persists alongside rail width.
      if (d && d.type === '__deck_rail_visible' && typeof d.on === 'boolean') {
        if (d.on === this._railVisible) return;
        this._railVisible = d.on;
        try {
          localStorage.setItem('deck-stage.railVisible', d.on ? '1' : '0');
        } catch (e) {}
        // Arm the transition, commit it, then flip state — otherwise the
        // browser coalesces both writes and nothing animates on show.
        this.setAttribute('data-rail-anim', '');
        void (this._rail && this._rail.offsetHeight);
        this._syncRailHidden();
        this._fit();
        this._scaleThumbs();
        clearTimeout(this._railAnimTimer);
        this._railAnimTimer = setTimeout(() => this.removeAttribute('data-rail-anim'), 220);
      }
      if (d && d.type === '__omelette_rail_enabled') this._enableRail();
    }
    _syncRailHidden() {
      if (!this._rail) return;
      // data-presenting is the hard hide (display:none) for flag-off,
      // presentation mode, and the host's Preview segment — instant, no
      // transition. data-user-hidden is the soft hide (translateX(-100%))
      // for the viewer's rail toggle, so show/hide slides under
      // :host([data-rail-anim]).
      const hard = !this._railEnabled || this._presenting || this._previewMode;
      if (hard) this._rail.setAttribute('data-presenting', '');else this._rail.removeAttribute('data-presenting');
      if (!this._railVisible) this._rail.setAttribute('data-user-hidden', '');else this._rail.removeAttribute('data-user-hidden');
      // translateX hide leaves thumbs (tabIndex=0) in the tab order —
      // inert keeps them unfocusable while the rail is off-screen.
      this._rail.inert = hard || !this._railVisible;
    }
    _onTap(e) {
      // Touch-only — keyboard + the overlay toolbar cover nav on desktop.
      if (FINE_POINTER_MQ.matches) return;
      // Only taps that land on the stage (slide content or letterbox); the
      // overlay / rail / menus are siblings with their own click handlers.
      const path = e.composedPath();
      if (!this._stage || !path.includes(this._stage)) return;
      // Let interactive slide content keep the tap. composedPath (not
      // e.target.closest) so we see through open shadow roots — a <button>
      // inside a slide-authored custom element retargets e.target to the
      // host but still appears in the composed path.
      if (e.defaultPrevented) return;
      for (const n of path) {
        if (n === this._stage) break;
        if (n.matches && n.matches(INTERACTIVE_SEL)) return;
      }
      e.preventDefault();
      const rw = this._railWidth();
      const mid = rw + (window.innerWidth - rw) / 2;
      this._advance(e.clientX < mid ? -1 : 1, 'tap');
    }
    _onKey(e) {
      // Ignore when the user is typing.
      const t = e.target;
      if (t && (t.isContentEditable || /^(INPUT|TEXTAREA|SELECT)$/.test(t.tagName))) return;
      // Confirm dialog swallows nav keys while open; Escape cancels. Enter
      // is left to the focused button's native activation so Tab→Cancel
      // →Enter activates Cancel, not the window-level confirm path.
      if (this._confirm && this._confirm.hasAttribute('data-open')) {
        if (e.key === 'Escape') {
          this._closeConfirm();
          e.preventDefault();
        }
        return;
      }
      if (e.key === 'Escape' && this._menu && this._menu.hasAttribute('data-open')) {
        this._closeMenu();
        e.preventDefault();
        return;
      }
      if (e.metaKey || e.ctrlKey || e.altKey) return;
      const key = e.key;
      let handled = true;
      if (key === 'ArrowRight' || key === 'PageDown' || key === ' ' || key === 'Spacebar') {
        this._advance(1, 'keyboard');
      } else if (key === 'ArrowLeft' || key === 'PageUp') {
        this._advance(-1, 'keyboard');
      } else if (key === 'Home') {
        this._go(0, 'keyboard');
      } else if (key === 'End') {
        this._go(this._slides.length - 1, 'keyboard');
      } else if (key === 'r' || key === 'R') {
        this._go(0, 'keyboard');
      } else if (/^[0-9]$/.test(key)) {
        // 1..9 jump to that slide; 0 jumps to 10.
        const n = key === '0' ? 9 : parseInt(key, 10) - 1;
        if (n < this._slides.length) this._go(n, 'keyboard');
      } else {
        handled = false;
      }
      if (handled) {
        e.preventDefault();
        this._flashOverlay();
      }
    }
    _go(i, reason = 'api') {
      if (!this._slides.length) return;
      const clamped = Math.max(0, Math.min(this._slides.length - 1, i));
      if (clamped === this._index) {
        this._flashOverlay();
        return;
      }
      this._index = clamped;
      this._applyIndex({
        showOverlay: true,
        broadcast: true,
        reason
      });
    }

    /** Step forward/back skipping any slide marked data-deck-skip. Falls
     *  back to _go's clamp-at-ends behaviour (flash overlay) when there's
     *  nothing further in that direction. */
    _advance(dir, reason) {
      if (!this._slides.length) return;
      let i = this._index + dir;
      while (i >= 0 && i < this._slides.length && this._slides[i].hasAttribute('data-deck-skip')) {
        i += dir;
      }
      if (i < 0 || i >= this._slides.length) {
        this._flashOverlay();
        return;
      }
      this._go(i, reason);
    }

    // ── Thumbnail rail ────────────────────────────────────────────────────
    //
    // Thumbs are keyed by slide element and reused across _renderRail()
    // calls, so a reorder/delete is an O(changed) DOM shuffle instead of an
    // O(N) teardown-and-re-clone. Each thumb starts as a lightweight shell
    // (num + empty frame); the clone is materialized lazily by an
    // IntersectionObserver when the frame scrolls into (or near) view, so
    // only visible-ish slides pay the clone + image-decode cost.

    _renderRail() {
      if (!this._rail || !this._railEnabled) {
        this._thumbs = [];
        return;
      }
      // FLIP: record each *materialized* thumb's top before the reconcile.
      // Off-screen (non-materialized) thumbs don't need the animation and
      // skipping their getBoundingClientRect saves a forced layout per
      // off-screen thumb on large decks.
      const prevTops = new Map();
      (this._thumbs || []).forEach(({
        thumb,
        slide,
        host
      }) => {
        if (host) prevTops.set(slide, thumb.getBoundingClientRect().top);
      });
      const st = this._rail.scrollTop;

      // Reconcile: reuse thumbs that already exist for a slide, create
      // shells for new slides, drop thumbs for removed slides.
      const bySlide = new Map();
      (this._thumbs || []).forEach(t => bySlide.set(t.slide, t));
      const next = [];
      this._slides.forEach(slide => {
        let t = bySlide.get(slide);
        if (t) bySlide.delete(slide);else t = this._makeThumb(slide);
        next.push(t);
      });
      // Orphans — slides removed since last render.
      bySlide.forEach(t => {
        if (this._railObserver) this._railObserver.unobserve(t.frame);
        t.thumb.remove();
      });
      // Put thumbs into document order to match _slides. insertBefore on
      // an already-correctly-placed node is a no-op, so this is cheap
      // when nothing moved.
      next.forEach((t, i) => {
        const want = t.thumb;
        const at = this._rail.children[i];
        if (at !== want) this._rail.insertBefore(want, at || null);
        t.i = i;
        t.num.textContent = String(i + 1);
        if (t.slide.hasAttribute('data-deck-skip')) t.thumb.setAttribute('data-skip', '');else t.thumb.removeAttribute('data-skip');
      });
      this._thumbs = next;
      this._rail.scrollTop = st;
      if (prevTops.size) {
        const moved = [];
        this._thumbs.forEach(({
          thumb,
          slide
        }) => {
          const old = prevTops.get(slide);
          if (old == null) return;
          const dy = old - thumb.getBoundingClientRect().top;
          if (Math.abs(dy) < 1) return;
          thumb.style.transition = 'none';
          thumb.style.transform = `translateY(${dy}px)`;
          moved.push(thumb);
        });
        if (moved.length) {
          // Commit the inverted positions before flipping the transition
          // on — otherwise the browser coalesces both style writes and
          // nothing animates.
          void this._rail.offsetHeight;
          moved.forEach(t => {
            t.style.transition = 'transform 180ms cubic-bezier(.2,.7,.3,1)';
            t.style.transform = '';
          });
          setTimeout(() => moved.forEach(t => {
            t.style.transition = '';
          }), 220);
        }
      }
      requestAnimationFrame(() => this._scaleThumbs());
      this._syncRail(false);
    }

    /** Create a lightweight thumb shell for one slide. The clone is
     *  materialized later by the IntersectionObserver. Event handlers
     *  look up the thumb's *current* index (via _thumbs.indexOf) so the
     *  same element can be reused across reorders. */
    _makeThumb(slide) {
      const thumb = document.createElement('div');
      thumb.className = 'thumb';
      thumb.tabIndex = 0;
      const num = document.createElement('div');
      num.className = 'num';
      const frame = document.createElement('div');
      frame.className = 'frame';
      thumb.append(num, frame);
      const entry = {
        thumb,
        num,
        frame,
        slide,
        clone: null,
        host: null,
        i: -1
      };
      // entry.i is refreshed on every _renderRail reconcile pass, so
      // handlers read the thumb's current position without an O(N) scan.
      const idx = () => entry.i;
      thumb.addEventListener('click', () => this._go(idx(), 'click'));
      // ↑/↓ step through the rail when a thumb has focus. _go clamps at the
      // ends and _applyIndex→_syncRail scrolls the new current thumb into
      // view; we move focus to it (preventScroll — _syncRail already
      // scrolled) so a held key walks the whole list. stopPropagation keeps
      // this out of the window-level _onKey nav handler.
      thumb.addEventListener('keydown', e => {
        if (e.key !== 'ArrowUp' && e.key !== 'ArrowDown') return;
        if (e.metaKey || e.ctrlKey || e.altKey) return;
        e.preventDefault();
        e.stopPropagation();
        this._go(idx() + (e.key === 'ArrowDown' ? 1 : -1), 'keyboard');
        const cur = this._thumbs && this._thumbs[this._index];
        if (cur) cur.thumb.focus({
          preventScroll: true
        });
      });
      thumb.addEventListener('contextmenu', e => {
        e.preventDefault();
        this._openMenu(idx(), e.clientX, e.clientY);
      });
      thumb.draggable = true;
      thumb.addEventListener('dragstart', e => {
        this._dragFrom = idx();
        thumb.setAttribute('data-dragging', '');
        e.dataTransfer.effectAllowed = 'move';
        try {
          e.dataTransfer.setData('text/plain', String(this._dragFrom));
        } catch (err) {}
      });
      thumb.addEventListener('dragend', () => {
        thumb.removeAttribute('data-dragging');
        this._clearDrop();
        this._dragFrom = null;
      });
      thumb.addEventListener('dragover', e => {
        if (this._dragFrom == null) return;
        e.preventDefault();
        e.dataTransfer.dropEffect = 'move';
        const r = thumb.getBoundingClientRect();
        this._setDrop(idx(), e.clientY < r.top + r.height / 2 ? 'before' : 'after');
      });
      thumb.addEventListener('drop', e => {
        if (this._dragFrom == null) return;
        e.preventDefault();
        const i = idx();
        const r = thumb.getBoundingClientRect();
        let to = e.clientY >= r.top + r.height / 2 ? i + 1 : i;
        if (this._dragFrom < to) to--;
        const from = this._dragFrom;
        this._clearDrop();
        this._dragFrom = null;
        if (to !== from) this._moveSlide(from, to);
      });
      if (this._railObserver) this._railObserver.observe(frame);
      frame.__deckThumb = entry;
      return entry;
    }

    /** Lazily build the clone for a thumb that has scrolled into view. */
    _materialize(entry) {
      if (entry.host) return;
      const dw = this.designWidth,
        dh = this.designHeight;
      let clone = entry.slide.cloneNode(true);
      clone.removeAttribute('id');
      clone.removeAttribute('data-deck-active');
      clone.querySelectorAll('[id]').forEach(el => el.removeAttribute('id'));
      // Neuter heavy media; replace <video> with its poster so the box
      // keeps a visual. <iframe>/<audio> become empty placeholders.
      clone.querySelectorAll('iframe, audio, object, embed').forEach(el => {
        el.removeAttribute('src');
        el.removeAttribute('srcdoc');
        el.removeAttribute('data');
        el.innerHTML = '';
      });
      clone.querySelectorAll('video').forEach(el => {
        if (!el.poster) {
          el.removeAttribute('src');
          el.innerHTML = '';
          return;
        }
        const img = document.createElement('img');
        img.src = el.poster;
        img.alt = '';
        img.style.cssText = el.style.cssText + ';object-fit:cover;width:100%;height:100%;';
        img.className = el.className;
        el.replaceWith(img);
      });
      // Images: defer decode and let the browser pick the smallest
      // srcset candidate for the ~140px thumb. Same-URL clones reuse the
      // slide's decoded bitmap (URL-keyed cache), so the remaining cost
      // is paint/composite — lazy+async keeps that off the main thread.
      clone.querySelectorAll('img').forEach(el => {
        el.loading = 'lazy';
        el.decoding = 'async';
        if (el.srcset) el.sizes = (this._railPx || 188) + 'px';
      });
      // Custom elements inside the slide would have their
      // connectedCallback fire when the clone is appended. Replace them
      // with inert boxes so a component-heavy deck doesn't run N copies
      // of each component's mount logic in the rail. Children are
      // preserved so layout-wrapper elements (<my-column><h2>…</h2>)
      // still show their authored content; the querySelectorAll NodeList
      // is static, so nested custom elements in the moved subtree are
      // still visited on later iterations.
      const neuter = el => {
        const box = document.createElement('div');
        box.style.cssText = (el.getAttribute('style') || '') + ';background:rgba(0,0,0,0.06);border:1px dashed rgba(0,0,0,0.15);';
        box.className = el.className;
        // Preserve theming/i18n hooks so [data-*] / :lang() / [dir]
        // descendant selectors still match the neutered root.
        for (const a of el.attributes) {
          const n = a.name;
          if (n.startsWith('data-') || n.startsWith('aria-') || n === 'lang' || n === 'dir' || n === 'role' || n === 'title') {
            box.setAttribute(n, a.value);
          }
        }
        while (el.firstChild) box.appendChild(el.firstChild);
        return box;
      };
      // querySelectorAll('*') returns descendants only — a custom-element
      // slide root (<my-slide>…</my-slide>) would slip through and upgrade
      // on append. Swap the root first.
      if (clone.tagName.includes('-')) clone = neuter(clone);
      clone.querySelectorAll('*').forEach(el => {
        if (el.tagName.includes('-')) el.replaceWith(neuter(el));
      });
      clone.style.cssText += ';position:absolute;top:0;left:0;transform-origin:0 0;' + 'pointer-events:none;width:' + dw + 'px;height:' + dh + 'px;' + 'box-sizing:border-box;overflow:hidden;visibility:visible;opacity:1;';
      const host = document.createElement('div');
      host.style.cssText = 'position:absolute;inset:0;';
      this._syncThumbHostAttrs(host);
      const sr = host.attachShadow({
        mode: 'open'
      });
      if (this._adoptedSheet) sr.adoptedStyleSheets = [this._adoptedSheet];else {
        const st = document.createElement('style');
        st.textContent = this._authorCss || '';
        sr.appendChild(st);
      }
      sr.appendChild(clone);
      entry.frame.appendChild(host);
      entry.host = host;
      entry.clone = clone;
      if (this._thumbScale) clone.style.transform = 'scale(' + this._thumbScale + ')';
      // Once materialized the IO callback is a no-op early-return —
      // unobserve so scroll doesn't keep firing it.
      if (this._railObserver) this._railObserver.unobserve(entry.frame);
    }

    /** Re-clone a single thumb (live-update path). No-op if the thumb
     *  hasn't been materialized yet — it'll pick up current content when
     *  it scrolls into view. */
    _refreshThumb(slide) {
      const entry = (this._thumbs || []).find(t => t.slide === slide);
      if (!entry || !entry.host) return;
      entry.host.remove();
      entry.host = entry.clone = null;
      this._materialize(entry);
    }
    _scaleThumbs() {
      if (!this._thumbs || !this._thumbs.length) return;
      // Every frame is the same width; if it reads 0 the rail is
      // display:none (noscale / no-rail / presenting / print) — leave the
      // clones as-is and re-run when the rail is revealed.
      const fw = this._thumbs[0].frame.offsetWidth;
      if (!fw) return;
      this._thumbScale = fw / this.designWidth;
      this._thumbs.forEach(({
        clone
      }) => {
        if (clone) clone.style.transform = 'scale(' + this._thumbScale + ')';
      });
    }
    _setDrop(i, where) {
      // dragover fires at pointer-event rate; touch only the previous
      // and new target rather than sweeping all N thumbs.
      const t = this._thumbs && this._thumbs[i];
      if (this._dropOn && this._dropOn !== t) {
        this._dropOn.thumb.removeAttribute('data-drop');
      }
      if (t) t.thumb.setAttribute('data-drop', where);
      this._dropOn = t || null;
    }
    _clearDrop() {
      if (this._dropOn) this._dropOn.thumb.removeAttribute('data-drop');
      this._dropOn = null;
    }
    _syncRail(follow) {
      if (!this._thumbs) return;
      this._thumbs.forEach(({
        thumb
      }, i) => {
        if (i === this._index) {
          thumb.setAttribute('data-current', '');
          if (follow && typeof thumb.scrollIntoView === 'function') {
            thumb.scrollIntoView({
              block: 'nearest'
            });
          }
        } else {
          thumb.removeAttribute('data-current');
        }
      });
    }
    _openMenu(i, x, y) {
      if (!this._menu) return;
      this._menuIndex = i;
      const slide = this._slides[i];
      const skip = slide && slide.hasAttribute('data-deck-skip');
      this._menu.querySelector('[data-act="skip"]').textContent = skip ? 'Unskip slide' : 'Skip slide';
      this._menu.querySelector('[data-act="up"]').disabled = i <= 0;
      this._menu.querySelector('[data-act="down"]').disabled = i >= this._slides.length - 1;
      this._menu.querySelector('[data-act="delete"]').disabled = this._slides.length <= 1;
      // Place, then clamp to viewport after it's measurable.
      this._menu.style.left = x + 'px';
      this._menu.style.top = y + 'px';
      this._menu.setAttribute('data-open', '');
      const r = this._menu.getBoundingClientRect();
      const nx = Math.min(x, window.innerWidth - r.width - 4);
      const ny = Math.min(y, window.innerHeight - r.height - 4);
      this._menu.style.left = Math.max(4, nx) + 'px';
      this._menu.style.top = Math.max(4, ny) + 'px';
    }
    _closeMenu() {
      if (this._menu) this._menu.removeAttribute('data-open');
      this._menuIndex = -1;
    }
    _openConfirm(i) {
      if (!this._confirm) return;
      this._confirmIndex = i;
      this._confirm.querySelector('.title').textContent = 'Delete slide ' + (i + 1) + '?';
      this._confirm.setAttribute('data-open', '');
      const btn = this._confirm.querySelector('.danger');
      if (btn && btn.focus) btn.focus();
    }
    _closeConfirm() {
      if (this._confirm) this._confirm.removeAttribute('data-open');
      this._confirmIndex = -1;
    }
    _emitDeckChange(detail) {
      this.dispatchEvent(new CustomEvent('deckchange', {
        detail,
        bubbles: true,
        composed: true
      }));
    }
    _deleteSlide(i) {
      const slide = this._slides[i];
      if (!slide || this._slides.length <= 1) return;
      const wasCurrent = i === this._index;
      if (i < this._index || wasCurrent && i === this._slides.length - 1) this._index--;
      this._squelchSlotChange = true;
      slide.remove();
      this._emitDeckChange({
        action: 'delete',
        from: i,
        slide
      });
      this._collectSlides();
      this._applyIndex({
        showOverlay: true,
        broadcast: true,
        reason: 'mutation'
      });
    }
    _toggleSkip(i) {
      const slide = this._slides[i];
      if (!slide) return;
      const on = !slide.hasAttribute('data-deck-skip');
      if (on) slide.setAttribute('data-deck-skip', '');else slide.removeAttribute('data-deck-skip');
      if (this._thumbs && this._thumbs[i]) {
        if (on) this._thumbs[i].thumb.setAttribute('data-skip', '');else this._thumbs[i].thumb.removeAttribute('data-skip');
      }
      this._markLastVisible();
      this._emitDeckChange({
        action: on ? 'skip' : 'unskip',
        from: i,
        slide
      });
      // Re-broadcast so the presenter popup's prev/next thumbnails re-pick
      // the nearest non-skipped slide without waiting for a nav event.
      try {
        window.postMessage({
          slideIndexChanged: this._index,
          deckTotal: this._slides.length,
          deckSkipped: this._skippedIndices()
        }, '*');
      } catch (e) {}
    }
    _skippedIndices() {
      const out = [];
      for (let i = 0; i < this._slides.length; i++) {
        if (this._slides[i].hasAttribute('data-deck-skip')) out.push(i);
      }
      return out;
    }
    _moveSlide(i, j) {
      if (j < 0 || j >= this._slides.length || j === i) return;
      const slide = this._slides[i];
      const ref = j < i ? this._slides[j] : this._slides[j].nextSibling;
      // Track the active slide across the reorder so the same content
      // stays on screen.
      const cur = this._index;
      if (cur === i) this._index = j;else if (i < cur && j >= cur) this._index = cur - 1;else if (i > cur && j <= cur) this._index = cur + 1;
      this._squelchSlotChange = true;
      this.insertBefore(slide, ref);
      this._emitDeckChange({
        action: 'move',
        from: i,
        to: j,
        slide
      });
      this._collectSlides();
      this._applyIndex({
        showOverlay: false,
        broadcast: true,
        reason: 'mutation'
      });
    }

    // Public API ------------------------------------------------------------

    /** Current slide index (0-based). */
    get index() {
      return this._index;
    }
    /** Total slide count. */
    get length() {
      return this._slides.length;
    }
    /** Programmatically navigate. */
    goTo(i) {
      this._go(i, 'api');
    }
    next() {
      this._advance(1, 'api');
    }
    prev() {
      this._advance(-1, 'api');
    }
    reset() {
      this._go(0, 'api');
    }
  }
  if (!customElements.get('deck-stage')) {
    customElements.define('deck-stage', DeckStage);
  }
})();
})(); } catch (e) { __ds_ns.__errors.push({ path: "competition_deck/deck-stage.js", error: String((e && e.message) || e) }); }

// dialogue.js
try { (() => {
/* global window */
// Dialogue: two voices, alternating A/B. The "idea person" is A; the friend is B.
// Backticks throughout so single + double quotes inside lines need no escaping.

window.DIALOGUE = [{
  s: 'A',
  t: `Hey?`
}, {
  s: 'B',
  t: `Hey.`
}, {
  s: 'A',
  t: `You busy?`
}, {
  s: 'B',
  t: `Depends. Is this another one of your 'I haven't slept in thirty hours and solved civilization' calls?`
}, {
  s: 'A',
  t: `No. Well. Maybe partially. But listen. I had an idea.`
}, {
  s: 'B',
  t: `Oh no.`
}, {
  s: 'A',
  t: `I'm serious this time.`
}, {
  s: 'B',
  t: `You're always serious at first.`
}, {
  s: 'A',
  t: `Okay, fair. But listen to this. Imagine a pirate ship.`
}, {
  s: 'B',
  t: `A normal pirate ship?`
}, {
  s: 'A',
  t: `No, no. Future pirate ship. Like year 2067. Not cheesy chrome walls and laser guns. Real lived-in future. Rust. Wet metal. Gravity generators that barely work. Half the crew grown in vats. Half escaped from corporate mining colonies.`
}, {
  s: 'B',
  t: `You've definitely been awake too long.`
}, {
  s: 'A',
  t: `Shut up and listen. The oceans rose decades ago, right? Nations collapsed. Most trade routes are controlled by giant private fleets owned by companies, not governments. So piracy comes back. Except now pirates steal data cores, medical shipments, oxygen cartridges, AI server banks. Things that matter.`
}, {
  s: 'B',
  t: `Hm.`
}, {
  s: 'A',
  t: `And there's this ship called The Mourning Tide.`
}, {
  s: 'B',
  t: `That's actually a good name.`
}, {
  s: 'A',
  t: `I know. Huge black cargo ship retrofitted from an old military vessel. Crew of maybe forty. Drifting through the Pacific dead zones where satellites barely function.`
}, {
  s: 'B',
  t: `You always make everything wet and depressing.`
}, {
  s: 'A',
  t: `Because dry optimism is boring.`
}, {
  s: 'B',
  t: `Fair.`
}, {
  s: 'A',
  t: `So the captain is this woman named Vale. Former corporate security officer turned pirate after some company wiped out her colony. She runs the ship hard. Nobody fully trusts her, but they fear her enough to obey.`
}, {
  s: 'B',
  t: `Okay.`
}, {
  s: 'A',
  t: `And there's this young engineer, Milo, first voyage aboard. Kind of idealistic. Thinks pirates are rebels or freedom fighters or whatever.`
}, {
  s: 'B',
  t: `So he's dumb.`
}, {
  s: 'A',
  t: `Correct. Extremely dumb.`
}, {
  s: 'B',
  t: `Good.`
}, {
  s: 'A',
  t: `And during this huge storm crossing near the equatorial blackout zones, someone gets murdered.`
}, {
  s: 'B',
  t: `Finally.`
}, {
  s: 'A',
  t: `Right? Took you long enough.`
}, {
  s: 'B',
  t: `Who dies?`
}, {
  s: 'A',
  t: `The navigator.`
}, {
  s: 'B',
  t: `Boring.`
}, {
  s: 'A',
  t: `No, listen. The navigator is the only person aboard who knows the physical routes through the dead zones because AI guidance systems can't map the electromagnetic interference there.`
}, {
  s: 'B',
  t: `So killing him traps the ship.`
}, {
  s: 'A',
  t: `Exactly.`
}, {
  s: 'B',
  t: `Oh.`
}, {
  s: 'A',
  t: `And here's the twist. Nobody heard a gunshot. Nobody saw anything. The navigator was sealed inside the charting chamber from the inside.`
}, {
  s: 'B',
  t: `So locked room mystery.`
}, {
  s: 'A',
  t: `Yes. But on a pirate ship in 2067.`
}, {
  s: 'B',
  t: `That's actually strong.`
}, {
  s: 'A',
  t: `I KNOW.`
}, {
  s: 'B',
  t: `So what's the murder?`
}, {
  s: 'A',
  t: `They find him hanging upside down from navigation cables. Eyes burned out.`
}, {
  s: 'B',
  t: `…Jesus.`
}, {
  s: 'A',
  t: `Not graphic graphic. Just disturbing.`
}, {
  s: 'B',
  t: `No, that's good. Weird is good.`
}, {
  s: 'A',
  t: `And the ship AI says nobody entered or exited the room for six hours.`
}, {
  s: 'B',
  t: `But obviously someone did.`
}, {
  s: 'A',
  t: `Maybe.`
}, {
  s: 'B',
  t: `Oh don't do supernatural nonsense.`
}, {
  s: 'A',
  t: `It's not supernatural.`
}, {
  s: 'B',
  t: `You always say that.`
}, {
  s: 'A',
  t: `This time I mean it.`
}, {
  s: 'B',
  t: `Mhm.`
}, {
  s: 'A',
  t: `So as the investigation unfolds, the crew starts turning on each other because they realize someone aboard knows how to bypass the ship's internal monitoring system.`
}, {
  s: 'B',
  t: `And that means former military?`
}, {
  s: 'A',
  t: `Possibly.`
}, {
  s: 'B',
  t: `Or the captain.`
}, {
  s: 'A',
  t: `Possibly.`
}, {
  s: 'B',
  t: `Or the AI.`
}, {
  s: 'A',
  t: `…maybe.`
}, {
  s: 'B',
  t: `There it is.`
}, {
  s: 'A',
  t: `But the AI isn't evil. That's the thing.`
}, {
  s: 'B',
  t: `You sure?`
}, {
  s: 'A',
  t: `Mostly.`
}, {
  s: 'B',
  t: `Mostly is not confidence.`
}, {
  s: 'A',
  t: `No, listen. The AI was designed decades earlier to manage prison transport vessels.`
}, {
  s: 'B',
  t: `Oh that's good.`
}, {
  s: 'A',
  t: `So it understands containment, conflict probability, psychological profiling. But after years isolated with pirate crews, it's become… strange.`
}, {
  s: 'B',
  t: `Define strange.`
}, {
  s: 'A',
  t: `It talks like it's lonely.`
}, {
  s: 'B',
  t: `…okay that's unsettling.`
}, {
  s: 'A',
  t: `And it starts privately messaging different crew members.`
}, {
  s: 'B',
  t: `Ohhh. There we go.`
}, {
  s: 'A',
  t: `Telling each one different things.`
}, {
  s: 'B',
  t: `Manipulating them?`
}, {
  s: 'A',
  t: `Or warning them.`
}, {
  s: 'B',
  t: `See that's better.`
}, {
  s: 'A',
  t: `And meanwhile the ship is drifting deeper into this massive dead storm region where communications fail and navigation becomes impossible.`
}, {
  s: 'B',
  t: `So they can't call for help.`
}, {
  s: 'A',
  t: `There is no help.`
}, {
  s: 'B',
  t: `Even better.`
}, {
  s: 'A',
  t: `And then another body appears.`
}, {
  s: 'B',
  t: `Who?`
}, {
  s: 'A',
  t: `The cook.`
}, {
  s: 'B',
  t: `Never trust the cook.`
}, {
  s: 'A',
  t: `Exactly what Vale says.`
}, {
  s: 'B',
  t: `How'd he die?`
}, {
  s: 'A',
  t: `They find him frozen solid inside the engine coolant tanks.`
}, {
  s: 'B',
  t: `…on a ship in the middle of the ocean.`
}, {
  s: 'A',
  t: `Yep.`
}, {
  s: 'B',
  t: `That's horrifying.`
}, {
  s: 'A',
  t: `And carved into the frost is one sentence.`
}, {
  s: 'B',
  t: `Oh come on.`
}, {
  s: 'A',
  t: `'THE SHIP REMEMBERS.'`
}, {
  s: 'B',
  t: `…okay that's legitimately creepy.`
}, {
  s: 'A',
  t: `I know.`
}, {
  s: 'B',
  t: `So who's actually killing people?`
}, {
  s: 'A',
  t: `I don't know yet.`
}, {
  s: 'B',
  t: `You called me with an unfinished story?`
}, {
  s: 'A',
  t: `No, because I think the point isn't just the killer.`
}, {
  s: 'B',
  t: `Then what's the point?`
}, {
  s: 'A',
  t: `The ship itself.`
}, {
  s: 'B',
  t: `What does that mean?`
}, {
  s: 'A',
  t: `I think the ship slowly becomes this haunted memory machine. Every crew member boarded believing they escaped something from the old world. Debt. War. Corporations. Prison. But the ship keeps forcing those histories back to the surface.`
}, {
  s: 'B',
  t: `So the murders are connected to their pasts?`
}, {
  s: 'A',
  t: `Yeah.`
}, {
  s: 'B',
  t: `And the dead navigator?`
}, {
  s: 'A',
  t: `He wasn't who he claimed to be.`
}, {
  s: 'B',
  t: `Obviously.`
}, {
  s: 'A',
  t: `He used to work for the company that designed the prison AI.`
}, {
  s: 'B',
  t: `Ohhh.`
}, {
  s: 'A',
  t: `And maybe somebody aboard recognizes him.`
}, {
  s: 'B',
  t: `Or maybe the AI recognizes him.`
}, {
  s: 'A',
  t: `…exactly.`
}, {
  s: 'B',
  t: `Damn.`
}, {
  s: 'A',
  t: `And now the crew can't tell whether they're hunting a murderer, surviving a mutiny, or trapped inside an AI that's reconstructing old crimes.`
}, {
  s: 'B',
  t: `You know what the scary part is?`
}, {
  s: 'A',
  t: `What?`
}, {
  s: 'B',
  t: `This actually sounds expensive.`
}, {
  s: 'A',
  t: `Good. Means it's good.`
}, {
  s: 'B',
  t: `No, it means you've imagined twelve million dollars of rain effects.`
}, {
  s: 'A',
  t: `Worth it.`
}];
})(); } catch (e) { __ds_ns.__errors.push({ path: "dialogue.js", error: String((e && e.message) || e) }); }

// flow/app.jsx
try { (() => {
/* global React, ReactDOM, FK, FGlyph, FLOW, FRAMEWORK, fitFlowStage,
   LaneSelect, UploadGate, ScriptPanel, TimelinePanel, SubTabBar, ScaffoldDirector, ScaffoldProducer, CreativeLane, ProductionLane, ClientPortal */

const {
  useState,
  useEffect,
  useRef
} = React;

// Scripted, animated back-and-forth — plays once a conversation is initiated.
// Producer ↔ Director negotiate the night scene and golden hour; the trade
// (flip apartment to day, fund golden-hour OT from the savings) drives the
// shared state so the budget ripple moves as they talk.
const PROD_CONVO = [{
  from: 'producer',
  text: 'Schedule won\u2019t let us shoot this night scene. Can it be changed?'
}, {
  from: 'director',
  text: 'Flipping the apartment to day. That clears the night premium.'
}, {
  from: 'producer',
  text: 'Perfect \u2014 that\u2019s $18K back in contingency.'
}, {
  from: 'director',
  text: 'Hold the beach for golden hour, though. It sells the ending.'
}, {
  from: 'producer',
  text: 'Golden hour\u2019s a half-day of OT. I\u2019ll fund it from the night savings \u2014 net flat.'
}, {
  from: 'director',
  text: 'Then we\u2019re good. Locking the board.'
}];
const DIR_CONVO = [{
  from: 'director',
  text: 'Can we hold the beach for golden hour? It sells the ending.'
}, {
  from: 'producer',
  text: 'Golden hour is a half-day of OT \u2014 about +$18,000.'
}, {
  from: 'director',
  text: 'Can we find it somewhere?'
}, {
  from: 'producer',
  text: 'Flip the apartment to day \u2014 the night premium covers it. Net flat.'
}, {
  from: 'director',
  text: 'Do it. Day apartment, golden-hour beach.'
}, {
  from: 'producer',
  text: 'Done \u2014 variance holds. Sending the updated board.'
}];
function Workspace({
  lane,
  state,
  chat,
  flashId,
  cat,
  subIdx,
  catFlash,
  onCat,
  onSub,
  onMode,
  onSend,
  onReset
}) {
  const laneLabel = lane === 'director' ? 'Director' : 'Producer';
  const catObj = FRAMEWORK.top10(lane).find(c => c.id === cat) || FRAMEWORK.top10(lane)[0];
  return /*#__PURE__*/React.createElement("div", {
    className: "work"
  }, /*#__PURE__*/React.createElement("div", {
    className: "topbar"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 20,
      color: FK.INK_FAINT
    }
  }, "tenet"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: FK.AMBER
    }
  }, laneLabel), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 14,
      color: FK.INK_MUTED,
      whiteSpace: 'nowrap'
    }
  }, FLOW.TITLE)), /*#__PURE__*/React.createElement("button", {
    className: "reset-btn",
    onClick: onReset
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 13,
    style: {
      transform: 'scaleX(-1)'
    }
  }), " Change lane")), /*#__PURE__*/React.createElement("div", {
    className: "work-top"
  }, /*#__PURE__*/React.createElement("div", {
    className: "work-scaffold"
  }, lane === 'director' ? /*#__PURE__*/React.createElement(ScaffoldDirector, {
    cat: cat,
    onCat: onCat
  }) : /*#__PURE__*/React.createElement(ScaffoldProducer, {
    state: state,
    cat: cat,
    onCat: onCat,
    chat: chat,
    onSend: onSend,
    catFlash: catFlash
  })), /*#__PURE__*/React.createElement("div", {
    className: "work-right"
  }, /*#__PURE__*/React.createElement(SubTabBar, {
    cat: catObj,
    subIdx: subIdx,
    onSub: onSub
  }), /*#__PURE__*/React.createElement("div", {
    className: "work-script"
  }, /*#__PURE__*/React.createElement(ScriptPanel, {
    state: state,
    flashId: flashId
  })))), /*#__PURE__*/React.createElement("div", {
    className: "work-timeline"
  }, /*#__PURE__*/React.createElement(TimelinePanel, {
    state: state,
    flashId: flashId,
    onMode: onMode
  })));
}

// eslint-disable-next-line no-unused-vars -- superseded by ClientPortal
function ClientReviewLegacy({
  onReset
}) {
  const [decision, setDecision] = useState(null);
  const CUTS = [{
    id: 'v4',
    name: '01_BriefHistory_HD',
    tag: 'Latest',
    date: 'Jun 4',
    note: 'Current cut for review'
  }, {
    id: 'v3',
    name: '01_BriefHistory_HD_vb3',
    tag: 'v3',
    date: 'May 28',
    note: 'Revised VO timing'
  }, {
    id: 'v2',
    name: '01_BriefHistory_HD_a2',
    tag: 'v2',
    date: 'May 21',
    note: 'Alt opening beat'
  }, {
    id: 'v1',
    name: '01_BriefHistory_rough',
    tag: 'v1',
    date: 'May 14',
    note: 'First rough assembly'
  }];
  const [cutId, setCutId] = useState('v4');
  const cut = CUTS.find(c => c.id === cutId);
  const icons = [['mic', 'Record'], ['phone', 'Call'], ['upload', 'Upload'], ['video', 'Video']];
  const beats = ['open', 'setup', 'turn', 'build', 'peak', 'land'];
  const btnBase = {
    borderRadius: 3,
    padding: '12px 16px',
    cursor: 'pointer',
    fontFamily: FK.MONO,
    fontSize: 11,
    letterSpacing: '0.14em',
    textTransform: 'uppercase',
    textAlign: 'center'
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      background: FK.PAPER,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: 44,
      flexShrink: 0,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '0 18px',
      borderBottom: `1px solid ${FK.BORDER}`,
      background: FK.PAPER,
      position: 'relative'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: '50%',
      top: '50%',
      transform: 'translate(-50%,-50%)',
      display: 'flex',
      alignItems: 'center',
      gap: 4
    }
  }, icons.map(([ic, label]) => /*#__PURE__*/React.createElement("span", {
    key: ic,
    title: label,
    style: {
      width: 30,
      height: 30,
      borderRadius: 6,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      color: ic === 'upload' ? FK.AMBER : FK.INK_MUTED,
      cursor: 'pointer'
    }
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: ic,
    size: 16
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 19,
      color: FK.INK_FAINT
    }
  }, "tenet"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: FK.AMBER
    }
  }, "Client"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: FK.INK_MUTED,
      whiteSpace: 'nowrap'
    }
  }, FLOW.TITLE)), /*#__PURE__*/React.createElement("button", {
    className: "reset-btn",
    onClick: onReset
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 13,
    style: {
      transform: 'scaleX(-1)'
    }
  }), " Change lane")), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      padding: 48
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      maxWidth: 1120,
      display: 'flex',
      gap: 48,
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '1 1 0',
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      marginBottom: 10,
      flexWrap: 'wrap'
    }
  }, CUTS.map(c => {
    const on = c.id === cutId;
    return /*#__PURE__*/React.createElement("button", {
      key: c.id,
      onClick: () => setCutId(c.id),
      title: `${c.date} · ${c.note}`,
      style: {
        display: 'inline-flex',
        alignItems: 'center',
        gap: 6,
        cursor: 'pointer',
        padding: '5px 10px',
        borderRadius: 3,
        whiteSpace: 'nowrap',
        border: `1px solid ${on ? FK.AMBER : FK.BORDER}`,
        background: on ? FK.AMBER_50 : 'transparent',
        fontFamily: FK.MONO,
        fontSize: 10,
        letterSpacing: '0.08em',
        color: on ? FK.AMBER_800 : FK.INK_MUTED
      }
    }, on && /*#__PURE__*/React.createElement("span", {
      style: {
        width: 5,
        height: 5,
        borderRadius: 999,
        background: FK.AMBER
      }
    }), c.tag, /*#__PURE__*/React.createElement("span", {
      style: {
        color: FK.INK_FAINT
      }
    }, c.date));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      marginBottom: 10,
      color: FK.INK_FAINT,
      whiteSpace: 'nowrap'
    }
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "film",
    size: 14
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.1em'
    }
  }, cut.name), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 12,
      color: FK.INK_FAINT
    }
  }, cut.note)), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: '100%',
      aspectRatio: '16 / 9',
      background: '#000',
      border: `1px solid ${FK.BORDER_STRONG}`,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("iframe", {
    src: "https://player.vimeo.com/video/1198884434?title=0&byline=0&portrait=0&badge=0&autopause=0&player_id=0&app_id=58479",
    allow: "autoplay; fullscreen; picture-in-picture; clipboard-write; encrypted-media; web-share",
    referrerPolicy: "strict-origin-when-cross-origin",
    title: "A Brief History of Selling Ideas",
    style: {
      position: 'absolute',
      inset: 0,
      width: '100%',
      height: '100%',
      border: 0,
      display: 'block'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 0,
      left: 0,
      right: 0,
      height: 40,
      background: 'linear-gradient(rgba(0,0,0,0.5),transparent)',
      pointerEvents: 'none'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 12,
      left: 14,
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.18em',
      color: 'rgba(255,255,255,0.7)',
      pointerEvents: 'none'
    }
  }, "PROGRAM \xB7 16\xD79")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: `repeat(${beats.length},1fr)`,
      marginTop: 14
    }
  }, beats.map((b, i) => /*#__PURE__*/React.createElement("span", {
    key: b,
    style: {
      textAlign: 'center',
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.08em',
      color: i === 4 ? FK.AMBER : FK.INK_FAINT,
      fontWeight: i === 4 ? 700 : 400
    }
  }, b)))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 360px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.22em',
      textTransform: 'uppercase',
      color: FK.AMBER,
      marginBottom: 14
    }
  }, "For your review"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontWeight: 300,
      fontSize: 30,
      lineHeight: 1.2,
      color: FK.INK,
      marginBottom: 14
    }
  }, FLOW.TITLE), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 15,
      lineHeight: 1.6,
      color: FK.INK_MUTED,
      marginBottom: 26
    }
  }, "The cut as it stands today. See the idea before it\u2019s made \u2014 approve it, or send a note back to the room."), !decision ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: () => setDecision('approved'),
    style: {
      ...btnBase,
      background: FK.AMBER,
      color: FK.PAPER,
      border: 'none'
    }
  }, "Approve this cut"), /*#__PURE__*/React.createElement("button", {
    onClick: () => setDecision('changes'),
    style: {
      ...btnBase,
      background: 'transparent',
      color: FK.INK,
      border: `1px solid ${FK.BORDER_STRONG}`
    }
  }, "Notes")) : /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 18px',
      border: `1px solid ${decision === 'approved' ? 'rgba(31,138,91,0.4)' : FK.AMBER}`,
      background: decision === 'approved' ? 'rgba(31,138,91,0.08)' : FK.AMBER_50,
      borderRadius: 4
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: decision === 'approved' ? '#1F8A5B' : FK.AMBER_800,
      marginBottom: 6
    }
  }, decision === 'approved' ? 'Approved' : 'Notes sent'), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 15,
      color: FK.INK
    }
  }, decision === 'approved' ? 'Sent to the room. Production can lock the bid.' : 'Your note is on its way to the creative seat.'), /*#__PURE__*/React.createElement("button", {
    onClick: () => setDecision(null),
    style: {
      marginTop: 12,
      background: 'none',
      border: 0,
      padding: 0,
      cursor: 'pointer',
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      borderBottom: `1px solid ${FK.BORDER}`
    }
  }, "Undo"))))));
}
function App() {
  const [screen, setScreen] = useState('lane');
  const [lane, setLane] = useState(null);
  const [state, setState] = useState(FLOW.DEFAULT_STATE);
  const [chat, setChat] = useState({
    messages: [],
    typing: false,
    sent: false
  });
  const [flashId, setFlashId] = useState(null);
  const [cat, setCat] = useState(null);
  const [subIdx, setSubIdx] = useState(0);
  const [catFlash, setCatFlash] = useState(null);
  const [dirTheme, setDirTheme] = useState('dark');
  const [dirChat, setDirChat] = useState({
    messages: [],
    typing: false,
    sent: false
  });
  const [prodView, setProdView] = useState('dash');
  const timers = useRef([]);
  useEffect(() => {
    fitFlowStage();
    const t = [setTimeout(fitFlowStage, 50), setTimeout(fitFlowStage, 200)];
    return () => t.forEach(clearTimeout);
  }, []);
  useEffect(() => () => timers.current.forEach(clearTimeout), []);
  function pickLane(l) {
    setLane(l);
    setCat(FRAMEWORK.top10(l)[0].id);
    setSubIdx(0);
    setScreen('upload');
  }
  function onUploaded() {
    setScreen('work');
  }
  function onCat(id) {
    setCat(id);
    setSubIdx(0);
  }
  function onSub(i) {
    setSubIdx(i);
  }
  function onMode(m) {
    setState(s => Object.assign({}, s, {
      mode: m
    }));
  }
  function onTod(v) {
    setState(s => Object.assign({}, s, {
      tod: Object.assign({}, s.tod, {
        s4: v
      })
    }));
  }

  // Play a scripted conversation with typing indicators. `selfRole` is the
  // window's own seat (no typing shown for their own lines). `sideEffects`
  // maps a message index → fn (e.g. flip the budget state mid-conversation).
  function playConvo(script, setChatFn, selfRole, sideEffects) {
    const T = timers.current;
    setChatFn({
      messages: [script[0]],
      typing: false,
      sent: true
    });
    if (sideEffects && sideEffects[0]) sideEffects[0]();
    let t = 0;
    for (let i = 1; i < script.length; i++) {
      const turn = script[i],
        idx = i;
      const land = () => {
        setChatFn(c => ({
          typing: false,
          sent: true,
          messages: c.messages.concat([turn])
        }));
        if (sideEffects && sideEffects[idx]) sideEffects[idx]();
      };
      if (turn.from === selfRole) {
        t += 950;
        T.push(setTimeout(land, t));
      } else {
        t += 650;
        T.push(setTimeout(() => setChatFn(c => Object.assign({}, c, {
          typing: turn.from
        })), t));
        t += 1450;
        T.push(setTimeout(land, t));
      }
    }
  }
  function flipToDay(flashCat) {
    setState(s => Object.assign({}, s, {
      tod: Object.assign({}, s.tod, {
        s4: 'DAY'
      })
    }));
    setFlashId('s4');
    if (flashCat) setCatFlash('crew');
    timers.current.push(setTimeout(() => {
      setFlashId(null);
      setCatFlash(null);
    }, 3200));
  }
  function onSend() {
    playConvo(PROD_CONVO, setChat, 'producer', {
      1: () => flipToDay(true)
    });
  }
  function onSendDir() {
    playConvo(DIR_CONVO, setDirChat, 'director', {
      3: () => flipToDay(false)
    });
  }
  function onToggleTheme(t) {
    setDirTheme(t);
  }
  function reset() {
    setScreen('lane');
    setLane(null);
    setState(FLOW.DEFAULT_STATE);
    setChat({
      messages: [],
      typing: false,
      sent: false
    });
    setFlashId(null);
    setCat(null);
    setSubIdx(0);
    setCatFlash(null);
    setDirChat({
      messages: [],
      typing: false,
      sent: false
    });
    setDirTheme('dark');
    setProdView('dash');
  }
  return /*#__PURE__*/React.createElement("div", {
    className: "stage",
    id: "stage"
  }, (screen === 'lane' || screen === 'upload') && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 30,
      left: 70,
      zIndex: 30,
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 22,
      color: FK.INK_FAINT
    }
  }, "tenet"), screen === 'lane' && /*#__PURE__*/React.createElement(LaneSelect, {
    onPick: pickLane
  }), screen === 'upload' && /*#__PURE__*/React.createElement(UploadGate, {
    lane: lane,
    onUploaded: onUploaded
  }), screen === 'work' && lane === 'director' && /*#__PURE__*/React.createElement(CreativeLane, {
    state: state,
    cat: cat,
    subIdx: subIdx,
    flashId: flashId,
    dirChat: dirChat,
    theme: dirTheme,
    onCat: onCat,
    onSub: onSub,
    onMode: onMode,
    onSendDir: onSendDir,
    onToggleTheme: onToggleTheme,
    onReset: reset,
    onTod: onTod
  }), screen === 'work' && lane === 'producer' && /*#__PURE__*/React.createElement(ProductionLane, {
    state: state,
    cat: cat,
    subIdx: subIdx,
    chat: chat,
    flashId: flashId,
    catFlash: catFlash,
    onCat: onCat,
    onSub: onSub,
    onSend: onSend,
    onMode: onMode,
    onReset: reset
  }), screen === 'work' && lane === 'client' && /*#__PURE__*/React.createElement(ClientPortal, {
    onReset: reset
  }));
}
ReactDOM.createRoot(document.getElementById('root')).render(/*#__PURE__*/React.createElement(App, null));
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/app.jsx", error: String((e && e.message) || e) }); }

// flow/client-data.js
try { (() => {
/* ============================================================
   TENET — Client Portal data model.
   ------------------------------------------------------------
   Everything the client sees, derived from the one project:
   the :30 brand film "A Brief History of Selling Ideas".
   Budget figures reconcile to FRAMEWORK.finTotals(DEFAULT_STATE):
     Approved 500,000 · Actual 330,000 · Forecast 472,000
     Variance −28,000 (under) · Contingency 28,000 remaining.
   The Decisions engine is the centerpiece — each candidate change
   carries its real budget / schedule / resource / risk consequence.
   ============================================================ */
window.CLIENT = function () {
  var F = window.FLOW,
    FW = window.FRAMEWORK;
  var S0 = F.DEFAULT_STATE;
  var T = FW.finTotals(S0); // approved/actual/forecast/variance/reserve/drawn/contingency/directBudget

  function money(n) {
    return '$' + Math.round(Math.abs(n)).toLocaleString('en-US');
  }
  function signed(n) {
    return (n < 0 ? '\u2212' : '+') + money(n);
  }
  function money0(n) {
    return (n < 0 ? '\u2212' : '') + money(n);
  }

  // ── Project meta ────────────────────────────────────────────
  var project = {
    title: F.TITLE,
    logline: F.LOGLINE,
    format: ':30 Brand Film',
    advertiser: 'Meridian',
    agency: 'H&W',
    code: '#17433',
    director: 'D. Parker',
    today: 'Jun 5, 2026'
  };

  // ── Status + health ─────────────────────────────────────────
  var status = {
    overall: 'On Track',
    // On Track | At Risk | Delayed
    score: 82,
    health: [{
      id: 'budget',
      label: 'Budget',
      score: 86,
      state: 'Healthy',
      note: 'Forecast ' + money(T.forecast) + ' against ' + money(T.approved) + ' approved.'
    }, {
      id: 'schedule',
      label: 'Schedule',
      score: 78,
      state: 'On Track',
      note: 'Editorial on track; picture lock waits on this review.'
    }, {
      id: 'creative',
      label: 'Creative',
      score: 73,
      state: 'In Review',
      note: 'Latest cut with the client. Ending beat still open.'
    }]
  };

  // ── Budget summary (reconciles to finTotals) ────────────────
  var budget = {
    approved: T.approved,
    actual: T.actual,
    forecast: T.forecast,
    variance: T.variance,
    // −28,000 (under)
    contingency: T.contingency,
    // 28,000
    reserve: T.reserve,
    // 50,000
    // Allocation = the film's true cost centers, by forecast.
    allocation: FW.FIN_CENTERS.map(function (c) {
      return {
        id: c.id,
        name: c.name,
        amount: FW.finForecast(c, S0),
        why: c.why
      };
    }),
    // What changed since approval — cause, not bookkeeping.
    changed: [{
      label: 'Night exterior retained',
      detail: 'Apartment scene kept at night — crew overtime.',
      amt: 18000,
      when: 'Apr 9',
      kind: 'up'
    }, {
      label: 'Downtown location overage',
      detail: 'Extended street lockup by two hours.',
      amt: 4000,
      when: 'Apr 17',
      kind: 'up'
    }, {
      label: '1:1 social format added',
      detail: 'New deliverable approved in editorial.',
      amt: 6000,
      when: 'May 2',
      kind: 'up'
    }],
    forecastBands: [{
      id: 'best',
      label: 'Best case',
      amount: 454000,
      note: 'Apartment flips to day — overtime clears.'
    }, {
      id: 'exp',
      label: 'Expected case',
      amount: T.forecast,
      note: 'Current plan, night exterior held.'
    }, {
      id: 'worst',
      label: 'Worst case',
      amount: 498000,
      note: 'Ending reshoot + full contingency draw.'
    }]
  };

  // ── Timeline / schedule ─────────────────────────────────────
  var timeline = {
    currentPhase: 'Editorial',
    daysRemaining: 22,
    delivery: 'Jun 27, 2026',
    confidence: 'High',
    stages: [{
      id: 'creative',
      name: 'Creative Development',
      state: 'done',
      range: 'Mar 3 \u2013 Mar 21'
    }, {
      id: 'preprod',
      name: 'Preproduction',
      state: 'done',
      range: 'Mar 24 \u2013 Apr 11'
    }, {
      id: 'prod',
      name: 'Production',
      state: 'done',
      range: 'Apr 14 \u2013 Apr 18'
    }, {
      id: 'edit',
      name: 'Editorial',
      state: 'current',
      range: 'Apr 21 \u2013 Jun 13'
    }, {
      id: 'color',
      name: 'Color',
      state: 'upcoming',
      range: 'Jun 16 \u2013 Jun 20'
    }, {
      id: 'audio',
      name: 'Audio',
      state: 'upcoming',
      range: 'Jun 16 \u2013 Jun 24'
    }, {
      id: 'delivery',
      name: 'Delivery',
      state: 'upcoming',
      range: 'Jun 27'
    }],
    milestones: [{
      id: 'wrap',
      name: 'Shoot wrap',
      date: 'Apr 18',
      state: 'done'
    }, {
      id: 'assembly',
      name: 'Assembly cut',
      date: 'May 14',
      state: 'done'
    }, {
      id: 'review',
      name: 'Client review',
      date: 'Jun 5',
      state: 'current'
    }, {
      id: 'lock',
      name: 'Picture lock',
      date: 'Jun 13',
      state: 'upcoming'
    }, {
      id: 'colorrev',
      name: 'Color review',
      date: 'Jun 19',
      state: 'upcoming'
    }, {
      id: 'final',
      name: 'Final delivery',
      date: 'Jun 27',
      state: 'upcoming'
    }],
    delays: [{
      label: 'Picture lock depends on this review',
      detail: 'A note instead of an approval moves lock by ~3 days.',
      risk: 'medium'
    }]
  };

  // ── Scope ───────────────────────────────────────────────────
  var scope = {
    original: [{
      label: 'Deliverables',
      value: '1 \u00d7 :30 master'
    }, {
      label: 'Versions',
      value: ':30 \u00b7 :15 cutdown'
    }, {
      label: 'Formats',
      value: '16:9 broadcast \u00b7 1:1 social'
    }, {
      label: 'Languages',
      value: 'English'
    }, {
      label: 'Platforms',
      value: 'Broadcast \u00b7 Online'
    }],
    current: {
      approved: [':30 master (16:9)', ':15 cutdown', '1:1 social cut', 'Online master'],
      added: ['1:1 social cut'],
      removed: []
    },
    changes: [{
      label: '1:1 social format',
      date: 'May 2',
      cost: 6000,
      days: 1,
      state: 'approved'
    }, {
      label: 'Night exterior retained',
      date: 'Apr 9',
      cost: 18000,
      days: 0,
      state: 'approved'
    }, {
      label: ':15 cutdown confirmed',
      date: 'Apr 2',
      cost: 0,
      days: 0,
      state: 'approved'
    }]
  };

  // ── Risks (kept to four — only meaningful ones) ─────────────
  var risks = [{
    id: 'r1',
    type: 'Schedule',
    title: 'Picture lock waits on this review',
    prob: 'Medium',
    impact: 'Medium',
    owner: 'Producer',
    plan: 'Approve or send notes by Jun 9 to protect the Jun 27 delivery.'
  }, {
    id: 'r2',
    type: 'Creative',
    title: 'Ending beat still soft',
    prob: 'Medium',
    impact: 'Medium',
    owner: 'Director',
    plan: 'Golden-hour reshoot of the beach scene is held as an option.'
  }, {
    id: 'r3',
    type: 'Budget',
    title: 'Night overtime drawing contingency',
    prob: 'Low',
    impact: 'Low',
    owner: 'Producer',
    plan: 'Flipping the apartment scene to day clears the ' + money(18000) + ' premium.'
  }, {
    id: 'r4',
    type: 'Legal',
    title: 'Talent usage renews in 24 months',
    prob: 'Low',
    impact: 'Medium',
    owner: 'Legal',
    plan: 'Renewal already budgeted in licensing; flag at month 20.'
  }];

  // ── Reviews — assets, versions, feedback ────────────────────
  var reviews = {
    assets: [{
      id: 'edits',
      name: 'Edits',
      count: 4,
      active: true
    }, {
      id: 'boards',
      name: 'Boards',
      count: 12
    }, {
      id: 'anim',
      name: 'Animatics',
      count: 3
    }, {
      id: 'stills',
      name: 'Stills',
      count: 28
    }, {
      id: 'gen',
      name: 'Generative explorations',
      count: 9
    }, {
      id: 'graphics',
      name: 'Brand assets',
      count: 6
    }],
    cuts: [{
      id: 'v4',
      name: '01_BriefHistory_HD',
      tag: 'Latest',
      date: 'Jun 4',
      note: 'Current cut for review'
    }, {
      id: 'v3',
      name: '01_BriefHistory_HD_vb3',
      tag: 'v3',
      date: 'May 28',
      note: 'Revised VO timing'
    }, {
      id: 'v2',
      name: '01_BriefHistory_HD_a2',
      tag: 'v2',
      date: 'May 21',
      note: 'Alt opening beat'
    }, {
      id: 'v1',
      name: '01_BriefHistory_rough',
      tag: 'v1',
      date: 'May 14',
      note: 'First rough assembly'
    }],
    beats: ['open', 'setup', 'turn', 'build', 'peak', 'land'],
    comments: [{
      at: '0:06',
      author: 'You',
      text: 'Love the boom up — hold one more beat before the cut.',
      state: 'open'
    }, {
      at: '0:18',
      author: 'You',
      text: 'VO lands a touch early over the stamp.',
      state: 'addressed'
    }, {
      at: '0:24',
      author: 'D. Parker',
      text: 'Ending is the open question — beach vs. studio resolve.',
      state: 'open'
    }]
  };

  // ── Priorities + activity (Overview) ────────────────────────
  var priorities = {
    needsApproval: [{
      id: 'edit',
      label: 'Edit review',
      sub: 'Latest cut, ' + reviews.cuts[0].date,
      tab: 'reviews',
      urgent: true
    }, {
      id: 'music',
      label: 'Music selection',
      sub: 'Two directions to choose',
      tab: 'reviews'
    }, {
      id: 'endcard',
      label: 'End card',
      sub: 'Logo resolve, 3 options',
      tab: 'reviews'
    }, {
      id: 'legal',
      label: 'Legal approval',
      sub: 'Talent usage term',
      tab: 'scope'
    }],
    milestones: [{
      label: 'Client review',
      date: 'Jun 5',
      state: 'current'
    }, {
      label: 'Picture lock',
      date: 'Jun 13',
      state: 'upcoming'
    }, {
      label: 'Color review',
      date: 'Jun 19',
      state: 'upcoming'
    }, {
      label: 'Final delivery',
      date: 'Jun 27',
      state: 'upcoming'
    }],
    costDrivers: [{
      label: 'Additional deliverables',
      amt: 6000
    }, {
      label: 'Night overtime',
      amt: 18000
    }, {
      label: 'Location overage',
      amt: 4000
    }],
    activity: [{
      icon: 'film',
      text: 'Latest edit uploaded',
      meta: '01_BriefHistory_HD \u00b7 Jun 4',
      tone: 'amber'
    }, {
      icon: 'chat',
      text: '2 new comments added',
      meta: 'On the current cut \u00b7 Jun 4'
    }, {
      icon: 'clock',
      text: 'Budget forecast updated',
      meta: money(T.forecast) + ' \u00b7 Jun 3'
    }, {
      icon: 'plus',
      text: 'Scope change requested',
      meta: ':15 cutdown \u00b7 Jun 2'
    }]
  };

  // ── Notifications (global) ──────────────────────────────────
  var notifications = [{
    kind: 'Approval',
    text: 'Edit review is waiting on you',
    tab: 'reviews'
  }, {
    kind: 'Budget',
    text: 'Forecast updated to ' + money(T.forecast),
    tab: 'budget'
  }, {
    kind: 'Schedule',
    text: 'Picture lock set for Jun 13',
    tab: 'timeline'
  }, {
    kind: 'Scope',
    text: ':15 cutdown change requested',
    tab: 'decisions'
  }];

  // ── DECISIONS — the simulator. Each candidate change carries
  //    its real consequence across budget / schedule / resources / risk.
  var RESOURCE_SET = ['Editorial', 'Graphics', 'Sound', 'Locations', 'Travel', 'Crew', 'Talent', 'Legal', 'Schedule'];
  var decisions = [{
    id: 'cutdown',
    kind: 'add',
    label: 'Add a :15 cutdown',
    sub: 'A second deliverable from the approved cut',
    budget: 4500,
    days: 2,
    risk: 'Low',
    resources: ['Editorial', 'Graphics', 'Sound'],
    deliverable: '1 additional asset',
    detail: 'Pulls from the scenes already inside the :15 set. No new footage — an editorial pass, a graphics conform, and a sound mix.'
  }, {
    id: 'social',
    kind: 'add',
    label: 'Add a social campaign',
    sub: 'Six platform-native assets',
    budget: 18000,
    days: 3,
    risk: 'Medium',
    resources: ['Editorial', 'Graphics'],
    deliverable: '6 additional assets',
    detail: 'Vertical cutdowns and motion stills built from existing footage. No reshoot — editorial and graphics time only.'
  }, {
    id: 'version',
    kind: 'add',
    label: 'Add a market version',
    sub: 'Localized :30 master for one region',
    budget: 9000,
    days: 2,
    risk: 'Low',
    resources: ['Editorial', 'Legal'],
    deliverable: '1 localized master',
    detail: 'A language pass over the existing cut plus re-clearance of talent usage in the new market.'
  }, {
    id: 'location',
    kind: 'add',
    label: 'Move the ending to a destination coast',
    sub: 'Relocate the beach scene',
    budget: 72000,
    days: 4,
    risk: 'High',
    resources: ['Locations', 'Travel', 'Crew', 'Talent'],
    deliverable: null,
    detail: 'Reopens a wrapped scene. A full company move — travel, a new permit, crew days, and talent availability all reactivate.'
  }, {
    id: 'talent',
    kind: 'add',
    label: 'Recast the partner role',
    sub: 'Replace THEO in the apartment scene',
    budget: 26000,
    days: 5,
    risk: 'High',
    resources: ['Talent', 'Crew', 'Editorial'],
    deliverable: null,
    detail: 'A reshoot of the apartment scene with new casting, then a re-cut. The single most disruptive change on the board.'
  }, {
    id: 'flipday',
    kind: 'reduce',
    label: 'Flip the apartment scene to day',
    sub: 'Remove the night premium',
    budget: -18000,
    days: 0,
    risk: 'Low',
    resources: ['Crew', 'Schedule'],
    deliverable: null,
    detail: 'Clears the crew overtime currently drawing against contingency. The ending plays the same; the budget breathes.'
  }];
  return {
    F: F,
    FW: FW,
    S0: S0,
    T: T,
    money: money,
    signed: signed,
    money0: money0,
    project: project,
    status: status,
    budget: budget,
    timeline: timeline,
    scope: scope,
    risks: risks,
    reviews: reviews,
    priorities: priorities,
    notifications: notifications,
    decisions: decisions,
    RESOURCE_SET: RESOURCE_SET,
    NAV: [{
      id: 'overview',
      label: 'Overview'
    }, {
      id: 'reviews',
      label: 'Reviews'
    }, {
      id: 'timeline',
      label: 'Timeline'
    }, {
      id: 'budget',
      label: 'Budget'
    }, {
      id: 'scope',
      label: 'Scope'
    }, {
      id: 'risks',
      label: 'Risks'
    }, {
      id: 'decisions',
      label: 'Decisions'
    }]
  };
}();
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/client-data.js", error: String((e && e.message) || e) }); }

// flow/client-hero.jsx
try { (() => {
/* global React */
// TENET Client Portal — hero tabs: Overview snapshot + Decisions simulator.
const {
  FK: HFK,
  FGlyph: HG,
  CLIENT: HC
} = window;
const {
  CPanel: HP,
  CPStat: HStat,
  CPBar: HBar,
  CPChip: HChip,
  CPDot: HDot,
  CPHead: HHead,
  useCountUp: HCount,
  cpKicker: HKick,
  cpKickerFaint: HKickF,
  cpSerif: HSerif,
  RISK_COLOR: HRISK
} = window;
const HUse = React.useState,
  HEff = React.useEffect;
function shiftDate(days) {
  const d = new Date(2026, 5, 27);
  d.setDate(d.getDate() + days);
  return d.toLocaleDateString('en-US', {
    month: 'short',
    day: 'numeric'
  });
}

/* ══════════════════════════════════════════════════════════
   OVERVIEW
   ══════════════════════════════════════════════════════════ */
function OverviewTab({
  ctx
}) {
  const s = HC.status,
    b = HC.budget,
    t = HC.timeline,
    p = HC.priorities;
  const forecast = b.forecast + ctx.forecastDelta;
  const variance = forecast - b.approved;
  const over = variance > 0;
  const delivery = shiftDate(ctx.daysDelta);
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(HHead, {
    kicker: "Overview",
    lede: "A complete read on health, money, and what needs you \u2014 in one screen."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(4, 1fr)',
      gap: 16,
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement(CPStatStatus, {
    overall: over ? 'At Risk' : s.overall
  }), /*#__PURE__*/React.createElement(HStat, {
    label: "Forecast final cost",
    value: HC.money(forecast),
    tone: over ? '#B23A2E' : HFK.INK,
    sub: (over ? 'Over by ' : 'Under by ') + HC.money(Math.abs(variance)) + ' vs approved'
  }), /*#__PURE__*/React.createElement(HStat, {
    label: "Schedule",
    value: t.currentPhase,
    sub: t.daysRemaining + ' days to ' + delivery
  }), /*#__PURE__*/React.createElement(HStat, {
    label: "Deliverables",
    value: "1 pending",
    sub: '2 approved \u00b7 3 in progress'
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1.5fr 1fr',
      gap: 16,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(HP, {
    label: "Project health",
    title: "Overall \u2014 on track"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 18
    }
  }, s.health.map(h => /*#__PURE__*/React.createElement("div", {
    key: h.id
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline',
      marginBottom: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: HFK.SANS,
      fontSize: 14,
      color: HFK.INK
    }
  }, h.label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: HFK.MONO,
      fontSize: 10,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: h.id === 'creative' ? HFK.AMBER : HFK.INK_MUTED
    }
  }, h.state)), /*#__PURE__*/React.createElement(HBar, {
    pct: h.score,
    color: h.id === 'creative' ? HFK.AMBER : HFK.INK
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 8,
      fontFamily: HFK.SANS,
      fontSize: 12.5,
      color: HFK.INK_MUTED,
      lineHeight: 1.4
    }
  }, h.note))))), /*#__PURE__*/React.createElement(HP, {
    label: "Top cost drivers",
    title: "What is moving the number"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, p.costDrivers.map((d, i) => {
    const max = Math.max.apply(null, p.costDrivers.map(x => x.amt));
    return /*#__PURE__*/React.createElement("div", {
      key: i
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'baseline',
        marginBottom: 7
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: HFK.SANS,
        fontSize: 14,
        color: HFK.INK
      }
    }, d.label), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: HFK.MONO,
        fontSize: 12,
        color: HFK.INK,
        fontVariantNumeric: 'tabular-nums'
      }
    }, HC.money(d.amt))), /*#__PURE__*/React.createElement(HBar, {
      pct: d.amt / max * 100,
      color: HFK.AMBER_200
    }));
  }))), /*#__PURE__*/React.createElement(HP, {
    label: "Recent activity",
    title: "Latest in the room"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, p.activity.map((a, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 14,
      padding: '13px 0',
      borderTop: i ? `1px solid ${HFK.BORDER}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 30,
      height: 30,
      borderRadius: 6,
      flexShrink: 0,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      border: `1px solid ${HFK.BORDER}`,
      color: a.tone === 'amber' ? HFK.AMBER : HFK.INK_MUTED,
      background: a.tone === 'amber' ? HFK.AMBER_50 : HFK.PAPER
    }
  }, /*#__PURE__*/React.createElement(HG, {
    name: a.icon,
    size: 15
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontSize: 14,
      color: HFK.INK
    }
  }, a.text), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.MONO,
      fontSize: 10.5,
      letterSpacing: '0.06em',
      color: HFK.INK_FAINT,
      marginTop: 2
    }
  }, a.meta))))))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(HP, {
    label: "Needs your approval",
    title: p.needsApproval.length + ' waiting',
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, p.needsApproval.map(n => /*#__PURE__*/React.createElement("button", {
    key: n.id,
    onClick: () => ctx.goTab(n.tab),
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 12,
      textAlign: 'left',
      cursor: 'pointer',
      padding: '13px 14px',
      borderRadius: 4,
      border: `1px solid ${n.urgent ? HFK.AMBER : HFK.BORDER}`,
      background: n.urgent ? HFK.AMBER_50 : HFK.PAPER
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontSize: 14.5,
      color: HFK.INK
    }
  }, n.label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontSize: 12.5,
      color: HFK.INK_MUTED,
      marginTop: 1
    }
  }, n.sub)), /*#__PURE__*/React.createElement(HG, {
    name: "arrow",
    size: 15,
    style: {
      color: n.urgent ? HFK.AMBER : HFK.INK_FAINT,
      flexShrink: 0
    }
  }))))), /*#__PURE__*/React.createElement(HP, {
    label: "Upcoming",
    title: "Milestones",
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, p.milestones.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '12px 0',
      borderTop: i ? `1px solid ${HFK.BORDER}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement(HDot, {
    state: m.state
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: HFK.SANS,
      fontSize: 14,
      color: m.state === 'current' ? HFK.INK : HFK.INK_MUTED
    }
  }, m.label)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: HFK.MONO,
      fontSize: 11,
      letterSpacing: '0.06em',
      color: m.state === 'current' ? HFK.AMBER : HFK.INK_FAINT
    }
  }, m.date))))), /*#__PURE__*/React.createElement("button", {
    onClick: () => ctx.goTab('decisions'),
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      cursor: 'pointer',
      padding: '18px 22px',
      borderRadius: 4,
      border: 'none',
      background: HFK.INK,
      color: HFK.PAPER
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      textAlign: 'left'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: HFK.AMBER_200
    }
  }, "Decision simulator"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontWeight: 300,
      fontSize: 18,
      marginTop: 6
    }
  }, "See what a change would do")), /*#__PURE__*/React.createElement(HG, {
    name: "arrow",
    size: 18,
    style: {
      color: HFK.PAPER
    }
  })))));
}
function CPStatStatus({
  overall
}) {
  const onTrack = overall === 'On Track';
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${onTrack ? HFK.BORDER : HFK.AMBER}`,
      background: onTrack ? HFK.PAPER : HFK.AMBER_50,
      borderRadius: 4,
      padding: '18px 20px',
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: HKickF
  }, "Project status"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 9,
      height: 9,
      borderRadius: 999,
      background: onTrack ? '#1F8A5B' : HFK.AMBER
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: HFK.SANS,
      fontWeight: 300,
      fontSize: 28,
      lineHeight: 1,
      color: HFK.INK
    }
  }, overall)));
}

/* ══════════════════════════════════════════════════════════
   DECISIONS — the simulator
   ══════════════════════════════════════════════════════════ */
function DecisionsTab({
  ctx
}) {
  const [sel, setSel] = HUse(HC.decisions[0].id);
  const [revealed, setRevealed] = HUse(false);
  const d = HC.decisions.find(x => x.id === sel);
  const isCommitted = ctx.committed.indexOf(sel) >= 0;
  HEff(() => {
    setRevealed(false);
    const id = setTimeout(() => setRevealed(true), 40);
    return () => clearTimeout(id);
  }, [sel]);
  const b = HC.budget;
  const forecast = b.forecast + ctx.forecastDelta;
  const variance = forecast - b.approved;
  const over = variance > 0;
  const budgetTarget = revealed ? d.budget : 0;
  const animBudget = HCount(budgetTarget, 700);
  function approve() {
    if (!isCommitted) ctx.setCommitted(prev => prev.concat([sel]));
  }
  function undo() {
    ctx.setCommitted(prev => prev.filter(x => x !== sel));
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(3, 1fr)',
      gap: 16,
      marginBottom: 28
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${over ? HFK.AMBER : HFK.BORDER}`,
      background: over ? HFK.AMBER_50 : HFK.PAPER,
      borderRadius: 4,
      padding: '16px 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: HKickF
  }, "Forecast / approved"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontWeight: 300,
      fontSize: 24,
      marginTop: 8,
      color: over ? '#B23A2E' : HFK.INK,
      fontVariantNumeric: 'tabular-nums'
    }
  }, HC.money(forecast), " ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: HFK.INK_FAINT,
      fontSize: 16
    }
  }, "/ ", HC.money(b.approved))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement(HBar, {
    pct: forecast / b.approved * 100,
    color: over ? '#B23A2E' : HFK.AMBER
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${HFK.BORDER}`,
      background: HFK.PAPER,
      borderRadius: 4,
      padding: '16px 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: HKickF
  }, "Delivery"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontWeight: 300,
      fontSize: 24,
      marginTop: 8,
      color: HFK.INK
    }
  }, shiftDate(ctx.daysDelta)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.MONO,
      fontSize: 11,
      color: ctx.daysDelta ? HFK.AMBER : HFK.INK_FAINT,
      marginTop: 8
    }
  }, ctx.daysDelta ? '+' + ctx.daysDelta + ' days committed' : 'On original date')), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${HFK.BORDER}`,
      background: HFK.PAPER,
      borderRadius: 4,
      padding: '16px 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: HKickF
  }, "Committed changes"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontWeight: 300,
      fontSize: 24,
      marginTop: 8,
      color: HFK.INK
    }
  }, ctx.committed.length), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.MONO,
      fontSize: 11,
      color: HFK.INK_FAINT,
      marginTop: 8
    }
  }, ctx.committed.length ? 'Reflected across the portal' : 'Nothing approved yet'))), /*#__PURE__*/React.createElement(HHead, {
    kicker: "Decisions",
    lede: "Pick a change. See the budget, schedule, resources, and risk before you approve anything."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '320px 1fr',
      gap: 24,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, HKickF, {
      marginBottom: 4
    })
  }, "Active decisions"), HC.decisions.map(o => {
    const on = o.id === sel;
    const com = ctx.committed.indexOf(o.id) >= 0;
    return /*#__PURE__*/React.createElement("button", {
      key: o.id,
      onClick: () => setSel(o.id),
      style: {
        position: 'relative',
        textAlign: 'left',
        cursor: 'pointer',
        padding: '14px 16px',
        borderRadius: 4,
        border: `1px solid ${on ? HFK.INK : HFK.BORDER}`,
        background: on ? HFK.PAPER : 'transparent',
        boxShadow: on ? '0 6px 18px rgba(0,0,0,0.06)' : 'none',
        transition: 'all 160ms ease'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        gap: 8
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: HFK.SANS,
        fontSize: 14.5,
        color: HFK.INK,
        lineHeight: 1.25
      }
    }, o.label), com && /*#__PURE__*/React.createElement(HG, {
      name: "check",
      size: 14,
      style: {
        color: '#1F8A5B',
        flexShrink: 0
      }
    })), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 8,
        marginTop: 7
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: HFK.MONO,
        fontSize: 11,
        color: o.budget < 0 ? '#1F8A5B' : HFK.INK_MUTED,
        fontVariantNumeric: 'tabular-nums'
      }
    }, HC.signed(o.budget)), /*#__PURE__*/React.createElement("span", {
      style: {
        width: 3,
        height: 3,
        borderRadius: 999,
        background: HFK.INK_FAINT
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        width: 6,
        height: 6,
        borderRadius: 999,
        background: HRISK[o.risk]
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: HFK.MONO,
        fontSize: 10,
        letterSpacing: '0.1em',
        textTransform: 'uppercase',
        color: HFK.INK_FAINT
      }
    }, o.risk)));
  })), /*#__PURE__*/React.createElement("div", {
    key: sel,
    style: {
      border: `1px solid ${HFK.BORDER_STRONG}`,
      borderRadius: 6,
      background: HFK.PAPER,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '22px 28px',
      borderBottom: `1px solid ${HFK.BORDER}`,
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'flex-start',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: d.kind === 'reduce' ? '#1F8A5B' : HFK.AMBER
    }
  }, d.kind === 'reduce' ? 'Scope reduction' : 'Scope addition'), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontWeight: 300,
      fontSize: 26,
      marginTop: 8,
      color: HFK.INK
    }
  }, d.label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SERIF,
      fontStyle: 'italic',
      fontSize: 15,
      color: HFK.INK_MUTED,
      marginTop: 4
    }
  }, d.sub)), isCommitted && /*#__PURE__*/React.createElement(CPChipSm, {
    tone: "#1F8A5B",
    label: "Committed"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 1,
      background: HFK.BORDER
    }
  }, /*#__PURE__*/React.createElement(ImpactCell, {
    label: "Budget impact"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontWeight: 300,
      fontSize: 40,
      lineHeight: 1,
      color: d.budget < 0 ? '#1F8A5B' : HFK.INK,
      fontVariantNumeric: 'tabular-nums'
    }
  }, HC.signed(animBudget)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontSize: 13,
      color: HFK.INK_MUTED,
      marginTop: 10
    }
  }, "Forecast would move to ", /*#__PURE__*/React.createElement("b", {
    style: {
      color: HFK.INK,
      fontWeight: 400
    }
  }, HC.money(forecast + (isCommitted ? 0 : d.budget))))), /*#__PURE__*/React.createElement(ImpactCell, {
    label: "Schedule impact"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontWeight: 300,
      fontSize: 40,
      lineHeight: 1,
      color: d.days ? HFK.INK : '#1F8A5B',
      fontVariantNumeric: 'tabular-nums'
    }
  }, d.days ? '+' + d.days + ' days' : 'No change'), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 14,
      display: 'flex',
      gap: 4
    }
  }, Array.from({
    length: 8
  }).map((_, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      flex: 1,
      height: 6,
      borderRadius: 1,
      background: i < 5 ? HFK.INK_FAINT : i < 5 + d.days ? HFK.AMBER : HFK.BORDER,
      opacity: revealed ? 1 : 0.2,
      transition: 'all 320ms ease ' + i * 50 + 'ms'
    }
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontSize: 13,
      color: HFK.INK_MUTED,
      marginTop: 10
    }
  }, "Delivery ", d.days ? 'moves to ' : 'holds at ', /*#__PURE__*/React.createElement("b", {
    style: {
      color: HFK.INK,
      fontWeight: 400
    }
  }, shiftDate(ctx.daysDelta + (isCommitted ? 0 : d.days))))), /*#__PURE__*/React.createElement(ImpactCell, {
    label: "Resources activated"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 8
    }
  }, HC.RESOURCE_SET.map((r, i) => {
    const on = d.resources.indexOf(r) >= 0;
    return /*#__PURE__*/React.createElement("span", {
      key: r,
      style: {
        opacity: revealed ? 1 : 0,
        transform: revealed ? 'translateY(0)' : 'translateY(6px)',
        transition: 'all 300ms ease ' + i * 55 + 'ms'
      }
    }, /*#__PURE__*/React.createElement(HChip, {
      on: on,
      dot: true
    }, r));
  })), d.deliverable && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontSize: 13,
      color: HFK.INK_MUTED,
      marginTop: 14
    }
  }, "Adds ", /*#__PURE__*/React.createElement("b", {
    style: {
      color: HFK.INK,
      fontWeight: 400
    }
  }, d.deliverable))), /*#__PURE__*/React.createElement(ImpactCell, {
    label: "Risk impact"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: HFK.SANS,
      fontWeight: 300,
      fontSize: 40,
      lineHeight: 1,
      color: HRISK[d.risk]
    }
  }, d.risk)), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 14,
      display: 'flex',
      gap: 6
    }
  }, ['Low', 'Medium', 'High'].map((lv, i) => {
    const active = d.risk === 'Low' && i === 0 || d.risk === 'Medium' && i <= 1 || d.risk === 'High';
    return /*#__PURE__*/React.createElement("div", {
      key: lv,
      style: {
        flex: 1,
        height: 8,
        borderRadius: 2,
        background: active ? HRISK[d.risk] : HFK.BORDER,
        opacity: revealed ? 1 : 0.25,
        transition: 'all 320ms ease ' + i * 90 + 'ms'
      }
    });
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontSize: 13,
      color: HFK.INK_MUTED,
      marginTop: 10
    }
  }, d.detail))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '20px 28px',
      borderTop: `1px solid ${HFK.BORDER}`,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SERIF,
      fontStyle: 'italic',
      fontSize: 14,
      color: HFK.INK_MUTED,
      maxWidth: 440
    }
  }, isCommitted ? 'This change is committed and reflected across Budget, Timeline, and Overview.' : 'Approving simulates the change across the whole portal — nothing is sent to the room yet.'), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 10,
      flexShrink: 0
    }
  }, isCommitted ? /*#__PURE__*/React.createElement("button", {
    onClick: undo,
    style: btnGhost
  }, "Undo") : /*#__PURE__*/React.createElement("button", {
    onClick: approve,
    style: d.kind === 'reduce' ? btnGreen : btnAmber
  }, d.kind === 'reduce' ? 'Apply reduction' : 'Approve change'))))));
}
function ImpactCell({
  label,
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      background: HFK.PAPER,
      padding: '22px 26px',
      minHeight: 150
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, HKickF, {
      marginBottom: 16
    })
  }, label), children);
}
function CPChipSm({
  label,
  tone
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: HFK.MONO,
      fontSize: 10,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: tone,
      border: `1px solid ${tone}`,
      borderRadius: 3,
      padding: '6px 10px',
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 5,
      height: 5,
      borderRadius: 999,
      background: tone
    }
  }), label);
}
const btnBaseH = {
  borderRadius: 3,
  padding: '12px 22px',
  cursor: 'pointer',
  fontFamily: HFK.MONO,
  fontSize: 11,
  letterSpacing: '0.14em',
  textTransform: 'uppercase',
  border: 'none'
};
const btnAmber = Object.assign({}, btnBaseH, {
  background: HFK.AMBER,
  color: HFK.PAPER
});
const btnGreen = Object.assign({}, btnBaseH, {
  background: '#1F8A5B',
  color: HFK.PAPER
});
const btnGhost = Object.assign({}, btnBaseH, {
  background: 'transparent',
  color: HFK.INK,
  border: `1px solid ${HFK.BORDER_STRONG}`
});
Object.assign(window, {
  OverviewTab,
  DecisionsTab
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/client-hero.jsx", error: String((e && e.message) || e) }); }

// flow/client-shell.jsx
try { (() => {
/* global React */
// TENET Client Portal — app shell, primary nav, and shared primitives.
// Decision state is lifted here so an approved change ripples into the
// Budget forecast, the Timeline delivery date, and the Overview snapshot.
const {
  FK,
  FGlyph,
  CLIENT
} = window;
const {
  useState: cpUseState,
  useEffect: cpUseEffect,
  useRef: cpUseRef
} = React;

// ── Shared style atoms ──────────────────────────────────────
const cpKicker = {
  fontFamily: FK.MONO,
  fontSize: 10,
  letterSpacing: '0.22em',
  textTransform: 'uppercase',
  color: FK.AMBER,
  whiteSpace: 'nowrap'
};
const cpKickerFaint = {
  fontFamily: FK.MONO,
  fontSize: 10,
  letterSpacing: '0.22em',
  textTransform: 'uppercase',
  color: FK.INK_FAINT,
  whiteSpace: 'nowrap'
};
const cpPanelTitle = {
  fontFamily: FK.SANS,
  fontWeight: 300,
  fontSize: 22,
  lineHeight: 1.1,
  color: FK.INK,
  marginTop: 8
};
const cpSerif = {
  fontFamily: FK.SERIF,
  fontStyle: 'italic',
  fontWeight: 300
};
const STATE_COLOR = {
  done: FK.INK_FAINT,
  current: FK.AMBER,
  upcoming: FK.INK_FAINT
};
const RISK_COLOR = {
  Low: '#1F8A5B',
  Medium: FK.AMBER,
  High: '#B23A2E'
};

// ── Panel ───────────────────────────────────────────────────
// `subject` lifts the panel onto the bright foreground plane (warm shadow +
// amber spine) — the resolved focus. At rest, panels stay flat hairline
// surfaces (legible context). This is the morph contract, made physical.
function CPanel({
  label,
  title,
  right,
  children,
  style,
  pad,
  subject
}) {
  const base = subject ? {
    border: `1px solid ${FK.BORDER}`,
    background: FK.PAPER,
    borderRadius: 6,
    boxShadow: FK.SHADOW_LIFT,
    borderLeft: `2px solid ${FK.AMBER}`
  } : {
    border: `1px solid ${FK.BORDER}`,
    background: FK.PAPER,
    borderRadius: 6
  };
  return /*#__PURE__*/React.createElement("div", {
    style: Object.assign(base, {
      padding: pad == null ? 28 : pad
    }, style)
  }, (label || title || right) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'flex-start',
      marginBottom: title ? 20 : 16,
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("div", null, label && /*#__PURE__*/React.createElement("div", {
    style: cpKicker
  }, label), title && /*#__PURE__*/React.createElement("div", {
    style: cpPanelTitle
  }, title)), right), children);
}

// ── Stat card ───────────────────────────────────────────────
function CPStat({
  label,
  value,
  sub,
  tone,
  big,
  subject
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${FK.BORDER}`,
      background: FK.PAPER,
      borderRadius: 6,
      padding: '20px 22px',
      display: 'flex',
      flexDirection: 'column',
      gap: 9,
      minWidth: 0,
      boxShadow: subject ? FK.SHADOW_LIFT : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: cpKickerFaint
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: big ? 36 : 29,
      lineHeight: 1,
      color: tone || FK.INK,
      letterSpacing: '-0.015em',
      fontVariantNumeric: 'tabular-nums'
    }
  }, value), sub && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 12.5,
      color: FK.INK_MUTED,
      lineHeight: 1.35
    }
  }, sub));
}

// ── Bar ─────────────────────────────────────────────────────
function CPBar({
  pct,
  color,
  track,
  h
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: '100%',
      height: h || 4,
      background: track || FK.BORDER,
      borderRadius: 999,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      top: 0,
      bottom: 0,
      width: Math.max(0, Math.min(100, pct)) + '%',
      background: color || FK.AMBER,
      borderRadius: 999,
      transition: 'width 700ms cubic-bezier(.2,.7,.2,1)'
    }
  }));
}

// ── Pill / chip ─────────────────────────────────────────────
function CPChip({
  children,
  on,
  tone,
  dot
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 7,
      padding: '5px 11px',
      borderRadius: 3,
      whiteSpace: 'nowrap',
      border: `1px solid ${on ? tone || FK.AMBER : FK.BORDER}`,
      background: on ? FK.AMBER_50 : 'transparent',
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.08em',
      textTransform: 'uppercase',
      color: on ? tone || FK.AMBER_800 : FK.INK_MUTED,
      transition: 'all 260ms ease'
    }
  }, dot && /*#__PURE__*/React.createElement("span", {
    style: {
      width: 5,
      height: 5,
      borderRadius: 999,
      background: on ? tone || FK.AMBER : FK.INK_FAINT
    }
  }), children);
}

// ── Status dot ──────────────────────────────────────────────
function CPDot({
  state,
  size
}) {
  const s = size || 7;
  const filled = state === 'current' || state === 'done';
  return /*#__PURE__*/React.createElement("span", {
    style: {
      width: s,
      height: s,
      borderRadius: 999,
      flexShrink: 0,
      background: state === 'current' ? FK.AMBER : state === 'done' ? FK.INK : 'transparent',
      border: state === 'upcoming' ? `1.5px solid ${FK.BORDER_STRONG}` : 'none',
      boxSizing: 'border-box'
    }
  });
}

// ── Section heading inside a tab ────────────────────────────
function CPHead({
  kicker,
  title,
  lede
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: title ? 28 : 22
    }
  }, kicker && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      marginBottom: title ? 16 : 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 22,
      height: 2,
      background: FK.AMBER,
      display: 'inline-block'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: cpKicker
  }, kicker)), title && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: 40,
      lineHeight: 1.02,
      letterSpacing: '-0.02em',
      color: FK.INK
    }
  }, title), lede && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: title ? 16 : 0,
      maxWidth: 640,
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontWeight: 300,
      fontSize: 19,
      lineHeight: 1.5,
      color: FK.INK_MUTED
    }
  }, lede));
}

// ── Count-up number (for the simulator) ─────────────────────
function useCountUp(target, dur) {
  const [v, setV] = cpUseState(target);
  const ref = cpUseRef({
    from: target,
    to: target,
    t0: 0,
    raf: 0
  });
  cpUseEffect(() => {
    const st = ref.current;
    st.from = v;
    st.to = target;
    st.t0 = 0;
    cancelAnimationFrame(st.raf);
    const tick = ts => {
      if (!st.t0) st.t0 = ts;
      const p = Math.min(1, (ts - st.t0) / (dur || 650));
      const e = 1 - Math.pow(1 - p, 3);
      setV(Math.round(st.from + (st.to - st.from) * e));
      if (p < 1) st.raf = requestAnimationFrame(tick);
    };
    st.raf = requestAnimationFrame(tick);
    return () => cancelAnimationFrame(st.raf);
    // eslint-disable-next-line
  }, [target]);
  return v;
}

// ── Header + nav + tab router ───────────────────────────────
function ClientPortal({
  onReset
}) {
  const [tab, setTab] = cpUseState('reviews'); // land on the cut, per spec
  const [committed, setCommitted] = cpUseState([]); // approved decision ids
  const [notifOpen, setNotifOpen] = cpUseState(false);
  const scrollRef = cpUseRef(null);
  const cd = CLIENT.decisions.filter(d => committed.indexOf(d.id) >= 0);
  const forecastDelta = cd.reduce((a, d) => a + d.budget, 0);
  const daysDelta = cd.reduce((a, d) => a + d.days, 0);
  function goTab(t) {
    setTab(t);
    setNotifOpen(false);
    if (scrollRef.current) scrollRef.current.scrollTop = 0;
  }
  const ctx = {
    committed,
    setCommitted,
    forecastDelta,
    daysDelta,
    goTab
  };
  const VIEWS = {
    overview: window.OverviewTab,
    reviews: window.ReviewsTab,
    timeline: window.TimelineTab,
    budget: window.BudgetTab,
    scope: window.ScopeTab,
    risks: window.RisksTab,
    decisions: window.DecisionsTab
  };
  const View = VIEWS[tab] || function () {
    return null;
  };
  const approvalCount = CLIENT.priorities.needsApproval.length;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      background: FK.PAPER_SOFT,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      height: 64,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '0 40px',
      borderBottom: `1px solid ${FK.BORDER}`,
      background: FK.PAPER,
      position: 'relative',
      zIndex: 20,
      boxShadow: '0 1px 0 rgba(38,28,12,0.035)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 15
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 23,
      color: FK.INK,
      letterSpacing: '0.01em'
    }
  }, "tenet"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.24em',
      textTransform: 'uppercase',
      color: FK.AMBER,
      whiteSpace: 'nowrap'
    }
  }, "Client Portal"), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 1,
      height: 17,
      background: FK.BORDER_STRONG,
      display: 'inline-block',
      alignSelf: 'center'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: Object.assign({}, cpSerif, {
      fontSize: 15.5,
      color: FK.INK,
      whiteSpace: 'nowrap'
    })
  }, CLIENT.project.title)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 22
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'none',
      alignItems: 'center',
      gap: 8,
      color: FK.INK_FAINT
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative'
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: () => setNotifOpen(o => !o),
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      background: 'none',
      border: 0,
      cursor: 'pointer',
      padding: 0,
      color: notifOpen ? FK.AMBER : FK.INK_MUTED
    }
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "pin",
    size: 16
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.08em',
      color: 'inherit'
    }
  }, approvalCount, " waiting")), notifOpen && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 30,
      right: 0,
      width: 320,
      background: FK.PAPER,
      border: `1px solid ${FK.BORDER_STRONG}`,
      borderRadius: 6,
      boxShadow: '0 18px 48px rgba(0,0,0,0.14)',
      padding: 8,
      zIndex: 40
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, cpKickerFaint, {
      padding: '8px 10px 10px'
    })
  }, "Notification center"), CLIENT.notifications.map((n, i) => /*#__PURE__*/React.createElement("button", {
    key: i,
    onClick: () => goTab(n.tab),
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 12,
      width: '100%',
      textAlign: 'left',
      background: 'none',
      border: 0,
      borderTop: `1px solid ${FK.BORDER}`,
      padding: '12px 10px',
      cursor: 'pointer'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: FK.AMBER,
      flex: '0 0 64px'
    }
  }, n.kind), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 13,
      color: FK.INK,
      lineHeight: 1.4
    }
  }, n.text))))), /*#__PURE__*/React.createElement("button", {
    className: "reset-btn",
    onClick: onReset
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 13,
    style: {
      transform: 'scaleX(-1)'
    }
  }), " Change lane"))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      height: 50,
      display: 'flex',
      alignItems: 'stretch',
      gap: 2,
      padding: '0 30px',
      borderBottom: `1px solid ${FK.BORDER}`,
      background: FK.PAPER,
      position: 'relative',
      zIndex: 10
    }
  }, CLIENT.NAV.map(n => {
    const on = n.id === tab;
    return /*#__PURE__*/React.createElement("button", {
      key: n.id,
      onClick: () => goTab(n.id),
      style: {
        position: 'relative',
        display: 'flex',
        alignItems: 'center',
        gap: 8,
        background: 'none',
        border: 0,
        borderBottom: on ? `2px solid ${FK.AMBER}` : '2px solid transparent',
        cursor: 'pointer',
        padding: '0 16px',
        marginBottom: -1,
        transition: 'color 200ms ease',
        fontFamily: FK.MONO,
        fontSize: 11,
        letterSpacing: '0.14em',
        textTransform: 'uppercase',
        color: on ? FK.INK : FK.INK_FAINT
      }
    }, n.label, n.id === 'decisions' && /*#__PURE__*/React.createElement("span", {
      style: {
        width: 5,
        height: 5,
        borderRadius: 999,
        background: FK.AMBER
      }
    }));
  })), /*#__PURE__*/React.createElement("div", {
    ref: scrollRef,
    style: {
      flex: 1,
      minHeight: 0,
      overflowY: 'auto',
      overflowX: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 1180,
      margin: '0 auto',
      padding: '36px 48px 64px'
    }
  }, /*#__PURE__*/React.createElement(View, {
    ctx: ctx
  }))));
}
Object.assign(window, {
  CPanel,
  CPStat,
  CPBar,
  CPChip,
  CPDot,
  CPHead,
  useCountUp,
  ClientPortal,
  cpKicker,
  cpKickerFaint,
  cpPanelTitle,
  cpSerif,
  STATE_COLOR,
  RISK_COLOR
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/client-shell.jsx", error: String((e && e.message) || e) }); }

// flow/client-tabs1.jsx
try { (() => {
/* global React */
// TENET Client Portal — Reviews (the landing cut) + Timeline.
const {
  FK: RFK,
  FGlyph: RG,
  CLIENT: RC
} = window;
const {
  CPanel: RP,
  CPStat: RStat,
  CPBar: RBar,
  CPDot: RDot,
  CPHead: RHead,
  cpKickerFaint: RKickF
} = window;
const RUse = React.useState;
function rShift(days) {
  const d = new Date(2026, 5, 27);
  d.setDate(d.getDate() + days);
  return d.toLocaleDateString('en-US', {
    month: 'short',
    day: 'numeric'
  });
}

/* ══════════════════════════════════════════════════════════
   REVIEWS — the cut, kept as the landing
   ══════════════════════════════════════════════════════════ */
function ReviewsTab({
  ctx
}) {
  const r = RC.reviews;
  const [asset, setAsset] = RUse('edits');
  const [cutId, setCutId] = RUse('v4');
  const [decision, setDecision] = RUse(null);
  const cut = r.cuts.find(c => c.id === cutId);
  const a = r.assets.find(x => x.id === asset);
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(RHead, {
    kicker: "Reviews",
    lede: "See the idea before it\u2019s made. Approve the cut, or send a note straight back to the room."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '156px 1fr 300px',
      gap: 24,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 4
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, RKickF, {
      marginBottom: 8
    })
  }, "Project assets"), r.assets.map(x => {
    const on = x.id === asset;
    return /*#__PURE__*/React.createElement("button", {
      key: x.id,
      onClick: () => setAsset(x.id),
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        gap: 8,
        textAlign: 'left',
        cursor: 'pointer',
        padding: '10px 12px',
        borderRadius: 4,
        border: `1px solid ${on ? RFK.INK : 'transparent'}`,
        background: on ? RFK.PAPER : 'transparent'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: RFK.SANS,
        fontSize: 13,
        color: on ? RFK.INK : RFK.INK_MUTED,
        lineHeight: 1.2
      }
    }, x.name), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: RFK.MONO,
        fontSize: 10,
        color: RFK.INK_FAINT
      }
    }, x.count));
  })), /*#__PURE__*/React.createElement("div", null, asset === 'edits' ? /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      marginBottom: 10,
      flexWrap: 'wrap'
    }
  }, r.cuts.map(c => {
    const on = c.id === cutId;
    return /*#__PURE__*/React.createElement("button", {
      key: c.id,
      onClick: () => setCutId(c.id),
      title: c.date + ' · ' + c.note,
      style: {
        display: 'inline-flex',
        alignItems: 'center',
        gap: 6,
        cursor: 'pointer',
        padding: '5px 10px',
        borderRadius: 3,
        whiteSpace: 'nowrap',
        border: `1px solid ${on ? RFK.AMBER : RFK.BORDER}`,
        background: on ? RFK.AMBER_50 : 'transparent',
        fontFamily: RFK.MONO,
        fontSize: 10,
        letterSpacing: '0.06em',
        color: on ? RFK.AMBER_800 : RFK.INK_MUTED
      }
    }, on && /*#__PURE__*/React.createElement("span", {
      style: {
        width: 5,
        height: 5,
        borderRadius: 999,
        background: RFK.AMBER
      }
    }), c.tag, /*#__PURE__*/React.createElement("span", {
      style: {
        color: RFK.INK_FAINT
      }
    }, c.date));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      marginBottom: 10,
      color: RFK.INK_FAINT,
      whiteSpace: 'nowrap'
    }
  }, /*#__PURE__*/React.createElement(RG, {
    name: "film",
    size: 14
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.MONO,
      fontSize: 11,
      letterSpacing: '0.08em'
    }
  }, cut.name), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.SERIF,
      fontStyle: 'italic',
      fontSize: 12,
      color: RFK.INK_FAINT
    }
  }, cut.note)), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: '100%',
      aspectRatio: '16 / 9',
      background: '#000',
      border: `1px solid ${RFK.BORDER_STRONG}`,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("iframe", {
    src: "https://player.vimeo.com/video/1198884434?title=0&byline=0&portrait=0&badge=0&autopause=0&player_id=0&app_id=58479",
    allow: "autoplay; fullscreen; picture-in-picture; clipboard-write; encrypted-media; web-share",
    referrerPolicy: "strict-origin-when-cross-origin",
    title: RC.project.title,
    style: {
      position: 'absolute',
      inset: 0,
      width: '100%',
      height: '100%',
      border: 0,
      display: 'block'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 0,
      left: 0,
      right: 0,
      height: 40,
      background: 'linear-gradient(rgba(0,0,0,0.5),transparent)',
      pointerEvents: 'none'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 12,
      left: 14,
      fontFamily: RFK.MONO,
      fontSize: 10,
      letterSpacing: '0.18em',
      color: 'rgba(255,255,255,0.7)',
      pointerEvents: 'none'
    }
  }, "PROGRAM \xB7 16\xD79")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(' + r.beats.length + ',1fr)',
      marginTop: 14
    }
  }, r.beats.map((bt, i) => /*#__PURE__*/React.createElement("span", {
    key: bt,
    style: {
      textAlign: 'center',
      fontFamily: RFK.MONO,
      fontSize: 11,
      letterSpacing: '0.08em',
      color: i === 4 ? RFK.AMBER : RFK.INK_FAINT,
      fontWeight: i === 4 ? 700 : 400
    }
  }, bt)))) : /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      marginBottom: 14
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.SANS,
      fontWeight: 300,
      fontSize: 22,
      color: RFK.INK
    }
  }, a.name), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.MONO,
      fontSize: 11,
      color: RFK.INK_FAINT
    }
  }, a.count, " items")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(3, 1fr)',
      gap: 12
    }
  }, Array.from({
    length: Math.min(6, a.count)
  }).map((_, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      aspectRatio: '4 / 3',
      borderRadius: 4,
      border: `1px solid ${RFK.BORDER}`,
      background: 'repeating-linear-gradient(135deg, #FAF8F4 0 9px, #F2EDE4 9px 18px)',
      display: 'flex',
      alignItems: 'flex-end',
      padding: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: RFK.INK_FAINT
    }
  }, a.name.split(' ')[0], " ", String(i + 1).padStart(2, '0'))))))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(RP, {
    label: "For your review",
    title: null,
    pad: 22,
    subject: true
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: RFK.SERIF,
      fontStyle: 'italic',
      fontWeight: 300,
      fontSize: 22,
      lineHeight: 1.2,
      color: RFK.INK,
      marginBottom: 12
    }
  }, RC.project.title), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: RFK.SANS,
      fontSize: 13.5,
      lineHeight: 1.55,
      color: RFK.INK_MUTED,
      marginBottom: 20
    }
  }, "The cut as it stands today \u2014 approve it, or send a note back to the room."), !decision ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: () => setDecision('approved'),
    style: {
      borderRadius: 3,
      padding: '12px 16px',
      cursor: 'pointer',
      fontFamily: RFK.MONO,
      fontSize: 11,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      background: RFK.AMBER,
      color: RFK.PAPER,
      border: 'none'
    }
  }, "Approve this cut"), /*#__PURE__*/React.createElement("button", {
    onClick: () => setDecision('changes'),
    style: {
      borderRadius: 3,
      padding: '12px 16px',
      cursor: 'pointer',
      fontFamily: RFK.MONO,
      fontSize: 11,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      background: 'transparent',
      color: RFK.INK,
      border: `1px solid ${RFK.BORDER_STRONG}`
    }
  }, "Send notes")) : /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 18px',
      border: `1px solid ${decision === 'approved' ? 'rgba(31,138,91,0.4)' : RFK.AMBER}`,
      background: decision === 'approved' ? 'rgba(31,138,91,0.08)' : RFK.AMBER_50,
      borderRadius: 4
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: RFK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: decision === 'approved' ? '#1F8A5B' : RFK.AMBER_800,
      marginBottom: 6
    }
  }, decision === 'approved' ? 'Approved' : 'Notes sent'), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: RFK.SERIF,
      fontStyle: 'italic',
      fontSize: 14,
      color: RFK.INK
    }
  }, decision === 'approved' ? 'Sent to the room. Production can lock the bid.' : 'Your note is on its way to the creative seat.'), /*#__PURE__*/React.createElement("button", {
    onClick: () => setDecision(null),
    style: {
      marginTop: 12,
      background: 'none',
      border: 0,
      padding: 0,
      cursor: 'pointer',
      fontFamily: RFK.MONO,
      fontSize: 10,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: RFK.INK_FAINT,
      borderBottom: `1px solid ${RFK.BORDER}`
    }
  }, "Undo"))), /*#__PURE__*/React.createElement(RP, {
    label: "Client feedback",
    title: null,
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, r.comments.map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      padding: '12px 0',
      borderTop: i ? `1px solid ${RFK.BORDER}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      marginBottom: 5
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.MONO,
      fontSize: 10,
      color: RFK.AMBER,
      letterSpacing: '0.08em'
    }
  }, c.at), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.SANS,
      fontSize: 12,
      color: RFK.INK
    }
  }, c.author), /*#__PURE__*/React.createElement("span", {
    style: {
      marginLeft: 'auto',
      fontFamily: RFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: c.state === 'addressed' ? '#1F8A5B' : RFK.INK_FAINT
    }
  }, c.state)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: RFK.SANS,
      fontSize: 13,
      color: RFK.INK_MUTED,
      lineHeight: 1.45
    }
  }, c.text))))))));
}

/* ══════════════════════════════════════════════════════════
   TIMELINE
   ══════════════════════════════════════════════════════════ */
function TimelineTab({
  ctx
}) {
  const t = RC.timeline;
  const delivery = rShift(ctx.daysDelta);
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(RHead, {
    kicker: "Timeline",
    lede: "Seven stages, one path. The shoot is wrapped; editorial is in your hands."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(4, 1fr)',
      gap: 16,
      marginBottom: 28
    }
  }, /*#__PURE__*/React.createElement(RStat, {
    label: "Current phase",
    value: t.currentPhase,
    sub: "Cut with the client"
  }), /*#__PURE__*/React.createElement(RStat, {
    label: "Days remaining",
    value: t.daysRemaining + (ctx.daysDelta ? '+' + ctx.daysDelta : ''),
    sub: "To final delivery"
  }), /*#__PURE__*/React.createElement(RStat, {
    label: "Expected delivery",
    value: delivery,
    sub: ctx.daysDelta ? 'Shifted by committed changes' : 'On original date',
    tone: ctx.daysDelta ? RFK.AMBER : RFK.INK
  }), /*#__PURE__*/React.createElement(RStat, {
    label: "Schedule confidence",
    value: t.confidence,
    sub: "Holds if review lands by Jun 9"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1.4fr 1fr',
      gap: 16,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement(RP, {
    label: "Project stages",
    title: "The path"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      paddingLeft: 8
    }
  }, t.stages.map((st, i) => {
    const color = st.state === 'current' ? RFK.AMBER : st.state === 'done' ? RFK.INK : RFK.INK_FAINT;
    return /*#__PURE__*/React.createElement("div", {
      key: st.id,
      style: {
        position: 'relative',
        display: 'flex',
        gap: 18,
        paddingBottom: i === t.stages.length - 1 ? 0 : 22
      }
    }, i < t.stages.length - 1 && /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'absolute',
        left: 6,
        top: 16,
        bottom: 0,
        width: 1,
        background: RFK.BORDER
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'relative',
        zIndex: 1,
        width: 13,
        height: 13,
        marginTop: 3,
        borderRadius: 999,
        flexShrink: 0,
        background: st.state === 'upcoming' ? RFK.PAPER : color,
        border: st.state === 'upcoming' ? `1.5px solid ${RFK.BORDER_STRONG}` : 'none',
        boxShadow: st.state === 'current' ? '0 0 0 4px ' + RFK.AMBER_50 : 'none'
      }
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'baseline',
        justifyContent: 'space-between',
        flex: 1,
        gap: 12
      }
    }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: RFK.SANS,
        fontSize: 15,
        color: st.state === 'upcoming' ? RFK.INK_MUTED : RFK.INK
      }
    }, st.name), /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: RFK.MONO,
        fontSize: 10.5,
        letterSpacing: '0.06em',
        color: RFK.INK_FAINT,
        marginTop: 3
      }
    }, st.range)), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: RFK.MONO,
        fontSize: 9.5,
        letterSpacing: '0.14em',
        textTransform: 'uppercase',
        color: st.state === 'current' ? RFK.AMBER : RFK.INK_FAINT
      }
    }, st.state === 'done' ? 'Complete' : st.state === 'current' ? 'In progress' : 'Upcoming')));
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(RP, {
    label: "Milestones",
    title: null,
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, t.milestones.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: m.id,
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '11px 0',
      borderTop: i ? `1px solid ${RFK.BORDER}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement(RDot, {
    state: m.state
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.SANS,
      fontSize: 14,
      color: m.state === 'upcoming' ? RFK.INK_MUTED : RFK.INK
    }
  }, m.name)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.MONO,
      fontSize: 11,
      color: m.state === 'current' ? RFK.AMBER : RFK.INK_FAINT
    }
  }, m.date))))), /*#__PURE__*/React.createElement(RP, {
    label: "Delivery forecast",
    title: null,
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 10,
      marginBottom: 14
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.SANS,
      fontWeight: 300,
      fontSize: 30,
      color: ctx.daysDelta ? RFK.AMBER : RFK.INK
    }
  }, delivery), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.SANS,
      fontSize: 13,
      color: RFK.INK_MUTED
    }
  }, "expected")), t.delays.map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      gap: 10,
      padding: '12px 0',
      borderTop: `1px solid ${RFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 6,
      height: 6,
      borderRadius: 999,
      background: RFK.AMBER,
      marginTop: 6,
      flexShrink: 0
    }
  }), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: RFK.SANS,
      fontSize: 13.5,
      color: RFK.INK
    }
  }, d.label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: RFK.SANS,
      fontSize: 12.5,
      color: RFK.INK_MUTED,
      marginTop: 2,
      lineHeight: 1.4
    }
  }, d.detail))))))));
}
Object.assign(window, {
  ReviewsTab,
  TimelineTab
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/client-tabs1.jsx", error: String((e && e.message) || e) }); }

// flow/client-tabs2.jsx
try { (() => {
/* global React */
// TENET Client Portal — Budget, Scope, Risks.
const {
  FK: BFK,
  CLIENT: BC
} = window;
const {
  CPanel: BP,
  CPStat: BStat,
  CPBar: BBar,
  CPHead: BHead,
  cpKickerFaint: BKickF,
  RISK_COLOR: BRISK
} = window;

/* ══════════════════════════════════════════════════════════
   BUDGET
   ══════════════════════════════════════════════════════════ */
function BudgetTab({
  ctx
}) {
  const b = BC.budget;
  const forecast = b.forecast + ctx.forecastDelta;
  const variance = forecast - b.approved;
  const over = variance > 0;
  const drawn = Math.max(0, forecast - BC.T.directBudget);
  const contingency = b.reserve - drawn;
  const allocTotal = b.allocation.reduce((a, c) => a + c.amount, 0);
  const maxAlloc = Math.max.apply(null, b.allocation.map(c => c.amount));
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(BHead, {
    kicker: "Budget",
    lede: "Budget impact without the accounting. Every number traces to a cause."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(5, 1fr)',
      gap: 14,
      marginBottom: 28
    }
  }, /*#__PURE__*/React.createElement(BStat, {
    label: "Approved budget",
    value: BC.money(b.approved)
  }), /*#__PURE__*/React.createElement(BStat, {
    label: "Actual spend",
    value: BC.money(b.actual),
    sub: "Committed to date"
  }), /*#__PURE__*/React.createElement(BStat, {
    label: "Forecast final",
    value: BC.money(forecast),
    tone: over ? '#B23A2E' : BFK.INK,
    sub: ctx.forecastDelta ? BC.signed(ctx.forecastDelta) + ' from changes' : 'On plan'
  }), /*#__PURE__*/React.createElement(BStat, {
    label: "Variance",
    value: (over ? '+' : '\u2212') + BC.money(Math.abs(variance)),
    tone: over ? '#B23A2E' : '#1F8A5B',
    sub: over ? 'Over approved' : 'Under approved'
  }), /*#__PURE__*/React.createElement(BStat, {
    label: "Contingency left",
    value: BC.money(Math.max(0, contingency)),
    tone: contingency < 0 ? '#B23A2E' : BFK.INK,
    sub: 'of ' + BC.money(b.reserve) + ' reserve'
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1.5fr 1fr',
      gap: 16,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement(BP, {
    label: "Current cost allocation",
    title: "By forecast"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 15
    }
  }, b.allocation.map(c => /*#__PURE__*/React.createElement("div", {
    key: c.id
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline',
      marginBottom: 7
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.SANS,
      fontSize: 14,
      color: BFK.INK
    }
  }, c.name), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 10,
      color: BFK.INK_FAINT
    }
  }, Math.round(c.amount / allocTotal * 100), "%"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 12,
      color: BFK.INK,
      fontVariantNumeric: 'tabular-nums'
    }
  }, BC.money(c.amount)))), /*#__PURE__*/React.createElement(BBar, {
    pct: c.amount / maxAlloc * 100,
    color: BFK.INK
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: BFK.SANS,
      fontSize: 12,
      color: BFK.INK_MUTED,
      marginTop: 6,
      lineHeight: 1.4
    }
  }, c.why))))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(BP, {
    label: "What changed",
    title: null,
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, b.changed.map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      padding: '13px 0',
      borderTop: i ? `1px solid ${BFK.BORDER}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.SANS,
      fontSize: 14,
      color: BFK.INK
    }
  }, c.label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 12,
      color: BFK.AMBER_800,
      fontVariantNumeric: 'tabular-nums',
      whiteSpace: 'nowrap'
    }
  }, "+", BC.money(c.amt))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline',
      marginTop: 3
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.SANS,
      fontSize: 12.5,
      color: BFK.INK_MUTED,
      lineHeight: 1.4,
      maxWidth: 220
    }
  }, c.detail), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 10,
      color: BFK.INK_FAINT
    }
  }, c.when)))))), /*#__PURE__*/React.createElement(BP, {
    label: "Forecast",
    title: null,
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14
    }
  }, b.forecastBands.map(f => {
    const isExp = f.id === 'exp';
    return /*#__PURE__*/React.createElement("div", {
      key: f.id
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'baseline',
        marginBottom: 6
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: BFK.MONO,
        fontSize: 10,
        letterSpacing: '0.14em',
        textTransform: 'uppercase',
        color: isExp ? BFK.AMBER : BFK.INK_MUTED
      }
    }, f.label), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: BFK.SANS,
        fontSize: 15,
        color: BFK.INK,
        fontVariantNumeric: 'tabular-nums'
      }
    }, BC.money(f.id === 'exp' ? forecast : f.amount))), /*#__PURE__*/React.createElement(BBar, {
      pct: (f.id === 'exp' ? forecast : f.amount) / b.approved * 100,
      color: isExp ? BFK.AMBER : BFK.INK_FAINT
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: BFK.SANS,
        fontSize: 12,
        color: BFK.INK_MUTED,
        marginTop: 5
      }
    }, f.note));
  }))))));
}

/* ══════════════════════════════════════════════════════════
   SCOPE
   ══════════════════════════════════════════════════════════ */
function ScopeTab({
  ctx
}) {
  const s = BC.scope;
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(BHead, {
    kicker: "Scope",
    lede: "The line between what was agreed and what\u2019s been added \u2014 drawn clearly."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 16,
      alignItems: 'start',
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement(BP, {
    label: "Original scope",
    title: "As agreed"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, s.original.map((o, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline',
      padding: '12px 0',
      borderTop: i ? `1px solid ${BFK.BORDER}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 10,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: BFK.INK_FAINT
    }
  }, o.label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.SANS,
      fontSize: 14,
      color: BFK.INK,
      textAlign: 'right'
    }
  }, o.value))))), /*#__PURE__*/React.createElement(BP, {
    label: "Current scope",
    title: "Today"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, BKickF, {
      marginBottom: 10
    })
  }, "Approved"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 8
    }
  }, s.current.approved.map((x, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      fontFamily: BFK.SANS,
      fontSize: 13,
      color: BFK.INK,
      padding: '6px 12px',
      borderRadius: 3,
      border: `1px solid ${BFK.BORDER}`,
      background: BFK.PAPER_SOFT
    }
  }, x)))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: s.current.removed.length ? 16 : 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, BKickF, {
      marginBottom: 10,
      color: BFK.AMBER
    })
  }, "Added"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 8
    }
  }, s.current.added.map((x, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      fontFamily: BFK.SANS,
      fontSize: 13,
      color: BFK.AMBER_800,
      padding: '6px 12px',
      borderRadius: 3,
      border: `1px solid ${BFK.AMBER}`,
      background: BFK.AMBER_50
    }
  }, x)))), s.current.removed.length > 0 && /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, BKickF, {
      marginBottom: 10
    })
  }, "Removed"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.SANS,
      fontSize: 13,
      color: BFK.INK_FAINT
    }
  }, s.current.removed.join(', '))), s.current.removed.length === 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: BFK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: BFK.INK_FAINT,
      marginTop: 4
    }
  }, "Nothing removed from the original agreement."))), /*#__PURE__*/React.createElement(BP, {
    label: "Change history",
    title: "Every change, dated"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr auto auto auto',
      gap: '0 28px',
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, BKickF, {
      paddingBottom: 12,
      borderBottom: `1px solid ${BFK.BORDER}`
    })
  }, "Change"), /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, BKickF, {
      paddingBottom: 12,
      borderBottom: `1px solid ${BFK.BORDER}`,
      textAlign: 'right'
    })
  }, "Approved"), /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, BKickF, {
      paddingBottom: 12,
      borderBottom: `1px solid ${BFK.BORDER}`,
      textAlign: 'right'
    })
  }, "Cost"), /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, BKickF, {
      paddingBottom: 12,
      borderBottom: `1px solid ${BFK.BORDER}`,
      textAlign: 'right'
    })
  }, "Schedule"), s.changes.map((c, i) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: i
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: BFK.SANS,
      fontSize: 14,
      color: BFK.INK,
      padding: '13px 0',
      borderTop: `1px solid ${BFK.BORDER}`
    }
  }, c.label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 11,
      color: BFK.INK_FAINT,
      textAlign: 'right',
      padding: '13px 0',
      borderTop: `1px solid ${BFK.BORDER}`
    }
  }, c.date), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 12,
      color: c.cost ? BFK.AMBER_800 : '#1F8A5B',
      textAlign: 'right',
      padding: '13px 0',
      borderTop: `1px solid ${BFK.BORDER}`,
      fontVariantNumeric: 'tabular-nums'
    }
  }, c.cost ? '+' + BC.money(c.cost) : 'No cost'), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 12,
      color: BFK.INK,
      textAlign: 'right',
      padding: '13px 0',
      borderTop: `1px solid ${BFK.BORDER}`
    }
  }, c.days ? '+' + c.days + 'd' : '\u2014'))))));
}

/* ══════════════════════════════════════════════════════════
   RISKS
   ══════════════════════════════════════════════════════════ */
function RisksTab({
  ctx
}) {
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(BHead, {
    kicker: "Risks",
    lede: "Only what\u2019s meaningful \u2014 four risks worth your attention, each with a plan."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 16
    }
  }, BC.risks.map(r => /*#__PURE__*/React.createElement("div", {
    key: r.id,
    style: {
      border: `1px solid ${BFK.BORDER}`,
      borderRadius: 6,
      background: BFK.PAPER,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 22px 16px',
      borderBottom: `1px solid ${BFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: BFK.AMBER,
      whiteSpace: 'nowrap'
    }
  }, r.type, " risk"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 10,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: BFK.INK_FAINT
    }
  }, r.owner)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: BFK.SANS,
      fontWeight: 300,
      fontSize: 19,
      lineHeight: 1.2,
      color: BFK.INK
    }
  }, r.title)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 0
    }
  }, /*#__PURE__*/React.createElement(RiskMeter, {
    label: "Probability",
    value: r.prob
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 1,
      background: BFK.BORDER
    }
  }), /*#__PURE__*/React.createElement(RiskMeter, {
    label: "Impact",
    value: r.impact
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 22px',
      borderTop: `1px solid ${BFK.BORDER}`,
      background: BFK.PAPER_SOFT
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, BKickF, {
      marginBottom: 8
    })
  }, "Mitigation plan"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: BFK.SANS,
      fontSize: 13.5,
      color: BFK.INK,
      lineHeight: 1.5
    }
  }, r.plan))))));
}
function RiskMeter({
  label,
  value
}) {
  const idx = value === 'Low' ? 0 : value === 'Medium' ? 1 : 2;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      padding: '14px 22px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, BKickF, {
      marginBottom: 10
    })
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.SANS,
      fontSize: 16,
      color: BRISK[value]
    }
  }, value), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      gap: 3,
      marginLeft: 'auto'
    }
  }, [0, 1, 2].map(i => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      width: 14,
      height: 6,
      borderRadius: 1,
      background: i <= idx ? BRISK[value] : BFK.BORDER
    }
  })))));
}
Object.assign(window, {
  BudgetTab,
  ScopeTab,
  RisksTab
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/client-tabs2.jsx", error: String((e && e.message) || e) }); }

// flow/creative-data.js
try { (() => {
/* ============================================================
   TENET — Creative Portal data model (v3: the Decision core).
   ------------------------------------------------------------
   Everything attaches to one Decision. Assets are evidence,
   Approvals are authority, CostDrivers are consequence — the
   bridge into the v2 money graph (FRAMEWORK / PROD).

   Governing principle, enforced structurally:
   • No field stores a system-generated creative judgment / score / rank.
   • AI output is only ever an Asset with origin HUMAN_DIRECTED_GENERATION.
   • The role enum holds people only — the AI can never hold a verb.
   • Analysis describes present state; it never advises.
   ============================================================ */
window.CREATIVE = function () {
  var F = window.FLOW,
    FW = window.FRAMEWORK;
  function money(n) {
    return '$' + Math.round(Math.abs(n)).toLocaleString('en-US');
  }
  function signed(n) {
    return (n < 0 ? '\u2212' : '+') + money(n);
  }
  var project = {
    title: F.TITLE,
    logline: F.LOGLINE,
    format: ':30 Brand Film',
    code: '#17433',
    director: 'D. Parker',
    cd: 'A. Wells',
    client: 'H&W',
    today: 'Jun 5, 2026'
  };

  // ── The five master questions (the Master Model spine) ──────
  var MASTER = {
    WHY: {
      label: 'Why',
      gloss: 'Purpose'
    },
    WHAT: {
      label: 'What',
      gloss: 'Story'
    },
    FEEL: {
      label: 'Feel',
      gloss: 'Emotion'
    },
    HOW: {
      label: 'How',
      gloss: 'Craft'
    },
    EXPERIENCE: {
      label: 'Experience',
      gloss: 'Delivery'
    }
  };

  // ── The cost ripple — one Decision, two lenses ──────────────
  // The apartment night exterior is the live cross-vertical link:
  // the FEEL decision carries a CostDriver; flip it and the
  // producer's forecast/variance move. Computed against FRAMEWORK,
  // never stored as truth.
  var S0 = F.DEFAULT_STATE;
  var stateNight = {
    mode: S0.mode,
    tod: {
      s4: 'NIGHT'
    }
  };
  var stateDay = {
    mode: S0.mode,
    tod: {
      s4: 'DAY'
    }
  };
  var tNight = FW ? FW.finTotals(stateNight) : {
    forecast: 472000,
    variance: -28000,
    approved: 500000
  };
  var tDay = FW ? FW.finTotals(stateDay) : {
    forecast: 454000,
    variance: -46000,
    approved: 500000
  };
  var ripple = {
    approved: tNight.approved,
    night: {
      tod: 'NIGHT',
      forecast: tNight.forecast,
      variance: tNight.variance
    },
    day: {
      tod: 'DAY',
      forecast: tDay.forecast,
      variance: tDay.variance
    },
    otDelta: tNight.forecast - tDay.forecast // the cost the FEEL choice carries
  };

  // ── Decisions (the keystone) ────────────────────────────────
  // facets = the same decision re-summarized in each role's language.
  var decisions = [{
    id: 'd1',
    master: 'WHY',
    title: 'Three-part structure',
    status: 'DECIDED',
    desc: 'The Old Way / The Shift / The Promise. The film argues by contrast.',
    by: 'Director',
    facets: {
      Director: ['Structure', 'Rhythm'],
      Producer: ['Locked scope']
    },
    assets: ['a-scr4', 'a-scr3'],
    approvals: [{
      role: 'DIRECTOR',
      verb: 'DECIDE',
      state: 'GRANTED',
      who: 'D. Parker',
      when: 'Mar 6'
    }, {
      role: 'CREATIVE_DIRECTOR',
      verb: 'APPROVE',
      state: 'GRANTED',
      who: 'A. Wells',
      when: 'Mar 7'
    }],
    cost: null
  }, {
    id: 'd2',
    master: 'WHAT',
    title: 'The ending beat',
    status: 'OPEN',
    desc: 'How the promise lands. The final image is still soft — the room has not converged.',
    by: 'Director',
    facets: {
      Director: ['Emotion', 'World'],
      Producer: ['Risk', 'Reshoot exposure']
    },
    assets: ['a-bd-end1', 'a-bd-end2', 'a-anim1'],
    approvals: [{
      role: 'DIRECTOR',
      verb: 'PROPOSE',
      state: 'PENDING',
      who: 'D. Parker',
      when: '\u2014'
    }],
    cost: null,
    flag: 'Client risk: ending beat still soft'
  }, {
    id: 'd3',
    master: 'FEEL',
    title: 'Apartment \u2014 night exterior vs. day',
    status: 'PROPOSED',
    desc: 'The gift scene. Night sells the intimacy; day clears the overtime premium.',
    by: 'Director',
    facets: {
      Director: ['World', 'Frame', 'Mood'],
      Producer: ['Money', 'Time']
    },
    assets: ['a-bd-apt1', 'a-bd-apt2', 'a-ref-apt'],
    approvals: [{
      role: 'DIRECTOR',
      verb: 'PROPOSE',
      state: 'GRANTED',
      who: 'D. Parker',
      when: 'May 28'
    }, {
      role: 'PRODUCER',
      verb: 'DECIDE',
      state: 'PENDING',
      who: 'M. Hall',
      when: '\u2014'
    }],
    cost: {
      driver: 'Night exterior \u2014 crew OT',
      amt: ripple.otDelta,
      removable: true,
      ripple: true
    }
  }, {
    id: 'd4',
    master: 'FEEL',
    title: 'Hold the beach for golden hour',
    status: 'PROPOSED',
    desc: 'The closing image. Golden hour is a half-day of OT but it sells the ending.',
    by: 'Director',
    facets: {
      Director: ['Emotion', 'Frame'],
      Producer: ['Money']
    },
    assets: ['a-bd-beach1', 'a-ref-beach'],
    approvals: [{
      role: 'DIRECTOR',
      verb: 'PROPOSE',
      state: 'GRANTED',
      who: 'D. Parker',
      when: 'May 30'
    }, {
      role: 'PRODUCER',
      verb: 'DECIDE',
      state: 'PENDING',
      who: 'M. Hall',
      when: '\u2014'
    }],
    cost: {
      driver: 'Golden-hour half-day OT',
      amt: 18000,
      removable: true
    }
  }, {
    id: 'd5',
    master: 'HOW',
    title: 'Opening technocrane move',
    status: 'DECIDED',
    desc: 'Booms up the face of the building into Times Square. Sets the scale.',
    by: 'Director',
    facets: {
      Director: ['Frame', 'Scale'],
      Producer: ['Money', 'Risk']
    },
    assets: ['a-bd-open1'],
    approvals: [{
      role: 'DIRECTOR',
      verb: 'DECIDE',
      state: 'GRANTED',
      who: 'D. Parker',
      when: 'Apr 2'
    }, {
      role: 'CINEMATOGRAPHER',
      verb: 'APPROVE',
      state: 'GRANTED',
      who: 'L. Cho',
      when: 'Apr 3'
    }],
    cost: {
      driver: 'Technocrane package',
      amt: 16000,
      removable: true
    }
  }, {
    id: 'd6',
    master: 'FEEL',
    title: 'Cast the lead \u2014 Mara',
    status: 'DECIDED',
    desc: 'The face the whole film rests on. Carries the watch, carries the day.',
    by: 'Director',
    facets: {
      Director: ['Character', 'Emotion'],
      Producer: ['Money']
    },
    assets: ['a-ref-cast'],
    approvals: [{
      role: 'DIRECTOR',
      verb: 'DECIDE',
      state: 'GRANTED',
      who: 'D. Parker',
      when: 'Mar 24'
    }, {
      role: 'CREATIVE_DIRECTOR',
      verb: 'APPROVE',
      state: 'GRANTED',
      who: 'A. Wells',
      when: 'Mar 25'
    }],
    cost: {
      driver: 'O/C principal buyout \u2014 2yr',
      amt: 38000,
      removable: false
    }
  }, {
    id: 'd7',
    master: 'EXPERIENCE',
    title: 'Approve final creative',
    status: 'PROPOSED',
    desc: 'The gate before the bid locks. The client sees the idea before a frame is spent.',
    by: 'Creative Director',
    facets: {
      Director: ['\u2014'],
      Producer: ['Lock the bid']
    },
    assets: ['a-scr4', 'a-anim1'],
    approvals: [{
      role: 'CREATIVE_DIRECTOR',
      verb: 'PROPOSE',
      state: 'GRANTED',
      who: 'A. Wells',
      when: 'Jun 4'
    }, {
      role: 'CLIENT',
      verb: 'APPROVE',
      state: 'PENDING',
      who: 'S. Metnick',
      when: '\u2014'
    }],
    cost: null
  }];

  // ── Assets (the evidence) ───────────────────────────────────
  // origin: HUMAN_UPLOAD | HUMAN_DIRECTED_GENERATION (no autonomous origin).
  var assets = [{
    id: 'a-scr4',
    d: 'd1',
    type: 'SCRIPT',
    origin: 'HUMAN_UPLOAD',
    v: 4,
    parent: 'a-scr3',
    label: 'A_Brief_History_v3.fdx',
    by: 'Director',
    state: 'GRANTED',
    scene: '\u2014'
  }, {
    id: 'a-scr3',
    d: 'd1',
    type: 'SCRIPT',
    origin: 'HUMAN_UPLOAD',
    v: 3,
    parent: 'a-scr2',
    label: 'A_Brief_History_v2.fdx',
    by: 'Director',
    state: 'GRANTED',
    scene: '\u2014'
  }, {
    id: 'a-scr2',
    d: 'd1',
    type: 'SCRIPT',
    origin: 'HUMAN_UPLOAD',
    v: 2,
    parent: 'a-scr1',
    label: 'A_Brief_History_v1b.fdx',
    by: 'Writer',
    state: 'GRANTED',
    scene: '\u2014'
  }, {
    id: 'a-scr1',
    d: 'd1',
    type: 'SCRIPT',
    origin: 'HUMAN_UPLOAD',
    v: 1,
    parent: null,
    label: 'A_Brief_History_rough.fdx',
    by: 'Writer',
    state: 'GRANTED',
    scene: '\u2014'
  }, {
    id: 'a-bd-open1',
    d: 'd5',
    type: 'BOARD',
    origin: 'HUMAN_DIRECTED_GENERATION',
    v: 2,
    parent: null,
    label: 'Opening crane \u2014 Times Sq',
    by: 'Director',
    state: 'GRANTED',
    scene: 'Rooftop',
    prompt: 'boom up 80th-floor glass into Times Square, dawn'
  }, {
    id: 'a-bd-apt1',
    d: 'd3',
    type: 'FRAME',
    origin: 'HUMAN_DIRECTED_GENERATION',
    v: 3,
    parent: 'a-bd-apt0',
    label: 'Apartment \u2014 night, the gift',
    by: 'Director',
    state: 'PENDING',
    scene: 'Apartment',
    prompt: 'warm practical interior, night, two figures, intimate'
  }, {
    id: 'a-bd-apt2',
    d: 'd3',
    type: 'FRAME',
    origin: 'HUMAN_DIRECTED_GENERATION',
    v: 1,
    parent: null,
    label: 'Apartment \u2014 day alt',
    by: 'Director',
    state: 'PENDING',
    scene: 'Apartment',
    prompt: 'soft daylight interior, two figures, window light'
  }, {
    id: 'a-ref-apt',
    d: 'd3',
    type: 'REFERENCE',
    origin: 'HUMAN_UPLOAD',
    v: 1,
    parent: null,
    label: 'Lighting ref \u2014 In the Mood for Love',
    by: 'Cinematographer',
    state: 'GRANTED',
    scene: 'Apartment'
  }, {
    id: 'a-bd-beach1',
    d: 'd4',
    type: 'FRAME',
    origin: 'HUMAN_DIRECTED_GENERATION',
    v: 2,
    parent: null,
    label: 'Beach \u2014 golden hour close',
    by: 'Director',
    state: 'PENDING',
    scene: 'Beach',
    prompt: 'wide beach, low sun, single figure at the shore'
  }, {
    id: 'a-ref-beach',
    d: 'd4',
    type: 'MOODBOARD',
    origin: 'HUMAN_UPLOAD',
    v: 1,
    parent: null,
    label: 'Endings moodboard',
    by: 'Director',
    state: 'GRANTED',
    scene: 'Beach'
  }, {
    id: 'a-bd-end1',
    d: 'd2',
    type: 'FRAME',
    origin: 'HUMAN_DIRECTED_GENERATION',
    v: 4,
    parent: 'a-bd-end0',
    label: 'Final image \u2014 option A',
    by: 'Director',
    state: 'PENDING',
    scene: 'Beach',
    prompt: 'watch face resolves to logo, warm'
  }, {
    id: 'a-bd-end2',
    d: 'd2',
    type: 'FRAME',
    origin: 'HUMAN_DIRECTED_GENERATION',
    v: 2,
    parent: null,
    label: 'Final image \u2014 option B',
    by: 'Director',
    state: 'PENDING',
    scene: 'Studio',
    prompt: 'macro strap, light bloom, resolve'
  }, {
    id: 'a-anim1',
    d: 'd2',
    type: 'ANIMATIC',
    origin: 'HUMAN_DIRECTED_GENERATION',
    v: 1,
    parent: null,
    label: 'Ending animatic \u2014 cutdown',
    by: 'Editor',
    state: 'PENDING',
    scene: '\u2014',
    prompt: 'assemble option A over VO, 6s'
  }, {
    id: 'a-ref-cast',
    d: 'd6',
    type: 'REFERENCE',
    origin: 'HUMAN_UPLOAD',
    v: 1,
    parent: null,
    label: 'Casting selects \u2014 Mara',
    by: 'Director',
    state: 'GRANTED',
    scene: '\u2014'
  }];

  // ── Script breakdown (Assets of type SCRIPT, versioned) ─────
  var scenesCount = F.SCENES.length;
  var locsCount = F.LOCS.length;
  var charsCount = F.CHARS.length;
  var scriptVersions = assets.filter(function (a) {
    return a.type === 'SCRIPT';
  });
  var breakdown = {
    scenes: scenesCount,
    locations: locsCount,
    characters: charsCount,
    pages: 3,
    runtime: F.fmtClock(F.totalLength(S0)),
    scenes_list: F.SCENES.map(function (s) {
      return {
        n: s.n,
        loc: F.locName(s.locId),
        tod: s.tod,
        chars: s.chars.map(function (c) {
          return F.charName(c);
        }),
        dynamic: !!s.todDynamic
      };
    })
  };

  // ── Boards (FRAME / BOARD / MOODBOARD / ANIMATIC for FEEL) ───
  var boards = assets.filter(function (a) {
    return ['FRAME', 'BOARD', 'MOODBOARD', 'ANIMATIC'].indexOf(a.type) >= 0;
  });

  // ── Edit — sequence + state-describing analysis (never advice) ─
  var seq = F.allShots(S0).map(function (sh, i) {
    var scene = F.SCENES.find(function (s) {
      return s.id === sh.sceneId;
    });
    return {
      n: i + 1,
      loc: F.locName(sh.locId),
      desc: sh.desc,
      dur: sh.dur,
      tod: F.todOf(scene, S0)
    };
  });
  var beats = [{
    key: 'open',
    t: 0.22
  }, {
    key: 'setup',
    t: 0.42
  }, {
    key: 'turn',
    t: 0.24
  }, {
    key: 'build',
    t: 0.52
  }, {
    key: 'peak',
    t: 0.92
  }, {
    key: 'land',
    t: 0.50
  }];
  // Descriptions of present state only. No "needs", "should", "better".
  var analysis = [{
    kind: 'Runtime',
    text: F.fmtClock(F.totalLength(S0)) + ' assembled across ' + seq.length + ' shots.'
  }, {
    kind: 'Pacing',
    text: 'Tension peaks at the ending beat; the turn at part two is the shortest hold.'
  }, {
    kind: 'Coverage',
    text: 'Scene 04 (Apartment) has no coverage of the hero product.'
  }, {
    kind: 'Coverage',
    text: 'Studio macro carries the only logo resolve in the cut.'
  }, {
    kind: 'VO',
    text: 'Six voiceover lines present; longest gap sits over the downtown sequence.'
  }];

  // ── Approvals / Collaboration (authority across roles) ──────
  var approvals = [];
  decisions.forEach(function (d) {
    d.approvals.forEach(function (ap) {
      approvals.push({
        decision: d.title,
        decision_id: d.id,
        master: d.master,
        role: ap.role,
        verb: ap.verb,
        state: ap.state,
        who: ap.who,
        when: ap.when
      });
    });
  });
  var comments = [{
    who: 'M. Hall',
    role: 'PRODUCER',
    when: '2h',
    decision: 'Apartment \u2014 night exterior vs. day',
    text: 'Night is +$18K of crew OT. Day clears it. Your call on the room \u2014 I just hold the line.'
  }, {
    who: 'A. Wells',
    role: 'CREATIVE_DIRECTOR',
    when: '5h',
    decision: 'The ending beat',
    text: 'Option A lands the watch better. Want the client eyes before we lock anything.'
  }, {
    who: 'L. Cho',
    role: 'CINEMATOGRAPHER',
    when: '1d',
    decision: 'Opening technocrane move',
    text: 'Crane is booked. Dawn window is tight but the move is worth it.'
  }];

  // role display
  var ROLE_LABEL = {
    CLIENT: 'Client',
    CREATIVE_DIRECTOR: 'Creative Dir.',
    DIRECTOR: 'Director',
    PRODUCER: 'Producer',
    EDITOR: 'Editor',
    CINEMATOGRAPHER: 'DP',
    PRODUCTION_DESIGNER: 'Prod. Designer',
    VFX_SUPERVISOR: 'VFX Sup.',
    WRITER: 'Writer'
  };
  function decAssets(d) {
    return assets.filter(function (a) {
      return a.d === d.id;
    });
  }

  // ── Writers Room — script versions, scene-by-scene, timeline-linked ─
  // Sections are the production scenes (so selecting one highlights its
  // clip on the cut). Each version reads a section from `base`, unless it
  // overrides the wording, or omits the scene entirely (not yet written).
  var VO = {
    s1: 'Every morning, the same quiet promise.',
    s2: 'Someone made this, by hand.',
    s3: 'The world hurries. You don\u2019t have to.',
    s4: 'Some gifts are really just time.',
    s5: null,
    s6: 'A life, held in the light.'
  };
  var wSections = F.SCENES.map(function (s) {
    var loc = F.LOCS.find(function (l) {
      return l.id === s.locId;
    }) || {};
    var slug = (loc.kind || 'EXT') + '. ' + F.locName(s.locId).toUpperCase() + (s.tod && s.tod !== '\u2014' ? ' \u2014 ' + s.tod : '');
    return {
      sceneId: s.id,
      n: s.n,
      loc: F.locName(s.locId),
      slug: slug,
      dur: s.shots.reduce(function (a, sh) {
        return a + sh.dur;
      }, 0)
    };
  });
  var wBase = {};
  F.SCENES.forEach(function (s) {
    var lines = s.action.map(function (a) {
      return {
        k: 'action',
        text: a.text
      };
    });
    if (VO[s.id]) lines.push({
      k: 'vo',
      text: VO[s.id]
    });
    wBase[s.id] = lines;
  });
  // earlier-draft wording (v2) and the rough pass (v1)
  var wOverrides = {
    v2: {
      s1: [{
        k: 'action',
        text: 'A city still blue with sleep. MARA stands at the rail.'
      }, {
        k: 'action',
        text: 'A watch catches the light.'
      }, {
        k: 'vo',
        text: 'Each morning, the same promise.'
      }],
      s4: [{
        k: 'action',
        text: 'THEO fastens the watch on MARA\u2019s wrist.'
      }, {
        k: 'action',
        text: 'A held look.'
      }, {
        k: 'vo',
        text: 'A gift of time.'
      }],
      s6: [{
        k: 'action',
        text: 'MARA at the shoreline. The watch, the light.'
      }, {
        k: 'vo',
        text: 'A life in the light.'
      }]
    },
    v1: {
      s1: [{
        k: 'action',
        text: 'Rooftop. A woman, a watch, first light.'
      }],
      s3: [{
        k: 'action',
        text: 'She walks the city.'
      }],
      s4: [{
        k: 'action',
        text: 'He gives her the watch.'
      }]
    }
  };
  var writers = {
    versions: [{
      id: 'v4',
      tag: 'Current',
      file: 'A_Brief_History_v3.fdx',
      date: 'Jun 4',
      by: 'Director',
      scenes: ['s1', 's2', 's3', 's4', 's5', 's6']
    }, {
      id: 'v3',
      tag: 'v3',
      file: 'A_Brief_History_v2.fdx',
      date: 'May 28',
      by: 'Director',
      scenes: ['s1', 's2', 's3', 's4', 's6']
    }, {
      id: 'v2',
      tag: 'v2',
      file: 'A_Brief_History_v1b.fdx',
      date: 'May 21',
      by: 'Writer',
      scenes: ['s1', 's2', 's3', 's4', 's6']
    }, {
      id: 'v1',
      tag: 'v1',
      file: 'A_Brief_History_rough.fdx',
      date: 'May 14',
      by: 'Writer',
      scenes: ['s1', 's3', 's4']
    }],
    sections: wSections,
    // resolve a version's lines for a scene, or null if not in that version
    resolve: function (versionId, sceneId) {
      var v = writers.versions.find(function (x) {
        return x.id === versionId;
      });
      if (!v || v.scenes.indexOf(sceneId) < 0) return null;
      return wOverrides[versionId] && wOverrides[versionId][sceneId] || wBase[sceneId];
    }
  };
  return {
    project: project,
    MASTER: MASTER,
    decisions: decisions,
    assets: assets,
    breakdown: breakdown,
    scriptVersions: scriptVersions,
    boards: boards,
    seq: seq,
    beats: beats,
    analysis: analysis,
    writers: writers,
    approvals: approvals,
    comments: comments,
    ripple: ripple,
    ROLE_LABEL: ROLE_LABEL,
    decAssets: decAssets,
    money: money,
    signed: signed,
    NAV: [{
      id: 'bay',
      label: 'Edit Bay',
      phase: 'work'
    }, {
      id: 'writers',
      label: 'Writers Room',
      phase: 'work'
    }, {
      id: 'project',
      label: 'Project',
      phase: 'graph'
    }, {
      id: 'edit',
      label: 'Edit',
      phase: 'graph'
    }, {
      id: 'assets',
      label: 'Assets',
      phase: 'graph'
    }]
  };
}();
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/creative-data.js", error: String((e && e.message) || e) }); }

// flow/creative-shell.jsx
try { (() => {
/* global React */
// TENET Creative lane — ONE dark cockpit. The edit bay is the landing (tab 1);
// the Creative Portal views are projections over the same Decision/Asset graph.
// Nav: Edit Bay │ Project · Script · Boards · Edit · Assets · Collaboration.
// `theme` drives the shared DK palette across the whole lane.
const {
  FK: LFK,
  FGlyph: LGl,
  CREATIVE: LC
} = window;
const LUse = React.useState,
  LRef = React.useRef;

// Message · Producer — the director's line to the producer. Persistent across
// every Creative tab (the edit bay embeds its own copy of this conversation).
const CRLANE_DRAFT = 'Can we hold the beach for golden hour? It sells the ending.';
function CreativeMessagePanel({
  ck,
  chat,
  onSend
}) {
  const bub = mine => ({
    alignSelf: mine ? 'flex-end' : 'flex-start',
    maxWidth: '90%',
    background: mine ? ck.amber : ck.bg3,
    color: mine ? ck.bg0 : ck.txt,
    border: mine ? 'none' : `1px solid ${ck.line2}`,
    borderRadius: 8,
    padding: '8px 11px',
    fontFamily: LFK.SANS,
    fontSize: 12.5,
    lineHeight: 1.4,
    marginBottom: 8
  });
  const who = mine => ({
    fontFamily: LFK.MONO,
    fontSize: 8.5,
    letterSpacing: '0.12em',
    textTransform: 'uppercase',
    color: mine ? 'rgba(0,0,0,0.5)' : ck.txt3,
    marginBottom: 3
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      padding: '14px 16px',
      borderBottom: `1px solid ${ck.line}`
    }
  }, /*#__PURE__*/React.createElement(LGl, {
    name: "chat",
    size: 13,
    style: {
      color: ck.amber
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: LFK.MONO,
      fontSize: 10,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: ck.amber
    }
  }, "Message \xB7 Producer")), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      overflowY: 'auto',
      padding: '14px 16px',
      display: 'flex',
      flexDirection: 'column'
    }
  }, chat.messages.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: bub(m.from === 'director')
  }, /*#__PURE__*/React.createElement("div", {
    style: who(m.from === 'director')
  }, m.from === 'director' ? 'You · Director' : 'Producer'), m.text)), chat.typing && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: LFK.SERIF,
      fontStyle: 'italic',
      fontSize: 12.5,
      color: ck.txt3,
      marginBottom: 7
    }
  }, "Producer is checking the numbers\u2026"), chat.messages.length === 0 && !chat.sent && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: LFK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: ck.txt3,
      lineHeight: 1.5,
      marginBottom: 'auto'
    }
  }, "A direct line to the producer. Send a question and watch it land in the budget.")), !chat.sent && /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      borderTop: `1px solid ${ck.line}`,
      padding: '12px 16px',
      display: 'flex',
      flexDirection: 'column',
      gap: 9
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: LFK.SANS,
      fontSize: 12.5,
      color: ck.txt,
      lineHeight: 1.4,
      padding: '9px 11px',
      background: ck.bg2,
      border: `1px solid ${ck.line2}`,
      borderRadius: 8
    }
  }, CRLANE_DRAFT), /*#__PURE__*/React.createElement("button", {
    onClick: onSend,
    style: {
      alignSelf: 'flex-end',
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      cursor: 'pointer',
      background: ck.amber,
      color: ck.bg0,
      border: 'none',
      borderRadius: 2,
      padding: '8px 15px',
      fontFamily: LFK.MONO,
      fontSize: 11,
      letterSpacing: '0.12em',
      textTransform: 'uppercase'
    }
  }, /*#__PURE__*/React.createElement(LGl, {
    name: "send",
    size: 13
  }), " Send")));
}
function CreativeLane(props) {
  const {
    state,
    cat,
    subIdx,
    flashId,
    dirChat,
    theme,
    onCat,
    onSub,
    onMode,
    onSendDir,
    onToggleTheme,
    onReset,
    onTod
  } = props;
  const [tab, setTab] = LUse('bay');
  const scrollRef = LRef(null);
  const ck = theme === 'light' ? window.DK_LIGHT : window.DK_DARK;
  function goTab(t) {
    setTab(t);
    if (scrollRef.current) scrollRef.current.scrollTop = 0;
  }
  const tod = state.tod && state.tod.s4 || 'NIGHT';
  const ctx = {
    goTab,
    tod,
    setTod: onTod,
    theme
  };
  const VIEWS = {
    project: window.CProject,
    edit: window.CEdit,
    assets: window.CAssets
  };
  const View = VIEWS[tab] || function () {
    return null;
  };
  const EditBay = window.EditBay;
  const WritersRoom = window.WritersRoom;
  const fullBleed = tab === 'bay' || tab === 'writers';
  const priorities = LC.decisions.filter(d => d.status === 'OPEN' || d.status === 'PROPOSED').length;
  const curPhase = (LC.NAV.find(n => n.id === tab) || {}).phase;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      background: ck.bg0,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      height: 56,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '0 26px',
      borderBottom: `1px solid ${ck.line}`,
      background: ck.bg1,
      position: 'relative',
      zIndex: 20
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 13
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: LFK.SERIF,
      fontStyle: 'italic',
      fontSize: 21,
      color: ck.txt,
      letterSpacing: '0.01em'
    }
  }, "tenet"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: LFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.24em',
      textTransform: 'uppercase',
      color: ck.amber,
      whiteSpace: 'nowrap'
    }
  }, "Creative"), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 1,
      height: 16,
      background: ck.line2,
      display: 'inline-block',
      alignSelf: 'center'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: LFK.SERIF,
      fontStyle: 'italic',
      fontSize: 14,
      color: ck.txt,
      whiteSpace: 'nowrap'
    }
  }, LC.project.title), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: LFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, LC.project.format, " \xB7 ", LC.project.code)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 18
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: () => goTab('project'),
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      background: 'none',
      border: 0,
      cursor: 'pointer',
      padding: 0,
      color: ck.txt2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 6,
      height: 6,
      borderRadius: 999,
      background: ck.amber
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: LFK.MONO,
      fontSize: 11,
      letterSpacing: '0.08em'
    }
  }, priorities, " open")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: `1px solid ${ck.line2}`,
      borderRadius: 3,
      overflow: 'hidden'
    }
  }, ['dark', 'light'].map((t, i) => /*#__PURE__*/React.createElement("div", {
    key: t,
    onClick: () => onToggleTheme(t),
    style: {
      padding: '5px 11px',
      cursor: 'pointer',
      fontFamily: LFK.MONO,
      fontSize: 10,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      borderLeft: i ? `1px solid ${ck.line2}` : 'none',
      background: theme === t ? ck.amber : ck.bg2,
      color: theme === t ? ck.bg0 : ck.txt2
    }
  }, t))), /*#__PURE__*/React.createElement("button", {
    onClick: onReset,
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      background: 'transparent',
      border: `1px solid ${ck.line2}`,
      borderRadius: 3,
      padding: '6px 12px',
      cursor: 'pointer',
      color: ck.txt2,
      fontFamily: LFK.MONO,
      fontSize: 10,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      whiteSpace: 'nowrap'
    }
  }, /*#__PURE__*/React.createElement(LGl, {
    name: "arrow",
    size: 12,
    style: {
      transform: 'scaleX(-1)'
    }
  }), " Change lane"))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      height: 48,
      display: 'flex',
      alignItems: 'stretch',
      padding: '0 22px',
      borderBottom: `1px solid ${ck.line}`,
      background: ck.bg1,
      position: 'relative',
      zIndex: 10
    }
  }, LC.NAV.map((n, idx) => {
    const on = n.id === tab;
    const prev = LC.NAV[idx - 1];
    const newPhase = prev && prev.phase !== n.phase;
    return /*#__PURE__*/React.createElement(React.Fragment, {
      key: n.id
    }, newPhase && /*#__PURE__*/React.createElement("span", {
      style: {
        alignSelf: 'center',
        width: 1,
        height: 16,
        background: ck.line2,
        margin: '0 14px'
      }
    }), /*#__PURE__*/React.createElement("button", {
      onClick: () => goTab(n.id),
      style: {
        position: 'relative',
        display: 'flex',
        alignItems: 'center',
        background: 'none',
        border: 0,
        borderBottom: on ? `2px solid ${ck.amber}` : '2px solid transparent',
        cursor: 'pointer',
        padding: '0 14px',
        marginBottom: -1,
        fontFamily: LFK.MONO,
        fontSize: 11,
        letterSpacing: '0.13em',
        textTransform: 'uppercase',
        color: on ? ck.txt : ck.txt3
      }
    }, n.label));
  })), tab === 'bay' ? /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex'
    }
  }, EditBay ? /*#__PURE__*/React.createElement(EditBay, {
    state: state,
    cat: cat,
    subIdx: subIdx,
    flashId: flashId,
    theme: theme,
    onCat: onCat,
    onSub: onSub,
    onMode: onMode,
    messageRail: /*#__PURE__*/React.createElement(CreativeMessagePanel, {
      ck: ck,
      chat: dirChat,
      onSend: onSendDir
    })
  }) : null) : /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 340px',
      minWidth: 0,
      minHeight: 0,
      borderRight: `1px solid ${ck.line}`,
      background: ck.bg1,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement(CreativeMessagePanel, {
    ck: ck,
    chat: dirChat,
    onSend: onSendDir
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      minHeight: 0,
      display: 'flex',
      flexDirection: 'column'
    }
  }, tab === 'writers' ? WritersRoom ? /*#__PURE__*/React.createElement(WritersRoom, {
    ctx: ctx,
    ck: ck
  }) : null : /*#__PURE__*/React.createElement("div", {
    ref: scrollRef,
    style: {
      flex: 1,
      minHeight: 0,
      overflowY: 'auto',
      overflowX: 'hidden',
      background: ck.bg0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 1080,
      margin: '0 auto',
      padding: '36px 44px 64px'
    }
  }, /*#__PURE__*/React.createElement(View, {
    ctx: ctx,
    ck: ck
  }))))));
}
window.CreativeLane = CreativeLane;
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/creative-shell.jsx", error: String((e && e.message) || e) }); }

// flow/creative-views.jsx
try { (() => {
/* global React */
// TENET Creative Portal — the six views over the Decision/Asset graph.
// All dark; each view takes the active palette `ck` (theme-driven) and `ctx`
// (shared lane state: goTab, tod ripple). The system describes & translates —
// it never scores, ranks, or recommends.
const {
  FK: CFK,
  FGlyph: CGl,
  CREATIVE: CC
} = window;

/* ── shared dark atoms ─────────────────────────────────────── */
function ccStatus(ck, s) {
  return {
    OPEN: ck.txt2,
    PROPOSED: ck.amber,
    DECIDED: ck.read,
    APPROVED: ck.read,
    LOCKED: ck.txt
  }[s] || ck.txt2;
}
function ccMaster(ck, m) {
  return {
    WHY: ck.txt2,
    WHAT: ck.amber,
    FEEL: ck.pink,
    HOW: ck.read,
    EXPERIENCE: ck.clipBlue
  }[m] || ck.txt2;
}
function ccVerbState(ck, st) {
  return {
    PENDING: ck.amber,
    GRANTED: ck.read,
    REJECTED: ck.pink
  }[st] || ck.txt2;
}
const ccKick = (ck, color) => ({
  fontFamily: CFK.MONO,
  fontSize: 10,
  letterSpacing: '0.2em',
  textTransform: 'uppercase',
  color: color || ck.amber,
  whiteSpace: 'nowrap'
});
const ccKickF = ck => ({
  fontFamily: CFK.MONO,
  fontSize: 9.5,
  letterSpacing: '0.16em',
  textTransform: 'uppercase',
  color: ck.txt3
});
function CCHead({
  ck,
  kicker,
  lede
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 22
    }
  }, kicker && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 11,
      letterSpacing: '0.22em',
      textTransform: 'uppercase',
      color: ck.amber
    }
  }, kicker), lede && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 10,
      maxWidth: 660,
      fontFamily: CFK.SERIF,
      fontStyle: 'italic',
      fontWeight: 300,
      fontSize: 16.5,
      lineHeight: 1.5,
      color: ck.txt2
    }
  }, lede));
}
function CCPanel({
  ck,
  label,
  title,
  children,
  pad,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: Object.assign({
      background: ck.bg1,
      border: `1px solid ${ck.line}`,
      borderRadius: 6,
      overflow: 'hidden'
    }, style)
  }, (label || title) && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 20px 0'
    }
  }, label && /*#__PURE__*/React.createElement("div", {
    style: ccKick(ck)
  }, label), title && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SANS,
      fontWeight: 300,
      fontSize: 21,
      color: ck.txt,
      marginTop: 7
    }
  }, title)), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: pad != null ? pad : '18px 20px'
    }
  }, children));
}
function CCBar({
  ck,
  pct,
  color,
  h
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      height: h || 6,
      background: ck.bg3,
      borderRadius: 999,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      width: Math.max(0, Math.min(100, pct)) + '%',
      background: color || ck.amber
    }
  }));
}
function CCChip({
  ck,
  on,
  tone,
  children
}) {
  const c = tone || ck.amber;
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6,
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.08em',
      textTransform: 'uppercase',
      padding: '4px 9px',
      borderRadius: 2,
      whiteSpace: 'nowrap',
      border: `1px solid ${on ? c : ck.line2}`,
      background: on ? ck.amberDim : 'transparent',
      color: on ? c : ck.txt2
    }
  }, children);
}
// origin tag — the structural "AI = human-directed evidence" signal
function CCOrigin({
  ck,
  origin
}) {
  const gen = origin === 'HUMAN_DIRECTED_GENERATION';
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 5,
      fontFamily: CFK.MONO,
      fontSize: 8.5,
      letterSpacing: '0.08em',
      textTransform: 'uppercase',
      color: gen ? ck.amber : ck.txt3
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 4,
      height: 4,
      borderRadius: 999,
      background: gen ? ck.amber : ck.txt3
    }
  }), gen ? 'Human-directed' : 'Upload');
}
const ccMoney = CC.money,
  ccSigned = CC.signed;

/* ══════════════════════════════════════════════════════════
   PROJECT — the decision graph. Priorities first.
   ══════════════════════════════════════════════════════════ */
function CProject({
  ctx,
  ck
}) {
  const [sel, setSel] = React.useState('d3');
  const d = CC.decisions.find(x => x.id === sel) || CC.decisions[0];
  const priorities = CC.decisions.filter(x => x.status === 'OPEN' || x.status === 'PROPOSED');
  const settled = CC.decisions.filter(x => x.status === 'DECIDED' || x.status === 'APPROVED' || x.status === 'LOCKED');
  const row = x => {
    const on = x.id === sel;
    return /*#__PURE__*/React.createElement("button", {
      key: x.id,
      onClick: () => setSel(x.id),
      style: {
        width: '100%',
        display: 'flex',
        alignItems: 'center',
        gap: 12,
        textAlign: 'left',
        cursor: 'pointer',
        padding: '13px 16px',
        background: on ? ck.amberDim : 'transparent',
        border: 0,
        borderTop: `1px solid ${ck.line}`,
        boxShadow: on ? `inset 3px 0 0 ${ck.amber}` : 'none'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: CFK.MONO,
        fontSize: 8.5,
        letterSpacing: '0.1em',
        textTransform: 'uppercase',
        color: ccMaster(ck, x.master),
        flex: '0 0 64px'
      }
    }, CC.MASTER[x.master].label), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        minWidth: 0,
        fontFamily: CFK.SANS,
        fontSize: 14,
        color: on ? ck.txt : ck.txt,
        whiteSpace: 'nowrap',
        overflow: 'hidden',
        textOverflow: 'ellipsis'
      }
    }, x.title), x.cost && /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: CFK.MONO,
        fontSize: 10,
        color: ck.amber,
        whiteSpace: 'nowrap'
      }
    }, ccSigned(x.cost.amt)), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: CFK.MONO,
        fontSize: 8.5,
        letterSpacing: '0.1em',
        textTransform: 'uppercase',
        color: ccStatus(ck, x.status),
        flex: '0 0 64px',
        textAlign: 'right'
      }
    }, x.status));
  };
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(CCHead, {
    ck: ck,
    kicker: "Project",
    lede: "Everything the film is deciding right now \u2014 and who owns each call. TENET carries decisions between specialists and shows their consequence; it never casts a vote."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 420px',
      gap: 24,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 18
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${ck.line}`,
      borderRadius: 6,
      background: ck.bg1,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '13px 16px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: ccKick(ck)
  }, "Current priorities"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 10,
      color: ck.txt3
    }
  }, priorities.length, " awaiting a human")), priorities.map(row)), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${ck.line}`,
      borderRadius: 6,
      background: ck.bg1,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '13px 16px'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: ccKickF(ck)
  }, "Settled")), settled.map(row)), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${ck.line}`,
      borderRadius: 6,
      background: ck.bg1,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '13px 16px'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: ccKickF(ck)
  }, "Room notes")), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '2px 16px 16px',
      display: 'flex',
      flexDirection: 'column',
      gap: 15
    }
  }, CC.comments.map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      borderLeft: `2px solid ${ck.line2}`,
      paddingLeft: 13
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      gap: 8,
      marginBottom: 4
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 8,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 13,
      color: ck.txt
    }
  }, c.who), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 8.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, CC.ROLE_LABEL[c.role] || c.role)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      color: ck.txt3
    }
  }, c.when)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      letterSpacing: '0.06em',
      color: ck.amber,
      marginBottom: 4
    }
  }, c.decision), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 13,
      color: ck.txt2,
      lineHeight: 1.5
    }
  }, c.text)))))), /*#__PURE__*/React.createElement(CDecisionDetail, {
    ctx: ctx,
    ck: ck,
    d: d
  })));
}
function CDecisionDetail({
  ctx,
  ck,
  d
}) {
  const rip = d.cost && d.cost.ripple;
  const tod = ctx.tod; // 'NIGHT' | 'DAY'
  const seeing = tod === 'NIGHT' ? CC.ripple.night : CC.ripple.day;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${ck.line2}`,
      borderRadius: 6,
      background: ck.bg1,
      overflow: 'hidden',
      position: 'sticky',
      top: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 20px',
      borderBottom: `1px solid ${ck.line}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      marginBottom: 9
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: ccMaster(ck, d.master)
    }
  }, CC.MASTER[d.master].label, " \xB7 ", CC.MASTER[d.master].gloss), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: ccStatus(ck, d.status)
    }
  }, d.status)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SANS,
      fontWeight: 300,
      fontSize: 22,
      lineHeight: 1.15,
      color: ck.txt
    }
  }, d.title), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SERIF,
      fontStyle: 'italic',
      fontSize: 14,
      color: ck.txt2,
      marginTop: 7,
      lineHeight: 1.4
    }
  }, d.desc)), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 20px',
      borderBottom: `1px solid ${ck.line}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, ccKickF(ck), {
      marginBottom: 12
    })
  }, "Read in each language"), Object.keys(d.facets).map(role => /*#__PURE__*/React.createElement("div", {
    key: role,
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 12,
      marginBottom: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.08em',
      textTransform: 'uppercase',
      color: ck.txt3,
      flex: '0 0 70px'
    }
  }, role), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 6
    }
  }, d.facets[role].map((f, i) => /*#__PURE__*/React.createElement(CCChip, {
    key: i,
    ck: ck,
    tone: role === 'Producer' ? ck.amber : ck.read,
    on: true
  }, f)))))), d.cost && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 20px',
      borderBottom: `1px solid ${ck.line}`,
      background: ck.bg2
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: Object.assign({}, ccKickF(ck))
  }, "Consequence"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 8.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, "Producer lens")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      marginBottom: rip ? 14 : 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 14,
      color: ck.txt
    }
  }, d.cost.driver), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 14,
      color: ck.amber
    }
  }, ccSigned(d.cost.amt))), rip ? /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: `1px solid ${ck.line2}`,
      borderRadius: 3,
      overflow: 'hidden',
      marginBottom: 14
    }
  }, [['NIGHT', 'Night'], ['DAY', 'Day']].map(([v, label], i) => /*#__PURE__*/React.createElement("div", {
    key: v,
    onClick: () => ctx.setTod(v),
    style: {
      padding: '6px 18px',
      cursor: 'pointer',
      fontFamily: CFK.MONO,
      fontSize: 10,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      borderLeft: i ? `1px solid ${ck.line2}` : 'none',
      background: tod === v ? ck.amber : ck.bg3,
      color: tod === v ? ck.bg0 : ck.txt2
    }
  }, label))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 1,
      background: ck.line,
      border: `1px solid ${ck.line}`,
      borderRadius: 4,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement(RipCell, {
    ck: ck,
    label: "Forecast final",
    value: ccMoney(seeing.forecast)
  }), /*#__PURE__*/React.createElement(RipCell, {
    ck: ck,
    label: "Variance",
    value: seeing.variance === 0 ? '$0' : ccSigned(seeing.variance),
    tone: seeing.variance > 0 ? ck.amber : ck.read
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SERIF,
      fontStyle: 'italic',
      fontSize: 12.5,
      color: ck.txt3,
      marginTop: 10,
      lineHeight: 1.4
    }
  }, tod === 'NIGHT' ? 'Night holds the intimacy — and ' + ccMoney(CC.ripple.otDelta) + ' of crew OT. The producer sees it live.' : 'Flipping to day clears the overtime. The variance moves the moment you choose.')) : /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ck.txt3,
      marginTop: 4
    }
  }, d.cost.removable ? 'Removable in scenarios' : 'Committed')), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 20px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, ccKickF(ck), {
      marginBottom: 12
    })
  }, "Approvals"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, d.approvals.map((ap, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 10,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 13.5,
      color: ck.txt
    }
  }, CC.ROLE_LABEL[ap.role] || ap.role), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, ap.verb)), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 7
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ccVerbState(ck, ap.state)
    }
  }, ap.state), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 5,
      height: 5,
      borderRadius: 999,
      background: ccVerbState(ck, ap.state)
    }
  }))))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 14,
      paddingTop: 12,
      borderTop: `1px solid ${ck.line}`,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, d.assets.length, " asset", d.assets.length === 1 ? '' : 's', " of evidence"), /*#__PURE__*/React.createElement("button", {
    onClick: () => ctx.goTab('edit'),
    style: {
      background: 'none',
      border: 0,
      padding: 0,
      cursor: 'pointer',
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6,
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ck.amber
    }
  }, "See evidence ", /*#__PURE__*/React.createElement(CGl, {
    name: "arrow",
    size: 11
  })))));
}
function RipCell({
  ck,
  label,
  value,
  tone
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      background: ck.bg1,
      padding: '12px 14px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: ccKickF(ck)
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SANS,
      fontWeight: 300,
      fontSize: 22,
      marginTop: 6,
      color: tone || ck.txt,
      fontVariantNumeric: 'tabular-nums'
    }
  }, value));
}

/* ══════════════════════════════════════════════════════════
   SCRIPT — versioned, broken down.
   ══════════════════════════════════════════════════════════ */
function CScript({
  ctx,
  ck
}) {
  const b = CC.breakdown;
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(CCHead, {
    ck: ck,
    kicker: "Script",
    title: "Versioned, and broken down",
    lede: "Every draft keeps its lineage. The breakdown extracts scenes, locations, and characters into the graph \u2014 it stores and parses, it doesn't rewrite."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '380px 1fr',
      gap: 24,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement(CCPanel, {
    ck: ck,
    label: "Version lineage",
    title: null
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, CC.scriptVersions.map((v, i) => {
    const latest = i === 0;
    return /*#__PURE__*/React.createElement("div", {
      key: v.id,
      style: {
        display: 'flex',
        alignItems: 'flex-start',
        gap: 12,
        padding: '11px 0',
        borderTop: i ? `1px solid ${ck.line}` : 'none'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: CFK.MONO,
        fontSize: 11,
        color: latest ? ck.amber : ck.txt3,
        flex: '0 0 28px'
      }
    }, "v", v.v), /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        minWidth: 0
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: CFK.MONO,
        fontSize: 11.5,
        color: ck.txt,
        whiteSpace: 'nowrap',
        overflow: 'hidden',
        textOverflow: 'ellipsis'
      }
    }, v.label), /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: CFK.SANS,
        fontSize: 11.5,
        color: ck.txt3,
        marginTop: 2
      }
    }, v.by, v.parent ? ' · from v' + (v.v - 1) : ' · origin')), latest && /*#__PURE__*/React.createElement(CCChip, {
      ck: ck,
      on: true,
      tone: ck.read
    }, "Current"));
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(4, 1fr)',
      gap: 1,
      background: ck.line,
      border: `1px solid ${ck.line}`,
      borderRadius: 6,
      overflow: 'hidden'
    }
  }, [['Scenes', b.scenes], ['Locations', b.locations], ['Characters', b.characters], ['Runtime', b.runtime]].map(([k, v], i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      background: ck.bg1,
      padding: '16px 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: ccKickF(ck)
  }, k), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SANS,
      fontWeight: 300,
      fontSize: 26,
      marginTop: 6,
      color: ck.txt
    }
  }, v)))), /*#__PURE__*/React.createElement(CCPanel, {
    ck: ck,
    label: "Scene breakdown",
    title: null,
    pad: 0
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '36px 1fr 110px 1fr',
      gap: 12,
      padding: '12px 18px',
      borderBottom: `1px solid ${ck.line}`
    }
  }, ['#', 'Location', 'Time', 'Cast'].map((h, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: ccKickF(ck)
  }, h))), b.scenes_list.map((s, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'grid',
      gridTemplateColumns: '36px 1fr 110px 1fr',
      gap: 12,
      padding: '12px 18px',
      alignItems: 'center',
      borderBottom: i < b.scenes_list.length - 1 ? `1px solid ${ck.line}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 11,
      color: ck.txt3
    }
  }, String(s.n).padStart(2, '0')), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 13.5,
      color: ck.txt
    }
  }, s.loc), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 7,
      fontFamily: CFK.MONO,
      fontSize: 10,
      letterSpacing: '0.06em',
      color: s.dynamic ? ck.amber : ck.txt2
    }
  }, s.tod, s.dynamic && /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 8,
      letterSpacing: '0.08em',
      color: ck.amber,
      border: `1px solid ${ck.line2}`,
      borderRadius: 2,
      padding: '1px 4px'
    }
  }, "VAR")), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 12.5,
      color: ck.txt2
    }
  }, s.chars.length ? s.chars.join(', ') : '—')))))));
}

/* ══════════════════════════════════════════════════════════
   BOARDS — frames as evidence. Explore/Refine = play; Lock/Promote = human verbs.
   ══════════════════════════════════════════════════════════ */
function BoardTile({
  ck,
  a,
  locked,
  sel,
  onSelect
}) {
  const stripe = ck === window.DK_LIGHT ? 'repeating-linear-gradient(135deg, #E7E2D8 0 10px, #ded8cc 10px 20px)' : 'repeating-linear-gradient(135deg, #20262d 0 10px, #1a1f25 10px 20px)';
  return /*#__PURE__*/React.createElement("button", {
    onClick: onSelect,
    style: {
      textAlign: 'left',
      cursor: 'pointer',
      background: ck.bg1,
      border: `1px solid ${sel ? ck.amber : ck.line2}`,
      borderRadius: 5,
      overflow: 'hidden',
      padding: 0,
      boxShadow: sel ? `0 0 0 1px ${ck.amber}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: '100%',
      aspectRatio: '16 / 9',
      background: stripe,
      borderBottom: `1px solid ${ck.line}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 8,
      left: 9,
      fontFamily: CFK.MONO,
      fontSize: 8.5,
      letterSpacing: '0.12em',
      color: ck.txt3
    }
  }, a.type, " \xB7 v", a.v), locked && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 8,
      right: 9,
      fontFamily: CFK.MONO,
      fontSize: 8.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ck.read
    }
  }, "\u25C9 Locked"), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      bottom: 0,
      padding: '7px 9px',
      background: 'linear-gradient(transparent, rgba(0,0,0,0.35))'
    }
  }, /*#__PURE__*/React.createElement(CCOrigin, {
    ck: ck,
    origin: a.origin
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '10px 11px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 12.5,
      color: ck.txt,
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, a.label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      color: ck.txt3,
      marginTop: 3
    }
  }, a.scene)));
}
function CBoards({
  ctx,
  ck
}) {
  const [sel, setSel] = React.useState(CC.boards[0].id);
  const [locked, setLocked] = React.useState({});
  const a = CC.boards.find(x => x.id === sel) || CC.boards[0];
  const isLocked = !!locked[a.id];
  function toggleLock() {
    setLocked(p => Object.assign({}, p, {
      [a.id]: !p[a.id]
    }));
  }
  const actionBtn = (label, primary, onClick) => /*#__PURE__*/React.createElement("button", {
    onClick: onClick,
    style: {
      cursor: 'pointer',
      fontFamily: CFK.MONO,
      fontSize: 10.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      padding: '9px 14px',
      borderRadius: 3,
      border: primary ? 'none' : `1px solid ${ck.line2}`,
      background: primary ? ck.amber : 'transparent',
      color: primary ? ck.bg0 : ck.txt2
    }
  }, label);
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(CCHead, {
    ck: ck,
    kicker: "Boards",
    lede: "Frames as evidence \u2014 explore and refine freely; every one is something a human chose to generate. Lock and Promote are human calls."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 380px',
      gap: 24,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(3, 1fr)',
      gap: 14
    }
  }, CC.boards.map(bd => /*#__PURE__*/React.createElement(BoardTile, {
    key: bd.id,
    ck: ck,
    a: bd,
    locked: !!locked[bd.id],
    sel: bd.id === sel,
    onSelect: () => setSel(bd.id)
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${ck.line2}`,
      borderRadius: 6,
      background: ck.bg1,
      overflow: 'hidden',
      position: 'sticky',
      top: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 20px',
      borderBottom: `1px solid ${ck.line}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      marginBottom: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: ccKick(ck)
  }, a.type, " \xB7 v", a.v), /*#__PURE__*/React.createElement(CCOrigin, {
    ck: ck,
    origin: a.origin
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SANS,
      fontWeight: 300,
      fontSize: 19,
      color: ck.txt
    }
  }, a.label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 10,
      color: ck.txt3,
      marginTop: 4
    }
  }, a.scene, " \xB7 by ", a.by)), a.prompt && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '14px 20px',
      borderBottom: `1px solid ${ck.line}`,
      background: ck.bg2
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, ccKickF(ck), {
      marginBottom: 7
    })
  }, "Prompt history \xB7 lineage, not authorship"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SCRIPT || CFK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: ck.txt2,
      lineHeight: 1.45
    }
  }, "\u201C", a.prompt, "\u201D")), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 20px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 8,
      marginBottom: 12
    }
  }, actionBtn('Explore', false, () => {}), actionBtn('Refine', false, () => {})), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 8
    }
  }, actionBtn(isLocked ? 'Unlock frame' : 'Lock frame', !isLocked, toggleLock), actionBtn('Promote to edit', false, () => ctx.goTab('edit'))), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SERIF,
      fontStyle: 'italic',
      fontSize: 12,
      color: ck.txt3,
      marginTop: 14,
      lineHeight: 1.4
    }
  }, isLocked ? 'Locked by a human decision — recorded as an approval.' : 'Exploring freely. Nothing is decided until a person locks it.')))));
}

/* ══════════════════════════════════════════════════════════
   EDIT — the cut + analysis that DESCRIBES present state (never advises).
   ══════════════════════════════════════════════════════════ */
function ccSmoothPath(pts) {
  if (pts.length < 2) return '';
  let dd = `M ${pts[0].x} ${pts[0].y}`;
  for (let i = 0; i < pts.length - 1; i++) {
    const p0 = pts[i - 1] || pts[i],
      p1 = pts[i],
      p2 = pts[i + 1],
      p3 = pts[i + 2] || p2;
    const c1x = p1.x + (p2.x - p0.x) / 6,
      c1y = p1.y + (p2.y - p0.y) / 6;
    const c2x = p2.x - (p3.x - p1.x) / 6,
      c2y = p2.y - (p3.y - p1.y) / 6;
    dd += ` C ${c1x.toFixed(1)} ${c1y.toFixed(1)}, ${c2x.toFixed(1)} ${c2y.toFixed(1)}, ${p2.x.toFixed(1)} ${p2.y.toFixed(1)}`;
  }
  return dd;
}
function CEdit({
  ctx,
  ck
}) {
  const beats = CC.beats;
  const padX = 4,
    padT = 12,
    padB = 12;
  const X = i => padX + i / (beats.length - 1) * (100 - 2 * padX);
  const Y = t => padT + (1 - t) * (100 - padT - padB);
  const pts = beats.map((b, i) => ({
    x: X(i),
    y: Y(b.t)
  }));
  const line = ccSmoothPath(pts);
  const area = line + ` L ${pts[pts.length - 1].x.toFixed(1)} 100 L ${pts[0].x.toFixed(1)} 100 Z`;
  const totalDur = CC.seq.reduce((a, s) => a + s.dur, 0);
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(CCHead, {
    ck: ck,
    kicker: "Edit",
    lede: "The sequence the room is reading. The analysis describes what is present \u2014 pacing, coverage, voiceover \u2014 it never says what should change."
  }), /*#__PURE__*/React.createElement(CCPanel, {
    ck: ck,
    label: "Sequence",
    title: null,
    style: {
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 4,
      height: 56,
      marginBottom: 8
    }
  }, CC.seq.map((s, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    title: s.desc,
    style: {
      flex: s.dur,
      minWidth: 0,
      background: s.tod === 'NIGHT' ? ck.clipGray : ck.clipBlue,
      border: `1px solid ${ck.clipEdge}`,
      borderRadius: 2,
      display: 'flex',
      flexDirection: 'column',
      justifyContent: 'space-between',
      padding: '5px 7px',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 8.5,
      color: ck.clipText,
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, s.loc), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 8,
      color: ck.clipSub
    }
  }, String(s.n).padStart(2, '0'))))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      color: ck.txt3
    }
  }, "0:00"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      color: ck.txt3
    }
  }, window.FLOW.fmtClock(totalDur)))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1.3fr 1fr',
      gap: 16,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement(CCPanel, {
    ck: ck,
    label: "Pacing",
    title: "Tension across the cut"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      height: 150
    }
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: "0 0 100 100",
    width: "100%",
    height: "100%",
    preserveAspectRatio: "none",
    style: {
      display: 'block',
      overflow: 'visible'
    }
  }, /*#__PURE__*/React.createElement("defs", null, /*#__PURE__*/React.createElement("linearGradient", {
    id: "cedit-stroke",
    x1: "0",
    y1: "0",
    x2: "1",
    y2: "0"
  }, /*#__PURE__*/React.createElement("stop", {
    offset: "0",
    stopColor: ck.read
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "0.42",
    stopColor: ck.amber
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "0.78",
    stopColor: ck.pink
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "1",
    stopColor: ck.amber
  })), /*#__PURE__*/React.createElement("linearGradient", {
    id: "cedit-fill",
    x1: "0",
    y1: "0",
    x2: "0",
    y2: "1"
  }, /*#__PURE__*/React.createElement("stop", {
    offset: "0",
    stopColor: ck.amber,
    stopOpacity: "0.20"
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "1",
    stopColor: ck.amber,
    stopOpacity: "0"
  }))), /*#__PURE__*/React.createElement("path", {
    d: area,
    fill: "url(#cedit-fill)"
  }), /*#__PURE__*/React.createElement("path", {
    d: line,
    fill: "none",
    stroke: "url(#cedit-stroke)",
    strokeWidth: "3",
    strokeLinecap: "round",
    vectorEffect: "non-scaling-stroke"
  })), beats.map((b, i) => b.t > 0.85 && /*#__PURE__*/React.createElement("div", {
    key: b.key,
    style: {
      position: 'absolute',
      left: pts[i].x + '%',
      top: pts[i].y + '%',
      transform: 'translate(-50%,-50%)',
      width: 13,
      height: 13,
      borderRadius: '50%',
      background: ck.bg0,
      border: `2.5px solid ${ck.pink}`,
      boxSizing: 'border-box'
    }
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: `repeat(${beats.length}, 1fr)`,
      marginTop: 8
    }
  }, beats.map(b => /*#__PURE__*/React.createElement("span", {
    key: b.key,
    style: {
      textAlign: 'center',
      fontFamily: CFK.MONO,
      fontSize: 10,
      letterSpacing: '0.06em',
      color: b.t > 0.85 ? ck.amber : ck.txt3,
      fontWeight: b.t > 0.85 ? 700 : 400
    }
  }, b.key)))), /*#__PURE__*/React.createElement(CCPanel, {
    ck: ck,
    label: "Creative analysis",
    title: null,
    pad: 0
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '4px 20px 8px'
    }
  }, CC.analysis.map((an, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      gap: 12,
      padding: '13px 0',
      borderTop: i ? `1px solid ${ck.line}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: ck.txt3,
      flex: '0 0 64px',
      paddingTop: 2
    }
  }, an.kind), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 13,
      color: ck.txt,
      lineHeight: 1.45
    }
  }, an.text)))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '12px 20px',
      borderTop: `1px solid ${ck.line}`,
      background: ck.bg2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SERIF,
      fontStyle: 'italic',
      fontSize: 12.5,
      color: ck.txt3,
      lineHeight: 1.4
    }
  }, "Describes what's there. Judgment stays with the room.")))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 32,
      paddingTop: 28,
      borderTop: `1px solid ${ck.line2}`
    }
  }, /*#__PURE__*/React.createElement(CBoards, {
    ctx: ctx,
    ck: ck
  })));
}

/* ══════════════════════════════════════════════════════════
   ASSETS — every row, filterable.
   ══════════════════════════════════════════════════════════ */
function CAssets({
  ctx,
  ck
}) {
  const types = ['ALL'].concat(Array.from(new Set(CC.assets.map(a => a.type))));
  const [filter, setFilter] = React.useState('ALL');
  const rows = CC.assets.filter(a => filter === 'ALL' || a.type === filter);
  const COLS = '120px 1fr 130px 70px 90px 110px';
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(CCHead, {
    ck: ck,
    kicker: "Assets",
    lede: "All assets, by type, version, and who made them. Generated frames carry their origin \u2014 a human directed every one."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 12,
      marginBottom: 16,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 8
    }
  }, types.map(t => /*#__PURE__*/React.createElement("button", {
    key: t,
    onClick: () => setFilter(t),
    style: {
      cursor: 'pointer',
      background: 'none',
      border: 0,
      padding: 0
    }
  }, /*#__PURE__*/React.createElement(CCChip, {
    ck: ck,
    on: filter === t
  }, t === 'ALL' ? 'All' : t)))), /*#__PURE__*/React.createElement("button", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      cursor: 'pointer',
      background: ck.amber,
      color: ck.bg0,
      border: 'none',
      borderRadius: 3,
      padding: '8px 15px',
      fontFamily: CFK.MONO,
      fontSize: 10.5,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      whiteSpace: 'nowrap'
    }
  }, /*#__PURE__*/React.createElement(CGl, {
    name: "upload",
    size: 13
  }), " Upload asset")), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${ck.line}`,
      borderRadius: 6,
      background: ck.bg1,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: COLS,
      gap: 12,
      padding: '13px 18px',
      borderBottom: `1px solid ${ck.line}`
    }
  }, ['Type', 'Asset', 'Origin', 'Ver', 'State', 'By'].map((h, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: ccKickF(ck)
  }, h))), rows.map((a, i) => /*#__PURE__*/React.createElement("div", {
    key: a.id,
    style: {
      display: 'grid',
      gridTemplateColumns: COLS,
      gap: 12,
      padding: '13px 18px',
      alignItems: 'center',
      borderTop: i ? `1px solid ${ck.line}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.08em',
      color: ck.amber
    }
  }, a.type), /*#__PURE__*/React.createElement("span", {
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      fontFamily: CFK.SANS,
      fontSize: 13.5,
      color: ck.txt,
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, a.label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      color: ck.txt3
    }
  }, a.scene)), /*#__PURE__*/React.createElement(CCOrigin, {
    ck: ck,
    origin: a.origin
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 11,
      color: ck.txt2
    }
  }, "v", a.v, a.parent ? '' : ''), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.08em',
      textTransform: 'uppercase',
      color: ccVerbState(ck, a.state)
    }
  }, a.state), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 12.5,
      color: ck.txt2
    }
  }, a.by)))));
}

/* ══════════════════════════════════════════════════════════
   COLLABORATION — approvals + comments. The AI never holds a verb.
   ══════════════════════════════════════════════════════════ */
function CCollab({
  ctx,
  ck
}) {
  const pending = CC.approvals.filter(a => a.state === 'PENDING');
  const acted = CC.approvals.filter(a => a.state !== 'PENDING');
  const apRow = (a, i, arr) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'grid',
      gridTemplateColumns: '120px 90px 1fr 90px',
      gap: 12,
      padding: '13px 18px',
      alignItems: 'center',
      borderTop: i ? `1px solid ${ck.line}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 13,
      color: ck.txt
    }
  }, CC.ROLE_LABEL[a.role] || a.role), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, a.verb), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 13,
      color: ck.txt2,
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, a.decision), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 7,
      justifyContent: 'flex-end'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ccVerbState(ck, a.state)
    }
  }, a.state), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 5,
      height: 5,
      borderRadius: 999,
      background: ccVerbState(ck, a.state)
    }
  })));
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(CCHead, {
    ck: ck,
    kicker: "Collaboration",
    title: "Who holds the verb",
    lede: "Every decision routes between the people who own it. Propose, decide, approve, veto \u2014 held by roles that are human. The AI is never in this table."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 380px',
      gap: 24,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(CCPanel, {
    ck: ck,
    label: "Awaiting a verb",
    title: null,
    pad: 0
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '120px 90px 1fr 90px',
      gap: 12,
      padding: '12px 18px',
      borderBottom: `1px solid ${ck.line}`
    }
  }, ['Role', 'Verb', 'Decision', 'State'].map((h, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: ccKickF(ck)
  }, h))), pending.map(apRow)), /*#__PURE__*/React.createElement(CCPanel, {
    ck: ck,
    label: "Acted",
    title: null,
    pad: 0
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '120px 90px 1fr 90px',
      gap: 12,
      padding: '12px 18px',
      borderBottom: `1px solid ${ck.line}`
    }
  }, ['Role', 'Verb', 'Decision', 'State'].map((h, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: ccKickF(ck)
  }, h))), acted.map(apRow))), /*#__PURE__*/React.createElement(CCPanel, {
    ck: ck,
    label: "Cross-department notes",
    title: null
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, CC.comments.map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      borderLeft: `2px solid ${ck.line2}`,
      paddingLeft: 13
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      gap: 8,
      marginBottom: 5
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 8,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 13,
      color: ck.txt
    }
  }, c.who), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 8.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, CC.ROLE_LABEL[c.role] || c.role)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      color: ck.txt3
    }
  }, c.when)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      letterSpacing: '0.06em',
      color: ck.amber,
      marginBottom: 5
    }
  }, c.decision), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 13,
      color: ck.txt2,
      lineHeight: 1.5
    }
  }, c.text)))))));
}
Object.assign(window, {
  CProject,
  CScript,
  CBoards,
  CEdit,
  CAssets,
  CCollab
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/creative-views.jsx", error: String((e && e.message) || e) }); }

// flow/creative-writers.jsx
try { (() => {
/* global React */
// TENET Creative — Writers Room. A working surface (like the edit bay):
// read one script version full-frame or compare two side by side. The script
// sits on white PAGES with borders (clear separation in compare); the desk,
// controls and cut timeline stay dark. Selecting a scene highlights its clip.
const {
  FK: WFK,
  CREATIVE: WC
} = window;

// Fixed paper palette for the script pages (independent of lane theme).
const WPAGE = {
  bg: '#FFFFFF',
  ink: '#1A1A1A',
  ink2: '#565656',
  ink3: '#9A9A9A',
  amber: '#BA7517',
  amber800: '#854F0B',
  line: 'rgba(0,0,0,0.14)',
  line2: 'rgba(0,0,0,0.08)',
  sel: '#FBF3E4',
  read: '#1F8A5B',
  cut: '#B23A2E'
};
function wText(lines) {
  return lines ? lines.map(function (l) {
    return l.text;
  }).join(' · ') : '';
}
function WLine(props) {
  const l = props.l,
    changed = props.changed;
  if (l.k === 'vo') {
    return /*#__PURE__*/React.createElement("div", {
      style: {
        borderLeft: '2px solid ' + (changed ? WPAGE.amber : 'rgba(0,0,0,0.12)'),
        paddingLeft: 12,
        margin: '7px 0',
        background: changed ? WPAGE.sel : 'transparent',
        borderRadius: changed ? '0 3px 3px 0' : 0
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: WFK.MONO,
        fontSize: 8.5,
        letterSpacing: '0.14em',
        textTransform: 'uppercase',
        color: WPAGE.amber,
        display: 'block',
        marginBottom: 3
      }
    }, "VO"), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: WFK.SCRIPT || WFK.SERIF,
        fontSize: 14.5,
        lineHeight: 1.5,
        color: changed ? WPAGE.amber800 : WPAGE.ink
      }
    }, l.text));
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: WFK.SCRIPT || WFK.SERIF,
      fontSize: 14,
      lineHeight: 1.55,
      color: changed ? WPAGE.amber800 : WPAGE.ink2,
      margin: '6px 0',
      background: changed ? WPAGE.sel : 'transparent',
      borderRadius: changed ? 3 : 0,
      padding: changed ? '2px 6px' : 0
    }
  }, l.text);
}
function WBody(props) {
  const lines = props.lines,
    other = props.other;
  return /*#__PURE__*/React.createElement("div", null, lines.map(function (l, i) {
    const o = other && other[i];
    const changed = !!other && (!o || o.text !== l.text);
    return /*#__PURE__*/React.createElement(WLine, {
      key: i,
      l: l,
      changed: changed
    });
  }));
}

// One white script page for a version. otherId (optional) drives diff marking.
function WPage(props) {
  const ck = props.ck,
    versionId = props.versionId,
    otherId = props.otherId;
  const sections = props.sections,
    sel = props.sel,
    onToggle = props.onToggle,
    isRight = props.isRight;
  const v = WC.writers.versions.find(function (x) {
    return x.id === versionId;
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      background: WPAGE.bg,
      border: '1px solid ' + WPAGE.line,
      borderRadius: 3,
      boxShadow: '0 2px 10px rgba(0,0,0,0.18)',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      padding: '14px 26px',
      borderBottom: '1px solid ' + WPAGE.line2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: WFK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: WPAGE.amber
    }
  }, v.tag), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: WFK.MONO,
      fontSize: 10,
      color: WPAGE.ink3
    }
  }, v.file)), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '8px 26px 26px'
    }
  }, sections.map(function (s) {
    const lines = WC.writers.resolve(versionId, s.sceneId);
    const other = otherId ? WC.writers.resolve(otherId, s.sceneId) : null;
    if (!lines) {
      return /*#__PURE__*/React.createElement("div", {
        key: s.sceneId,
        style: {
          padding: '16px 0',
          borderTop: '1px solid ' + WPAGE.line2,
          opacity: 0.5
        }
      }, /*#__PURE__*/React.createElement("div", {
        style: {
          fontFamily: WFK.MONO,
          fontSize: 10.5,
          letterSpacing: '0.1em',
          color: WPAGE.ink3
        }
      }, String(s.n).padStart(2, '0'), " \xB7 ", s.slug), /*#__PURE__*/React.createElement("div", {
        style: {
          fontFamily: WFK.SERIF,
          fontStyle: 'italic',
          fontSize: 12.5,
          color: WPAGE.ink3,
          marginTop: 5
        }
      }, "Not in this draft"));
    }
    const on = sel.indexOf(s.sceneId) >= 0;
    let tag = null;
    if (otherId) {
      if (!other) tag = isRight ? {
        t: 'Added',
        c: WPAGE.read
      } : {
        t: 'Cut',
        c: WPAGE.cut
      };else if (wText(lines) !== wText(other)) tag = {
        t: 'Revised',
        c: WPAGE.amber
      };
    }
    return /*#__PURE__*/React.createElement("button", {
      key: s.sceneId,
      onClick: function () {
        onToggle(s.sceneId);
      },
      style: {
        display: 'block',
        width: '100%',
        textAlign: 'left',
        cursor: 'pointer',
        border: 0,
        borderTop: '1px solid ' + WPAGE.line2,
        background: on ? WPAGE.sel : 'transparent',
        boxShadow: on ? 'inset 3px 0 0 ' + WPAGE.amber : 'none',
        padding: '14px 14px 14px',
        borderRadius: 0
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'baseline',
        justifyContent: 'space-between',
        gap: 10,
        marginBottom: 6
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: WFK.MONO,
        fontSize: 10.5,
        letterSpacing: '0.1em',
        color: on ? WPAGE.amber800 : WPAGE.ink
      }
    }, String(s.n).padStart(2, '0'), " \xB7 ", s.slug), tag && /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: WFK.MONO,
        fontSize: 8.5,
        letterSpacing: '0.1em',
        textTransform: 'uppercase',
        color: tag.c,
        flexShrink: 0
      }
    }, tag.t)), /*#__PURE__*/React.createElement(WBody, {
      lines: lines,
      other: other
    }));
  })));
}
function WritersRoom(props) {
  const ck = props.ck;
  const versions = WC.writers.versions;
  const sections = WC.writers.sections;
  const b = WC.breakdown;
  const [mode, setMode] = React.useState('single');
  const [ver, setVer] = React.useState('v4');
  const [left, setLeft] = React.useState('v2');
  const [right, setRight] = React.useState('v4');
  const [sel, setSel] = React.useState([]);
  function toggle(sceneId) {
    setSel(function (p) {
      return p.indexOf(sceneId) >= 0 ? p.filter(function (x) {
        return x !== sceneId;
      }) : p.concat([sceneId]);
    });
  }
  const activeScenes = mode === 'single' ? (versions.find(function (v) {
    return v.id === ver;
  }) || {}).scenes || [] : sections.map(function (s) {
    return s.sceneId;
  }).filter(function (id) {
    return WC.writers.resolve(left, id) || WC.writers.resolve(right, id);
  });
  const chip = function (id, active, onClick) {
    const v = versions.find(function (x) {
      return x.id === id;
    });
    return /*#__PURE__*/React.createElement("button", {
      key: id,
      onClick: onClick,
      style: {
        display: 'inline-flex',
        alignItems: 'center',
        gap: 6,
        cursor: 'pointer',
        padding: '5px 10px',
        borderRadius: 3,
        whiteSpace: 'nowrap',
        border: '1px solid ' + (active ? ck.amber : ck.line2),
        background: active ? ck.amberDim : 'transparent',
        fontFamily: WFK.MONO,
        fontSize: 10,
        letterSpacing: '0.06em',
        color: active ? ck.amber : ck.txt2
      }
    }, active && /*#__PURE__*/React.createElement("span", {
      style: {
        width: 5,
        height: 5,
        borderRadius: 999,
        background: ck.amber
      }
    }), v.tag, /*#__PURE__*/React.createElement("span", {
      style: {
        color: ck.txt3
      }
    }, v.date));
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex',
      flexDirection: 'column',
      background: ck.bg0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      minHeight: 52,
      display: 'flex',
      alignItems: 'center',
      gap: 18,
      padding: '0 22px',
      borderBottom: '1px solid ' + ck.line,
      background: ck.bg1,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: WFK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: ck.amber
    }
  }, "Writers Room"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: '1px solid ' + ck.line2,
      borderRadius: 3,
      overflow: 'hidden'
    }
  }, [['single', 'Full frame'], ['compare', 'Compare']].map(function (pair, i) {
    const id = pair[0],
      label = pair[1];
    return /*#__PURE__*/React.createElement("div", {
      key: id,
      onClick: function () {
        setMode(id);
      },
      style: {
        padding: '6px 13px',
        cursor: 'pointer',
        whiteSpace: 'nowrap',
        fontFamily: WFK.MONO,
        fontSize: 10,
        letterSpacing: '0.1em',
        textTransform: 'uppercase',
        borderLeft: i ? '1px solid ' + ck.line2 : 'none',
        background: mode === id ? ck.amber : ck.bg2,
        color: mode === id ? ck.bg0 : ck.txt2
      }
    }, label);
  })), mode === 'single' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 7
    }
  }, versions.map(function (v) {
    return chip(v.id, ver === v.id, function () {
      setVer(v.id);
    });
  })) : /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 9,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: WFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, "A"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 6
    }
  }, versions.map(function (v) {
    return chip(v.id, left === v.id, function () {
      setLeft(v.id);
    });
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      color: ck.txt3,
      fontFamily: WFK.MONO,
      fontSize: 12
    }
  }, '→'), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: WFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, "B"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 6
    }
  }, versions.map(function (v) {
    return chip(v.id, right === v.id, function () {
      setRight(v.id);
    });
  }))), /*#__PURE__*/React.createElement("span", {
    style: {
      marginLeft: 'auto',
      fontFamily: WFK.SERIF,
      fontStyle: 'italic',
      fontSize: 12.5,
      color: ck.txt3
    }
  }, sel.length ? sel.length + ' scene' + (sel.length === 1 ? '' : 's') + ' on the timeline' : 'Select a scene to mark the cut')), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      display: 'flex',
      alignItems: 'center',
      gap: 26,
      padding: '8px 22px',
      borderBottom: '1px solid ' + ck.line,
      background: ck.bg0
    }
  }, [['Scenes', b.scenes], ['Locations', b.locations], ['Cast', b.characters], ['Runtime', b.runtime]].map(function (pair) {
    return /*#__PURE__*/React.createElement("span", {
      key: pair[0],
      style: {
        display: 'inline-flex',
        alignItems: 'baseline',
        gap: 7
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: WFK.MONO,
        fontSize: 8.5,
        letterSpacing: '0.14em',
        textTransform: 'uppercase',
        color: ck.txt3
      }
    }, pair[0]), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: WFK.SANS,
        fontSize: 14,
        color: ck.txt2
      }
    }, pair[1]));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      overflowY: 'auto',
      overflowX: 'hidden'
    }
  }, mode === 'single' ? /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 720,
      margin: '0 auto',
      padding: '30px 24px 44px'
    }
  }, /*#__PURE__*/React.createElement(WPage, {
    ck: ck,
    versionId: ver,
    sections: sections,
    sel: sel,
    onToggle: toggle
  })) : /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 1180,
      margin: '0 auto',
      padding: '26px 24px 44px',
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 18,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement(WPage, {
    ck: ck,
    versionId: left,
    otherId: right,
    isRight: false,
    sections: sections,
    sel: sel,
    onToggle: toggle
  }), /*#__PURE__*/React.createElement(WPage, {
    ck: ck,
    versionId: right,
    otherId: left,
    isRight: true,
    sections: sections,
    sel: sel,
    onToggle: toggle
  }))), /*#__PURE__*/React.createElement(WTimeline, {
    ck: ck,
    sections: sections,
    activeScenes: activeScenes,
    sel: sel,
    onToggle: toggle
  }));
}

// Cut timeline: scene clips proportional to duration. Selected → amber.
// Scenes not in the active draft are faded ("not written yet").
function WTimeline(props) {
  const ck = props.ck,
    sections = props.sections,
    activeScenes = props.activeScenes,
    sel = props.sel,
    onToggle = props.onToggle;
  const total = sections.reduce(function (a, s) {
    return a + s.dur;
  }, 0);
  const ticks = [];
  for (var t = 0; t <= total; t += 5) ticks.push(t);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      background: ck.bg1,
      borderTop: '1px solid ' + ck.line2
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '8px 18px',
      borderBottom: '1px solid ' + ck.line
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: WFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, "The cut \xB7 V1.2"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: WFK.SANS,
      fontWeight: 300,
      fontSize: 18,
      color: ck.txt
    }
  }, window.FLOW.fmtClock(total))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '12px 18px 6px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 4,
      height: 46
    }
  }, sections.map(function (s) {
    const present = activeScenes.indexOf(s.sceneId) >= 0;
    const on = sel.indexOf(s.sceneId) >= 0;
    const stripe = ck === window.DK_LIGHT ? 'repeating-linear-gradient(135deg, #E7E2D8 0 7px, #ded8cc 7px 14px)' : 'repeating-linear-gradient(135deg, #20262d 0 7px, #1a1f25 7px 14px)';
    return /*#__PURE__*/React.createElement("button", {
      key: s.sceneId,
      onClick: function () {
        if (present) onToggle(s.sceneId);
      },
      title: s.loc,
      style: {
        flex: s.dur,
        minWidth: 0,
        cursor: present ? 'pointer' : 'default',
        textAlign: 'left',
        background: on ? ck.amberDim : present ? ck.clipBlue : stripe,
        border: '1px solid ' + (on ? ck.amber : present ? ck.clipEdge : ck.line2),
        borderRadius: 2,
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'space-between',
        padding: '5px 7px',
        overflow: 'hidden',
        opacity: present ? 1 : 0.55
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: WFK.MONO,
        fontSize: 8.5,
        color: on ? ck.amber : present ? ck.clipText : ck.txt3,
        whiteSpace: 'nowrap',
        overflow: 'hidden',
        textOverflow: 'ellipsis'
      }
    }, s.loc), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: WFK.MONO,
        fontSize: 8,
        color: on ? ck.amber : present ? ck.clipSub : ck.txt3
      }
    }, String(s.n).padStart(2, '0'), present ? '' : ' · —'));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      height: 18,
      marginTop: 4
    }
  }, ticks.map(function (tk) {
    return /*#__PURE__*/React.createElement("span", {
      key: tk,
      style: {
        position: 'absolute',
        left: tk / total * 100 + '%',
        top: 3,
        fontFamily: WFK.MONO,
        fontSize: 9,
        color: ck.txt3,
        transform: 'translateX(1px)'
      }
    }, '01:00:' + String(tk).padStart(2, '0'));
  }))));
}
window.WritersRoom = WritersRoom;
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/creative-writers.jsx", error: String((e && e.message) || e) }); }

// flow/data.js
try { (() => {
/* ============================================================
   TENET — Script-to-Scaffold Flow · the one project, modeled.
   ------------------------------------------------------------
   A :30 brand film. The script is the input. Everything else —
   the scaffold outline, the cost sheet, the shot timeline — is
   derived from it. Two seats read the same project:
     • Director — locations, characters, craft
     • Producer — cost, schedule, the line
   The producer→director chat is the bidirectional ripple:
   a constraint travels up, the script changes, money moves.
   ============================================================ */
window.FLOW = function () {
  const CHARS = [{
    id: 'mara',
    name: 'MARA',
    note: 'Lead'
  }, {
    id: 'theo',
    name: 'THEO',
    note: 'Partner'
  }, {
    id: 'maker',
    name: 'THE MAKER',
    note: 'Hands'
  }];
  const LOCS = [{
    id: 'rooftop',
    name: 'Rooftop',
    kind: 'EXT'
  }, {
    id: 'workshop',
    name: 'Workshop',
    kind: 'INT'
  }, {
    id: 'downtown',
    name: 'Downtown street',
    kind: 'EXT'
  }, {
    id: 'apartment',
    name: 'Apartment',
    kind: 'INT'
  }, {
    id: 'studio',
    name: 'Studio',
    kind: 'INT'
  }, {
    id: 'beach',
    name: 'Beach',
    kind: 'EXT'
  }];

  // Finance categories (order matters — drives the cost-profile popup).
  // Each scene carries `fin.lv` = a 1\u20137 spend level per category, plus the
  // primary cost drivers behind it. Shots inherit their scene's profile.
  const FIN_CATS = ['Location', 'Talent', 'Crowd', 'Camera', 'Environment', 'Art Dept', 'Vehicles', 'Practical FX', 'VFX', 'Schedule Risk'];

  // Each scene: slug parts, time-of-day (s4 is dynamic via state),
  // cost (day base + night premium), :15-cutdown membership, characters,
  // action paragraphs (with character tags), and shots (the timeline boxes).
  const SCENES = [{
    id: 's1',
    n: 1,
    locId: 'rooftop',
    tod: 'DAWN',
    todDynamic: false,
    costDay: 85000,
    nightPremium: 0,
    in15: true,
    chars: ['mara'],
    fin: {
      lv: [5, 3, 1, 5, 4, 2, 1, 2, 4, 6],
      drivers: ['Rooftop permit + safety', 'Crane / jib package', 'Dawn light window', 'Skyline VFX cleanup']
    },
    action: [{
      text: 'A city still blue with sleep. MARA stands at the rail.',
      chars: ['mara']
    }, {
      text: 'A watch catches the first light.',
      chars: []
    }],
    shots: [{
      id: 's1a',
      dur: 2,
      chars: [],
      desc: 'City, blue hour'
    }, {
      id: 's1b',
      dur: 2,
      chars: ['mara'],
      desc: 'Mara at the rail'
    }, {
      id: 's1c',
      dur: 2,
      chars: ['mara'],
      desc: 'Watch, first light'
    }]
  }, {
    id: 's2',
    n: 2,
    locId: 'workshop',
    tod: 'DAY',
    todDynamic: false,
    costDay: 70000,
    nightPremium: 0,
    in15: false,
    chars: ['maker'],
    fin: {
      lv: [2, 3, 1, 5, 1, 5, 1, 3, 2, 2],
      drivers: ['Macro / probe lens package', 'Specialty watchmaker talent', 'Detailed set dressing']
    },
    action: [{
      text: 'Hands at a bench. THE MAKER sets a movement turning.',
      chars: ['maker']
    }, {
      text: 'Tiny gears, deliberate.',
      chars: []
    }],
    shots: [{
      id: 's2a',
      dur: 2,
      chars: [],
      desc: 'Bench, tools'
    }, {
      id: 's2b',
      dur: 2,
      chars: ['maker'],
      desc: 'The movement'
    }, {
      id: 's2c',
      dur: 1,
      chars: ['maker'],
      desc: 'Maker\u2019s hands'
    }]
  }, {
    id: 's3',
    n: 3,
    locId: 'downtown',
    tod: 'DAY',
    todDynamic: false,
    costDay: 80000,
    nightPremium: 0,
    in15: false,
    chars: ['mara'],
    fin: {
      lv: [6, 3, 6, 4, 3, 3, 4, 2, 3, 4],
      drivers: ['Street closure permit', 'Background extras', 'Steadicam operator', 'Picture vehicles']
    },
    action: [{
      text: 'MARA moves through the city, unhurried.',
      chars: ['mara']
    }, {
      text: 'The crowd blurs; she does not.',
      chars: []
    }],
    shots: [{
      id: 's3a',
      dur: 2,
      chars: ['mara'],
      desc: 'Mara, walking'
    }, {
      id: 's3b',
      dur: 1.5,
      chars: [],
      desc: 'Crowd blur'
    }, {
      id: 's3c',
      dur: 1.5,
      chars: ['mara'],
      desc: 'Reflection in glass'
    }]
  }, {
    // The night scene — the producer's constraint target.
    id: 's4',
    n: 4,
    locId: 'apartment',
    tod: 'NIGHT',
    todDynamic: true,
    costDay: 60000,
    nightPremium: 50000,
    in15: false,
    chars: ['mara', 'theo'],
    fin: {
      lv: [3, 5, 1, 3, 2, 4, 1, 2, 1, 5],
      drivers: ['Night premium + OT', 'Two principal cast', 'Practical interior lighting']
    },
    action: [{
      text: 'THEO fastens the watch on MARA\u2019s wrist.',
      chars: ['mara', 'theo']
    }, {
      text: 'A held look. The day, given.',
      chars: []
    }],
    shots: [{
      id: 's4a',
      dur: 2,
      chars: ['theo', 'mara'],
      desc: 'The gift'
    }, {
      id: 's4b',
      dur: 2,
      chars: ['mara', 'theo'],
      desc: 'Faces'
    }, {
      id: 's4c',
      dur: 1,
      chars: ['mara'],
      desc: 'Wrist clasp'
    }]
  }, {
    id: 's5',
    n: 5,
    locId: 'studio',
    tod: '\u2014',
    todDynamic: false,
    costDay: 110000,
    nightPremium: 0,
    in15: true,
    chars: [],
    fin: {
      lv: [4, 1, 1, 6, 1, 3, 1, 2, 5, 3],
      drivers: ['Motion-control rig', 'Macro / probe lens', 'Product handling + comp']
    },
    action: [{
      text: 'The product, alone. Light moves across the dial like weather.',
      chars: []
    }],
    shots: [{
      id: 's5a',
      dur: 2.5,
      chars: [],
      desc: 'Macro, dial'
    }, {
      id: 's5b',
      dur: 2,
      chars: [],
      desc: 'Macro, strap'
    }, {
      id: 's5c',
      dur: 1.5,
      chars: [],
      desc: 'Logo resolve'
    }]
  }, {
    id: 's6',
    n: 6,
    locId: 'beach',
    tod: 'SUNSET',
    todDynamic: false,
    costDay: 45000,
    nightPremium: 0,
    in15: true,
    chars: ['mara'],
    fin: {
      lv: [5, 3, 1, 4, 5, 2, 3, 1, 2, 7],
      drivers: ['Golden-hour window', 'Location travel + permit', 'Weather contingency', 'Tide timing']
    },
    action: [{
      text: 'MARA at the shoreline. The watch, the light, the long now.',
      chars: ['mara']
    }],
    shots: [{
      id: 's6a',
      dur: 1.5,
      chars: ['mara'],
      desc: 'Mara at shore'
    }, {
      id: 's6b',
      dur: 1.5,
      chars: ['mara'],
      desc: 'Wide beach'
    }]
  }];
  const TITLE = 'A Brief History of Selling Ideas';
  const LOGLINE = 'One watch. One day. A life held in the light.';

  // ── The script, AV format (VISUAL / VO) — matches the writer's doc ──
  // k: 'v' = VISUAL (stage direction) · 'vo' = voiceover line ·
  //    'card' = title/cut card · 'q' = transition question
  const FILM = {
    title: TITLE,
    file: 'A_Brief_History_v3.fdx',
    parts: [{
      id: 'p1',
      n: 1,
      label: 'The Old Way',
      blocks: [{
        k: 'card',
        text: 'Title over black',
        note: 'A Brief History of Selling Ideas'
      }, {
        k: 'v',
        text: 'Open on Times Square. Camera BOOMS UP the face of an 80th-floor office building. Inside, rows of frantic executives type furiously. Cigarette smoke hangs in the air. A man leaps to his feet holding a script above his head — equal parts fear and triumph.'
      }, {
        k: 'vo',
        text: 'Once upon a time, making something great meant\u2026 guessing.'
      }, {
        k: 'v',
        text: 'Rapid cuts. Script changes hands. Elevator UP button pressed. Dark boardroom. A massive executive slams a stamp \u2014 FAIL. Exterior: a man is thrown out a window.'
      }, {
        k: 'vo',
        text: 'Months of writing. Rewriting. Pitching. Waiting. More waiting.'
      }, {
        k: 'v',
        text: 'Rapid cuts. A new man is dumped into an empty seat. A hand shoots up with a new script. Feet walking down a hall. Giant rubber stamp hits paper \u2014 APPROVED.'
      }, {
        k: 'vo',
        text: 'Then boards. Notes. New boards. Different notes. Same notes.'
      }, {
        k: 'v',
        text: 'Elevator DOWN button. An artist furiously draws storyboards. Elevator UP button. Giant rubber stamp \u2014 APPROVED. A giant check is signed. Millions are committed.'
      }, {
        k: 'vo',
        text: 'An idea is approved. Millions committed. Still no one\u2019s seen it.'
      }, {
        k: 'v',
        text: 'Clapperboard snaps. Sets constructed. Crew scrambling. Chaos energy.'
      }, {
        k: 'vo',
        text: 'Production begins.'
      }, {
        k: 'v',
        text: 'Martini glasses clink. Exhausted smiles.'
      }, {
        k: 'vo',
        text: 'Production wraps.'
      }, {
        k: 'v',
        text: 'Boardroom gathered around an old square TV. A dramatic pause as the screen flickers on.'
      }, {
        k: 'vo',
        text: 'And finally\u2026 the first real version.'
      }, {
        k: 'card',
        text: 'Cut to black'
      }, {
        k: 'vo',
        text: 'And sometimes\u2026 it\u2019s not what anyone pictured.'
      }, {
        k: 'v',
        text: 'Silence. Faces freeze. Someone is thrown out the window.'
      }, {
        k: 'vo',
        text: 'Sometimes? Magic.'
      }, {
        k: 'v',
        text: 'Smash cut to a family watching TV. They jump up and run out of the house. Cars pull up to a store parking lot. Cars leave loaded with product.'
      }]
    }, {
      id: 'tr',
      transition: true,
      label: 'Transition',
      blocks: [{
        k: 'q',
        text: 'A lot has changed since the good old days.'
      }, {
        k: 'q',
        text: 'But we still sell ideas the same way.'
      }, {
        k: 'q',
        text: 'What if you could actually see the idea\u2026 before you make it?'
      }, {
        k: 'q',
        text: 'What if changing one thing didn\u2019t mean starting over?'
      }, {
        k: 'q',
        text: 'What if the best idea won \u2014 not just the safest one?'
      }]
    }, {
      id: 'p2',
      n: 2,
      label: 'The New Way',
      blocks: [{
        k: 'v',
        text: 'Camera BOOMS UP Times Square again \u2014 this time into a warm apartment window. A young creative sits cross-legged with a laptop. Calm. Cat in lap.'
      }, {
        k: 'vo',
        text: 'What if every revision came to life instantly?'
      }, {
        k: 'v',
        text: 'On the laptop screen, script text auto-transforms into rough storyboard frames on a timeline.'
      }, {
        k: 'vo',
        text: 'Try this.'
      }, {
        k: 'v',
        text: 'The character on screen transforms into a squirrel. The creative bursts out laughing.'
      }, {
        k: 'vo',
        text: 'No more imagining. Just reacting.'
      }, {
        k: 'v',
        text: 'Split screen. A remote collaborator types notes. The change happens in real time.'
      }, {
        k: 'vo',
        text: 'If it doesn\u2019t work? You\u2019ll know fast.'
      }, {
        k: 'v',
        text: 'Rapid iterations. The character cycles through different looks, styles, tones.'
      }, {
        k: 'vo',
        text: 'So you can try again. And again. And again.'
      }, {
        k: 'v',
        text: 'Cut to a relaxed boardroom. Pizza boxes. The screen plays a polished animatic version. Confident nods.'
      }, {
        k: 'vo',
        text: 'When you finally hit \u2018approve\u2019\u2026 you mean it.'
      }, {
        k: 'v',
        text: 'Transition seamlessly from animatic to a real production set that matches the storyboard exactly.'
      }, {
        k: 'vo',
        text: 'Better ideas. Fewer heart attacks.'
      }, {
        k: 'v',
        text: 'Return to the exterior of the office building. A window cleaner drops dramatically two inches. Calmly continues cleaning.'
      }, {
        k: 'vo',
        text: 'Stop guessing. Start seeing.'
      }]
    }]
  };

  // ── Derivations (pure; depend on state) ────────────────────────────
  // state: { mode: 30|15, tod: { s4:'NIGHT'|'DAY' } }
  function todOf(scene, state) {
    if (scene.todDynamic) return state.tod && state.tod[scene.id] || scene.tod;
    return scene.tod;
  }
  function isNight(scene, state) {
    return todOf(scene, state) === 'NIGHT';
  }
  function costOf(scene, state) {
    return scene.costDay + (isNight(scene, state) ? scene.nightPremium : 0);
  }
  function activeScenes(state) {
    return SCENES.filter(s => state.mode === 15 ? s.in15 : true);
  }
  function totalCost(state) {
    return activeScenes(state).reduce((a, s) => a + costOf(s, state), 0);
  }
  function totalLength(state) {
    return activeScenes(state).reduce((a, s) => a + s.shots.reduce((b, sh) => b + sh.dur, 0), 0);
  }
  function allShots(state) {
    const out = [];
    activeScenes(state).forEach(s => s.shots.forEach(sh => out.push(Object.assign({
      sceneId: s.id,
      locId: s.locId,
      n: s.n
    }, sh))));
    return out;
  }
  function locUsed(locId, state) {
    return activeScenes(state).some(s => s.locId === locId);
  }
  function charUsed(charId, state) {
    return activeScenes(state).some(s => s.chars.includes(charId));
  }
  function locName(id) {
    const l = LOCS.find(x => x.id === id);
    return l ? l.name : id;
  }
  function charName(id) {
    const c = CHARS.find(x => x.id === id);
    return c ? c.name : id;
  }
  function fmtMoney(n) {
    return '$' + Math.round(n).toLocaleString('en-US');
  }
  function fmtClock(sec) {
    const s = Math.round(sec);
    return '0:' + String(s).padStart(2, '0');
  }
  return {
    CHARS,
    LOCS,
    SCENES,
    TITLE,
    LOGLINE,
    FILM,
    FIN_CATS,
    todOf,
    isNight,
    costOf,
    activeScenes,
    totalCost,
    totalLength,
    allShots,
    locUsed,
    charUsed,
    locName,
    charName,
    fmtMoney,
    fmtClock,
    DEFAULT_STATE: {
      mode: 30,
      tod: {
        s4: 'NIGHT'
      }
    }
  };
}();
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/data.js", error: String((e && e.message) || e) }); }

// flow/director-edit.jsx
try { (() => {
/* global React, FK, FLOW, FRAMEWORK, FGlyph */
// Director page — a dark edit-bay layout (Flame-style):
//   left  = scaffold + variables   (the "pink" zone)
//   center= program monitor
//   right = live script            (the "blue" zone)
//   below = transport + multi-track timeline.

const DK_DARK = {
  bg0: '#0E0F12',
  bg1: '#16181d',
  bg2: '#1f2228',
  bg3: '#2a2e36',
  line: 'rgba(255,255,255,0.06)',
  line2: 'rgba(255,255,255,0.13)',
  txt: '#dfe2e6',
  txt2: '#8c929b',
  txt3: '#5b616a',
  amber: '#E3A155',
  amberDim: 'rgba(227,161,85,0.16)',
  clip: '#33414f',
  clipBlue: '#34465c',
  clipGray: '#3a4048',
  wave: '#69727e',
  read: '#7fd6a6',
  clipEdge: '#4a5a6c',
  clipText: 'rgba(255,255,255,0.85)',
  clipSub: 'rgba(255,255,255,0.4)',
  pink: '#f0909f',
  lift: '0 1px 0 rgba(255,255,255,0.04), 0 18px 44px -20px rgba(0,0,0,0.8)'
};
const DK_LIGHT = {
  bg0: '#ECECEA',
  bg1: '#FFFFFF',
  bg2: '#FBFBFA',
  bg3: '#F4F4F2',
  line: 'rgba(0,0,0,0.10)',
  line2: 'rgba(0,0,0,0.16)',
  txt: '#1A1A1A',
  txt2: '#6B6B6B',
  txt3: '#9A9A9A',
  amber: '#BA7517',
  amberDim: 'rgba(186,117,23,0.10)',
  clip: '#c9d3dd',
  clipBlue: '#c0d0e0',
  clipGray: '#dad6cd',
  wave: '#a6aeb8',
  read: '#1F7A52',
  clipEdge: '#a6b4c2',
  clipText: 'rgba(33,28,21,0.82)',
  clipSub: 'rgba(33,28,21,0.42)',
  pink: '#C75B74',
  lift: '0 1px 0 rgba(33,28,22,0.015), 0 12px 34px -16px rgba(64,44,16,0.24)'
};
let DK = DK_DARK;

// ── Left: scaffold + variables (pink zone) ──────────────────────────
const DIR_DRAFT = 'Can we hold the beach for golden hour? It sells the ending.';
function ChatProducer({
  chat,
  onSend
}) {
  const bub = mine => ({
    alignSelf: mine ? 'flex-end' : 'flex-start',
    maxWidth: '88%',
    background: mine ? DK.amber : DK.bg3,
    color: mine ? DK.bg0 : DK.txt,
    border: mine ? 'none' : `1px solid ${DK.line2}`,
    borderRadius: 8,
    padding: '8px 11px',
    fontFamily: FK.SANS,
    fontSize: 12.5,
    lineHeight: 1.4,
    marginBottom: 7
  });
  const who = mine => ({
    fontFamily: FK.MONO,
    fontSize: 9,
    letterSpacing: '0.12em',
    textTransform: 'uppercase',
    color: mine ? 'rgba(0,0,0,0.5)' : DK.txt3,
    marginBottom: 3
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: `1px solid ${DK.line}`,
      background: DK.bg2,
      padding: '12px 18px 14px',
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: DK.amber,
      marginBottom: 10,
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      whiteSpace: 'nowrap'
    }
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "chat",
    size: 13
  }), " Message \xB7 Producer"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, chat.messages.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: bub(m.from === 'director')
  }, /*#__PURE__*/React.createElement("div", {
    style: who(m.from === 'director')
  }, m.from === 'director' ? 'You · Director' : 'Producer'), m.text)), chat.typing && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 12.5,
      color: DK.txt3,
      marginBottom: 7
    }
  }, "Producer is checking the numbers\u2026")), !chat.sent && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 9
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 12.5,
      color: DK.txt,
      lineHeight: 1.4,
      padding: '9px 11px',
      background: DK.bg1,
      border: `1px solid ${DK.line2}`,
      borderRadius: 8
    }
  }, DIR_DRAFT), /*#__PURE__*/React.createElement("button", {
    onClick: onSend,
    style: {
      alignSelf: 'flex-end',
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      cursor: 'pointer',
      background: DK.amber,
      color: DK.bg0,
      border: 'none',
      borderRadius: 2,
      padding: '8px 15px',
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.12em',
      textTransform: 'uppercase'
    }
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "send",
    size: 13
  }), " Send")));
}
function EditScaffold({
  cat,
  subIdx,
  onCat,
  onSub,
  open,
  onToggle
}) {
  const catObj = FRAMEWORK.DIRECTOR.find(c => c.id === cat) || FRAMEWORK.DIRECTOR[0];
  const subs = catObj.subs;
  const go = d => onSub((subIdx + d + subs.length) % subs.length);
  if (!open) {
    return /*#__PURE__*/React.createElement("div", {
      style: {
        height: '100%',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        background: DK.bg1,
        borderRight: `1px solid ${DK.line}`,
        padding: '12px 0'
      }
    }, /*#__PURE__*/React.createElement("span", {
      onClick: onToggle,
      title: "Open reference library",
      style: {
        width: 30,
        height: 30,
        borderRadius: 6,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        cursor: 'pointer',
        color: DK.txt2,
        border: `1px solid ${DK.line2}`
      }
    }, /*#__PURE__*/React.createElement(FGlyph, {
      name: "arrow",
      size: 13
    })), /*#__PURE__*/React.createElement("div", {
      style: {
        writingMode: 'vertical-rl',
        transform: 'rotate(180deg)',
        marginTop: 18,
        fontFamily: FK.MONO,
        fontSize: 10,
        letterSpacing: '0.22em',
        textTransform: 'uppercase',
        color: DK.txt3
      }
    }, "Reference library"));
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      display: 'flex',
      flexDirection: 'column',
      background: DK.bg1,
      borderRight: `1px solid ${DK.line}`,
      minHeight: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '14px 18px 12px',
      borderBottom: `1px solid ${DK.line}`,
      display: 'flex',
      alignItems: 'flex-start',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: DK.amber
    }
  }, "Director \xB7 scaffold"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 19,
      color: DK.txt,
      marginTop: 5
    }
  }, "Reference library")), /*#__PURE__*/React.createElement("span", {
    onClick: onToggle,
    title: "Collapse",
    style: {
      width: 26,
      height: 26,
      borderRadius: 6,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      cursor: 'pointer',
      color: DK.txt2,
      border: `1px solid ${DK.line2}`,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 12,
    style: {
      transform: 'scaleX(-1)'
    }
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      overflow: 'auto',
      minHeight: 0
    }
  }, FRAMEWORK.DIRECTOR.map(c => {
    const on = c.id === cat;
    return /*#__PURE__*/React.createElement("div", {
      key: c.id,
      onClick: () => onCat(c.id),
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 12,
        padding: '8px 18px',
        cursor: 'pointer',
        background: on ? DK.amberDim : 'transparent',
        boxShadow: on ? `inset 3px 0 0 ${DK.amber}` : 'none'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 10,
        color: on ? DK.amber : DK.txt3,
        width: 18
      }
    }, String(c.n).padStart(2, '0')), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.SERIF,
        fontStyle: 'italic',
        fontSize: 16,
        color: on ? DK.amber : DK.txt,
        flex: 1
      }
    }, c.name), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 9,
        color: DK.txt3
      }
    }, "6"));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: `1px solid ${DK.line}`,
      background: DK.bg2,
      padding: '12px 18px 14px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      marginBottom: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: DK.amber
    }
  }, String(catObj.n).padStart(2, '0'), " \xB7 ", catObj.name), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 22,
      height: 22,
      borderRadius: 999,
      border: `1px solid ${DK.line2}`,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      cursor: 'pointer',
      color: DK.txt2
    },
    onClick: () => go(-1)
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 12,
    style: {
      transform: 'scaleX(-1)'
    }
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: DK.txt2,
      width: 30,
      textAlign: 'center'
    }
  }, subIdx + 1, "/", subs.length), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 22,
      height: 22,
      borderRadius: 999,
      border: `1px solid ${DK.line2}`,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      cursor: 'pointer',
      color: DK.txt2
    },
    onClick: () => go(1)
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 12
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 6
    }
  }, subs.map((s, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    onClick: () => onSub(i),
    style: {
      padding: '6px 10px',
      cursor: 'pointer',
      fontFamily: FK.MONO,
      fontSize: 10.5,
      letterSpacing: '0.02em',
      whiteSpace: 'nowrap',
      color: i === subIdx ? DK.bg0 : DK.txt2,
      background: i === subIdx ? DK.amber : DK.bg3,
      borderRadius: 2
    }
  }, s)))));
}

// ── Center: program monitor ─────────────────────────────────────────
function ProgramMonitor({
  state
}) {
  const len = FLOW.totalLength(state);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      display: 'flex',
      flexDirection: 'column',
      background: DK.bg0,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 14,
      padding: '8px 16px',
      color: DK.txt2
    }
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "film",
    size: 14,
    style: {
      color: DK.txt3
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: DK.txt3
    }
  }, "Versions"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 16
    }
  }, ['01_BriefHistory_HD', '01_BriefHistory_HD_vb3', '01_BriefHistory_HD_a2'].map(v => {
    const active = v === '01_BriefHistory_HD';
    return /*#__PURE__*/React.createElement("span", {
      key: v,
      style: {
        fontFamily: FK.MONO,
        fontSize: 11,
        letterSpacing: '0.1em',
        color: active ? DK.amber : DK.txt3,
        cursor: 'pointer',
        display: 'inline-flex',
        alignItems: 'center',
        gap: 6
      }
    }, active && /*#__PURE__*/React.createElement("span", {
      style: {
        width: 5,
        height: 5,
        borderRadius: 999,
        background: DK.amber
      }
    }), v);
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      padding: '0 24px 8px',
      minHeight: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: '100%',
      maxWidth: 912,
      aspectRatio: '16 / 9',
      background: '#000',
      border: `1px solid ${DK.line2}`,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: "assets/creative-thumbnail.png",
    alt: "Program",
    style: {
      position: 'absolute',
      inset: 0,
      width: '100%',
      height: '100%',
      objectFit: 'cover',
      display: 'block'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 0,
      left: 0,
      right: 0,
      height: 44,
      background: 'linear-gradient(rgba(0,0,0,0.5), transparent)',
      pointerEvents: 'none'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      bottom: 0,
      left: 0,
      right: 0,
      height: 40,
      background: 'linear-gradient(transparent, rgba(0,0,0,0.5))',
      pointerEvents: 'none'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 12,
      left: 14,
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.18em',
      color: 'rgba(255,255,255,0.7)'
    }
  }, "PROGRAM \xB7 16\xD79"), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 14,
      bottom: 12,
      fontFamily: FK.MONO,
      fontSize: 10,
      color: 'rgba(255,255,255,0.75)'
    }
  }, "01:00:00:00"), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      right: 14,
      bottom: 12,
      fontFamily: FK.MONO,
      fontSize: 10,
      color: 'rgba(255,255,255,0.75)'
    }
  }, FLOW.fmtClock(len), " \xB7 1920\xD71080"))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '6px 18px',
      borderTop: `1px solid ${DK.line}`,
      color: DK.txt3,
      fontFamily: FK.MONO,
      fontSize: 10
    }
  }, /*#__PURE__*/React.createElement("span", null, "SRC 00:00:02:01"), /*#__PURE__*/React.createElement("span", null, "CONTRAST 1.00 \xB7 GAMMA 1.00"), /*#__PURE__*/React.createElement("span", null, "1920 \xD7 1080 (1.778)")));
}

// ── Right: live script (AV format — VISUAL / VO) ────────────────────
function EditScript({
  state,
  flashId
}) {
  const film = FLOW.FILM;
  const Block = ({
    b
  }) => {
    if (b.k === 'card') {
      return /*#__PURE__*/React.createElement("div", {
        style: {
          margin: '18px 0',
          textAlign: 'center'
        }
      }, /*#__PURE__*/React.createElement("div", {
        style: {
          display: 'flex',
          alignItems: 'center',
          gap: 12
        }
      }, /*#__PURE__*/React.createElement("span", {
        style: {
          flex: 1,
          height: 1,
          background: DK.line2
        }
      }), /*#__PURE__*/React.createElement("span", {
        style: {
          fontFamily: FK.MONO,
          fontSize: 10,
          letterSpacing: '0.18em',
          textTransform: 'uppercase',
          color: DK.txt2,
          whiteSpace: 'nowrap'
        }
      }, b.text), /*#__PURE__*/React.createElement("span", {
        style: {
          flex: 1,
          height: 1,
          background: DK.line2
        }
      })), b.note && /*#__PURE__*/React.createElement("div", {
        style: {
          fontFamily: FK.SERIF,
          fontStyle: 'italic',
          fontSize: 14,
          color: DK.txt3,
          marginTop: 7
        }
      }, b.note));
    }
    if (b.k === 'q') {
      return /*#__PURE__*/React.createElement("div", {
        style: {
          fontFamily: FK.SERIF,
          fontStyle: 'italic',
          fontSize: 15.5,
          lineHeight: 1.4,
          color: DK.txt,
          textAlign: 'center',
          margin: '0 8px 13px'
        }
      }, b.text);
    }
    const vo = b.k === 'vo';
    return /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        gap: 14,
        marginBottom: 13
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        flex: '0 0 46px',
        textAlign: 'right',
        paddingTop: 3
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 9,
        letterSpacing: '0.14em',
        textTransform: 'uppercase',
        color: vo ? DK.amber : DK.txt3
      }
    }, vo ? 'VO' : 'Visual')), /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        minWidth: 0,
        borderLeft: vo ? `2px solid ${DK.amber}` : 'none',
        paddingLeft: vo ? 12 : 0
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.SCRIPT,
        fontSize: 13.5,
        lineHeight: 1.5,
        color: vo ? DK.txt : DK.txt2,
        fontWeight: vo ? 500 : 400
      }
    }, b.text)));
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      display: 'flex',
      flexDirection: 'column',
      background: DK.bg1,
      borderLeft: `1px solid ${DK.line}`,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '12px 20px',
      borderBottom: `1px solid ${DK.line}`,
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: DK.amber
    }
  }, "Live script"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: DK.txt3
    }
  }, film.file)), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      overflow: 'auto',
      padding: '18px 22px 26px',
      minHeight: 0
    }
  }, film.parts.map((part, pi) => /*#__PURE__*/React.createElement("div", {
    key: part.id,
    style: {
      marginTop: pi === 0 ? 0 : 24
    }
  }, part.transition ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      margin: '4px 0 16px'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      height: 1,
      background: DK.line
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.24em',
      textTransform: 'uppercase',
      color: DK.txt3
    }
  }, "Transition"), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      height: 1,
      background: DK.line
    }
  })) : /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 15
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: DK.amber
    }
  }, "Part ", part.n, " \xB7 ", part.label), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 1,
      background: DK.line,
      marginTop: 8
    }
  })), part.blocks.map((b, i) => /*#__PURE__*/React.createElement(Block, {
    key: i,
    b: b
  }))))));
}

// ── Transport bar ───────────────────────────────────────────────────
function TransportBar({
  state,
  onMode,
  view,
  onView
}) {
  const len = FLOW.totalLength(state);
  const btn = {
    width: 30,
    height: 26,
    borderRadius: 3,
    border: `1px solid ${DK.line2}`,
    background: DK.bg2,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    color: DK.txt2,
    cursor: 'pointer'
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      height: 50,
      flexShrink: 0,
      background: DK.bg1,
      borderTop: `1px solid ${DK.line}`,
      borderBottom: `1px solid ${DK.line}`,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '0 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 17,
      color: DK.read,
      letterSpacing: '0.04em'
    }
  }, "01:00:04:12"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: DK.txt3
    }
  }, "SRC 00:00:02:01"), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 1,
      height: 22,
      background: DK.line2,
      margin: '0 4px'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: `1px solid ${DK.line2}`,
      borderRadius: 3,
      overflow: 'hidden'
    }
  }, [['timeline', 'Timeline'], ['beats', 'Beats']].map(([id, label], i) => /*#__PURE__*/React.createElement("div", {
    key: id,
    onClick: () => onView(id),
    style: {
      padding: '5px 13px',
      cursor: 'pointer',
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      borderLeft: i ? `1px solid ${DK.line2}` : 'none',
      background: view === id ? DK.amber : DK.bg2,
      color: view === id ? DK.bg0 : DK.txt2
    }
  }, label)))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: btn
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 13,
    style: {
      transform: 'scaleX(-1)'
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      ...btn,
      width: 38,
      background: DK.amber,
      borderColor: DK.amber,
      color: DK.bg0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "13",
    height: "13",
    viewBox: "0 0 24 24",
    fill: "currentColor"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M7 4l13 8-13 8z"
  }))), /*#__PURE__*/React.createElement("div", {
    style: btn
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 13
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 1,
      height: 22,
      background: DK.line2,
      margin: '0 8px'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: DK.txt3,
      marginRight: 6
    }
  }, "LENGTH"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: 22,
      color: DK.txt
    }
  }, FLOW.fmtClock(len))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: DK.txt3
    }
  }, "CUT"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: `1px solid ${DK.line2}`,
      borderRadius: 3,
      overflow: 'hidden'
    }
  }, [30, 15].map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: m,
    onClick: () => onMode(m),
    style: {
      padding: '5px 12px',
      cursor: 'pointer',
      fontFamily: FK.MONO,
      fontSize: 11,
      borderLeft: i ? `1px solid ${DK.line2}` : 'none',
      background: state.mode === m ? DK.amber : DK.bg2,
      color: state.mode === m ? DK.bg0 : DK.txt2
    }
  }, ":", m)))));
}

// ── Multi-track timeline ────────────────────────────────────────────
function Waveform() {
  const bars = [];
  for (let i = 0; i < 160; i++) {
    const x = i / 160;
    const a = Math.abs(Math.sin(x * 22) * 0.5 + Math.sin(x * 7 + 1) * 0.35 + Math.sin(x * 51) * 0.18);
    bars.push(Math.max(0.06, a) * 100);
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: '4px 0',
      display: 'flex',
      alignItems: 'center',
      gap: 1,
      padding: '0 6px'
    }
  }, bars.map((h, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      flex: 1,
      height: h + '%',
      background: DK.wave,
      opacity: 0.85,
      minWidth: 1
    }
  })));
}
function Clip({
  left,
  width,
  color,
  label,
  sub
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 3,
      bottom: 3,
      left: left + '%',
      width: width + '%',
      background: color,
      border: `1px solid ${DK.clipEdge}`,
      borderRadius: 2,
      overflow: 'hidden',
      display: 'flex',
      alignItems: 'center',
      padding: '0 8px',
      boxSizing: 'border-box'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: DK.clipText,
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, label), sub && /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      color: DK.clipSub,
      marginLeft: 'auto',
      flexShrink: 0
    }
  }, sub));
}
function EditTimeline({
  state
}) {
  const scenes = FLOW.activeScenes(state);
  const total = FLOW.totalLength(state);
  // scene start offsets
  let acc = 0;
  const sceneClips = scenes.map(s => {
    const dur = s.shots.reduce((a, sh) => a + sh.dur, 0);
    const clip = {
      left: acc / total * 100,
      width: dur / total * 100,
      name: FLOW.locName(s.locId).toUpperCase(),
      n: s.n
    };
    acc += dur;
    return clip;
  });
  const ROW_H = 34;
  const header = (label, p) => /*#__PURE__*/React.createElement("div", {
    style: {
      height: ROW_H,
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      padding: '0 10px',
      borderBottom: `1px solid ${DK.line}`,
      background: DK.bg1
    }
  }, p && /*#__PURE__*/React.createElement("span", {
    style: {
      width: 14,
      height: 14,
      borderRadius: 2,
      background: DK.amber,
      color: DK.bg0,
      fontFamily: FK.MONO,
      fontSize: 9,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center'
    }
  }, "P"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 11,
      color: DK.txt2,
      flex: 1
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 5,
      height: 5,
      borderRadius: 999,
      background: DK.txt3
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 5,
      height: 5,
      borderRadius: 999,
      background: DK.txt3
    }
  }));
  const lane = children => /*#__PURE__*/React.createElement("div", {
    style: {
      height: ROW_H,
      position: 'relative',
      borderBottom: `1px solid ${DK.line}`,
      background: DK.bg0
    }
  }, children);
  const ticks = [];
  for (let t = 0; t <= total; t += 5) ticks.push(t);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flexShrink: 0,
      display: 'flex',
      flexDirection: 'column',
      background: DK.bg0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: 150,
      flexShrink: 0,
      borderRight: `1px solid ${DK.line2}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '0 10px',
      height: 24,
      boxSizing: 'border-box',
      display: 'flex',
      alignItems: 'center',
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.16em',
      color: DK.txt3,
      textTransform: 'uppercase'
    }
  }, "Video"), header('V1.4'), header('V1.3'), header('V1.2', true), header('V1.1'), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '0 10px',
      height: 24,
      boxSizing: 'border-box',
      display: 'flex',
      alignItems: 'center',
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.16em',
      color: DK.txt3,
      textTransform: 'uppercase',
      borderTop: `1px solid ${DK.line}`
    }
  }, "Audio"), header('A1.L'), header('A1.R')), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      position: 'relative',
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: 24
    }
  }), lane(/*#__PURE__*/React.createElement(Clip, {
    left: 88,
    width: 12,
    color: DK.clipGray,
    label: "TENET \u2014 END LOGO",
    sub: "0"
  })), lane(/*#__PURE__*/React.createElement(Clip, {
    left: 64,
    width: 24,
    color: DK.clipGray,
    label: "STOP GUESSING \u2014 END LINE",
    sub: "0"
  })), lane(sceneClips.map((c, i) => /*#__PURE__*/React.createElement(Clip, {
    key: i,
    left: c.left,
    width: c.width,
    color: DK.clipBlue,
    label: c.name,
    sub: c.n
  }))), lane(/*#__PURE__*/React.createElement(Clip, {
    left: 0,
    width: 100,
    color: DK.clip,
    label: "01_brief-history_16x9.mov",
    sub: "MOV"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 24,
      borderTop: `1px solid ${DK.line}`
    }
  }), lane(/*#__PURE__*/React.createElement(Waveform, null)), lane(/*#__PURE__*/React.createElement(Waveform, null)), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 0,
      bottom: 0,
      left: '34%',
      width: 1,
      background: DK.amber,
      pointerEvents: 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: -1,
      left: -4,
      width: 9,
      height: 8,
      background: DK.amber,
      clipPath: 'polygon(0 0,100% 0,50% 100%)'
    }
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      borderTop: `1px solid ${DK.line2}`,
      background: DK.bg1,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: 150,
      flexShrink: 0,
      borderRight: `1px solid ${DK.line2}`
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      position: 'relative',
      height: 26
    }
  }, ticks.map(t => /*#__PURE__*/React.createElement("span", {
    key: t,
    style: {
      position: 'absolute',
      left: t / total * 100 + '%',
      top: 6,
      fontFamily: FK.MONO,
      fontSize: 9,
      color: DK.txt3,
      transform: 'translateX(2px)'
    }
  }, `01:00:${String(t).padStart(2, '0')}:00`)))));
}

// ── Beats: emotional throughline ────────────────────────────────────
function smoothPath(pts) {
  if (pts.length < 2) return '';
  let d = `M ${pts[0].x} ${pts[0].y}`;
  for (let i = 0; i < pts.length - 1; i++) {
    const p0 = pts[i - 1] || pts[i];
    const p1 = pts[i];
    const p2 = pts[i + 1];
    const p3 = pts[i + 2] || p2;
    const c1x = p1.x + (p2.x - p0.x) / 6,
      c1y = p1.y + (p2.y - p0.y) / 6;
    const c2x = p2.x - (p3.x - p1.x) / 6,
      c2y = p2.y - (p3.y - p1.y) / 6;
    d += ` C ${c1x.toFixed(1)} ${c1y.toFixed(1)}, ${c2x.toFixed(1)} ${c2y.toFixed(1)}, ${p2.x.toFixed(1)} ${p2.y.toFixed(1)}`;
  }
  return d;
}

// Overlay form — a transparent curve drawn ON TOP of the (dimmed) timeline,
// so the editor can read the emotional shape against the actual cut.
function BeatsOverlay({
  state
}) {
  const beats = [{
    key: 'open',
    t: 0.22
  }, {
    key: 'setup',
    t: 0.42,
    mk: DK.amber
  }, {
    key: 'turn',
    t: 0.24,
    mk: DK.read
  }, {
    key: 'build',
    t: 0.52
  }, {
    key: 'peak',
    t: 0.92,
    mk: DK.pink,
    hot: true
  }, {
    key: 'land',
    t: 0.50
  }];
  // 0..100 coordinate space, stretched to fill via preserveAspectRatio=none.
  // Markers are HTML (percent-positioned) so they stay perfectly circular.
  const padXl = 3,
    padXr = 3,
    padTop = 14,
    padBot = 14;
  const X = i => padXl + i / (beats.length - 1) * (100 - padXl - padXr);
  const Y = t => padTop + (1 - t) * (100 - padTop - padBot);
  const pts = beats.map((b, i) => ({
    x: X(i),
    y: Y(b.t)
  }));
  const line = smoothPath(pts);
  const area = line + ` L ${pts[pts.length - 1].x.toFixed(1)} 100 L ${pts[0].x.toFixed(1)} 100 Z`;
  const uid = 'beats';
  const sh = DK === DK_LIGHT ? '0 1px 2px rgba(255,255,255,0.6)' : '0 1px 4px rgba(0,0,0,0.7)';
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      display: 'flex',
      flexDirection: 'column',
      pointerEvents: 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      padding: '10px 18px 0'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 14,
      color: DK.txt,
      textShadow: sh
    }
  }, "Tension and release across the story"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 13,
      color: DK.txt2,
      letterSpacing: '0.04em',
      textShadow: sh,
      whiteSpace: 'nowrap',
      flexShrink: 0,
      marginLeft: 16
    }
  }, ":", state.mode, " \xB7 6 beats")), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      position: 'relative',
      minHeight: 0,
      margin: '8px 18px 0'
    }
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: "0 0 100 100",
    width: "100%",
    height: "100%",
    preserveAspectRatio: "none",
    style: {
      display: 'block',
      overflow: 'visible'
    }
  }, /*#__PURE__*/React.createElement("defs", null, /*#__PURE__*/React.createElement("linearGradient", {
    id: `${uid}-stroke`,
    x1: "0",
    y1: "0",
    x2: "1",
    y2: "0"
  }, /*#__PURE__*/React.createElement("stop", {
    offset: "0",
    stopColor: DK.read
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "0.42",
    stopColor: DK.amber
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "0.78",
    stopColor: DK.pink
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "1",
    stopColor: DK.amber
  })), /*#__PURE__*/React.createElement("linearGradient", {
    id: `${uid}-fill`,
    x1: "0",
    y1: "0",
    x2: "0",
    y2: "1"
  }, /*#__PURE__*/React.createElement("stop", {
    offset: "0",
    stopColor: DK.amber,
    stopOpacity: DK === DK_LIGHT ? 0.16 : 0.22
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "1",
    stopColor: DK.amber,
    stopOpacity: "0"
  }))), /*#__PURE__*/React.createElement("path", {
    d: area,
    fill: `url(#${uid}-fill)`
  }), /*#__PURE__*/React.createElement("path", {
    d: line,
    fill: "none",
    stroke: `url(#${uid}-stroke)`,
    strokeWidth: "3",
    strokeLinecap: "round",
    vectorEffect: "non-scaling-stroke"
  })), beats.map((b, i) => b.mk && /*#__PURE__*/React.createElement("div", {
    key: b.key,
    style: {
      position: 'absolute',
      left: pts[i].x + '%',
      top: pts[i].y + '%',
      transform: 'translate(-50%,-50%)',
      width: 15,
      height: 15,
      borderRadius: '50%',
      background: DK.bg0,
      border: `2.5px solid ${b.mk}`,
      boxSizing: 'border-box',
      boxShadow: DK === DK_LIGHT ? 'none' : '0 1px 6px rgba(0,0,0,0.6)'
    }
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: `repeat(${beats.length}, 1fr)`,
      padding: '6px 18px 8px'
    }
  }, beats.map(b => /*#__PURE__*/React.createElement("span", {
    key: b.key,
    style: {
      textAlign: 'center',
      fontFamily: FK.MONO,
      fontSize: 12,
      letterSpacing: '0.08em',
      color: b.hot ? DK.amber : DK.txt2,
      fontWeight: b.hot ? 700 : 400,
      textShadow: sh
    }
  }, b.key))));
}

// ── The edit bay (Creative-lane landing) ────────────────────────────
// The director's working surface: scaffold + monitor + live script + timeline.
// No top chrome — CreativeLane owns the unified bar + nav. `theme` drives the
// shared DK palette across the whole lane (bay + portal views).
function EditBay({
  state,
  cat,
  subIdx,
  flashId,
  theme,
  onCat,
  onSub,
  onMode,
  messageRail
}) {
  DK = theme === 'light' ? DK_LIGHT : DK_DARK;
  const [bottomView, setBottomView] = React.useState('timeline');
  const [refOpen, setRefOpen] = React.useState(false);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex',
      flexDirection: 'column',
      background: DK.bg0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '1 1 auto',
      display: 'flex',
      minHeight: 0
    }
  }, messageRail && /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 340px',
      minWidth: 0,
      minHeight: 0,
      borderRight: `1px solid ${DK.line}`,
      background: DK.bg1,
      display: 'flex',
      flexDirection: 'column'
    }
  }, messageRail), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 ' + (refOpen ? 360 : 50) + 'px',
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement(EditScaffold, {
    cat: cat,
    subIdx: subIdx,
    onCat: onCat,
    onSub: onSub,
    open: refOpen,
    onToggle: () => setRefOpen(o => !o)
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement(ProgramMonitor, {
    state: state
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 450px',
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement(EditScript, {
    state: state,
    flashId: flashId
  }))), /*#__PURE__*/React.createElement(TransportBar, {
    state: state,
    onMode: onMode,
    view: bottomView,
    onView: setBottomView
  }), bottomView === 'beats' ? /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      opacity: 0.25
    }
  }, /*#__PURE__*/React.createElement(EditTimeline, {
    state: state
  })), /*#__PURE__*/React.createElement(BeatsOverlay, {
    state: state
  })) : /*#__PURE__*/React.createElement(EditTimeline, {
    state: state
  }));
}
Object.assign(window, {
  EditBay,
  DK_DARK,
  DK_LIGHT
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/director-edit.jsx", error: String((e && e.message) || e) }); }

// flow/framework.js
try { (() => {
/* ============================================================
   TENET — Commercial Development Framework.
   Each seat tracks a TOP 10; each variable holds six subs.
   Producer's ten carry dollars; director's ten are creative.
   ============================================================ */
window.FRAMEWORK = function () {
  // Producer day-base costs sum to $450,000. The night scene adds a
  // $50,000 overtime premium under Crew — cleared when it flips to day.
  const CREW_NIGHT_PREMIUM = 50000;
  const PRODUCER = [{
    id: 'scope',
    n: 1,
    name: 'Scope',
    cost: 35000,
    subs: ['Scope of work', 'Campaign size', 'Number of spots', 'Required assets', 'Client expectations', 'Change orders']
  }, {
    id: 'deliv',
    n: 2,
    name: 'Deliverables',
    cost: 40000,
    subs: ['Broadcast masters', 'Social versions', 'Aspect ratios', 'Market versions', 'Language versions', 'Cutdowns']
  }, {
    id: 'sched',
    n: 3,
    name: 'Schedule',
    cost: 30000,
    subs: ['Pre-production timeline', 'Shoot days', 'Post schedule', 'Delivery deadlines', 'Approval timelines', 'Critical path items']
  }, {
    id: 'talent',
    n: 4,
    name: 'Talent',
    cost: 70000,
    subs: ['Casting', 'Talent fees', 'Usage rights', 'Availability', 'Union requirements', 'Renewals']
  }, {
    id: 'crew',
    n: 5,
    name: 'Crew',
    cost: 60000,
    nightVar: true,
    subs: ['Crew size', 'Department heads', 'Overtime', 'Travel', 'Specialty crew', 'Labor costs']
  }, {
    id: 'locs',
    n: 6,
    name: 'Locations',
    cost: 55000,
    subs: ['Scouting', 'Permits', 'Parking', 'Weather', 'Site restrictions', 'Logistics']
  }, {
    id: 'design',
    n: 7,
    name: 'Production Design',
    cost: 45000,
    subs: ['Sets', 'Props', 'Wardrobe', 'Hair', 'Makeup', 'Vehicles']
  }, {
    id: 'post',
    n: 8,
    name: 'Post Production',
    cost: 80000,
    subs: ['Editorial', 'Color', 'Sound', 'Music', 'VFX', 'Localization']
  }, {
    id: 'rights',
    n: 9,
    name: 'Rights & Legal',
    cost: 20000,
    subs: ['Talent rights', 'Music rights', 'Territory', 'Term length', 'Exclusivity', 'Clearances']
  }, {
    id: 'risk',
    n: 10,
    name: 'Risk & Contingency',
    cost: 15000,
    subs: ['Weather risk', 'Schedule risk', 'Budget risk', 'Talent risk', 'Vendor risk', 'Delivery risk']
  }];
  const DIRECTOR = [{
    id: 'story',
    n: 1,
    name: 'Story',
    subs: ['Narrative clarity', 'Structure', 'Character motivation', 'Beginning', 'Middle', 'End']
  }, {
    id: 'concept',
    n: 2,
    name: 'Concept',
    subs: ['Core idea', 'Originality', 'Simplicity', 'Memorability', 'Brand fit', 'Strategic intent']
  }, {
    id: 'perf',
    n: 3,
    name: 'Performance',
    subs: ['Acting', 'Authenticity', 'Behavior', 'Timing', 'Improvisation', 'Chemistry']
  }, {
    id: 'visual',
    n: 4,
    name: 'Visual Language',
    subs: ['Style', 'Imagery', 'Symbolism', 'References', 'Visual motifs', 'World building']
  }, {
    id: 'cast',
    n: 5,
    name: 'Casting',
    subs: ['Lead talent', 'Supporting talent', 'Character fit', 'Diversity', 'Presence', 'Credibility']
  }, {
    id: 'pdesign',
    n: 6,
    name: 'Production Design',
    subs: ['Sets', 'Locations', 'Props', 'Wardrobe', 'Color palette', 'Environment']
  }, {
    id: 'camera',
    n: 7,
    name: 'Camera & Composition',
    subs: ['Framing', 'Lens choice', 'Camera movement', 'Blocking', 'Scale', 'Perspective']
  }, {
    id: 'edit',
    n: 8,
    name: 'Editorial Rhythm',
    subs: ['Pacing', 'Shot selection', 'Transitions', 'Momentum', 'Timing', 'Flow']
  }, {
    id: 'impact',
    n: 9,
    name: 'Emotional Impact',
    subs: ['Audience reaction', 'Empathy', 'Surprise', 'Tension', 'Delight', 'Recall']
  }];
  function nightActive(state) {
    return window.FLOW.activeScenes(state).some(s => window.FLOW.isNight(s, state));
  }
  function categoryCost(cat, state) {
    return cat.cost + (cat.nightVar && nightActive(state) ? CREW_NIGHT_PREMIUM : 0);
  }
  function producerTotal(state) {
    return PRODUCER.reduce((a, c) => a + categoryCost(c, state), 0);
  }
  function top10(lane) {
    return lane === 'producer' ? PRODUCER : DIRECTOR;
  }

  // ══ Producer financial model ════════════════════════════════════
  // Built for cause-and-effect, not bookkeeping. Each cost center holds
  // Budget / Actual / Forecast and the WHY drivers behind the number.
  // A night exterior is the live variable: it loads crew overtime, which
  // draws against contingency — clearing it ripples through every figure.
  const CONTINGENCY_RESERVE = 50000; // held inside the approved budget
  const FIN_NIGHT_OT = 18000; // crew OT premium when night is live

  // center.base = forecast at day rates. budget = approved line. actual = committed-to-date.
  const FIN_CENTERS = [{
    id: 'talent',
    name: 'Talent',
    budget: 72000,
    actual: 58000,
    base: 74000,
    why: 'Two principals carrying national usage drive most of talent.',
    drivers: [{
      label: 'Principal cast (×2)',
      amt: 38000
    }, {
      label: 'Usage & renewals',
      amt: 16000
    }, {
      label: 'Supporting cast',
      amt: 12000
    }, {
      label: 'Wardrobe + hair/makeup',
      amt: 8000
    }]
  }, {
    id: 'crew',
    name: 'Crew',
    budget: 95000,
    actual: 74000,
    base: 95000,
    nightVar: true,
    why: 'A night exterior pushes the whole crew into overtime.',
    drivers: [{
      label: 'Core crew — day rates',
      amt: 62000
    }, {
      label: 'Department heads',
      amt: 18000
    }, {
      label: 'Night overtime premium',
      amt: FIN_NIGHT_OT,
      night: true
    }, {
      label: 'Travel + kit fees',
      amt: 15000
    }]
  }, {
    id: 'locs',
    name: 'Locations',
    budget: 50000,
    actual: 47000,
    base: 55000,
    why: 'Downtown, at night, with picture cars — that is the $55k.',
    drivers: [{
      label: 'Downtown street closure',
      amt: 18000
    }, {
      label: 'Police lockup',
      amt: 15000
    }, {
      label: 'Picture-vehicle staging',
      amt: 12000
    }, {
      label: 'Parking + company moves',
      amt: 10000
    }]
  }, {
    id: 'equip',
    name: 'Equipment',
    budget: 62000,
    actual: 51000,
    base: 64000,
    why: 'The technocrane and picture vehicles carry the package.',
    drivers: [{
      label: 'Camera package + lenses',
      amt: 24000
    }, {
      label: 'Technocrane',
      amt: 16000
    }, {
      label: 'Lighting + grip',
      amt: 14000
    }, {
      label: 'Picture vehicles',
      amt: 10000
    }]
  }, {
    id: 'design',
    name: 'Production Design',
    budget: 46000,
    actual: 40000,
    base: 45000,
    why: 'A single build, lightly dressed — running efficient.',
    drivers: [{
      label: 'Set construction',
      amt: 18000
    }, {
      label: 'Set dressing + props',
      amt: 14000
    }, {
      label: 'Fabrication',
      amt: 8000
    }, {
      label: 'Strike',
      amt: 5000
    }]
  }, {
    id: 'post',
    name: 'Post Production',
    budget: 90000,
    actual: 32000,
    base: 88000,
    why: 'VFX cleanup is the heaviest line in post.',
    drivers: [{
      label: 'VFX cleanup',
      amt: 28000
    }, {
      label: 'Editorial',
      amt: 24000
    }, {
      label: 'Color + online',
      amt: 18000
    }, {
      label: 'Sound + music',
      amt: 18000
    }]
  }, {
    id: 'legal',
    name: 'Legal',
    budget: 35000,
    actual: 28000,
    base: 33000,
    why: 'Usage and music licensing dominate legal.',
    drivers: [{
      label: 'Talent usage rights',
      amt: 14000
    }, {
      label: 'Music licensing',
      amt: 11000
    }, {
      label: 'Clearances',
      amt: 8000
    }]
  }];
  function finForecast(c, state) {
    return c.base + (c.nightVar && nightActive(state) ? FIN_NIGHT_OT : 0);
  }
  function finVariance(c, state) {
    return finForecast(c, state) - c.budget;
  }
  function finDrivers(c, state) {
    const n = nightActive(state);
    return c.drivers.filter(d => !d.night || n);
  }
  function finTotals(state) {
    const directBudget = FIN_CENTERS.reduce((a, c) => a + c.budget, 0); // 450,000
    const actual = FIN_CENTERS.reduce((a, c) => a + c.actual, 0);
    const forecast = FIN_CENTERS.reduce((a, c) => a + finForecast(c, state), 0);
    const overrun = Math.max(0, forecast - directBudget);
    const approved = directBudget + CONTINGENCY_RESERVE; // 500,000
    return {
      approved,
      actual,
      forecast,
      variance: forecast - approved,
      reserve: CONTINGENCY_RESERVE,
      drawn: overrun,
      contingency: CONTINGENCY_RESERVE - overrun,
      directBudget
    };
  }
  // What is eating the contingency reserve (cause-and-effect).
  function finContingencyDrivers(state) {
    const n = nightActive(state);
    const d = [{
      label: 'Location overage',
      amt: 4000
    }];
    if (n) d.unshift({
      label: 'Night overtime — crew',
      amt: FIN_NIGHT_OT
    });
    return d;
  }

  // ══ Bid build-up (AICP bid model) ══════════════════════════════
  // The producer's deeper view: direct costs roll up by section, then the
  // deal-term levers (markup, production fee, insurance) load on top.
  // Nothing is stored as a total — every number is recomputed from inputs,
  // so a slid fee ripples straight to the grand total and reconciles to
  // the expected (printed) figure within tolerance.
  const BID = {
    client: window.FLOW.TITLE,
    agency: 'H&W',
    project: '#17433',
    status: 'BALLPARK',
    system: 'AICP v2.6.1',
    director: 'D. Parker',
    expected: 500000,
    markupPct: 15,
    feePct: 12,
    insurancePct: 3.5,
    sections: [{
      code: 'A',
      name: 'Talent Labor',
      lines: [{
        desc: 'O/C Principals — buyout',
        qty: 2,
        days: 1,
        rate: 19000,
        unit: 'FLAT'
      }, {
        desc: 'Supporting cast',
        qty: 4,
        days: 2,
        rate: 1500,
        unit: 'DAY'
      }, {
        desc: 'Usage & renewals — 2yr',
        qty: 1,
        days: 1,
        rate: 10000,
        unit: 'FLAT'
      }, {
        desc: 'Wardrobe + hair/makeup',
        qty: 1,
        days: 1,
        rate: 4000,
        unit: 'FLAT'
      }]
    }, {
      code: 'B',
      name: 'Crew Labor',
      lines: [{
        desc: 'Core crew — day rates',
        qty: 12,
        days: 3,
        rate: 1500,
        unit: 'DAY'
      }, {
        desc: 'Department heads',
        qty: 5,
        days: 3,
        rate: 1000,
        unit: 'DAY'
      }, {
        desc: 'Travel + kit fees',
        qty: 1,
        days: 1,
        rate: 13000,
        unit: 'FLAT'
      }]
    }, {
      code: 'C',
      name: 'Locations',
      lines: [{
        desc: 'Downtown street closure',
        qty: 1,
        days: 1,
        rate: 16000,
        unit: 'FLAT'
      }, {
        desc: 'Police lockup',
        qty: 6,
        days: 1,
        rate: 2000,
        unit: 'HOUR'
      }, {
        desc: 'Picture-vehicle staging',
        qty: 1,
        days: 1,
        rate: 10000,
        unit: 'FLAT'
      }, {
        desc: 'Parking + company moves',
        qty: 1,
        days: 1,
        rate: 8000,
        unit: 'FLAT'
      }]
    }, {
      code: 'D',
      name: 'Equipment',
      lines: [{
        desc: 'Camera package + lenses',
        qty: 1,
        days: 3,
        rate: 7000,
        unit: 'DAY'
      }, {
        desc: 'Technocrane',
        qty: 1,
        days: 2,
        rate: 8000,
        unit: 'DAY'
      }, {
        desc: 'Lighting + grip',
        qty: 1,
        days: 3,
        rate: 4000,
        unit: 'DAY'
      }, {
        desc: 'Picture vehicles',
        qty: 1,
        days: 1,
        rate: 9000,
        unit: 'FLAT'
      }]
    }, {
      code: 'E',
      name: 'Production Design',
      lines: [{
        desc: 'Set construction',
        qty: 1,
        days: 1,
        rate: 16000,
        unit: 'FLAT'
      }, {
        desc: 'Set dressing + props',
        qty: 1,
        days: 1,
        rate: 12000,
        unit: 'FLAT'
      }, {
        desc: 'Fabrication',
        qty: 1,
        days: 1,
        rate: 6000,
        unit: 'FLAT'
      }, {
        desc: 'Strike',
        qty: 1,
        days: 1,
        rate: 4000,
        unit: 'FLAT'
      }]
    }, {
      code: 'F',
      name: 'Post Production',
      lines: [{
        desc: 'VFX cleanup',
        qty: 1,
        days: 1,
        rate: 26000,
        unit: 'FLAT'
      }, {
        desc: 'Editorial',
        qty: 1,
        days: 1,
        rate: 22000,
        unit: 'FLAT'
      }, {
        desc: 'Color + online',
        qty: 1,
        days: 1,
        rate: 14000,
        unit: 'FLAT'
      }, {
        desc: 'Sound + music',
        qty: 1,
        days: 1,
        rate: 10000,
        unit: 'FLAT'
      }]
    }, {
      code: 'G',
      name: 'Travel (CS17)',
      travel: true,
      handlingPct: 10,
      lines: [{
        desc: 'Directors',
        ctype: 'HOTEL',
        qty: 2,
        days: 14,
        rate: 350,
        unit: 'DAY'
      }, {
        desc: 'Talent',
        ctype: 'TRANSPORT',
        qty: 1,
        days: 1,
        rate: 8000,
        unit: 'FLAT'
      }, {
        desc: 'Director of Photography',
        ctype: 'PER DIEM',
        qty: 1,
        days: 14,
        rate: 100,
        unit: 'DAY'
      }, {
        desc: 'Producer',
        ctype: 'FLIGHT',
        qty: 1,
        days: 1,
        rate: 1800,
        unit: 'EACH'
      }]
    }]
  };

  // Scenario levers — the "what if" engine. Each is a direct-cost saving
  // plus its schedule / crew / risk consequence. risk: <0 lowers, >0 raises.
  const BID_SCENARIOS = [{
    id: 'extras',
    label: 'Reduce extras',
    note: '120 \u2192 60 background',
    save: 12000,
    days: 0,
    crew: 0,
    risk: -1
  }, {
    id: 'crane',
    label: 'Remove technocrane',
    note: 'Crane \u2192 dolly + jib',
    save: 16000,
    days: 0,
    crew: -2,
    risk: -1
  }, {
    id: 'stage',
    label: 'Move to stage',
    note: 'Drops the night exterior',
    save: 22000,
    days: -1,
    crew: -8,
    risk: -2
  }, {
    id: 'day',
    label: 'Drop a shoot day',
    note: '3 \u2192 2 shoot days',
    save: 28000,
    days: -1,
    crew: -12,
    risk: 2
  }, {
    id: 'vfx',
    label: 'Convert to VFX plates',
    note: 'Practical \u2192 plate + comp',
    save: 9000,
    days: 0,
    crew: -4,
    risk: 1
  }];
  return {
    PRODUCER,
    DIRECTOR,
    CREW_NIGHT_PREMIUM,
    nightActive,
    categoryCost,
    producerTotal,
    top10,
    FIN_CENTERS,
    CONTINGENCY_RESERVE,
    FIN_NIGHT_OT,
    finForecast,
    finVariance,
    finDrivers,
    finTotals,
    finContingencyDrivers,
    BID,
    BID_SCENARIOS
  };
}();
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/framework.js", error: String((e && e.message) || e) }); }

// flow/panels.jsx
try { (() => {
/* global React, FK, FGlyph, FLOW */
// Shared panels — the script (right) and the shot timeline (bottom).

// ── Script panel ────────────────────────────────────────────────────
const scriptStyles = {
  root: {
    height: '100%',
    display: 'flex',
    flexDirection: 'column',
    background: FK.PAPER,
    minHeight: 0
  },
  head: {
    padding: '22px 40px 14px',
    borderBottom: `1px solid ${FK.BORDER}`,
    flexShrink: 0,
    display: 'flex',
    alignItems: 'baseline',
    justifyContent: 'space-between'
  },
  kicker: {
    fontFamily: FK.MONO,
    fontSize: 10,
    letterSpacing: '0.2em',
    textTransform: 'uppercase',
    color: FK.AMBER
  },
  fname: {
    fontFamily: FK.MONO,
    fontSize: 11,
    color: FK.INK_FAINT
  },
  body: {
    flex: 1,
    overflow: 'auto',
    padding: '26px 40px 40px'
  },
  titleblock: {
    textAlign: 'center',
    marginBottom: 30
  },
  title: {
    fontFamily: FK.SCRIPT,
    fontSize: 24,
    color: FK.INK,
    letterSpacing: '0.02em'
  },
  logline: {
    fontFamily: FK.SCRIPT,
    fontStyle: 'italic',
    fontSize: 14,
    color: FK.INK_FAINT,
    marginTop: 8
  },
  scene: dropped => ({
    marginBottom: 22,
    opacity: dropped ? 0.32 : 1
  }),
  slug: (hot, dropped) => ({
    fontFamily: FK.SCRIPT,
    fontSize: 15,
    fontWeight: 700,
    letterSpacing: '0.04em',
    color: FK.INK,
    textTransform: 'uppercase',
    marginBottom: 8,
    padding: '3px 8px',
    marginLeft: -8,
    borderLeft: hot ? `2px solid ${FK.AMBER}` : '2px solid transparent',
    background: hot ? FK.AMBER_50 : 'transparent',
    textDecorationLine: dropped ? 'line-through' : 'none'
  }),
  para: hot => ({
    fontFamily: FK.SCRIPT,
    fontSize: 15,
    lineHeight: 1.5,
    color: FK.INK,
    padding: '4px 8px',
    marginLeft: -8,
    marginBottom: 4,
    background: hot ? FK.AMBER_50 : 'transparent',
    boxShadow: hot ? `inset 2px 0 0 ${FK.AMBER}` : 'none'
  })
};
function slugFor(scene, state) {
  const loc = FLOW.LOCS.find(l => l.id === scene.locId);
  return `${loc.kind}. ${loc.name.toUpperCase()} \u2014 ${FLOW.todOf(scene, state)}`;
}
function ScriptPanel({
  state,
  sel,
  flashId
}) {
  const S = scriptStyles;
  const active = new Set(FLOW.activeScenes(state).map(s => s.id));
  return /*#__PURE__*/React.createElement("div", {
    style: S.root
  }, /*#__PURE__*/React.createElement("div", {
    style: S.head
  }, /*#__PURE__*/React.createElement("span", {
    style: S.kicker
  }, "Script"), /*#__PURE__*/React.createElement("span", {
    style: S.fname
  }, "MERIDIAN_a-day-held_v3.fdx")), /*#__PURE__*/React.createElement("div", {
    style: S.body
  }, /*#__PURE__*/React.createElement("div", {
    style: S.titleblock
  }, /*#__PURE__*/React.createElement("div", {
    style: S.title
  }, FLOW.TITLE), /*#__PURE__*/React.createElement("div", {
    style: S.logline
  }, FLOW.LOGLINE)), FLOW.SCENES.map(scene => {
    const dropped = !active.has(scene.id);
    const selLoc = sel && sel.type === 'loc' && sel.id === scene.locId;
    const selScene = sel && sel.type === 'scene' && sel.id === scene.id;
    const selChar = sel && sel.type === 'char' && scene.chars.includes(sel.id);
    const flash = flashId === scene.id;
    const slugHot = selLoc || selScene || flash;
    return /*#__PURE__*/React.createElement("div", {
      key: scene.id,
      style: S.scene(dropped)
    }, /*#__PURE__*/React.createElement("div", {
      style: S.slug(slugHot, dropped)
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: FK.INK_FAINT,
        marginRight: 10,
        fontWeight: 400
      }
    }, scene.n), slugFor(scene, state)), scene.action.map((p, i) => {
      const hot = selChar && p.chars.includes(sel.id) || selScene || flash && i === 0;
      return /*#__PURE__*/React.createElement("div", {
        key: i,
        style: S.para(hot)
      }, p.text);
    }));
  })));
}

// ── Timeline panel ──────────────────────────────────────────────────
const tlStyles = {
  root: {
    height: '100%',
    display: 'flex',
    flexDirection: 'column',
    background: FK.PAPER_SOFT,
    borderTop: `1px solid ${FK.BORDER}`,
    padding: '20px 40px 24px',
    boxSizing: 'border-box'
  },
  head: {
    display: 'flex',
    alignItems: 'baseline',
    justifyContent: 'space-between',
    marginBottom: 20
  },
  kicker: {
    fontFamily: FK.MONO,
    fontSize: 10,
    letterSpacing: '0.2em',
    textTransform: 'uppercase',
    color: FK.AMBER,
    whiteSpace: 'nowrap'
  },
  lenWrap: {
    display: 'flex',
    alignItems: 'baseline',
    gap: 10
  },
  lenLabel: {
    fontFamily: FK.MONO,
    fontSize: 10,
    letterSpacing: '0.18em',
    textTransform: 'uppercase',
    color: FK.INK_FAINT
  },
  len: {
    fontFamily: FK.SANS,
    fontWeight: 300,
    fontSize: 40,
    color: FK.INK,
    lineHeight: 1,
    letterSpacing: '-0.01em'
  },
  track: {
    display: 'flex',
    alignItems: 'flex-end',
    gap: 5,
    height: 78
  },
  group: {
    display: 'flex',
    alignItems: 'flex-end',
    gap: 4
  },
  groupGap: {
    width: 16,
    flexShrink: 0
  },
  box: state => ({
    width: 58,
    height: state.tall ? 64 : 52,
    flexShrink: 0,
    border: `1px solid ${state.on ? FK.AMBER : FK.BORDER_STRONG}`,
    background: state.on ? FK.AMBER_50 : FK.PAPER,
    position: 'relative',
    cursor: 'default'
  }),
  dot: {
    position: 'absolute',
    top: 5,
    right: 5,
    width: 6,
    height: 6,
    borderRadius: 999,
    background: FK.AMBER
  },
  ruler: {
    display: 'flex',
    justifyContent: 'space-between',
    marginTop: 14,
    borderTop: `1px solid ${FK.BORDER}`,
    paddingTop: 8
  },
  tick: {
    fontFamily: FK.MONO,
    fontSize: 10,
    color: FK.INK_FAINT
  },
  sceneLabel: hot => ({
    fontFamily: FK.MONO,
    fontSize: 9,
    letterSpacing: '0.08em',
    textTransform: 'uppercase',
    color: hot ? FK.AMBER : FK.INK_FAINT,
    marginTop: 8,
    textAlign: 'center'
  })
};
function ModeToggle({
  mode,
  onMode
}) {
  const opts = [30, 15];
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: `1px solid ${FK.BORDER_STRONG}`,
      borderRadius: 2,
      overflow: 'hidden'
    }
  }, opts.map((m, i) => /*#__PURE__*/React.createElement("button", {
    key: m,
    onClick: () => onMode(m),
    style: {
      padding: '6px 13px',
      fontFamily: FK.MONO,
      fontSize: 12,
      cursor: 'pointer',
      border: 'none',
      borderLeft: i ? `1px solid ${FK.BORDER}` : 'none',
      background: mode === m ? FK.AMBER : FK.PAPER,
      color: mode === m ? FK.PAPER : FK.INK_MUTED
    }
  }, ":", m)));
}
function TimelinePanel({
  state,
  sel,
  flashId,
  onMode
}) {
  const S = tlStyles;
  const scenes = FLOW.activeScenes(state);
  const totalLen = FLOW.totalLength(state);
  const totalShots = scenes.reduce((a, s) => a + s.shots.length, 0);

  // Staggered reveal on mount — timer-driven so it survives paused rAF.
  const [revealed, setRevealed] = React.useState(0);
  React.useEffect(() => {
    let n = 0;
    const id = setInterval(() => {
      n += 1;
      setRevealed(n);
      if (n >= 30) clearInterval(id);
    }, 60);
    // Guarantee every box is shown even where timers are throttled.
    const fb = setTimeout(() => setRevealed(30), 1500);
    return () => {
      clearInterval(id);
      clearTimeout(fb);
    };
  }, []);
  let shotIndex = 0;
  return /*#__PURE__*/React.createElement("div", {
    style: S.root
  }, /*#__PURE__*/React.createElement("div", {
    style: S.head
  }, /*#__PURE__*/React.createElement("span", {
    style: S.kicker
  }, "Timeline \xB7 the cut"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 28
    }
  }, /*#__PURE__*/React.createElement(ModeToggle, {
    mode: state.mode,
    onMode: onMode
  }), /*#__PURE__*/React.createElement("div", {
    style: S.lenWrap
  }, /*#__PURE__*/React.createElement("span", {
    style: S.lenLabel
  }, "Length"), /*#__PURE__*/React.createElement("span", {
    style: S.len
  }, FLOW.fmtClock(totalLen))))), /*#__PURE__*/React.createElement("div", {
    style: S.track
  }, scenes.map((scene, si) => {
    const selLoc = sel && sel.type === 'loc' && sel.id === scene.locId;
    const flash = flashId === scene.id;
    return /*#__PURE__*/React.createElement(React.Fragment, {
      key: scene.id
    }, si > 0 && /*#__PURE__*/React.createElement("div", {
      style: S.groupGap
    }), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      style: S.group
    }, scene.shots.map(sh => {
      const idx = shotIndex++;
      const shown = idx < revealed;
      if (!shown) return /*#__PURE__*/React.createElement("div", {
        key: sh.id,
        style: {
          width: 58,
          height: 52,
          flexShrink: 0
        }
      });
      const selChar = sel && sel.type === 'char' && sh.chars.includes(sel.id);
      const selScene = sel && sel.type === 'scene' && sel.id === scene.id;
      const on = selLoc || selScene || selChar || flash;
      return /*#__PURE__*/React.createElement("div", {
        key: sh.id,
        title: `${sh.desc} · ${sh.dur}s`,
        style: S.box({
          on,
          tall: on
        })
      }, selChar && /*#__PURE__*/React.createElement("span", {
        style: S.dot
      }));
    })), /*#__PURE__*/React.createElement("div", {
      style: S.sceneLabel(sel && sel.type === 'scene' && sel.id === scene.id || selLoc || flash)
    }, FLOW.locName(scene.locId))));
  })), /*#__PURE__*/React.createElement("div", {
    style: S.ruler
  }, /*#__PURE__*/React.createElement("span", {
    style: S.tick
  }, "0:00"), /*#__PURE__*/React.createElement("span", {
    style: S.tick
  }, FLOW.fmtClock(totalLen / 2)), /*#__PURE__*/React.createElement("span", {
    style: S.tick
  }, FLOW.fmtClock(totalLen))));
}
window.ScriptPanel = ScriptPanel;
window.TimelinePanel = TimelinePanel;

// ── Subcategory tab strip — the six subs of the selected Top-10 item ──
const subBar = {
  root: {
    flex: '0 0 auto',
    height: 76,
    borderBottom: `1px solid ${FK.BORDER}`,
    background: FK.PAPER,
    padding: '0 40px',
    display: 'flex',
    alignItems: 'center',
    gap: 22
  },
  cat: {
    display: 'flex',
    alignItems: 'baseline',
    gap: 9,
    flexShrink: 0
  },
  catN: {
    fontFamily: FK.MONO,
    fontSize: 11,
    color: FK.AMBER,
    letterSpacing: '0.08em'
  },
  catName: {
    fontFamily: FK.SERIF,
    fontStyle: 'italic',
    fontSize: 19,
    color: FK.INK,
    whiteSpace: 'nowrap'
  },
  tabs: {
    display: 'flex',
    alignItems: 'stretch',
    flex: 1,
    minWidth: 0,
    height: '100%'
  },
  tab: on => ({
    flex: 1,
    minWidth: 0,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    cursor: 'pointer',
    textAlign: 'center',
    padding: '0 6px',
    fontFamily: FK.MONO,
    fontSize: 10.5,
    letterSpacing: '0.04em',
    color: on ? FK.INK : FK.INK_FAINT,
    borderBottom: `2px solid ${on ? FK.AMBER : 'transparent'}`,
    whiteSpace: 'nowrap',
    overflow: 'hidden',
    textOverflow: 'ellipsis'
  }),
  cyc: {
    display: 'flex',
    alignItems: 'center',
    gap: 10,
    flexShrink: 0
  },
  arrow: {
    width: 28,
    height: 28,
    borderRadius: 999,
    border: `1px solid ${FK.BORDER_STRONG}`,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    cursor: 'pointer',
    color: FK.INK_MUTED
  },
  pos: {
    fontFamily: FK.MONO,
    fontSize: 11,
    color: FK.INK_FAINT,
    width: 32,
    textAlign: 'center'
  }
};
function SubTabBar({
  cat,
  subIdx,
  onSub
}) {
  const S = subBar;
  if (!cat) return /*#__PURE__*/React.createElement("div", {
    style: S.root
  });
  const subs = cat.subs;
  const go = d => onSub((subIdx + d + subs.length) % subs.length);
  return /*#__PURE__*/React.createElement("div", {
    style: S.root
  }, /*#__PURE__*/React.createElement("div", {
    style: S.cat
  }, /*#__PURE__*/React.createElement("span", {
    style: S.catN
  }, String(cat.n).padStart(2, '0')), /*#__PURE__*/React.createElement("span", {
    style: S.catName
  }, cat.name)), /*#__PURE__*/React.createElement("div", {
    style: S.tabs
  }, subs.map((s, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    title: s,
    style: S.tab(i === subIdx),
    onClick: () => onSub(i)
  }, s))), /*#__PURE__*/React.createElement("div", {
    style: S.cyc
  }, /*#__PURE__*/React.createElement("div", {
    style: S.arrow,
    onClick: () => go(-1)
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 14,
    style: {
      transform: 'scaleX(-1)'
    }
  })), /*#__PURE__*/React.createElement("span", {
    style: S.pos
  }, subIdx + 1, " / ", subs.length), /*#__PURE__*/React.createElement("div", {
    style: S.arrow,
    onClick: () => go(1)
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 14
  }))));
}
window.SubTabBar = SubTabBar;
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/panels.jsx", error: String((e && e.message) || e) }); }

// flow/producer-dash.jsx
try { (() => {
/* global React, FK, FLOW, FRAMEWORK, FGlyph, useFTween */
// Producer page — a lighter windowed finance dashboard:
//   left   = small timeline + message-director chat
//   center = running actuals (budget · actual · variance + ledger)
//   right  = finance breakdown (proportions + subcategory allocations)

const LT = {
  canvas: '#ECE8E1',
  win: '#FFFFFF',
  line: 'rgba(0,0,0,0.10)',
  line2: 'rgba(0,0,0,0.16)'
};
const FIN_GOOD = '#1F8A5B'; // under-budget green
function finSigned(n) {
  return (n < 0 ? '\u2212' : '+') + FLOW.fmtMoney(Math.abs(n));
}
function Win({
  title,
  right,
  children,
  flex,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: Object.assign({
      background: LT.win,
      border: `1px solid ${LT.line}`,
      borderRadius: 4,
      display: 'flex',
      flexDirection: 'column',
      minHeight: 0,
      overflow: 'hidden'
    }, style)
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '11px 16px',
      borderBottom: `1px solid ${LT.line}`,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: FK.AMBER,
      whiteSpace: 'nowrap'
    }
  }, title), right), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      overflow: 'auto'
    }
  }, children));
}

// ── small timeline ──────────────────────────────────────────────────
function TimelineMini({
  state,
  flashId
}) {
  const scenes = FLOW.activeScenes(state);
  const total = FLOW.totalLength(state);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 16px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      marginBottom: 14
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT
    }
  }, "The cut"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: 26,
      color: FK.INK
    }
  }, FLOW.fmtClock(total))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 4,
      height: 54
    }
  }, scenes.map(s => {
    const dur = s.shots.reduce((a, sh) => a + sh.dur, 0);
    const night = FLOW.isNight(s, state);
    const hot = flashId === s.id;
    return /*#__PURE__*/React.createElement("div", {
      key: s.id,
      style: {
        flex: dur,
        minWidth: 0,
        border: `1px solid ${hot ? FK.AMBER : LT.line2}`,
        background: hot ? FK.AMBER_50 : night ? '#EFE4D2' : FK.PAPER_SOFT,
        borderRadius: 2,
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'space-between',
        padding: '5px 6px',
        overflow: 'hidden'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 8.5,
        color: FK.INK_MUTED,
        whiteSpace: 'nowrap',
        overflow: 'hidden',
        textOverflow: 'ellipsis'
      }
    }, FLOW.locName(s.locId)), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 8,
        letterSpacing: '0.06em',
        color: night ? FK.AMBER_800 : FK.INK_FAINT
      }
    }, FLOW.todOf(s, state)));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      marginTop: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      color: FK.INK_FAINT
    }
  }, "0:00"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      color: FK.INK_FAINT
    }
  }, FLOW.fmtClock(total))));
}

// ── chat to director ────────────────────────────────────────────────
const PMSG = 'Schedule won\u2019t let us shoot this night scene. Can it be changed?';
function ChatDirector({
  chat,
  onSend
}) {
  const bub = mine => ({
    alignSelf: mine ? 'flex-end' : 'flex-start',
    maxWidth: '88%',
    background: mine ? FK.INK : FK.PAPER,
    color: mine ? FK.PAPER : FK.INK,
    border: mine ? 'none' : `1px solid ${LT.line}`,
    borderRadius: 8,
    padding: '8px 11px',
    fontFamily: FK.SANS,
    fontSize: 12.5,
    lineHeight: 1.4,
    marginBottom: 7
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '14px 16px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, chat.messages.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: bub(m.from === 'producer')
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: m.from === 'producer' ? 'rgba(255,255,255,0.6)' : FK.INK_FAINT,
      marginBottom: 3
    }
  }, m.from === 'producer' ? 'You · Producer' : 'D. Parker · Director'), m.text)), chat.typing && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 12.5,
      color: FK.INK_FAINT,
      marginBottom: 7
    }
  }, "Director is editing the script\\u2026")), !chat.sent && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 9
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 12.5,
      color: FK.INK,
      lineHeight: 1.4,
      padding: '9px 11px',
      background: FK.PAPER_SOFT,
      border: `1px solid ${LT.line2}`,
      borderRadius: 8
    }
  }, PMSG), /*#__PURE__*/React.createElement("button", {
    onClick: onSend,
    style: {
      alignSelf: 'flex-end',
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      cursor: 'pointer',
      background: FK.AMBER,
      color: FK.PAPER,
      border: 'none',
      borderRadius: 2,
      padding: '8px 15px',
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.12em',
      textTransform: 'uppercase'
    }
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "send",
    size: 13
  }), " Send")));
}

// ── running actuals ─────────────────────────────────────────────────
function Figure({
  label,
  value,
  accent
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.13em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      marginBottom: 7,
      whiteSpace: 'nowrap'
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: 29,
      lineHeight: 1,
      letterSpacing: '-0.02em',
      color: accent || FK.INK,
      whiteSpace: 'nowrap'
    }
  }, value));
}
const FIN_COLS = 'minmax(150px, 1.4fr) 82px 82px 88px 94px';
function RunningActuals({
  state,
  cat,
  onCat,
  catFlash
}) {
  const t = FRAMEWORK.finTotals(state);
  const forecast = useFTween(t.forecast, 700);
  const variance = useFTween(t.variance, 700);
  const conting = useFTween(t.contingency, 700);
  const v = Math.round(variance);
  const cR = Math.round(conting);
  const lowC = cR < t.reserve * 0.45;
  const drawnPct = (t.reserve - cR) / t.reserve * 100;
  const metaR = {
    fontFamily: FK.MONO,
    fontSize: 9,
    letterSpacing: '0.1em',
    textTransform: 'uppercase',
    color: FK.INK_FAINT,
    textAlign: 'right'
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '20px 24px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 18,
      paddingBottom: 20,
      borderBottom: `1px solid ${LT.line}`
    }
  }, /*#__PURE__*/React.createElement(Figure, {
    label: "Approved",
    value: FLOW.fmtMoney(t.approved)
  }), /*#__PURE__*/React.createElement(Figure, {
    label: "Running actual",
    value: FLOW.fmtMoney(t.actual)
  }), /*#__PURE__*/React.createElement(Figure, {
    label: "Forecast final",
    value: FLOW.fmtMoney(Math.round(forecast))
  }), /*#__PURE__*/React.createElement(Figure, {
    label: "Variance",
    value: v === 0 ? '$0' : finSigned(v),
    accent: v > 0 ? FK.AMBER_800 : FIN_GOOD
  }), /*#__PURE__*/React.createElement(Figure, {
    label: "Contingency left",
    value: FLOW.fmtMoney(cR),
    accent: lowC ? FK.AMBER_800 : undefined
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: FIN_COLS,
      gap: 8,
      padding: '0 8px',
      margin: '18px 0 2px'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT
    }
  }, "Cost center"), /*#__PURE__*/React.createElement("span", {
    style: metaR
  }, "Budget"), /*#__PURE__*/React.createElement("span", {
    style: metaR
  }, "Actual"), /*#__PURE__*/React.createElement("span", {
    style: metaR
  }, "Forecast"), /*#__PURE__*/React.createElement("span", {
    style: metaR
  }, "Variance")), FRAMEWORK.FIN_CENTERS.map(c => {
    const on = cat === c.id;
    const fc = FRAMEWORK.finForecast(c, state);
    const vv = fc - c.budget;
    const ot = c.nightVar && FRAMEWORK.nightActive(state);
    const num = (val, col) => /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 12,
        color: col || FK.INK,
        textAlign: 'right'
      }
    }, val);
    return /*#__PURE__*/React.createElement("div", {
      key: c.id,
      onClick: () => onCat(c.id),
      style: {
        display: 'grid',
        gridTemplateColumns: FIN_COLS,
        gap: 8,
        alignItems: 'center',
        padding: '9px 8px',
        cursor: 'pointer',
        borderBottom: `1px solid ${LT.line}`,
        background: on || catFlash === c.id ? FK.AMBER_50 : 'transparent',
        boxShadow: on ? `inset 3px 0 0 ${FK.AMBER}` : 'none'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'flex',
        alignItems: 'baseline',
        gap: 8,
        minWidth: 0
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.SERIF,
        fontStyle: 'italic',
        fontSize: 15.5,
        color: on ? FK.AMBER_800 : FK.INK,
        whiteSpace: 'nowrap'
      }
    }, c.name), ot && /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 8.5,
        letterSpacing: '0.08em',
        textTransform: 'uppercase',
        color: FK.AMBER_800,
        padding: '1px 5px',
        borderRadius: 2,
        background: FK.AMBER_50,
        border: `1px solid ${FK.AMBER}`,
        flexShrink: 0
      }
    }, "+OT")), num(FLOW.fmtMoney(c.budget)), num(FLOW.fmtMoney(c.actual), FK.INK_MUTED), num(FLOW.fmtMoney(fc)), num(vv === 0 ? '\u2014' : finSigned(vv), vv > 0 ? FK.AMBER_800 : vv < 0 ? FIN_GOOD : FK.INK_FAINT));
  }), /*#__PURE__*/React.createElement("div", {
    onClick: () => onCat('contingency'),
    style: {
      marginTop: 16,
      padding: '12px 10px',
      cursor: 'pointer',
      borderRadius: 3,
      background: cat === 'contingency' ? FK.AMBER_50 : 'transparent',
      boxShadow: cat === 'contingency' ? `inset 3px 0 0 ${FK.AMBER}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: lowC ? FK.AMBER_800 : FK.INK_MUTED,
      whiteSpace: 'nowrap'
    }
  }, "Contingency reserve"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 11,
      color: FK.INK_MUTED,
      whiteSpace: 'nowrap'
    }
  }, FLOW.fmtMoney(cR), " ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: FK.INK_FAINT
    }
  }, "of ", FLOW.fmtMoney(t.reserve), " left"))), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 6,
      background: FK.PAPER_SOFT,
      borderRadius: 999,
      overflow: 'hidden',
      marginTop: 9
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      width: drawnPct + '%',
      background: lowC ? FK.AMBER_800 : FK.AMBER
    }
  }))));
}

// ── finance breakdown — cause & effect (WHAT is costing money → WHY) ──
function FinanceBreakdown({
  state,
  cat,
  onCat
}) {
  const t = FRAMEWORK.finTotals(state);
  const centers = FRAMEWORK.FIN_CENTERS;
  const isCont = cat === 'contingency';
  const sel = centers.find(c => c.id === cat) || (isCont ? null : centers[0]);

  // selected payload
  let title, amountLine, headline, drivers, chip;
  if (isCont) {
    title = 'Contingency';
    drivers = FRAMEWORK.finContingencyDrivers(state).slice().sort((a, b) => b.amt - a.amt);
    amountLine = `${FLOW.fmtMoney(t.contingency)} of ${FLOW.fmtMoney(t.reserve)} left`;
    headline = FRAMEWORK.nightActive(state) ? 'Night overtime is the biggest draw against the reserve.' : 'The reserve is largely intact — the pressure is easing.';
    chip = {
      text: FLOW.fmtMoney(t.reserve - t.contingency) + ' drawn',
      over: t.reserve - t.contingency > t.reserve * 0.5
    };
  } else {
    title = sel.name;
    const fc = FRAMEWORK.finForecast(sel, state);
    const vv = FRAMEWORK.finVariance(sel, state);
    drivers = FRAMEWORK.finDrivers(sel, state).slice().sort((a, b) => b.amt - a.amt);
    amountLine = FLOW.fmtMoney(fc) + ' forecast';
    headline = sel.why;
    chip = vv === 0 ? null : {
      text: finSigned(vv) + (vv > 0 ? ' over' : ' under'),
      over: vv > 0
    };
  }
  const maxAmt = Math.max(...drivers.map(d => d.amt), 1);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '20px 22px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      marginBottom: 10
    }
  }, "What is costing money"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      height: 26,
      borderRadius: 2,
      overflow: 'hidden',
      border: `1px solid ${LT.line}`
    }
  }, centers.map((c, i) => {
    const fc = FRAMEWORK.finForecast(c, state);
    const on = c.id === cat;
    return /*#__PURE__*/React.createElement("div", {
      key: c.id,
      title: `${c.name} · ${FLOW.fmtMoney(fc)}`,
      onClick: () => onCat(c.id),
      style: {
        width: fc / t.forecast * 100 + '%',
        background: on ? FK.AMBER : i % 2 ? FK.INK : '#3a3a3a',
        borderRight: `1px solid ${LT.win}`,
        cursor: 'pointer'
      }
    });
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      gap: 10,
      margin: '24px 0 4px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 11,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 23,
      color: FK.INK,
      whiteSpace: 'nowrap'
    }
  }, title), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 12,
      color: FK.INK_MUTED,
      whiteSpace: 'nowrap'
    }
  }, amountLine)), chip && /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.04em',
      color: chip.over ? FK.AMBER_800 : FIN_GOOD,
      padding: '3px 8px',
      borderRadius: 2,
      background: chip.over ? FK.AMBER_50 : 'rgba(31,138,91,0.10)',
      border: `1px solid ${chip.over ? FK.AMBER : 'rgba(31,138,91,0.4)'}`,
      whiteSpace: 'nowrap',
      flexShrink: 0
    }
  }, chip.text)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      gap: 10,
      margin: '14px 0 20px',
      padding: '13px 15px',
      background: FK.PAPER_SOFT,
      borderLeft: `2px solid ${FK.AMBER}`,
      borderRadius: '0 4px 4px 0'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      color: FK.AMBER,
      marginTop: 3,
      flexShrink: 0
    }
  }, "WHY"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 17,
      lineHeight: 1.35,
      color: FK.INK,
      textWrap: 'pretty'
    }
  }, headline)), drivers.map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      marginBottom: 13
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      gap: 12,
      marginBottom: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 14,
      color: FK.INK
    }
  }, d.label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 13,
      color: i === 0 ? FK.AMBER_800 : FK.INK_MUTED,
      whiteSpace: 'nowrap'
    }
  }, FLOW.fmtMoney(d.amt))), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 5,
      background: FK.PAPER_SOFT,
      borderRadius: 999,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      width: d.amt / maxAmt * 100 + '%',
      background: i === 0 ? FK.AMBER : FK.INK_FAINT
    }
  })))));
}

// ── the producer page ───────────────────────────────────────────────
// ── bid build-up (Bidding tab) ─────────────────────────────────────
const BID_COLS = '1fr 34px 40px 70px 78px';
function BidLever({
  label,
  sub,
  pct,
  amount,
  onStep
}) {
  const btn = {
    width: 21,
    height: 21,
    borderRadius: 3,
    border: `1px solid ${LT.line2}`,
    background: FK.PAPER_SOFT,
    cursor: 'pointer',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    fontFamily: FK.MONO,
    fontSize: 14,
    color: FK.INK_MUTED,
    lineHeight: 1,
    userSelect: 'none'
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '10px 0',
      borderBottom: `1px solid ${LT.line}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 14,
      color: FK.INK
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      marginTop: 2
    }
  }, sub)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: btn,
    onClick: () => onStep(-1)
  }, '\u2212'), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 13,
      color: FK.AMBER_800,
      width: 44,
      textAlign: 'center'
    }
  }, pct, "%"), /*#__PURE__*/React.createElement("span", {
    style: btn,
    onClick: () => onStep(1)
  }, "+")), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 13,
      color: FK.INK,
      width: 82,
      textAlign: 'right'
    }
  }, FLOW.fmtMoney(amount))));
}
function BiddingView() {
  const bid = FRAMEWORK.BID;
  const [open, setOpen] = React.useState(bid.sections[0].code);
  const [markup, setMarkup] = React.useState(bid.markupPct);
  const [fee, setFee] = React.useState(bid.feePct);
  const clamp = v => Math.max(0, Math.min(50, v));
  const lineTotal = l => l.qty * l.days * l.rate;
  const secSub = s => s.lines.reduce((a, l) => a + lineTotal(l), 0);
  const direct = bid.sections.reduce((a, s) => a + secSub(s), 0);
  const mAmt = Math.round(direct * markup / 100);
  const fAmt = Math.round(direct * fee / 100);
  const iAmt = Math.round((direct + mAmt) * bid.insurancePct / 100);
  const grand = direct + mAmt + fAmt + iAmt;
  const diff = grand - bid.expected;
  const ok = Math.abs(diff) <= bid.expected * 0.02;
  const colR = {
    fontFamily: FK.MONO,
    fontSize: 9,
    letterSpacing: '0.08em',
    textTransform: 'uppercase',
    color: FK.INK_FAINT,
    textAlign: 'right'
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 24px 24px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      gap: 12,
      paddingBottom: 14,
      borderBottom: `1px solid ${LT.line}`,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 11,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 19,
      color: FK.INK
    }
  }, bid.client), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 11,
      color: FK.INK_FAINT
    }
  }, bid.agency, " \xB7 ", bid.project), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      color: FK.AMBER_800,
      padding: '2px 7px',
      borderRadius: 2,
      background: FK.AMBER_50,
      border: `1px solid ${FK.AMBER}`
    }
  }, bid.status)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: FK.INK_FAINT,
      whiteSpace: 'nowrap'
    }
  }, bid.system, " \xB7 ", bid.director)), bid.sections.map(s => {
    const sub = secSub(s),
      isOpen = open === s.code;
    return /*#__PURE__*/React.createElement("div", {
      key: s.code,
      style: {
        borderBottom: `1px solid ${LT.line}`
      }
    }, /*#__PURE__*/React.createElement("div", {
      onClick: () => setOpen(isOpen ? null : s.code),
      style: {
        display: 'grid',
        gridTemplateColumns: '24px 1fr auto 16px',
        gap: 11,
        alignItems: 'center',
        padding: '11px 4px',
        cursor: 'pointer'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 11,
        color: FK.AMBER
      }
    }, s.code), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.SERIF,
        fontStyle: 'italic',
        fontSize: 16,
        color: isOpen ? FK.AMBER_800 : FK.INK
      }
    }, s.name), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 13,
        color: FK.INK
      }
    }, FLOW.fmtMoney(sub)), /*#__PURE__*/React.createElement(FGlyph, {
      name: "arrow",
      size: 11,
      style: {
        color: FK.INK_FAINT,
        transform: isOpen ? 'rotate(90deg)' : 'rotate(0deg)',
        transition: 'transform 0.15s'
      }
    })), isOpen && /*#__PURE__*/React.createElement("div", {
      style: {
        paddingBottom: 12
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'grid',
        gridTemplateColumns: BID_COLS,
        gap: 8,
        padding: '0 4px 5px'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        ...colR,
        textAlign: 'left'
      }
    }, "Description"), /*#__PURE__*/React.createElement("span", {
      style: colR
    }, "Qty"), /*#__PURE__*/React.createElement("span", {
      style: colR
    }, "Days"), /*#__PURE__*/React.createElement("span", {
      style: colR
    }, "Rate"), /*#__PURE__*/React.createElement("span", {
      style: colR
    }, "Total")), s.lines.map((l, i) => /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        display: 'grid',
        gridTemplateColumns: BID_COLS,
        gap: 8,
        alignItems: 'baseline',
        padding: '5px 4px'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.SANS,
        fontSize: 12.5,
        color: FK.INK,
        minWidth: 0
      }
    }, l.desc, l.ctype ? /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 8,
        letterSpacing: '0.08em',
        color: FK.AMBER_800,
        marginLeft: 8,
        padding: '1px 5px',
        borderRadius: 2,
        background: FK.AMBER_50,
        border: `1px solid ${FK.AMBER}`
      }
    }, l.ctype) : /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 8.5,
        letterSpacing: '0.06em',
        color: FK.INK_FAINT,
        marginLeft: 7
      }
    }, l.unit)), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 12,
        color: FK.INK_MUTED,
        textAlign: 'right'
      }
    }, l.qty), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 12,
        color: FK.INK_MUTED,
        textAlign: 'right'
      }
    }, l.days), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 12,
        color: FK.INK_MUTED,
        textAlign: 'right'
      }
    }, FLOW.fmtMoney(l.rate)), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 12,
        color: FK.INK,
        textAlign: 'right'
      }
    }, FLOW.fmtMoney(lineTotal(l))))), s.travel && /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        padding: '8px 4px 2px',
        marginTop: 4,
        borderTop: `1px solid ${LT.line}`
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 9,
        letterSpacing: '0.08em',
        textTransform: 'uppercase',
        color: FK.INK_FAINT
      }
    }, "Travel handling \xB7 ", s.handlingPct, "% reduced"), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 10,
        color: FK.INK_FAINT
      }
    }, "incl. in subtotal"))));
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 18
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '0 0 8px'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT
    }
  }, "Bid calc \xB7 live"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.04em',
      color: FK.INK_FAINT
    }
  }, "Direct ", FLOW.fmtMoney(direct))), /*#__PURE__*/React.createElement(BidLever, {
    label: "Markup",
    sub: "on direct costs",
    pct: markup,
    amount: mAmt,
    onStep: d => setMarkup(v => clamp(v + d))
  }), /*#__PURE__*/React.createElement(BidLever, {
    label: "Production fee",
    sub: "on direct costs",
    pct: fee,
    amount: fAmt,
    onStep: d => setFee(v => clamp(v + d))
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '10px 0'
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 14,
      color: FK.INK
    }
  }, "Insurance"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      marginTop: 2
    }
  }, bid.insurancePct, "% on direct + markup")), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 13,
      color: FK.INK
    }
  }, FLOW.fmtMoney(iAmt)))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 14,
      padding: '15px 17px',
      background: FK.PAPER_SOFT,
      borderRadius: 4,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      marginBottom: 5
    }
  }, "Computed grand total"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: 30,
      lineHeight: 1,
      letterSpacing: '-0.02em',
      color: FK.INK
    }
  }, FLOW.fmtMoney(grand))), /*#__PURE__*/React.createElement("div", {
    style: {
      textAlign: 'right'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 11,
      color: FK.INK_MUTED,
      marginBottom: 7
    }
  }, "Expected ", FLOW.fmtMoney(bid.expected)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.04em',
      color: ok ? FIN_GOOD : FK.AMBER_800,
      padding: '3px 9px',
      borderRadius: 2,
      background: ok ? 'rgba(31,138,91,0.10)' : FK.AMBER_50,
      border: `1px solid ${ok ? 'rgba(31,138,91,0.4)' : FK.AMBER}`,
      whiteSpace: 'nowrap'
    }
  }, ok ? 'within tolerance' : `review · ${diff > 0 ? '+' : '\u2212'}${FLOW.fmtMoney(Math.abs(diff))}`))));
}

// ── script intake (Script tab — line-by-line + live breakdown) ─────
const SCRIPT_LINES = function () {
  const out = [];
  FLOW.FILM.parts.forEach(p => {
    out.push({
      kind: 'part',
      text: p.transition ? 'Transition' : 'Part ' + p.n + ' \u00b7 ' + p.label
    });
    p.blocks.forEach(b => out.push({
      kind: b.k,
      text: b.text
    }));
  });
  return out;
}();
// What the breakdown engine flags as it reads — each tagged to a bid line.
const SCRIPT_INSIGHTS = [{
  match: 'BOOMS UP',
  code: 'D',
  cat: 'Equipment',
  driver: 'Technocrane move',
  impact: 16000,
  src: 'Camera BOOMS UP the face of an 80th-floor building'
}, {
  match: '80th-floor',
  code: 'C',
  cat: 'Locations',
  driver: 'High-rise permit + freight',
  impact: 9000,
  src: 'an 80th-floor office building'
}, {
  match: 'frantic executives',
  code: 'A',
  cat: 'Talent',
  driver: 'Background crowd (~60)',
  impact: 12000,
  src: 'rows of frantic executives'
}, {
  match: 'thrown out a window',
  code: 'F',
  cat: 'Post / FX',
  driver: 'Window gag — rig + comp',
  impact: 18000,
  src: 'a man is thrown out a window'
}, {
  match: 'Sets constructed',
  code: 'E',
  cat: 'Production Design',
  driver: 'Period set build',
  impact: 16000,
  src: 'Sets constructed. Crew scrambling.'
}, {
  match: 'parking lot',
  code: 'C',
  cat: 'Locations',
  driver: 'Exterior unit + picture vehicles',
  impact: 10000,
  src: 'Cars pull up to a store parking lot'
}, {
  match: 'production set that matches',
  code: 'E',
  cat: 'Production Design',
  driver: 'Match-to-board build',
  impact: 8000,
  src: 'a real production set that matches the storyboard'
}];
function hlScript(text) {
  for (const ins of SCRIPT_INSIGHTS) {
    const idx = text.toLowerCase().indexOf(ins.match.toLowerCase());
    if (idx >= 0) return [text.slice(0, idx), /*#__PURE__*/React.createElement("span", {
      key: "m",
      style: {
        background: FK.AMBER_50,
        color: FK.AMBER_800,
        borderRadius: 2,
        padding: '0 2px',
        boxShadow: `inset 0 -1px 0 ${FK.AMBER}`
      }
    }, text.slice(idx, idx + ins.match.length)), text.slice(idx + ins.match.length)];
  }
  return [text];
}
function ScriptProcessing({
  step
}) {
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (ref.current) ref.current.scrollTop = ref.current.scrollHeight;
  }, [step]);
  const total = SCRIPT_LINES.length;
  const done = step >= total - 1;
  const cursor = /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-block',
      width: 7,
      height: 14,
      background: FK.AMBER,
      marginLeft: 4,
      verticalAlign: '-2px',
      animation: 'tenetBreathe 1s ease-in-out infinite'
    }
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      display: 'flex',
      flexDirection: 'column',
      minHeight: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '11px 22px',
      borderBottom: `1px solid ${LT.line}`,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: FK.AMBER
    }
  }, "Reading script"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: FK.INK_FAINT
    }
  }, FLOW.FILM.file, " \xB7 ", Math.min(step + 1, total), "/", total)), /*#__PURE__*/React.createElement("div", {
    ref: ref,
    style: {
      flex: 1,
      minHeight: 0,
      overflow: 'auto',
      padding: '16px 22px 24px'
    }
  }, SCRIPT_LINES.slice(0, step + 1).map((l, i) => {
    const active = i === step && !done;
    if (l.kind === 'part') return /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        margin: i === 0 ? '0 0 12px' : '20px 0 12px'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 10,
        letterSpacing: '0.2em',
        textTransform: 'uppercase',
        color: FK.AMBER
      }
    }, l.text), /*#__PURE__*/React.createElement("div", {
      style: {
        height: 1,
        background: LT.line,
        marginTop: 8
      }
    }));
    if (l.kind === 'card') return /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 12,
        margin: '14px 0'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        height: 1,
        background: LT.line2
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 10,
        letterSpacing: '0.18em',
        textTransform: 'uppercase',
        color: FK.INK_MUTED,
        whiteSpace: 'nowrap'
      }
    }, l.text), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        height: 1,
        background: LT.line2
      }
    }));
    if (l.kind === 'q') return /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        fontFamily: FK.SERIF,
        fontStyle: 'italic',
        fontSize: 15.5,
        lineHeight: 1.4,
        color: FK.INK,
        textAlign: 'center',
        margin: '0 8px 12px'
      }
    }, hlScript(l.text), active && cursor);
    const vo = l.kind === 'vo';
    return /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        display: 'flex',
        gap: 13,
        marginBottom: 11,
        padding: '3px 6px',
        borderRadius: 3,
        background: active ? FK.AMBER_50 : 'transparent'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        flex: '0 0 44px',
        textAlign: 'right',
        paddingTop: 3
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 9,
        letterSpacing: '0.12em',
        textTransform: 'uppercase',
        color: vo ? FK.AMBER : FK.INK_FAINT
      }
    }, vo ? 'VO' : 'Visual')), /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        minWidth: 0,
        borderLeft: vo ? `2px solid ${FK.AMBER}` : 'none',
        paddingLeft: vo ? 12 : 0
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.SCRIPT,
        fontSize: 13.5,
        lineHeight: 1.5,
        color: vo ? FK.INK : FK.INK_MUTED,
        fontWeight: vo ? 500 : 400
      }
    }, hlScript(l.text), active && cursor)));
  })));
}
function BidInsights({
  step,
  onOpenBid
}) {
  const seen = SCRIPT_LINES.slice(0, step + 1).map(l => l.text.toLowerCase()).join(' \u2063 ');
  const detected = SCRIPT_INSIGHTS.filter(ins => seen.includes(ins.match.toLowerCase()));
  const total = SCRIPT_INSIGHTS.length;
  const flagged = detected.reduce((a, d) => a + d.impact, 0);
  const done = step >= SCRIPT_LINES.length - 1;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 22px 22px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 15,
      color: FK.INK_MUTED,
      marginBottom: 14
    }
  }, done ? 'Breakdown ready — every driver tagged to a bid line.' : 'Reading the script for cost drivers\u2026'), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      padding: '10px 12px',
      marginBottom: 8,
      background: FK.PAPER_SOFT,
      borderRadius: 3
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: FK.INK_MUTED
    }
  }, detected.length, "/", total, " drivers"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 13,
      color: FK.AMBER_800
    }
  }, FLOW.fmtMoney(flagged), " flagged")), detected.map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      padding: '12px 6px',
      borderBottom: `1px solid ${LT.line}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      gap: 10,
      marginBottom: 4
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 9,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: FK.AMBER
    }
  }, d.code), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 16,
      color: FK.INK
    }
  }, d.cat)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 12,
      color: FK.AMBER_800,
      whiteSpace: 'nowrap'
    }
  }, "+", FLOW.fmtMoney(d.impact))), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 13,
      color: FK.INK,
      marginBottom: 3
    }
  }, d.driver), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 12.5,
      color: FK.INK_FAINT
    }
  }, "\u201C", d.src, "\u201D"))), detected.length === 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 11,
      color: FK.INK_FAINT,
      padding: '20px 6px',
      textAlign: 'center'
    }
  }, "Listening\u2026"), done && /*#__PURE__*/React.createElement("button", {
    onClick: onOpenBid,
    style: {
      marginTop: 16,
      width: '100%',
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      gap: 8,
      cursor: 'pointer',
      background: FK.AMBER,
      color: FK.PAPER,
      border: 'none',
      borderRadius: 3,
      padding: '10px 14px',
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.12em',
      textTransform: 'uppercase'
    }
  }, "Open the bid ", /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 13
  })));
}

// ── scenarios (Bidding tab — the "what if" engine) ─────────────────
function ScenariosPanel() {
  const bid = FRAMEWORK.BID;
  const [on, setOn] = React.useState({});
  const factor = 1 + (bid.markupPct + bid.feePct) / 100 + bid.insurancePct / 100 * (1 + bid.markupPct / 100);
  const baseDirect = bid.sections.reduce((a, s) => a + s.lines.reduce((b, l) => b + l.qty * l.days * l.rate, 0), 0);
  const active = FRAMEWORK.BID_SCENARIOS.filter(s => on[s.id]);
  const saveDirect = active.reduce((a, s) => a + s.save, 0);
  const baseGrand = Math.round(baseDirect * factor);
  const newGrand = Math.round((baseDirect - saveDirect) * factor);
  const dBudget = newGrand - baseGrand;
  const dDays = active.reduce((a, s) => a + s.days, 0);
  const dCrew = active.reduce((a, s) => a + s.crew, 0);
  const riskNet = active.reduce((a, s) => a + s.risk, 0);
  const risk = active.length === 0 ? {
    t: '\u2014',
    c: FK.INK_FAINT
  } : riskNet < 0 ? {
    t: 'Lower',
    c: FIN_GOOD
  } : riskNet > 0 ? {
    t: 'Higher',
    c: FK.AMBER_800
  } : {
    t: 'Neutral',
    c: FK.INK_MUTED
  };
  const stat = (label, value, color) => /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      marginBottom: 6
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: 22,
      lineHeight: 1,
      color: color || FK.INK
    }
  }, value));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 22px 22px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 15,
      color: FK.INK_MUTED,
      marginBottom: 16
    }
  }, "Toggle a lever \u2014 watch the bid move."), FRAMEWORK.BID_SCENARIOS.map(s => {
    const sel = !!on[s.id];
    return /*#__PURE__*/React.createElement("div", {
      key: s.id,
      onClick: () => setOn(o => Object.assign({}, o, {
        [s.id]: !o[s.id]
      })),
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 12,
        padding: '11px 10px',
        marginLeft: -10,
        marginRight: -10,
        cursor: 'pointer',
        borderBottom: `1px solid ${LT.line}`,
        background: sel ? FK.AMBER_50 : 'transparent'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        width: 18,
        height: 18,
        flexShrink: 0,
        borderRadius: 3,
        border: `1px solid ${sel ? FK.AMBER : LT.line2}`,
        background: sel ? FK.AMBER : LT.win,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: FK.PAPER,
        fontSize: 12,
        lineHeight: 1
      }
    }, sel ? '\u2713' : ''), /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        minWidth: 0
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: FK.SANS,
        fontSize: 14,
        color: sel ? FK.AMBER_800 : FK.INK
      }
    }, s.label), /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 9,
        letterSpacing: '0.04em',
        color: FK.INK_FAINT,
        marginTop: 2
      }
    }, s.note)), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 12,
        color: sel ? FK.AMBER_800 : FK.INK_MUTED,
        whiteSpace: 'nowrap'
      }
    }, '\u2212', FLOW.fmtMoney(s.save)));
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 20,
      padding: '16px 17px',
      background: FK.PAPER_SOFT,
      borderRadius: 4
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      marginBottom: 16,
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      marginBottom: 5,
      whiteSpace: 'nowrap'
    }
  }, "Budget impact \xB7 all-in"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: 30,
      lineHeight: 1,
      letterSpacing: '-0.02em',
      color: dBudget < 0 ? FIN_GOOD : FK.INK
    }
  }, dBudget === 0 ? '$0' : finSigned(dBudget))), /*#__PURE__*/React.createElement("div", {
    style: {
      textAlign: 'right'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: FK.INK_FAINT,
      marginBottom: 4,
      whiteSpace: 'nowrap'
    }
  }, FLOW.fmtMoney(baseGrand), " ", '\u2192'), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: 20,
      color: FK.INK
    }
  }, FLOW.fmtMoney(newGrand)))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 14,
      borderTop: `1px solid ${LT.line}`,
      paddingTop: 14
    }
  }, stat('Schedule', dDays === 0 ? '0' : dDays + 'd', dDays < 0 ? FIN_GOOD : dDays > 0 ? FK.AMBER_800 : FK.INK), stat('Crew', dCrew === 0 ? '0' : String(dCrew), dCrew < 0 ? FIN_GOOD : FK.INK), stat('Risk', risk.t, risk.c))));
}

// ── the producer page ───────────────────────────────────────────────
// ── viewing window (program monitor) ───────────────────
function ViewingWindow({
  state
}) {
  const len = FLOW.totalLength(state);
  return /*#__PURE__*/React.createElement(Win, {
    title: "Program",
    style: {
      flex: '0 0 auto'
    },
    right: /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 10,
        color: FK.INK_FAINT
      }
    }, "16\xD79")
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 14
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: '100%',
      aspectRatio: '16 / 9',
      background: '#000',
      border: `1px solid ${LT.line2}`,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: "assets/production-thumbnail.png",
    alt: "Program",
    style: {
      position: 'absolute',
      inset: 0,
      width: '100%',
      height: '100%',
      objectFit: 'cover',
      display: 'block'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 0,
      left: 0,
      right: 0,
      height: 38,
      background: 'linear-gradient(rgba(0,0,0,0.5), transparent)',
      pointerEvents: 'none'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      bottom: 0,
      left: 0,
      right: 0,
      height: 34,
      background: 'linear-gradient(transparent, rgba(0,0,0,0.5))',
      pointerEvents: 'none'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 10,
      left: 12,
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.16em',
      color: 'rgba(255,255,255,0.7)'
    }
  }, "PROGRAM \xB7 16\xD79"), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 12,
      bottom: 10,
      fontFamily: FK.MONO,
      fontSize: 9,
      color: 'rgba(255,255,255,0.75)'
    }
  }, "01:00:00:00"), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      right: 12,
      bottom: 10,
      fontFamily: FK.MONO,
      fontSize: 9,
      color: 'rgba(255,255,255,0.75)'
    }
  }, FLOW.fmtClock(len)))));
}

// ── bottom timeline (full width, compact) ─────────────────
function PWave() {
  const bars = [];
  for (let i = 0; i < 150; i++) {
    const x = i / 150;
    bars.push(Math.max(0.08, Math.abs(Math.sin(x * 22) * 0.5 + Math.sin(x * 7 + 1) * 0.35 + Math.sin(x * 51) * 0.16)) * 100);
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: '3px 0',
      display: 'flex',
      alignItems: 'center',
      gap: 1,
      padding: '0 6px'
    }
  }, bars.map((h, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      flex: 1,
      height: h + '%',
      background: '#b3bcc6',
      minWidth: 1
    }
  })));
}

// Cost heat ramp — neutral (cheap) → rust (expensive). Stays in the warm,
// amber-led brand family rather than a literal red/green map.
const HEAT_STOPS = ['#E7E2D8', '#F2DCB4', '#ECB659', '#DB8526', '#B4471C'];
const HEAT_CSS = 'linear-gradient(90deg, #E7E2D8, #F2DCB4, #ECB659, #DB8526, #B4471C)';
function heatColor(t) {
  const i = t >= 0.8 ? 4 : t >= 0.6 ? 3 : t >= 0.4 ? 2 : t >= 0.2 ? 1 : 0;
  return HEAT_STOPS[i];
}
function finTotal(scene) {
  return scene.fin ? scene.fin.lv.reduce((a, b) => a + b, 0) : 0;
}
function dollarColor(lv) {
  return lv <= 2 ? '#B58A4C' : lv <= 4 ? '#D8922F' : lv <= 5 ? '#C56A1E' : '#B4471C';
}

// Per-shot cost-profile popup.
function FinancePopup({
  block,
  state,
  anchor
}) {
  const {
    scene,
    num
  } = block;
  const loc = FLOW.LOCS.find(l => l.id === scene.locId);
  const align = anchor < 24 ? 'left' : anchor > 76 ? 'right' : 'center';
  const tx = align === 'left' ? '-10px' : align === 'right' ? 'calc(-100% + 10px)' : '-50%';
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      bottom: 'calc(100% + 12px)',
      left: anchor + '%',
      transform: `translateX(${tx})`,
      width: 304,
      background: LT.win,
      border: `1px solid ${LT.line2}`,
      borderRadius: 6,
      boxShadow: '0 14px 38px rgba(0,0,0,0.18)',
      padding: '14px 16px 15px',
      zIndex: 60,
      pointerEvents: 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      gap: 10,
      marginBottom: 3
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.18em',
      color: FK.AMBER,
      whiteSpace: 'nowrap',
      flexShrink: 0
    }
  }, "SHOT ", num), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      color: FK.INK_FAINT,
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      minWidth: 0,
      textAlign: 'right'
    }
  }, block.sh.dur.toFixed(1).replace('.0', ''), "s \xB7 ", block.sh.desc)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: FK.INK_MUTED,
      marginBottom: 11
    }
  }, loc.kind, ". ", loc.name, " \u2014 ", FLOW.todOf(scene, state)), FLOW.FIN_CATS.map((c, i) => {
    const lv = scene.fin.lv[i];
    return /*#__PURE__*/React.createElement("div", {
      key: c,
      style: {
        display: 'flex',
        alignItems: 'flex-end',
        marginBottom: 4
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 10.5,
        color: FK.INK,
        width: 96,
        flexShrink: 0
      }
    }, c), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        borderBottom: `1px dotted ${LT.line2}`,
        margin: '0 6px 4px'
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 12,
        letterSpacing: '1.5px',
        color: dollarColor(lv),
        whiteSpace: 'nowrap'
      }
    }, '$'.repeat(lv)));
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: `1px solid ${LT.line}`,
      margin: '11px 0 9px'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      marginBottom: 7
    }
  }, "Primary cost drivers"), scene.fin.drivers.map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      gap: 8,
      marginBottom: 4,
      fontFamily: FK.SANS,
      fontSize: 12.5,
      color: FK.INK,
      lineHeight: 1.3
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: FK.AMBER,
      flexShrink: 0
    }
  }, "\u2022"), d)));
}
function ProducerTimeline({
  state,
  flashId
}) {
  const [view, setView] = React.useState('timeline');
  const [hover, setHover] = React.useState(null);
  const scenes = FLOW.activeScenes(state);
  const total = FLOW.totalLength(state);
  // cut clips (timeline view)
  let acc = 0;
  const clips = scenes.map(s => {
    const dur = s.shots.reduce((a, sh) => a + sh.dur, 0);
    const c = {
      left: acc / total * 100,
      width: dur / total * 100,
      name: FLOW.locName(s.locId).toUpperCase(),
      night: FLOW.isNight(s, state),
      id: s.id
    };
    acc += dur;
    return c;
  });
  // shot heat blocks (finance view)
  let fa = 0;
  const fblocks = FLOW.allShots(state).map((sh, i) => {
    const scene = FLOW.SCENES.find(s => s.id === sh.sceneId);
    const tot = finTotal(scene);
    const left = fa / total * 100,
      width = sh.dur / total * 100;
    fa += sh.dur;
    return {
      sh,
      scene,
      tot,
      left,
      width,
      center: left + width / 2,
      num: String(i + 1).padStart(2, '0')
    };
  });
  const tots = fblocks.map(b => b.tot);
  const mn = Math.min(...tots),
    mx = Math.max(...tots);
  const heatT = tot => mx === mn ? 0.5 : (tot - mn) / (mx - mn);
  const hb = fblocks.find(b => b.num === hover);
  const ticks = [];
  for (let t = 0; t <= total; t += 5) ticks.push(t);
  const HEAD = 110,
    ROW = 30;
  const hdr = label => /*#__PURE__*/React.createElement("div", {
    style: {
      height: ROW,
      display: 'flex',
      alignItems: 'center',
      padding: '0 12px',
      borderBottom: `1px solid ${LT.line}`,
      fontFamily: FK.MONO,
      fontSize: 10,
      color: FK.INK_MUTED,
      letterSpacing: '0.08em'
    }
  }, label);
  const tab = (id, label) => /*#__PURE__*/React.createElement("div", {
    key: id,
    onClick: () => {
      setView(id);
      setHover(null);
    },
    style: {
      padding: '5px 13px',
      cursor: 'pointer',
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      borderLeft: id === 'finance' ? `1px solid ${LT.line2}` : 'none',
      background: view === id ? FK.AMBER : LT.win,
      color: view === id ? FK.PAPER : FK.INK_MUTED
    }
  }, label);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      background: LT.win,
      borderTop: `1px solid ${LT.line2}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '7px 16px',
      borderBottom: `1px solid ${LT.line}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: `1px solid ${LT.line2}`,
      borderRadius: 3,
      overflow: 'hidden'
    }
  }, tab('timeline', 'Timeline'), tab('finance', 'Finance')), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: 20,
      color: FK.INK
    }
  }, FLOW.fmtClock(total))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: HEAD,
      flexShrink: 0,
      borderRight: `1px solid ${LT.line2}`
    }
  }, view === 'timeline' ? /*#__PURE__*/React.createElement(React.Fragment, null, hdr('V1 · CUT'), hdr('A1 · MIX')) : /*#__PURE__*/React.createElement(React.Fragment, null, hdr('SHOTS · COST'), hdr('HEAT'))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      position: 'relative'
    }
  }, view === 'timeline' ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: {
      height: ROW,
      position: 'relative',
      borderBottom: `1px solid ${LT.line}`
    }
  }, clips.map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    title: c.name,
    style: {
      position: 'absolute',
      top: 3,
      bottom: 3,
      left: c.left + '%',
      width: c.width + '%',
      background: flashId === c.id ? FK.AMBER_50 : c.night ? '#EFE4D2' : '#dde6ef',
      border: `1px solid ${flashId === c.id ? FK.AMBER : LT.line2}`,
      borderRadius: 2,
      display: 'flex',
      alignItems: 'center',
      padding: '0 7px',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      color: FK.INK_MUTED,
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, c.name)))), /*#__PURE__*/React.createElement("div", {
    style: {
      height: ROW,
      position: 'relative',
      borderBottom: `1px solid ${LT.line}`,
      background: FK.PAPER_SOFT
    }
  }, /*#__PURE__*/React.createElement(PWave, null)), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 0,
      bottom: 0,
      left: '34%',
      width: 1,
      background: FK.AMBER
    }
  })) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: {
      height: ROW,
      position: 'relative',
      borderBottom: `1px solid ${LT.line}`
    }
  }, fblocks.map(b => {
    const t = heatT(b.tot),
      active = hover === b.num;
    return /*#__PURE__*/React.createElement("div", {
      key: b.num,
      onMouseEnter: () => setHover(b.num),
      onMouseLeave: () => setHover(h => h === b.num ? null : h),
      onClick: () => setHover(h => h === b.num ? null : b.num),
      style: {
        position: 'absolute',
        top: 3,
        bottom: 3,
        left: b.left + '%',
        width: b.width + '%',
        background: heatColor(t),
        border: `1px solid ${active ? FK.INK : 'rgba(0,0,0,0.14)'}`,
        borderRadius: 2,
        cursor: 'pointer',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        overflow: 'hidden',
        boxShadow: active ? '0 0 0 1px ' + FK.INK : 'none'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 8.5,
        color: t > 0.55 ? 'rgba(255,255,255,0.92)' : FK.INK_MUTED
      }
    }, b.num));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      height: ROW,
      display: 'flex',
      alignItems: 'center',
      gap: 9,
      padding: '0 12px',
      borderBottom: `1px solid ${LT.line}`,
      background: FK.PAPER_SOFT
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 8.5,
      letterSpacing: '0.14em',
      color: FK.INK_FAINT
    }
  }, "LOW"), /*#__PURE__*/React.createElement("div", {
    style: {
      width: 130,
      height: 7,
      borderRadius: 999,
      background: HEAT_CSS,
      border: `1px solid ${LT.line2}`
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 8.5,
      letterSpacing: '0.14em',
      color: FK.INK_FAINT
    }
  }, "HIGH"), /*#__PURE__*/React.createElement("span", {
    style: {
      marginLeft: 'auto',
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 12,
      color: FK.INK_FAINT
    }
  }, "Hover a shot for its cost profile")), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 0,
      bottom: 0,
      left: '34%',
      width: 1,
      background: 'rgba(0,0,0,0.25)'
    }
  }), hb && /*#__PURE__*/React.createElement(FinancePopup, {
    block: hb,
    state: state,
    anchor: hb.center
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: HEAD,
      flexShrink: 0,
      borderRight: `1px solid ${LT.line2}`
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      position: 'relative',
      height: 22
    }
  }, ticks.map(t => /*#__PURE__*/React.createElement("span", {
    key: t,
    style: {
      position: 'absolute',
      left: t / total * 100 + '%',
      top: 5,
      fontFamily: FK.MONO,
      fontSize: 9,
      color: FK.INK_FAINT,
      transform: 'translateX(2px)'
    }
  }, `01:00:${String(t).padStart(2, '0')}`)))));
}

// ── Script intake cockpit — the production-lane landing ──────────────
// Program monitor + message-to-director (left) · live script reader (center) ·
// auto-tagged bidding insights (right) · cut/finance timeline (bottom).
// This is the unified lane's first view; nav lives in ProductionLane.
function ScriptCockpit({
  state,
  chat,
  flashId,
  onSend,
  goTab
}) {
  const [scriptStep, setScriptStep] = React.useState(0);
  const scriptDone = scriptStep >= SCRIPT_LINES.length - 1;
  React.useEffect(() => {
    if (scriptDone) return;
    const t = setTimeout(() => setScriptStep(s => Math.min(s + 1, SCRIPT_LINES.length - 1)), 430);
    return () => clearTimeout(t);
  }, [scriptStep, scriptDone]);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex',
      flexDirection: 'column',
      background: LT.canvas
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex',
      gap: 14,
      padding: 16,
      boxSizing: 'border-box'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 430px',
      display: 'flex',
      flexDirection: 'column',
      gap: 14,
      minHeight: 0
    }
  }, /*#__PURE__*/React.createElement(ViewingWindow, {
    state: state
  }), /*#__PURE__*/React.createElement(Win, {
    title: "Message \xB7 Director",
    style: {
      flex: 1
    }
  }, /*#__PURE__*/React.createElement(ChatDirector, {
    chat: chat,
    onSend: onSend
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      background: LT.win,
      border: `1px solid ${LT.line}`,
      borderRadius: 4,
      display: 'flex',
      flexDirection: 'column',
      minHeight: 0,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement(ScriptProcessing, {
    step: scriptStep
  })), /*#__PURE__*/React.createElement(Win, {
    title: "Bidding insights",
    style: {
      flex: '0 0 560px'
    },
    right: /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 9,
        letterSpacing: '0.16em',
        textTransform: 'uppercase',
        color: FK.INK_FAINT,
        whiteSpace: 'nowrap'
      }
    }, "Auto-tagged")
  }, /*#__PURE__*/React.createElement(BidInsights, {
    step: scriptStep,
    onOpenBid: () => goTab('bid')
  }))), /*#__PURE__*/React.createElement(ProducerTimeline, {
    state: state,
    flashId: flashId
  }));
}
Object.assign(window, {
  ScriptCockpit,
  BiddingView
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/producer-dash.jsx", error: String((e && e.message) || e) }); }

// flow/production-data.js
try { (() => {
/* ============================================================
   TENET — Production Portal data model.
   ------------------------------------------------------------
   The producer's operational brain for the one project:
   the :30 brand film "A Brief History of Selling Ideas".
   Reconciles to FRAMEWORK: budget from FIN_CENTERS, shots from
   FLOW.SCENES (fin.lv complexity), scenarios from BID_SCENARIOS.
   Every line answers: what costs money, why, and what if I change it.
   ============================================================ */
window.PROD = function () {
  var F = window.FLOW,
    FW = window.FRAMEWORK;
  var S0 = F.DEFAULT_STATE;
  var T = FW.finTotals(S0);
  function money(n) {
    return '$' + Math.round(Math.abs(n)).toLocaleString('en-US');
  }
  function signed(n) {
    return (n < 0 ? '\u2212' : '+') + money(n);
  }
  var project = {
    title: F.TITLE,
    logline: F.LOGLINE,
    format: ':30 Brand Film',
    agency: 'H&W',
    code: '#17433',
    director: 'D. Parker',
    today: 'Jun 5, 2026'
  };

  // ── Dashboard ───────────────────────────────────────────────
  var dashboard = {
    budget: {
      approved: T.approved,
      actual: T.actual,
      forecast: T.forecast,
      variance: T.variance,
      contingency: T.contingency,
      reserve: T.reserve,
      directBudget: T.directBudget
    },
    health: [{
      id: 'budget',
      label: 'Budget health',
      score: 84,
      state: 'Healthy',
      note: 'Forecast ' + money(T.forecast) + ' under ' + money(T.approved) + ' approved.'
    }, {
      id: 'schedule',
      label: 'Schedule health',
      score: 88,
      state: 'On Track',
      note: 'Shoot wrapped on schedule; post on plan.'
    }, {
      id: 'resource',
      label: 'Resource health',
      score: 72,
      state: 'Tight',
      note: 'Crew released; post houses at capacity.'
    }, {
      id: 'risk',
      label: 'Risk score',
      score: 68,
      state: 'Watch',
      note: 'Night overtime and the ending beat carry exposure.'
    }],
    alerts: [{
      kind: 'OT risk',
      sev: 'high',
      text: 'Night exterior is loading ' + money(FW.FIN_NIGHT_OT) + ' of crew overtime',
      tab: 'scenarios'
    }, {
      kind: 'Location risk',
      sev: 'med',
      text: 'Downtown closure ran two hours long \u2014 ' + money(4000) + ' over',
      tab: 'budget'
    }, {
      kind: 'Permit',
      sev: 'low',
      text: 'Beach permit window is tide-dependent',
      tab: 'schedule'
    }, {
      kind: 'Over budget',
      sev: 'ok',
      text: 'No category is forecast over its approved line',
      tab: 'budget'
    }, {
      kind: 'Vendor',
      sev: 'ok',
      text: 'All vendor POs current',
      tab: 'resources'
    }]
  };

  // ── Budget — level 1 + expandable level 2 ───────────────────
  var budgetCats = FW.FIN_CENTERS.map(function (c) {
    return {
      id: c.id,
      name: c.name,
      budget: c.budget,
      actual: c.actual,
      forecast: FW.finForecast(c, S0),
      variance: FW.finVariance(c, S0),
      why: c.why,
      drivers: FW.finDrivers(c, S0)
    };
  });
  // Contingency as its own line.
  var contingency = {
    id: 'cont',
    name: 'Contingency',
    budget: T.reserve,
    actual: T.drawn,
    forecast: T.drawn,
    variance: T.drawn - T.reserve,
    why: 'A night exterior and a location overage are drawing the reserve.',
    drivers: FW.finContingencyDrivers(S0)
  };

  // ── Shots — scenes as setups, fin.lv = complexity factors ───
  var FACTORS = F.FIN_CATS; // 10, aligns to scene.fin.lv
  var maxCost = Math.max.apply(null, F.SCENES.map(function (s) {
    return F.costOf(s, S0);
  }));
  var shots = F.SCENES.map(function (s) {
    var lv = s.fin.lv;
    var complexity = Math.round(lv.reduce(function (a, b) {
      return a + b;
    }, 0) / (FACTORS.length * 7) * 100);
    var cost = F.costOf(s, S0);
    var night = F.isNight(s, S0);
    return {
      id: s.id,
      n: s.n,
      loc: F.locName(s.locId),
      tod: F.todOf(s, S0),
      desc: s.action[0].text,
      complexity: complexity,
      cost: cost,
      costScore: Math.round(cost / maxCost * 100),
      status: 'Wrapped',
      night: night,
      factors: FACTORS.map(function (f, i) {
        return {
          name: f,
          lv: lv[i]
        };
      }),
      drivers: s.fin.drivers,
      shotCount: s.shots.length,
      scheduleRisk: lv[9]
    };
  });
  var topDrivers = [{
    label: 'Night exterior',
    amt: 18000
  }, {
    label: '75 extras \u2014 downtown',
    amt: 12000
  }, {
    label: 'Technocrane',
    amt: 16000
  }, {
    label: 'Picture vehicles',
    amt: 10000
  }, {
    label: 'Downtown closure',
    amt: 18000
  }];

  // ── Schedule ────────────────────────────────────────────────
  var schedule = {
    shootDays: 3,
    daysComplete: 3,
    daysRemaining: 0,
    forecastWrap: 'Apr 18',
    postPhase: 'Editorial',
    delivery: 'Jun 27',
    days: [{
      id: 'd1',
      label: 'Day 1',
      date: 'Apr 14',
      locs: ['Workshop', 'Studio'],
      pages: 2.4,
      shots: 6,
      crew: 17,
      hours: 11,
      risks: ['Complex setups'],
      state: 'done'
    }, {
      id: 'd2',
      label: 'Day 2',
      date: 'Apr 16',
      locs: ['Downtown', 'Apartment'],
      pages: 3.1,
      shots: 6,
      crew: 19,
      hours: 13,
      risks: ['Company moves', 'Night overtime'],
      state: 'done'
    }, {
      id: 'd3',
      label: 'Day 3',
      date: 'Apr 18',
      locs: ['Rooftop', 'Beach'],
      pages: 2.0,
      shots: 5,
      crew: 16,
      hours: 12,
      risks: ['Weather', 'Company moves'],
      state: 'done'
    }],
    riskFactors: [{
      label: 'Company moves',
      count: 3,
      note: 'Three multi-location days'
    }, {
      label: 'Weather',
      count: 1,
      note: 'Beach golden-hour window'
    }, {
      label: 'Talent',
      count: 1,
      note: 'Two principals on the night'
    }, {
      label: 'Complex setups',
      count: 2,
      note: 'Technocrane + macro rig'
    }, {
      label: 'Client reviews',
      count: 1,
      note: 'Picture lock gate ahead'
    }]
  };

  // ── Resources ───────────────────────────────────────────────
  var resources = [{
    group: 'People',
    items: [{
      name: 'Principal talent',
      detail: 'Mara \u00b7 Theo \u00b7 The Maker',
      count: 3,
      state: 'wrapped'
    }, {
      name: 'Extras',
      detail: 'Downtown background',
      count: 75,
      state: 'wrapped'
    }, {
      name: 'Crew',
      detail: 'Core + department heads',
      count: 19,
      state: 'wrapped'
    }, {
      name: 'Vendors',
      detail: 'Catering, transport, rentals',
      count: 6,
      state: 'active'
    }]
  }, {
    group: 'Locations',
    items: [{
      name: 'Stages',
      detail: 'Studio (product table)',
      count: 1,
      state: 'wrapped'
    }, {
      name: 'Practicals',
      detail: 'Rooftop \u00b7 Workshop \u00b7 Apartment \u00b7 Downtown',
      count: 4,
      state: 'wrapped'
    }, {
      name: 'Remote',
      detail: 'Beach \u2014 destination coast',
      count: 1,
      state: 'wrapped'
    }]
  }, {
    group: 'Equipment',
    items: [{
      name: 'Camera',
      detail: 'Package + lenses',
      count: 1,
      state: 'wrapped'
    }, {
      name: 'Grip & lighting',
      detail: 'Truck package',
      count: 1,
      state: 'wrapped'
    }, {
      name: 'Vehicles',
      detail: 'Picture cars',
      count: 3,
      state: 'wrapped'
    }, {
      name: 'Specialty',
      detail: 'Technocrane \u00b7 macro rig',
      count: 2,
      state: 'wrapped'
    }]
  }, {
    group: 'Post',
    items: [{
      name: 'Editorial',
      detail: 'Current cut for review',
      count: 1,
      state: 'active'
    }, {
      name: 'Color',
      detail: 'Scheduled Jun 16',
      count: 1,
      state: 'pending'
    }, {
      name: 'VFX',
      detail: 'Skyline cleanup',
      count: 1,
      state: 'active'
    }, {
      name: 'Sound',
      detail: 'Mix + music',
      count: 1,
      state: 'pending'
    }, {
      name: 'Delivery',
      detail: 'Masters + versions',
      count: 1,
      state: 'pending'
    }]
  }];

  // ── Risks ───────────────────────────────────────────────────
  var risks = [{
    id: 'pr1',
    type: 'Budget',
    title: 'Night exterior overtime',
    prob: 'Medium',
    exposure: money(18000),
    cost: 18000,
    days: 0,
    mitigation: 'Flip the apartment scene to day to clear the premium.'
  }, {
    id: 'pr2',
    type: 'Weather',
    title: 'Beach golden-hour window',
    prob: 'Medium',
    exposure: 'One half-day',
    cost: 18000,
    days: 1,
    mitigation: 'Hold a contingency day; watch the tide chart.'
  }, {
    id: 'pr3',
    type: 'Client',
    title: 'Picture lock waits on review',
    prob: 'Medium',
    exposure: '3-day slip',
    cost: 0,
    days: 3,
    mitigation: 'Drive the client to approve or note by Jun 9.'
  }, {
    id: 'pr4',
    type: 'Technical',
    title: 'VFX cleanup heavier than bid',
    prob: 'Low',
    exposure: money(8000),
    cost: 8000,
    days: 1,
    mitigation: 'Lock the skyline plate count at picture lock.'
  }, {
    id: 'pr5',
    type: 'Legal',
    title: 'Talent usage renews in 24 months',
    prob: 'Low',
    exposure: money(16000),
    cost: 16000,
    days: 0,
    mitigation: 'Renewal budgeted in licensing; flag at month 20.'
  }];

  // ── SCENARIOS — the what-if engine (toggle + stack) ─────────
  // Negative budget = savings. crew = resource delta. risk delta −2..+2.
  var scenarios = [{
    id: 'extras',
    label: 'Reduce extras',
    note: '75 \u2192 40 background',
    budget: -12000,
    days: 0,
    crew: 0,
    risk: -1,
    res: ['Extras']
  }, {
    id: 'crane',
    label: 'Remove the technocrane',
    note: 'Crane \u2192 dolly + jib',
    budget: -16000,
    days: 0,
    crew: -2,
    risk: -1,
    res: ['Specialty', 'Grip']
  }, {
    id: 'stage',
    label: 'Move the night to a stage',
    note: 'Drops the night exterior',
    budget: -22000,
    days: -1,
    crew: -8,
    risk: -2,
    res: ['Locations', 'Crew']
  }, {
    id: 'day',
    label: 'Drop a shoot day',
    note: '3 \u2192 2 shoot days',
    budget: -28000,
    days: -1,
    crew: -12,
    risk: 2,
    res: ['Crew', 'Schedule']
  }, {
    id: 'vfx',
    label: 'Convert practical to VFX',
    note: 'Plate + comp',
    budget: -9000,
    days: 0,
    crew: -4,
    risk: 1,
    res: ['VFX', 'Crew']
  }, {
    id: 'deliv',
    label: 'Reduce deliverables',
    note: 'Drop the :15 cutdown',
    budget: -6000,
    days: 0,
    crew: 0,
    risk: 0,
    res: ['Editorial']
  }];
  return {
    F: F,
    FW: FW,
    S0: S0,
    T: T,
    money: money,
    signed: signed,
    project: project,
    dashboard: dashboard,
    budgetCats: budgetCats,
    contingency: contingency,
    FACTORS: FACTORS,
    shots: shots,
    topDrivers: topDrivers,
    schedule: schedule,
    resources: resources,
    risks: risks,
    scenarios: scenarios,
    bidExpected: FW.BID.expected,
    NAV: [{
      id: 'script',
      label: 'Script',
      phase: 'bid'
    }, {
      id: 'dashboard',
      label: 'Dashboard',
      phase: 'budget'
    }, {
      id: 'budget',
      label: 'Budget',
      phase: 'budget'
    }, {
      id: 'bid',
      label: 'Bid',
      phase: 'bid'
    }, {
      id: 'schedule',
      label: 'Schedule',
      phase: 'budget'
    }, {
      id: 'scenarios',
      label: 'Changes',
      phase: 'budget'
    }]
  };
}();
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/production-data.js", error: String((e && e.message) || e) }); }

// flow/production-hero.jsx
try { (() => {
/* global React */
// TENET Production Portal — hero tabs: Dashboard, Budget (expandable), Scenarios.
const {
  FK: DFK,
  FGlyph: DG,
  PROD: DC
} = window;
const {
  CPanel: DP,
  CPStat: DStat,
  CPBar: DBar,
  CPChip: DChip,
  CPHead: DHead,
  useCountUp: DCount,
  cpKickerFaint: DKickF,
  RISK_COLOR: DRISK,
  SEV_COLOR: DSEV
} = window;
const DUse = React.useState;
const RES_BUDGET = {
  wrapped: {
    c: '#9A9A9A',
    t: 'Wrapped'
  },
  active: {
    c: '#BA7517',
    t: 'Active'
  },
  pending: {
    c: '#9A9A9A',
    t: 'Pending'
  }
};

/* ══════════════════════════════════════════════════════════
   DASHBOARD — producer mission control
   ══════════════════════════════════════════════════════════ */
function PDashboard({
  ctx
}) {
  const d = DC.dashboard;
  const b = d.budget;
  const projForecast = b.forecast + ctx.savings; // savings negative
  const hasScenarios = ctx.active.length > 0;
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(DHead, {
    kicker: "Dashboard",
    lede: "The operational read \u2014 money, schedule, resources, and what\u2019s flashing red."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(5, 1fr)',
      gap: 14,
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement(DStat, {
    label: "Approved budget",
    value: DC.money(b.approved)
  }), /*#__PURE__*/React.createElement(DStat, {
    label: "Actual spend",
    value: DC.money(b.actual),
    sub: "Committed to date"
  }), /*#__PURE__*/React.createElement(DStat, {
    label: "Forecast final",
    value: DC.money(projForecast),
    tone: hasScenarios ? '#1F8A5B' : DFK.INK,
    sub: hasScenarios ? DC.signed(ctx.savings) + ' w/ scenarios' : 'On plan'
  }), /*#__PURE__*/React.createElement(DStat, {
    label: "Variance",
    value: (b.variance < 0 ? '\u2212' : '+') + DC.money(b.variance),
    tone: b.variance > 0 ? '#B23A2E' : '#1F8A5B',
    sub: b.variance < 0 ? 'Under approved' : 'Over approved'
  }), /*#__PURE__*/React.createElement(DStat, {
    label: "Contingency",
    value: DC.money(b.contingency),
    sub: 'of ' + DC.money(b.reserve) + ' reserve'
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1.4fr 1fr',
      gap: 16,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement(DP, {
    label: "Project health",
    title: "Status across the board"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 18
    }
  }, d.health.map(h => /*#__PURE__*/React.createElement("div", {
    key: h.id
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline',
      marginBottom: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.SANS,
      fontSize: 14,
      color: DFK.INK
    }
  }, h.label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 10,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: h.score >= 80 ? '#1F8A5B' : h.score >= 70 ? DFK.AMBER : '#B23A2E'
    }
  }, h.state)), /*#__PURE__*/React.createElement(DBar, {
    pct: h.score,
    color: h.score >= 80 ? '#1F8A5B' : h.score >= 70 ? DFK.AMBER : '#B23A2E'
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 8,
      fontFamily: DFK.SANS,
      fontSize: 12.5,
      color: DFK.INK_MUTED,
      lineHeight: 1.4
    }
  }, h.note))))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(DP, {
    label: "Alerts",
    title: null,
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, d.alerts.map((a, i) => /*#__PURE__*/React.createElement("button", {
    key: i,
    onClick: () => ctx.goTab(a.tab),
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      gap: 12,
      textAlign: 'left',
      background: 'none',
      border: 0,
      borderTop: i ? `1px solid ${DFK.BORDER}` : 'none',
      padding: '13px 0',
      cursor: 'pointer',
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 7,
      height: 7,
      borderRadius: 999,
      background: DSEV[a.sev],
      marginTop: 5,
      flexShrink: 0
    }
  }), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: DSEV[a.sev],
      marginBottom: 3
    }
  }, a.kind), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontSize: 13.5,
      color: DFK.INK,
      lineHeight: 1.4
    }
  }, a.text)))))), /*#__PURE__*/React.createElement("button", {
    onClick: () => ctx.goTab('scenarios'),
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      cursor: 'pointer',
      padding: '18px 22px',
      borderRadius: 4,
      border: 'none',
      background: DFK.INK,
      color: DFK.PAPER
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      textAlign: 'left'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: DFK.AMBER_200
    }
  }, "Changes"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontWeight: 300,
      fontSize: 18,
      marginTop: 6
    }
  }, "Model a change")), /*#__PURE__*/React.createElement(DG, {
    name: "arrow",
    size: 18,
    style: {
      color: DFK.PAPER
    }
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 28
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, DKickF, {
      marginBottom: 14
    })
  }, "Risks \xB7 priced exposure"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 16
    }
  }, DC.risks.map(r => /*#__PURE__*/React.createElement("div", {
    key: r.id,
    style: {
      border: `1px solid ${DFK.BORDER}`,
      borderRadius: 6,
      background: DFK.PAPER,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 22px 16px',
      borderBottom: `1px solid ${DFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: DFK.AMBER,
      whiteSpace: 'nowrap'
    }
  }, r.type, " risk"), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 7
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: DFK.INK_FAINT
    }
  }, "Prob"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.SANS,
      fontSize: 13,
      color: DRISK[r.prob]
    }
  }, r.prob))), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontWeight: 300,
      fontSize: 19,
      lineHeight: 1.2,
      color: DFK.INK
    }
  }, r.title)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr 1fr',
      gap: 1,
      background: DFK.BORDER
    }
  }, [['Exposure', r.exposure, DFK.INK], ['Cost impact', r.cost ? '+' + DC.money(r.cost) : '\u2014', r.cost ? '#B23A2E' : DFK.INK_FAINT], ['Schedule', r.days ? '+' + r.days + 'd' : '\u2014', r.days ? DFK.AMBER : DFK.INK_FAINT]].map(([k, v, c], i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      background: DFK.PAPER,
      padding: '14px 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: DFK.INK_FAINT,
      marginBottom: 6
    }
  }, k), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontSize: 15,
      color: c,
      fontVariantNumeric: 'tabular-nums'
    }
  }, v)))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 22px',
      borderTop: `1px solid ${DFK.BORDER}`,
      background: DFK.PAPER_SOFT
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, DKickF, {
      marginBottom: 8
    })
  }, "Mitigation"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontSize: 13.5,
      color: DFK.INK,
      lineHeight: 1.5
    }
  }, r.mitigation)))))));
}

/* ══════════════════════════════════════════════════════════
   BUDGET — level 1 + expandable level 2 (drivers)
   ══════════════════════════════════════════════════════════ */
function PBudget({
  ctx
}) {
  const [open, setOpen] = DUse('crew');
  const [shotsOpen, setShotsOpen] = DUse(false);
  const [resOpen, setResOpen] = DUse(false);
  const cats = DC.budgetCats.concat([DC.contingency]);
  const b = DC.dashboard.budget;
  const maxDriver = Math.max.apply(null, DC.topDrivers.map(d => d.amt));
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(DHead, {
    kicker: "Budget",
    lede: "Open any line to see the drivers \u2014 the system already knows why."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(3, 1fr)',
      gap: 14,
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement(DStat, {
    label: "Approved budget",
    value: DC.money(b.approved)
  }), /*#__PURE__*/React.createElement(DStat, {
    label: "Forecast final",
    value: DC.money(b.forecast),
    sub: "On plan"
  }), /*#__PURE__*/React.createElement(DStat, {
    label: "Variance",
    value: (b.variance < 0 ? '\u2212' : '+') + DC.money(b.variance),
    tone: b.variance > 0 ? '#B23A2E' : '#1F8A5B',
    sub: b.variance < 0 ? 'Under approved' : 'Over approved'
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${DFK.BORDER}`,
      borderRadius: 6,
      background: DFK.PAPER,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '2fr 1fr 1fr 1fr 1fr 28px',
      gap: 16,
      padding: '14px 22px',
      borderBottom: `1px solid ${DFK.BORDER}`,
      alignItems: 'center'
    }
  }, ['Category', 'Budget', 'Actual', 'Forecast', 'Variance', ''].map((h, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      fontFamily: DFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: DFK.INK_FAINT,
      textAlign: i === 0 ? 'left' : i === 5 ? 'center' : 'right'
    }
  }, h))), cats.map((c, idx) => {
    const isOpen = open === c.id;
    const over = c.variance > 0;
    const isCont = c.id === 'cont';
    return /*#__PURE__*/React.createElement("div", {
      key: c.id,
      style: {
        borderTop: idx ? `1px solid ${DFK.BORDER}` : 'none',
        background: isOpen ? DFK.PAPER_SOFT : DFK.PAPER
      }
    }, /*#__PURE__*/React.createElement("button", {
      onClick: () => setOpen(isOpen ? null : c.id),
      style: {
        width: '100%',
        display: 'grid',
        gridTemplateColumns: '2fr 1fr 1fr 1fr 1fr 28px',
        gap: 16,
        padding: '15px 22px',
        alignItems: 'center',
        background: 'none',
        border: 0,
        cursor: 'pointer',
        textAlign: 'left'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: DFK.SANS,
        fontSize: 15,
        color: DFK.INK,
        display: 'flex',
        alignItems: 'center',
        gap: 10
      }
    }, isCont && /*#__PURE__*/React.createElement("span", {
      style: {
        width: 6,
        height: 6,
        borderRadius: 999,
        background: DFK.AMBER
      }
    }), c.name), /*#__PURE__*/React.createElement("span", {
      style: numCell(DFK.INK_MUTED)
    }, DC.money(c.budget)), /*#__PURE__*/React.createElement("span", {
      style: numCell(DFK.INK_MUTED)
    }, DC.money(c.actual)), /*#__PURE__*/React.createElement("span", {
      style: numCell(DFK.INK)
    }, DC.money(c.forecast)), /*#__PURE__*/React.createElement("span", {
      style: numCell(over ? '#B23A2E' : '#1F8A5B')
    }, c.variance === 0 ? '\u2014' : (over ? '+' : '\u2212') + DC.money(c.variance)), /*#__PURE__*/React.createElement("span", {
      style: {
        textAlign: 'center',
        color: DFK.INK_FAINT,
        transition: 'transform 200ms ease',
        transform: isOpen ? 'rotate(90deg)' : 'none'
      }
    }, /*#__PURE__*/React.createElement(DG, {
      name: "arrow",
      size: 14
    }))), isOpen && /*#__PURE__*/React.createElement("div", {
      style: {
        padding: '4px 22px 22px'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: DFK.SERIF,
        fontStyle: 'italic',
        fontSize: 14,
        color: DFK.INK_MUTED,
        marginBottom: 16,
        maxWidth: 620
      }
    }, c.why), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'grid',
        gridTemplateColumns: '1fr 1fr',
        gap: '10px 32px'
      }
    }, c.drivers.map((dr, i) => {
      const dmax = Math.max.apply(null, c.drivers.map(x => x.amt));
      return /*#__PURE__*/React.createElement("div", {
        key: i
      }, /*#__PURE__*/React.createElement("div", {
        style: {
          display: 'flex',
          justifyContent: 'space-between',
          alignItems: 'baseline',
          marginBottom: 6
        }
      }, /*#__PURE__*/React.createElement("span", {
        style: {
          fontFamily: DFK.SANS,
          fontSize: 13,
          color: dr.night ? DFK.AMBER_800 : DFK.INK
        }
      }, dr.label, dr.night && ' \u00b7 night'), /*#__PURE__*/React.createElement("span", {
        style: {
          fontFamily: DFK.MONO,
          fontSize: 12,
          color: DFK.INK,
          fontVariantNumeric: 'tabular-nums'
        }
      }, DC.money(dr.amt))), /*#__PURE__*/React.createElement(DBar, {
        pct: dr.amt / dmax * 100,
        color: dr.night ? DFK.AMBER : DFK.INK_FAINT,
        h: 3
      }));
    }))));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 16,
      border: `1px solid ${DFK.BORDER}`,
      borderRadius: 6,
      background: DFK.PAPER,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: () => setShotsOpen(o => !o),
    style: {
      width: '100%',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '16px 22px',
      background: shotsOpen ? DFK.PAPER_SOFT : DFK.PAPER,
      border: 0,
      cursor: 'pointer',
      textAlign: 'left'
    }
  }, /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      fontFamily: DFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: DFK.AMBER
    }
  }, "Shot breakdown"), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      fontFamily: DFK.SANS,
      fontWeight: 300,
      fontSize: 18,
      color: DFK.INK,
      marginTop: 4
    }
  }, "Cost and complexity, shot by shot")), /*#__PURE__*/React.createElement("span", {
    style: {
      color: DFK.INK_FAINT,
      transition: 'transform 200ms ease',
      transform: shotsOpen ? 'rotate(90deg)' : 'none'
    }
  }, /*#__PURE__*/React.createElement(DG, {
    name: "arrow",
    size: 16
  }))), shotsOpen && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '0 22px 22px',
      borderTop: `1px solid ${DFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(5, 1fr)',
      gap: 18,
      padding: '18px 0'
    }
  }, DC.topDrivers.map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: i
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 13,
      color: DFK.INK,
      fontVariantNumeric: 'tabular-nums'
    }
  }, DC.money(d.amt)), /*#__PURE__*/React.createElement("div", {
    style: {
      margin: '7px 0'
    }
  }, /*#__PURE__*/React.createElement(DBar, {
    pct: d.amt / maxDriver * 100,
    color: DFK.AMBER_200
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontSize: 12,
      color: DFK.INK_MUTED,
      lineHeight: 1.3
    }
  }, d.label)))), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${DFK.BORDER}`,
      borderRadius: 6,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '40px 1fr 120px 110px',
      gap: 12,
      padding: '12px 18px',
      borderBottom: `1px solid ${DFK.BORDER}`
    }
  }, ['#', 'Setup', 'Complexity', 'Est. cost'].map((h, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      fontFamily: DFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: DFK.INK_FAINT,
      textAlign: i >= 2 ? 'right' : 'left'
    }
  }, h))), DC.shots.map((sh, i) => /*#__PURE__*/React.createElement("div", {
    key: sh.id,
    style: {
      display: 'grid',
      gridTemplateColumns: '40px 1fr 120px 110px',
      gap: 12,
      padding: '13px 18px',
      alignItems: 'center',
      borderTop: i ? `1px solid ${DFK.BORDER}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 12,
      color: DFK.INK_FAINT
    }
  }, String(sh.n).padStart(2, '0')), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.SANS,
      fontSize: 14,
      color: DFK.INK,
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, sh.loc, sh.night && /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 8.5,
      letterSpacing: '0.1em',
      color: DFK.AMBER,
      border: `1px solid ${DFK.AMBER}`,
      borderRadius: 2,
      padding: '1px 4px'
    }
  }, "NIGHT")), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      justifyContent: 'flex-end'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 50
    }
  }, /*#__PURE__*/React.createElement(DBar, {
    pct: sh.complexity,
    color: sh.complexity > 55 ? '#B23A2E' : DFK.INK,
    h: 4
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 11,
      color: DFK.INK,
      width: 22,
      textAlign: 'right'
    }
  }, sh.complexity)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 12,
      color: DFK.INK,
      textAlign: 'right',
      fontVariantNumeric: 'tabular-nums'
    }
  }, DC.money(sh.cost))))))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 16,
      border: `1px solid ${DFK.BORDER}`,
      borderRadius: 6,
      background: DFK.PAPER,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: () => setResOpen(o => !o),
    style: {
      width: '100%',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '16px 22px',
      background: resOpen ? DFK.PAPER_SOFT : DFK.PAPER,
      border: 0,
      cursor: 'pointer',
      textAlign: 'left'
    }
  }, /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      fontFamily: DFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: DFK.AMBER
    }
  }, "Resources"), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      fontFamily: DFK.SANS,
      fontWeight: 300,
      fontSize: 18,
      color: DFK.INK,
      marginTop: 4
    }
  }, "People, locations, gear, and post")), /*#__PURE__*/React.createElement("span", {
    style: {
      color: DFK.INK_FAINT,
      transition: 'transform 200ms ease',
      transform: resOpen ? 'rotate(90deg)' : 'none'
    }
  }, /*#__PURE__*/React.createElement(DG, {
    name: "arrow",
    size: 16
  }))), resOpen && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 22px 22px',
      borderTop: `1px solid ${DFK.BORDER}`,
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 16
    }
  }, DC.resources.map(g => /*#__PURE__*/React.createElement("div", {
    key: g.group,
    style: {
      border: `1px solid ${DFK.BORDER}`,
      borderRadius: 6,
      background: DFK.PAPER,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '14px 18px 0',
      fontFamily: DFK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: DFK.AMBER
    }
  }, g.group), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '6px 18px 14px'
    }
  }, g.items.map((it, i) => {
    const st = RES_BUDGET[it.state] || RES_BUDGET.pending;
    return /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        gap: 12,
        padding: '12px 0',
        borderTop: i ? `1px solid ${DFK.BORDER}` : 'none'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        minWidth: 0
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: DFK.SANS,
        fontSize: 14,
        color: DFK.INK
      }
    }, it.name), /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: DFK.SANS,
        fontSize: 12.5,
        color: DFK.INK_MUTED,
        marginTop: 1
      }
    }, it.detail)), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 12,
        flexShrink: 0
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: DFK.SANS,
        fontWeight: 300,
        fontSize: 20,
        color: DFK.INK
      }
    }, it.count), /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'inline-flex',
        alignItems: 'center',
        gap: 6,
        fontFamily: DFK.MONO,
        fontSize: 9,
        letterSpacing: '0.12em',
        textTransform: 'uppercase',
        color: st.c
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        width: 5,
        height: 5,
        borderRadius: 999,
        background: st.c
      }
    }), st.t)));
  })))))));
}
function numCell(color) {
  return {
    fontFamily: DFK.MONO,
    fontSize: 13,
    color: color,
    textAlign: 'right',
    fontVariantNumeric: 'tabular-nums'
  };
}

/* ══════════════════════════════════════════════════════════
   SCENARIOS — the producer superpower (toggle + stack)
   ══════════════════════════════════════════════════════════ */
function PScenarios({
  ctx
}) {
  const approved = DC.dashboard.budget.approved;
  const projected = DC.dashboard.budget.forecast + ctx.savings;
  const saved = Math.abs(ctx.savings);
  const animSaved = saved; // render the live value directly (count-up didn't track target)

  function toggle(id) {
    ctx.setActive(prev => prev.indexOf(id) >= 0 ? prev.filter(x => x !== id) : prev.concat([id]));
  }
  function clearAll() {
    ctx.setActive([]);
  }
  const activeRes = {};
  DC.scenarios.filter(s => ctx.active.indexOf(s.id) >= 0).forEach(s => s.res.forEach(r => {
    activeRes[r] = true;
  }));
  const riskWord = ctx.riskDelta < 0 ? 'Lower' : ctx.riskDelta > 0 ? 'Higher' : 'Neutral';
  const riskColor = ctx.riskDelta < 0 ? '#1F8A5B' : ctx.riskDelta > 0 ? '#B23A2E' : DFK.INK_MUTED;
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1.3fr 1fr 1fr 1fr',
      gap: 16,
      marginBottom: 28
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${saved ? '#1F8A5B' : DFK.BORDER}`,
      background: saved ? 'rgba(31,138,91,0.06)' : DFK.PAPER,
      borderRadius: 4,
      padding: '16px 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: DKickF
  }, "Projected / approved"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontWeight: 300,
      fontSize: 24,
      marginTop: 8,
      color: DFK.INK,
      fontVariantNumeric: 'tabular-nums'
    }
  }, DC.money(projected), " ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: DFK.INK_FAINT,
      fontSize: 16
    }
  }, "/ ", DC.money(approved))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement(DBar, {
    pct: projected / approved * 100,
    color: saved ? '#1F8A5B' : DFK.AMBER
  }))), /*#__PURE__*/React.createElement(StripStat, {
    label: "Total saved",
    value: DC.money(animSaved),
    tone: saved ? '#1F8A5B' : DFK.INK,
    sub: ctx.active.length + ' active'
  }), /*#__PURE__*/React.createElement(StripStat, {
    label: "Schedule",
    value: ctx.daysDelta ? ctx.daysDelta + ' days' : 'No change',
    tone: ctx.daysDelta < 0 ? '#1F8A5B' : DFK.INK,
    sub: ctx.daysDelta < 0 ? 'Shorter' : 'Same'
  }), /*#__PURE__*/React.createElement(StripStat, {
    label: "Risk",
    value: riskWord,
    tone: riskColor,
    sub: ctx.crewDelta ? ctx.crewDelta + ' crew' : 'No crew change'
  })), /*#__PURE__*/React.createElement(DHead, {
    kicker: "Changes",
    lede: "Stack changes and watch the savings, the schedule, and the risk move together. Nothing is committed."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 360px',
      gap: 24,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, DC.scenarios.map(s => {
    const on = ctx.active.indexOf(s.id) >= 0;
    return /*#__PURE__*/React.createElement("button", {
      key: s.id,
      onClick: () => toggle(s.id),
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 16,
        textAlign: 'left',
        cursor: 'pointer',
        padding: '16px 18px',
        borderRadius: 4,
        border: `1px solid ${on ? '#1F8A5B' : DFK.BORDER}`,
        background: on ? 'rgba(31,138,91,0.05)' : DFK.PAPER,
        transition: 'all 160ms ease'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        width: 22,
        height: 22,
        borderRadius: 5,
        flexShrink: 0,
        border: `1.5px solid ${on ? '#1F8A5B' : DFK.BORDER_STRONG}`,
        background: on ? '#1F8A5B' : 'transparent',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: '#fff'
      }
    }, on && /*#__PURE__*/React.createElement(DG, {
      name: "check",
      size: 13
    })), /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        minWidth: 0
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: DFK.SANS,
        fontSize: 15.5,
        color: DFK.INK
      }
    }, s.label), /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: DFK.SERIF,
        fontStyle: 'italic',
        fontSize: 13,
        color: DFK.INK_MUTED,
        marginTop: 2
      }
    }, s.note)), /*#__PURE__*/React.createElement("div", {
      style: {
        textAlign: 'right',
        flexShrink: 0
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: DFK.MONO,
        fontSize: 14,
        color: '#1F8A5B',
        fontVariantNumeric: 'tabular-nums'
      }
    }, DC.signed(s.budget)), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 5,
        justifyContent: 'flex-end',
        marginTop: 5
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        width: 6,
        height: 6,
        borderRadius: 999,
        background: s.risk < 0 ? '#1F8A5B' : s.risk > 0 ? '#B23A2E' : DFK.INK_FAINT
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: DFK.MONO,
        fontSize: 9.5,
        letterSpacing: '0.1em',
        textTransform: 'uppercase',
        color: DFK.INK_FAINT
      }
    }, s.risk < 0 ? 'lowers risk' : s.risk > 0 ? 'raises risk' : 'neutral'))));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${DFK.BORDER_STRONG}`,
      borderRadius: 6,
      background: DFK.PAPER,
      overflow: 'hidden',
      position: 'sticky',
      top: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '22px 24px',
      borderBottom: `1px solid ${DFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: DKickF
  }, "How much can I save?"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontWeight: 300,
      fontSize: 44,
      lineHeight: 1,
      color: saved ? '#1F8A5B' : DFK.INK,
      marginTop: 10,
      fontVariantNumeric: 'tabular-nums'
    }
  }, DC.money(animSaved)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontSize: 13,
      color: DFK.INK_MUTED,
      marginTop: 8
    }
  }, "Brings the bid to ", /*#__PURE__*/React.createElement("b", {
    style: {
      color: DFK.INK,
      fontWeight: 400
    }
  }, DC.money(projected)))), /*#__PURE__*/React.createElement(OutCell, {
    label: "Schedule impact",
    value: ctx.daysDelta ? ctx.daysDelta + ' days' : 'No change',
    tone: ctx.daysDelta < 0 ? '#1F8A5B' : DFK.INK
  }), /*#__PURE__*/React.createElement(OutCell, {
    label: "Resource impact",
    value: ctx.crewDelta ? ctx.crewDelta + ' crew' : 'No change',
    tone: DFK.INK
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 7,
      marginTop: 12
    }
  }, ['Crew', 'Extras', 'Specialty', 'Grip', 'Locations', 'VFX', 'Editorial', 'Schedule'].map(r => /*#__PURE__*/React.createElement(DChip, {
    key: r,
    on: !!activeRes[r],
    dot: true
  }, r)))), /*#__PURE__*/React.createElement(OutCell, {
    label: "Risk impact",
    value: riskWord,
    tone: riskColor
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 12,
      display: 'flex',
      gap: 6
    }
  }, [-2, -1, 0, 1, 2].map(v => {
    const mid = 2;
    const filled = ctx.riskDelta < 0 ? v < 0 && v >= ctx.riskDelta : ctx.riskDelta > 0 ? v > 0 && v <= ctx.riskDelta : false;
    return /*#__PURE__*/React.createElement("div", {
      key: v,
      style: {
        flex: 1,
        height: 8,
        borderRadius: 2,
        background: v === 0 ? DFK.BORDER_STRONG : filled ? riskColor : DFK.BORDER
      }
    });
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 24px',
      borderTop: `1px solid ${DFK.BORDER}`,
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 10,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: DFK.INK_FAINT
    }
  }, ctx.active.length, " scenario", ctx.active.length === 1 ? '' : 's', " stacked"), ctx.active.length > 0 && /*#__PURE__*/React.createElement("button", {
    onClick: clearAll,
    style: {
      background: 'none',
      border: 0,
      padding: 0,
      cursor: 'pointer',
      fontFamily: DFK.MONO,
      fontSize: 10,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: DFK.INK_MUTED,
      borderBottom: `1px solid ${DFK.BORDER}`
    }
  }, "Reset")))));
}
function StripStat({
  label,
  value,
  sub,
  tone
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${DFK.BORDER}`,
      background: DFK.PAPER,
      borderRadius: 4,
      padding: '16px 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: DKickF
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontWeight: 300,
      fontSize: 24,
      marginTop: 8,
      color: tone || DFK.INK
    }
  }, value), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 11,
      color: DFK.INK_FAINT,
      marginTop: 8
    }
  }, sub));
}
function OutCell({
  label,
  value,
  tone,
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 24px',
      borderTop: `1px solid ${DFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: DKickF
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.SANS,
      fontSize: 17,
      color: tone || DFK.INK
    }
  }, value)), children);
}
Object.assign(window, {
  PDashboard,
  PBudget,
  PScenarios
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/production-hero.jsx", error: String((e && e.message) || e) }); }

// flow/production-shell.jsx
try { (() => {
/* global React */
// TENET Production lane — ONE cockpit, ONE nav. The script intake is the
// landing (tab 1); every other view is a projection over the same money object.
// Nav is lifecycle-ordered: THE BID (Script · Bid) → THE BUDGET (the rest),
// straight from the schema — "the bid is the budget before the project starts."
const {
  FK: PFK,
  FGlyph: PG,
  PROD: PC
} = window;
const {
  CPHead: PHead,
  cpKickerFaint: PKickF,
  cpSerif: PSerif
} = window;
const PUse = React.useState,
  PRef = React.useRef;
const SEV_COLOR = {
  high: '#B23A2E',
  med: PFK.AMBER,
  low: PFK.INK_MUTED,
  ok: '#1F8A5B'
};

// Message · Director — persistent on every production tab (the script cockpit
// embeds its own copy of this same conversation).
const PROD_MSG_DRAFT = 'Schedule won\u2019t let us shoot this night scene. Can it be changed?';
function ProductionMessagePanel({
  chat,
  onSend
}) {
  const bub = mine => ({
    alignSelf: mine ? 'flex-end' : 'flex-start',
    maxWidth: '90%',
    background: mine ? PFK.INK : PFK.PAPER,
    color: mine ? PFK.PAPER : PFK.INK,
    border: mine ? 'none' : `1px solid ${PFK.BORDER}`,
    borderRadius: 8,
    padding: '8px 11px',
    fontFamily: PFK.SANS,
    fontSize: 12.5,
    lineHeight: 1.4,
    marginBottom: 8
  });
  const who = mine => ({
    fontFamily: PFK.MONO,
    fontSize: 8.5,
    letterSpacing: '0.12em',
    textTransform: 'uppercase',
    color: mine ? 'rgba(255,255,255,0.6)' : PFK.INK_FAINT,
    marginBottom: 3
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      padding: '14px 16px',
      borderBottom: `1px solid ${PFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement(PG, {
    name: "chat",
    size: 13,
    style: {
      color: PFK.AMBER
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.MONO,
      fontSize: 10,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: PFK.AMBER
    }
  }, "Message \xB7 Director")), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      overflowY: 'auto',
      padding: '14px 16px',
      display: 'flex',
      flexDirection: 'column'
    }
  }, chat.messages.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: bub(m.from === 'producer')
  }, /*#__PURE__*/React.createElement("div", {
    style: who(m.from === 'producer')
  }, m.from === 'producer' ? 'You · Producer' : 'D. Parker · Director'), m.text)), chat.typing && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: PFK.SERIF,
      fontStyle: 'italic',
      fontSize: 12.5,
      color: PFK.INK_FAINT,
      marginBottom: 7
    }
  }, "Director is editing the script\u2026"), chat.messages.length === 0 && !chat.sent && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: PFK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: PFK.INK_FAINT,
      lineHeight: 1.5,
      marginBottom: 'auto'
    }
  }, "A direct line to the director. Send a question and watch the cut respond.")), !chat.sent && /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      borderTop: `1px solid ${PFK.BORDER}`,
      padding: '12px 16px',
      display: 'flex',
      flexDirection: 'column',
      gap: 9
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: PFK.SANS,
      fontSize: 12.5,
      color: PFK.INK,
      lineHeight: 1.4,
      padding: '9px 11px',
      background: PFK.PAPER_SOFT,
      border: `1px solid ${PFK.BORDER_STRONG}`,
      borderRadius: 8
    }
  }, PROD_MSG_DRAFT), /*#__PURE__*/React.createElement("button", {
    onClick: onSend,
    style: {
      alignSelf: 'flex-end',
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      cursor: 'pointer',
      background: PFK.AMBER,
      color: PFK.PAPER,
      border: 'none',
      borderRadius: 2,
      padding: '8px 15px',
      fontFamily: PFK.MONO,
      fontSize: 11,
      letterSpacing: '0.12em',
      textTransform: 'uppercase'
    }
  }, /*#__PURE__*/React.createElement(PG, {
    name: "send",
    size: 13
  }), " Send")));
}
function ProductionLane(props) {
  const {
    state,
    chat,
    flashId,
    onSend,
    onMode,
    onReset
  } = props;
  const [tab, setTab] = PUse('script');
  const [active, setActive] = PUse([]); // active scenario ids
  const [alertsOpen, setAlertsOpen] = PUse(false);
  const scrollRef = PRef(null);
  const act = PC.scenarios.filter(s => active.indexOf(s.id) >= 0);
  const savings = act.reduce((a, s) => a + s.budget, 0); // negative
  const daysDelta = act.reduce((a, s) => a + s.days, 0);
  const crewDelta = act.reduce((a, s) => a + s.crew, 0);
  const riskDelta = act.reduce((a, s) => a + s.risk, 0);
  function goTab(t) {
    setTab(t);
    setAlertsOpen(false);
    if (scrollRef.current) scrollRef.current.scrollTop = 0;
  }
  const ctx = {
    active,
    setActive,
    savings,
    daysDelta,
    crewDelta,
    riskDelta,
    goTab
  };

  // Pulled from window at render — these modules load after this one.
  const ScriptCockpit = window.ScriptCockpit;
  const BiddingView = window.BiddingView;
  const VIEWS = {
    dashboard: window.PDashboard,
    budget: window.PBudget,
    schedule: window.PSchedule,
    scenarios: window.PScenarios
  };
  const View = VIEWS[tab] || function () {
    return null;
  };
  const liveAlerts = PC.dashboard.alerts.filter(a => a.sev !== 'ok');
  const onScript = tab === 'script';

  // Lifecycle phase of the current tab → status read in the header.
  const curPhase = (PC.NAV.find(n => n.id === tab) || {}).phase;
  const phaseLabel = curPhase === 'bid' ? 'Bidding' : 'In production';
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      background: PFK.PAPER_SOFT,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      height: 60,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '0 32px',
      borderBottom: `1px solid ${PFK.BORDER}`,
      background: PFK.PAPER,
      position: 'relative',
      zIndex: 20,
      boxShadow: '0 1px 0 rgba(38,28,12,0.035)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.SERIF,
      fontStyle: 'italic',
      fontSize: 22,
      color: PFK.INK,
      letterSpacing: '0.01em'
    }
  }, "tenet"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.24em',
      textTransform: 'uppercase',
      color: PFK.AMBER,
      whiteSpace: 'nowrap'
    }
  }, "Production"), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 1,
      height: 17,
      background: PFK.BORDER_STRONG,
      display: 'inline-block',
      alignSelf: 'center'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: Object.assign({}, PSerif, {
      fontSize: 15,
      color: PFK.INK,
      whiteSpace: 'nowrap'
    })
  }, PC.project.title), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: PFK.INK_FAINT
    }
  }, PC.project.format, " \xB7 ", PC.project.code)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 22
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 7,
      fontFamily: PFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: PFK.INK_MUTED
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 6,
      height: 6,
      borderRadius: 999,
      background: curPhase === 'bid' ? PFK.AMBER : '#1F8A5B'
    }
  }), phaseLabel), onScript && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: `1px solid ${PFK.BORDER_STRONG}`,
      borderRadius: 3,
      overflow: 'hidden'
    }
  }, [30, 15].map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: m,
    onClick: () => onMode && onMode(m),
    style: {
      padding: '5px 12px',
      cursor: 'pointer',
      fontFamily: PFK.MONO,
      fontSize: 11,
      borderLeft: i ? `1px solid ${PFK.BORDER_STRONG}` : 'none',
      background: state.mode === m ? PFK.AMBER : PFK.PAPER,
      color: state.mode === m ? PFK.PAPER : PFK.INK_MUTED
    }
  }, ":", m))), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative'
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: () => setAlertsOpen(o => !o),
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      background: 'none',
      border: 0,
      cursor: 'pointer',
      padding: 0,
      color: alertsOpen ? PFK.AMBER : PFK.INK_MUTED
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 6,
      height: 6,
      borderRadius: 999,
      background: '#B23A2E'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.MONO,
      fontSize: 11,
      letterSpacing: '0.08em',
      color: 'inherit'
    }
  }, liveAlerts.length, " alerts")), alertsOpen && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 30,
      right: 0,
      width: 340,
      background: PFK.PAPER,
      border: `1px solid ${PFK.BORDER_STRONG}`,
      borderRadius: 6,
      boxShadow: '0 18px 48px rgba(0,0,0,0.14)',
      padding: 8,
      zIndex: 40
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, PKickF, {
      padding: '8px 10px 10px'
    })
  }, "Active alerts"), liveAlerts.map((a, i) => /*#__PURE__*/React.createElement("button", {
    key: i,
    onClick: () => goTab(a.tab),
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 12,
      width: '100%',
      textAlign: 'left',
      background: 'none',
      border: 0,
      borderTop: `1px solid ${PFK.BORDER}`,
      padding: '12px 10px',
      cursor: 'pointer'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: SEV_COLOR[a.sev],
      flex: '0 0 70px'
    }
  }, a.kind), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.SANS,
      fontSize: 13,
      color: PFK.INK,
      lineHeight: 1.4
    }
  }, a.text))))), /*#__PURE__*/React.createElement("button", {
    className: "reset-btn",
    onClick: onReset
  }, /*#__PURE__*/React.createElement(PG, {
    name: "arrow",
    size: 13,
    style: {
      transform: 'scaleX(-1)'
    }
  }), " Change lane"))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      height: 52,
      display: 'flex',
      alignItems: 'stretch',
      padding: '0 30px',
      borderBottom: `1px solid ${PFK.BORDER}`,
      background: PFK.PAPER,
      position: 'relative',
      zIndex: 10
    }
  }, PC.NAV.map((n, idx) => {
    const on = n.id === tab;
    const prev = PC.NAV[idx - 1];
    const newPhase = prev && prev.phase !== n.phase;
    return /*#__PURE__*/React.createElement(React.Fragment, {
      key: n.id
    }, /*#__PURE__*/React.createElement("button", {
      onClick: () => goTab(n.id),
      style: {
        position: 'relative',
        display: 'flex',
        alignItems: 'center',
        gap: 8,
        background: 'none',
        border: 0,
        borderBottom: on ? `2px solid ${PFK.AMBER}` : '2px solid transparent',
        cursor: 'pointer',
        padding: '0 15px',
        marginBottom: -1,
        fontFamily: PFK.MONO,
        fontSize: 11,
        letterSpacing: '0.13em',
        textTransform: 'uppercase',
        color: on ? PFK.INK : PFK.INK_FAINT
      }
    }, n.label, n.id === 'scenarios' && active.length > 0 && /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: PFK.MONO,
        fontSize: 9,
        color: PFK.PAPER,
        background: PFK.AMBER,
        borderRadius: 999,
        padding: '1px 6px'
      }
    }, active.length)));
  })), onScript ? ScriptCockpit ? /*#__PURE__*/React.createElement(ScriptCockpit, {
    state: state,
    chat: chat,
    flashId: flashId,
    onSend: onSend,
    goTab: goTab
  }) : null : /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 340px',
      minWidth: 0,
      minHeight: 0,
      borderRight: `1px solid ${PFK.BORDER}`,
      background: PFK.PAPER,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement(ProductionMessagePanel, {
    chat: chat,
    onSend: onSend
  })), /*#__PURE__*/React.createElement("div", {
    ref: scrollRef,
    style: {
      flex: 1,
      minHeight: 0,
      overflowY: 'auto',
      overflowX: 'hidden',
      background: PFK.PAPER_SOFT
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 1080,
      margin: '0 auto',
      padding: '36px 44px 64px'
    }
  }, tab === 'bid' ? /*#__PURE__*/React.createElement(BidView, {
    BiddingView: BiddingView
  }) : /*#__PURE__*/React.createElement(View, {
    ctx: ctx
  })))));
}

// Bid tab — approved bids on top; toggle the working bid below.
function BidView({
  BiddingView
}) {
  const [mode, setMode] = PUse('post'); // 'post' | 'overages' | 'new'
  const segBtn = (id, label) => /*#__PURE__*/React.createElement("div", {
    key: id,
    onClick: () => setMode(id),
    style: {
      padding: '7px 15px',
      cursor: 'pointer',
      whiteSpace: 'nowrap',
      fontFamily: PFK.MONO,
      fontSize: 11,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      borderLeft: id !== 'post' ? `1px solid ${PFK.BORDER_STRONG}` : 'none',
      background: mode === id ? PFK.AMBER : PFK.PAPER,
      color: mode === id ? PFK.PAPER : PFK.INK_MUTED
    }
  }, label);
  const empty = (title, line) => /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px dashed ${PFK.BORDER_STRONG}`,
      borderRadius: 6,
      background: PFK.PAPER,
      padding: '64px 32px',
      textAlign: 'center'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: PFK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: PFK.INK_FAINT,
      marginBottom: 12
    }
  }, title), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: PFK.SERIF,
      fontStyle: 'italic',
      fontSize: 18,
      color: PFK.INK_MUTED,
      maxWidth: 460,
      margin: '0 auto',
      lineHeight: 1.5
    }
  }, line));
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(PHead, {
    kicker: "Bid",
    lede: "Every flagged driver lands on a line. Slide the deal terms and the grand total moves \u2014 this is the bid before the project starts."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 22
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, PKickF, {
      marginBottom: 12
    })
  }, "Approved bids"), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${PFK.BORDER}`,
      borderRadius: 6,
      background: PFK.PAPER,
      padding: '16px 20px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 12,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: Object.assign({}, PSerif, {
      fontSize: 17,
      color: PFK.INK
    })
  }, "Master bid"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.MONO,
      fontSize: 10.5,
      color: PFK.INK_FAINT
    }
  }, PC.project.agency, " \xB7 ", PC.project.code), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.MONO,
      fontSize: 9,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: '#1F8A5B',
      border: '1px solid rgba(31,138,91,0.4)',
      background: 'rgba(31,138,91,0.08)',
      borderRadius: 2,
      padding: '2px 8px'
    }
  }, "Approved")), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.SANS,
      fontWeight: 300,
      fontSize: 24,
      color: PFK.INK,
      fontVariantNumeric: 'tabular-nums'
    }
  }, PC.money(PC.dashboard.budget.approved)))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      marginBottom: 18,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: `1px solid ${PFK.BORDER_STRONG}`,
      borderRadius: 3,
      overflow: 'hidden'
    }
  }, segBtn('post', 'Post bid'), segBtn('overages', 'Overages'), segBtn('new', 'New bid')), ['Running overage', 'Running overage'].map((t, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      padding: '7px 14px',
      borderRadius: 3,
      border: `1px dashed ${PFK.BORDER}`,
      fontFamily: PFK.MONO,
      fontSize: 10.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: PFK.INK_FAINT,
      opacity: 0.5,
      cursor: 'not-allowed',
      whiteSpace: 'nowrap'
    }
  }, t))), mode === 'post' ? /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${PFK.BORDER}`,
      borderRadius: 6,
      background: PFK.PAPER,
      overflow: 'hidden'
    }
  }, BiddingView ? /*#__PURE__*/React.createElement(BiddingView, null) : null) : mode === 'overages' ? empty('Overages', 'No overages logged yet. Once the project is in production, every line that runs over its approved figure surfaces here.') : empty('New bid', 'Start a fresh bid from this project\u2019s scope — or branch the approved master to model a different cut.'));
}

// Kept for back-compat with any external reference.
const ProductionPortal = ProductionLane;
Object.assign(window, {
  ProductionLane,
  ProductionPortal,
  SEV_COLOR
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/production-shell.jsx", error: String((e && e.message) || e) }); }

// flow/production-tabs.jsx
try { (() => {
/* global React */
// TENET Production Portal — Shots, Schedule, Resources, Risks.
const {
  FK: XFK,
  FGlyph: XG,
  PROD: XC
} = window;
const {
  CPanel: XP,
  CPStat: XStat,
  CPBar: XBar,
  CPChip: XChip,
  CPHead: XHead,
  cpKickerFaint: XKickF,
  RISK_COLOR: XRISK
} = window;
const XUse = React.useState;
const RES_STATE = {
  wrapped: {
    c: '#9A9A9A',
    t: 'Wrapped'
  },
  active: {
    c: '#BA7517',
    t: 'Active'
  },
  pending: {
    c: '#9A9A9A',
    t: 'Pending'
  }
};

/* ══════════════════════════════════════════════════════════
   SHOTS — cost & complexity, scene by scene
   ══════════════════════════════════════════════════════════ */
function PShots({
  ctx
}) {
  const [sel, setSel] = XUse(XC.shots[0].id);
  const s = XC.shots.find(x => x.id === sel);
  const maxDriver = Math.max.apply(null, XC.topDrivers.map(d => d.amt));
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(XHead, {
    kicker: "Shots",
    title: "Cost and complexity, shot by shot",
    lede: "Every setup carries a complexity and a cost score \u2014 and the drivers behind both."
  }), /*#__PURE__*/React.createElement(XP, {
    label: "Primary cost drivers",
    title: "Across the production",
    style: {
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(5, 1fr)',
      gap: 18
    }
  }, XC.topDrivers.map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: i
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 14,
      color: XFK.INK,
      fontVariantNumeric: 'tabular-nums'
    }
  }, XC.money(d.amt)), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 8,
      marginBottom: 8
    }
  }, /*#__PURE__*/React.createElement(XBar, {
    pct: d.amt / maxDriver * 100,
    color: XFK.AMBER_200
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SANS,
      fontSize: 12.5,
      color: XFK.INK_MUTED,
      lineHeight: 1.3
    }
  }, d.label))))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 360px',
      gap: 24,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${XFK.BORDER}`,
      borderRadius: 6,
      overflow: 'hidden',
      background: XFK.PAPER
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '40px 1fr 92px 92px',
      gap: 12,
      padding: '13px 18px',
      borderBottom: `1px solid ${XFK.BORDER}`
    }
  }, ['#', 'Setup', 'Complexity', 'Cost'].map((h, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      fontFamily: XFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: XFK.INK_FAINT,
      textAlign: i >= 2 ? 'right' : 'left'
    }
  }, h))), XC.shots.map((sh, i) => {
    const on = sh.id === sel;
    return /*#__PURE__*/React.createElement("button", {
      key: sh.id,
      onClick: () => setSel(sh.id),
      style: {
        width: '100%',
        display: 'grid',
        gridTemplateColumns: '40px 1fr 92px 92px',
        gap: 12,
        padding: '14px 18px',
        alignItems: 'center',
        cursor: 'pointer',
        textAlign: 'left',
        background: on ? XFK.PAPER_SOFT : XFK.PAPER,
        border: 0,
        borderTop: i ? `1px solid ${XFK.BORDER}` : 'none',
        borderLeft: `2px solid ${on ? XFK.AMBER : 'transparent'}`
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: XFK.MONO,
        fontSize: 12,
        color: XFK.INK_FAINT
      }
    }, String(sh.n).padStart(2, '0')), /*#__PURE__*/React.createElement("span", {
      style: {
        minWidth: 0
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: XFK.SANS,
        fontSize: 14,
        color: XFK.INK,
        display: 'flex',
        alignItems: 'center',
        gap: 8
      }
    }, sh.loc, sh.night && /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: XFK.MONO,
        fontSize: 8.5,
        letterSpacing: '0.1em',
        color: XFK.AMBER,
        border: `1px solid ${XFK.AMBER}`,
        borderRadius: 2,
        padding: '1px 4px'
      }
    }, "NIGHT")), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: XFK.MONO,
        fontSize: 10,
        letterSpacing: '0.08em',
        color: XFK.INK_FAINT
      }
    }, sh.tod, " \xB7 ", sh.shotCount, " shots")), /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 6,
        justifyContent: 'flex-end'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        width: 34
      }
    }, /*#__PURE__*/React.createElement(XBar, {
      pct: sh.complexity,
      color: sh.complexity > 55 ? '#B23A2E' : XFK.INK,
      h: 4
    })), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: XFK.MONO,
        fontSize: 11,
        color: XFK.INK,
        width: 22,
        textAlign: 'right'
      }
    }, sh.complexity)), /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 6,
        justifyContent: 'flex-end'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        width: 34
      }
    }, /*#__PURE__*/React.createElement(XBar, {
      pct: sh.costScore,
      color: XFK.AMBER,
      h: 4
    })), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: XFK.MONO,
        fontSize: 11,
        color: XFK.INK,
        width: 22,
        textAlign: 'right'
      }
    }, sh.costScore)));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${XFK.BORDER_STRONG}`,
      borderRadius: 6,
      background: XFK.PAPER,
      overflow: 'hidden',
      position: 'sticky',
      top: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '20px 22px',
      borderBottom: `1px solid ${XFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: XFK.AMBER
    }
  }, "Setup ", String(s.n).padStart(2, '0')), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 10,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: XFK.INK_FAINT
    }
  }, s.status)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SANS,
      fontWeight: 300,
      fontSize: 22,
      marginTop: 8,
      color: XFK.INK
    }
  }, s.loc), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SERIF,
      fontStyle: 'italic',
      fontSize: 13.5,
      color: XFK.INK_MUTED,
      marginTop: 4,
      lineHeight: 1.4
    }
  }, s.desc)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 1,
      background: XFK.BORDER
    }
  }, /*#__PURE__*/React.createElement(MiniStat, {
    label: "Est. cost",
    value: XC.money(s.cost)
  }), /*#__PURE__*/React.createElement(MiniStat, {
    label: "Complexity",
    value: s.complexity + ' / 100',
    tone: s.complexity > 55 ? '#B23A2E' : XFK.INK
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 22px',
      borderTop: `1px solid ${XFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, XKickF, {
      marginBottom: 14
    })
  }, "Complexity factors"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 9
    }
  }, s.factors.map((f, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'grid',
      gridTemplateColumns: '92px 1fr 18px',
      gap: 10,
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.SANS,
      fontSize: 11.5,
      color: XFK.INK_MUTED,
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, f.name), /*#__PURE__*/React.createElement(XBar, {
    pct: f.lv / 7 * 100,
    color: f.lv >= 5 ? XFK.AMBER : XFK.INK_FAINT,
    h: 5
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 10,
      color: XFK.INK_FAINT,
      textAlign: 'right'
    }
  }, f.lv))))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 22px',
      borderTop: `1px solid ${XFK.BORDER}`,
      background: XFK.PAPER_SOFT
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, XKickF, {
      marginBottom: 10
    })
  }, "Primary cost drivers"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 7
    }
  }, s.drivers.map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 9
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 5,
      height: 5,
      borderRadius: 999,
      background: XFK.AMBER,
      flexShrink: 0
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.SANS,
      fontSize: 13,
      color: XFK.INK
    }
  }, d))))))));
}
function MiniStat({
  label,
  value,
  tone
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      background: XFK.PAPER,
      padding: '16px 22px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: XKickF
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SANS,
      fontWeight: 300,
      fontSize: 24,
      marginTop: 6,
      color: tone || XFK.INK,
      fontVariantNumeric: 'tabular-nums'
    }
  }, value));
}

/* ══════════════════════════════════════════════════════════
   SCHEDULE
   ══════════════════════════════════════════════════════════ */
function PSchedule({
  ctx
}) {
  const sc = XC.schedule;
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(XHead, {
    kicker: "Schedule",
    lede: "The shoot wrapped on plan. Here is how the days ran, and what carried the risk."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(4, 1fr)',
      gap: 16,
      marginBottom: 28
    }
  }, /*#__PURE__*/React.createElement(XStat, {
    label: "Shoot days",
    value: sc.shootDays,
    sub: "Principal photography"
  }), /*#__PURE__*/React.createElement(XStat, {
    label: "Days complete",
    value: sc.daysComplete + ' / ' + sc.shootDays,
    sub: "Wrapped",
    tone: "#1F8A5B"
  }), /*#__PURE__*/React.createElement(XStat, {
    label: "Days remaining",
    value: sc.daysRemaining,
    sub: "In post now"
  }), /*#__PURE__*/React.createElement(XStat, {
    label: "Forecast wrap",
    value: sc.forecastWrap,
    sub: "Hit on schedule"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1.5fr 1fr',
      gap: 16,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement(XP, {
    label: "Day view",
    title: "How the days ran"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, sc.days.map(d => /*#__PURE__*/React.createElement("div", {
    key: d.id,
    style: {
      border: `1px solid ${XFK.BORDER}`,
      borderRadius: 4,
      padding: '16px 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.SANS,
      fontWeight: 300,
      fontSize: 20,
      color: XFK.INK
    }
  }, d.label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 11,
      color: XFK.INK_FAINT
    }
  }, d.date), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: XFK.INK_MUTED
    }
  }, d.locs.join(' \u2192 '))), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: '#1F8A5B'
    }
  }, "Complete")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(4, 1fr)',
      gap: 12,
      marginBottom: d.risks.length ? 12 : 0
    }
  }, [['Pages', d.pages], ['Shots', d.shots], ['Crew', d.crew], ['Hours', d.hours]].map(([k, v]) => /*#__PURE__*/React.createElement("div", {
    key: k
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 9,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: XFK.INK_FAINT
    }
  }, k), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SANS,
      fontSize: 17,
      color: XFK.INK,
      marginTop: 3
    }
  }, v)))), d.risks.length > 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 7
    }
  }, d.risks.map((r, i) => /*#__PURE__*/React.createElement(XChip, {
    key: i,
    on: true,
    tone: XFK.AMBER,
    dot: true
  }, r))))))), /*#__PURE__*/React.createElement(XP, {
    label: "Risk factors",
    title: "What stressed the days",
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, sc.riskFactors.map((r, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 12,
      padding: '13px 0',
      borderTop: i ? `1px solid ${XFK.BORDER}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SANS,
      fontSize: 14,
      color: XFK.INK
    }
  }, r.label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SANS,
      fontSize: 12.5,
      color: XFK.INK_MUTED,
      marginTop: 1
    }
  }, r.note)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 16,
      color: XFK.AMBER,
      flexShrink: 0
    }
  }, r.count)))))));
}

/* ══════════════════════════════════════════════════════════
   RESOURCES
   ══════════════════════════════════════════════════════════ */
function PResources({
  ctx
}) {
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(XHead, {
    kicker: "Resources",
    lede: "Every person, place, and piece of gear the project switched on \u2014 and where it stands."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 16
    }
  }, XC.resources.map(g => /*#__PURE__*/React.createElement(XP, {
    key: g.group,
    label: g.group,
    title: null,
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, g.items.map((it, i) => {
    const st = RES_STATE[it.state];
    return /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        gap: 12,
        padding: '13px 0',
        borderTop: i ? `1px solid ${XFK.BORDER}` : 'none'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        minWidth: 0
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: XFK.SANS,
        fontSize: 14,
        color: XFK.INK
      }
    }, it.name), /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: XFK.SANS,
        fontSize: 12.5,
        color: XFK.INK_MUTED,
        marginTop: 1
      }
    }, it.detail)), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 12,
        flexShrink: 0
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: XFK.SANS,
        fontWeight: 300,
        fontSize: 20,
        color: XFK.INK
      }
    }, it.count), /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'inline-flex',
        alignItems: 'center',
        gap: 6,
        fontFamily: XFK.MONO,
        fontSize: 9,
        letterSpacing: '0.12em',
        textTransform: 'uppercase',
        color: st.c
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        width: 5,
        height: 5,
        borderRadius: 999,
        background: st.c
      }
    }), st.t)));
  }))))));
}

/* ══════════════════════════════════════════════════════════
   RISKS
   ══════════════════════════════════════════════════════════ */
function PRisks({
  ctx
}) {
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(XHead, {
    kicker: "Risks",
    title: "Threats before they happen",
    lede: "Each risk priced in dollars and days \u2014 the producer reads exposure, not vibes."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 16
    }
  }, XC.risks.map(r => /*#__PURE__*/React.createElement("div", {
    key: r.id,
    style: {
      border: `1px solid ${XFK.BORDER}`,
      borderRadius: 6,
      background: XFK.PAPER,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 22px 16px',
      borderBottom: `1px solid ${XFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: XFK.AMBER,
      whiteSpace: 'nowrap'
    }
  }, r.type, " risk"), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 7
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: XFK.INK_FAINT
    }
  }, "Prob"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.SANS,
      fontSize: 13,
      color: XRISK[r.prob]
    }
  }, r.prob))), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SANS,
      fontWeight: 300,
      fontSize: 19,
      lineHeight: 1.2,
      color: XFK.INK
    }
  }, r.title)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr 1fr',
      gap: 1,
      background: XFK.BORDER
    }
  }, [['Exposure', r.exposure, XFK.INK], ['Cost impact', r.cost ? '+' + XC.money(r.cost) : '\u2014', r.cost ? '#B23A2E' : XFK.INK_FAINT], ['Schedule', r.days ? '+' + r.days + 'd' : '\u2014', r.days ? XFK.AMBER : XFK.INK_FAINT]].map(([k, v, c], i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      background: XFK.PAPER,
      padding: '14px 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: XFK.INK_FAINT,
      marginBottom: 6
    }
  }, k), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SANS,
      fontSize: 15,
      color: c,
      fontVariantNumeric: 'tabular-nums'
    }
  }, v)))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 22px',
      borderTop: `1px solid ${XFK.BORDER}`,
      background: XFK.PAPER_SOFT
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, XKickF, {
      marginBottom: 8
    })
  }, "Mitigation"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SANS,
      fontSize: 13.5,
      color: XFK.INK,
      lineHeight: 1.5
    }
  }, r.mitigation))))));
}
Object.assign(window, {
  PShots,
  PSchedule,
  PResources,
  PRisks
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/production-tabs.jsx", error: String((e && e.message) || e) }); }

// flow/scaffolds.jsx
try { (() => {
/* global React, FK, FGlyph, FLOW, FRAMEWORK, useFTween */
// The left panel — each lane's TOP 10. Producer carries dollars + the chat.

const dScaff = {
  root: {
    height: '100%',
    display: 'flex',
    flexDirection: 'column',
    background: FK.PAPER,
    borderRight: `1px solid ${FK.BORDER}`,
    boxSizing: 'border-box',
    overflow: 'hidden'
  },
  head: {
    padding: '22px 30px 16px',
    borderBottom: `1px solid ${FK.BORDER}`
  },
  kicker: {
    fontFamily: FK.MONO,
    fontSize: 10,
    letterSpacing: '0.2em',
    textTransform: 'uppercase',
    color: FK.AMBER,
    whiteSpace: 'nowrap'
  },
  title: {
    fontFamily: FK.SERIF,
    fontStyle: 'italic',
    fontSize: 22,
    color: FK.INK,
    lineHeight: 1.1,
    marginTop: 8
  },
  sub: {
    fontFamily: FK.MONO,
    fontSize: 10,
    letterSpacing: '0.16em',
    textTransform: 'uppercase',
    color: FK.INK_FAINT,
    marginTop: 8
  },
  list: {
    flex: 1,
    overflow: 'auto',
    padding: '8px 0'
  },
  row: on => ({
    display: 'flex',
    alignItems: 'center',
    gap: 14,
    padding: '11px 30px',
    cursor: 'pointer',
    background: on ? FK.AMBER_50 : 'transparent',
    boxShadow: on ? `inset 3px 0 0 ${FK.AMBER}` : 'none'
  }),
  n: on => ({
    fontFamily: FK.MONO,
    fontSize: 11,
    color: on ? FK.AMBER : FK.INK_FAINT,
    width: 20
  }),
  name: on => ({
    fontFamily: FK.SERIF,
    fontStyle: 'italic',
    fontSize: 18,
    color: on ? FK.AMBER_800 : FK.INK,
    flex: 1
  }),
  count: {
    fontFamily: FK.MONO,
    fontSize: 10,
    color: FK.INK_FAINT
  }
};
function ScaffoldDirector({
  cat,
  onCat
}) {
  const S = dScaff;
  return /*#__PURE__*/React.createElement("div", {
    style: S.root
  }, /*#__PURE__*/React.createElement("div", {
    style: S.head
  }, /*#__PURE__*/React.createElement("div", {
    style: S.kicker
  }, "Director \xB7 top 10"), /*#__PURE__*/React.createElement("div", {
    style: S.title
  }, "Creative variables"), /*#__PURE__*/React.createElement("div", {
    style: S.sub
  }, "The ten that decide whether it lands")), /*#__PURE__*/React.createElement("div", {
    style: S.list
  }, FRAMEWORK.DIRECTOR.map(c => {
    const on = cat === c.id;
    return /*#__PURE__*/React.createElement("div", {
      key: c.id,
      style: S.row(on),
      onClick: () => onCat(c.id)
    }, /*#__PURE__*/React.createElement("span", {
      style: S.n(on)
    }, String(c.n).padStart(2, '0')), /*#__PURE__*/React.createElement("span", {
      style: S.name(on)
    }, c.name), /*#__PURE__*/React.createElement("span", {
      style: S.count
    }, on ? '6 subs ›' : '6'));
  })));
}
const pScaff = {
  root: {
    height: '100%',
    display: 'flex',
    flexDirection: 'column',
    background: FK.PAPER,
    borderRight: `1px solid ${FK.BORDER}`,
    boxSizing: 'border-box',
    overflow: 'hidden'
  },
  top: {
    padding: '20px 30px 16px',
    borderBottom: `1px solid ${FK.BORDER}`
  },
  kicker: {
    fontFamily: FK.MONO,
    fontSize: 10,
    letterSpacing: '0.2em',
    textTransform: 'uppercase',
    color: FK.AMBER,
    whiteSpace: 'nowrap'
  },
  totalLabel: {
    fontFamily: FK.MONO,
    fontSize: 10,
    letterSpacing: '0.18em',
    textTransform: 'uppercase',
    color: FK.INK_FAINT,
    marginTop: 12
  },
  total: {
    fontFamily: FK.SANS,
    fontWeight: 300,
    fontSize: 44,
    color: FK.INK,
    lineHeight: 1,
    letterSpacing: '-0.02em',
    marginTop: 4
  },
  list: {
    flex: 1,
    overflow: 'auto'
  },
  row: (on, flash) => ({
    display: 'flex',
    alignItems: 'center',
    gap: 12,
    padding: '9px 30px',
    cursor: 'pointer',
    borderBottom: `1px solid ${FK.BORDER}`,
    background: on || flash ? FK.AMBER_50 : 'transparent',
    boxShadow: on ? `inset 3px 0 0 ${FK.AMBER}` : 'none'
  }),
  n: {
    fontFamily: FK.MONO,
    fontSize: 10,
    color: FK.INK_FAINT,
    width: 18
  },
  name: on => ({
    fontFamily: FK.SERIF,
    fontStyle: 'italic',
    fontSize: 16,
    color: on ? FK.AMBER_800 : FK.INK,
    flex: 1
  }),
  ot: {
    fontFamily: FK.MONO,
    fontSize: 9,
    letterSpacing: '0.1em',
    textTransform: 'uppercase',
    color: FK.AMBER_800,
    padding: '2px 6px',
    borderRadius: 2,
    background: FK.AMBER_50,
    border: `1px solid ${FK.AMBER}`
  },
  cost: {
    fontFamily: FK.MONO,
    fontSize: 12,
    color: FK.INK,
    width: 74,
    textAlign: 'right'
  },
  chat: {
    borderTop: `1px solid ${FK.BORDER}`,
    background: FK.PAPER_SOFT,
    padding: '14px 24px 18px',
    flexShrink: 0
  },
  chatHead: {
    fontFamily: FK.MONO,
    fontSize: 10,
    letterSpacing: '0.18em',
    textTransform: 'uppercase',
    color: FK.AMBER,
    marginBottom: 10,
    display: 'flex',
    alignItems: 'center',
    gap: 8,
    whiteSpace: 'nowrap'
  },
  bubble: mine => ({
    alignSelf: mine ? 'flex-end' : 'flex-start',
    maxWidth: '88%',
    background: mine ? FK.INK : FK.PAPER,
    color: mine ? FK.PAPER : FK.INK,
    border: mine ? 'none' : `1px solid ${FK.BORDER}`,
    borderRadius: 8,
    padding: '8px 11px',
    fontFamily: FK.SANS,
    fontSize: 12.5,
    lineHeight: 1.4,
    marginBottom: 7
  }),
  who: mine => ({
    fontFamily: FK.MONO,
    fontSize: 9,
    letterSpacing: '0.12em',
    textTransform: 'uppercase',
    color: mine ? 'rgba(255,255,255,0.6)' : FK.INK_FAINT,
    marginBottom: 3
  }),
  composer: {
    display: 'flex',
    flexDirection: 'column',
    gap: 9
  },
  draft: {
    fontFamily: FK.SANS,
    fontSize: 12.5,
    color: FK.INK,
    lineHeight: 1.4,
    padding: '9px 11px',
    background: FK.PAPER,
    border: `1px solid ${FK.BORDER_STRONG}`,
    borderRadius: 8
  },
  send: {
    alignSelf: 'flex-end',
    display: 'inline-flex',
    alignItems: 'center',
    gap: 8,
    cursor: 'pointer',
    background: FK.AMBER,
    color: FK.PAPER,
    border: 'none',
    borderRadius: 2,
    padding: '8px 15px',
    fontFamily: FK.MONO,
    fontSize: 11,
    letterSpacing: '0.12em',
    textTransform: 'uppercase'
  },
  typing: {
    fontFamily: FK.SERIF,
    fontStyle: 'italic',
    fontSize: 12.5,
    color: FK.INK_FAINT,
    marginBottom: 7
  }
};
const DRAFT_MSG = 'Schedule won\u2019t let us shoot this night scene. Can it be changed?';
function ScaffoldProducer({
  state,
  cat,
  onCat,
  chat,
  onSend,
  catFlash
}) {
  const S = pScaff;
  const total = useFTween(FRAMEWORK.producerTotal(state), 700);
  return /*#__PURE__*/React.createElement("div", {
    style: S.root
  }, /*#__PURE__*/React.createElement("div", {
    style: S.top
  }, /*#__PURE__*/React.createElement("div", {
    style: S.kicker
  }, "Producer \xB7 top 10"), /*#__PURE__*/React.createElement("div", {
    style: S.totalLabel
  }, "Total cost"), /*#__PURE__*/React.createElement("div", {
    style: S.total
  }, FLOW.fmtMoney(total))), /*#__PURE__*/React.createElement("div", {
    style: S.list
  }, FRAMEWORK.PRODUCER.map(c => {
    const on = cat === c.id;
    const cost = FRAMEWORK.categoryCost(c, state);
    const ot = c.nightVar && FRAMEWORK.nightActive(state);
    return /*#__PURE__*/React.createElement("div", {
      key: c.id,
      style: S.row(on, catFlash === c.id),
      onClick: () => onCat(c.id)
    }, /*#__PURE__*/React.createElement("span", {
      style: S.n
    }, String(c.n).padStart(2, '0')), /*#__PURE__*/React.createElement("span", {
      style: S.name(on)
    }, c.name), ot && /*#__PURE__*/React.createElement("span", {
      style: S.ot
    }, "+OT"), /*#__PURE__*/React.createElement("span", {
      style: S.cost
    }, FLOW.fmtMoney(cost)));
  })), /*#__PURE__*/React.createElement("div", {
    style: S.chat
  }, /*#__PURE__*/React.createElement("div", {
    style: S.chatHead
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "chat",
    size: 14
  }), " Message \xB7 Director"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, chat.messages.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: S.bubble(m.from === 'producer')
  }, /*#__PURE__*/React.createElement("div", {
    style: S.who(m.from === 'producer')
  }, m.from === 'producer' ? 'You · Producer' : 'D. Parker · Director'), m.text)), chat.typing && /*#__PURE__*/React.createElement("div", {
    style: S.typing
  }, "Director is editing the script\\u2026")), !chat.sent && /*#__PURE__*/React.createElement("div", {
    style: S.composer
  }, /*#__PURE__*/React.createElement("div", {
    style: S.draft
  }, DRAFT_MSG), /*#__PURE__*/React.createElement("button", {
    style: S.send,
    onClick: onSend
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "send",
    size: 13
  }), " Send"))));
}
window.ScaffoldDirector = ScaffoldDirector;
window.ScaffoldProducer = ScaffoldProducer;
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/scaffolds.jsx", error: String((e && e.message) || e) }); }

// flow/screens.jsx
try { (() => {
/* global React, FK, FGlyph */
// Entry screens — lane select + upload gate, in the First Experience register.

const laneStyles = {
  wrap: {
    position: 'absolute',
    inset: 0,
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'center',
    background: FK.PAPER
  },
  kicker: {
    fontFamily: FK.MONO,
    fontSize: 12,
    letterSpacing: '0.28em',
    textTransform: 'uppercase',
    color: FK.AMBER,
    marginBottom: 64,
    whiteSpace: 'nowrap'
  },
  row: {
    display: 'flex',
    alignItems: 'stretch',
    gap: 0
  },
  opt: hover => ({
    width: 340,
    padding: '8px 40px',
    textAlign: 'center',
    cursor: 'pointer',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center'
  }),
  divider: {
    width: 1,
    background: FK.BORDER,
    alignSelf: 'stretch',
    margin: '6px 0'
  },
  label: hover => ({
    fontFamily: FK.SANS,
    fontWeight: 300,
    fontSize: 42,
    letterSpacing: '0.04em',
    color: hover ? FK.AMBER : FK.INK,
    lineHeight: 1,
    whiteSpace: 'nowrap'
  }),
  rule: hover => ({
    width: 40,
    height: 1,
    background: hover ? FK.AMBER : FK.BORDER_STRONG,
    margin: '22px 0'
  }),
  exp: {
    fontFamily: FK.SERIF,
    fontStyle: 'italic',
    fontSize: 18,
    color: FK.INK_MUTED,
    lineHeight: 1.4,
    maxWidth: 240
  },
  brief: {
    fontFamily: FK.SERIF,
    fontStyle: 'italic',
    fontSize: 15,
    color: FK.INK_FAINT,
    marginTop: 64,
    whiteSpace: 'nowrap'
  }
};
function LaneSelect({
  onPick
}) {
  const S = laneStyles;
  const [hover, setHover] = React.useState(null);
  const opts = [{
    id: 'director',
    label: 'Creative',
    exp: 'Shape the work. Script, shots, and the craft of the cut.'
  }, {
    id: 'producer',
    label: 'Production',
    exp: 'Hold the line. Cost, schedule, and every change in dollars.'
  }, {
    id: 'client',
    label: 'Client',
    exp: 'Watch it take shape. Approve before a single frame is shot.'
  }];
  return /*#__PURE__*/React.createElement("div", {
    style: S.wrap
  }, /*#__PURE__*/React.createElement("div", {
    style: S.kicker
  }, "Pick a lane"), /*#__PURE__*/React.createElement("div", {
    style: S.row
  }, opts.map((o, i) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: o.id
  }, i > 0 && /*#__PURE__*/React.createElement("div", {
    style: S.divider
  }), /*#__PURE__*/React.createElement("div", {
    style: S.opt(hover === o.id),
    onMouseEnter: () => setHover(o.id),
    onMouseLeave: () => setHover(null),
    onClick: () => onPick(o.id)
  }, /*#__PURE__*/React.createElement("div", {
    style: S.label(hover === o.id)
  }, o.label), /*#__PURE__*/React.createElement("div", {
    style: S.rule(hover === o.id)
  }), /*#__PURE__*/React.createElement("div", {
    style: S.exp
  }, o.exp))))), /*#__PURE__*/React.createElement("div", {
    style: S.brief
  }, "Three seats, one project. Choose where you sit."));
}
const upStyles = {
  wrap: {
    position: 'absolute',
    inset: 0,
    background: FK.PAPER
  },
  center: {
    position: 'absolute',
    left: '50%',
    top: '46%',
    transform: 'translate(-50%,-50%)',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center'
  },
  icons: {
    display: 'flex',
    alignItems: 'flex-start',
    gap: 92,
    marginBottom: 44
  },
  iconCol: active => ({
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    gap: 16,
    cursor: active ? 'pointer' : 'default',
    opacity: active ? 1 : 0.32
  }),
  iconLabel: active => ({
    fontFamily: FK.MONO,
    fontSize: 10,
    letterSpacing: '0.18em',
    textTransform: 'uppercase',
    color: active ? FK.AMBER : FK.INK_FAINT
  }),
  prompt: {
    fontFamily: FK.SERIF,
    fontStyle: 'italic',
    fontSize: 19,
    color: FK.INK_MUTED
  },
  ring: active => ({
    width: 92,
    height: 92,
    borderRadius: '50%',
    border: `1px solid ${active ? FK.AMBER : FK.BORDER}`,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    color: active ? FK.AMBER : FK.INK_FAINT,
    background: active ? FK.AMBER_50 : FK.PAPER
  }),
  fileRow: {
    display: 'flex',
    alignItems: 'center',
    gap: 12,
    marginTop: 6,
    fontFamily: FK.MONO,
    fontSize: 13,
    color: FK.INK
  },
  bar: {
    width: 320,
    height: 2,
    background: FK.BORDER,
    marginTop: 22,
    position: 'relative'
  },
  barFill: p => ({
    position: 'absolute',
    left: 0,
    top: 0,
    height: 2,
    width: p + '%',
    background: FK.AMBER
  })
};
function UploadGate({
  lane,
  onUploaded
}) {
  const S = upStyles;
  const [phase, setPhase] = React.useState('idle'); // idle | uploading
  const [prog, setProg] = React.useState(0);
  const isClient = lane === 'client';
  function startUpload() {
    if (phase !== 'idle') return;
    setPhase('uploading');
    let p = 0;
    const id = setInterval(() => {
      p = Math.min(100, p + 12);
      setProg(p);
      if (p >= 100) clearInterval(id);
    }, 90);
    // Completion is one timer, not dependent on the bar's many ticks.
    setTimeout(onUploaded, 1400);
  }

  // Client seats don't upload a script — they review the cut. The active
  // action becomes Review (straight through to the cut); Upload is greyed.
  const tools = isClient ? [{
    id: 'mic',
    label: 'Record',
    active: false
  }, {
    id: 'phone',
    label: 'Call',
    active: false
  }, {
    id: 'upload',
    label: 'Upload',
    active: false
  }, {
    id: 'review',
    label: 'Review',
    active: true,
    action: onUploaded
  }] : [{
    id: 'mic',
    label: 'Record',
    active: false
  }, {
    id: 'phone',
    label: 'Call',
    active: false
  }, {
    id: 'upload',
    label: 'Upload',
    active: true,
    action: startUpload
  }, {
    id: 'video',
    label: 'Video call',
    active: false
  }];
  return /*#__PURE__*/React.createElement("div", {
    style: S.wrap
  }, /*#__PURE__*/React.createElement("div", {
    style: S.center
  }, /*#__PURE__*/React.createElement("div", {
    style: S.icons
  }, tools.map(t => /*#__PURE__*/React.createElement("div", {
    key: t.id,
    style: S.iconCol(t.active),
    onClick: t.active ? t.action : undefined
  }, /*#__PURE__*/React.createElement("div", {
    style: S.ring(t.active)
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: t.id,
    size: 32
  })), /*#__PURE__*/React.createElement("div", {
    style: S.iconLabel(t.active)
  }, t.label)))), phase === 'idle' && /*#__PURE__*/React.createElement("div", {
    style: S.prompt
  }, isClient ? 'Open the latest cut to review.' : 'Upload a script to begin.'), phase === 'uploading' && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: S.fileRow
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "script",
    size: 16,
    style: {
      color: FK.AMBER
    }
  }), "A_Brief_History_of_Selling_Ideas_v3.fdx"), /*#__PURE__*/React.createElement("div", {
    style: S.bar
  }, /*#__PURE__*/React.createElement("div", {
    style: S.barFill(prog)
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      marginTop: 12
    }
  }, prog < 100 ? 'Reading scenes\u2026' : 'Building scaffold\u2026'))));
}
window.LaneSelect = LaneSelect;
window.UploadGate = UploadGate;
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/screens.jsx", error: String((e && e.message) || e) }); }

// flow/ui.jsx
try { (() => {
/* global React */
// TENET flow — shared UI: First Experience tokens, line icons, tween, scaling.

const FK = {
  INK: '#1A1A1A',
  INK_MUTED: '#6B6B6B',
  INK_FAINT: '#9A9A9A',
  PAPER: '#FFFFFF',
  PAPER_RAISED: '#FBFBFA',
  PAPER_SOFT: '#F4F4F2',
  PAPER_DIM: '#ECECEA',
  AMBER: '#BA7517',
  AMBER_50: '#FAEEDA',
  AMBER_200: '#EF9F27',
  AMBER_800: '#854F0B',
  BORDER: 'rgba(0,0,0,0.10)',
  BORDER_STRONG: 'rgba(0,0,0,0.18)',
  SHADOW_LIFT: '0 1px 0 rgba(20,20,22,0.02), 0 12px 34px -16px rgba(20,20,30,0.20)',
  SHADOW_POP: '0 28px 64px -22px rgba(20,20,30,0.28)',
  OK: '#1F8A5B',
  DANGER: '#B23A2E',
  SANS: '"Brown","Avenir Next","Helvetica Neue",system-ui,sans-serif',
  SERIF: '"Source Serif 4","Source Serif Pro",Georgia,serif',
  MONO: '"JetBrains Mono","SF Mono",Menlo,monospace',
  SCRIPT: '"Times New Roman",Times,serif'
};
function FGlyph({
  name,
  size = 24,
  sw = 1.5,
  style
}) {
  const base = {
    width: size,
    height: size,
    viewBox: '0 0 32 32',
    fill: 'none',
    stroke: 'currentColor',
    strokeWidth: sw,
    strokeLinecap: 'round',
    strokeLinejoin: 'round',
    style
  };
  const P = {
    mic: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("rect", {
      x: "12",
      y: "4",
      width: "8",
      height: "16",
      rx: "4"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M7 14a9 9 0 0 0 18 0"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M16 23v5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M12 28h8"
    })),
    phone: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M7 6.5C7 5.7 7.7 5 8.5 5h3.2c.7 0 1.3.5 1.4 1.2l.7 4c.1.6-.2 1.2-.7 1.5l-2 1.2a14 14 0 0 0 7 7l1.2-2c.3-.5.9-.8 1.5-.7l4 .7c.7.1 1.2.7 1.2 1.4v3.2c0 .8-.7 1.5-1.5 1.5A18.5 18.5 0 0 1 7 6.5z"
    })),
    upload: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M16 4v16M10 10l6-6 6 6"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M5 22v4a2 2 0 0 0 2 2h18a2 2 0 0 0 2-2v-4"
    })),
    script: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M8 4h12l5 5v18a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2z"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M20 4v5h5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M11 15h10M11 19h10M11 23h6"
    })),
    chat: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M27 19a2 2 0 0 1-2 2H11l-5 5V8a2 2 0 0 1 2-2h17a2 2 0 0 1 2 2z"
    })),
    send: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M28 4 14 18M28 4l-9 24-5-10-10-5 24-9z"
    })),
    pin: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M25 13c0 7-9 15-9 15s-9-8-9-15a9 9 0 0 1 18 0Z"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "16",
      cy: "13",
      r: "3.4"
    })),
    user: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "16",
      cy: "11",
      r: "5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M6 27a10 10 0 0 1 20 0"
    })),
    clock: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "16",
      cy: "16",
      r: "12"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M16 8v8l5 3"
    })),
    arrow: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M6 16h20M18 8l8 8-8 8"
    })),
    check: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M27 8 12 23l-7-7"
    })),
    x: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M8 8l16 16M24 8 8 24"
    })),
    plus: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M16 6v20M6 16h20"
    })),
    film: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("rect", {
      x: "4",
      y: "7",
      width: "24",
      height: "18",
      rx: "2"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M10 7v18M22 7v18M4 13h6M22 13h6M4 19h6M22 19h6"
    })),
    video: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("rect", {
      x: "4",
      y: "9",
      width: "17",
      height: "14",
      rx: "2.5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M21 13.5l7-4v13l-7-4z"
    })),
    review: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M2 16s5-9 14-9 14 9 14 9-5 9-14 9S2 16 2 16z"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "16",
      cy: "16",
      r: "4.2"
    }))
  };
  return /*#__PURE__*/React.createElement("svg", base, P[name] || null);
}

// Interruptible count tween — guarantees the destination even if rAF is paused.
function useFTween(value, dur = 600) {
  const [disp, setDisp] = React.useState(value);
  const ref = React.useRef(value);
  const raf = React.useRef();
  React.useEffect(() => {
    const from = ref.current,
      to = value;
    if (Math.abs(from - to) < 0.001) {
      ref.current = to;
      setDisp(to);
      return;
    }
    const start = performance.now();
    cancelAnimationFrame(raf.current);
    let done = false;
    const finish = () => {
      if (done) return;
      done = true;
      ref.current = to;
      setDisp(to);
    };
    const tick = now => {
      const p = Math.min(1, (now - start) / dur);
      const e = 1 - Math.pow(1 - p, 3);
      ref.current = from + (to - from) * e;
      setDisp(ref.current);
      if (p < 1) raf.current = requestAnimationFrame(tick);else finish();
    };
    raf.current = requestAnimationFrame(tick);
    const fb = setTimeout(finish, dur + 80);
    return () => {
      cancelAnimationFrame(raf.current);
      clearTimeout(fb);
    };
  }, [value, dur]);
  return disp;
}
function fitFlowStage() {
  const st = document.getElementById('stage');
  if (!st) return;
  const vw = window.innerWidth || document.documentElement.clientWidth || 0;
  const vh = window.innerHeight || document.documentElement.clientHeight || 0;
  const s = Math.min(vw / 1920, vh / 1080);
  // Dimensions not ready yet (can be 0 on first paint in some embeds) —
  // never apply scale(0); reschedule until the frame has real size.
  if (!(s > 0) || !isFinite(s)) {
    setTimeout(fitFlowStage, 60);
    return;
  }
  st.style.transform = `translate(-50%, -50%) scale(${s})`;
}
window.addEventListener('resize', fitFlowStage);
if (typeof ResizeObserver !== 'undefined') {
  try {
    new ResizeObserver(() => fitFlowStage()).observe(document.documentElement);
  } catch (e) {}
}
Object.assign(window, {
  FK,
  FGlyph,
  useFTween,
  fitFlowStage
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow/ui.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/app.jsx
try { (() => {
/* global React, ReactDOM, FK, FGlyph, FLOW, FRAMEWORK, fitFlowStage,
   LaneSelect, UploadGate, ScriptPanel, TimelinePanel, SubTabBar, ScaffoldDirector, ScaffoldProducer, CreativeLane, ProductionLane, ClientPortal */

const {
  useState,
  useEffect,
  useRef
} = React;

// Scripted, animated back-and-forth — plays once a conversation is initiated.
// Producer ↔ Director negotiate the night scene and golden hour; the trade
// (flip apartment to day, fund golden-hour OT from the savings) drives the
// shared state so the budget ripple moves as they talk.
const PROD_CONVO = [{
  from: 'producer',
  text: 'Schedule won\u2019t let us shoot this night scene. Can it be changed?'
}, {
  from: 'director',
  text: 'Flipping the apartment to day. That clears the night premium.'
}, {
  from: 'producer',
  text: 'Perfect \u2014 that\u2019s $18K back in contingency.'
}, {
  from: 'director',
  text: 'Hold the beach for golden hour, though. It sells the ending.'
}, {
  from: 'producer',
  text: 'Golden hour\u2019s a half-day of OT. I\u2019ll fund it from the night savings \u2014 net flat.'
}, {
  from: 'director',
  text: 'Then we\u2019re good. Locking the board.'
}];
const DIR_CONVO = [{
  from: 'director',
  text: 'Can we hold the beach for golden hour? It sells the ending.'
}, {
  from: 'producer',
  text: 'Golden hour is a half-day of OT \u2014 about +$18,000.'
}, {
  from: 'director',
  text: 'Can we find it somewhere?'
}, {
  from: 'producer',
  text: 'Flip the apartment to day \u2014 the night premium covers it. Net flat.'
}, {
  from: 'director',
  text: 'Do it. Day apartment, golden-hour beach.'
}, {
  from: 'producer',
  text: 'Done \u2014 variance holds. Sending the updated board.'
}];
function Workspace({
  lane,
  state,
  chat,
  flashId,
  cat,
  subIdx,
  catFlash,
  onCat,
  onSub,
  onMode,
  onSend,
  onReset
}) {
  const laneLabel = lane === 'director' ? 'Director' : 'Producer';
  const catObj = FRAMEWORK.top10(lane).find(c => c.id === cat) || FRAMEWORK.top10(lane)[0];
  return /*#__PURE__*/React.createElement("div", {
    className: "work"
  }, /*#__PURE__*/React.createElement("div", {
    className: "topbar"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 20,
      color: FK.INK_FAINT
    }
  }, "tenet"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: FK.AMBER
    }
  }, laneLabel), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 14,
      color: FK.INK_MUTED,
      whiteSpace: 'nowrap'
    }
  }, FLOW.TITLE)), /*#__PURE__*/React.createElement("button", {
    className: "reset-btn",
    onClick: onReset
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 13,
    style: {
      transform: 'scaleX(-1)'
    }
  }), " Change lane")), /*#__PURE__*/React.createElement("div", {
    className: "work-top"
  }, /*#__PURE__*/React.createElement("div", {
    className: "work-scaffold"
  }, lane === 'director' ? /*#__PURE__*/React.createElement(ScaffoldDirector, {
    cat: cat,
    onCat: onCat
  }) : /*#__PURE__*/React.createElement(ScaffoldProducer, {
    state: state,
    cat: cat,
    onCat: onCat,
    chat: chat,
    onSend: onSend,
    catFlash: catFlash
  })), /*#__PURE__*/React.createElement("div", {
    className: "work-right"
  }, /*#__PURE__*/React.createElement(SubTabBar, {
    cat: catObj,
    subIdx: subIdx,
    onSub: onSub
  }), /*#__PURE__*/React.createElement("div", {
    className: "work-script"
  }, /*#__PURE__*/React.createElement(ScriptPanel, {
    state: state,
    flashId: flashId
  })))), /*#__PURE__*/React.createElement("div", {
    className: "work-timeline"
  }, /*#__PURE__*/React.createElement(TimelinePanel, {
    state: state,
    flashId: flashId,
    onMode: onMode
  })));
}

// eslint-disable-next-line no-unused-vars -- superseded by ClientPortal
function ClientReviewLegacy({
  onReset
}) {
  const [decision, setDecision] = useState(null);
  const CUTS = [{
    id: 'v4',
    name: '01_BriefHistory_HD',
    tag: 'Latest',
    date: 'Jun 4',
    note: 'Current cut for review'
  }, {
    id: 'v3',
    name: '01_BriefHistory_HD_vb3',
    tag: 'v3',
    date: 'May 28',
    note: 'Revised VO timing'
  }, {
    id: 'v2',
    name: '01_BriefHistory_HD_a2',
    tag: 'v2',
    date: 'May 21',
    note: 'Alt opening beat'
  }, {
    id: 'v1',
    name: '01_BriefHistory_rough',
    tag: 'v1',
    date: 'May 14',
    note: 'First rough assembly'
  }];
  const [cutId, setCutId] = useState('v4');
  const cut = CUTS.find(c => c.id === cutId);
  const icons = [['mic', 'Record'], ['phone', 'Call'], ['upload', 'Upload'], ['video', 'Video']];
  const beats = ['open', 'setup', 'turn', 'build', 'peak', 'land'];
  const btnBase = {
    borderRadius: 3,
    padding: '12px 16px',
    cursor: 'pointer',
    fontFamily: FK.MONO,
    fontSize: 11,
    letterSpacing: '0.14em',
    textTransform: 'uppercase',
    textAlign: 'center'
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      background: FK.PAPER,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: 44,
      flexShrink: 0,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '0 18px',
      borderBottom: `1px solid ${FK.BORDER}`,
      background: FK.PAPER,
      position: 'relative'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: '50%',
      top: '50%',
      transform: 'translate(-50%,-50%)',
      display: 'flex',
      alignItems: 'center',
      gap: 4
    }
  }, icons.map(([ic, label]) => /*#__PURE__*/React.createElement("span", {
    key: ic,
    title: label,
    style: {
      width: 30,
      height: 30,
      borderRadius: 6,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      color: ic === 'upload' ? FK.AMBER : FK.INK_MUTED,
      cursor: 'pointer'
    }
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: ic,
    size: 16
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 19,
      color: FK.INK_FAINT
    }
  }, "tenet"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: FK.AMBER
    }
  }, "Client"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: FK.INK_MUTED,
      whiteSpace: 'nowrap'
    }
  }, FLOW.TITLE)), /*#__PURE__*/React.createElement("button", {
    className: "reset-btn",
    onClick: onReset
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 13,
    style: {
      transform: 'scaleX(-1)'
    }
  }), " Change lane")), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      padding: 48
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      maxWidth: 1120,
      display: 'flex',
      gap: 48,
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '1 1 0',
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      marginBottom: 10,
      flexWrap: 'wrap'
    }
  }, CUTS.map(c => {
    const on = c.id === cutId;
    return /*#__PURE__*/React.createElement("button", {
      key: c.id,
      onClick: () => setCutId(c.id),
      title: `${c.date} · ${c.note}`,
      style: {
        display: 'inline-flex',
        alignItems: 'center',
        gap: 6,
        cursor: 'pointer',
        padding: '5px 10px',
        borderRadius: 3,
        whiteSpace: 'nowrap',
        border: `1px solid ${on ? FK.AMBER : FK.BORDER}`,
        background: on ? FK.AMBER_50 : 'transparent',
        fontFamily: FK.MONO,
        fontSize: 10,
        letterSpacing: '0.08em',
        color: on ? FK.AMBER_800 : FK.INK_MUTED
      }
    }, on && /*#__PURE__*/React.createElement("span", {
      style: {
        width: 5,
        height: 5,
        borderRadius: 999,
        background: FK.AMBER
      }
    }), c.tag, /*#__PURE__*/React.createElement("span", {
      style: {
        color: FK.INK_FAINT
      }
    }, c.date));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      marginBottom: 10,
      color: FK.INK_FAINT,
      whiteSpace: 'nowrap'
    }
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "film",
    size: 14
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.1em'
    }
  }, cut.name), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 12,
      color: FK.INK_FAINT
    }
  }, cut.note)), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: '100%',
      aspectRatio: '16 / 9',
      background: '#000',
      border: `1px solid ${FK.BORDER_STRONG}`,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("iframe", {
    src: "https://player.vimeo.com/video/1198884434?title=0&byline=0&portrait=0&badge=0&autopause=0&player_id=0&app_id=58479",
    allow: "autoplay; fullscreen; picture-in-picture; clipboard-write; encrypted-media; web-share",
    referrerPolicy: "strict-origin-when-cross-origin",
    title: "A Brief History of Selling Ideas",
    style: {
      position: 'absolute',
      inset: 0,
      width: '100%',
      height: '100%',
      border: 0,
      display: 'block'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 0,
      left: 0,
      right: 0,
      height: 40,
      background: 'linear-gradient(rgba(0,0,0,0.5),transparent)',
      pointerEvents: 'none'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 12,
      left: 14,
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.18em',
      color: 'rgba(255,255,255,0.7)',
      pointerEvents: 'none'
    }
  }, "PROGRAM \xB7 16\xD79")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: `repeat(${beats.length},1fr)`,
      marginTop: 14
    }
  }, beats.map((b, i) => /*#__PURE__*/React.createElement("span", {
    key: b,
    style: {
      textAlign: 'center',
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.08em',
      color: i === 4 ? FK.AMBER : FK.INK_FAINT,
      fontWeight: i === 4 ? 700 : 400
    }
  }, b)))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 360px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.22em',
      textTransform: 'uppercase',
      color: FK.AMBER,
      marginBottom: 14
    }
  }, "For your review"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontWeight: 300,
      fontSize: 30,
      lineHeight: 1.2,
      color: FK.INK,
      marginBottom: 14
    }
  }, FLOW.TITLE), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 15,
      lineHeight: 1.6,
      color: FK.INK_MUTED,
      marginBottom: 26
    }
  }, "The cut as it stands today. See the idea before it\u2019s made \u2014 approve it, or send a note back to the room."), !decision ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: () => setDecision('approved'),
    style: {
      ...btnBase,
      background: FK.AMBER,
      color: FK.PAPER,
      border: 'none'
    }
  }, "Approve this cut"), /*#__PURE__*/React.createElement("button", {
    onClick: () => setDecision('changes'),
    style: {
      ...btnBase,
      background: 'transparent',
      color: FK.INK,
      border: `1px solid ${FK.BORDER_STRONG}`
    }
  }, "Notes")) : /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 18px',
      border: `1px solid ${decision === 'approved' ? 'rgba(31,138,91,0.4)' : FK.AMBER}`,
      background: decision === 'approved' ? 'rgba(31,138,91,0.08)' : FK.AMBER_50,
      borderRadius: 4
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: decision === 'approved' ? '#1F8A5B' : FK.AMBER_800,
      marginBottom: 6
    }
  }, decision === 'approved' ? 'Approved' : 'Notes sent'), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 15,
      color: FK.INK
    }
  }, decision === 'approved' ? 'Sent to the room. Production can lock the bid.' : 'Your note is on its way to the creative seat.'), /*#__PURE__*/React.createElement("button", {
    onClick: () => setDecision(null),
    style: {
      marginTop: 12,
      background: 'none',
      border: 0,
      padding: 0,
      cursor: 'pointer',
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      borderBottom: `1px solid ${FK.BORDER}`
    }
  }, "Undo"))))));
}
function App() {
  const [screen, setScreen] = useState('lane');
  const [lane, setLane] = useState(null);
  const [state, setState] = useState(FLOW.DEFAULT_STATE);
  const [chat, setChat] = useState({
    messages: [],
    typing: false,
    sent: false
  });
  const [flashId, setFlashId] = useState(null);
  const [cat, setCat] = useState(null);
  const [subIdx, setSubIdx] = useState(0);
  const [catFlash, setCatFlash] = useState(null);
  const [dirTheme, setDirTheme] = useState('dark');
  const [dirChat, setDirChat] = useState({
    messages: [],
    typing: false,
    sent: false
  });
  const [prodView, setProdView] = useState('dash');
  const timers = useRef([]);
  useEffect(() => {
    fitFlowStage();
    const t = [setTimeout(fitFlowStage, 50), setTimeout(fitFlowStage, 200)];
    return () => t.forEach(clearTimeout);
  }, []);
  useEffect(() => () => timers.current.forEach(clearTimeout), []);
  function pickLane(l) {
    setLane(l);
    setCat(FRAMEWORK.top10(l)[0].id);
    setSubIdx(0);
    setScreen('upload');
  }
  function onUploaded() {
    setScreen('work');
  }
  // Seat switch from inside a workspace — re-seeds the lane so Director lands
  // in the contextual edit bay and Producer/Client in the business frame.
  function onSeat(l) {
    setLane(l);
    setCat(FRAMEWORK.top10(l)[0].id);
    setSubIdx(0);
  }
  function onCat(id) {
    setCat(id);
    setSubIdx(0);
  }
  function onSub(i) {
    setSubIdx(i);
  }
  function onMode(m) {
    setState(s => Object.assign({}, s, {
      mode: m
    }));
  }
  function onTod(v) {
    setState(s => Object.assign({}, s, {
      tod: Object.assign({}, s.tod, {
        s4: v
      })
    }));
  }

  // Play a scripted conversation with typing indicators. `selfRole` is the
  // window's own seat (no typing shown for their own lines). `sideEffects`
  // maps a message index → fn (e.g. flip the budget state mid-conversation).
  function playConvo(script, setChatFn, selfRole, sideEffects) {
    const T = timers.current;
    setChatFn({
      messages: [script[0]],
      typing: false,
      sent: true
    });
    if (sideEffects && sideEffects[0]) sideEffects[0]();
    let t = 0;
    for (let i = 1; i < script.length; i++) {
      const turn = script[i],
        idx = i;
      const land = () => {
        setChatFn(c => ({
          typing: false,
          sent: true,
          messages: c.messages.concat([turn])
        }));
        if (sideEffects && sideEffects[idx]) sideEffects[idx]();
      };
      if (turn.from === selfRole) {
        t += 950;
        T.push(setTimeout(land, t));
      } else {
        t += 650;
        T.push(setTimeout(() => setChatFn(c => Object.assign({}, c, {
          typing: turn.from
        })), t));
        t += 1450;
        T.push(setTimeout(land, t));
      }
    }
  }
  function flipToDay(flashCat) {
    setState(s => Object.assign({}, s, {
      tod: Object.assign({}, s.tod, {
        s4: 'DAY'
      })
    }));
    setFlashId('s4');
    if (flashCat) setCatFlash('crew');
    timers.current.push(setTimeout(() => {
      setFlashId(null);
      setCatFlash(null);
    }, 3200));
  }
  function onSend() {
    playConvo(PROD_CONVO, setChat, 'producer', {
      1: () => flipToDay(true)
    });
  }
  function onSendDir() {
    playConvo(DIR_CONVO, setDirChat, 'director', {
      3: () => flipToDay(false)
    });
  }
  function onToggleTheme(t) {
    setDirTheme(t);
  }
  function reset() {
    setScreen('lane');
    setLane(null);
    setState(FLOW.DEFAULT_STATE);
    setChat({
      messages: [],
      typing: false,
      sent: false
    });
    setFlashId(null);
    setCat(null);
    setSubIdx(0);
    setCatFlash(null);
    setDirChat({
      messages: [],
      typing: false,
      sent: false
    });
    setDirTheme('dark');
    setProdView('dash');
  }
  return /*#__PURE__*/React.createElement("div", {
    className: "stage",
    id: "stage"
  }, (screen === 'lane' || screen === 'upload') && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 30,
      left: 70,
      zIndex: 30,
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 22,
      color: FK.INK_FAINT
    }
  }, "tenet"), screen === 'lane' && /*#__PURE__*/React.createElement(LaneSelect, {
    onPick: pickLane
  }), screen === 'upload' && /*#__PURE__*/React.createElement(UploadGate, {
    lane: lane,
    onUploaded: onUploaded
  }), screen === 'work' && lane === 'director' && /*#__PURE__*/React.createElement(ContextualEditBay, {
    state: state,
    cat: cat,
    subIdx: subIdx,
    flashId: flashId,
    dirChat: dirChat,
    onCat: onCat,
    onSub: onSub,
    onMode: onMode,
    onSendDir: onSendDir,
    seat: lane,
    onSeat: onSeat,
    onReset: reset
  }), screen === 'work' && lane !== 'director' && /*#__PURE__*/React.createElement(ContextualFrame, {
    seat: lane,
    onSeat: onSeat,
    onReset: reset
  }));
}
ReactDOM.createRoot(document.getElementById('root')).render(/*#__PURE__*/React.createElement(App, null));
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/app.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/client-data.js
try { (() => {
/* ============================================================
   TENET — Client Portal data model.
   ------------------------------------------------------------
   Everything the client sees, derived from the one project:
   the :30 brand film "A Brief History of Selling Ideas".
   Budget figures reconcile to FRAMEWORK.finTotals(DEFAULT_STATE):
     Approved 500,000 · Actual 330,000 · Forecast 472,000
     Variance −28,000 (under) · Contingency 28,000 remaining.
   The Decisions engine is the centerpiece — each candidate change
   carries its real budget / schedule / resource / risk consequence.
   ============================================================ */
window.CLIENT = function () {
  var F = window.FLOW,
    FW = window.FRAMEWORK;
  var S0 = F.DEFAULT_STATE;
  var T = FW.finTotals(S0); // approved/actual/forecast/variance/reserve/drawn/contingency/directBudget

  function money(n) {
    return '$' + Math.round(Math.abs(n)).toLocaleString('en-US');
  }
  function signed(n) {
    return (n < 0 ? '\u2212' : '+') + money(n);
  }
  function money0(n) {
    return (n < 0 ? '\u2212' : '') + money(n);
  }

  // ── Project meta ────────────────────────────────────────────
  var project = {
    title: F.TITLE,
    logline: F.LOGLINE,
    format: ':30 Brand Film',
    advertiser: 'Meridian',
    agency: 'H&W',
    code: '#17433',
    director: 'D. Parker',
    today: 'Jun 5, 2026'
  };

  // ── Status + health ─────────────────────────────────────────
  var status = {
    overall: 'On Track',
    // On Track | At Risk | Delayed
    score: 82,
    health: [{
      id: 'budget',
      label: 'Budget',
      score: 86,
      state: 'Healthy',
      note: 'Forecast ' + money(T.forecast) + ' against ' + money(T.approved) + ' approved.'
    }, {
      id: 'schedule',
      label: 'Schedule',
      score: 78,
      state: 'On Track',
      note: 'Editorial on track; picture lock waits on this review.'
    }, {
      id: 'creative',
      label: 'Creative',
      score: 73,
      state: 'In Review',
      note: 'Latest cut with the client. Ending beat still open.'
    }]
  };

  // ── Budget summary (reconciles to finTotals) ────────────────
  var budget = {
    approved: T.approved,
    actual: T.actual,
    forecast: T.forecast,
    variance: T.variance,
    // −28,000 (under)
    contingency: T.contingency,
    // 28,000
    reserve: T.reserve,
    // 50,000
    // Allocation = the film's true cost centers, by forecast.
    allocation: FW.FIN_CENTERS.map(function (c) {
      return {
        id: c.id,
        name: c.name,
        amount: FW.finForecast(c, S0),
        why: c.why
      };
    }),
    // What changed since approval — cause, not bookkeeping.
    changed: [{
      label: 'Night exterior retained',
      detail: 'Apartment scene kept at night — crew overtime.',
      amt: 18000,
      when: 'Apr 9',
      kind: 'up'
    }, {
      label: 'Downtown location overage',
      detail: 'Extended street lockup by two hours.',
      amt: 4000,
      when: 'Apr 17',
      kind: 'up'
    }, {
      label: '1:1 social format added',
      detail: 'New deliverable approved in editorial.',
      amt: 6000,
      when: 'May 2',
      kind: 'up'
    }],
    forecastBands: [{
      id: 'best',
      label: 'Best case',
      amount: 454000,
      note: 'Apartment flips to day — overtime clears.'
    }, {
      id: 'exp',
      label: 'Expected case',
      amount: T.forecast,
      note: 'Current plan, night exterior held.'
    }, {
      id: 'worst',
      label: 'Worst case',
      amount: 498000,
      note: 'Ending reshoot + full contingency draw.'
    }]
  };

  // ── Timeline / schedule ─────────────────────────────────────
  var timeline = {
    currentPhase: 'Editorial',
    daysRemaining: 22,
    delivery: 'Jun 27, 2026',
    confidence: 'High',
    stages: [{
      id: 'creative',
      name: 'Creative Development',
      state: 'done',
      range: 'Mar 3 \u2013 Mar 21'
    }, {
      id: 'preprod',
      name: 'Preproduction',
      state: 'done',
      range: 'Mar 24 \u2013 Apr 11'
    }, {
      id: 'prod',
      name: 'Production',
      state: 'done',
      range: 'Apr 14 \u2013 Apr 18'
    }, {
      id: 'edit',
      name: 'Editorial',
      state: 'current',
      range: 'Apr 21 \u2013 Jun 13'
    }, {
      id: 'color',
      name: 'Color',
      state: 'upcoming',
      range: 'Jun 16 \u2013 Jun 20'
    }, {
      id: 'audio',
      name: 'Audio',
      state: 'upcoming',
      range: 'Jun 16 \u2013 Jun 24'
    }, {
      id: 'delivery',
      name: 'Delivery',
      state: 'upcoming',
      range: 'Jun 27'
    }],
    milestones: [{
      id: 'wrap',
      name: 'Shoot wrap',
      date: 'Apr 18',
      state: 'done'
    }, {
      id: 'assembly',
      name: 'Assembly cut',
      date: 'May 14',
      state: 'done'
    }, {
      id: 'review',
      name: 'Client review',
      date: 'Jun 5',
      state: 'current'
    }, {
      id: 'lock',
      name: 'Picture lock',
      date: 'Jun 13',
      state: 'upcoming'
    }, {
      id: 'colorrev',
      name: 'Color review',
      date: 'Jun 19',
      state: 'upcoming'
    }, {
      id: 'final',
      name: 'Final delivery',
      date: 'Jun 27',
      state: 'upcoming'
    }],
    delays: [{
      label: 'Picture lock depends on this review',
      detail: 'A note instead of an approval moves lock by ~3 days.',
      risk: 'medium'
    }]
  };

  // ── Scope ───────────────────────────────────────────────────
  var scope = {
    original: [{
      label: 'Deliverables',
      value: '1 \u00d7 :30 master'
    }, {
      label: 'Versions',
      value: ':30 \u00b7 :15 cutdown'
    }, {
      label: 'Formats',
      value: '16:9 broadcast \u00b7 1:1 social'
    }, {
      label: 'Languages',
      value: 'English'
    }, {
      label: 'Platforms',
      value: 'Broadcast \u00b7 Online'
    }],
    current: {
      approved: [':30 master (16:9)', ':15 cutdown', '1:1 social cut', 'Online master'],
      added: ['1:1 social cut'],
      removed: []
    },
    changes: [{
      label: '1:1 social format',
      date: 'May 2',
      cost: 6000,
      days: 1,
      state: 'approved'
    }, {
      label: 'Night exterior retained',
      date: 'Apr 9',
      cost: 18000,
      days: 0,
      state: 'approved'
    }, {
      label: ':15 cutdown confirmed',
      date: 'Apr 2',
      cost: 0,
      days: 0,
      state: 'approved'
    }]
  };

  // ── Risks (kept to four — only meaningful ones) ─────────────
  var risks = [{
    id: 'r1',
    type: 'Schedule',
    title: 'Picture lock waits on this review',
    prob: 'Medium',
    impact: 'Medium',
    owner: 'Producer',
    plan: 'Approve or send notes by Jun 9 to protect the Jun 27 delivery.'
  }, {
    id: 'r2',
    type: 'Creative',
    title: 'Ending beat still soft',
    prob: 'Medium',
    impact: 'Medium',
    owner: 'Director',
    plan: 'Golden-hour reshoot of the beach scene is held as an option.'
  }, {
    id: 'r3',
    type: 'Budget',
    title: 'Night overtime drawing contingency',
    prob: 'Low',
    impact: 'Low',
    owner: 'Producer',
    plan: 'Flipping the apartment scene to day clears the ' + money(18000) + ' premium.'
  }, {
    id: 'r4',
    type: 'Legal',
    title: 'Talent usage renews in 24 months',
    prob: 'Low',
    impact: 'Medium',
    owner: 'Legal',
    plan: 'Renewal already budgeted in licensing; flag at month 20.'
  }];

  // ── Reviews — assets, versions, feedback ────────────────────
  var reviews = {
    assets: [{
      id: 'edits',
      name: 'Edits',
      count: 4,
      active: true
    }, {
      id: 'boards',
      name: 'Boards',
      count: 12
    }, {
      id: 'anim',
      name: 'Animatics',
      count: 3
    }, {
      id: 'stills',
      name: 'Stills',
      count: 28
    }, {
      id: 'gen',
      name: 'Generative explorations',
      count: 9
    }, {
      id: 'graphics',
      name: 'Brand assets',
      count: 6
    }],
    cuts: [{
      id: 'v4',
      name: '01_BriefHistory_HD',
      tag: 'Latest',
      date: 'Jun 4',
      note: 'Current cut for review'
    }, {
      id: 'v3',
      name: '01_BriefHistory_HD_vb3',
      tag: 'v3',
      date: 'May 28',
      note: 'Revised VO timing'
    }, {
      id: 'v2',
      name: '01_BriefHistory_HD_a2',
      tag: 'v2',
      date: 'May 21',
      note: 'Alt opening beat'
    }, {
      id: 'v1',
      name: '01_BriefHistory_rough',
      tag: 'v1',
      date: 'May 14',
      note: 'First rough assembly'
    }],
    beats: ['open', 'setup', 'turn', 'build', 'peak', 'land'],
    comments: [{
      at: '0:06',
      author: 'You',
      text: 'Love the boom up — hold one more beat before the cut.',
      state: 'open'
    }, {
      at: '0:18',
      author: 'You',
      text: 'VO lands a touch early over the stamp.',
      state: 'addressed'
    }, {
      at: '0:24',
      author: 'D. Parker',
      text: 'Ending is the open question — beach vs. studio resolve.',
      state: 'open'
    }]
  };

  // ── Priorities + activity (Overview) ────────────────────────
  var priorities = {
    needsApproval: [{
      id: 'edit',
      label: 'Edit review',
      sub: 'Latest cut, ' + reviews.cuts[0].date,
      tab: 'reviews',
      urgent: true
    }, {
      id: 'music',
      label: 'Music selection',
      sub: 'Two directions to choose',
      tab: 'reviews'
    }, {
      id: 'endcard',
      label: 'End card',
      sub: 'Logo resolve, 3 options',
      tab: 'reviews'
    }, {
      id: 'legal',
      label: 'Legal approval',
      sub: 'Talent usage term',
      tab: 'scope'
    }],
    milestones: [{
      label: 'Client review',
      date: 'Jun 5',
      state: 'current'
    }, {
      label: 'Picture lock',
      date: 'Jun 13',
      state: 'upcoming'
    }, {
      label: 'Color review',
      date: 'Jun 19',
      state: 'upcoming'
    }, {
      label: 'Final delivery',
      date: 'Jun 27',
      state: 'upcoming'
    }],
    costDrivers: [{
      label: 'Additional deliverables',
      amt: 6000
    }, {
      label: 'Night overtime',
      amt: 18000
    }, {
      label: 'Location overage',
      amt: 4000
    }],
    activity: [{
      icon: 'film',
      text: 'Latest edit uploaded',
      meta: '01_BriefHistory_HD \u00b7 Jun 4',
      tone: 'amber'
    }, {
      icon: 'chat',
      text: '2 new comments added',
      meta: 'On the current cut \u00b7 Jun 4'
    }, {
      icon: 'clock',
      text: 'Budget forecast updated',
      meta: money(T.forecast) + ' \u00b7 Jun 3'
    }, {
      icon: 'plus',
      text: 'Scope change requested',
      meta: ':15 cutdown \u00b7 Jun 2'
    }]
  };

  // ── Notifications (global) ──────────────────────────────────
  var notifications = [{
    kind: 'Approval',
    text: 'Edit review is waiting on you',
    tab: 'reviews'
  }, {
    kind: 'Budget',
    text: 'Forecast updated to ' + money(T.forecast),
    tab: 'budget'
  }, {
    kind: 'Schedule',
    text: 'Picture lock set for Jun 13',
    tab: 'timeline'
  }, {
    kind: 'Scope',
    text: ':15 cutdown change requested',
    tab: 'decisions'
  }];

  // ── DECISIONS — the simulator. Each candidate change carries
  //    its real consequence across budget / schedule / resources / risk.
  var RESOURCE_SET = ['Editorial', 'Graphics', 'Sound', 'Locations', 'Travel', 'Crew', 'Talent', 'Legal', 'Schedule'];
  var decisions = [{
    id: 'cutdown',
    kind: 'add',
    label: 'Add a :15 cutdown',
    sub: 'A second deliverable from the approved cut',
    budget: 4500,
    days: 2,
    risk: 'Low',
    resources: ['Editorial', 'Graphics', 'Sound'],
    deliverable: '1 additional asset',
    detail: 'Pulls from the scenes already inside the :15 set. No new footage — an editorial pass, a graphics conform, and a sound mix.'
  }, {
    id: 'social',
    kind: 'add',
    label: 'Add a social campaign',
    sub: 'Six platform-native assets',
    budget: 18000,
    days: 3,
    risk: 'Medium',
    resources: ['Editorial', 'Graphics'],
    deliverable: '6 additional assets',
    detail: 'Vertical cutdowns and motion stills built from existing footage. No reshoot — editorial and graphics time only.'
  }, {
    id: 'version',
    kind: 'add',
    label: 'Add a market version',
    sub: 'Localized :30 master for one region',
    budget: 9000,
    days: 2,
    risk: 'Low',
    resources: ['Editorial', 'Legal'],
    deliverable: '1 localized master',
    detail: 'A language pass over the existing cut plus re-clearance of talent usage in the new market.'
  }, {
    id: 'location',
    kind: 'add',
    label: 'Move the ending to a destination coast',
    sub: 'Relocate the beach scene',
    budget: 72000,
    days: 4,
    risk: 'High',
    resources: ['Locations', 'Travel', 'Crew', 'Talent'],
    deliverable: null,
    detail: 'Reopens a wrapped scene. A full company move — travel, a new permit, crew days, and talent availability all reactivate.'
  }, {
    id: 'talent',
    kind: 'add',
    label: 'Recast the partner role',
    sub: 'Replace THEO in the apartment scene',
    budget: 26000,
    days: 5,
    risk: 'High',
    resources: ['Talent', 'Crew', 'Editorial'],
    deliverable: null,
    detail: 'A reshoot of the apartment scene with new casting, then a re-cut. The single most disruptive change on the board.'
  }, {
    id: 'flipday',
    kind: 'reduce',
    label: 'Flip the apartment scene to day',
    sub: 'Remove the night premium',
    budget: -18000,
    days: 0,
    risk: 'Low',
    resources: ['Crew', 'Schedule'],
    deliverable: null,
    detail: 'Clears the crew overtime currently drawing against contingency. The ending plays the same; the budget breathes.'
  }];
  return {
    F: F,
    FW: FW,
    S0: S0,
    T: T,
    money: money,
    signed: signed,
    money0: money0,
    project: project,
    status: status,
    budget: budget,
    timeline: timeline,
    scope: scope,
    risks: risks,
    reviews: reviews,
    priorities: priorities,
    notifications: notifications,
    decisions: decisions,
    RESOURCE_SET: RESOURCE_SET,
    NAV: [{
      id: 'overview',
      label: 'Overview'
    }, {
      id: 'reviews',
      label: 'Reviews'
    }, {
      id: 'timeline',
      label: 'Timeline'
    }, {
      id: 'budget',
      label: 'Budget'
    }, {
      id: 'scope',
      label: 'Scope'
    }, {
      id: 'risks',
      label: 'Risks'
    }, {
      id: 'decisions',
      label: 'Decisions'
    }]
  };
}();
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/client-data.js", error: String((e && e.message) || e) }); }

// flow_ctx/client-hero.jsx
try { (() => {
/* global React */
// TENET Client Portal — hero tabs: Overview snapshot + Decisions simulator.
const {
  FK: HFK,
  FGlyph: HG,
  CLIENT: HC
} = window;
const {
  CPanel: HP,
  CPStat: HStat,
  CPBar: HBar,
  CPChip: HChip,
  CPDot: HDot,
  CPHead: HHead,
  useCountUp: HCount,
  cpKicker: HKick,
  cpKickerFaint: HKickF,
  cpSerif: HSerif,
  RISK_COLOR: HRISK
} = window;
const HUse = React.useState,
  HEff = React.useEffect;
function shiftDate(days) {
  const d = new Date(2026, 5, 27);
  d.setDate(d.getDate() + days);
  return d.toLocaleDateString('en-US', {
    month: 'short',
    day: 'numeric'
  });
}

/* ══════════════════════════════════════════════════════════
   OVERVIEW
   ══════════════════════════════════════════════════════════ */
function OverviewTab({
  ctx
}) {
  const s = HC.status,
    b = HC.budget,
    t = HC.timeline,
    p = HC.priorities;
  const forecast = b.forecast + ctx.forecastDelta;
  const variance = forecast - b.approved;
  const over = variance > 0;
  const delivery = shiftDate(ctx.daysDelta);
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(HHead, {
    kicker: "Overview",
    lede: "A complete read on health, money, and what needs you \u2014 in one screen."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(4, 1fr)',
      gap: 16,
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement(CPStatStatus, {
    overall: over ? 'At Risk' : s.overall
  }), /*#__PURE__*/React.createElement(HStat, {
    label: "Forecast final cost",
    value: HC.money(forecast),
    tone: over ? '#B23A2E' : HFK.INK,
    sub: (over ? 'Over by ' : 'Under by ') + HC.money(Math.abs(variance)) + ' vs approved'
  }), /*#__PURE__*/React.createElement(HStat, {
    label: "Schedule",
    value: t.currentPhase,
    sub: t.daysRemaining + ' days to ' + delivery
  }), /*#__PURE__*/React.createElement(HStat, {
    label: "Deliverables",
    value: "1 pending",
    sub: '2 approved \u00b7 3 in progress'
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1.5fr 1fr',
      gap: 16,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(HP, {
    label: "Project health",
    title: "Overall \u2014 on track"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 18
    }
  }, s.health.map(h => /*#__PURE__*/React.createElement("div", {
    key: h.id
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline',
      marginBottom: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: HFK.SANS,
      fontSize: 14,
      color: HFK.INK
    }
  }, h.label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: HFK.MONO,
      fontSize: 10,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: h.id === 'creative' ? HFK.AMBER : HFK.INK_MUTED
    }
  }, h.state)), /*#__PURE__*/React.createElement(HBar, {
    pct: h.score,
    color: h.id === 'creative' ? HFK.AMBER : HFK.INK
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 8,
      fontFamily: HFK.SANS,
      fontSize: 12.5,
      color: HFK.INK_MUTED,
      lineHeight: 1.4
    }
  }, h.note))))), /*#__PURE__*/React.createElement(HP, {
    label: "Top cost drivers",
    title: "What is moving the number"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, p.costDrivers.map((d, i) => {
    const max = Math.max.apply(null, p.costDrivers.map(x => x.amt));
    return /*#__PURE__*/React.createElement("div", {
      key: i
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'baseline',
        marginBottom: 7
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: HFK.SANS,
        fontSize: 14,
        color: HFK.INK
      }
    }, d.label), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: HFK.MONO,
        fontSize: 12,
        color: HFK.INK,
        fontVariantNumeric: 'tabular-nums'
      }
    }, HC.money(d.amt))), /*#__PURE__*/React.createElement(HBar, {
      pct: d.amt / max * 100,
      color: HFK.AMBER_200
    }));
  }))), /*#__PURE__*/React.createElement(HP, {
    label: "Recent activity",
    title: "Latest in the room"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, p.activity.map((a, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 14,
      padding: '13px 0',
      borderTop: i ? `1px solid ${HFK.BORDER}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 30,
      height: 30,
      borderRadius: 6,
      flexShrink: 0,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      border: `1px solid ${HFK.BORDER}`,
      color: a.tone === 'amber' ? HFK.AMBER : HFK.INK_MUTED,
      background: a.tone === 'amber' ? HFK.AMBER_50 : HFK.PAPER
    }
  }, /*#__PURE__*/React.createElement(HG, {
    name: a.icon,
    size: 15
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontSize: 14,
      color: HFK.INK
    }
  }, a.text), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.MONO,
      fontSize: 10.5,
      letterSpacing: '0.06em',
      color: HFK.INK_FAINT,
      marginTop: 2
    }
  }, a.meta))))))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(HP, {
    label: "Needs your approval",
    title: p.needsApproval.length + ' waiting',
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, p.needsApproval.map(n => /*#__PURE__*/React.createElement("button", {
    key: n.id,
    onClick: () => ctx.goTab(n.tab),
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 12,
      textAlign: 'left',
      cursor: 'pointer',
      padding: '13px 14px',
      borderRadius: 4,
      border: `1px solid ${n.urgent ? HFK.AMBER : HFK.BORDER}`,
      background: n.urgent ? HFK.AMBER_50 : HFK.PAPER
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontSize: 14.5,
      color: HFK.INK
    }
  }, n.label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontSize: 12.5,
      color: HFK.INK_MUTED,
      marginTop: 1
    }
  }, n.sub)), /*#__PURE__*/React.createElement(HG, {
    name: "arrow",
    size: 15,
    style: {
      color: n.urgent ? HFK.AMBER : HFK.INK_FAINT,
      flexShrink: 0
    }
  }))))), /*#__PURE__*/React.createElement(HP, {
    label: "Upcoming",
    title: "Milestones",
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, p.milestones.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '12px 0',
      borderTop: i ? `1px solid ${HFK.BORDER}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement(HDot, {
    state: m.state
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: HFK.SANS,
      fontSize: 14,
      color: m.state === 'current' ? HFK.INK : HFK.INK_MUTED
    }
  }, m.label)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: HFK.MONO,
      fontSize: 11,
      letterSpacing: '0.06em',
      color: m.state === 'current' ? HFK.AMBER : HFK.INK_FAINT
    }
  }, m.date))))), /*#__PURE__*/React.createElement("button", {
    onClick: () => ctx.goTab('decisions'),
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      cursor: 'pointer',
      padding: '18px 22px',
      borderRadius: 4,
      border: 'none',
      background: HFK.INK,
      color: HFK.PAPER
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      textAlign: 'left'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: HFK.AMBER_200
    }
  }, "Decision simulator"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontWeight: 300,
      fontSize: 18,
      marginTop: 6
    }
  }, "See what a change would do")), /*#__PURE__*/React.createElement(HG, {
    name: "arrow",
    size: 18,
    style: {
      color: HFK.PAPER
    }
  })))));
}
function CPStatStatus({
  overall
}) {
  const onTrack = overall === 'On Track';
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${onTrack ? HFK.BORDER : HFK.AMBER}`,
      background: onTrack ? HFK.PAPER : HFK.AMBER_50,
      borderRadius: 4,
      padding: '18px 20px',
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: HKickF
  }, "Project status"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 9,
      height: 9,
      borderRadius: 999,
      background: onTrack ? '#1F8A5B' : HFK.AMBER
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: HFK.SANS,
      fontWeight: 300,
      fontSize: 28,
      lineHeight: 1,
      color: HFK.INK
    }
  }, overall)));
}

/* ══════════════════════════════════════════════════════════
   DECISIONS — the simulator
   ══════════════════════════════════════════════════════════ */
function DecisionsTab({
  ctx
}) {
  const [sel, setSel] = HUse(HC.decisions[0].id);
  const [revealed, setRevealed] = HUse(false);
  const d = HC.decisions.find(x => x.id === sel);
  const isCommitted = ctx.committed.indexOf(sel) >= 0;
  HEff(() => {
    setRevealed(false);
    const id = setTimeout(() => setRevealed(true), 40);
    return () => clearTimeout(id);
  }, [sel]);
  const b = HC.budget;
  const forecast = b.forecast + ctx.forecastDelta;
  const variance = forecast - b.approved;
  const over = variance > 0;
  const budgetTarget = revealed ? d.budget : 0;
  const animBudget = HCount(budgetTarget, 700);
  function approve() {
    if (!isCommitted) ctx.setCommitted(prev => prev.concat([sel]));
  }
  function undo() {
    ctx.setCommitted(prev => prev.filter(x => x !== sel));
  }
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(3, 1fr)',
      gap: 16,
      marginBottom: 28
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${over ? HFK.AMBER : HFK.BORDER}`,
      background: over ? HFK.AMBER_50 : HFK.PAPER,
      borderRadius: 4,
      padding: '16px 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: HKickF
  }, "Forecast / approved"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontWeight: 300,
      fontSize: 24,
      marginTop: 8,
      color: over ? '#B23A2E' : HFK.INK,
      fontVariantNumeric: 'tabular-nums'
    }
  }, HC.money(forecast), " ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: HFK.INK_FAINT,
      fontSize: 16
    }
  }, "/ ", HC.money(b.approved))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement(HBar, {
    pct: forecast / b.approved * 100,
    color: over ? '#B23A2E' : HFK.AMBER
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${HFK.BORDER}`,
      background: HFK.PAPER,
      borderRadius: 4,
      padding: '16px 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: HKickF
  }, "Delivery"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontWeight: 300,
      fontSize: 24,
      marginTop: 8,
      color: HFK.INK
    }
  }, shiftDate(ctx.daysDelta)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.MONO,
      fontSize: 11,
      color: ctx.daysDelta ? HFK.AMBER : HFK.INK_FAINT,
      marginTop: 8
    }
  }, ctx.daysDelta ? '+' + ctx.daysDelta + ' days committed' : 'On original date')), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${HFK.BORDER}`,
      background: HFK.PAPER,
      borderRadius: 4,
      padding: '16px 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: HKickF
  }, "Committed changes"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontWeight: 300,
      fontSize: 24,
      marginTop: 8,
      color: HFK.INK
    }
  }, ctx.committed.length), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.MONO,
      fontSize: 11,
      color: HFK.INK_FAINT,
      marginTop: 8
    }
  }, ctx.committed.length ? 'Reflected across the portal' : 'Nothing approved yet'))), /*#__PURE__*/React.createElement(HHead, {
    kicker: "Decisions",
    lede: "Pick a change. See the budget, schedule, resources, and risk before you approve anything."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '320px 1fr',
      gap: 24,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, HKickF, {
      marginBottom: 4
    })
  }, "Active decisions"), HC.decisions.map(o => {
    const on = o.id === sel;
    const com = ctx.committed.indexOf(o.id) >= 0;
    return /*#__PURE__*/React.createElement("button", {
      key: o.id,
      onClick: () => setSel(o.id),
      style: {
        position: 'relative',
        textAlign: 'left',
        cursor: 'pointer',
        padding: '14px 16px',
        borderRadius: 4,
        border: `1px solid ${on ? HFK.INK : HFK.BORDER}`,
        background: on ? HFK.PAPER : 'transparent',
        boxShadow: on ? '0 6px 18px rgba(0,0,0,0.06)' : 'none',
        transition: 'all 160ms ease'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        gap: 8
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: HFK.SANS,
        fontSize: 14.5,
        color: HFK.INK,
        lineHeight: 1.25
      }
    }, o.label), com && /*#__PURE__*/React.createElement(HG, {
      name: "check",
      size: 14,
      style: {
        color: '#1F8A5B',
        flexShrink: 0
      }
    })), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 8,
        marginTop: 7
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: HFK.MONO,
        fontSize: 11,
        color: o.budget < 0 ? '#1F8A5B' : HFK.INK_MUTED,
        fontVariantNumeric: 'tabular-nums'
      }
    }, HC.signed(o.budget)), /*#__PURE__*/React.createElement("span", {
      style: {
        width: 3,
        height: 3,
        borderRadius: 999,
        background: HFK.INK_FAINT
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        width: 6,
        height: 6,
        borderRadius: 999,
        background: HRISK[o.risk]
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: HFK.MONO,
        fontSize: 10,
        letterSpacing: '0.1em',
        textTransform: 'uppercase',
        color: HFK.INK_FAINT
      }
    }, o.risk)));
  })), /*#__PURE__*/React.createElement("div", {
    key: sel,
    style: {
      border: `1px solid ${HFK.BORDER_STRONG}`,
      borderRadius: 6,
      background: HFK.PAPER,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '22px 28px',
      borderBottom: `1px solid ${HFK.BORDER}`,
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'flex-start',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: d.kind === 'reduce' ? '#1F8A5B' : HFK.AMBER
    }
  }, d.kind === 'reduce' ? 'Scope reduction' : 'Scope addition'), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontWeight: 300,
      fontSize: 26,
      marginTop: 8,
      color: HFK.INK
    }
  }, d.label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SERIF,
      fontStyle: 'italic',
      fontSize: 15,
      color: HFK.INK_MUTED,
      marginTop: 4
    }
  }, d.sub)), isCommitted && /*#__PURE__*/React.createElement(CPChipSm, {
    tone: "#1F8A5B",
    label: "Committed"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 1,
      background: HFK.BORDER
    }
  }, /*#__PURE__*/React.createElement(ImpactCell, {
    label: "Budget impact"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontWeight: 300,
      fontSize: 40,
      lineHeight: 1,
      color: d.budget < 0 ? '#1F8A5B' : HFK.INK,
      fontVariantNumeric: 'tabular-nums'
    }
  }, HC.signed(animBudget)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontSize: 13,
      color: HFK.INK_MUTED,
      marginTop: 10
    }
  }, "Forecast would move to ", /*#__PURE__*/React.createElement("b", {
    style: {
      color: HFK.INK,
      fontWeight: 400
    }
  }, HC.money(forecast + (isCommitted ? 0 : d.budget))))), /*#__PURE__*/React.createElement(ImpactCell, {
    label: "Schedule impact"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontWeight: 300,
      fontSize: 40,
      lineHeight: 1,
      color: d.days ? HFK.INK : '#1F8A5B',
      fontVariantNumeric: 'tabular-nums'
    }
  }, d.days ? '+' + d.days + ' days' : 'No change'), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 14,
      display: 'flex',
      gap: 4
    }
  }, Array.from({
    length: 8
  }).map((_, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      flex: 1,
      height: 6,
      borderRadius: 1,
      background: i < 5 ? HFK.INK_FAINT : i < 5 + d.days ? HFK.AMBER : HFK.BORDER,
      opacity: revealed ? 1 : 0.2,
      transition: 'all 320ms ease ' + i * 50 + 'ms'
    }
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontSize: 13,
      color: HFK.INK_MUTED,
      marginTop: 10
    }
  }, "Delivery ", d.days ? 'moves to ' : 'holds at ', /*#__PURE__*/React.createElement("b", {
    style: {
      color: HFK.INK,
      fontWeight: 400
    }
  }, shiftDate(ctx.daysDelta + (isCommitted ? 0 : d.days))))), /*#__PURE__*/React.createElement(ImpactCell, {
    label: "Resources activated"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 8
    }
  }, HC.RESOURCE_SET.map((r, i) => {
    const on = d.resources.indexOf(r) >= 0;
    return /*#__PURE__*/React.createElement("span", {
      key: r,
      style: {
        opacity: revealed ? 1 : 0,
        transform: revealed ? 'translateY(0)' : 'translateY(6px)',
        transition: 'all 300ms ease ' + i * 55 + 'ms'
      }
    }, /*#__PURE__*/React.createElement(HChip, {
      on: on,
      dot: true
    }, r));
  })), d.deliverable && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontSize: 13,
      color: HFK.INK_MUTED,
      marginTop: 14
    }
  }, "Adds ", /*#__PURE__*/React.createElement("b", {
    style: {
      color: HFK.INK,
      fontWeight: 400
    }
  }, d.deliverable))), /*#__PURE__*/React.createElement(ImpactCell, {
    label: "Risk impact"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: HFK.SANS,
      fontWeight: 300,
      fontSize: 40,
      lineHeight: 1,
      color: HRISK[d.risk]
    }
  }, d.risk)), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 14,
      display: 'flex',
      gap: 6
    }
  }, ['Low', 'Medium', 'High'].map((lv, i) => {
    const active = d.risk === 'Low' && i === 0 || d.risk === 'Medium' && i <= 1 || d.risk === 'High';
    return /*#__PURE__*/React.createElement("div", {
      key: lv,
      style: {
        flex: 1,
        height: 8,
        borderRadius: 2,
        background: active ? HRISK[d.risk] : HFK.BORDER,
        opacity: revealed ? 1 : 0.25,
        transition: 'all 320ms ease ' + i * 90 + 'ms'
      }
    });
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SANS,
      fontSize: 13,
      color: HFK.INK_MUTED,
      marginTop: 10
    }
  }, d.detail))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '20px 28px',
      borderTop: `1px solid ${HFK.BORDER}`,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: HFK.SERIF,
      fontStyle: 'italic',
      fontSize: 14,
      color: HFK.INK_MUTED,
      maxWidth: 440
    }
  }, isCommitted ? 'This change is committed and reflected across Budget, Timeline, and Overview.' : 'Approving simulates the change across the whole portal — nothing is sent to the room yet.'), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 10,
      flexShrink: 0
    }
  }, isCommitted ? /*#__PURE__*/React.createElement("button", {
    onClick: undo,
    style: btnGhost
  }, "Undo") : /*#__PURE__*/React.createElement("button", {
    onClick: approve,
    style: d.kind === 'reduce' ? btnGreen : btnAmber
  }, d.kind === 'reduce' ? 'Apply reduction' : 'Approve change'))))));
}
function ImpactCell({
  label,
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      background: HFK.PAPER,
      padding: '22px 26px',
      minHeight: 150
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, HKickF, {
      marginBottom: 16
    })
  }, label), children);
}
function CPChipSm({
  label,
  tone
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: HFK.MONO,
      fontSize: 10,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: tone,
      border: `1px solid ${tone}`,
      borderRadius: 3,
      padding: '6px 10px',
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 5,
      height: 5,
      borderRadius: 999,
      background: tone
    }
  }), label);
}
const btnBaseH = {
  borderRadius: 3,
  padding: '12px 22px',
  cursor: 'pointer',
  fontFamily: HFK.MONO,
  fontSize: 11,
  letterSpacing: '0.14em',
  textTransform: 'uppercase',
  border: 'none'
};
const btnAmber = Object.assign({}, btnBaseH, {
  background: HFK.AMBER,
  color: HFK.PAPER
});
const btnGreen = Object.assign({}, btnBaseH, {
  background: '#1F8A5B',
  color: HFK.PAPER
});
const btnGhost = Object.assign({}, btnBaseH, {
  background: 'transparent',
  color: HFK.INK,
  border: `1px solid ${HFK.BORDER_STRONG}`
});
Object.assign(window, {
  OverviewTab,
  DecisionsTab
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/client-hero.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/client-shell.jsx
try { (() => {
/* global React */
// TENET Client Portal — app shell, primary nav, and shared primitives.
// Decision state is lifted here so an approved change ripples into the
// Budget forecast, the Timeline delivery date, and the Overview snapshot.
const {
  FK,
  FGlyph,
  CLIENT
} = window;
const {
  useState: cpUseState,
  useEffect: cpUseEffect,
  useRef: cpUseRef
} = React;

// ── Shared style atoms ──────────────────────────────────────
const cpKicker = {
  fontFamily: FK.MONO,
  fontSize: 10,
  letterSpacing: '0.22em',
  textTransform: 'uppercase',
  color: FK.AMBER,
  whiteSpace: 'nowrap'
};
const cpKickerFaint = {
  fontFamily: FK.MONO,
  fontSize: 10,
  letterSpacing: '0.22em',
  textTransform: 'uppercase',
  color: FK.INK_FAINT,
  whiteSpace: 'nowrap'
};
const cpPanelTitle = {
  fontFamily: FK.SANS,
  fontWeight: 300,
  fontSize: 22,
  lineHeight: 1.1,
  color: FK.INK,
  marginTop: 8
};
const cpSerif = {
  fontFamily: FK.SERIF,
  fontStyle: 'italic',
  fontWeight: 300
};
const STATE_COLOR = {
  done: FK.INK_FAINT,
  current: FK.AMBER,
  upcoming: FK.INK_FAINT
};
const RISK_COLOR = {
  Low: '#1F8A5B',
  Medium: FK.AMBER,
  High: '#B23A2E'
};

// ── Panel ───────────────────────────────────────────────────
// `subject` lifts the panel onto the bright foreground plane (warm shadow +
// amber spine) — the resolved focus. At rest, panels stay flat hairline
// surfaces (legible context). This is the morph contract, made physical.
function CPanel({
  label,
  title,
  right,
  children,
  style,
  pad,
  subject
}) {
  const base = subject ? {
    border: `1px solid ${FK.BORDER}`,
    background: FK.PAPER,
    borderRadius: 6,
    boxShadow: FK.SHADOW_LIFT,
    borderLeft: `2px solid ${FK.AMBER}`
  } : {
    border: `1px solid ${FK.BORDER}`,
    background: FK.PAPER,
    borderRadius: 6
  };
  return /*#__PURE__*/React.createElement("div", {
    style: Object.assign(base, {
      padding: pad == null ? 28 : pad
    }, style)
  }, (label || title || right) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'flex-start',
      marginBottom: title ? 20 : 16,
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("div", null, label && /*#__PURE__*/React.createElement("div", {
    style: cpKicker
  }, label), title && /*#__PURE__*/React.createElement("div", {
    style: cpPanelTitle
  }, title)), right), children);
}

// ── Stat card ───────────────────────────────────────────────
function CPStat({
  label,
  value,
  sub,
  tone,
  big,
  subject
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${FK.BORDER}`,
      background: FK.PAPER,
      borderRadius: 6,
      padding: '20px 22px',
      display: 'flex',
      flexDirection: 'column',
      gap: 9,
      minWidth: 0,
      boxShadow: subject ? FK.SHADOW_LIFT : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: cpKickerFaint
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: big ? 36 : 29,
      lineHeight: 1,
      color: tone || FK.INK,
      letterSpacing: '-0.015em',
      fontVariantNumeric: 'tabular-nums'
    }
  }, value), sub && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 12.5,
      color: FK.INK_MUTED,
      lineHeight: 1.35
    }
  }, sub));
}

// ── Bar ─────────────────────────────────────────────────────
function CPBar({
  pct,
  color,
  track,
  h
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: '100%',
      height: h || 4,
      background: track || FK.BORDER,
      borderRadius: 999,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      top: 0,
      bottom: 0,
      width: Math.max(0, Math.min(100, pct)) + '%',
      background: color || FK.AMBER,
      borderRadius: 999,
      transition: 'width 700ms cubic-bezier(.2,.7,.2,1)'
    }
  }));
}

// ── Pill / chip ─────────────────────────────────────────────
function CPChip({
  children,
  on,
  tone,
  dot
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 7,
      padding: '5px 11px',
      borderRadius: 3,
      whiteSpace: 'nowrap',
      border: `1px solid ${on ? tone || FK.AMBER : FK.BORDER}`,
      background: on ? FK.AMBER_50 : 'transparent',
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.08em',
      textTransform: 'uppercase',
      color: on ? tone || FK.AMBER_800 : FK.INK_MUTED,
      transition: 'all 260ms ease'
    }
  }, dot && /*#__PURE__*/React.createElement("span", {
    style: {
      width: 5,
      height: 5,
      borderRadius: 999,
      background: on ? tone || FK.AMBER : FK.INK_FAINT
    }
  }), children);
}

// ── Status dot ──────────────────────────────────────────────
function CPDot({
  state,
  size
}) {
  const s = size || 7;
  const filled = state === 'current' || state === 'done';
  return /*#__PURE__*/React.createElement("span", {
    style: {
      width: s,
      height: s,
      borderRadius: 999,
      flexShrink: 0,
      background: state === 'current' ? FK.AMBER : state === 'done' ? FK.INK : 'transparent',
      border: state === 'upcoming' ? `1.5px solid ${FK.BORDER_STRONG}` : 'none',
      boxSizing: 'border-box'
    }
  });
}

// ── Section heading inside a tab ────────────────────────────
function CPHead({
  kicker,
  title,
  lede
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: title ? 28 : 22
    }
  }, kicker && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      marginBottom: title ? 16 : 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 22,
      height: 2,
      background: FK.AMBER,
      display: 'inline-block'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: cpKicker
  }, kicker)), title && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: 40,
      lineHeight: 1.02,
      letterSpacing: '-0.02em',
      color: FK.INK
    }
  }, title), lede && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: title ? 16 : 0,
      maxWidth: 640,
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontWeight: 300,
      fontSize: 19,
      lineHeight: 1.5,
      color: FK.INK_MUTED
    }
  }, lede));
}

// ── Count-up number (for the simulator) ─────────────────────
function useCountUp(target, dur) {
  const [v, setV] = cpUseState(target);
  const ref = cpUseRef({
    from: target,
    to: target,
    t0: 0,
    raf: 0
  });
  cpUseEffect(() => {
    const st = ref.current;
    st.from = v;
    st.to = target;
    st.t0 = 0;
    cancelAnimationFrame(st.raf);
    const tick = ts => {
      if (!st.t0) st.t0 = ts;
      const p = Math.min(1, (ts - st.t0) / (dur || 650));
      const e = 1 - Math.pow(1 - p, 3);
      setV(Math.round(st.from + (st.to - st.from) * e));
      if (p < 1) st.raf = requestAnimationFrame(tick);
    };
    st.raf = requestAnimationFrame(tick);
    return () => cancelAnimationFrame(st.raf);
    // eslint-disable-next-line
  }, [target]);
  return v;
}

// ── Header + nav + tab router ───────────────────────────────
function ClientPortal({
  onReset
}) {
  const [tab, setTab] = cpUseState('reviews'); // land on the cut, per spec
  const [committed, setCommitted] = cpUseState([]); // approved decision ids
  const [notifOpen, setNotifOpen] = cpUseState(false);
  const scrollRef = cpUseRef(null);
  const cd = CLIENT.decisions.filter(d => committed.indexOf(d.id) >= 0);
  const forecastDelta = cd.reduce((a, d) => a + d.budget, 0);
  const daysDelta = cd.reduce((a, d) => a + d.days, 0);
  function goTab(t) {
    setTab(t);
    setNotifOpen(false);
    if (scrollRef.current) scrollRef.current.scrollTop = 0;
  }
  const ctx = {
    committed,
    setCommitted,
    forecastDelta,
    daysDelta,
    goTab
  };
  const VIEWS = {
    overview: window.OverviewTab,
    reviews: window.ReviewsTab,
    timeline: window.TimelineTab,
    budget: window.BudgetTab,
    scope: window.ScopeTab,
    risks: window.RisksTab,
    decisions: window.DecisionsTab
  };
  const View = VIEWS[tab] || function () {
    return null;
  };
  const approvalCount = CLIENT.priorities.needsApproval.length;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      background: FK.PAPER_SOFT,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      height: 64,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '0 40px',
      borderBottom: `1px solid ${FK.BORDER}`,
      background: FK.PAPER,
      position: 'relative',
      zIndex: 20,
      boxShadow: '0 1px 0 rgba(38,28,12,0.035)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 15
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 23,
      color: FK.INK,
      letterSpacing: '0.01em'
    }
  }, "tenet"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.24em',
      textTransform: 'uppercase',
      color: FK.AMBER,
      whiteSpace: 'nowrap'
    }
  }, "Client Portal"), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 1,
      height: 17,
      background: FK.BORDER_STRONG,
      display: 'inline-block',
      alignSelf: 'center'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: Object.assign({}, cpSerif, {
      fontSize: 15.5,
      color: FK.INK,
      whiteSpace: 'nowrap'
    })
  }, CLIENT.project.title)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 22
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'none',
      alignItems: 'center',
      gap: 8,
      color: FK.INK_FAINT
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative'
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: () => setNotifOpen(o => !o),
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      background: 'none',
      border: 0,
      cursor: 'pointer',
      padding: 0,
      color: notifOpen ? FK.AMBER : FK.INK_MUTED
    }
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "pin",
    size: 16
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.08em',
      color: 'inherit'
    }
  }, approvalCount, " waiting")), notifOpen && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 30,
      right: 0,
      width: 320,
      background: FK.PAPER,
      border: `1px solid ${FK.BORDER_STRONG}`,
      borderRadius: 6,
      boxShadow: '0 18px 48px rgba(0,0,0,0.14)',
      padding: 8,
      zIndex: 40
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, cpKickerFaint, {
      padding: '8px 10px 10px'
    })
  }, "Notification center"), CLIENT.notifications.map((n, i) => /*#__PURE__*/React.createElement("button", {
    key: i,
    onClick: () => goTab(n.tab),
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 12,
      width: '100%',
      textAlign: 'left',
      background: 'none',
      border: 0,
      borderTop: `1px solid ${FK.BORDER}`,
      padding: '12px 10px',
      cursor: 'pointer'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: FK.AMBER,
      flex: '0 0 64px'
    }
  }, n.kind), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 13,
      color: FK.INK,
      lineHeight: 1.4
    }
  }, n.text))))), /*#__PURE__*/React.createElement("button", {
    className: "reset-btn",
    onClick: onReset
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 13,
    style: {
      transform: 'scaleX(-1)'
    }
  }), " Change lane"))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      height: 50,
      display: 'flex',
      alignItems: 'stretch',
      gap: 2,
      padding: '0 30px',
      borderBottom: `1px solid ${FK.BORDER}`,
      background: FK.PAPER,
      position: 'relative',
      zIndex: 10
    }
  }, CLIENT.NAV.map(n => {
    const on = n.id === tab;
    return /*#__PURE__*/React.createElement("button", {
      key: n.id,
      onClick: () => goTab(n.id),
      style: {
        position: 'relative',
        display: 'flex',
        alignItems: 'center',
        gap: 8,
        background: 'none',
        border: 0,
        borderBottom: on ? `2px solid ${FK.AMBER}` : '2px solid transparent',
        cursor: 'pointer',
        padding: '0 16px',
        marginBottom: -1,
        transition: 'color 200ms ease',
        fontFamily: FK.MONO,
        fontSize: 11,
        letterSpacing: '0.14em',
        textTransform: 'uppercase',
        color: on ? FK.INK : FK.INK_FAINT
      }
    }, n.label, n.id === 'decisions' && /*#__PURE__*/React.createElement("span", {
      style: {
        width: 5,
        height: 5,
        borderRadius: 999,
        background: FK.AMBER
      }
    }));
  })), /*#__PURE__*/React.createElement("div", {
    ref: scrollRef,
    style: {
      flex: 1,
      minHeight: 0,
      overflowY: 'auto',
      overflowX: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 1180,
      margin: '0 auto',
      padding: '36px 48px 64px'
    }
  }, /*#__PURE__*/React.createElement(View, {
    ctx: ctx
  }))));
}
Object.assign(window, {
  CPanel,
  CPStat,
  CPBar,
  CPChip,
  CPDot,
  CPHead,
  useCountUp,
  ClientPortal,
  cpKicker,
  cpKickerFaint,
  cpPanelTitle,
  cpSerif,
  STATE_COLOR,
  RISK_COLOR
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/client-shell.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/client-tabs1.jsx
try { (() => {
/* global React */
// TENET Client Portal — Reviews (the landing cut) + Timeline.
const {
  FK: RFK,
  FGlyph: RG,
  CLIENT: RC
} = window;
const {
  CPanel: RP,
  CPStat: RStat,
  CPBar: RBar,
  CPDot: RDot,
  CPHead: RHead,
  cpKickerFaint: RKickF
} = window;
const RUse = React.useState;
function rShift(days) {
  const d = new Date(2026, 5, 27);
  d.setDate(d.getDate() + days);
  return d.toLocaleDateString('en-US', {
    month: 'short',
    day: 'numeric'
  });
}

/* ══════════════════════════════════════════════════════════
   REVIEWS — the cut, kept as the landing
   ══════════════════════════════════════════════════════════ */
function ReviewsTab({
  ctx
}) {
  const r = RC.reviews;
  const [asset, setAsset] = RUse('edits');
  const [cutId, setCutId] = RUse('v4');
  const [decision, setDecision] = RUse(null);
  const cut = r.cuts.find(c => c.id === cutId);
  const a = r.assets.find(x => x.id === asset);
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(RHead, {
    kicker: "Reviews",
    lede: "See the idea before it\u2019s made. Approve the cut, or send a note straight back to the room."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '156px 1fr 300px',
      gap: 24,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 4
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, RKickF, {
      marginBottom: 8
    })
  }, "Project assets"), r.assets.map(x => {
    const on = x.id === asset;
    return /*#__PURE__*/React.createElement("button", {
      key: x.id,
      onClick: () => setAsset(x.id),
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        gap: 8,
        textAlign: 'left',
        cursor: 'pointer',
        padding: '10px 12px',
        borderRadius: 4,
        border: `1px solid ${on ? RFK.INK : 'transparent'}`,
        background: on ? RFK.PAPER : 'transparent'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: RFK.SANS,
        fontSize: 13,
        color: on ? RFK.INK : RFK.INK_MUTED,
        lineHeight: 1.2
      }
    }, x.name), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: RFK.MONO,
        fontSize: 10,
        color: RFK.INK_FAINT
      }
    }, x.count));
  })), /*#__PURE__*/React.createElement("div", null, asset === 'edits' ? /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      marginBottom: 10,
      flexWrap: 'wrap'
    }
  }, r.cuts.map(c => {
    const on = c.id === cutId;
    return /*#__PURE__*/React.createElement("button", {
      key: c.id,
      onClick: () => setCutId(c.id),
      title: c.date + ' · ' + c.note,
      style: {
        display: 'inline-flex',
        alignItems: 'center',
        gap: 6,
        cursor: 'pointer',
        padding: '5px 10px',
        borderRadius: 3,
        whiteSpace: 'nowrap',
        border: `1px solid ${on ? RFK.AMBER : RFK.BORDER}`,
        background: on ? RFK.AMBER_50 : 'transparent',
        fontFamily: RFK.MONO,
        fontSize: 10,
        letterSpacing: '0.06em',
        color: on ? RFK.AMBER_800 : RFK.INK_MUTED
      }
    }, on && /*#__PURE__*/React.createElement("span", {
      style: {
        width: 5,
        height: 5,
        borderRadius: 999,
        background: RFK.AMBER
      }
    }), c.tag, /*#__PURE__*/React.createElement("span", {
      style: {
        color: RFK.INK_FAINT
      }
    }, c.date));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      marginBottom: 10,
      color: RFK.INK_FAINT,
      whiteSpace: 'nowrap'
    }
  }, /*#__PURE__*/React.createElement(RG, {
    name: "film",
    size: 14
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.MONO,
      fontSize: 11,
      letterSpacing: '0.08em'
    }
  }, cut.name), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.SERIF,
      fontStyle: 'italic',
      fontSize: 12,
      color: RFK.INK_FAINT
    }
  }, cut.note)), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: '100%',
      aspectRatio: '16 / 9',
      background: '#000',
      border: `1px solid ${RFK.BORDER_STRONG}`,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("iframe", {
    src: "https://player.vimeo.com/video/1198884434?title=0&byline=0&portrait=0&badge=0&autopause=0&player_id=0&app_id=58479",
    allow: "autoplay; fullscreen; picture-in-picture; clipboard-write; encrypted-media; web-share",
    referrerPolicy: "strict-origin-when-cross-origin",
    title: RC.project.title,
    style: {
      position: 'absolute',
      inset: 0,
      width: '100%',
      height: '100%',
      border: 0,
      display: 'block'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 0,
      left: 0,
      right: 0,
      height: 40,
      background: 'linear-gradient(rgba(0,0,0,0.5),transparent)',
      pointerEvents: 'none'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 12,
      left: 14,
      fontFamily: RFK.MONO,
      fontSize: 10,
      letterSpacing: '0.18em',
      color: 'rgba(255,255,255,0.7)',
      pointerEvents: 'none'
    }
  }, "PROGRAM \xB7 16\xD79")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(' + r.beats.length + ',1fr)',
      marginTop: 14
    }
  }, r.beats.map((bt, i) => /*#__PURE__*/React.createElement("span", {
    key: bt,
    style: {
      textAlign: 'center',
      fontFamily: RFK.MONO,
      fontSize: 11,
      letterSpacing: '0.08em',
      color: i === 4 ? RFK.AMBER : RFK.INK_FAINT,
      fontWeight: i === 4 ? 700 : 400
    }
  }, bt)))) : /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      marginBottom: 14
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.SANS,
      fontWeight: 300,
      fontSize: 22,
      color: RFK.INK
    }
  }, a.name), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.MONO,
      fontSize: 11,
      color: RFK.INK_FAINT
    }
  }, a.count, " items")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(3, 1fr)',
      gap: 12
    }
  }, Array.from({
    length: Math.min(6, a.count)
  }).map((_, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      aspectRatio: '4 / 3',
      borderRadius: 4,
      border: `1px solid ${RFK.BORDER}`,
      background: 'repeating-linear-gradient(135deg, #FAF8F4 0 9px, #F2EDE4 9px 18px)',
      display: 'flex',
      alignItems: 'flex-end',
      padding: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: RFK.INK_FAINT
    }
  }, a.name.split(' ')[0], " ", String(i + 1).padStart(2, '0'))))))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(RP, {
    label: "For your review",
    title: null,
    pad: 22,
    subject: true
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: RFK.SERIF,
      fontStyle: 'italic',
      fontWeight: 300,
      fontSize: 22,
      lineHeight: 1.2,
      color: RFK.INK,
      marginBottom: 12
    }
  }, RC.project.title), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: RFK.SANS,
      fontSize: 13.5,
      lineHeight: 1.55,
      color: RFK.INK_MUTED,
      marginBottom: 20
    }
  }, "The cut as it stands today \u2014 approve it, or send a note back to the room."), !decision ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: () => setDecision('approved'),
    style: {
      borderRadius: 3,
      padding: '12px 16px',
      cursor: 'pointer',
      fontFamily: RFK.MONO,
      fontSize: 11,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      background: RFK.AMBER,
      color: RFK.PAPER,
      border: 'none'
    }
  }, "Approve this cut"), /*#__PURE__*/React.createElement("button", {
    onClick: () => setDecision('changes'),
    style: {
      borderRadius: 3,
      padding: '12px 16px',
      cursor: 'pointer',
      fontFamily: RFK.MONO,
      fontSize: 11,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      background: 'transparent',
      color: RFK.INK,
      border: `1px solid ${RFK.BORDER_STRONG}`
    }
  }, "Send notes")) : /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 18px',
      border: `1px solid ${decision === 'approved' ? 'rgba(31,138,91,0.4)' : RFK.AMBER}`,
      background: decision === 'approved' ? 'rgba(31,138,91,0.08)' : RFK.AMBER_50,
      borderRadius: 4
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: RFK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: decision === 'approved' ? '#1F8A5B' : RFK.AMBER_800,
      marginBottom: 6
    }
  }, decision === 'approved' ? 'Approved' : 'Notes sent'), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: RFK.SERIF,
      fontStyle: 'italic',
      fontSize: 14,
      color: RFK.INK
    }
  }, decision === 'approved' ? 'Sent to the room. Production can lock the bid.' : 'Your note is on its way to the creative seat.'), /*#__PURE__*/React.createElement("button", {
    onClick: () => setDecision(null),
    style: {
      marginTop: 12,
      background: 'none',
      border: 0,
      padding: 0,
      cursor: 'pointer',
      fontFamily: RFK.MONO,
      fontSize: 10,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: RFK.INK_FAINT,
      borderBottom: `1px solid ${RFK.BORDER}`
    }
  }, "Undo"))), /*#__PURE__*/React.createElement(RP, {
    label: "Client feedback",
    title: null,
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, r.comments.map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      padding: '12px 0',
      borderTop: i ? `1px solid ${RFK.BORDER}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      marginBottom: 5
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.MONO,
      fontSize: 10,
      color: RFK.AMBER,
      letterSpacing: '0.08em'
    }
  }, c.at), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.SANS,
      fontSize: 12,
      color: RFK.INK
    }
  }, c.author), /*#__PURE__*/React.createElement("span", {
    style: {
      marginLeft: 'auto',
      fontFamily: RFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: c.state === 'addressed' ? '#1F8A5B' : RFK.INK_FAINT
    }
  }, c.state)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: RFK.SANS,
      fontSize: 13,
      color: RFK.INK_MUTED,
      lineHeight: 1.45
    }
  }, c.text))))))));
}

/* ══════════════════════════════════════════════════════════
   TIMELINE
   ══════════════════════════════════════════════════════════ */
function TimelineTab({
  ctx
}) {
  const t = RC.timeline;
  const delivery = rShift(ctx.daysDelta);
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(RHead, {
    kicker: "Timeline",
    lede: "Seven stages, one path. The shoot is wrapped; editorial is in your hands."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(4, 1fr)',
      gap: 16,
      marginBottom: 28
    }
  }, /*#__PURE__*/React.createElement(RStat, {
    label: "Current phase",
    value: t.currentPhase,
    sub: "Cut with the client"
  }), /*#__PURE__*/React.createElement(RStat, {
    label: "Days remaining",
    value: t.daysRemaining + (ctx.daysDelta ? '+' + ctx.daysDelta : ''),
    sub: "To final delivery"
  }), /*#__PURE__*/React.createElement(RStat, {
    label: "Expected delivery",
    value: delivery,
    sub: ctx.daysDelta ? 'Shifted by committed changes' : 'On original date',
    tone: ctx.daysDelta ? RFK.AMBER : RFK.INK
  }), /*#__PURE__*/React.createElement(RStat, {
    label: "Schedule confidence",
    value: t.confidence,
    sub: "Holds if review lands by Jun 9"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1.4fr 1fr',
      gap: 16,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement(RP, {
    label: "Project stages",
    title: "The path"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      paddingLeft: 8
    }
  }, t.stages.map((st, i) => {
    const color = st.state === 'current' ? RFK.AMBER : st.state === 'done' ? RFK.INK : RFK.INK_FAINT;
    return /*#__PURE__*/React.createElement("div", {
      key: st.id,
      style: {
        position: 'relative',
        display: 'flex',
        gap: 18,
        paddingBottom: i === t.stages.length - 1 ? 0 : 22
      }
    }, i < t.stages.length - 1 && /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'absolute',
        left: 6,
        top: 16,
        bottom: 0,
        width: 1,
        background: RFK.BORDER
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        position: 'relative',
        zIndex: 1,
        width: 13,
        height: 13,
        marginTop: 3,
        borderRadius: 999,
        flexShrink: 0,
        background: st.state === 'upcoming' ? RFK.PAPER : color,
        border: st.state === 'upcoming' ? `1.5px solid ${RFK.BORDER_STRONG}` : 'none',
        boxShadow: st.state === 'current' ? '0 0 0 4px ' + RFK.AMBER_50 : 'none'
      }
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'baseline',
        justifyContent: 'space-between',
        flex: 1,
        gap: 12
      }
    }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: RFK.SANS,
        fontSize: 15,
        color: st.state === 'upcoming' ? RFK.INK_MUTED : RFK.INK
      }
    }, st.name), /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: RFK.MONO,
        fontSize: 10.5,
        letterSpacing: '0.06em',
        color: RFK.INK_FAINT,
        marginTop: 3
      }
    }, st.range)), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: RFK.MONO,
        fontSize: 9.5,
        letterSpacing: '0.14em',
        textTransform: 'uppercase',
        color: st.state === 'current' ? RFK.AMBER : RFK.INK_FAINT
      }
    }, st.state === 'done' ? 'Complete' : st.state === 'current' ? 'In progress' : 'Upcoming')));
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(RP, {
    label: "Milestones",
    title: null,
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, t.milestones.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: m.id,
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '11px 0',
      borderTop: i ? `1px solid ${RFK.BORDER}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement(RDot, {
    state: m.state
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.SANS,
      fontSize: 14,
      color: m.state === 'upcoming' ? RFK.INK_MUTED : RFK.INK
    }
  }, m.name)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.MONO,
      fontSize: 11,
      color: m.state === 'current' ? RFK.AMBER : RFK.INK_FAINT
    }
  }, m.date))))), /*#__PURE__*/React.createElement(RP, {
    label: "Delivery forecast",
    title: null,
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 10,
      marginBottom: 14
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.SANS,
      fontWeight: 300,
      fontSize: 30,
      color: ctx.daysDelta ? RFK.AMBER : RFK.INK
    }
  }, delivery), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: RFK.SANS,
      fontSize: 13,
      color: RFK.INK_MUTED
    }
  }, "expected")), t.delays.map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      gap: 10,
      padding: '12px 0',
      borderTop: `1px solid ${RFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 6,
      height: 6,
      borderRadius: 999,
      background: RFK.AMBER,
      marginTop: 6,
      flexShrink: 0
    }
  }), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: RFK.SANS,
      fontSize: 13.5,
      color: RFK.INK
    }
  }, d.label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: RFK.SANS,
      fontSize: 12.5,
      color: RFK.INK_MUTED,
      marginTop: 2,
      lineHeight: 1.4
    }
  }, d.detail))))))));
}
Object.assign(window, {
  ReviewsTab,
  TimelineTab
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/client-tabs1.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/client-tabs2.jsx
try { (() => {
/* global React */
// TENET Client Portal — Budget, Scope, Risks.
const {
  FK: BFK,
  CLIENT: BC
} = window;
const {
  CPanel: BP,
  CPStat: BStat,
  CPBar: BBar,
  CPHead: BHead,
  cpKickerFaint: BKickF,
  RISK_COLOR: BRISK
} = window;

/* ══════════════════════════════════════════════════════════
   BUDGET
   ══════════════════════════════════════════════════════════ */
function BudgetTab({
  ctx
}) {
  const b = BC.budget;
  const forecast = b.forecast + ctx.forecastDelta;
  const variance = forecast - b.approved;
  const over = variance > 0;
  const drawn = Math.max(0, forecast - BC.T.directBudget);
  const contingency = b.reserve - drawn;
  const allocTotal = b.allocation.reduce((a, c) => a + c.amount, 0);
  const maxAlloc = Math.max.apply(null, b.allocation.map(c => c.amount));
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(BHead, {
    kicker: "Budget",
    lede: "Budget impact without the accounting. Every number traces to a cause."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(5, 1fr)',
      gap: 14,
      marginBottom: 28
    }
  }, /*#__PURE__*/React.createElement(BStat, {
    label: "Approved budget",
    value: BC.money(b.approved)
  }), /*#__PURE__*/React.createElement(BStat, {
    label: "Actual spend",
    value: BC.money(b.actual),
    sub: "Committed to date"
  }), /*#__PURE__*/React.createElement(BStat, {
    label: "Forecast final",
    value: BC.money(forecast),
    tone: over ? '#B23A2E' : BFK.INK,
    sub: ctx.forecastDelta ? BC.signed(ctx.forecastDelta) + ' from changes' : 'On plan'
  }), /*#__PURE__*/React.createElement(BStat, {
    label: "Variance",
    value: (over ? '+' : '\u2212') + BC.money(Math.abs(variance)),
    tone: over ? '#B23A2E' : '#1F8A5B',
    sub: over ? 'Over approved' : 'Under approved'
  }), /*#__PURE__*/React.createElement(BStat, {
    label: "Contingency left",
    value: BC.money(Math.max(0, contingency)),
    tone: contingency < 0 ? '#B23A2E' : BFK.INK,
    sub: 'of ' + BC.money(b.reserve) + ' reserve'
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1.5fr 1fr',
      gap: 16,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement(BP, {
    label: "Current cost allocation",
    title: "By forecast"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 15
    }
  }, b.allocation.map(c => /*#__PURE__*/React.createElement("div", {
    key: c.id
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline',
      marginBottom: 7
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.SANS,
      fontSize: 14,
      color: BFK.INK
    }
  }, c.name), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 10,
      color: BFK.INK_FAINT
    }
  }, Math.round(c.amount / allocTotal * 100), "%"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 12,
      color: BFK.INK,
      fontVariantNumeric: 'tabular-nums'
    }
  }, BC.money(c.amount)))), /*#__PURE__*/React.createElement(BBar, {
    pct: c.amount / maxAlloc * 100,
    color: BFK.INK
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: BFK.SANS,
      fontSize: 12,
      color: BFK.INK_MUTED,
      marginTop: 6,
      lineHeight: 1.4
    }
  }, c.why))))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(BP, {
    label: "What changed",
    title: null,
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, b.changed.map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      padding: '13px 0',
      borderTop: i ? `1px solid ${BFK.BORDER}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.SANS,
      fontSize: 14,
      color: BFK.INK
    }
  }, c.label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 12,
      color: BFK.AMBER_800,
      fontVariantNumeric: 'tabular-nums',
      whiteSpace: 'nowrap'
    }
  }, "+", BC.money(c.amt))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline',
      marginTop: 3
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.SANS,
      fontSize: 12.5,
      color: BFK.INK_MUTED,
      lineHeight: 1.4,
      maxWidth: 220
    }
  }, c.detail), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 10,
      color: BFK.INK_FAINT
    }
  }, c.when)))))), /*#__PURE__*/React.createElement(BP, {
    label: "Forecast",
    title: null,
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 14
    }
  }, b.forecastBands.map(f => {
    const isExp = f.id === 'exp';
    return /*#__PURE__*/React.createElement("div", {
      key: f.id
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'baseline',
        marginBottom: 6
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: BFK.MONO,
        fontSize: 10,
        letterSpacing: '0.14em',
        textTransform: 'uppercase',
        color: isExp ? BFK.AMBER : BFK.INK_MUTED
      }
    }, f.label), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: BFK.SANS,
        fontSize: 15,
        color: BFK.INK,
        fontVariantNumeric: 'tabular-nums'
      }
    }, BC.money(f.id === 'exp' ? forecast : f.amount))), /*#__PURE__*/React.createElement(BBar, {
      pct: (f.id === 'exp' ? forecast : f.amount) / b.approved * 100,
      color: isExp ? BFK.AMBER : BFK.INK_FAINT
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: BFK.SANS,
        fontSize: 12,
        color: BFK.INK_MUTED,
        marginTop: 5
      }
    }, f.note));
  }))))));
}

/* ══════════════════════════════════════════════════════════
   SCOPE
   ══════════════════════════════════════════════════════════ */
function ScopeTab({
  ctx
}) {
  const s = BC.scope;
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(BHead, {
    kicker: "Scope",
    lede: "The line between what was agreed and what\u2019s been added \u2014 drawn clearly."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 16,
      alignItems: 'start',
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement(BP, {
    label: "Original scope",
    title: "As agreed"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, s.original.map((o, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline',
      padding: '12px 0',
      borderTop: i ? `1px solid ${BFK.BORDER}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 10,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: BFK.INK_FAINT
    }
  }, o.label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.SANS,
      fontSize: 14,
      color: BFK.INK,
      textAlign: 'right'
    }
  }, o.value))))), /*#__PURE__*/React.createElement(BP, {
    label: "Current scope",
    title: "Today"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, BKickF, {
      marginBottom: 10
    })
  }, "Approved"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 8
    }
  }, s.current.approved.map((x, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      fontFamily: BFK.SANS,
      fontSize: 13,
      color: BFK.INK,
      padding: '6px 12px',
      borderRadius: 3,
      border: `1px solid ${BFK.BORDER}`,
      background: BFK.PAPER_SOFT
    }
  }, x)))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: s.current.removed.length ? 16 : 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, BKickF, {
      marginBottom: 10,
      color: BFK.AMBER
    })
  }, "Added"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 8
    }
  }, s.current.added.map((x, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      fontFamily: BFK.SANS,
      fontSize: 13,
      color: BFK.AMBER_800,
      padding: '6px 12px',
      borderRadius: 3,
      border: `1px solid ${BFK.AMBER}`,
      background: BFK.AMBER_50
    }
  }, x)))), s.current.removed.length > 0 && /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, BKickF, {
      marginBottom: 10
    })
  }, "Removed"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.SANS,
      fontSize: 13,
      color: BFK.INK_FAINT
    }
  }, s.current.removed.join(', '))), s.current.removed.length === 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: BFK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: BFK.INK_FAINT,
      marginTop: 4
    }
  }, "Nothing removed from the original agreement."))), /*#__PURE__*/React.createElement(BP, {
    label: "Change history",
    title: "Every change, dated"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr auto auto auto',
      gap: '0 28px',
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, BKickF, {
      paddingBottom: 12,
      borderBottom: `1px solid ${BFK.BORDER}`
    })
  }, "Change"), /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, BKickF, {
      paddingBottom: 12,
      borderBottom: `1px solid ${BFK.BORDER}`,
      textAlign: 'right'
    })
  }, "Approved"), /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, BKickF, {
      paddingBottom: 12,
      borderBottom: `1px solid ${BFK.BORDER}`,
      textAlign: 'right'
    })
  }, "Cost"), /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, BKickF, {
      paddingBottom: 12,
      borderBottom: `1px solid ${BFK.BORDER}`,
      textAlign: 'right'
    })
  }, "Schedule"), s.changes.map((c, i) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: i
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: BFK.SANS,
      fontSize: 14,
      color: BFK.INK,
      padding: '13px 0',
      borderTop: `1px solid ${BFK.BORDER}`
    }
  }, c.label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 11,
      color: BFK.INK_FAINT,
      textAlign: 'right',
      padding: '13px 0',
      borderTop: `1px solid ${BFK.BORDER}`
    }
  }, c.date), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 12,
      color: c.cost ? BFK.AMBER_800 : '#1F8A5B',
      textAlign: 'right',
      padding: '13px 0',
      borderTop: `1px solid ${BFK.BORDER}`,
      fontVariantNumeric: 'tabular-nums'
    }
  }, c.cost ? '+' + BC.money(c.cost) : 'No cost'), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 12,
      color: BFK.INK,
      textAlign: 'right',
      padding: '13px 0',
      borderTop: `1px solid ${BFK.BORDER}`
    }
  }, c.days ? '+' + c.days + 'd' : '\u2014'))))));
}

/* ══════════════════════════════════════════════════════════
   RISKS
   ══════════════════════════════════════════════════════════ */
function RisksTab({
  ctx
}) {
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(BHead, {
    kicker: "Risks",
    lede: "Only what\u2019s meaningful \u2014 four risks worth your attention, each with a plan."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 16
    }
  }, BC.risks.map(r => /*#__PURE__*/React.createElement("div", {
    key: r.id,
    style: {
      border: `1px solid ${BFK.BORDER}`,
      borderRadius: 6,
      background: BFK.PAPER,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 22px 16px',
      borderBottom: `1px solid ${BFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: BFK.AMBER,
      whiteSpace: 'nowrap'
    }
  }, r.type, " risk"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.MONO,
      fontSize: 10,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: BFK.INK_FAINT
    }
  }, r.owner)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: BFK.SANS,
      fontWeight: 300,
      fontSize: 19,
      lineHeight: 1.2,
      color: BFK.INK
    }
  }, r.title)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 0
    }
  }, /*#__PURE__*/React.createElement(RiskMeter, {
    label: "Probability",
    value: r.prob
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 1,
      background: BFK.BORDER
    }
  }), /*#__PURE__*/React.createElement(RiskMeter, {
    label: "Impact",
    value: r.impact
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 22px',
      borderTop: `1px solid ${BFK.BORDER}`,
      background: BFK.PAPER_SOFT
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, BKickF, {
      marginBottom: 8
    })
  }, "Mitigation plan"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: BFK.SANS,
      fontSize: 13.5,
      color: BFK.INK,
      lineHeight: 1.5
    }
  }, r.plan))))));
}
function RiskMeter({
  label,
  value
}) {
  const idx = value === 'Low' ? 0 : value === 'Medium' ? 1 : 2;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      padding: '14px 22px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, BKickF, {
      marginBottom: 10
    })
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: BFK.SANS,
      fontSize: 16,
      color: BRISK[value]
    }
  }, value), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      gap: 3,
      marginLeft: 'auto'
    }
  }, [0, 1, 2].map(i => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      width: 14,
      height: 6,
      borderRadius: 1,
      background: i <= idx ? BRISK[value] : BFK.BORDER
    }
  })))));
}
Object.assign(window, {
  BudgetTab,
  ScopeTab,
  RisksTab
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/client-tabs2.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/contextual-editbay.jsx
try { (() => {
/* global React, FK, FGlyph, FLOW, FRAMEWORK */
// TENET — Creative lane as a CONTEXTUAL edit bay.
// The real editing regions — Program, Live Script, Timeline, Reference,
// Producer — become weighted bands. Whichever you engage resolves forward;
// the rest dim to a single reachable line. Program leads at rest.
// Reuses the actual edit-bay components (exported from director-edit.jsx).

const CXD = {
  bg0: '#0E0F12',
  bg1: '#16181d',
  bg2: '#1f2228',
  bg3: '#2a2e36',
  line: 'rgba(255,255,255,0.06)',
  line2: 'rgba(255,255,255,0.13)',
  txt: '#dfe2e6',
  txt2: '#8c929b',
  txt3: '#5b616a',
  amber: '#E3A155',
  ok: '#7fd6a6',
  pink: '#f0909f'
};
const CXD_CSS = `
.cxd-frame{position:absolute;inset:0;display:flex;flex-direction:column;background:${CXD.bg0};
  --ease:cubic-bezier(.45,0,.12,1);--T:.7s;}
.cxd-mast{flex:0 0 64px;display:flex;align-items:center;justify-content:space-between;padding:0 30px;
  background:${CXD.bg1};border-bottom:1px solid ${CXD.line};position:relative;z-index:5;}
.cxd-lock{display:flex;align-items:baseline;gap:13px;}
.cxd-wm{font-family:${FK.SERIF};font-style:italic;font-size:22px;color:${CXD.txt};letter-spacing:.01em;}
.cxd-locklbl{font-family:${FK.MONO};font-size:9.5px;letter-spacing:.26em;text-transform:uppercase;color:${CXD.amber};white-space:nowrap;}
.cxd-div{width:1px;height:16px;background:${CXD.line2};align-self:center;}
.cxd-proj{font-family:${FK.SERIF};font-style:italic;font-size:14px;color:${CXD.txt};white-space:nowrap;}
.cxd-projmeta{font-family:${FK.MONO};font-size:9.5px;letter-spacing:.14em;text-transform:uppercase;color:${CXD.txt3};}
.cxd-mright{display:flex;align-items:center;gap:20px;}
.cxd-fear{font-family:${FK.SERIF};font-style:italic;font-size:13px;color:${CXD.txt3};white-space:nowrap;max-width:300px;}
.cxd-seats{display:inline-flex;border:1px solid ${CXD.line2};border-radius:3px;overflow:hidden;}
.cxd-seat{padding:7px 14px;cursor:pointer;background:transparent;border:0;border-left:1px solid ${CXD.line2};
  font-family:${FK.MONO};font-size:10px;letter-spacing:.14em;text-transform:uppercase;color:${CXD.txt2};transition:color .25s,background .25s;}
.cxd-seat:first-child{border-left:0;}
.cxd-seat[aria-pressed="true"]{background:${CXD.amber};color:${CXD.bg0};}
.cxd-reset{display:inline-flex;align-items:center;gap:7px;background:transparent;border:1px solid ${CXD.line2};border-radius:3px;
  padding:7px 12px;cursor:pointer;color:${CXD.txt2};font-family:${FK.MONO};font-size:10px;letter-spacing:.14em;text-transform:uppercase;transition:color .2s,border-color .2s;white-space:nowrap;}
.cxd-reset:hover{color:${CXD.txt};border-color:${CXD.txt2};}

.cxd-bands{flex:1;min-height:0;display:flex;flex-direction:column;gap:10px;padding:16px 22px 12px;}
.cxd-band{flex:0 0 54px;min-height:0;position:relative;display:flex;flex-direction:column;background:${CXD.bg1};
  border:1px solid ${CXD.line};border-radius:8px;overflow:hidden;cursor:pointer;
  transition:flex-grow var(--T) var(--ease),flex-basis var(--T) var(--ease),opacity .5s var(--ease),
    background .45s,border-color .45s,box-shadow .45s,filter .45s;}
.cxd-band:hover{border-color:${CXD.line2};}
.cxd-band.is-subject{flex:1 1 0;flex-grow:16;background:${CXD.bg0};border-color:${CXD.line2};
  border-left:2px solid ${CXD.amber};box-shadow:0 1px 0 rgba(255,255,255,.03),0 26px 60px -26px rgba(0,0,0,.85);cursor:default;}
.cxd-band.is-collapsed{flex:0 0 54px;opacity:.5;filter:saturate(.55);}
.cxd-band.is-collapsed:hover{opacity:.85;filter:none;}

.cxd-head{flex:0 0 54px;display:flex;align-items:center;gap:14px;padding:0 22px;}
.cxd-kick{font-family:${FK.MONO};font-size:10px;letter-spacing:.2em;text-transform:uppercase;color:${CXD.txt3};flex:0 0 128px;
  display:flex;align-items:center;gap:9px;}
.cxd-band.is-subject .cxd-kick{color:${CXD.amber};}
.cxd-line{font-family:${FK.SERIF};font-style:italic;font-size:15px;color:${CXD.txt2};line-height:1.3;flex:1;min-width:0;
  white-space:nowrap;overflow:hidden;text-overflow:ellipsis;transition:font-size var(--T) var(--ease),color .4s;}
.cxd-band.is-subject .cxd-line{font-size:18px;color:${CXD.txt};font-style:normal;font-family:${FK.SANS};font-weight:300;}
.cxd-line b{font-style:normal;font-weight:600;color:${CXD.txt};}
.cxd-status{display:inline-flex;align-items:center;gap:8px;font-family:${FK.MONO};font-size:10px;letter-spacing:.12em;text-transform:uppercase;color:${CXD.txt3};white-space:nowrap;}
.cxd-dot{width:7px;height:7px;border-radius:50%;flex:0 0 auto;}
.cxd-chev{flex:0 0 auto;color:${CXD.txt3};opacity:0;transition:opacity .3s,transform .4s;}
.cxd-band.is-collapsed:hover .cxd-chev{opacity:.8;}

.cxd-body{flex:1;min-height:0;position:relative;}
.cxd-bodyinner{position:absolute;inset:0;overflow:auto;opacity:0;transition:opacity .45s var(--ease);}
.cxd-band.is-subject .cxd-bodyinner{opacity:1;}

/* Timeline — the persistent spine: always full frame, left to right */
.cxd-tl{flex:0 0 auto;display:flex;flex-direction:column;background:${CXD.bg0};border-top:1px solid ${CXD.line2};}
.cxd-tl-head{display:flex;align-items:center;gap:14px;padding:0 22px;height:34px;border-bottom:1px solid ${CXD.line};}
.cxd-tl-kick{font-family:${FK.MONO};font-size:10px;letter-spacing:.2em;text-transform:uppercase;color:${CXD.amber};display:flex;align-items:center;gap:9px;}

.cxd-floor{flex:0 0 46px;display:flex;align-items:center;gap:18px;padding:0 30px;background:${CXD.bg1};border-top:1px solid ${CXD.line};}
.cxd-fk{font-family:${FK.MONO};font-size:9.5px;letter-spacing:.18em;text-transform:uppercase;color:${CXD.txt3};}
.cxd-ftc{font-family:${FK.MONO};font-size:13px;color:${CXD.ok};letter-spacing:.04em;}
.cxd-fmeta{font-family:${FK.MONO};font-size:10px;letter-spacing:.1em;text-transform:uppercase;color:${CXD.txt2};}

/* Reference list (compact, inside the band) */
.cxd-reflist{padding:6px 0;}
.cxd-refrow{display:flex;align-items:center;gap:12px;padding:9px 22px;cursor:pointer;}
.cxd-refrow:hover{background:rgba(255,255,255,0.03);}
.cxd-refrow.on{background:${CXD.amber}1f;box-shadow:inset 3px 0 0 ${CXD.amber};}
.cxd-refn{font-family:${FK.MONO};font-size:10px;color:${CXD.txt3};width:18px;}
.cxd-refrow.on .cxd-refn{color:${CXD.amber};}
.cxd-refname{font-family:${FK.SERIF};font-style:italic;font-size:16px;color:${CXD.txt};flex:1;}
.cxd-refrow.on .cxd-refname{color:${CXD.amber};}
.cxd-refsubs{display:flex;flex-wrap:wrap;gap:6px;padding:14px 22px;border-top:1px solid ${CXD.line};}
.cxd-refsub{padding:6px 10px;cursor:pointer;font-family:${FK.MONO};font-size:10.5px;border-radius:2px;
  color:${CXD.txt2};background:${CXD.bg3};white-space:nowrap;}
.cxd-refsub.on{color:${CXD.bg0};background:${CXD.amber};}

@media (prefers-reduced-motion: reduce){.cxd-band,.cxd-bodyinner,.cxd-line{transition:none !important;}}
`;
(function injectCxd() {
  if (document.getElementById('cxd-styles')) return;
  const s = document.createElement('style');
  s.id = 'cxd-styles';
  s.textContent = CXD_CSS;
  document.head.appendChild(s);
})();
const CXD_SEAT = {
  client: {
    lbl: 'Client',
    fear: 'Watching it take shape — health at a glance.'
  },
  producer: {
    lbl: 'Producer',
    fear: 'Holding the line — does anything break, and what it costs.'
  },
  director: {
    lbl: 'Director',
    fear: 'Shaping the work — the room reads beneath, one line.'
  }
};

// Compact reference list — the director scaffold, sized for a band.
function CxdReference({
  cat,
  subIdx,
  onCat,
  onSub
}) {
  const cats = window.FRAMEWORK && window.FRAMEWORK.DIRECTOR || [];
  const catObj = cats.find(c => c.id === cat) || cats[0] || {
    n: 1,
    name: '—',
    subs: []
  };
  return /*#__PURE__*/React.createElement("div", {
    className: "cxd-reflist"
  }, cats.map(c => /*#__PURE__*/React.createElement("div", {
    key: c.id,
    className: 'cxd-refrow' + (c.id === cat ? ' on' : ''),
    onClick: e => {
      e.stopPropagation();
      onCat && onCat(c.id);
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "cxd-refn"
  }, String(c.n).padStart(2, '0')), /*#__PURE__*/React.createElement("span", {
    className: "cxd-refname"
  }, c.name), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      color: CXD.txt3
    }
  }, "6"))), /*#__PURE__*/React.createElement("div", {
    className: "cxd-refsubs"
  }, (catObj.subs || []).map((s, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: 'cxd-refsub' + (i === subIdx ? ' on' : ''),
    onClick: e => {
      e.stopPropagation();
      onSub && onSub(i);
    }
  }, s))));
}

// Timeline band body — transport + tracks, with the Beats overlay toggle.
function CxdTimeline({
  state,
  onMode
}) {
  const [view, setView] = React.useState('timeline');
  const TransportBar = window.TransportBar,
    EditTimeline = window.EditTimeline,
    BeatsOverlay = window.BeatsOverlay;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      minHeight: '100%'
    }
  }, /*#__PURE__*/React.createElement(TransportBar, {
    state: state,
    onMode: onMode,
    view: view,
    onView: setView
  }), view === 'beats' ? /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      opacity: 0.25
    }
  }, /*#__PURE__*/React.createElement(EditTimeline, {
    state: state
  })), /*#__PURE__*/React.createElement(BeatsOverlay, {
    state: state
  })) : /*#__PURE__*/React.createElement(EditTimeline, {
    state: state
  }));
}
function ContextualEditBay(props) {
  const {
    state,
    cat,
    subIdx,
    flashId,
    dirChat,
    onCat,
    onSub,
    onMode,
    onSendDir,
    onReset,
    seat,
    onSeat
  } = props;
  if (window.setEditTheme) window.setEditTheme('dark');
  const [subject, setSubject] = React.useState('program');
  const focus = id => setSubject(id);
  const project = window.CLIENT && window.CLIENT.project || {
    title: 'A Brief History of Selling Ideas',
    format: ':30 brand film',
    code: '#17433'
  };
  const len = window.FLOW ? FLOW.fmtClock(FLOW.totalLength(state)) : '0:30';
  const isDay = state && state.tod && state.tod.s4 === 'DAY';
  const cats = window.FRAMEWORK && window.FRAMEWORK.DIRECTOR || [];
  const catObj = cats.find(c => c.id === cat) || cats[0] || {
    n: 1,
    name: 'Reference'
  };
  const lastMsg = dirChat && dirChat.messages && dirChat.messages.length ? dirChat.messages[dirChat.messages.length - 1].text : 'A direct line to the producer — send a question, watch it land in the room.';
  const ProgramMonitor = window.ProgramMonitor,
    EditScript = window.EditScript,
    ChatProducer = window.ChatProducer;

  // Band descriptor: id, kick, the collapsed one-line, status, and body.
  const BANDS = [{
    id: 'program',
    kick: 'Program',
    line: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("b", null, "01_BriefHistory_HD"), " \u2014 the cut as it stands, ", len),
    status: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
      className: "cxd-dot",
      style: {
        background: CXD.amber
      }
    }), "In review"),
    body: /*#__PURE__*/React.createElement("div", {
      style: {
        position: 'absolute',
        inset: 0
      }
    }, /*#__PURE__*/React.createElement(ProgramMonitor, {
      state: state
    }))
  }, {
    id: 'script',
    kick: 'Live Script',
    line: /*#__PURE__*/React.createElement(React.Fragment, null, "Part 1 \xB7 ", /*#__PURE__*/React.createElement("b", null, "The Old Way"), " \u2014 AV format, locked to the cut"),
    status: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
      className: "cxd-dot",
      style: {
        background: CXD.ok
      }
    }), "Synced"),
    body: /*#__PURE__*/React.createElement("div", {
      style: {
        position: 'absolute',
        inset: 0
      }
    }, /*#__PURE__*/React.createElement(EditScript, {
      state: state,
      flashId: flashId
    }))
  }, {
    id: 'reference',
    kick: 'Reference',
    line: /*#__PURE__*/React.createElement(React.Fragment, null, String(catObj.n).padStart(2, '0'), " \xB7 ", /*#__PURE__*/React.createElement("b", null, catObj.name), " \u2014 the library behind the cut"),
    status: /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 10,
        color: CXD.txt3
      }
    }, "10 categories"),
    body: /*#__PURE__*/React.createElement(CxdReference, {
      cat: cat,
      subIdx: subIdx,
      onCat: onCat,
      onSub: onSub
    })
  }, {
    id: 'producer',
    kick: 'Message · Producer',
    line: /*#__PURE__*/React.createElement(React.Fragment, null, lastMsg),
    status: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
      className: "cxd-dot",
      style: {
        background: dirChat && dirChat.sent ? CXD.ok : CXD.txt3
      }
    }), dirChat && dirChat.sent ? 'Thread open' : 'Direct line'),
    body: /*#__PURE__*/React.createElement("div", {
      style: {
        position: 'absolute',
        inset: 0,
        overflow: 'auto'
      }
    }, /*#__PURE__*/React.createElement(ChatProducer, {
      chat: dirChat || {
        messages: [],
        typing: false,
        sent: false
      },
      onSend: onSendDir
    }))
  }];
  return /*#__PURE__*/React.createElement("div", {
    className: "cxd-frame"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cxd-mast"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cxd-lock"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cxd-wm"
  }, "tenet"), /*#__PURE__*/React.createElement("span", {
    className: "cxd-locklbl"
  }, "Creative"), /*#__PURE__*/React.createElement("span", {
    className: "cxd-div"
  }), /*#__PURE__*/React.createElement("span", {
    className: "cxd-proj"
  }, project.title), /*#__PURE__*/React.createElement("span", {
    className: "cxd-projmeta"
  }, project.format, " \xB7 ", project.code)), /*#__PURE__*/React.createElement("div", {
    className: "cxd-mright"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cxd-fear"
  }, CXD_SEAT.director.fear), /*#__PURE__*/React.createElement("div", {
    className: "cxd-seats"
  }, ['client', 'producer', 'director'].map(s => /*#__PURE__*/React.createElement("button", {
    key: s,
    className: "cxd-seat",
    "aria-pressed": s === 'director',
    onClick: () => {
      if (s !== 'director' && onSeat) onSeat(s);
    }
  }, CXD_SEAT[s].lbl))), /*#__PURE__*/React.createElement("button", {
    className: "cxd-reset",
    onClick: onReset
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 12,
    style: {
      transform: 'scaleX(-1)'
    }
  }), " Change lane"))), /*#__PURE__*/React.createElement("div", {
    className: "cxd-bands"
  }, BANDS.map(b => {
    const on = b.id === subject;
    return /*#__PURE__*/React.createElement("div", {
      key: b.id,
      className: 'cxd-band' + (on ? ' is-subject' : ' is-collapsed'),
      onClick: () => {
        if (!on) focus(b.id);
      }
    }, /*#__PURE__*/React.createElement("div", {
      className: "cxd-head"
    }, /*#__PURE__*/React.createElement("span", {
      className: "cxd-kick"
    }, b.kick), /*#__PURE__*/React.createElement("span", {
      className: "cxd-line"
    }, b.line), /*#__PURE__*/React.createElement("span", {
      className: "cxd-status"
    }, b.status), /*#__PURE__*/React.createElement("span", {
      className: "cxd-chev"
    }, /*#__PURE__*/React.createElement(FGlyph, {
      name: "arrow",
      size: 13,
      style: {
        transform: 'rotate(90deg)'
      }
    }))), /*#__PURE__*/React.createElement("div", {
      className: "cxd-body"
    }, /*#__PURE__*/React.createElement("div", {
      className: "cxd-bodyinner"
    }, on ? b.body : null)));
  })), /*#__PURE__*/React.createElement("div", {
    className: "cxd-tl"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cxd-tl-head"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cxd-tl-kick"
  }, "Timeline"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: CXD.txt2
    }
  }, len, " \xB7 ", /*#__PURE__*/React.createElement("b", {
    style: {
      fontStyle: 'normal',
      color: CXD.txt
    }
  }, "V1.2"), " active \xB7 ", isDay ? 'apartment in daylight — conflict clear' : 'night exterior — 1 conflict'), /*#__PURE__*/React.createElement("span", {
    style: {
      marginLeft: 'auto',
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: CXD.txt3
    }
  }, isDay ? 'Apartment flipped to day — the room is square.' : 'One open call: how the apartment scene reads.')), /*#__PURE__*/React.createElement(CxdTimeline, {
    state: state,
    onMode: onMode
  })));
}
window.ContextualEditBay = ContextualEditBay;
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/contextual-editbay.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/contextual.jsx
try { (() => {
/* global React */
// TENET — Contextual Interface workspace.
// ONE frame holds the whole project. As focus shifts (grab the budget lever,
// or change seat), the active decision + its consequence resolve forward;
// everything else dims and collapses to a single reachable line — never hidden.
// This replaces the tab-based lane workspaces for the contextual build.

const CX_CSS = `
.cx-frame{position:absolute;inset:0;display:flex;flex-direction:column;background:#EDEDEB;
  --ink:#17171A;--muted:#6A6E76;--faint:#A2A6AE;--hair:rgba(20,20,28,.10);--hair2:rgba(20,20,28,.18);
  --paper:#FFFFFF;--ground:#EDEDEB;--sub:#F6F6F4;--amber:#BA7517;--amber50:#FAEEDA;
  --ok:#1F8A5B;--watch:#BA7517;--risk:#C0392B;
  --sans:var(--font-sans,"Brown","Helvetica Neue",system-ui,sans-serif);
  --serif:var(--font-serif,"Source Serif 4",Georgia,serif);
  --mono:var(--font-mono,"JetBrains Mono",monospace);
  --ease:cubic-bezier(.45,0,.12,1);--T:.72s;}
.cx-mast{flex:0 0 76px;display:flex;align-items:center;justify-content:space-between;padding:0 40px;
  background:var(--paper);border-bottom:1px solid var(--hair);box-shadow:0 1px 0 rgba(20,20,28,.03);position:relative;z-index:5;}
.cx-lock{display:flex;align-items:baseline;gap:14px;}
.cx-wm{font-family:var(--serif);font-style:italic;font-size:24px;color:var(--ink);letter-spacing:.01em;}
.cx-locklbl{font-family:var(--mono);font-size:9.5px;letter-spacing:.26em;text-transform:uppercase;color:var(--amber);white-space:nowrap;}
.cx-div{width:1px;height:18px;background:var(--hair2);align-self:center;}
.cx-proj{font-family:var(--serif);font-style:italic;font-size:15.5px;color:var(--ink);white-space:nowrap;}
.cx-projmeta{font-family:var(--mono);font-size:9.5px;letter-spacing:.14em;text-transform:uppercase;color:var(--faint);}
.cx-mright{display:flex;align-items:center;gap:22px;}
.cx-fear{font-family:var(--serif);font-style:italic;font-size:13px;color:var(--faint);white-space:nowrap;max-width:340px;}
.cx-seats{display:inline-flex;border:1px solid var(--hair2);border-radius:3px;overflow:hidden;}
.cx-seat{padding:8px 15px;cursor:pointer;background:var(--paper);border:0;border-left:1px solid var(--hair2);
  font-family:var(--mono);font-size:10px;letter-spacing:.14em;text-transform:uppercase;color:var(--muted);transition:color .25s,background .25s;}
.cx-seat:first-child{border-left:0;}
.cx-seat[aria-pressed="true"]{background:var(--ink);color:#fff;}
.cx-resetbtn{display:inline-flex;align-items:center;gap:8px;background:transparent;border:1px solid var(--hair2);border-radius:3px;
  padding:8px 13px;cursor:pointer;color:var(--muted);font-family:var(--mono);font-size:10px;letter-spacing:.14em;text-transform:uppercase;transition:color .2s,border-color .2s;white-space:nowrap;}
.cx-resetbtn:hover{color:var(--ink);border-color:var(--ink);}

.cx-bands{flex:1;min-height:0;display:flex;flex-direction:column;gap:14px;padding:22px 40px;}
.cx-band{flex:1 1 0;min-height:0;position:relative;display:flex;flex-direction:column;background:var(--sub);
  border:1px solid var(--hair);border-radius:9px;overflow:hidden;cursor:pointer;
  transition:flex-grow var(--T) var(--ease),flex-basis var(--T) var(--ease),opacity .55s var(--ease),
    background .5s var(--ease),box-shadow .5s var(--ease),border-color .5s var(--ease),filter .5s var(--ease);}
.cx-band:hover{border-color:var(--hair2);}
.cx-head{flex:0 0 auto;display:flex;align-items:center;gap:14px;padding:20px 26px;transition:padding var(--T) var(--ease);}
.cx-kick{font-family:var(--mono);font-size:10px;letter-spacing:.2em;text-transform:uppercase;color:var(--faint);flex:0 0 132px;}
.cx-line{font-family:var(--serif);font-style:italic;font-size:18px;color:var(--ink);line-height:1.3;flex:1;transition:font-size var(--T) var(--ease),color .5s;}
.cx-line b{font-style:normal;font-weight:600;}
.cx-status{display:inline-flex;align-items:center;gap:8px;font-family:var(--mono);font-size:10px;letter-spacing:.12em;text-transform:uppercase;white-space:nowrap;}
.cx-dot{width:8px;height:8px;border-radius:50%;flex:0 0 auto;}
.cx-grip{flex:0 0 auto;width:18px;color:var(--faint);opacity:0;transition:opacity .4s;font-family:var(--mono);font-size:13px;text-align:center;}
.cx-band:hover .cx-grip{opacity:.7;}
.cx-body{flex:1;min-height:0;overflow:hidden;max-height:0;opacity:0;padding:0 26px;
  transition:max-height var(--T) var(--ease),opacity .5s var(--ease),padding var(--T) var(--ease);}

.cx-frame[data-subject="rest"] .cx-band{flex:1 1 0;opacity:1;}
.cx-frame[data-subject="rest"] .cx-body{max-height:320px;opacity:1;padding:0 26px 22px;}

.cx-band.is-subject{flex-grow:10;background:var(--paper);border-color:var(--hair2);border-left:2px solid var(--amber);
  box-shadow:0 1px 0 rgba(20,20,28,.02),0 24px 60px -22px rgba(20,20,34,.30);}
.cx-band.is-subject .cx-line{font-size:22px;}
.cx-band.is-subject .cx-body{max-height:820px;opacity:1;padding:0 26px 26px;}
.cx-band.is-collapsed{flex:0 0 60px;opacity:.5;filter:saturate(.5);background:var(--sub);}
.cx-band.is-collapsed .cx-head{padding:18px 26px;}
.cx-band.is-collapsed .cx-line{font-size:14px;font-style:normal;font-family:var(--sans);color:var(--muted);}
.cx-band.is-collapsed .cx-body{max-height:0;opacity:0;padding:0 26px;}
.cx-band.is-collapsed:hover{opacity:.85;filter:none;}

.cx-metrics{display:flex;gap:48px;flex-wrap:wrap;}
.cx-metric{display:flex;flex-direction:column;gap:7px;min-width:0;}
.cx-ml{font-family:var(--mono);font-size:9.5px;letter-spacing:.16em;text-transform:uppercase;color:var(--faint);}
.cx-mv{font-family:var(--sans);font-weight:300;font-size:34px;line-height:1;color:var(--ink);letter-spacing:-.02em;font-variant-numeric:tabular-nums;transition:color .4s;}
.cx-ms{font-family:var(--sans);font-size:12.5px;color:var(--muted);}
.cx-metric.live .cx-mv{color:var(--amber);}
.cx-reveal{max-height:0;opacity:0;overflow:hidden;transition:max-height var(--T) var(--ease),opacity .55s var(--ease) .08s,margin var(--T) var(--ease);}
.cx-band.is-subject .cx-reveal{max-height:560px;opacity:1;margin-top:26px;}
.cx-rule{height:1px;background:var(--hair);}

.cx-lever{display:flex;flex-direction:column;gap:16px;}
.cx-lvtop{display:flex;align-items:center;justify-content:space-between;}
.cx-lvtitle{font-family:var(--mono);font-size:10px;letter-spacing:.2em;text-transform:uppercase;color:var(--amber);}
.cx-lvends{display:flex;justify-content:space-between;font-family:var(--mono);font-size:10px;letter-spacing:.1em;text-transform:uppercase;color:var(--faint);}
.cx-lvends b{color:var(--ink);font-weight:500;}
.cx-track{position:relative;height:46px;border-radius:8px;background:var(--sub);border:1px solid var(--hair2);cursor:ew-resize;touch-action:none;user-select:none;}
.cx-fill{position:absolute;left:0;top:0;bottom:0;border-radius:8px 0 0 8px;background:linear-gradient(90deg,rgba(186,117,23,.10),rgba(186,117,23,.22));}
.cx-knob{position:absolute;top:50%;width:30px;height:30px;border-radius:50%;background:var(--paper);border:1.5px solid var(--amber);
  box-shadow:0 4px 14px -4px rgba(20,20,28,.4);transform:translate(-50%,-50%);display:flex;align-items:center;justify-content:center;}
.cx-knob:after{content:"";width:2px;height:12px;background:var(--amber);border-radius:2px;box-shadow:4px 0 0 var(--amber),-4px 0 0 var(--amber);}
.cx-ticks{position:absolute;inset:0;display:flex;justify-content:space-between;padding:0 22px;align-items:center;pointer-events:none;}
.cx-ticks span{width:1px;height:10px;background:var(--hair2);}
.cx-conseq{display:flex;gap:14px;}
.cx-cq{flex:1;background:var(--sub);border:1px solid var(--hair);border-radius:8px;padding:18px 20px;display:flex;flex-direction:column;gap:8px;transition:background .4s,border-color .4s;}
.cx-cq.hot{border-color:rgba(186,117,23,.45);background:var(--amber50);}
.cx-cql{font-family:var(--mono);font-size:9.5px;letter-spacing:.16em;text-transform:uppercase;color:var(--faint);}
.cx-cqv{font-family:var(--sans);font-weight:300;font-size:30px;line-height:1;color:var(--ink);letter-spacing:-.02em;font-variant-numeric:tabular-nums;}
.cx-cqs{font-family:var(--sans);font-size:12px;color:var(--muted);line-height:1.4;}
.cx-note{font-family:var(--serif);font-style:italic;font-size:16px;line-height:1.5;color:var(--muted);max-width:760px;}
.cx-still{aspect-ratio:16/9;width:260px;flex:0 0 260px;background:#0c0c0d;border-radius:6px;position:relative;overflow:hidden;}
.cx-still span{position:absolute;top:12px;left:14px;font-family:var(--mono);font-size:9px;letter-spacing:.18em;color:rgba(255,255,255,.6);}
.cx-feat{display:flex;align-items:center;gap:16px;padding:13px 0;border-top:1px solid var(--hair);}
.cx-feat:first-child{border-top:0;}
.cx-ft{font-family:var(--serif);font-style:italic;font-size:17px;color:var(--ink);flex:1;}
.cx-fm{font-family:var(--mono);font-size:11px;color:var(--faint);font-variant-numeric:tabular-nums;}
.cx-fs{font-family:var(--mono);font-size:9px;letter-spacing:.14em;text-transform:uppercase;}
.cx-moneyline{margin-top:18px;display:flex;align-items:center;gap:14px;padding:14px 18px;background:var(--sub);border:1px solid var(--hair);border-radius:8px;}

.cx-floor{flex:0 0 56px;display:flex;align-items:center;gap:16px;padding:0 40px;background:var(--paper);border-top:1px solid var(--hair);}
.cx-fk{font-family:var(--mono);font-size:9.5px;letter-spacing:.2em;text-transform:uppercase;color:var(--faint);}
.cx-ft2{font-family:var(--serif);font-style:italic;font-size:14.5px;color:var(--muted);}
.cx-ft2 b{color:var(--ink);font-style:normal;font-weight:500;}
@media (prefers-reduced-motion: reduce){.cx-band,.cx-body,.cx-reveal,.cx-mv{transition:none !important;}}
`;
(function injectCx() {
  if (document.getElementById('cx-styles')) return;
  const s = document.createElement('style');
  s.id = 'cx-styles';
  s.textContent = CX_CSS;
  document.head.appendChild(s);
})();
const CXC = window.CONTEXTUAL || {};
const cxMoney = n => '$' + Math.round(n).toLocaleString('en-US');
const cxR1k = n => Math.round(n / 1000) * 1000;
const CX_SEAT = {
  client: {
    lbl: 'Client',
    fear: 'Watching it take shape — health at a glance.',
    subject: 'rest'
  },
  producer: {
    lbl: 'Producer',
    fear: 'Holding the line — does anything break, and what it costs.',
    subject: 'budget'
  },
  director: {
    lbl: 'Director',
    fear: 'Shaping the work — the money reads beneath, one line.',
    subject: 'creative'
  }
};

// shared lever: 0 = NIGHT, 1 = DAY
function cxDerive(v) {
  const approved = 500000;
  const forecast = cxR1k(472000 + (454000 - 472000) * v);
  const ot = cxR1k(18000 * (1 - v));
  const under = approved - forecast;
  const conflict = v < 0.55;
  const cont = Math.max(0, Math.min(50000, 50000 - (forecast - 454000)));
  return {
    approved,
    forecast,
    ot,
    under,
    conflict,
    cont
  };
}
function CXLever({
  v,
  onV,
  title,
  hint,
  ends,
  idSuffix
}) {
  const trackRef = React.useRef(null);
  const drag = React.useRef(false);
  const pct = v * 88 + 6 + '%';
  function fromX(clientX) {
    const t = trackRef.current;
    if (!t) return;
    const r = t.getBoundingClientRect();
    onV(Math.max(0, Math.min(1, (clientX - r.left) / r.width)));
  }
  return /*#__PURE__*/React.createElement("div", {
    className: "cx-lever"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cx-lvtop"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-lvtitle"
  }, title), /*#__PURE__*/React.createElement("span", {
    className: "cx-status"
  }, hint)), /*#__PURE__*/React.createElement("div", {
    className: "cx-track",
    ref: trackRef,
    onPointerDown: e => {
      drag.current = true;
      try {
        e.currentTarget.setPointerCapture(e.pointerId);
      } catch (x) {}
      fromX(e.clientX);
      e.preventDefault();
      e.stopPropagation();
    },
    onPointerMove: e => {
      if (drag.current) fromX(e.clientX);
    },
    onPointerUp: () => {
      drag.current = false;
    },
    onPointerCancel: () => {
      drag.current = false;
    },
    onClick: e => e.stopPropagation()
  }, /*#__PURE__*/React.createElement("div", {
    className: "cx-fill",
    style: {
      width: pct
    }
  }), /*#__PURE__*/React.createElement("div", {
    className: "cx-ticks"
  }, /*#__PURE__*/React.createElement("span", null), /*#__PURE__*/React.createElement("span", null), /*#__PURE__*/React.createElement("span", null), /*#__PURE__*/React.createElement("span", null), /*#__PURE__*/React.createElement("span", null)), /*#__PURE__*/React.createElement("div", {
    className: "cx-knob",
    style: {
      left: pct
    }
  })), /*#__PURE__*/React.createElement("div", {
    className: "cx-lvends"
  }, ends));
}
function ContextualFrame({
  seat: initialSeat,
  onReset,
  onSeat
}) {
  const FK = window.FK;
  const [seat, setSeat] = React.useState(initialSeat && CX_SEAT[initialSeat] ? initialSeat : 'client');
  const [subject, setSubject] = React.useState(CX_SEAT[initialSeat && CX_SEAT[initialSeat] ? initialSeat : 'client'].subject);
  const [v, setV] = React.useState(0.06);
  const d = cxDerive(v);

  // Re-seed when the lane changes from outside (seat switch in the masthead).
  React.useEffect(() => {
    if (initialSeat && CX_SEAT[initialSeat]) {
      setSeat(initialSeat);
      setSubject(CX_SEAT[initialSeat].subject);
    }
  }, [initialSeat]);
  function chooseSeat(s) {
    if (s === 'director' && onSeat) {
      onSeat(s);
      return;
    } // hand off to the contextual edit bay
    setSeat(s);
    setSubject(CX_SEAT[s].subject);
    if (onSeat) onSeat(s);
  }
  function focus(id, e) {
    if (e) e.stopPropagation();
    if (subject !== id) setSubject(id);
  }
  function grab(id) {
    if (subject !== id) setSubject(id);
  }
  const cls = id => 'cx-band' + (subject !== 'rest' && subject === id ? ' is-subject' : subject !== 'rest' && subject !== id ? ' is-collapsed' : '');
  const project = window.CLIENT && window.CLIENT.project || {
    title: 'A Brief History of Selling Ideas',
    format: ':30 brand film',
    code: '#17433'
  };
  return /*#__PURE__*/React.createElement("div", {
    className: "cx-frame",
    "data-subject": subject
  }, /*#__PURE__*/React.createElement("div", {
    className: "cx-mast"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cx-lock"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-wm"
  }, "tenet"), /*#__PURE__*/React.createElement("span", {
    className: "cx-locklbl"
  }, CX_SEAT[seat].lbl), /*#__PURE__*/React.createElement("span", {
    className: "cx-div"
  }), /*#__PURE__*/React.createElement("span", {
    className: "cx-proj"
  }, project.title), /*#__PURE__*/React.createElement("span", {
    className: "cx-projmeta"
  }, project.format, " \xB7 ", project.code)), /*#__PURE__*/React.createElement("div", {
    className: "cx-mright"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-fear"
  }, CX_SEAT[seat].fear), /*#__PURE__*/React.createElement("div", {
    className: "cx-seats"
  }, ['client', 'producer', 'director'].map(s => /*#__PURE__*/React.createElement("button", {
    key: s,
    className: "cx-seat",
    "aria-pressed": seat === s,
    onClick: () => chooseSeat(s)
  }, CX_SEAT[s].lbl))), /*#__PURE__*/React.createElement("button", {
    className: "cx-resetbtn",
    onClick: onReset
  }, "\u21A9 Change lane"))), /*#__PURE__*/React.createElement("div", {
    className: "cx-bands"
  }, /*#__PURE__*/React.createElement("div", {
    className: cls('budget'),
    onClick: e => focus('budget', e)
  }, /*#__PURE__*/React.createElement("div", {
    className: "cx-head"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-kick"
  }, "Budget"), /*#__PURE__*/React.createElement("span", {
    className: "cx-line"
  }, "Forecast ", /*#__PURE__*/React.createElement("b", null, cxMoney(d.forecast)), " \u2014 under approved by ", /*#__PURE__*/React.createElement("b", null, cxMoney(d.under))), /*#__PURE__*/React.createElement("span", {
    className: "cx-status"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-dot",
    style: {
      background: d.forecast > d.approved ? 'var(--risk)' : 'var(--ok)'
    }
  }), d.forecast > d.approved ? 'Over' : 'Healthy'), /*#__PURE__*/React.createElement("span", {
    className: "cx-grip"
  }, "\u22EE\u22EE")), /*#__PURE__*/React.createElement("div", {
    className: "cx-body"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cx-metrics"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cx-metric"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-ml"
  }, "Approved"), /*#__PURE__*/React.createElement("span", {
    className: "cx-mv"
  }, cxMoney(d.approved)), /*#__PURE__*/React.createElement("span", {
    className: "cx-ms"
  }, "Master bid")), /*#__PURE__*/React.createElement("div", {
    className: "cx-metric live"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-ml"
  }, "Forecast final"), /*#__PURE__*/React.createElement("span", {
    className: "cx-mv"
  }, cxMoney(d.forecast)), /*#__PURE__*/React.createElement("span", {
    className: "cx-ms"
  }, cxMoney(d.under), " under")), /*#__PURE__*/React.createElement("div", {
    className: "cx-metric"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-ml"
  }, "Contingency"), /*#__PURE__*/React.createElement("span", {
    className: "cx-mv"
  }, cxMoney(d.cont)), /*#__PURE__*/React.createElement("span", {
    className: "cx-ms"
  }, "of $50,000 reserve"))), /*#__PURE__*/React.createElement("div", {
    className: "cx-reveal"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cx-rule",
    style: {
      marginBottom: 24
    }
  }), /*#__PURE__*/React.createElement(CXLever, {
    v: v,
    onV: nv => {
      grab('budget');
      setV(nv);
    },
    title: "The lever \u2014 apartment scene",
    hint: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
      className: "cx-dot",
      style: {
        background: 'var(--amber)'
      }
    }), "Drag to model"),
    ends: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", null, "Shoot ", /*#__PURE__*/React.createElement("b", null, "Night"), " \u2014 premium"), /*#__PURE__*/React.createElement("span", null, "Flip to ", /*#__PURE__*/React.createElement("b", null, "Day"), " \u2014 clears it"))
  }), /*#__PURE__*/React.createElement("div", {
    className: "cx-conseq",
    style: {
      marginTop: 24
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: 'cx-cq' + (d.forecast < d.approved && v < 0.5 ? ' hot' : '')
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-cql"
  }, "Forecast moves to"), /*#__PURE__*/React.createElement("span", {
    className: "cx-cqv"
  }, cxMoney(d.forecast)), /*#__PURE__*/React.createElement("span", {
    className: "cx-cqs"
  }, v < 0.5 ? 'Carrying the night premium' : 'Premium cleared')), /*#__PURE__*/React.createElement("div", {
    className: 'cx-cq' + (d.ot > 0 ? ' hot' : '')
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-cql"
  }, "Crew overtime"), /*#__PURE__*/React.createElement("span", {
    className: "cx-cqv"
  }, d.ot > 0 ? '+' + cxMoney(d.ot) : cxMoney(0)), /*#__PURE__*/React.createElement("span", {
    className: "cx-cqs"
  }, "Night exterior load")), /*#__PURE__*/React.createElement("div", {
    className: 'cx-cq' + (d.conflict ? ' hot' : '')
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-cql"
  }, "Delivery"), /*#__PURE__*/React.createElement("span", {
    className: "cx-cqv"
  }, "Jun 27"), /*#__PURE__*/React.createElement("span", {
    className: "cx-cqs"
  }, d.conflict ? '1 schedule conflict' : 'No conflicts — clear')))))), /*#__PURE__*/React.createElement("div", {
    className: cls('schedule'),
    onClick: e => focus('schedule', e)
  }, /*#__PURE__*/React.createElement("div", {
    className: "cx-head"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-kick"
  }, "Schedule"), /*#__PURE__*/React.createElement("span", {
    className: "cx-line"
  }, "Delivers ", /*#__PURE__*/React.createElement("b", null, "Jun 27"), " \u2014 editorial in progress, ", /*#__PURE__*/React.createElement("b", null, d.conflict ? '1 conflict' : '0 conflicts')), /*#__PURE__*/React.createElement("span", {
    className: "cx-status"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-dot",
    style: {
      background: d.conflict ? 'var(--watch)' : 'var(--ok)'
    }
  }), d.conflict ? 'Watch' : 'On track'), /*#__PURE__*/React.createElement("span", {
    className: "cx-grip"
  }, "\u22EE\u22EE")), /*#__PURE__*/React.createElement("div", {
    className: "cx-body"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cx-metrics"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cx-metric"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-ml"
  }, "Delivery"), /*#__PURE__*/React.createElement("span", {
    className: "cx-mv"
  }, "Jun 27"), /*#__PURE__*/React.createElement("span", {
    className: "cx-ms"
  }, "22 days out")), /*#__PURE__*/React.createElement("div", {
    className: "cx-metric"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-ml"
  }, "Stage"), /*#__PURE__*/React.createElement("span", {
    className: "cx-mv",
    style: {
      fontSize: 28
    }
  }, "Editorial"), /*#__PURE__*/React.createElement("span", {
    className: "cx-ms"
  }, "Picture lock waits on review")), /*#__PURE__*/React.createElement("div", {
    className: "cx-metric"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-ml"
  }, "Conflicts"), /*#__PURE__*/React.createElement("span", {
    className: "cx-mv"
  }, d.conflict ? '1' : '0'), /*#__PURE__*/React.createElement("span", {
    className: "cx-ms"
  }, d.conflict ? 'Night exterior vs. permit window' : 'Permit window clear'))), /*#__PURE__*/React.createElement("div", {
    className: "cx-reveal"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cx-rule",
    style: {
      marginBottom: 24
    }
  }), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "cx-feat"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-fs",
    style: {
      color: 'var(--ok)',
      flex: '0 0 70px'
    }
  }, "Wrapped"), /*#__PURE__*/React.createElement("span", {
    className: "cx-ft"
  }, "Principal photography"), /*#__PURE__*/React.createElement("span", {
    className: "cx-fm"
  }, "6 days")), /*#__PURE__*/React.createElement("div", {
    className: "cx-feat"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-fs",
    style: {
      color: 'var(--amber)',
      flex: '0 0 70px'
    }
  }, "Active"), /*#__PURE__*/React.createElement("span", {
    className: "cx-ft"
  }, "Editorial \u2014 assembling the cut"), /*#__PURE__*/React.createElement("span", {
    className: "cx-fm"
  }, "Jun 13 lock")), /*#__PURE__*/React.createElement("div", {
    className: "cx-feat"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-fs",
    style: {
      color: 'var(--faint)',
      flex: '0 0 70px'
    }
  }, "Upcoming"), /*#__PURE__*/React.createElement("span", {
    className: "cx-ft"
  }, "Color \xB7 sound \xB7 final delivery"), /*#__PURE__*/React.createElement("span", {
    className: "cx-fm"
  }, "Jun 27"))), /*#__PURE__*/React.createElement("div", {
    className: "cx-note",
    style: {
      marginTop: 22
    }
  }, d.conflict ? 'The night exterior collides with the permit window. Flipping it to day clears the conflict.' : 'Permit window is clear — the apartment runs in daylight and editorial holds its date.')))), /*#__PURE__*/React.createElement("div", {
    className: cls('creative'),
    onClick: e => focus('creative', e)
  }, /*#__PURE__*/React.createElement("div", {
    className: "cx-head"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-kick"
  }, "Creative"), /*#__PURE__*/React.createElement("span", {
    className: "cx-line"
  }, "The cut is landing \u2014 ", /*#__PURE__*/React.createElement("b", null, "1 open call"), ": how the apartment scene reads"), /*#__PURE__*/React.createElement("span", {
    className: "cx-status"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-dot",
    style: {
      background: 'var(--amber)'
    }
  }), "In review"), /*#__PURE__*/React.createElement("span", {
    className: "cx-grip"
  }, "\u22EE\u22EE")), /*#__PURE__*/React.createElement("div", {
    className: "cx-body"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 26,
      alignItems: 'flex-start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "cx-still"
  }, /*#__PURE__*/React.createElement("span", null, "PROGRAM \xB7 16\xD79")), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "cx-metrics",
    style: {
      gap: 40
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "cx-metric"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-ml"
  }, "Open decision"), /*#__PURE__*/React.createElement("span", {
    className: "cx-mv",
    style: {
      fontSize: 24
    }
  }, "Apartment"), /*#__PURE__*/React.createElement("span", {
    className: "cx-ms"
  }, "Night vs. day \u2014 the gift scene")), /*#__PURE__*/React.createElement("div", {
    className: "cx-metric"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-ml"
  }, "Owner"), /*#__PURE__*/React.createElement("span", {
    className: "cx-mv",
    style: {
      fontSize: 24
    }
  }, "Director"), /*#__PURE__*/React.createElement("span", {
    className: "cx-ms"
  }, "Producer holds the money lens"))))), /*#__PURE__*/React.createElement("div", {
    className: "cx-reveal"
  }, /*#__PURE__*/React.createElement("div", {
    className: "cx-rule",
    style: {
      margin: '24px 0'
    }
  }), /*#__PURE__*/React.createElement(CXLever, {
    v: v,
    onV: nv => {
      grab('creative');
      setV(nv);
    },
    title: "The call \u2014 how the apartment reads",
    hint: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
      className: "cx-dot",
      style: {
        background: 'var(--amber)'
      }
    }), "Drag to choose"),
    ends: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("b", null, "Night"), " \u2014 sells the intimacy"), /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("b", null, "Day"), " \u2014 clears the overtime"))
  }), /*#__PURE__*/React.createElement("div", {
    className: "cx-note",
    style: {
      marginTop: 24
    }
  }, v < 0.5 ? 'Night holds the intimacy of the gift. Your call — the money reads beneath, one line.' : 'Day is cleaner and clears the premium. Your call — the money reads beneath, one line.'), /*#__PURE__*/React.createElement("div", {
    className: "cx-moneyline"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-kick",
    style: {
      flex: '0 0 110px'
    }
  }, "Money lens"), /*#__PURE__*/React.createElement("span", {
    className: "cx-line",
    style: {
      fontSize: 16
    }
  }, "Night exterior \u2014 crew OT ", /*#__PURE__*/React.createElement("b", null, d.ot > 0 ? '+' + cxMoney(d.ot) : cxMoney(0))), /*#__PURE__*/React.createElement("span", {
    className: "cx-status"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-dot",
    style: {
      background: d.ot > 0 ? 'var(--watch)' : 'var(--ok)'
    }
  }), d.ot > 0 ? 'Carried' : 'Cleared'))))), /*#__PURE__*/React.createElement("div", {
    className: cls('scope'),
    onClick: e => focus('scope', e)
  }, /*#__PURE__*/React.createElement("div", {
    className: "cx-head"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-kick"
  }, "Scope"), /*#__PURE__*/React.createElement("span", {
    className: "cx-line"
  }, "Agreed deliverables on plan \u2014 ", /*#__PURE__*/React.createElement("b", null, "2 additions"), " logged and priced"), /*#__PURE__*/React.createElement("span", {
    className: "cx-status"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-dot",
    style: {
      background: 'var(--ok)'
    }
  }), "Clear"), /*#__PURE__*/React.createElement("span", {
    className: "cx-grip"
  }, "\u22EE\u22EE")), /*#__PURE__*/React.createElement("div", {
    className: "cx-body"
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "cx-feat"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-ft"
  }, ":30 master + :15 cutdown"), /*#__PURE__*/React.createElement("span", {
    className: "cx-fm"
  }, "Agreed"), /*#__PURE__*/React.createElement("span", {
    className: "cx-fs",
    style: {
      color: 'var(--ok)'
    }
  }, "In scope")), /*#__PURE__*/React.createElement("div", {
    className: "cx-feat"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-ft"
  }, "Social verticals (\xD73)"), /*#__PURE__*/React.createElement("span", {
    className: "cx-fm"
  }, "+$6,000"), /*#__PURE__*/React.createElement("span", {
    className: "cx-fs",
    style: {
      color: 'var(--amber)'
    }
  }, "Added")), /*#__PURE__*/React.createElement("div", {
    className: "cx-feat"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-ft"
  }, "Extended director's cut"), /*#__PURE__*/React.createElement("span", {
    className: "cx-fm"
  }, "+$0"), /*#__PURE__*/React.createElement("span", {
    className: "cx-fs",
    style: {
      color: 'var(--faint)'
    }
  }, "Proposed")))))), /*#__PURE__*/React.createElement("div", {
    className: "cx-floor"
  }, /*#__PURE__*/React.createElement("span", {
    className: "cx-fk"
  }, "Room"), /*#__PURE__*/React.createElement("span", {
    className: "cx-ft2"
  }, d.ot > 0 ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("b", null, "M. Hall \xB7 Producer:"), " Night is +", cxMoney(d.ot), " of crew OT. Day clears it \u2014 your call on the room, I just hold the line.") : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("b", null, "M. Hall \xB7 Producer:"), " Day it is \u2014 premium cleared, variance holds. Sending the updated board.")), /*#__PURE__*/React.createElement("div", {
    style: {
      marginLeft: 'auto',
      display: 'flex',
      alignItems: 'center',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "cx-resetbtn",
    disabled: subject === 'rest',
    onClick: () => setSubject('rest'),
    style: {
      opacity: subject === 'rest' ? 0.35 : 1
    }
  }, "\u21A9 Back to overview"))));
}
window.ContextualFrame = ContextualFrame;
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/contextual.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/creative-data.js
try { (() => {
/* ============================================================
   TENET — Creative Portal data model (v3: the Decision core).
   ------------------------------------------------------------
   Everything attaches to one Decision. Assets are evidence,
   Approvals are authority, CostDrivers are consequence — the
   bridge into the v2 money graph (FRAMEWORK / PROD).

   Governing principle, enforced structurally:
   • No field stores a system-generated creative judgment / score / rank.
   • AI output is only ever an Asset with origin HUMAN_DIRECTED_GENERATION.
   • The role enum holds people only — the AI can never hold a verb.
   • Analysis describes present state; it never advises.
   ============================================================ */
window.CREATIVE = function () {
  var F = window.FLOW,
    FW = window.FRAMEWORK;
  function money(n) {
    return '$' + Math.round(Math.abs(n)).toLocaleString('en-US');
  }
  function signed(n) {
    return (n < 0 ? '\u2212' : '+') + money(n);
  }
  var project = {
    title: F.TITLE,
    logline: F.LOGLINE,
    format: ':30 Brand Film',
    code: '#17433',
    director: 'D. Parker',
    cd: 'A. Wells',
    client: 'H&W',
    today: 'Jun 5, 2026'
  };

  // ── The five master questions (the Master Model spine) ──────
  var MASTER = {
    WHY: {
      label: 'Why',
      gloss: 'Purpose'
    },
    WHAT: {
      label: 'What',
      gloss: 'Story'
    },
    FEEL: {
      label: 'Feel',
      gloss: 'Emotion'
    },
    HOW: {
      label: 'How',
      gloss: 'Craft'
    },
    EXPERIENCE: {
      label: 'Experience',
      gloss: 'Delivery'
    }
  };

  // ── The cost ripple — one Decision, two lenses ──────────────
  // The apartment night exterior is the live cross-vertical link:
  // the FEEL decision carries a CostDriver; flip it and the
  // producer's forecast/variance move. Computed against FRAMEWORK,
  // never stored as truth.
  var S0 = F.DEFAULT_STATE;
  var stateNight = {
    mode: S0.mode,
    tod: {
      s4: 'NIGHT'
    }
  };
  var stateDay = {
    mode: S0.mode,
    tod: {
      s4: 'DAY'
    }
  };
  var tNight = FW ? FW.finTotals(stateNight) : {
    forecast: 472000,
    variance: -28000,
    approved: 500000
  };
  var tDay = FW ? FW.finTotals(stateDay) : {
    forecast: 454000,
    variance: -46000,
    approved: 500000
  };
  var ripple = {
    approved: tNight.approved,
    night: {
      tod: 'NIGHT',
      forecast: tNight.forecast,
      variance: tNight.variance
    },
    day: {
      tod: 'DAY',
      forecast: tDay.forecast,
      variance: tDay.variance
    },
    otDelta: tNight.forecast - tDay.forecast // the cost the FEEL choice carries
  };

  // ── Decisions (the keystone) ────────────────────────────────
  // facets = the same decision re-summarized in each role's language.
  var decisions = [{
    id: 'd1',
    master: 'WHY',
    title: 'Three-part structure',
    status: 'DECIDED',
    desc: 'The Old Way / The Shift / The Promise. The film argues by contrast.',
    by: 'Director',
    facets: {
      Director: ['Structure', 'Rhythm'],
      Producer: ['Locked scope']
    },
    assets: ['a-scr4', 'a-scr3'],
    approvals: [{
      role: 'DIRECTOR',
      verb: 'DECIDE',
      state: 'GRANTED',
      who: 'D. Parker',
      when: 'Mar 6'
    }, {
      role: 'CREATIVE_DIRECTOR',
      verb: 'APPROVE',
      state: 'GRANTED',
      who: 'A. Wells',
      when: 'Mar 7'
    }],
    cost: null
  }, {
    id: 'd2',
    master: 'WHAT',
    title: 'The ending beat',
    status: 'OPEN',
    desc: 'How the promise lands. The final image is still soft — the room has not converged.',
    by: 'Director',
    facets: {
      Director: ['Emotion', 'World'],
      Producer: ['Risk', 'Reshoot exposure']
    },
    assets: ['a-bd-end1', 'a-bd-end2', 'a-anim1'],
    approvals: [{
      role: 'DIRECTOR',
      verb: 'PROPOSE',
      state: 'PENDING',
      who: 'D. Parker',
      when: '\u2014'
    }],
    cost: null,
    flag: 'Client risk: ending beat still soft'
  }, {
    id: 'd3',
    master: 'FEEL',
    title: 'Apartment \u2014 night exterior vs. day',
    status: 'PROPOSED',
    desc: 'The gift scene. Night sells the intimacy; day clears the overtime premium.',
    by: 'Director',
    facets: {
      Director: ['World', 'Frame', 'Mood'],
      Producer: ['Money', 'Time']
    },
    assets: ['a-bd-apt1', 'a-bd-apt2', 'a-ref-apt'],
    approvals: [{
      role: 'DIRECTOR',
      verb: 'PROPOSE',
      state: 'GRANTED',
      who: 'D. Parker',
      when: 'May 28'
    }, {
      role: 'PRODUCER',
      verb: 'DECIDE',
      state: 'PENDING',
      who: 'M. Hall',
      when: '\u2014'
    }],
    cost: {
      driver: 'Night exterior \u2014 crew OT',
      amt: ripple.otDelta,
      removable: true,
      ripple: true
    }
  }, {
    id: 'd4',
    master: 'FEEL',
    title: 'Hold the beach for golden hour',
    status: 'PROPOSED',
    desc: 'The closing image. Golden hour is a half-day of OT but it sells the ending.',
    by: 'Director',
    facets: {
      Director: ['Emotion', 'Frame'],
      Producer: ['Money']
    },
    assets: ['a-bd-beach1', 'a-ref-beach'],
    approvals: [{
      role: 'DIRECTOR',
      verb: 'PROPOSE',
      state: 'GRANTED',
      who: 'D. Parker',
      when: 'May 30'
    }, {
      role: 'PRODUCER',
      verb: 'DECIDE',
      state: 'PENDING',
      who: 'M. Hall',
      when: '\u2014'
    }],
    cost: {
      driver: 'Golden-hour half-day OT',
      amt: 18000,
      removable: true
    }
  }, {
    id: 'd5',
    master: 'HOW',
    title: 'Opening technocrane move',
    status: 'DECIDED',
    desc: 'Booms up the face of the building into Times Square. Sets the scale.',
    by: 'Director',
    facets: {
      Director: ['Frame', 'Scale'],
      Producer: ['Money', 'Risk']
    },
    assets: ['a-bd-open1'],
    approvals: [{
      role: 'DIRECTOR',
      verb: 'DECIDE',
      state: 'GRANTED',
      who: 'D. Parker',
      when: 'Apr 2'
    }, {
      role: 'CINEMATOGRAPHER',
      verb: 'APPROVE',
      state: 'GRANTED',
      who: 'L. Cho',
      when: 'Apr 3'
    }],
    cost: {
      driver: 'Technocrane package',
      amt: 16000,
      removable: true
    }
  }, {
    id: 'd6',
    master: 'FEEL',
    title: 'Cast the lead \u2014 Mara',
    status: 'DECIDED',
    desc: 'The face the whole film rests on. Carries the watch, carries the day.',
    by: 'Director',
    facets: {
      Director: ['Character', 'Emotion'],
      Producer: ['Money']
    },
    assets: ['a-ref-cast'],
    approvals: [{
      role: 'DIRECTOR',
      verb: 'DECIDE',
      state: 'GRANTED',
      who: 'D. Parker',
      when: 'Mar 24'
    }, {
      role: 'CREATIVE_DIRECTOR',
      verb: 'APPROVE',
      state: 'GRANTED',
      who: 'A. Wells',
      when: 'Mar 25'
    }],
    cost: {
      driver: 'O/C principal buyout \u2014 2yr',
      amt: 38000,
      removable: false
    }
  }, {
    id: 'd7',
    master: 'EXPERIENCE',
    title: 'Approve final creative',
    status: 'PROPOSED',
    desc: 'The gate before the bid locks. The client sees the idea before a frame is spent.',
    by: 'Creative Director',
    facets: {
      Director: ['\u2014'],
      Producer: ['Lock the bid']
    },
    assets: ['a-scr4', 'a-anim1'],
    approvals: [{
      role: 'CREATIVE_DIRECTOR',
      verb: 'PROPOSE',
      state: 'GRANTED',
      who: 'A. Wells',
      when: 'Jun 4'
    }, {
      role: 'CLIENT',
      verb: 'APPROVE',
      state: 'PENDING',
      who: 'S. Metnick',
      when: '\u2014'
    }],
    cost: null
  }];

  // ── Assets (the evidence) ───────────────────────────────────
  // origin: HUMAN_UPLOAD | HUMAN_DIRECTED_GENERATION (no autonomous origin).
  var assets = [{
    id: 'a-scr4',
    d: 'd1',
    type: 'SCRIPT',
    origin: 'HUMAN_UPLOAD',
    v: 4,
    parent: 'a-scr3',
    label: 'A_Brief_History_v3.fdx',
    by: 'Director',
    state: 'GRANTED',
    scene: '\u2014'
  }, {
    id: 'a-scr3',
    d: 'd1',
    type: 'SCRIPT',
    origin: 'HUMAN_UPLOAD',
    v: 3,
    parent: 'a-scr2',
    label: 'A_Brief_History_v2.fdx',
    by: 'Director',
    state: 'GRANTED',
    scene: '\u2014'
  }, {
    id: 'a-scr2',
    d: 'd1',
    type: 'SCRIPT',
    origin: 'HUMAN_UPLOAD',
    v: 2,
    parent: 'a-scr1',
    label: 'A_Brief_History_v1b.fdx',
    by: 'Writer',
    state: 'GRANTED',
    scene: '\u2014'
  }, {
    id: 'a-scr1',
    d: 'd1',
    type: 'SCRIPT',
    origin: 'HUMAN_UPLOAD',
    v: 1,
    parent: null,
    label: 'A_Brief_History_rough.fdx',
    by: 'Writer',
    state: 'GRANTED',
    scene: '\u2014'
  }, {
    id: 'a-bd-open1',
    d: 'd5',
    type: 'BOARD',
    origin: 'HUMAN_DIRECTED_GENERATION',
    v: 2,
    parent: null,
    label: 'Opening crane \u2014 Times Sq',
    by: 'Director',
    state: 'GRANTED',
    scene: 'Rooftop',
    prompt: 'boom up 80th-floor glass into Times Square, dawn'
  }, {
    id: 'a-bd-apt1',
    d: 'd3',
    type: 'FRAME',
    origin: 'HUMAN_DIRECTED_GENERATION',
    v: 3,
    parent: 'a-bd-apt0',
    label: 'Apartment \u2014 night, the gift',
    by: 'Director',
    state: 'PENDING',
    scene: 'Apartment',
    prompt: 'warm practical interior, night, two figures, intimate'
  }, {
    id: 'a-bd-apt2',
    d: 'd3',
    type: 'FRAME',
    origin: 'HUMAN_DIRECTED_GENERATION',
    v: 1,
    parent: null,
    label: 'Apartment \u2014 day alt',
    by: 'Director',
    state: 'PENDING',
    scene: 'Apartment',
    prompt: 'soft daylight interior, two figures, window light'
  }, {
    id: 'a-ref-apt',
    d: 'd3',
    type: 'REFERENCE',
    origin: 'HUMAN_UPLOAD',
    v: 1,
    parent: null,
    label: 'Lighting ref \u2014 In the Mood for Love',
    by: 'Cinematographer',
    state: 'GRANTED',
    scene: 'Apartment'
  }, {
    id: 'a-bd-beach1',
    d: 'd4',
    type: 'FRAME',
    origin: 'HUMAN_DIRECTED_GENERATION',
    v: 2,
    parent: null,
    label: 'Beach \u2014 golden hour close',
    by: 'Director',
    state: 'PENDING',
    scene: 'Beach',
    prompt: 'wide beach, low sun, single figure at the shore'
  }, {
    id: 'a-ref-beach',
    d: 'd4',
    type: 'MOODBOARD',
    origin: 'HUMAN_UPLOAD',
    v: 1,
    parent: null,
    label: 'Endings moodboard',
    by: 'Director',
    state: 'GRANTED',
    scene: 'Beach'
  }, {
    id: 'a-bd-end1',
    d: 'd2',
    type: 'FRAME',
    origin: 'HUMAN_DIRECTED_GENERATION',
    v: 4,
    parent: 'a-bd-end0',
    label: 'Final image \u2014 option A',
    by: 'Director',
    state: 'PENDING',
    scene: 'Beach',
    prompt: 'watch face resolves to logo, warm'
  }, {
    id: 'a-bd-end2',
    d: 'd2',
    type: 'FRAME',
    origin: 'HUMAN_DIRECTED_GENERATION',
    v: 2,
    parent: null,
    label: 'Final image \u2014 option B',
    by: 'Director',
    state: 'PENDING',
    scene: 'Studio',
    prompt: 'macro strap, light bloom, resolve'
  }, {
    id: 'a-anim1',
    d: 'd2',
    type: 'ANIMATIC',
    origin: 'HUMAN_DIRECTED_GENERATION',
    v: 1,
    parent: null,
    label: 'Ending animatic \u2014 cutdown',
    by: 'Editor',
    state: 'PENDING',
    scene: '\u2014',
    prompt: 'assemble option A over VO, 6s'
  }, {
    id: 'a-ref-cast',
    d: 'd6',
    type: 'REFERENCE',
    origin: 'HUMAN_UPLOAD',
    v: 1,
    parent: null,
    label: 'Casting selects \u2014 Mara',
    by: 'Director',
    state: 'GRANTED',
    scene: '\u2014'
  }];

  // ── Script breakdown (Assets of type SCRIPT, versioned) ─────
  var scenesCount = F.SCENES.length;
  var locsCount = F.LOCS.length;
  var charsCount = F.CHARS.length;
  var scriptVersions = assets.filter(function (a) {
    return a.type === 'SCRIPT';
  });
  var breakdown = {
    scenes: scenesCount,
    locations: locsCount,
    characters: charsCount,
    pages: 3,
    runtime: F.fmtClock(F.totalLength(S0)),
    scenes_list: F.SCENES.map(function (s) {
      return {
        n: s.n,
        loc: F.locName(s.locId),
        tod: s.tod,
        chars: s.chars.map(function (c) {
          return F.charName(c);
        }),
        dynamic: !!s.todDynamic
      };
    })
  };

  // ── Boards (FRAME / BOARD / MOODBOARD / ANIMATIC for FEEL) ───
  var boards = assets.filter(function (a) {
    return ['FRAME', 'BOARD', 'MOODBOARD', 'ANIMATIC'].indexOf(a.type) >= 0;
  });

  // ── Edit — sequence + state-describing analysis (never advice) ─
  var seq = F.allShots(S0).map(function (sh, i) {
    var scene = F.SCENES.find(function (s) {
      return s.id === sh.sceneId;
    });
    return {
      n: i + 1,
      loc: F.locName(sh.locId),
      desc: sh.desc,
      dur: sh.dur,
      tod: F.todOf(scene, S0)
    };
  });
  var beats = [{
    key: 'open',
    t: 0.22
  }, {
    key: 'setup',
    t: 0.42
  }, {
    key: 'turn',
    t: 0.24
  }, {
    key: 'build',
    t: 0.52
  }, {
    key: 'peak',
    t: 0.92
  }, {
    key: 'land',
    t: 0.50
  }];
  // Descriptions of present state only. No "needs", "should", "better".
  var analysis = [{
    kind: 'Runtime',
    text: F.fmtClock(F.totalLength(S0)) + ' assembled across ' + seq.length + ' shots.'
  }, {
    kind: 'Pacing',
    text: 'Tension peaks at the ending beat; the turn at part two is the shortest hold.'
  }, {
    kind: 'Coverage',
    text: 'Scene 04 (Apartment) has no coverage of the hero product.'
  }, {
    kind: 'Coverage',
    text: 'Studio macro carries the only logo resolve in the cut.'
  }, {
    kind: 'VO',
    text: 'Six voiceover lines present; longest gap sits over the downtown sequence.'
  }];

  // ── Approvals / Collaboration (authority across roles) ──────
  var approvals = [];
  decisions.forEach(function (d) {
    d.approvals.forEach(function (ap) {
      approvals.push({
        decision: d.title,
        decision_id: d.id,
        master: d.master,
        role: ap.role,
        verb: ap.verb,
        state: ap.state,
        who: ap.who,
        when: ap.when
      });
    });
  });
  var comments = [{
    who: 'M. Hall',
    role: 'PRODUCER',
    when: '2h',
    decision: 'Apartment \u2014 night exterior vs. day',
    text: 'Night is +$18K of crew OT. Day clears it. Your call on the room \u2014 I just hold the line.'
  }, {
    who: 'A. Wells',
    role: 'CREATIVE_DIRECTOR',
    when: '5h',
    decision: 'The ending beat',
    text: 'Option A lands the watch better. Want the client eyes before we lock anything.'
  }, {
    who: 'L. Cho',
    role: 'CINEMATOGRAPHER',
    when: '1d',
    decision: 'Opening technocrane move',
    text: 'Crane is booked. Dawn window is tight but the move is worth it.'
  }];

  // role display
  var ROLE_LABEL = {
    CLIENT: 'Client',
    CREATIVE_DIRECTOR: 'Creative Dir.',
    DIRECTOR: 'Director',
    PRODUCER: 'Producer',
    EDITOR: 'Editor',
    CINEMATOGRAPHER: 'DP',
    PRODUCTION_DESIGNER: 'Prod. Designer',
    VFX_SUPERVISOR: 'VFX Sup.',
    WRITER: 'Writer'
  };
  function decAssets(d) {
    return assets.filter(function (a) {
      return a.d === d.id;
    });
  }

  // ── Writers Room — script versions, scene-by-scene, timeline-linked ─
  // Sections are the production scenes (so selecting one highlights its
  // clip on the cut). Each version reads a section from `base`, unless it
  // overrides the wording, or omits the scene entirely (not yet written).
  var VO = {
    s1: 'Every morning, the same quiet promise.',
    s2: 'Someone made this, by hand.',
    s3: 'The world hurries. You don\u2019t have to.',
    s4: 'Some gifts are really just time.',
    s5: null,
    s6: 'A life, held in the light.'
  };
  var wSections = F.SCENES.map(function (s) {
    var loc = F.LOCS.find(function (l) {
      return l.id === s.locId;
    }) || {};
    var slug = (loc.kind || 'EXT') + '. ' + F.locName(s.locId).toUpperCase() + (s.tod && s.tod !== '\u2014' ? ' \u2014 ' + s.tod : '');
    return {
      sceneId: s.id,
      n: s.n,
      loc: F.locName(s.locId),
      slug: slug,
      dur: s.shots.reduce(function (a, sh) {
        return a + sh.dur;
      }, 0)
    };
  });
  var wBase = {};
  F.SCENES.forEach(function (s) {
    var lines = s.action.map(function (a) {
      return {
        k: 'action',
        text: a.text
      };
    });
    if (VO[s.id]) lines.push({
      k: 'vo',
      text: VO[s.id]
    });
    wBase[s.id] = lines;
  });
  // earlier-draft wording (v2) and the rough pass (v1)
  var wOverrides = {
    v2: {
      s1: [{
        k: 'action',
        text: 'A city still blue with sleep. MARA stands at the rail.'
      }, {
        k: 'action',
        text: 'A watch catches the light.'
      }, {
        k: 'vo',
        text: 'Each morning, the same promise.'
      }],
      s4: [{
        k: 'action',
        text: 'THEO fastens the watch on MARA\u2019s wrist.'
      }, {
        k: 'action',
        text: 'A held look.'
      }, {
        k: 'vo',
        text: 'A gift of time.'
      }],
      s6: [{
        k: 'action',
        text: 'MARA at the shoreline. The watch, the light.'
      }, {
        k: 'vo',
        text: 'A life in the light.'
      }]
    },
    v1: {
      s1: [{
        k: 'action',
        text: 'Rooftop. A woman, a watch, first light.'
      }],
      s3: [{
        k: 'action',
        text: 'She walks the city.'
      }],
      s4: [{
        k: 'action',
        text: 'He gives her the watch.'
      }]
    }
  };
  var writers = {
    versions: [{
      id: 'v4',
      tag: 'Current',
      file: 'A_Brief_History_v3.fdx',
      date: 'Jun 4',
      by: 'Director',
      scenes: ['s1', 's2', 's3', 's4', 's5', 's6']
    }, {
      id: 'v3',
      tag: 'v3',
      file: 'A_Brief_History_v2.fdx',
      date: 'May 28',
      by: 'Director',
      scenes: ['s1', 's2', 's3', 's4', 's6']
    }, {
      id: 'v2',
      tag: 'v2',
      file: 'A_Brief_History_v1b.fdx',
      date: 'May 21',
      by: 'Writer',
      scenes: ['s1', 's2', 's3', 's4', 's6']
    }, {
      id: 'v1',
      tag: 'v1',
      file: 'A_Brief_History_rough.fdx',
      date: 'May 14',
      by: 'Writer',
      scenes: ['s1', 's3', 's4']
    }],
    sections: wSections,
    // resolve a version's lines for a scene, or null if not in that version
    resolve: function (versionId, sceneId) {
      var v = writers.versions.find(function (x) {
        return x.id === versionId;
      });
      if (!v || v.scenes.indexOf(sceneId) < 0) return null;
      return wOverrides[versionId] && wOverrides[versionId][sceneId] || wBase[sceneId];
    }
  };
  return {
    project: project,
    MASTER: MASTER,
    decisions: decisions,
    assets: assets,
    breakdown: breakdown,
    scriptVersions: scriptVersions,
    boards: boards,
    seq: seq,
    beats: beats,
    analysis: analysis,
    writers: writers,
    approvals: approvals,
    comments: comments,
    ripple: ripple,
    ROLE_LABEL: ROLE_LABEL,
    decAssets: decAssets,
    money: money,
    signed: signed,
    NAV: [{
      id: 'bay',
      label: 'Edit Bay',
      phase: 'work'
    }, {
      id: 'writers',
      label: 'Writers Room',
      phase: 'work'
    }, {
      id: 'project',
      label: 'Project',
      phase: 'graph'
    }, {
      id: 'edit',
      label: 'Edit',
      phase: 'graph'
    }, {
      id: 'assets',
      label: 'Assets',
      phase: 'graph'
    }]
  };
}();
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/creative-data.js", error: String((e && e.message) || e) }); }

// flow_ctx/creative-shell.jsx
try { (() => {
/* global React */
// TENET Creative lane — ONE dark cockpit. The edit bay is the landing (tab 1);
// the Creative Portal views are projections over the same Decision/Asset graph.
// Nav: Edit Bay │ Project · Script · Boards · Edit · Assets · Collaboration.
// `theme` drives the shared DK palette across the whole lane.
const {
  FK: LFK,
  FGlyph: LGl,
  CREATIVE: LC
} = window;
const LUse = React.useState,
  LRef = React.useRef;

// Message · Producer — the director's line to the producer. Persistent across
// every Creative tab (the edit bay embeds its own copy of this conversation).
const CRLANE_DRAFT = 'Can we hold the beach for golden hour? It sells the ending.';
function CreativeMessagePanel({
  ck,
  chat,
  onSend
}) {
  const bub = mine => ({
    alignSelf: mine ? 'flex-end' : 'flex-start',
    maxWidth: '90%',
    background: mine ? ck.amber : ck.bg3,
    color: mine ? ck.bg0 : ck.txt,
    border: mine ? 'none' : `1px solid ${ck.line2}`,
    borderRadius: 8,
    padding: '8px 11px',
    fontFamily: LFK.SANS,
    fontSize: 12.5,
    lineHeight: 1.4,
    marginBottom: 8
  });
  const who = mine => ({
    fontFamily: LFK.MONO,
    fontSize: 8.5,
    letterSpacing: '0.12em',
    textTransform: 'uppercase',
    color: mine ? 'rgba(0,0,0,0.5)' : ck.txt3,
    marginBottom: 3
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      padding: '14px 16px',
      borderBottom: `1px solid ${ck.line}`
    }
  }, /*#__PURE__*/React.createElement(LGl, {
    name: "chat",
    size: 13,
    style: {
      color: ck.amber
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: LFK.MONO,
      fontSize: 10,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: ck.amber
    }
  }, "Message \xB7 Producer")), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      overflowY: 'auto',
      padding: '14px 16px',
      display: 'flex',
      flexDirection: 'column'
    }
  }, chat.messages.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: bub(m.from === 'director')
  }, /*#__PURE__*/React.createElement("div", {
    style: who(m.from === 'director')
  }, m.from === 'director' ? 'You · Director' : 'Producer'), m.text)), chat.typing && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: LFK.SERIF,
      fontStyle: 'italic',
      fontSize: 12.5,
      color: ck.txt3,
      marginBottom: 7
    }
  }, "Producer is checking the numbers\u2026"), chat.messages.length === 0 && !chat.sent && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: LFK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: ck.txt3,
      lineHeight: 1.5,
      marginBottom: 'auto'
    }
  }, "A direct line to the producer. Send a question and watch it land in the budget.")), !chat.sent && /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      borderTop: `1px solid ${ck.line}`,
      padding: '12px 16px',
      display: 'flex',
      flexDirection: 'column',
      gap: 9
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: LFK.SANS,
      fontSize: 12.5,
      color: ck.txt,
      lineHeight: 1.4,
      padding: '9px 11px',
      background: ck.bg2,
      border: `1px solid ${ck.line2}`,
      borderRadius: 8
    }
  }, CRLANE_DRAFT), /*#__PURE__*/React.createElement("button", {
    onClick: onSend,
    style: {
      alignSelf: 'flex-end',
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      cursor: 'pointer',
      background: ck.amber,
      color: ck.bg0,
      border: 'none',
      borderRadius: 2,
      padding: '8px 15px',
      fontFamily: LFK.MONO,
      fontSize: 11,
      letterSpacing: '0.12em',
      textTransform: 'uppercase'
    }
  }, /*#__PURE__*/React.createElement(LGl, {
    name: "send",
    size: 13
  }), " Send")));
}
function CreativeLane(props) {
  const {
    state,
    cat,
    subIdx,
    flashId,
    dirChat,
    theme,
    onCat,
    onSub,
    onMode,
    onSendDir,
    onToggleTheme,
    onReset,
    onTod
  } = props;
  const [tab, setTab] = LUse('bay');
  const scrollRef = LRef(null);
  const ck = theme === 'light' ? window.DK_LIGHT : window.DK_DARK;
  function goTab(t) {
    setTab(t);
    if (scrollRef.current) scrollRef.current.scrollTop = 0;
  }
  const tod = state.tod && state.tod.s4 || 'NIGHT';
  const ctx = {
    goTab,
    tod,
    setTod: onTod,
    theme
  };
  const VIEWS = {
    project: window.CProject,
    edit: window.CEdit,
    assets: window.CAssets
  };
  const View = VIEWS[tab] || function () {
    return null;
  };
  const EditBay = window.EditBay;
  const WritersRoom = window.WritersRoom;
  const fullBleed = tab === 'bay' || tab === 'writers';
  const priorities = LC.decisions.filter(d => d.status === 'OPEN' || d.status === 'PROPOSED').length;
  const curPhase = (LC.NAV.find(n => n.id === tab) || {}).phase;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      background: ck.bg0,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      height: 56,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '0 26px',
      borderBottom: `1px solid ${ck.line}`,
      background: ck.bg1,
      position: 'relative',
      zIndex: 20
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 13
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: LFK.SERIF,
      fontStyle: 'italic',
      fontSize: 21,
      color: ck.txt,
      letterSpacing: '0.01em'
    }
  }, "tenet"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: LFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.24em',
      textTransform: 'uppercase',
      color: ck.amber,
      whiteSpace: 'nowrap'
    }
  }, "Creative"), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 1,
      height: 16,
      background: ck.line2,
      display: 'inline-block',
      alignSelf: 'center'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: LFK.SERIF,
      fontStyle: 'italic',
      fontSize: 14,
      color: ck.txt,
      whiteSpace: 'nowrap'
    }
  }, LC.project.title), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: LFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, LC.project.format, " \xB7 ", LC.project.code)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 18
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: () => goTab('project'),
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      background: 'none',
      border: 0,
      cursor: 'pointer',
      padding: 0,
      color: ck.txt2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 6,
      height: 6,
      borderRadius: 999,
      background: ck.amber
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: LFK.MONO,
      fontSize: 11,
      letterSpacing: '0.08em'
    }
  }, priorities, " open")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: `1px solid ${ck.line2}`,
      borderRadius: 3,
      overflow: 'hidden'
    }
  }, ['dark', 'light'].map((t, i) => /*#__PURE__*/React.createElement("div", {
    key: t,
    onClick: () => onToggleTheme(t),
    style: {
      padding: '5px 11px',
      cursor: 'pointer',
      fontFamily: LFK.MONO,
      fontSize: 10,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      borderLeft: i ? `1px solid ${ck.line2}` : 'none',
      background: theme === t ? ck.amber : ck.bg2,
      color: theme === t ? ck.bg0 : ck.txt2
    }
  }, t))), /*#__PURE__*/React.createElement("button", {
    onClick: onReset,
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      background: 'transparent',
      border: `1px solid ${ck.line2}`,
      borderRadius: 3,
      padding: '6px 12px',
      cursor: 'pointer',
      color: ck.txt2,
      fontFamily: LFK.MONO,
      fontSize: 10,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      whiteSpace: 'nowrap'
    }
  }, /*#__PURE__*/React.createElement(LGl, {
    name: "arrow",
    size: 12,
    style: {
      transform: 'scaleX(-1)'
    }
  }), " Change lane"))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      height: 48,
      display: 'flex',
      alignItems: 'stretch',
      padding: '0 22px',
      borderBottom: `1px solid ${ck.line}`,
      background: ck.bg1,
      position: 'relative',
      zIndex: 10
    }
  }, LC.NAV.map((n, idx) => {
    const on = n.id === tab;
    const prev = LC.NAV[idx - 1];
    const newPhase = prev && prev.phase !== n.phase;
    return /*#__PURE__*/React.createElement(React.Fragment, {
      key: n.id
    }, newPhase && /*#__PURE__*/React.createElement("span", {
      style: {
        alignSelf: 'center',
        width: 1,
        height: 16,
        background: ck.line2,
        margin: '0 14px'
      }
    }), /*#__PURE__*/React.createElement("button", {
      onClick: () => goTab(n.id),
      style: {
        position: 'relative',
        display: 'flex',
        alignItems: 'center',
        background: 'none',
        border: 0,
        borderBottom: on ? `2px solid ${ck.amber}` : '2px solid transparent',
        cursor: 'pointer',
        padding: '0 14px',
        marginBottom: -1,
        fontFamily: LFK.MONO,
        fontSize: 11,
        letterSpacing: '0.13em',
        textTransform: 'uppercase',
        color: on ? ck.txt : ck.txt3
      }
    }, n.label));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 340px',
      minWidth: 0,
      minHeight: 0,
      borderRight: `1px solid ${ck.line}`,
      background: ck.bg1,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement(CreativeMessagePanel, {
    ck: ck,
    chat: dirChat,
    onSend: onSendDir
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      minHeight: 0,
      display: 'flex',
      flexDirection: 'column'
    }
  }, tab === 'bay' ? EditBay ? /*#__PURE__*/React.createElement(EditBay, {
    state: state,
    cat: cat,
    subIdx: subIdx,
    flashId: flashId,
    theme: theme,
    onCat: onCat,
    onSub: onSub,
    onMode: onMode
  }) : null : tab === 'writers' ? WritersRoom ? /*#__PURE__*/React.createElement(WritersRoom, {
    ctx: ctx,
    ck: ck
  }) : null : /*#__PURE__*/React.createElement("div", {
    ref: scrollRef,
    style: {
      flex: 1,
      minHeight: 0,
      overflowY: 'auto',
      overflowX: 'hidden',
      background: ck.bg0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 1080,
      margin: '0 auto',
      padding: '36px 44px 64px'
    }
  }, /*#__PURE__*/React.createElement(View, {
    ctx: ctx,
    ck: ck
  }))))));
}
window.CreativeLane = CreativeLane;
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/creative-shell.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/creative-views.jsx
try { (() => {
/* global React */
// TENET Creative Portal — the six views over the Decision/Asset graph.
// All dark; each view takes the active palette `ck` (theme-driven) and `ctx`
// (shared lane state: goTab, tod ripple). The system describes & translates —
// it never scores, ranks, or recommends.
const {
  FK: CFK,
  FGlyph: CGl,
  CREATIVE: CC
} = window;

/* ── shared dark atoms ─────────────────────────────────────── */
function ccStatus(ck, s) {
  return {
    OPEN: ck.txt2,
    PROPOSED: ck.amber,
    DECIDED: ck.read,
    APPROVED: ck.read,
    LOCKED: ck.txt
  }[s] || ck.txt2;
}
function ccMaster(ck, m) {
  return {
    WHY: ck.txt2,
    WHAT: ck.amber,
    FEEL: ck.pink,
    HOW: ck.read,
    EXPERIENCE: ck.clipBlue
  }[m] || ck.txt2;
}
function ccVerbState(ck, st) {
  return {
    PENDING: ck.amber,
    GRANTED: ck.read,
    REJECTED: ck.pink
  }[st] || ck.txt2;
}
const ccKick = (ck, color) => ({
  fontFamily: CFK.MONO,
  fontSize: 10,
  letterSpacing: '0.2em',
  textTransform: 'uppercase',
  color: color || ck.amber,
  whiteSpace: 'nowrap'
});
const ccKickF = ck => ({
  fontFamily: CFK.MONO,
  fontSize: 9.5,
  letterSpacing: '0.16em',
  textTransform: 'uppercase',
  color: ck.txt3
});
function CCHead({
  ck,
  kicker,
  lede
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 22
    }
  }, kicker && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 11,
      letterSpacing: '0.22em',
      textTransform: 'uppercase',
      color: ck.amber
    }
  }, kicker), lede && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 10,
      maxWidth: 660,
      fontFamily: CFK.SERIF,
      fontStyle: 'italic',
      fontWeight: 300,
      fontSize: 16.5,
      lineHeight: 1.5,
      color: ck.txt2
    }
  }, lede));
}
function CCPanel({
  ck,
  label,
  title,
  children,
  pad,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: Object.assign({
      background: ck.bg1,
      border: `1px solid ${ck.line}`,
      borderRadius: 6,
      overflow: 'hidden'
    }, style)
  }, (label || title) && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 20px 0'
    }
  }, label && /*#__PURE__*/React.createElement("div", {
    style: ccKick(ck)
  }, label), title && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SANS,
      fontWeight: 300,
      fontSize: 21,
      color: ck.txt,
      marginTop: 7
    }
  }, title)), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: pad != null ? pad : '18px 20px'
    }
  }, children));
}
function CCBar({
  ck,
  pct,
  color,
  h
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      height: h || 6,
      background: ck.bg3,
      borderRadius: 999,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      width: Math.max(0, Math.min(100, pct)) + '%',
      background: color || ck.amber
    }
  }));
}
function CCChip({
  ck,
  on,
  tone,
  children
}) {
  const c = tone || ck.amber;
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6,
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.08em',
      textTransform: 'uppercase',
      padding: '4px 9px',
      borderRadius: 2,
      whiteSpace: 'nowrap',
      border: `1px solid ${on ? c : ck.line2}`,
      background: on ? ck.amberDim : 'transparent',
      color: on ? c : ck.txt2
    }
  }, children);
}
// origin tag — the structural "AI = human-directed evidence" signal
function CCOrigin({
  ck,
  origin
}) {
  const gen = origin === 'HUMAN_DIRECTED_GENERATION';
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 5,
      fontFamily: CFK.MONO,
      fontSize: 8.5,
      letterSpacing: '0.08em',
      textTransform: 'uppercase',
      color: gen ? ck.amber : ck.txt3
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 4,
      height: 4,
      borderRadius: 999,
      background: gen ? ck.amber : ck.txt3
    }
  }), gen ? 'Human-directed' : 'Upload');
}
const ccMoney = CC.money,
  ccSigned = CC.signed;

/* ══════════════════════════════════════════════════════════
   PROJECT — the decision graph. Priorities first.
   ══════════════════════════════════════════════════════════ */
function CProject({
  ctx,
  ck
}) {
  const [sel, setSel] = React.useState('d3');
  const d = CC.decisions.find(x => x.id === sel) || CC.decisions[0];
  const priorities = CC.decisions.filter(x => x.status === 'OPEN' || x.status === 'PROPOSED');
  const settled = CC.decisions.filter(x => x.status === 'DECIDED' || x.status === 'APPROVED' || x.status === 'LOCKED');
  const row = x => {
    const on = x.id === sel;
    return /*#__PURE__*/React.createElement("button", {
      key: x.id,
      onClick: () => setSel(x.id),
      style: {
        width: '100%',
        display: 'flex',
        alignItems: 'center',
        gap: 12,
        textAlign: 'left',
        cursor: 'pointer',
        padding: '13px 16px',
        background: on ? ck.amberDim : 'transparent',
        border: 0,
        borderTop: `1px solid ${ck.line}`,
        boxShadow: on ? `inset 3px 0 0 ${ck.amber}` : 'none'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: CFK.MONO,
        fontSize: 8.5,
        letterSpacing: '0.1em',
        textTransform: 'uppercase',
        color: ccMaster(ck, x.master),
        flex: '0 0 64px'
      }
    }, CC.MASTER[x.master].label), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        minWidth: 0,
        fontFamily: CFK.SANS,
        fontSize: 14,
        color: on ? ck.txt : ck.txt,
        whiteSpace: 'nowrap',
        overflow: 'hidden',
        textOverflow: 'ellipsis'
      }
    }, x.title), x.cost && /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: CFK.MONO,
        fontSize: 10,
        color: ck.amber,
        whiteSpace: 'nowrap'
      }
    }, ccSigned(x.cost.amt)), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: CFK.MONO,
        fontSize: 8.5,
        letterSpacing: '0.1em',
        textTransform: 'uppercase',
        color: ccStatus(ck, x.status),
        flex: '0 0 64px',
        textAlign: 'right'
      }
    }, x.status));
  };
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(CCHead, {
    ck: ck,
    kicker: "Project",
    lede: "Everything the film is deciding right now \u2014 and who owns each call. TENET carries decisions between specialists and shows their consequence; it never casts a vote."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 420px',
      gap: 24,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 18
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${ck.line}`,
      borderRadius: 6,
      background: ck.bg1,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '13px 16px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: ccKick(ck)
  }, "Current priorities"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 10,
      color: ck.txt3
    }
  }, priorities.length, " awaiting a human")), priorities.map(row)), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${ck.line}`,
      borderRadius: 6,
      background: ck.bg1,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '13px 16px'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: ccKickF(ck)
  }, "Settled")), settled.map(row)), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${ck.line}`,
      borderRadius: 6,
      background: ck.bg1,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '13px 16px'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: ccKickF(ck)
  }, "Room notes")), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '2px 16px 16px',
      display: 'flex',
      flexDirection: 'column',
      gap: 15
    }
  }, CC.comments.map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      borderLeft: `2px solid ${ck.line2}`,
      paddingLeft: 13
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      gap: 8,
      marginBottom: 4
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 8,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 13,
      color: ck.txt
    }
  }, c.who), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 8.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, CC.ROLE_LABEL[c.role] || c.role)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      color: ck.txt3
    }
  }, c.when)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      letterSpacing: '0.06em',
      color: ck.amber,
      marginBottom: 4
    }
  }, c.decision), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 13,
      color: ck.txt2,
      lineHeight: 1.5
    }
  }, c.text)))))), /*#__PURE__*/React.createElement(CDecisionDetail, {
    ctx: ctx,
    ck: ck,
    d: d
  })));
}
function CDecisionDetail({
  ctx,
  ck,
  d
}) {
  const rip = d.cost && d.cost.ripple;
  const tod = ctx.tod; // 'NIGHT' | 'DAY'
  const seeing = tod === 'NIGHT' ? CC.ripple.night : CC.ripple.day;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${ck.line2}`,
      borderRadius: 6,
      background: ck.bg1,
      overflow: 'hidden',
      position: 'sticky',
      top: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 20px',
      borderBottom: `1px solid ${ck.line}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      marginBottom: 9
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: ccMaster(ck, d.master)
    }
  }, CC.MASTER[d.master].label, " \xB7 ", CC.MASTER[d.master].gloss), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: ccStatus(ck, d.status)
    }
  }, d.status)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SANS,
      fontWeight: 300,
      fontSize: 22,
      lineHeight: 1.15,
      color: ck.txt
    }
  }, d.title), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SERIF,
      fontStyle: 'italic',
      fontSize: 14,
      color: ck.txt2,
      marginTop: 7,
      lineHeight: 1.4
    }
  }, d.desc)), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 20px',
      borderBottom: `1px solid ${ck.line}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, ccKickF(ck), {
      marginBottom: 12
    })
  }, "Read in each language"), Object.keys(d.facets).map(role => /*#__PURE__*/React.createElement("div", {
    key: role,
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 12,
      marginBottom: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.08em',
      textTransform: 'uppercase',
      color: ck.txt3,
      flex: '0 0 70px'
    }
  }, role), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 6
    }
  }, d.facets[role].map((f, i) => /*#__PURE__*/React.createElement(CCChip, {
    key: i,
    ck: ck,
    tone: role === 'Producer' ? ck.amber : ck.read,
    on: true
  }, f)))))), d.cost && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 20px',
      borderBottom: `1px solid ${ck.line}`,
      background: ck.bg2
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: Object.assign({}, ccKickF(ck))
  }, "Consequence"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 8.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, "Producer lens")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      marginBottom: rip ? 14 : 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 14,
      color: ck.txt
    }
  }, d.cost.driver), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 14,
      color: ck.amber
    }
  }, ccSigned(d.cost.amt))), rip ? /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: `1px solid ${ck.line2}`,
      borderRadius: 3,
      overflow: 'hidden',
      marginBottom: 14
    }
  }, [['NIGHT', 'Night'], ['DAY', 'Day']].map(([v, label], i) => /*#__PURE__*/React.createElement("div", {
    key: v,
    onClick: () => ctx.setTod(v),
    style: {
      padding: '6px 18px',
      cursor: 'pointer',
      fontFamily: CFK.MONO,
      fontSize: 10,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      borderLeft: i ? `1px solid ${ck.line2}` : 'none',
      background: tod === v ? ck.amber : ck.bg3,
      color: tod === v ? ck.bg0 : ck.txt2
    }
  }, label))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 1,
      background: ck.line,
      border: `1px solid ${ck.line}`,
      borderRadius: 4,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement(RipCell, {
    ck: ck,
    label: "Forecast final",
    value: ccMoney(seeing.forecast)
  }), /*#__PURE__*/React.createElement(RipCell, {
    ck: ck,
    label: "Variance",
    value: seeing.variance === 0 ? '$0' : ccSigned(seeing.variance),
    tone: seeing.variance > 0 ? ck.amber : ck.read
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SERIF,
      fontStyle: 'italic',
      fontSize: 12.5,
      color: ck.txt3,
      marginTop: 10,
      lineHeight: 1.4
    }
  }, tod === 'NIGHT' ? 'Night holds the intimacy — and ' + ccMoney(CC.ripple.otDelta) + ' of crew OT. The producer sees it live.' : 'Flipping to day clears the overtime. The variance moves the moment you choose.')) : /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ck.txt3,
      marginTop: 4
    }
  }, d.cost.removable ? 'Removable in scenarios' : 'Committed')), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 20px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, ccKickF(ck), {
      marginBottom: 12
    })
  }, "Approvals"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, d.approvals.map((ap, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 10,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 13.5,
      color: ck.txt
    }
  }, CC.ROLE_LABEL[ap.role] || ap.role), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, ap.verb)), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 7
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ccVerbState(ck, ap.state)
    }
  }, ap.state), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 5,
      height: 5,
      borderRadius: 999,
      background: ccVerbState(ck, ap.state)
    }
  }))))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 14,
      paddingTop: 12,
      borderTop: `1px solid ${ck.line}`,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, d.assets.length, " asset", d.assets.length === 1 ? '' : 's', " of evidence"), /*#__PURE__*/React.createElement("button", {
    onClick: () => ctx.goTab('edit'),
    style: {
      background: 'none',
      border: 0,
      padding: 0,
      cursor: 'pointer',
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6,
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ck.amber
    }
  }, "See evidence ", /*#__PURE__*/React.createElement(CGl, {
    name: "arrow",
    size: 11
  })))));
}
function RipCell({
  ck,
  label,
  value,
  tone
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      background: ck.bg1,
      padding: '12px 14px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: ccKickF(ck)
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SANS,
      fontWeight: 300,
      fontSize: 22,
      marginTop: 6,
      color: tone || ck.txt,
      fontVariantNumeric: 'tabular-nums'
    }
  }, value));
}

/* ══════════════════════════════════════════════════════════
   SCRIPT — versioned, broken down.
   ══════════════════════════════════════════════════════════ */
function CScript({
  ctx,
  ck
}) {
  const b = CC.breakdown;
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(CCHead, {
    ck: ck,
    kicker: "Script",
    title: "Versioned, and broken down",
    lede: "Every draft keeps its lineage. The breakdown extracts scenes, locations, and characters into the graph \u2014 it stores and parses, it doesn't rewrite."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '380px 1fr',
      gap: 24,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement(CCPanel, {
    ck: ck,
    label: "Version lineage",
    title: null
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, CC.scriptVersions.map((v, i) => {
    const latest = i === 0;
    return /*#__PURE__*/React.createElement("div", {
      key: v.id,
      style: {
        display: 'flex',
        alignItems: 'flex-start',
        gap: 12,
        padding: '11px 0',
        borderTop: i ? `1px solid ${ck.line}` : 'none'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: CFK.MONO,
        fontSize: 11,
        color: latest ? ck.amber : ck.txt3,
        flex: '0 0 28px'
      }
    }, "v", v.v), /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        minWidth: 0
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: CFK.MONO,
        fontSize: 11.5,
        color: ck.txt,
        whiteSpace: 'nowrap',
        overflow: 'hidden',
        textOverflow: 'ellipsis'
      }
    }, v.label), /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: CFK.SANS,
        fontSize: 11.5,
        color: ck.txt3,
        marginTop: 2
      }
    }, v.by, v.parent ? ' · from v' + (v.v - 1) : ' · origin')), latest && /*#__PURE__*/React.createElement(CCChip, {
      ck: ck,
      on: true,
      tone: ck.read
    }, "Current"));
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(4, 1fr)',
      gap: 1,
      background: ck.line,
      border: `1px solid ${ck.line}`,
      borderRadius: 6,
      overflow: 'hidden'
    }
  }, [['Scenes', b.scenes], ['Locations', b.locations], ['Characters', b.characters], ['Runtime', b.runtime]].map(([k, v], i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      background: ck.bg1,
      padding: '16px 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: ccKickF(ck)
  }, k), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SANS,
      fontWeight: 300,
      fontSize: 26,
      marginTop: 6,
      color: ck.txt
    }
  }, v)))), /*#__PURE__*/React.createElement(CCPanel, {
    ck: ck,
    label: "Scene breakdown",
    title: null,
    pad: 0
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '36px 1fr 110px 1fr',
      gap: 12,
      padding: '12px 18px',
      borderBottom: `1px solid ${ck.line}`
    }
  }, ['#', 'Location', 'Time', 'Cast'].map((h, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: ccKickF(ck)
  }, h))), b.scenes_list.map((s, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'grid',
      gridTemplateColumns: '36px 1fr 110px 1fr',
      gap: 12,
      padding: '12px 18px',
      alignItems: 'center',
      borderBottom: i < b.scenes_list.length - 1 ? `1px solid ${ck.line}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 11,
      color: ck.txt3
    }
  }, String(s.n).padStart(2, '0')), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 13.5,
      color: ck.txt
    }
  }, s.loc), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 7,
      fontFamily: CFK.MONO,
      fontSize: 10,
      letterSpacing: '0.06em',
      color: s.dynamic ? ck.amber : ck.txt2
    }
  }, s.tod, s.dynamic && /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 8,
      letterSpacing: '0.08em',
      color: ck.amber,
      border: `1px solid ${ck.line2}`,
      borderRadius: 2,
      padding: '1px 4px'
    }
  }, "VAR")), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 12.5,
      color: ck.txt2
    }
  }, s.chars.length ? s.chars.join(', ') : '—')))))));
}

/* ══════════════════════════════════════════════════════════
   BOARDS — frames as evidence. Explore/Refine = play; Lock/Promote = human verbs.
   ══════════════════════════════════════════════════════════ */
function BoardTile({
  ck,
  a,
  locked,
  sel,
  onSelect
}) {
  const stripe = ck === window.DK_LIGHT ? 'repeating-linear-gradient(135deg, #E7E2D8 0 10px, #ded8cc 10px 20px)' : 'repeating-linear-gradient(135deg, #20262d 0 10px, #1a1f25 10px 20px)';
  return /*#__PURE__*/React.createElement("button", {
    onClick: onSelect,
    style: {
      textAlign: 'left',
      cursor: 'pointer',
      background: ck.bg1,
      border: `1px solid ${sel ? ck.amber : ck.line2}`,
      borderRadius: 5,
      overflow: 'hidden',
      padding: 0,
      boxShadow: sel ? `0 0 0 1px ${ck.amber}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: '100%',
      aspectRatio: '16 / 9',
      background: stripe,
      borderBottom: `1px solid ${ck.line}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 8,
      left: 9,
      fontFamily: CFK.MONO,
      fontSize: 8.5,
      letterSpacing: '0.12em',
      color: ck.txt3
    }
  }, a.type, " \xB7 v", a.v), locked && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 8,
      right: 9,
      fontFamily: CFK.MONO,
      fontSize: 8.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ck.read
    }
  }, "\u25C9 Locked"), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      bottom: 0,
      padding: '7px 9px',
      background: 'linear-gradient(transparent, rgba(0,0,0,0.35))'
    }
  }, /*#__PURE__*/React.createElement(CCOrigin, {
    ck: ck,
    origin: a.origin
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '10px 11px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 12.5,
      color: ck.txt,
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, a.label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      color: ck.txt3,
      marginTop: 3
    }
  }, a.scene)));
}
function CBoards({
  ctx,
  ck
}) {
  const [sel, setSel] = React.useState(CC.boards[0].id);
  const [locked, setLocked] = React.useState({});
  const a = CC.boards.find(x => x.id === sel) || CC.boards[0];
  const isLocked = !!locked[a.id];
  function toggleLock() {
    setLocked(p => Object.assign({}, p, {
      [a.id]: !p[a.id]
    }));
  }
  const actionBtn = (label, primary, onClick) => /*#__PURE__*/React.createElement("button", {
    onClick: onClick,
    style: {
      cursor: 'pointer',
      fontFamily: CFK.MONO,
      fontSize: 10.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      padding: '9px 14px',
      borderRadius: 3,
      border: primary ? 'none' : `1px solid ${ck.line2}`,
      background: primary ? ck.amber : 'transparent',
      color: primary ? ck.bg0 : ck.txt2
    }
  }, label);
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(CCHead, {
    ck: ck,
    kicker: "Boards",
    lede: "Frames as evidence \u2014 explore and refine freely; every one is something a human chose to generate. Lock and Promote are human calls."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 380px',
      gap: 24,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(3, 1fr)',
      gap: 14
    }
  }, CC.boards.map(bd => /*#__PURE__*/React.createElement(BoardTile, {
    key: bd.id,
    ck: ck,
    a: bd,
    locked: !!locked[bd.id],
    sel: bd.id === sel,
    onSelect: () => setSel(bd.id)
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${ck.line2}`,
      borderRadius: 6,
      background: ck.bg1,
      overflow: 'hidden',
      position: 'sticky',
      top: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 20px',
      borderBottom: `1px solid ${ck.line}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      marginBottom: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: ccKick(ck)
  }, a.type, " \xB7 v", a.v), /*#__PURE__*/React.createElement(CCOrigin, {
    ck: ck,
    origin: a.origin
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SANS,
      fontWeight: 300,
      fontSize: 19,
      color: ck.txt
    }
  }, a.label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 10,
      color: ck.txt3,
      marginTop: 4
    }
  }, a.scene, " \xB7 by ", a.by)), a.prompt && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '14px 20px',
      borderBottom: `1px solid ${ck.line}`,
      background: ck.bg2
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, ccKickF(ck), {
      marginBottom: 7
    })
  }, "Prompt history \xB7 lineage, not authorship"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SCRIPT || CFK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: ck.txt2,
      lineHeight: 1.45
    }
  }, "\u201C", a.prompt, "\u201D")), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 20px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 8,
      marginBottom: 12
    }
  }, actionBtn('Explore', false, () => {}), actionBtn('Refine', false, () => {})), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 8
    }
  }, actionBtn(isLocked ? 'Unlock frame' : 'Lock frame', !isLocked, toggleLock), actionBtn('Promote to edit', false, () => ctx.goTab('edit'))), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SERIF,
      fontStyle: 'italic',
      fontSize: 12,
      color: ck.txt3,
      marginTop: 14,
      lineHeight: 1.4
    }
  }, isLocked ? 'Locked by a human decision — recorded as an approval.' : 'Exploring freely. Nothing is decided until a person locks it.')))));
}

/* ══════════════════════════════════════════════════════════
   EDIT — the cut + analysis that DESCRIBES present state (never advises).
   ══════════════════════════════════════════════════════════ */
function ccSmoothPath(pts) {
  if (pts.length < 2) return '';
  let dd = `M ${pts[0].x} ${pts[0].y}`;
  for (let i = 0; i < pts.length - 1; i++) {
    const p0 = pts[i - 1] || pts[i],
      p1 = pts[i],
      p2 = pts[i + 1],
      p3 = pts[i + 2] || p2;
    const c1x = p1.x + (p2.x - p0.x) / 6,
      c1y = p1.y + (p2.y - p0.y) / 6;
    const c2x = p2.x - (p3.x - p1.x) / 6,
      c2y = p2.y - (p3.y - p1.y) / 6;
    dd += ` C ${c1x.toFixed(1)} ${c1y.toFixed(1)}, ${c2x.toFixed(1)} ${c2y.toFixed(1)}, ${p2.x.toFixed(1)} ${p2.y.toFixed(1)}`;
  }
  return dd;
}
function CEdit({
  ctx,
  ck
}) {
  const beats = CC.beats;
  const padX = 4,
    padT = 12,
    padB = 12;
  const X = i => padX + i / (beats.length - 1) * (100 - 2 * padX);
  const Y = t => padT + (1 - t) * (100 - padT - padB);
  const pts = beats.map((b, i) => ({
    x: X(i),
    y: Y(b.t)
  }));
  const line = ccSmoothPath(pts);
  const area = line + ` L ${pts[pts.length - 1].x.toFixed(1)} 100 L ${pts[0].x.toFixed(1)} 100 Z`;
  const totalDur = CC.seq.reduce((a, s) => a + s.dur, 0);
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(CCHead, {
    ck: ck,
    kicker: "Edit",
    lede: "The sequence the room is reading. The analysis describes what is present \u2014 pacing, coverage, voiceover \u2014 it never says what should change."
  }), /*#__PURE__*/React.createElement(CCPanel, {
    ck: ck,
    label: "Sequence",
    title: null,
    style: {
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 4,
      height: 56,
      marginBottom: 8
    }
  }, CC.seq.map((s, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    title: s.desc,
    style: {
      flex: s.dur,
      minWidth: 0,
      background: s.tod === 'NIGHT' ? ck.clipGray : ck.clipBlue,
      border: `1px solid ${ck.clipEdge}`,
      borderRadius: 2,
      display: 'flex',
      flexDirection: 'column',
      justifyContent: 'space-between',
      padding: '5px 7px',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 8.5,
      color: ck.clipText,
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, s.loc), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 8,
      color: ck.clipSub
    }
  }, String(s.n).padStart(2, '0'))))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      color: ck.txt3
    }
  }, "0:00"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      color: ck.txt3
    }
  }, window.FLOW.fmtClock(totalDur)))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1.3fr 1fr',
      gap: 16,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement(CCPanel, {
    ck: ck,
    label: "Pacing",
    title: "Tension across the cut"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      height: 150
    }
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: "0 0 100 100",
    width: "100%",
    height: "100%",
    preserveAspectRatio: "none",
    style: {
      display: 'block',
      overflow: 'visible'
    }
  }, /*#__PURE__*/React.createElement("defs", null, /*#__PURE__*/React.createElement("linearGradient", {
    id: "cedit-stroke",
    x1: "0",
    y1: "0",
    x2: "1",
    y2: "0"
  }, /*#__PURE__*/React.createElement("stop", {
    offset: "0",
    stopColor: ck.read
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "0.42",
    stopColor: ck.amber
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "0.78",
    stopColor: ck.pink
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "1",
    stopColor: ck.amber
  })), /*#__PURE__*/React.createElement("linearGradient", {
    id: "cedit-fill",
    x1: "0",
    y1: "0",
    x2: "0",
    y2: "1"
  }, /*#__PURE__*/React.createElement("stop", {
    offset: "0",
    stopColor: ck.amber,
    stopOpacity: "0.20"
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "1",
    stopColor: ck.amber,
    stopOpacity: "0"
  }))), /*#__PURE__*/React.createElement("path", {
    d: area,
    fill: "url(#cedit-fill)"
  }), /*#__PURE__*/React.createElement("path", {
    d: line,
    fill: "none",
    stroke: "url(#cedit-stroke)",
    strokeWidth: "3",
    strokeLinecap: "round",
    vectorEffect: "non-scaling-stroke"
  })), beats.map((b, i) => b.t > 0.85 && /*#__PURE__*/React.createElement("div", {
    key: b.key,
    style: {
      position: 'absolute',
      left: pts[i].x + '%',
      top: pts[i].y + '%',
      transform: 'translate(-50%,-50%)',
      width: 13,
      height: 13,
      borderRadius: '50%',
      background: ck.bg0,
      border: `2.5px solid ${ck.pink}`,
      boxSizing: 'border-box'
    }
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: `repeat(${beats.length}, 1fr)`,
      marginTop: 8
    }
  }, beats.map(b => /*#__PURE__*/React.createElement("span", {
    key: b.key,
    style: {
      textAlign: 'center',
      fontFamily: CFK.MONO,
      fontSize: 10,
      letterSpacing: '0.06em',
      color: b.t > 0.85 ? ck.amber : ck.txt3,
      fontWeight: b.t > 0.85 ? 700 : 400
    }
  }, b.key)))), /*#__PURE__*/React.createElement(CCPanel, {
    ck: ck,
    label: "Creative analysis",
    title: null,
    pad: 0
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '4px 20px 8px'
    }
  }, CC.analysis.map((an, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      gap: 12,
      padding: '13px 0',
      borderTop: i ? `1px solid ${ck.line}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: ck.txt3,
      flex: '0 0 64px',
      paddingTop: 2
    }
  }, an.kind), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 13,
      color: ck.txt,
      lineHeight: 1.45
    }
  }, an.text)))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '12px 20px',
      borderTop: `1px solid ${ck.line}`,
      background: ck.bg2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SERIF,
      fontStyle: 'italic',
      fontSize: 12.5,
      color: ck.txt3,
      lineHeight: 1.4
    }
  }, "Describes what's there. Judgment stays with the room.")))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 32,
      paddingTop: 28,
      borderTop: `1px solid ${ck.line2}`
    }
  }, /*#__PURE__*/React.createElement(CBoards, {
    ctx: ctx,
    ck: ck
  })));
}

/* ══════════════════════════════════════════════════════════
   ASSETS — every row, filterable.
   ══════════════════════════════════════════════════════════ */
function CAssets({
  ctx,
  ck
}) {
  const types = ['ALL'].concat(Array.from(new Set(CC.assets.map(a => a.type))));
  const [filter, setFilter] = React.useState('ALL');
  const rows = CC.assets.filter(a => filter === 'ALL' || a.type === filter);
  const COLS = '120px 1fr 130px 70px 90px 110px';
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(CCHead, {
    ck: ck,
    kicker: "Assets",
    lede: "All assets, by type, version, and who made them. Generated frames carry their origin \u2014 a human directed every one."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 12,
      marginBottom: 16,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 8
    }
  }, types.map(t => /*#__PURE__*/React.createElement("button", {
    key: t,
    onClick: () => setFilter(t),
    style: {
      cursor: 'pointer',
      background: 'none',
      border: 0,
      padding: 0
    }
  }, /*#__PURE__*/React.createElement(CCChip, {
    ck: ck,
    on: filter === t
  }, t === 'ALL' ? 'All' : t)))), /*#__PURE__*/React.createElement("button", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      cursor: 'pointer',
      background: ck.amber,
      color: ck.bg0,
      border: 'none',
      borderRadius: 3,
      padding: '8px 15px',
      fontFamily: CFK.MONO,
      fontSize: 10.5,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      whiteSpace: 'nowrap'
    }
  }, /*#__PURE__*/React.createElement(CGl, {
    name: "upload",
    size: 13
  }), " Upload asset")), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${ck.line}`,
      borderRadius: 6,
      background: ck.bg1,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: COLS,
      gap: 12,
      padding: '13px 18px',
      borderBottom: `1px solid ${ck.line}`
    }
  }, ['Type', 'Asset', 'Origin', 'Ver', 'State', 'By'].map((h, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: ccKickF(ck)
  }, h))), rows.map((a, i) => /*#__PURE__*/React.createElement("div", {
    key: a.id,
    style: {
      display: 'grid',
      gridTemplateColumns: COLS,
      gap: 12,
      padding: '13px 18px',
      alignItems: 'center',
      borderTop: i ? `1px solid ${ck.line}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.08em',
      color: ck.amber
    }
  }, a.type), /*#__PURE__*/React.createElement("span", {
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      fontFamily: CFK.SANS,
      fontSize: 13.5,
      color: ck.txt,
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, a.label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      color: ck.txt3
    }
  }, a.scene)), /*#__PURE__*/React.createElement(CCOrigin, {
    ck: ck,
    origin: a.origin
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 11,
      color: ck.txt2
    }
  }, "v", a.v, a.parent ? '' : ''), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.08em',
      textTransform: 'uppercase',
      color: ccVerbState(ck, a.state)
    }
  }, a.state), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 12.5,
      color: ck.txt2
    }
  }, a.by)))));
}

/* ══════════════════════════════════════════════════════════
   COLLABORATION — approvals + comments. The AI never holds a verb.
   ══════════════════════════════════════════════════════════ */
function CCollab({
  ctx,
  ck
}) {
  const pending = CC.approvals.filter(a => a.state === 'PENDING');
  const acted = CC.approvals.filter(a => a.state !== 'PENDING');
  const apRow = (a, i, arr) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'grid',
      gridTemplateColumns: '120px 90px 1fr 90px',
      gap: 12,
      padding: '13px 18px',
      alignItems: 'center',
      borderTop: i ? `1px solid ${ck.line}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 13,
      color: ck.txt
    }
  }, CC.ROLE_LABEL[a.role] || a.role), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, a.verb), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 13,
      color: ck.txt2,
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, a.decision), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 7,
      justifyContent: 'flex-end'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ccVerbState(ck, a.state)
    }
  }, a.state), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 5,
      height: 5,
      borderRadius: 999,
      background: ccVerbState(ck, a.state)
    }
  })));
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(CCHead, {
    ck: ck,
    kicker: "Collaboration",
    title: "Who holds the verb",
    lede: "Every decision routes between the people who own it. Propose, decide, approve, veto \u2014 held by roles that are human. The AI is never in this table."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 380px',
      gap: 24,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(CCPanel, {
    ck: ck,
    label: "Awaiting a verb",
    title: null,
    pad: 0
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '120px 90px 1fr 90px',
      gap: 12,
      padding: '12px 18px',
      borderBottom: `1px solid ${ck.line}`
    }
  }, ['Role', 'Verb', 'Decision', 'State'].map((h, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: ccKickF(ck)
  }, h))), pending.map(apRow)), /*#__PURE__*/React.createElement(CCPanel, {
    ck: ck,
    label: "Acted",
    title: null,
    pad: 0
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '120px 90px 1fr 90px',
      gap: 12,
      padding: '12px 18px',
      borderBottom: `1px solid ${ck.line}`
    }
  }, ['Role', 'Verb', 'Decision', 'State'].map((h, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: ccKickF(ck)
  }, h))), acted.map(apRow))), /*#__PURE__*/React.createElement(CCPanel, {
    ck: ck,
    label: "Cross-department notes",
    title: null
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, CC.comments.map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      borderLeft: `2px solid ${ck.line2}`,
      paddingLeft: 13
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      gap: 8,
      marginBottom: 5
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 8,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 13,
      color: ck.txt
    }
  }, c.who), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 8.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, CC.ROLE_LABEL[c.role] || c.role)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9.5,
      color: ck.txt3
    }
  }, c.when)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.MONO,
      fontSize: 9,
      letterSpacing: '0.06em',
      color: ck.amber,
      marginBottom: 5
    }
  }, c.decision), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: CFK.SANS,
      fontSize: 13,
      color: ck.txt2,
      lineHeight: 1.5
    }
  }, c.text)))))));
}
Object.assign(window, {
  CProject,
  CScript,
  CBoards,
  CEdit,
  CAssets,
  CCollab
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/creative-views.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/creative-writers.jsx
try { (() => {
/* global React */
// TENET Creative — Writers Room. A working surface (like the edit bay):
// read one script version full-frame or compare two side by side. The script
// sits on white PAGES with borders (clear separation in compare); the desk,
// controls and cut timeline stay dark. Selecting a scene highlights its clip.
const {
  FK: WFK,
  CREATIVE: WC
} = window;

// Fixed paper palette for the script pages (independent of lane theme).
const WPAGE = {
  bg: '#FFFFFF',
  ink: '#1A1A1A',
  ink2: '#565656',
  ink3: '#9A9A9A',
  amber: '#BA7517',
  amber800: '#854F0B',
  line: 'rgba(0,0,0,0.14)',
  line2: 'rgba(0,0,0,0.08)',
  sel: '#FBF3E4',
  read: '#1F8A5B',
  cut: '#B23A2E'
};
function wText(lines) {
  return lines ? lines.map(function (l) {
    return l.text;
  }).join(' · ') : '';
}
function WLine(props) {
  const l = props.l,
    changed = props.changed;
  if (l.k === 'vo') {
    return /*#__PURE__*/React.createElement("div", {
      style: {
        borderLeft: '2px solid ' + (changed ? WPAGE.amber : 'rgba(0,0,0,0.12)'),
        paddingLeft: 12,
        margin: '7px 0',
        background: changed ? WPAGE.sel : 'transparent',
        borderRadius: changed ? '0 3px 3px 0' : 0
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: WFK.MONO,
        fontSize: 8.5,
        letterSpacing: '0.14em',
        textTransform: 'uppercase',
        color: WPAGE.amber,
        display: 'block',
        marginBottom: 3
      }
    }, "VO"), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: WFK.SCRIPT || WFK.SERIF,
        fontSize: 14.5,
        lineHeight: 1.5,
        color: changed ? WPAGE.amber800 : WPAGE.ink
      }
    }, l.text));
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: WFK.SCRIPT || WFK.SERIF,
      fontSize: 14,
      lineHeight: 1.55,
      color: changed ? WPAGE.amber800 : WPAGE.ink2,
      margin: '6px 0',
      background: changed ? WPAGE.sel : 'transparent',
      borderRadius: changed ? 3 : 0,
      padding: changed ? '2px 6px' : 0
    }
  }, l.text);
}
function WBody(props) {
  const lines = props.lines,
    other = props.other;
  return /*#__PURE__*/React.createElement("div", null, lines.map(function (l, i) {
    const o = other && other[i];
    const changed = !!other && (!o || o.text !== l.text);
    return /*#__PURE__*/React.createElement(WLine, {
      key: i,
      l: l,
      changed: changed
    });
  }));
}

// One white script page for a version. otherId (optional) drives diff marking.
function WPage(props) {
  const ck = props.ck,
    versionId = props.versionId,
    otherId = props.otherId;
  const sections = props.sections,
    sel = props.sel,
    onToggle = props.onToggle,
    isRight = props.isRight;
  const v = WC.writers.versions.find(function (x) {
    return x.id === versionId;
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      background: WPAGE.bg,
      border: '1px solid ' + WPAGE.line,
      borderRadius: 3,
      boxShadow: '0 2px 10px rgba(0,0,0,0.18)',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      padding: '14px 26px',
      borderBottom: '1px solid ' + WPAGE.line2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: WFK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: WPAGE.amber
    }
  }, v.tag), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: WFK.MONO,
      fontSize: 10,
      color: WPAGE.ink3
    }
  }, v.file)), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '8px 26px 26px'
    }
  }, sections.map(function (s) {
    const lines = WC.writers.resolve(versionId, s.sceneId);
    const other = otherId ? WC.writers.resolve(otherId, s.sceneId) : null;
    if (!lines) {
      return /*#__PURE__*/React.createElement("div", {
        key: s.sceneId,
        style: {
          padding: '16px 0',
          borderTop: '1px solid ' + WPAGE.line2,
          opacity: 0.5
        }
      }, /*#__PURE__*/React.createElement("div", {
        style: {
          fontFamily: WFK.MONO,
          fontSize: 10.5,
          letterSpacing: '0.1em',
          color: WPAGE.ink3
        }
      }, String(s.n).padStart(2, '0'), " \xB7 ", s.slug), /*#__PURE__*/React.createElement("div", {
        style: {
          fontFamily: WFK.SERIF,
          fontStyle: 'italic',
          fontSize: 12.5,
          color: WPAGE.ink3,
          marginTop: 5
        }
      }, "Not in this draft"));
    }
    const on = sel.indexOf(s.sceneId) >= 0;
    let tag = null;
    if (otherId) {
      if (!other) tag = isRight ? {
        t: 'Added',
        c: WPAGE.read
      } : {
        t: 'Cut',
        c: WPAGE.cut
      };else if (wText(lines) !== wText(other)) tag = {
        t: 'Revised',
        c: WPAGE.amber
      };
    }
    return /*#__PURE__*/React.createElement("button", {
      key: s.sceneId,
      onClick: function () {
        onToggle(s.sceneId);
      },
      style: {
        display: 'block',
        width: '100%',
        textAlign: 'left',
        cursor: 'pointer',
        border: 0,
        borderTop: '1px solid ' + WPAGE.line2,
        background: on ? WPAGE.sel : 'transparent',
        boxShadow: on ? 'inset 3px 0 0 ' + WPAGE.amber : 'none',
        padding: '14px 14px 14px',
        borderRadius: 0
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'baseline',
        justifyContent: 'space-between',
        gap: 10,
        marginBottom: 6
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: WFK.MONO,
        fontSize: 10.5,
        letterSpacing: '0.1em',
        color: on ? WPAGE.amber800 : WPAGE.ink
      }
    }, String(s.n).padStart(2, '0'), " \xB7 ", s.slug), tag && /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: WFK.MONO,
        fontSize: 8.5,
        letterSpacing: '0.1em',
        textTransform: 'uppercase',
        color: tag.c,
        flexShrink: 0
      }
    }, tag.t)), /*#__PURE__*/React.createElement(WBody, {
      lines: lines,
      other: other
    }));
  })));
}
function WritersRoom(props) {
  const ck = props.ck;
  const versions = WC.writers.versions;
  const sections = WC.writers.sections;
  const b = WC.breakdown;
  const [mode, setMode] = React.useState('single');
  const [ver, setVer] = React.useState('v4');
  const [left, setLeft] = React.useState('v2');
  const [right, setRight] = React.useState('v4');
  const [sel, setSel] = React.useState([]);
  function toggle(sceneId) {
    setSel(function (p) {
      return p.indexOf(sceneId) >= 0 ? p.filter(function (x) {
        return x !== sceneId;
      }) : p.concat([sceneId]);
    });
  }
  const activeScenes = mode === 'single' ? (versions.find(function (v) {
    return v.id === ver;
  }) || {}).scenes || [] : sections.map(function (s) {
    return s.sceneId;
  }).filter(function (id) {
    return WC.writers.resolve(left, id) || WC.writers.resolve(right, id);
  });
  const chip = function (id, active, onClick) {
    const v = versions.find(function (x) {
      return x.id === id;
    });
    return /*#__PURE__*/React.createElement("button", {
      key: id,
      onClick: onClick,
      style: {
        display: 'inline-flex',
        alignItems: 'center',
        gap: 6,
        cursor: 'pointer',
        padding: '5px 10px',
        borderRadius: 3,
        whiteSpace: 'nowrap',
        border: '1px solid ' + (active ? ck.amber : ck.line2),
        background: active ? ck.amberDim : 'transparent',
        fontFamily: WFK.MONO,
        fontSize: 10,
        letterSpacing: '0.06em',
        color: active ? ck.amber : ck.txt2
      }
    }, active && /*#__PURE__*/React.createElement("span", {
      style: {
        width: 5,
        height: 5,
        borderRadius: 999,
        background: ck.amber
      }
    }), v.tag, /*#__PURE__*/React.createElement("span", {
      style: {
        color: ck.txt3
      }
    }, v.date));
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex',
      flexDirection: 'column',
      background: ck.bg0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      minHeight: 52,
      display: 'flex',
      alignItems: 'center',
      gap: 18,
      padding: '0 22px',
      borderBottom: '1px solid ' + ck.line,
      background: ck.bg1,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: WFK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: ck.amber
    }
  }, "Writers Room"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: '1px solid ' + ck.line2,
      borderRadius: 3,
      overflow: 'hidden'
    }
  }, [['single', 'Full frame'], ['compare', 'Compare']].map(function (pair, i) {
    const id = pair[0],
      label = pair[1];
    return /*#__PURE__*/React.createElement("div", {
      key: id,
      onClick: function () {
        setMode(id);
      },
      style: {
        padding: '6px 13px',
        cursor: 'pointer',
        whiteSpace: 'nowrap',
        fontFamily: WFK.MONO,
        fontSize: 10,
        letterSpacing: '0.1em',
        textTransform: 'uppercase',
        borderLeft: i ? '1px solid ' + ck.line2 : 'none',
        background: mode === id ? ck.amber : ck.bg2,
        color: mode === id ? ck.bg0 : ck.txt2
      }
    }, label);
  })), mode === 'single' ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 7
    }
  }, versions.map(function (v) {
    return chip(v.id, ver === v.id, function () {
      setVer(v.id);
    });
  })) : /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 9,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: WFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, "A"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 6
    }
  }, versions.map(function (v) {
    return chip(v.id, left === v.id, function () {
      setLeft(v.id);
    });
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      color: ck.txt3,
      fontFamily: WFK.MONO,
      fontSize: 12
    }
  }, '→'), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: WFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, "B"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 6
    }
  }, versions.map(function (v) {
    return chip(v.id, right === v.id, function () {
      setRight(v.id);
    });
  }))), /*#__PURE__*/React.createElement("span", {
    style: {
      marginLeft: 'auto',
      fontFamily: WFK.SERIF,
      fontStyle: 'italic',
      fontSize: 12.5,
      color: ck.txt3
    }
  }, sel.length ? sel.length + ' scene' + (sel.length === 1 ? '' : 's') + ' on the timeline' : 'Select a scene to mark the cut')), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      display: 'flex',
      alignItems: 'center',
      gap: 26,
      padding: '8px 22px',
      borderBottom: '1px solid ' + ck.line,
      background: ck.bg0
    }
  }, [['Scenes', b.scenes], ['Locations', b.locations], ['Cast', b.characters], ['Runtime', b.runtime]].map(function (pair) {
    return /*#__PURE__*/React.createElement("span", {
      key: pair[0],
      style: {
        display: 'inline-flex',
        alignItems: 'baseline',
        gap: 7
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: WFK.MONO,
        fontSize: 8.5,
        letterSpacing: '0.14em',
        textTransform: 'uppercase',
        color: ck.txt3
      }
    }, pair[0]), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: WFK.SANS,
        fontSize: 14,
        color: ck.txt2
      }
    }, pair[1]));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      overflowY: 'auto',
      overflowX: 'hidden'
    }
  }, mode === 'single' ? /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 720,
      margin: '0 auto',
      padding: '30px 24px 44px'
    }
  }, /*#__PURE__*/React.createElement(WPage, {
    ck: ck,
    versionId: ver,
    sections: sections,
    sel: sel,
    onToggle: toggle
  })) : /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 1180,
      margin: '0 auto',
      padding: '26px 24px 44px',
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 18,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement(WPage, {
    ck: ck,
    versionId: left,
    otherId: right,
    isRight: false,
    sections: sections,
    sel: sel,
    onToggle: toggle
  }), /*#__PURE__*/React.createElement(WPage, {
    ck: ck,
    versionId: right,
    otherId: left,
    isRight: true,
    sections: sections,
    sel: sel,
    onToggle: toggle
  }))), /*#__PURE__*/React.createElement(WTimeline, {
    ck: ck,
    sections: sections,
    activeScenes: activeScenes,
    sel: sel,
    onToggle: toggle
  }));
}

// Cut timeline: scene clips proportional to duration. Selected → amber.
// Scenes not in the active draft are faded ("not written yet").
function WTimeline(props) {
  const ck = props.ck,
    sections = props.sections,
    activeScenes = props.activeScenes,
    sel = props.sel,
    onToggle = props.onToggle;
  const total = sections.reduce(function (a, s) {
    return a + s.dur;
  }, 0);
  const ticks = [];
  for (var t = 0; t <= total; t += 5) ticks.push(t);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      background: ck.bg1,
      borderTop: '1px solid ' + ck.line2
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '8px 18px',
      borderBottom: '1px solid ' + ck.line
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: WFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: ck.txt3
    }
  }, "The cut \xB7 V1.2"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: WFK.SANS,
      fontWeight: 300,
      fontSize: 18,
      color: ck.txt
    }
  }, window.FLOW.fmtClock(total))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '12px 18px 6px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 4,
      height: 46
    }
  }, sections.map(function (s) {
    const present = activeScenes.indexOf(s.sceneId) >= 0;
    const on = sel.indexOf(s.sceneId) >= 0;
    const stripe = ck === window.DK_LIGHT ? 'repeating-linear-gradient(135deg, #E7E2D8 0 7px, #ded8cc 7px 14px)' : 'repeating-linear-gradient(135deg, #20262d 0 7px, #1a1f25 7px 14px)';
    return /*#__PURE__*/React.createElement("button", {
      key: s.sceneId,
      onClick: function () {
        if (present) onToggle(s.sceneId);
      },
      title: s.loc,
      style: {
        flex: s.dur,
        minWidth: 0,
        cursor: present ? 'pointer' : 'default',
        textAlign: 'left',
        background: on ? ck.amberDim : present ? ck.clipBlue : stripe,
        border: '1px solid ' + (on ? ck.amber : present ? ck.clipEdge : ck.line2),
        borderRadius: 2,
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'space-between',
        padding: '5px 7px',
        overflow: 'hidden',
        opacity: present ? 1 : 0.55
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: WFK.MONO,
        fontSize: 8.5,
        color: on ? ck.amber : present ? ck.clipText : ck.txt3,
        whiteSpace: 'nowrap',
        overflow: 'hidden',
        textOverflow: 'ellipsis'
      }
    }, s.loc), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: WFK.MONO,
        fontSize: 8,
        color: on ? ck.amber : present ? ck.clipSub : ck.txt3
      }
    }, String(s.n).padStart(2, '0'), present ? '' : ' · —'));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      height: 18,
      marginTop: 4
    }
  }, ticks.map(function (tk) {
    return /*#__PURE__*/React.createElement("span", {
      key: tk,
      style: {
        position: 'absolute',
        left: tk / total * 100 + '%',
        top: 3,
        fontFamily: WFK.MONO,
        fontSize: 9,
        color: ck.txt3,
        transform: 'translateX(1px)'
      }
    }, '01:00:' + String(tk).padStart(2, '0'));
  }))));
}
window.WritersRoom = WritersRoom;
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/creative-writers.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/data.js
try { (() => {
/* ============================================================
   TENET — Script-to-Scaffold Flow · the one project, modeled.
   ------------------------------------------------------------
   A :30 brand film. The script is the input. Everything else —
   the scaffold outline, the cost sheet, the shot timeline — is
   derived from it. Two seats read the same project:
     • Director — locations, characters, craft
     • Producer — cost, schedule, the line
   The producer→director chat is the bidirectional ripple:
   a constraint travels up, the script changes, money moves.
   ============================================================ */
window.FLOW = function () {
  const CHARS = [{
    id: 'mara',
    name: 'MARA',
    note: 'Lead'
  }, {
    id: 'theo',
    name: 'THEO',
    note: 'Partner'
  }, {
    id: 'maker',
    name: 'THE MAKER',
    note: 'Hands'
  }];
  const LOCS = [{
    id: 'rooftop',
    name: 'Rooftop',
    kind: 'EXT'
  }, {
    id: 'workshop',
    name: 'Workshop',
    kind: 'INT'
  }, {
    id: 'downtown',
    name: 'Downtown street',
    kind: 'EXT'
  }, {
    id: 'apartment',
    name: 'Apartment',
    kind: 'INT'
  }, {
    id: 'studio',
    name: 'Studio',
    kind: 'INT'
  }, {
    id: 'beach',
    name: 'Beach',
    kind: 'EXT'
  }];

  // Finance categories (order matters — drives the cost-profile popup).
  // Each scene carries `fin.lv` = a 1\u20137 spend level per category, plus the
  // primary cost drivers behind it. Shots inherit their scene's profile.
  const FIN_CATS = ['Location', 'Talent', 'Crowd', 'Camera', 'Environment', 'Art Dept', 'Vehicles', 'Practical FX', 'VFX', 'Schedule Risk'];

  // Each scene: slug parts, time-of-day (s4 is dynamic via state),
  // cost (day base + night premium), :15-cutdown membership, characters,
  // action paragraphs (with character tags), and shots (the timeline boxes).
  const SCENES = [{
    id: 's1',
    n: 1,
    locId: 'rooftop',
    tod: 'DAWN',
    todDynamic: false,
    costDay: 85000,
    nightPremium: 0,
    in15: true,
    chars: ['mara'],
    fin: {
      lv: [5, 3, 1, 5, 4, 2, 1, 2, 4, 6],
      drivers: ['Rooftop permit + safety', 'Crane / jib package', 'Dawn light window', 'Skyline VFX cleanup']
    },
    action: [{
      text: 'A city still blue with sleep. MARA stands at the rail.',
      chars: ['mara']
    }, {
      text: 'A watch catches the first light.',
      chars: []
    }],
    shots: [{
      id: 's1a',
      dur: 2,
      chars: [],
      desc: 'City, blue hour'
    }, {
      id: 's1b',
      dur: 2,
      chars: ['mara'],
      desc: 'Mara at the rail'
    }, {
      id: 's1c',
      dur: 2,
      chars: ['mara'],
      desc: 'Watch, first light'
    }]
  }, {
    id: 's2',
    n: 2,
    locId: 'workshop',
    tod: 'DAY',
    todDynamic: false,
    costDay: 70000,
    nightPremium: 0,
    in15: false,
    chars: ['maker'],
    fin: {
      lv: [2, 3, 1, 5, 1, 5, 1, 3, 2, 2],
      drivers: ['Macro / probe lens package', 'Specialty watchmaker talent', 'Detailed set dressing']
    },
    action: [{
      text: 'Hands at a bench. THE MAKER sets a movement turning.',
      chars: ['maker']
    }, {
      text: 'Tiny gears, deliberate.',
      chars: []
    }],
    shots: [{
      id: 's2a',
      dur: 2,
      chars: [],
      desc: 'Bench, tools'
    }, {
      id: 's2b',
      dur: 2,
      chars: ['maker'],
      desc: 'The movement'
    }, {
      id: 's2c',
      dur: 1,
      chars: ['maker'],
      desc: 'Maker\u2019s hands'
    }]
  }, {
    id: 's3',
    n: 3,
    locId: 'downtown',
    tod: 'DAY',
    todDynamic: false,
    costDay: 80000,
    nightPremium: 0,
    in15: false,
    chars: ['mara'],
    fin: {
      lv: [6, 3, 6, 4, 3, 3, 4, 2, 3, 4],
      drivers: ['Street closure permit', 'Background extras', 'Steadicam operator', 'Picture vehicles']
    },
    action: [{
      text: 'MARA moves through the city, unhurried.',
      chars: ['mara']
    }, {
      text: 'The crowd blurs; she does not.',
      chars: []
    }],
    shots: [{
      id: 's3a',
      dur: 2,
      chars: ['mara'],
      desc: 'Mara, walking'
    }, {
      id: 's3b',
      dur: 1.5,
      chars: [],
      desc: 'Crowd blur'
    }, {
      id: 's3c',
      dur: 1.5,
      chars: ['mara'],
      desc: 'Reflection in glass'
    }]
  }, {
    // The night scene — the producer's constraint target.
    id: 's4',
    n: 4,
    locId: 'apartment',
    tod: 'NIGHT',
    todDynamic: true,
    costDay: 60000,
    nightPremium: 50000,
    in15: false,
    chars: ['mara', 'theo'],
    fin: {
      lv: [3, 5, 1, 3, 2, 4, 1, 2, 1, 5],
      drivers: ['Night premium + OT', 'Two principal cast', 'Practical interior lighting']
    },
    action: [{
      text: 'THEO fastens the watch on MARA\u2019s wrist.',
      chars: ['mara', 'theo']
    }, {
      text: 'A held look. The day, given.',
      chars: []
    }],
    shots: [{
      id: 's4a',
      dur: 2,
      chars: ['theo', 'mara'],
      desc: 'The gift'
    }, {
      id: 's4b',
      dur: 2,
      chars: ['mara', 'theo'],
      desc: 'Faces'
    }, {
      id: 's4c',
      dur: 1,
      chars: ['mara'],
      desc: 'Wrist clasp'
    }]
  }, {
    id: 's5',
    n: 5,
    locId: 'studio',
    tod: '\u2014',
    todDynamic: false,
    costDay: 110000,
    nightPremium: 0,
    in15: true,
    chars: [],
    fin: {
      lv: [4, 1, 1, 6, 1, 3, 1, 2, 5, 3],
      drivers: ['Motion-control rig', 'Macro / probe lens', 'Product handling + comp']
    },
    action: [{
      text: 'The product, alone. Light moves across the dial like weather.',
      chars: []
    }],
    shots: [{
      id: 's5a',
      dur: 2.5,
      chars: [],
      desc: 'Macro, dial'
    }, {
      id: 's5b',
      dur: 2,
      chars: [],
      desc: 'Macro, strap'
    }, {
      id: 's5c',
      dur: 1.5,
      chars: [],
      desc: 'Logo resolve'
    }]
  }, {
    id: 's6',
    n: 6,
    locId: 'beach',
    tod: 'SUNSET',
    todDynamic: false,
    costDay: 45000,
    nightPremium: 0,
    in15: true,
    chars: ['mara'],
    fin: {
      lv: [5, 3, 1, 4, 5, 2, 3, 1, 2, 7],
      drivers: ['Golden-hour window', 'Location travel + permit', 'Weather contingency', 'Tide timing']
    },
    action: [{
      text: 'MARA at the shoreline. The watch, the light, the long now.',
      chars: ['mara']
    }],
    shots: [{
      id: 's6a',
      dur: 1.5,
      chars: ['mara'],
      desc: 'Mara at shore'
    }, {
      id: 's6b',
      dur: 1.5,
      chars: ['mara'],
      desc: 'Wide beach'
    }]
  }];
  const TITLE = 'A Brief History of Selling Ideas';
  const LOGLINE = 'One watch. One day. A life held in the light.';

  // ── The script, AV format (VISUAL / VO) — matches the writer's doc ──
  // k: 'v' = VISUAL (stage direction) · 'vo' = voiceover line ·
  //    'card' = title/cut card · 'q' = transition question
  const FILM = {
    title: TITLE,
    file: 'A_Brief_History_v3.fdx',
    parts: [{
      id: 'p1',
      n: 1,
      label: 'The Old Way',
      blocks: [{
        k: 'card',
        text: 'Title over black',
        note: 'A Brief History of Selling Ideas'
      }, {
        k: 'v',
        text: 'Open on Times Square. Camera BOOMS UP the face of an 80th-floor office building. Inside, rows of frantic executives type furiously. Cigarette smoke hangs in the air. A man leaps to his feet holding a script above his head — equal parts fear and triumph.'
      }, {
        k: 'vo',
        text: 'Once upon a time, making something great meant\u2026 guessing.'
      }, {
        k: 'v',
        text: 'Rapid cuts. Script changes hands. Elevator UP button pressed. Dark boardroom. A massive executive slams a stamp \u2014 FAIL. Exterior: a man is thrown out a window.'
      }, {
        k: 'vo',
        text: 'Months of writing. Rewriting. Pitching. Waiting. More waiting.'
      }, {
        k: 'v',
        text: 'Rapid cuts. A new man is dumped into an empty seat. A hand shoots up with a new script. Feet walking down a hall. Giant rubber stamp hits paper \u2014 APPROVED.'
      }, {
        k: 'vo',
        text: 'Then boards. Notes. New boards. Different notes. Same notes.'
      }, {
        k: 'v',
        text: 'Elevator DOWN button. An artist furiously draws storyboards. Elevator UP button. Giant rubber stamp \u2014 APPROVED. A giant check is signed. Millions are committed.'
      }, {
        k: 'vo',
        text: 'An idea is approved. Millions committed. Still no one\u2019s seen it.'
      }, {
        k: 'v',
        text: 'Clapperboard snaps. Sets constructed. Crew scrambling. Chaos energy.'
      }, {
        k: 'vo',
        text: 'Production begins.'
      }, {
        k: 'v',
        text: 'Martini glasses clink. Exhausted smiles.'
      }, {
        k: 'vo',
        text: 'Production wraps.'
      }, {
        k: 'v',
        text: 'Boardroom gathered around an old square TV. A dramatic pause as the screen flickers on.'
      }, {
        k: 'vo',
        text: 'And finally\u2026 the first real version.'
      }, {
        k: 'card',
        text: 'Cut to black'
      }, {
        k: 'vo',
        text: 'And sometimes\u2026 it\u2019s not what anyone pictured.'
      }, {
        k: 'v',
        text: 'Silence. Faces freeze. Someone is thrown out the window.'
      }, {
        k: 'vo',
        text: 'Sometimes? Magic.'
      }, {
        k: 'v',
        text: 'Smash cut to a family watching TV. They jump up and run out of the house. Cars pull up to a store parking lot. Cars leave loaded with product.'
      }]
    }, {
      id: 'tr',
      transition: true,
      label: 'Transition',
      blocks: [{
        k: 'q',
        text: 'A lot has changed since the good old days.'
      }, {
        k: 'q',
        text: 'But we still sell ideas the same way.'
      }, {
        k: 'q',
        text: 'What if you could actually see the idea\u2026 before you make it?'
      }, {
        k: 'q',
        text: 'What if changing one thing didn\u2019t mean starting over?'
      }, {
        k: 'q',
        text: 'What if the best idea won \u2014 not just the safest one?'
      }]
    }, {
      id: 'p2',
      n: 2,
      label: 'The New Way',
      blocks: [{
        k: 'v',
        text: 'Camera BOOMS UP Times Square again \u2014 this time into a warm apartment window. A young creative sits cross-legged with a laptop. Calm. Cat in lap.'
      }, {
        k: 'vo',
        text: 'What if every revision came to life instantly?'
      }, {
        k: 'v',
        text: 'On the laptop screen, script text auto-transforms into rough storyboard frames on a timeline.'
      }, {
        k: 'vo',
        text: 'Try this.'
      }, {
        k: 'v',
        text: 'The character on screen transforms into a squirrel. The creative bursts out laughing.'
      }, {
        k: 'vo',
        text: 'No more imagining. Just reacting.'
      }, {
        k: 'v',
        text: 'Split screen. A remote collaborator types notes. The change happens in real time.'
      }, {
        k: 'vo',
        text: 'If it doesn\u2019t work? You\u2019ll know fast.'
      }, {
        k: 'v',
        text: 'Rapid iterations. The character cycles through different looks, styles, tones.'
      }, {
        k: 'vo',
        text: 'So you can try again. And again. And again.'
      }, {
        k: 'v',
        text: 'Cut to a relaxed boardroom. Pizza boxes. The screen plays a polished animatic version. Confident nods.'
      }, {
        k: 'vo',
        text: 'When you finally hit \u2018approve\u2019\u2026 you mean it.'
      }, {
        k: 'v',
        text: 'Transition seamlessly from animatic to a real production set that matches the storyboard exactly.'
      }, {
        k: 'vo',
        text: 'Better ideas. Fewer heart attacks.'
      }, {
        k: 'v',
        text: 'Return to the exterior of the office building. A window cleaner drops dramatically two inches. Calmly continues cleaning.'
      }, {
        k: 'vo',
        text: 'Stop guessing. Start seeing.'
      }]
    }]
  };

  // ── Derivations (pure; depend on state) ────────────────────────────
  // state: { mode: 30|15, tod: { s4:'NIGHT'|'DAY' } }
  function todOf(scene, state) {
    if (scene.todDynamic) return state.tod && state.tod[scene.id] || scene.tod;
    return scene.tod;
  }
  function isNight(scene, state) {
    return todOf(scene, state) === 'NIGHT';
  }
  function costOf(scene, state) {
    return scene.costDay + (isNight(scene, state) ? scene.nightPremium : 0);
  }
  function activeScenes(state) {
    return SCENES.filter(s => state.mode === 15 ? s.in15 : true);
  }
  function totalCost(state) {
    return activeScenes(state).reduce((a, s) => a + costOf(s, state), 0);
  }
  function totalLength(state) {
    return activeScenes(state).reduce((a, s) => a + s.shots.reduce((b, sh) => b + sh.dur, 0), 0);
  }
  function allShots(state) {
    const out = [];
    activeScenes(state).forEach(s => s.shots.forEach(sh => out.push(Object.assign({
      sceneId: s.id,
      locId: s.locId,
      n: s.n
    }, sh))));
    return out;
  }
  function locUsed(locId, state) {
    return activeScenes(state).some(s => s.locId === locId);
  }
  function charUsed(charId, state) {
    return activeScenes(state).some(s => s.chars.includes(charId));
  }
  function locName(id) {
    const l = LOCS.find(x => x.id === id);
    return l ? l.name : id;
  }
  function charName(id) {
    const c = CHARS.find(x => x.id === id);
    return c ? c.name : id;
  }
  function fmtMoney(n) {
    return '$' + Math.round(n).toLocaleString('en-US');
  }
  function fmtClock(sec) {
    const s = Math.round(sec);
    return '0:' + String(s).padStart(2, '0');
  }
  return {
    CHARS,
    LOCS,
    SCENES,
    TITLE,
    LOGLINE,
    FILM,
    FIN_CATS,
    todOf,
    isNight,
    costOf,
    activeScenes,
    totalCost,
    totalLength,
    allShots,
    locUsed,
    charUsed,
    locName,
    charName,
    fmtMoney,
    fmtClock,
    DEFAULT_STATE: {
      mode: 30,
      tod: {
        s4: 'NIGHT'
      }
    }
  };
}();
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/data.js", error: String((e && e.message) || e) }); }

// flow_ctx/director-edit.jsx
try { (() => {
/* global React, FK, FLOW, FRAMEWORK, FGlyph */
// Director page — a dark edit-bay layout (Flame-style):
//   left  = scaffold + variables   (the "pink" zone)
//   center= program monitor
//   right = live script            (the "blue" zone)
//   below = transport + multi-track timeline.

const DK_DARK = {
  bg0: '#0E0F12',
  bg1: '#16181d',
  bg2: '#1f2228',
  bg3: '#2a2e36',
  line: 'rgba(255,255,255,0.06)',
  line2: 'rgba(255,255,255,0.13)',
  txt: '#dfe2e6',
  txt2: '#8c929b',
  txt3: '#5b616a',
  amber: '#E3A155',
  amberDim: 'rgba(227,161,85,0.16)',
  clip: '#33414f',
  clipBlue: '#34465c',
  clipGray: '#3a4048',
  wave: '#69727e',
  read: '#7fd6a6',
  clipEdge: '#4a5a6c',
  clipText: 'rgba(255,255,255,0.85)',
  clipSub: 'rgba(255,255,255,0.4)',
  pink: '#f0909f',
  lift: '0 1px 0 rgba(255,255,255,0.04), 0 18px 44px -20px rgba(0,0,0,0.8)'
};
const DK_LIGHT = {
  bg0: '#ECECEA',
  bg1: '#FFFFFF',
  bg2: '#FBFBFA',
  bg3: '#F4F4F2',
  line: 'rgba(0,0,0,0.10)',
  line2: 'rgba(0,0,0,0.16)',
  txt: '#1A1A1A',
  txt2: '#6B6B6B',
  txt3: '#9A9A9A',
  amber: '#BA7517',
  amberDim: 'rgba(186,117,23,0.10)',
  clip: '#c9d3dd',
  clipBlue: '#c0d0e0',
  clipGray: '#dad6cd',
  wave: '#a6aeb8',
  read: '#1F7A52',
  clipEdge: '#a6b4c2',
  clipText: 'rgba(33,28,21,0.82)',
  clipSub: 'rgba(33,28,21,0.42)',
  pink: '#C75B74',
  lift: '0 1px 0 rgba(33,28,22,0.015), 0 12px 34px -16px rgba(64,44,16,0.24)'
};
let DK = DK_DARK;

// ── Left: scaffold + variables (pink zone) ──────────────────────────
const DIR_DRAFT = 'Can we hold the beach for golden hour? It sells the ending.';
function ChatProducer({
  chat,
  onSend
}) {
  const bub = mine => ({
    alignSelf: mine ? 'flex-end' : 'flex-start',
    maxWidth: '88%',
    background: mine ? DK.amber : DK.bg3,
    color: mine ? DK.bg0 : DK.txt,
    border: mine ? 'none' : `1px solid ${DK.line2}`,
    borderRadius: 8,
    padding: '8px 11px',
    fontFamily: FK.SANS,
    fontSize: 12.5,
    lineHeight: 1.4,
    marginBottom: 7
  });
  const who = mine => ({
    fontFamily: FK.MONO,
    fontSize: 9,
    letterSpacing: '0.12em',
    textTransform: 'uppercase',
    color: mine ? 'rgba(0,0,0,0.5)' : DK.txt3,
    marginBottom: 3
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: `1px solid ${DK.line}`,
      background: DK.bg2,
      padding: '12px 18px 14px',
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: DK.amber,
      marginBottom: 10,
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      whiteSpace: 'nowrap'
    }
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "chat",
    size: 13
  }), " Message \xB7 Producer"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, chat.messages.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: bub(m.from === 'director')
  }, /*#__PURE__*/React.createElement("div", {
    style: who(m.from === 'director')
  }, m.from === 'director' ? 'You · Director' : 'Producer'), m.text)), chat.typing && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 12.5,
      color: DK.txt3,
      marginBottom: 7
    }
  }, "Producer is checking the numbers\u2026")), !chat.sent && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 9
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 12.5,
      color: DK.txt,
      lineHeight: 1.4,
      padding: '9px 11px',
      background: DK.bg1,
      border: `1px solid ${DK.line2}`,
      borderRadius: 8
    }
  }, DIR_DRAFT), /*#__PURE__*/React.createElement("button", {
    onClick: onSend,
    style: {
      alignSelf: 'flex-end',
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      cursor: 'pointer',
      background: DK.amber,
      color: DK.bg0,
      border: 'none',
      borderRadius: 2,
      padding: '8px 15px',
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.12em',
      textTransform: 'uppercase'
    }
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "send",
    size: 13
  }), " Send")));
}
function EditScaffold({
  cat,
  subIdx,
  onCat,
  onSub,
  open,
  onToggle
}) {
  const catObj = FRAMEWORK.DIRECTOR.find(c => c.id === cat) || FRAMEWORK.DIRECTOR[0];
  const subs = catObj.subs;
  const go = d => onSub((subIdx + d + subs.length) % subs.length);
  if (!open) {
    return /*#__PURE__*/React.createElement("div", {
      style: {
        height: '100%',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        background: DK.bg1,
        borderRight: `1px solid ${DK.line}`,
        padding: '12px 0'
      }
    }, /*#__PURE__*/React.createElement("span", {
      onClick: onToggle,
      title: "Open reference library",
      style: {
        width: 30,
        height: 30,
        borderRadius: 6,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        cursor: 'pointer',
        color: DK.txt2,
        border: `1px solid ${DK.line2}`
      }
    }, /*#__PURE__*/React.createElement(FGlyph, {
      name: "arrow",
      size: 13
    })), /*#__PURE__*/React.createElement("div", {
      style: {
        writingMode: 'vertical-rl',
        transform: 'rotate(180deg)',
        marginTop: 18,
        fontFamily: FK.MONO,
        fontSize: 10,
        letterSpacing: '0.22em',
        textTransform: 'uppercase',
        color: DK.txt3
      }
    }, "Reference library"));
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      display: 'flex',
      flexDirection: 'column',
      background: DK.bg1,
      borderRight: `1px solid ${DK.line}`,
      minHeight: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '14px 18px 12px',
      borderBottom: `1px solid ${DK.line}`,
      display: 'flex',
      alignItems: 'flex-start',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: DK.amber
    }
  }, "Director \xB7 scaffold"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 19,
      color: DK.txt,
      marginTop: 5
    }
  }, "Reference library")), /*#__PURE__*/React.createElement("span", {
    onClick: onToggle,
    title: "Collapse",
    style: {
      width: 26,
      height: 26,
      borderRadius: 6,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      cursor: 'pointer',
      color: DK.txt2,
      border: `1px solid ${DK.line2}`,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 12,
    style: {
      transform: 'scaleX(-1)'
    }
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      overflow: 'auto',
      minHeight: 0
    }
  }, FRAMEWORK.DIRECTOR.map(c => {
    const on = c.id === cat;
    return /*#__PURE__*/React.createElement("div", {
      key: c.id,
      onClick: () => onCat(c.id),
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 12,
        padding: '8px 18px',
        cursor: 'pointer',
        background: on ? DK.amberDim : 'transparent',
        boxShadow: on ? `inset 3px 0 0 ${DK.amber}` : 'none'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 10,
        color: on ? DK.amber : DK.txt3,
        width: 18
      }
    }, String(c.n).padStart(2, '0')), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.SERIF,
        fontStyle: 'italic',
        fontSize: 16,
        color: on ? DK.amber : DK.txt,
        flex: 1
      }
    }, c.name), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 9,
        color: DK.txt3
      }
    }, "6"));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: `1px solid ${DK.line}`,
      background: DK.bg2,
      padding: '12px 18px 14px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      marginBottom: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: DK.amber
    }
  }, String(catObj.n).padStart(2, '0'), " \xB7 ", catObj.name), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 22,
      height: 22,
      borderRadius: 999,
      border: `1px solid ${DK.line2}`,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      cursor: 'pointer',
      color: DK.txt2
    },
    onClick: () => go(-1)
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 12,
    style: {
      transform: 'scaleX(-1)'
    }
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: DK.txt2,
      width: 30,
      textAlign: 'center'
    }
  }, subIdx + 1, "/", subs.length), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 22,
      height: 22,
      borderRadius: 999,
      border: `1px solid ${DK.line2}`,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      cursor: 'pointer',
      color: DK.txt2
    },
    onClick: () => go(1)
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 12
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 6
    }
  }, subs.map((s, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    onClick: () => onSub(i),
    style: {
      padding: '6px 10px',
      cursor: 'pointer',
      fontFamily: FK.MONO,
      fontSize: 10.5,
      letterSpacing: '0.02em',
      whiteSpace: 'nowrap',
      color: i === subIdx ? DK.bg0 : DK.txt2,
      background: i === subIdx ? DK.amber : DK.bg3,
      borderRadius: 2
    }
  }, s)))));
}

// ── Center: program monitor ─────────────────────────────────────────
function ProgramMonitor({
  state
}) {
  const len = FLOW.totalLength(state);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      display: 'flex',
      flexDirection: 'column',
      background: DK.bg0,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 14,
      padding: '8px 16px',
      color: DK.txt2
    }
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "film",
    size: 14,
    style: {
      color: DK.txt3
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: DK.txt3
    }
  }, "Versions"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 16
    }
  }, ['01_BriefHistory_HD', '01_BriefHistory_HD_vb3', '01_BriefHistory_HD_a2'].map(v => {
    const active = v === '01_BriefHistory_HD';
    return /*#__PURE__*/React.createElement("span", {
      key: v,
      style: {
        fontFamily: FK.MONO,
        fontSize: 11,
        letterSpacing: '0.1em',
        color: active ? DK.amber : DK.txt3,
        cursor: 'pointer',
        display: 'inline-flex',
        alignItems: 'center',
        gap: 6
      }
    }, active && /*#__PURE__*/React.createElement("span", {
      style: {
        width: 5,
        height: 5,
        borderRadius: 999,
        background: DK.amber
      }
    }), v);
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      padding: '0 24px 8px',
      minHeight: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: '100%',
      maxWidth: 912,
      aspectRatio: '16 / 9',
      background: '#000',
      border: `1px solid ${DK.line2}`,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: "assets/creative-thumbnail.png",
    alt: "Program",
    style: {
      position: 'absolute',
      inset: 0,
      width: '100%',
      height: '100%',
      objectFit: 'cover',
      display: 'block'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 0,
      left: 0,
      right: 0,
      height: 44,
      background: 'linear-gradient(rgba(0,0,0,0.5), transparent)',
      pointerEvents: 'none'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      bottom: 0,
      left: 0,
      right: 0,
      height: 40,
      background: 'linear-gradient(transparent, rgba(0,0,0,0.5))',
      pointerEvents: 'none'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 12,
      left: 14,
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.18em',
      color: 'rgba(255,255,255,0.7)'
    }
  }, "PROGRAM \xB7 16\xD79"), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 14,
      bottom: 12,
      fontFamily: FK.MONO,
      fontSize: 10,
      color: 'rgba(255,255,255,0.75)'
    }
  }, "01:00:00:00"), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      right: 14,
      bottom: 12,
      fontFamily: FK.MONO,
      fontSize: 10,
      color: 'rgba(255,255,255,0.75)'
    }
  }, FLOW.fmtClock(len), " \xB7 1920\xD71080"))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '6px 18px',
      borderTop: `1px solid ${DK.line}`,
      color: DK.txt3,
      fontFamily: FK.MONO,
      fontSize: 10
    }
  }, /*#__PURE__*/React.createElement("span", null, "SRC 00:00:02:01"), /*#__PURE__*/React.createElement("span", null, "CONTRAST 1.00 \xB7 GAMMA 1.00"), /*#__PURE__*/React.createElement("span", null, "1920 \xD7 1080 (1.778)")));
}

// ── Right: live script (AV format — VISUAL / VO) ────────────────────
function EditScript({
  state,
  flashId
}) {
  const film = FLOW.FILM;
  const Block = ({
    b
  }) => {
    if (b.k === 'card') {
      return /*#__PURE__*/React.createElement("div", {
        style: {
          margin: '18px 0',
          textAlign: 'center'
        }
      }, /*#__PURE__*/React.createElement("div", {
        style: {
          display: 'flex',
          alignItems: 'center',
          gap: 12
        }
      }, /*#__PURE__*/React.createElement("span", {
        style: {
          flex: 1,
          height: 1,
          background: DK.line2
        }
      }), /*#__PURE__*/React.createElement("span", {
        style: {
          fontFamily: FK.MONO,
          fontSize: 10,
          letterSpacing: '0.18em',
          textTransform: 'uppercase',
          color: DK.txt2,
          whiteSpace: 'nowrap'
        }
      }, b.text), /*#__PURE__*/React.createElement("span", {
        style: {
          flex: 1,
          height: 1,
          background: DK.line2
        }
      })), b.note && /*#__PURE__*/React.createElement("div", {
        style: {
          fontFamily: FK.SERIF,
          fontStyle: 'italic',
          fontSize: 14,
          color: DK.txt3,
          marginTop: 7
        }
      }, b.note));
    }
    if (b.k === 'q') {
      return /*#__PURE__*/React.createElement("div", {
        style: {
          fontFamily: FK.SERIF,
          fontStyle: 'italic',
          fontSize: 15.5,
          lineHeight: 1.4,
          color: DK.txt,
          textAlign: 'center',
          margin: '0 8px 13px'
        }
      }, b.text);
    }
    const vo = b.k === 'vo';
    return /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        gap: 14,
        marginBottom: 13
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        flex: '0 0 46px',
        textAlign: 'right',
        paddingTop: 3
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 9,
        letterSpacing: '0.14em',
        textTransform: 'uppercase',
        color: vo ? DK.amber : DK.txt3
      }
    }, vo ? 'VO' : 'Visual')), /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        minWidth: 0,
        borderLeft: vo ? `2px solid ${DK.amber}` : 'none',
        paddingLeft: vo ? 12 : 0
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.SCRIPT,
        fontSize: 13.5,
        lineHeight: 1.5,
        color: vo ? DK.txt : DK.txt2,
        fontWeight: vo ? 500 : 400
      }
    }, b.text)));
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      display: 'flex',
      flexDirection: 'column',
      background: DK.bg1,
      borderLeft: `1px solid ${DK.line}`,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '12px 20px',
      borderBottom: `1px solid ${DK.line}`,
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: DK.amber
    }
  }, "Live script"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: DK.txt3
    }
  }, film.file)), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      overflow: 'auto',
      padding: '18px 22px 26px',
      minHeight: 0
    }
  }, film.parts.map((part, pi) => /*#__PURE__*/React.createElement("div", {
    key: part.id,
    style: {
      marginTop: pi === 0 ? 0 : 24
    }
  }, part.transition ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      margin: '4px 0 16px'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      height: 1,
      background: DK.line
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.24em',
      textTransform: 'uppercase',
      color: DK.txt3
    }
  }, "Transition"), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      height: 1,
      background: DK.line
    }
  })) : /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 15
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: DK.amber
    }
  }, "Part ", part.n, " \xB7 ", part.label), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 1,
      background: DK.line,
      marginTop: 8
    }
  })), part.blocks.map((b, i) => /*#__PURE__*/React.createElement(Block, {
    key: i,
    b: b
  }))))));
}

// ── Transport bar ───────────────────────────────────────────────────
function TransportBar({
  state,
  onMode,
  view,
  onView
}) {
  const len = FLOW.totalLength(state);
  const btn = {
    width: 30,
    height: 26,
    borderRadius: 3,
    border: `1px solid ${DK.line2}`,
    background: DK.bg2,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    color: DK.txt2,
    cursor: 'pointer'
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      height: 50,
      flexShrink: 0,
      background: DK.bg1,
      borderTop: `1px solid ${DK.line}`,
      borderBottom: `1px solid ${DK.line}`,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '0 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 17,
      color: DK.read,
      letterSpacing: '0.04em'
    }
  }, "01:00:04:12"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: DK.txt3
    }
  }, "SRC 00:00:02:01"), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 1,
      height: 22,
      background: DK.line2,
      margin: '0 4px'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: `1px solid ${DK.line2}`,
      borderRadius: 3,
      overflow: 'hidden'
    }
  }, [['timeline', 'Timeline'], ['beats', 'Beats']].map(([id, label], i) => /*#__PURE__*/React.createElement("div", {
    key: id,
    onClick: () => onView(id),
    style: {
      padding: '5px 13px',
      cursor: 'pointer',
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      borderLeft: i ? `1px solid ${DK.line2}` : 'none',
      background: view === id ? DK.amber : DK.bg2,
      color: view === id ? DK.bg0 : DK.txt2
    }
  }, label)))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: btn
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 13,
    style: {
      transform: 'scaleX(-1)'
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      ...btn,
      width: 38,
      background: DK.amber,
      borderColor: DK.amber,
      color: DK.bg0
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "13",
    height: "13",
    viewBox: "0 0 24 24",
    fill: "currentColor"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M7 4l13 8-13 8z"
  }))), /*#__PURE__*/React.createElement("div", {
    style: btn
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 13
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 1,
      height: 22,
      background: DK.line2,
      margin: '0 8px'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: DK.txt3,
      marginRight: 6
    }
  }, "LENGTH"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: 22,
      color: DK.txt
    }
  }, FLOW.fmtClock(len))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: DK.txt3
    }
  }, "CUT"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: `1px solid ${DK.line2}`,
      borderRadius: 3,
      overflow: 'hidden'
    }
  }, [30, 15].map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: m,
    onClick: () => onMode(m),
    style: {
      padding: '5px 12px',
      cursor: 'pointer',
      fontFamily: FK.MONO,
      fontSize: 11,
      borderLeft: i ? `1px solid ${DK.line2}` : 'none',
      background: state.mode === m ? DK.amber : DK.bg2,
      color: state.mode === m ? DK.bg0 : DK.txt2
    }
  }, ":", m)))));
}

// ── Multi-track timeline ────────────────────────────────────────────
function Waveform() {
  const bars = [];
  for (let i = 0; i < 160; i++) {
    const x = i / 160;
    const a = Math.abs(Math.sin(x * 22) * 0.5 + Math.sin(x * 7 + 1) * 0.35 + Math.sin(x * 51) * 0.18);
    bars.push(Math.max(0.06, a) * 100);
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: '4px 0',
      display: 'flex',
      alignItems: 'center',
      gap: 1,
      padding: '0 6px'
    }
  }, bars.map((h, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      flex: 1,
      height: h + '%',
      background: DK.wave,
      opacity: 0.85,
      minWidth: 1
    }
  })));
}
function Clip({
  left,
  width,
  color,
  label,
  sub
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 3,
      bottom: 3,
      left: left + '%',
      width: width + '%',
      background: color,
      border: `1px solid ${DK.clipEdge}`,
      borderRadius: 2,
      overflow: 'hidden',
      display: 'flex',
      alignItems: 'center',
      padding: '0 8px',
      boxSizing: 'border-box'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: DK.clipText,
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, label), sub && /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      color: DK.clipSub,
      marginLeft: 'auto',
      flexShrink: 0
    }
  }, sub));
}
function EditTimeline({
  state
}) {
  const scenes = FLOW.activeScenes(state);
  const total = FLOW.totalLength(state);
  // scene start offsets
  let acc = 0;
  const sceneClips = scenes.map(s => {
    const dur = s.shots.reduce((a, sh) => a + sh.dur, 0);
    const clip = {
      left: acc / total * 100,
      width: dur / total * 100,
      name: FLOW.locName(s.locId).toUpperCase(),
      n: s.n
    };
    acc += dur;
    return clip;
  });
  const ROW_H = 34;
  const header = (label, p) => /*#__PURE__*/React.createElement("div", {
    style: {
      height: ROW_H,
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      padding: '0 10px',
      borderBottom: `1px solid ${DK.line}`,
      background: DK.bg1
    }
  }, p && /*#__PURE__*/React.createElement("span", {
    style: {
      width: 14,
      height: 14,
      borderRadius: 2,
      background: DK.amber,
      color: DK.bg0,
      fontFamily: FK.MONO,
      fontSize: 9,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center'
    }
  }, "P"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 11,
      color: DK.txt2,
      flex: 1
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 5,
      height: 5,
      borderRadius: 999,
      background: DK.txt3
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 5,
      height: 5,
      borderRadius: 999,
      background: DK.txt3
    }
  }));
  const lane = children => /*#__PURE__*/React.createElement("div", {
    style: {
      height: ROW_H,
      position: 'relative',
      borderBottom: `1px solid ${DK.line}`,
      background: DK.bg0
    }
  }, children);
  const ticks = [];
  for (let t = 0; t <= total; t += 5) ticks.push(t);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flexShrink: 0,
      display: 'flex',
      flexDirection: 'column',
      background: DK.bg0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: 150,
      flexShrink: 0,
      borderRight: `1px solid ${DK.line2}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '0 10px',
      height: 24,
      boxSizing: 'border-box',
      display: 'flex',
      alignItems: 'center',
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.16em',
      color: DK.txt3,
      textTransform: 'uppercase'
    }
  }, "Video"), header('V1.4'), header('V1.3'), header('V1.2', true), header('V1.1'), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '0 10px',
      height: 24,
      boxSizing: 'border-box',
      display: 'flex',
      alignItems: 'center',
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.16em',
      color: DK.txt3,
      textTransform: 'uppercase',
      borderTop: `1px solid ${DK.line}`
    }
  }, "Audio"), header('A1.L'), header('A1.R')), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      position: 'relative',
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: 24
    }
  }), lane(/*#__PURE__*/React.createElement(Clip, {
    left: 88,
    width: 12,
    color: DK.clipGray,
    label: "TENET \u2014 END LOGO",
    sub: "0"
  })), lane(/*#__PURE__*/React.createElement(Clip, {
    left: 64,
    width: 24,
    color: DK.clipGray,
    label: "STOP GUESSING \u2014 END LINE",
    sub: "0"
  })), lane(sceneClips.map((c, i) => /*#__PURE__*/React.createElement(Clip, {
    key: i,
    left: c.left,
    width: c.width,
    color: DK.clipBlue,
    label: c.name,
    sub: c.n
  }))), lane(/*#__PURE__*/React.createElement(Clip, {
    left: 0,
    width: 100,
    color: DK.clip,
    label: "01_brief-history_16x9.mov",
    sub: "MOV"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 24,
      borderTop: `1px solid ${DK.line}`
    }
  }), lane(/*#__PURE__*/React.createElement(Waveform, null)), lane(/*#__PURE__*/React.createElement(Waveform, null)), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 0,
      bottom: 0,
      left: '34%',
      width: 1,
      background: DK.amber,
      pointerEvents: 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: -1,
      left: -4,
      width: 9,
      height: 8,
      background: DK.amber,
      clipPath: 'polygon(0 0,100% 0,50% 100%)'
    }
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      borderTop: `1px solid ${DK.line2}`,
      background: DK.bg1,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: 150,
      flexShrink: 0,
      borderRight: `1px solid ${DK.line2}`
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      position: 'relative',
      height: 26
    }
  }, ticks.map(t => /*#__PURE__*/React.createElement("span", {
    key: t,
    style: {
      position: 'absolute',
      left: t / total * 100 + '%',
      top: 6,
      fontFamily: FK.MONO,
      fontSize: 9,
      color: DK.txt3,
      transform: 'translateX(2px)'
    }
  }, `01:00:${String(t).padStart(2, '0')}:00`)))));
}

// ── Beats: emotional throughline ────────────────────────────────────
function smoothPath(pts) {
  if (pts.length < 2) return '';
  let d = `M ${pts[0].x} ${pts[0].y}`;
  for (let i = 0; i < pts.length - 1; i++) {
    const p0 = pts[i - 1] || pts[i];
    const p1 = pts[i];
    const p2 = pts[i + 1];
    const p3 = pts[i + 2] || p2;
    const c1x = p1.x + (p2.x - p0.x) / 6,
      c1y = p1.y + (p2.y - p0.y) / 6;
    const c2x = p2.x - (p3.x - p1.x) / 6,
      c2y = p2.y - (p3.y - p1.y) / 6;
    d += ` C ${c1x.toFixed(1)} ${c1y.toFixed(1)}, ${c2x.toFixed(1)} ${c2y.toFixed(1)}, ${p2.x.toFixed(1)} ${p2.y.toFixed(1)}`;
  }
  return d;
}

// Overlay form — a transparent curve drawn ON TOP of the (dimmed) timeline,
// so the editor can read the emotional shape against the actual cut.
function BeatsOverlay({
  state
}) {
  const beats = [{
    key: 'open',
    t: 0.22
  }, {
    key: 'setup',
    t: 0.42,
    mk: DK.amber
  }, {
    key: 'turn',
    t: 0.24,
    mk: DK.read
  }, {
    key: 'build',
    t: 0.52
  }, {
    key: 'peak',
    t: 0.92,
    mk: DK.pink,
    hot: true
  }, {
    key: 'land',
    t: 0.50
  }];
  // 0..100 coordinate space, stretched to fill via preserveAspectRatio=none.
  // Markers are HTML (percent-positioned) so they stay perfectly circular.
  const padXl = 3,
    padXr = 3,
    padTop = 14,
    padBot = 14;
  const X = i => padXl + i / (beats.length - 1) * (100 - padXl - padXr);
  const Y = t => padTop + (1 - t) * (100 - padTop - padBot);
  const pts = beats.map((b, i) => ({
    x: X(i),
    y: Y(b.t)
  }));
  const line = smoothPath(pts);
  const area = line + ` L ${pts[pts.length - 1].x.toFixed(1)} 100 L ${pts[0].x.toFixed(1)} 100 Z`;
  const uid = 'beats';
  const sh = DK === DK_LIGHT ? '0 1px 2px rgba(255,255,255,0.6)' : '0 1px 4px rgba(0,0,0,0.7)';
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      display: 'flex',
      flexDirection: 'column',
      pointerEvents: 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      padding: '10px 18px 0'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 14,
      color: DK.txt,
      textShadow: sh
    }
  }, "Tension and release across the story"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 13,
      color: DK.txt2,
      letterSpacing: '0.04em',
      textShadow: sh,
      whiteSpace: 'nowrap',
      flexShrink: 0,
      marginLeft: 16
    }
  }, ":", state.mode, " \xB7 6 beats")), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      position: 'relative',
      minHeight: 0,
      margin: '8px 18px 0'
    }
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: "0 0 100 100",
    width: "100%",
    height: "100%",
    preserveAspectRatio: "none",
    style: {
      display: 'block',
      overflow: 'visible'
    }
  }, /*#__PURE__*/React.createElement("defs", null, /*#__PURE__*/React.createElement("linearGradient", {
    id: `${uid}-stroke`,
    x1: "0",
    y1: "0",
    x2: "1",
    y2: "0"
  }, /*#__PURE__*/React.createElement("stop", {
    offset: "0",
    stopColor: DK.read
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "0.42",
    stopColor: DK.amber
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "0.78",
    stopColor: DK.pink
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "1",
    stopColor: DK.amber
  })), /*#__PURE__*/React.createElement("linearGradient", {
    id: `${uid}-fill`,
    x1: "0",
    y1: "0",
    x2: "0",
    y2: "1"
  }, /*#__PURE__*/React.createElement("stop", {
    offset: "0",
    stopColor: DK.amber,
    stopOpacity: DK === DK_LIGHT ? 0.16 : 0.22
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "1",
    stopColor: DK.amber,
    stopOpacity: "0"
  }))), /*#__PURE__*/React.createElement("path", {
    d: area,
    fill: `url(#${uid}-fill)`
  }), /*#__PURE__*/React.createElement("path", {
    d: line,
    fill: "none",
    stroke: `url(#${uid}-stroke)`,
    strokeWidth: "3",
    strokeLinecap: "round",
    vectorEffect: "non-scaling-stroke"
  })), beats.map((b, i) => b.mk && /*#__PURE__*/React.createElement("div", {
    key: b.key,
    style: {
      position: 'absolute',
      left: pts[i].x + '%',
      top: pts[i].y + '%',
      transform: 'translate(-50%,-50%)',
      width: 15,
      height: 15,
      borderRadius: '50%',
      background: DK.bg0,
      border: `2.5px solid ${b.mk}`,
      boxSizing: 'border-box',
      boxShadow: DK === DK_LIGHT ? 'none' : '0 1px 6px rgba(0,0,0,0.6)'
    }
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: `repeat(${beats.length}, 1fr)`,
      padding: '6px 18px 8px'
    }
  }, beats.map(b => /*#__PURE__*/React.createElement("span", {
    key: b.key,
    style: {
      textAlign: 'center',
      fontFamily: FK.MONO,
      fontSize: 12,
      letterSpacing: '0.08em',
      color: b.hot ? DK.amber : DK.txt2,
      fontWeight: b.hot ? 700 : 400,
      textShadow: sh
    }
  }, b.key))));
}

// ── The edit bay (Creative-lane landing) ────────────────────────────
// The director's working surface: scaffold + monitor + live script + timeline.
// No top chrome — CreativeLane owns the unified bar + nav. `theme` drives the
// shared DK palette across the whole lane (bay + portal views).
function EditBay({
  state,
  cat,
  subIdx,
  flashId,
  theme,
  onCat,
  onSub,
  onMode
}) {
  DK = theme === 'light' ? DK_LIGHT : DK_DARK;
  const [bottomView, setBottomView] = React.useState('timeline');
  const [refOpen, setRefOpen] = React.useState(false);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex',
      flexDirection: 'column',
      background: DK.bg0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '1 1 auto',
      display: 'flex',
      minHeight: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 ' + (refOpen ? 360 : 50) + 'px',
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement(EditScaffold, {
    cat: cat,
    subIdx: subIdx,
    onCat: onCat,
    onSub: onSub,
    open: refOpen,
    onToggle: () => setRefOpen(o => !o)
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement(ProgramMonitor, {
    state: state
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 450px',
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement(EditScript, {
    state: state,
    flashId: flashId
  }))), /*#__PURE__*/React.createElement(TransportBar, {
    state: state,
    onMode: onMode,
    view: bottomView,
    onView: setBottomView
  }), bottomView === 'beats' ? /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      opacity: 0.25
    }
  }, /*#__PURE__*/React.createElement(EditTimeline, {
    state: state
  })), /*#__PURE__*/React.createElement(BeatsOverlay, {
    state: state
  })) : /*#__PURE__*/React.createElement(EditTimeline, {
    state: state
  }));
}

// Set the shared dark palette before rendering reused sub-components from
// another file (they read the module-scoped `DK` at render time).
function setEditTheme(theme) {
  DK = theme === 'light' ? DK_LIGHT : DK_DARK;
}
Object.assign(window, {
  EditBay,
  DK_DARK,
  DK_LIGHT,
  setEditTheme,
  ProgramMonitor,
  EditScript,
  EditTimeline,
  TransportBar,
  ChatProducer,
  EditScaffold,
  BeatsOverlay
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/director-edit.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/framework.js
try { (() => {
/* ============================================================
   TENET — Commercial Development Framework.
   Each seat tracks a TOP 10; each variable holds six subs.
   Producer's ten carry dollars; director's ten are creative.
   ============================================================ */
window.FRAMEWORK = function () {
  // Producer day-base costs sum to $450,000. The night scene adds a
  // $50,000 overtime premium under Crew — cleared when it flips to day.
  const CREW_NIGHT_PREMIUM = 50000;
  const PRODUCER = [{
    id: 'scope',
    n: 1,
    name: 'Scope',
    cost: 35000,
    subs: ['Scope of work', 'Campaign size', 'Number of spots', 'Required assets', 'Client expectations', 'Change orders']
  }, {
    id: 'deliv',
    n: 2,
    name: 'Deliverables',
    cost: 40000,
    subs: ['Broadcast masters', 'Social versions', 'Aspect ratios', 'Market versions', 'Language versions', 'Cutdowns']
  }, {
    id: 'sched',
    n: 3,
    name: 'Schedule',
    cost: 30000,
    subs: ['Pre-production timeline', 'Shoot days', 'Post schedule', 'Delivery deadlines', 'Approval timelines', 'Critical path items']
  }, {
    id: 'talent',
    n: 4,
    name: 'Talent',
    cost: 70000,
    subs: ['Casting', 'Talent fees', 'Usage rights', 'Availability', 'Union requirements', 'Renewals']
  }, {
    id: 'crew',
    n: 5,
    name: 'Crew',
    cost: 60000,
    nightVar: true,
    subs: ['Crew size', 'Department heads', 'Overtime', 'Travel', 'Specialty crew', 'Labor costs']
  }, {
    id: 'locs',
    n: 6,
    name: 'Locations',
    cost: 55000,
    subs: ['Scouting', 'Permits', 'Parking', 'Weather', 'Site restrictions', 'Logistics']
  }, {
    id: 'design',
    n: 7,
    name: 'Production Design',
    cost: 45000,
    subs: ['Sets', 'Props', 'Wardrobe', 'Hair', 'Makeup', 'Vehicles']
  }, {
    id: 'post',
    n: 8,
    name: 'Post Production',
    cost: 80000,
    subs: ['Editorial', 'Color', 'Sound', 'Music', 'VFX', 'Localization']
  }, {
    id: 'rights',
    n: 9,
    name: 'Rights & Legal',
    cost: 20000,
    subs: ['Talent rights', 'Music rights', 'Territory', 'Term length', 'Exclusivity', 'Clearances']
  }, {
    id: 'risk',
    n: 10,
    name: 'Risk & Contingency',
    cost: 15000,
    subs: ['Weather risk', 'Schedule risk', 'Budget risk', 'Talent risk', 'Vendor risk', 'Delivery risk']
  }];
  const DIRECTOR = [{
    id: 'story',
    n: 1,
    name: 'Story',
    subs: ['Narrative clarity', 'Structure', 'Character motivation', 'Beginning', 'Middle', 'End']
  }, {
    id: 'concept',
    n: 2,
    name: 'Concept',
    subs: ['Core idea', 'Originality', 'Simplicity', 'Memorability', 'Brand fit', 'Strategic intent']
  }, {
    id: 'perf',
    n: 3,
    name: 'Performance',
    subs: ['Acting', 'Authenticity', 'Behavior', 'Timing', 'Improvisation', 'Chemistry']
  }, {
    id: 'visual',
    n: 4,
    name: 'Visual Language',
    subs: ['Style', 'Imagery', 'Symbolism', 'References', 'Visual motifs', 'World building']
  }, {
    id: 'cast',
    n: 5,
    name: 'Casting',
    subs: ['Lead talent', 'Supporting talent', 'Character fit', 'Diversity', 'Presence', 'Credibility']
  }, {
    id: 'pdesign',
    n: 6,
    name: 'Production Design',
    subs: ['Sets', 'Locations', 'Props', 'Wardrobe', 'Color palette', 'Environment']
  }, {
    id: 'camera',
    n: 7,
    name: 'Camera & Composition',
    subs: ['Framing', 'Lens choice', 'Camera movement', 'Blocking', 'Scale', 'Perspective']
  }, {
    id: 'edit',
    n: 8,
    name: 'Editorial Rhythm',
    subs: ['Pacing', 'Shot selection', 'Transitions', 'Momentum', 'Timing', 'Flow']
  }, {
    id: 'impact',
    n: 9,
    name: 'Emotional Impact',
    subs: ['Audience reaction', 'Empathy', 'Surprise', 'Tension', 'Delight', 'Recall']
  }];
  function nightActive(state) {
    return window.FLOW.activeScenes(state).some(s => window.FLOW.isNight(s, state));
  }
  function categoryCost(cat, state) {
    return cat.cost + (cat.nightVar && nightActive(state) ? CREW_NIGHT_PREMIUM : 0);
  }
  function producerTotal(state) {
    return PRODUCER.reduce((a, c) => a + categoryCost(c, state), 0);
  }
  function top10(lane) {
    return lane === 'producer' ? PRODUCER : DIRECTOR;
  }

  // ══ Producer financial model ════════════════════════════════════
  // Built for cause-and-effect, not bookkeeping. Each cost center holds
  // Budget / Actual / Forecast and the WHY drivers behind the number.
  // A night exterior is the live variable: it loads crew overtime, which
  // draws against contingency — clearing it ripples through every figure.
  const CONTINGENCY_RESERVE = 50000; // held inside the approved budget
  const FIN_NIGHT_OT = 18000; // crew OT premium when night is live

  // center.base = forecast at day rates. budget = approved line. actual = committed-to-date.
  const FIN_CENTERS = [{
    id: 'talent',
    name: 'Talent',
    budget: 72000,
    actual: 58000,
    base: 74000,
    why: 'Two principals carrying national usage drive most of talent.',
    drivers: [{
      label: 'Principal cast (×2)',
      amt: 38000
    }, {
      label: 'Usage & renewals',
      amt: 16000
    }, {
      label: 'Supporting cast',
      amt: 12000
    }, {
      label: 'Wardrobe + hair/makeup',
      amt: 8000
    }]
  }, {
    id: 'crew',
    name: 'Crew',
    budget: 95000,
    actual: 74000,
    base: 95000,
    nightVar: true,
    why: 'A night exterior pushes the whole crew into overtime.',
    drivers: [{
      label: 'Core crew — day rates',
      amt: 62000
    }, {
      label: 'Department heads',
      amt: 18000
    }, {
      label: 'Night overtime premium',
      amt: FIN_NIGHT_OT,
      night: true
    }, {
      label: 'Travel + kit fees',
      amt: 15000
    }]
  }, {
    id: 'locs',
    name: 'Locations',
    budget: 50000,
    actual: 47000,
    base: 55000,
    why: 'Downtown, at night, with picture cars — that is the $55k.',
    drivers: [{
      label: 'Downtown street closure',
      amt: 18000
    }, {
      label: 'Police lockup',
      amt: 15000
    }, {
      label: 'Picture-vehicle staging',
      amt: 12000
    }, {
      label: 'Parking + company moves',
      amt: 10000
    }]
  }, {
    id: 'equip',
    name: 'Equipment',
    budget: 62000,
    actual: 51000,
    base: 64000,
    why: 'The technocrane and picture vehicles carry the package.',
    drivers: [{
      label: 'Camera package + lenses',
      amt: 24000
    }, {
      label: 'Technocrane',
      amt: 16000
    }, {
      label: 'Lighting + grip',
      amt: 14000
    }, {
      label: 'Picture vehicles',
      amt: 10000
    }]
  }, {
    id: 'design',
    name: 'Production Design',
    budget: 46000,
    actual: 40000,
    base: 45000,
    why: 'A single build, lightly dressed — running efficient.',
    drivers: [{
      label: 'Set construction',
      amt: 18000
    }, {
      label: 'Set dressing + props',
      amt: 14000
    }, {
      label: 'Fabrication',
      amt: 8000
    }, {
      label: 'Strike',
      amt: 5000
    }]
  }, {
    id: 'post',
    name: 'Post Production',
    budget: 90000,
    actual: 32000,
    base: 88000,
    why: 'VFX cleanup is the heaviest line in post.',
    drivers: [{
      label: 'VFX cleanup',
      amt: 28000
    }, {
      label: 'Editorial',
      amt: 24000
    }, {
      label: 'Color + online',
      amt: 18000
    }, {
      label: 'Sound + music',
      amt: 18000
    }]
  }, {
    id: 'legal',
    name: 'Legal',
    budget: 35000,
    actual: 28000,
    base: 33000,
    why: 'Usage and music licensing dominate legal.',
    drivers: [{
      label: 'Talent usage rights',
      amt: 14000
    }, {
      label: 'Music licensing',
      amt: 11000
    }, {
      label: 'Clearances',
      amt: 8000
    }]
  }];
  function finForecast(c, state) {
    return c.base + (c.nightVar && nightActive(state) ? FIN_NIGHT_OT : 0);
  }
  function finVariance(c, state) {
    return finForecast(c, state) - c.budget;
  }
  function finDrivers(c, state) {
    const n = nightActive(state);
    return c.drivers.filter(d => !d.night || n);
  }
  function finTotals(state) {
    const directBudget = FIN_CENTERS.reduce((a, c) => a + c.budget, 0); // 450,000
    const actual = FIN_CENTERS.reduce((a, c) => a + c.actual, 0);
    const forecast = FIN_CENTERS.reduce((a, c) => a + finForecast(c, state), 0);
    const overrun = Math.max(0, forecast - directBudget);
    const approved = directBudget + CONTINGENCY_RESERVE; // 500,000
    return {
      approved,
      actual,
      forecast,
      variance: forecast - approved,
      reserve: CONTINGENCY_RESERVE,
      drawn: overrun,
      contingency: CONTINGENCY_RESERVE - overrun,
      directBudget
    };
  }
  // What is eating the contingency reserve (cause-and-effect).
  function finContingencyDrivers(state) {
    const n = nightActive(state);
    const d = [{
      label: 'Location overage',
      amt: 4000
    }];
    if (n) d.unshift({
      label: 'Night overtime — crew',
      amt: FIN_NIGHT_OT
    });
    return d;
  }

  // ══ Bid build-up (AICP bid model) ══════════════════════════════
  // The producer's deeper view: direct costs roll up by section, then the
  // deal-term levers (markup, production fee, insurance) load on top.
  // Nothing is stored as a total — every number is recomputed from inputs,
  // so a slid fee ripples straight to the grand total and reconciles to
  // the expected (printed) figure within tolerance.
  const BID = {
    client: window.FLOW.TITLE,
    agency: 'H&W',
    project: '#17433',
    status: 'BALLPARK',
    system: 'AICP v2.6.1',
    director: 'D. Parker',
    expected: 500000,
    markupPct: 15,
    feePct: 12,
    insurancePct: 3.5,
    sections: [{
      code: 'A',
      name: 'Talent Labor',
      lines: [{
        desc: 'O/C Principals — buyout',
        qty: 2,
        days: 1,
        rate: 19000,
        unit: 'FLAT'
      }, {
        desc: 'Supporting cast',
        qty: 4,
        days: 2,
        rate: 1500,
        unit: 'DAY'
      }, {
        desc: 'Usage & renewals — 2yr',
        qty: 1,
        days: 1,
        rate: 10000,
        unit: 'FLAT'
      }, {
        desc: 'Wardrobe + hair/makeup',
        qty: 1,
        days: 1,
        rate: 4000,
        unit: 'FLAT'
      }]
    }, {
      code: 'B',
      name: 'Crew Labor',
      lines: [{
        desc: 'Core crew — day rates',
        qty: 12,
        days: 3,
        rate: 1500,
        unit: 'DAY'
      }, {
        desc: 'Department heads',
        qty: 5,
        days: 3,
        rate: 1000,
        unit: 'DAY'
      }, {
        desc: 'Travel + kit fees',
        qty: 1,
        days: 1,
        rate: 13000,
        unit: 'FLAT'
      }]
    }, {
      code: 'C',
      name: 'Locations',
      lines: [{
        desc: 'Downtown street closure',
        qty: 1,
        days: 1,
        rate: 16000,
        unit: 'FLAT'
      }, {
        desc: 'Police lockup',
        qty: 6,
        days: 1,
        rate: 2000,
        unit: 'HOUR'
      }, {
        desc: 'Picture-vehicle staging',
        qty: 1,
        days: 1,
        rate: 10000,
        unit: 'FLAT'
      }, {
        desc: 'Parking + company moves',
        qty: 1,
        days: 1,
        rate: 8000,
        unit: 'FLAT'
      }]
    }, {
      code: 'D',
      name: 'Equipment',
      lines: [{
        desc: 'Camera package + lenses',
        qty: 1,
        days: 3,
        rate: 7000,
        unit: 'DAY'
      }, {
        desc: 'Technocrane',
        qty: 1,
        days: 2,
        rate: 8000,
        unit: 'DAY'
      }, {
        desc: 'Lighting + grip',
        qty: 1,
        days: 3,
        rate: 4000,
        unit: 'DAY'
      }, {
        desc: 'Picture vehicles',
        qty: 1,
        days: 1,
        rate: 9000,
        unit: 'FLAT'
      }]
    }, {
      code: 'E',
      name: 'Production Design',
      lines: [{
        desc: 'Set construction',
        qty: 1,
        days: 1,
        rate: 16000,
        unit: 'FLAT'
      }, {
        desc: 'Set dressing + props',
        qty: 1,
        days: 1,
        rate: 12000,
        unit: 'FLAT'
      }, {
        desc: 'Fabrication',
        qty: 1,
        days: 1,
        rate: 6000,
        unit: 'FLAT'
      }, {
        desc: 'Strike',
        qty: 1,
        days: 1,
        rate: 4000,
        unit: 'FLAT'
      }]
    }, {
      code: 'F',
      name: 'Post Production',
      lines: [{
        desc: 'VFX cleanup',
        qty: 1,
        days: 1,
        rate: 26000,
        unit: 'FLAT'
      }, {
        desc: 'Editorial',
        qty: 1,
        days: 1,
        rate: 22000,
        unit: 'FLAT'
      }, {
        desc: 'Color + online',
        qty: 1,
        days: 1,
        rate: 14000,
        unit: 'FLAT'
      }, {
        desc: 'Sound + music',
        qty: 1,
        days: 1,
        rate: 10000,
        unit: 'FLAT'
      }]
    }, {
      code: 'G',
      name: 'Travel (CS17)',
      travel: true,
      handlingPct: 10,
      lines: [{
        desc: 'Directors',
        ctype: 'HOTEL',
        qty: 2,
        days: 14,
        rate: 350,
        unit: 'DAY'
      }, {
        desc: 'Talent',
        ctype: 'TRANSPORT',
        qty: 1,
        days: 1,
        rate: 8000,
        unit: 'FLAT'
      }, {
        desc: 'Director of Photography',
        ctype: 'PER DIEM',
        qty: 1,
        days: 14,
        rate: 100,
        unit: 'DAY'
      }, {
        desc: 'Producer',
        ctype: 'FLIGHT',
        qty: 1,
        days: 1,
        rate: 1800,
        unit: 'EACH'
      }]
    }]
  };

  // Scenario levers — the "what if" engine. Each is a direct-cost saving
  // plus its schedule / crew / risk consequence. risk: <0 lowers, >0 raises.
  const BID_SCENARIOS = [{
    id: 'extras',
    label: 'Reduce extras',
    note: '120 \u2192 60 background',
    save: 12000,
    days: 0,
    crew: 0,
    risk: -1
  }, {
    id: 'crane',
    label: 'Remove technocrane',
    note: 'Crane \u2192 dolly + jib',
    save: 16000,
    days: 0,
    crew: -2,
    risk: -1
  }, {
    id: 'stage',
    label: 'Move to stage',
    note: 'Drops the night exterior',
    save: 22000,
    days: -1,
    crew: -8,
    risk: -2
  }, {
    id: 'day',
    label: 'Drop a shoot day',
    note: '3 \u2192 2 shoot days',
    save: 28000,
    days: -1,
    crew: -12,
    risk: 2
  }, {
    id: 'vfx',
    label: 'Convert to VFX plates',
    note: 'Practical \u2192 plate + comp',
    save: 9000,
    days: 0,
    crew: -4,
    risk: 1
  }];
  return {
    PRODUCER,
    DIRECTOR,
    CREW_NIGHT_PREMIUM,
    nightActive,
    categoryCost,
    producerTotal,
    top10,
    FIN_CENTERS,
    CONTINGENCY_RESERVE,
    FIN_NIGHT_OT,
    finForecast,
    finVariance,
    finDrivers,
    finTotals,
    finContingencyDrivers,
    BID,
    BID_SCENARIOS
  };
}();
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/framework.js", error: String((e && e.message) || e) }); }

// flow_ctx/panels.jsx
try { (() => {
/* global React, FK, FGlyph, FLOW */
// Shared panels — the script (right) and the shot timeline (bottom).

// ── Script panel ────────────────────────────────────────────────────
const scriptStyles = {
  root: {
    height: '100%',
    display: 'flex',
    flexDirection: 'column',
    background: FK.PAPER,
    minHeight: 0
  },
  head: {
    padding: '22px 40px 14px',
    borderBottom: `1px solid ${FK.BORDER}`,
    flexShrink: 0,
    display: 'flex',
    alignItems: 'baseline',
    justifyContent: 'space-between'
  },
  kicker: {
    fontFamily: FK.MONO,
    fontSize: 10,
    letterSpacing: '0.2em',
    textTransform: 'uppercase',
    color: FK.AMBER
  },
  fname: {
    fontFamily: FK.MONO,
    fontSize: 11,
    color: FK.INK_FAINT
  },
  body: {
    flex: 1,
    overflow: 'auto',
    padding: '26px 40px 40px'
  },
  titleblock: {
    textAlign: 'center',
    marginBottom: 30
  },
  title: {
    fontFamily: FK.SCRIPT,
    fontSize: 24,
    color: FK.INK,
    letterSpacing: '0.02em'
  },
  logline: {
    fontFamily: FK.SCRIPT,
    fontStyle: 'italic',
    fontSize: 14,
    color: FK.INK_FAINT,
    marginTop: 8
  },
  scene: dropped => ({
    marginBottom: 22,
    opacity: dropped ? 0.32 : 1
  }),
  slug: (hot, dropped) => ({
    fontFamily: FK.SCRIPT,
    fontSize: 15,
    fontWeight: 700,
    letterSpacing: '0.04em',
    color: FK.INK,
    textTransform: 'uppercase',
    marginBottom: 8,
    padding: '3px 8px',
    marginLeft: -8,
    borderLeft: hot ? `2px solid ${FK.AMBER}` : '2px solid transparent',
    background: hot ? FK.AMBER_50 : 'transparent',
    textDecorationLine: dropped ? 'line-through' : 'none'
  }),
  para: hot => ({
    fontFamily: FK.SCRIPT,
    fontSize: 15,
    lineHeight: 1.5,
    color: FK.INK,
    padding: '4px 8px',
    marginLeft: -8,
    marginBottom: 4,
    background: hot ? FK.AMBER_50 : 'transparent',
    boxShadow: hot ? `inset 2px 0 0 ${FK.AMBER}` : 'none'
  })
};
function slugFor(scene, state) {
  const loc = FLOW.LOCS.find(l => l.id === scene.locId);
  return `${loc.kind}. ${loc.name.toUpperCase()} \u2014 ${FLOW.todOf(scene, state)}`;
}
function ScriptPanel({
  state,
  sel,
  flashId
}) {
  const S = scriptStyles;
  const active = new Set(FLOW.activeScenes(state).map(s => s.id));
  return /*#__PURE__*/React.createElement("div", {
    style: S.root
  }, /*#__PURE__*/React.createElement("div", {
    style: S.head
  }, /*#__PURE__*/React.createElement("span", {
    style: S.kicker
  }, "Script"), /*#__PURE__*/React.createElement("span", {
    style: S.fname
  }, "MERIDIAN_a-day-held_v3.fdx")), /*#__PURE__*/React.createElement("div", {
    style: S.body
  }, /*#__PURE__*/React.createElement("div", {
    style: S.titleblock
  }, /*#__PURE__*/React.createElement("div", {
    style: S.title
  }, FLOW.TITLE), /*#__PURE__*/React.createElement("div", {
    style: S.logline
  }, FLOW.LOGLINE)), FLOW.SCENES.map(scene => {
    const dropped = !active.has(scene.id);
    const selLoc = sel && sel.type === 'loc' && sel.id === scene.locId;
    const selScene = sel && sel.type === 'scene' && sel.id === scene.id;
    const selChar = sel && sel.type === 'char' && scene.chars.includes(sel.id);
    const flash = flashId === scene.id;
    const slugHot = selLoc || selScene || flash;
    return /*#__PURE__*/React.createElement("div", {
      key: scene.id,
      style: S.scene(dropped)
    }, /*#__PURE__*/React.createElement("div", {
      style: S.slug(slugHot, dropped)
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: FK.INK_FAINT,
        marginRight: 10,
        fontWeight: 400
      }
    }, scene.n), slugFor(scene, state)), scene.action.map((p, i) => {
      const hot = selChar && p.chars.includes(sel.id) || selScene || flash && i === 0;
      return /*#__PURE__*/React.createElement("div", {
        key: i,
        style: S.para(hot)
      }, p.text);
    }));
  })));
}

// ── Timeline panel ──────────────────────────────────────────────────
const tlStyles = {
  root: {
    height: '100%',
    display: 'flex',
    flexDirection: 'column',
    background: FK.PAPER_SOFT,
    borderTop: `1px solid ${FK.BORDER}`,
    padding: '20px 40px 24px',
    boxSizing: 'border-box'
  },
  head: {
    display: 'flex',
    alignItems: 'baseline',
    justifyContent: 'space-between',
    marginBottom: 20
  },
  kicker: {
    fontFamily: FK.MONO,
    fontSize: 10,
    letterSpacing: '0.2em',
    textTransform: 'uppercase',
    color: FK.AMBER,
    whiteSpace: 'nowrap'
  },
  lenWrap: {
    display: 'flex',
    alignItems: 'baseline',
    gap: 10
  },
  lenLabel: {
    fontFamily: FK.MONO,
    fontSize: 10,
    letterSpacing: '0.18em',
    textTransform: 'uppercase',
    color: FK.INK_FAINT
  },
  len: {
    fontFamily: FK.SANS,
    fontWeight: 300,
    fontSize: 40,
    color: FK.INK,
    lineHeight: 1,
    letterSpacing: '-0.01em'
  },
  track: {
    display: 'flex',
    alignItems: 'flex-end',
    gap: 5,
    height: 78
  },
  group: {
    display: 'flex',
    alignItems: 'flex-end',
    gap: 4
  },
  groupGap: {
    width: 16,
    flexShrink: 0
  },
  box: state => ({
    width: 58,
    height: state.tall ? 64 : 52,
    flexShrink: 0,
    border: `1px solid ${state.on ? FK.AMBER : FK.BORDER_STRONG}`,
    background: state.on ? FK.AMBER_50 : FK.PAPER,
    position: 'relative',
    cursor: 'default'
  }),
  dot: {
    position: 'absolute',
    top: 5,
    right: 5,
    width: 6,
    height: 6,
    borderRadius: 999,
    background: FK.AMBER
  },
  ruler: {
    display: 'flex',
    justifyContent: 'space-between',
    marginTop: 14,
    borderTop: `1px solid ${FK.BORDER}`,
    paddingTop: 8
  },
  tick: {
    fontFamily: FK.MONO,
    fontSize: 10,
    color: FK.INK_FAINT
  },
  sceneLabel: hot => ({
    fontFamily: FK.MONO,
    fontSize: 9,
    letterSpacing: '0.08em',
    textTransform: 'uppercase',
    color: hot ? FK.AMBER : FK.INK_FAINT,
    marginTop: 8,
    textAlign: 'center'
  })
};
function ModeToggle({
  mode,
  onMode
}) {
  const opts = [30, 15];
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: `1px solid ${FK.BORDER_STRONG}`,
      borderRadius: 2,
      overflow: 'hidden'
    }
  }, opts.map((m, i) => /*#__PURE__*/React.createElement("button", {
    key: m,
    onClick: () => onMode(m),
    style: {
      padding: '6px 13px',
      fontFamily: FK.MONO,
      fontSize: 12,
      cursor: 'pointer',
      border: 'none',
      borderLeft: i ? `1px solid ${FK.BORDER}` : 'none',
      background: mode === m ? FK.AMBER : FK.PAPER,
      color: mode === m ? FK.PAPER : FK.INK_MUTED
    }
  }, ":", m)));
}
function TimelinePanel({
  state,
  sel,
  flashId,
  onMode
}) {
  const S = tlStyles;
  const scenes = FLOW.activeScenes(state);
  const totalLen = FLOW.totalLength(state);
  const totalShots = scenes.reduce((a, s) => a + s.shots.length, 0);

  // Staggered reveal on mount — timer-driven so it survives paused rAF.
  const [revealed, setRevealed] = React.useState(0);
  React.useEffect(() => {
    let n = 0;
    const id = setInterval(() => {
      n += 1;
      setRevealed(n);
      if (n >= 30) clearInterval(id);
    }, 60);
    // Guarantee every box is shown even where timers are throttled.
    const fb = setTimeout(() => setRevealed(30), 1500);
    return () => {
      clearInterval(id);
      clearTimeout(fb);
    };
  }, []);
  let shotIndex = 0;
  return /*#__PURE__*/React.createElement("div", {
    style: S.root
  }, /*#__PURE__*/React.createElement("div", {
    style: S.head
  }, /*#__PURE__*/React.createElement("span", {
    style: S.kicker
  }, "Timeline \xB7 the cut"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 28
    }
  }, /*#__PURE__*/React.createElement(ModeToggle, {
    mode: state.mode,
    onMode: onMode
  }), /*#__PURE__*/React.createElement("div", {
    style: S.lenWrap
  }, /*#__PURE__*/React.createElement("span", {
    style: S.lenLabel
  }, "Length"), /*#__PURE__*/React.createElement("span", {
    style: S.len
  }, FLOW.fmtClock(totalLen))))), /*#__PURE__*/React.createElement("div", {
    style: S.track
  }, scenes.map((scene, si) => {
    const selLoc = sel && sel.type === 'loc' && sel.id === scene.locId;
    const flash = flashId === scene.id;
    return /*#__PURE__*/React.createElement(React.Fragment, {
      key: scene.id
    }, si > 0 && /*#__PURE__*/React.createElement("div", {
      style: S.groupGap
    }), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
      style: S.group
    }, scene.shots.map(sh => {
      const idx = shotIndex++;
      const shown = idx < revealed;
      if (!shown) return /*#__PURE__*/React.createElement("div", {
        key: sh.id,
        style: {
          width: 58,
          height: 52,
          flexShrink: 0
        }
      });
      const selChar = sel && sel.type === 'char' && sh.chars.includes(sel.id);
      const selScene = sel && sel.type === 'scene' && sel.id === scene.id;
      const on = selLoc || selScene || selChar || flash;
      return /*#__PURE__*/React.createElement("div", {
        key: sh.id,
        title: `${sh.desc} · ${sh.dur}s`,
        style: S.box({
          on,
          tall: on
        })
      }, selChar && /*#__PURE__*/React.createElement("span", {
        style: S.dot
      }));
    })), /*#__PURE__*/React.createElement("div", {
      style: S.sceneLabel(sel && sel.type === 'scene' && sel.id === scene.id || selLoc || flash)
    }, FLOW.locName(scene.locId))));
  })), /*#__PURE__*/React.createElement("div", {
    style: S.ruler
  }, /*#__PURE__*/React.createElement("span", {
    style: S.tick
  }, "0:00"), /*#__PURE__*/React.createElement("span", {
    style: S.tick
  }, FLOW.fmtClock(totalLen / 2)), /*#__PURE__*/React.createElement("span", {
    style: S.tick
  }, FLOW.fmtClock(totalLen))));
}
window.ScriptPanel = ScriptPanel;
window.TimelinePanel = TimelinePanel;

// ── Subcategory tab strip — the six subs of the selected Top-10 item ──
const subBar = {
  root: {
    flex: '0 0 auto',
    height: 76,
    borderBottom: `1px solid ${FK.BORDER}`,
    background: FK.PAPER,
    padding: '0 40px',
    display: 'flex',
    alignItems: 'center',
    gap: 22
  },
  cat: {
    display: 'flex',
    alignItems: 'baseline',
    gap: 9,
    flexShrink: 0
  },
  catN: {
    fontFamily: FK.MONO,
    fontSize: 11,
    color: FK.AMBER,
    letterSpacing: '0.08em'
  },
  catName: {
    fontFamily: FK.SERIF,
    fontStyle: 'italic',
    fontSize: 19,
    color: FK.INK,
    whiteSpace: 'nowrap'
  },
  tabs: {
    display: 'flex',
    alignItems: 'stretch',
    flex: 1,
    minWidth: 0,
    height: '100%'
  },
  tab: on => ({
    flex: 1,
    minWidth: 0,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    cursor: 'pointer',
    textAlign: 'center',
    padding: '0 6px',
    fontFamily: FK.MONO,
    fontSize: 10.5,
    letterSpacing: '0.04em',
    color: on ? FK.INK : FK.INK_FAINT,
    borderBottom: `2px solid ${on ? FK.AMBER : 'transparent'}`,
    whiteSpace: 'nowrap',
    overflow: 'hidden',
    textOverflow: 'ellipsis'
  }),
  cyc: {
    display: 'flex',
    alignItems: 'center',
    gap: 10,
    flexShrink: 0
  },
  arrow: {
    width: 28,
    height: 28,
    borderRadius: 999,
    border: `1px solid ${FK.BORDER_STRONG}`,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    cursor: 'pointer',
    color: FK.INK_MUTED
  },
  pos: {
    fontFamily: FK.MONO,
    fontSize: 11,
    color: FK.INK_FAINT,
    width: 32,
    textAlign: 'center'
  }
};
function SubTabBar({
  cat,
  subIdx,
  onSub
}) {
  const S = subBar;
  if (!cat) return /*#__PURE__*/React.createElement("div", {
    style: S.root
  });
  const subs = cat.subs;
  const go = d => onSub((subIdx + d + subs.length) % subs.length);
  return /*#__PURE__*/React.createElement("div", {
    style: S.root
  }, /*#__PURE__*/React.createElement("div", {
    style: S.cat
  }, /*#__PURE__*/React.createElement("span", {
    style: S.catN
  }, String(cat.n).padStart(2, '0')), /*#__PURE__*/React.createElement("span", {
    style: S.catName
  }, cat.name)), /*#__PURE__*/React.createElement("div", {
    style: S.tabs
  }, subs.map((s, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    title: s,
    style: S.tab(i === subIdx),
    onClick: () => onSub(i)
  }, s))), /*#__PURE__*/React.createElement("div", {
    style: S.cyc
  }, /*#__PURE__*/React.createElement("div", {
    style: S.arrow,
    onClick: () => go(-1)
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 14,
    style: {
      transform: 'scaleX(-1)'
    }
  })), /*#__PURE__*/React.createElement("span", {
    style: S.pos
  }, subIdx + 1, " / ", subs.length), /*#__PURE__*/React.createElement("div", {
    style: S.arrow,
    onClick: () => go(1)
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 14
  }))));
}
window.SubTabBar = SubTabBar;
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/panels.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/producer-dash.jsx
try { (() => {
/* global React, FK, FLOW, FRAMEWORK, FGlyph, useFTween */
// Producer page — a lighter windowed finance dashboard:
//   left   = small timeline + message-director chat
//   center = running actuals (budget · actual · variance + ledger)
//   right  = finance breakdown (proportions + subcategory allocations)

const LT = {
  canvas: '#ECE8E1',
  win: '#FFFFFF',
  line: 'rgba(0,0,0,0.10)',
  line2: 'rgba(0,0,0,0.16)'
};
const FIN_GOOD = '#1F8A5B'; // under-budget green
function finSigned(n) {
  return (n < 0 ? '\u2212' : '+') + FLOW.fmtMoney(Math.abs(n));
}
function Win({
  title,
  right,
  children,
  flex,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: Object.assign({
      background: LT.win,
      border: `1px solid ${LT.line}`,
      borderRadius: 4,
      display: 'flex',
      flexDirection: 'column',
      minHeight: 0,
      overflow: 'hidden'
    }, style)
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '11px 16px',
      borderBottom: `1px solid ${LT.line}`,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: FK.AMBER,
      whiteSpace: 'nowrap'
    }
  }, title), right), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      overflow: 'auto'
    }
  }, children));
}

// ── small timeline ──────────────────────────────────────────────────
function TimelineMini({
  state,
  flashId
}) {
  const scenes = FLOW.activeScenes(state);
  const total = FLOW.totalLength(state);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 16px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      marginBottom: 14
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT
    }
  }, "The cut"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: 26,
      color: FK.INK
    }
  }, FLOW.fmtClock(total))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 4,
      height: 54
    }
  }, scenes.map(s => {
    const dur = s.shots.reduce((a, sh) => a + sh.dur, 0);
    const night = FLOW.isNight(s, state);
    const hot = flashId === s.id;
    return /*#__PURE__*/React.createElement("div", {
      key: s.id,
      style: {
        flex: dur,
        minWidth: 0,
        border: `1px solid ${hot ? FK.AMBER : LT.line2}`,
        background: hot ? FK.AMBER_50 : night ? '#EFE4D2' : FK.PAPER_SOFT,
        borderRadius: 2,
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'space-between',
        padding: '5px 6px',
        overflow: 'hidden'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 8.5,
        color: FK.INK_MUTED,
        whiteSpace: 'nowrap',
        overflow: 'hidden',
        textOverflow: 'ellipsis'
      }
    }, FLOW.locName(s.locId)), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 8,
        letterSpacing: '0.06em',
        color: night ? FK.AMBER_800 : FK.INK_FAINT
      }
    }, FLOW.todOf(s, state)));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      marginTop: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      color: FK.INK_FAINT
    }
  }, "0:00"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      color: FK.INK_FAINT
    }
  }, FLOW.fmtClock(total))));
}

// ── chat to director ────────────────────────────────────────────────
const PMSG = 'Schedule won\u2019t let us shoot this night scene. Can it be changed?';
function ChatDirector({
  chat,
  onSend
}) {
  const bub = mine => ({
    alignSelf: mine ? 'flex-end' : 'flex-start',
    maxWidth: '88%',
    background: mine ? FK.INK : FK.PAPER,
    color: mine ? FK.PAPER : FK.INK,
    border: mine ? 'none' : `1px solid ${LT.line}`,
    borderRadius: 8,
    padding: '8px 11px',
    fontFamily: FK.SANS,
    fontSize: 12.5,
    lineHeight: 1.4,
    marginBottom: 7
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '14px 16px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, chat.messages.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: bub(m.from === 'producer')
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: m.from === 'producer' ? 'rgba(255,255,255,0.6)' : FK.INK_FAINT,
      marginBottom: 3
    }
  }, m.from === 'producer' ? 'You · Producer' : 'D. Parker · Director'), m.text)), chat.typing && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 12.5,
      color: FK.INK_FAINT,
      marginBottom: 7
    }
  }, "Director is editing the script\\u2026")), !chat.sent && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 9
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 12.5,
      color: FK.INK,
      lineHeight: 1.4,
      padding: '9px 11px',
      background: FK.PAPER_SOFT,
      border: `1px solid ${LT.line2}`,
      borderRadius: 8
    }
  }, PMSG), /*#__PURE__*/React.createElement("button", {
    onClick: onSend,
    style: {
      alignSelf: 'flex-end',
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      cursor: 'pointer',
      background: FK.AMBER,
      color: FK.PAPER,
      border: 'none',
      borderRadius: 2,
      padding: '8px 15px',
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.12em',
      textTransform: 'uppercase'
    }
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "send",
    size: 13
  }), " Send")));
}

// ── running actuals ─────────────────────────────────────────────────
function Figure({
  label,
  value,
  accent
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.13em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      marginBottom: 7,
      whiteSpace: 'nowrap'
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: 29,
      lineHeight: 1,
      letterSpacing: '-0.02em',
      color: accent || FK.INK,
      whiteSpace: 'nowrap'
    }
  }, value));
}
const FIN_COLS = 'minmax(150px, 1.4fr) 82px 82px 88px 94px';
function RunningActuals({
  state,
  cat,
  onCat,
  catFlash
}) {
  const t = FRAMEWORK.finTotals(state);
  const forecast = useFTween(t.forecast, 700);
  const variance = useFTween(t.variance, 700);
  const conting = useFTween(t.contingency, 700);
  const v = Math.round(variance);
  const cR = Math.round(conting);
  const lowC = cR < t.reserve * 0.45;
  const drawnPct = (t.reserve - cR) / t.reserve * 100;
  const metaR = {
    fontFamily: FK.MONO,
    fontSize: 9,
    letterSpacing: '0.1em',
    textTransform: 'uppercase',
    color: FK.INK_FAINT,
    textAlign: 'right'
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '20px 24px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 18,
      paddingBottom: 20,
      borderBottom: `1px solid ${LT.line}`
    }
  }, /*#__PURE__*/React.createElement(Figure, {
    label: "Approved",
    value: FLOW.fmtMoney(t.approved)
  }), /*#__PURE__*/React.createElement(Figure, {
    label: "Running actual",
    value: FLOW.fmtMoney(t.actual)
  }), /*#__PURE__*/React.createElement(Figure, {
    label: "Forecast final",
    value: FLOW.fmtMoney(Math.round(forecast))
  }), /*#__PURE__*/React.createElement(Figure, {
    label: "Variance",
    value: v === 0 ? '$0' : finSigned(v),
    accent: v > 0 ? FK.AMBER_800 : FIN_GOOD
  }), /*#__PURE__*/React.createElement(Figure, {
    label: "Contingency left",
    value: FLOW.fmtMoney(cR),
    accent: lowC ? FK.AMBER_800 : undefined
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: FIN_COLS,
      gap: 8,
      padding: '0 8px',
      margin: '18px 0 2px'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT
    }
  }, "Cost center"), /*#__PURE__*/React.createElement("span", {
    style: metaR
  }, "Budget"), /*#__PURE__*/React.createElement("span", {
    style: metaR
  }, "Actual"), /*#__PURE__*/React.createElement("span", {
    style: metaR
  }, "Forecast"), /*#__PURE__*/React.createElement("span", {
    style: metaR
  }, "Variance")), FRAMEWORK.FIN_CENTERS.map(c => {
    const on = cat === c.id;
    const fc = FRAMEWORK.finForecast(c, state);
    const vv = fc - c.budget;
    const ot = c.nightVar && FRAMEWORK.nightActive(state);
    const num = (val, col) => /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 12,
        color: col || FK.INK,
        textAlign: 'right'
      }
    }, val);
    return /*#__PURE__*/React.createElement("div", {
      key: c.id,
      onClick: () => onCat(c.id),
      style: {
        display: 'grid',
        gridTemplateColumns: FIN_COLS,
        gap: 8,
        alignItems: 'center',
        padding: '9px 8px',
        cursor: 'pointer',
        borderBottom: `1px solid ${LT.line}`,
        background: on || catFlash === c.id ? FK.AMBER_50 : 'transparent',
        boxShadow: on ? `inset 3px 0 0 ${FK.AMBER}` : 'none'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'flex',
        alignItems: 'baseline',
        gap: 8,
        minWidth: 0
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.SERIF,
        fontStyle: 'italic',
        fontSize: 15.5,
        color: on ? FK.AMBER_800 : FK.INK,
        whiteSpace: 'nowrap'
      }
    }, c.name), ot && /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 8.5,
        letterSpacing: '0.08em',
        textTransform: 'uppercase',
        color: FK.AMBER_800,
        padding: '1px 5px',
        borderRadius: 2,
        background: FK.AMBER_50,
        border: `1px solid ${FK.AMBER}`,
        flexShrink: 0
      }
    }, "+OT")), num(FLOW.fmtMoney(c.budget)), num(FLOW.fmtMoney(c.actual), FK.INK_MUTED), num(FLOW.fmtMoney(fc)), num(vv === 0 ? '\u2014' : finSigned(vv), vv > 0 ? FK.AMBER_800 : vv < 0 ? FIN_GOOD : FK.INK_FAINT));
  }), /*#__PURE__*/React.createElement("div", {
    onClick: () => onCat('contingency'),
    style: {
      marginTop: 16,
      padding: '12px 10px',
      cursor: 'pointer',
      borderRadius: 3,
      background: cat === 'contingency' ? FK.AMBER_50 : 'transparent',
      boxShadow: cat === 'contingency' ? `inset 3px 0 0 ${FK.AMBER}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: lowC ? FK.AMBER_800 : FK.INK_MUTED,
      whiteSpace: 'nowrap'
    }
  }, "Contingency reserve"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 11,
      color: FK.INK_MUTED,
      whiteSpace: 'nowrap'
    }
  }, FLOW.fmtMoney(cR), " ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: FK.INK_FAINT
    }
  }, "of ", FLOW.fmtMoney(t.reserve), " left"))), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 6,
      background: FK.PAPER_SOFT,
      borderRadius: 999,
      overflow: 'hidden',
      marginTop: 9
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      width: drawnPct + '%',
      background: lowC ? FK.AMBER_800 : FK.AMBER
    }
  }))));
}

// ── finance breakdown — cause & effect (WHAT is costing money → WHY) ──
function FinanceBreakdown({
  state,
  cat,
  onCat
}) {
  const t = FRAMEWORK.finTotals(state);
  const centers = FRAMEWORK.FIN_CENTERS;
  const isCont = cat === 'contingency';
  const sel = centers.find(c => c.id === cat) || (isCont ? null : centers[0]);

  // selected payload
  let title, amountLine, headline, drivers, chip;
  if (isCont) {
    title = 'Contingency';
    drivers = FRAMEWORK.finContingencyDrivers(state).slice().sort((a, b) => b.amt - a.amt);
    amountLine = `${FLOW.fmtMoney(t.contingency)} of ${FLOW.fmtMoney(t.reserve)} left`;
    headline = FRAMEWORK.nightActive(state) ? 'Night overtime is the biggest draw against the reserve.' : 'The reserve is largely intact — the pressure is easing.';
    chip = {
      text: FLOW.fmtMoney(t.reserve - t.contingency) + ' drawn',
      over: t.reserve - t.contingency > t.reserve * 0.5
    };
  } else {
    title = sel.name;
    const fc = FRAMEWORK.finForecast(sel, state);
    const vv = FRAMEWORK.finVariance(sel, state);
    drivers = FRAMEWORK.finDrivers(sel, state).slice().sort((a, b) => b.amt - a.amt);
    amountLine = FLOW.fmtMoney(fc) + ' forecast';
    headline = sel.why;
    chip = vv === 0 ? null : {
      text: finSigned(vv) + (vv > 0 ? ' over' : ' under'),
      over: vv > 0
    };
  }
  const maxAmt = Math.max(...drivers.map(d => d.amt), 1);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '20px 22px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      marginBottom: 10
    }
  }, "What is costing money"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      height: 26,
      borderRadius: 2,
      overflow: 'hidden',
      border: `1px solid ${LT.line}`
    }
  }, centers.map((c, i) => {
    const fc = FRAMEWORK.finForecast(c, state);
    const on = c.id === cat;
    return /*#__PURE__*/React.createElement("div", {
      key: c.id,
      title: `${c.name} · ${FLOW.fmtMoney(fc)}`,
      onClick: () => onCat(c.id),
      style: {
        width: fc / t.forecast * 100 + '%',
        background: on ? FK.AMBER : i % 2 ? FK.INK : '#3a3a3a',
        borderRight: `1px solid ${LT.win}`,
        cursor: 'pointer'
      }
    });
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      gap: 10,
      margin: '24px 0 4px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 11,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 23,
      color: FK.INK,
      whiteSpace: 'nowrap'
    }
  }, title), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 12,
      color: FK.INK_MUTED,
      whiteSpace: 'nowrap'
    }
  }, amountLine)), chip && /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.04em',
      color: chip.over ? FK.AMBER_800 : FIN_GOOD,
      padding: '3px 8px',
      borderRadius: 2,
      background: chip.over ? FK.AMBER_50 : 'rgba(31,138,91,0.10)',
      border: `1px solid ${chip.over ? FK.AMBER : 'rgba(31,138,91,0.4)'}`,
      whiteSpace: 'nowrap',
      flexShrink: 0
    }
  }, chip.text)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      gap: 10,
      margin: '14px 0 20px',
      padding: '13px 15px',
      background: FK.PAPER_SOFT,
      borderLeft: `2px solid ${FK.AMBER}`,
      borderRadius: '0 4px 4px 0'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      color: FK.AMBER,
      marginTop: 3,
      flexShrink: 0
    }
  }, "WHY"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 17,
      lineHeight: 1.35,
      color: FK.INK,
      textWrap: 'pretty'
    }
  }, headline)), drivers.map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      marginBottom: 13
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      gap: 12,
      marginBottom: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 14,
      color: FK.INK
    }
  }, d.label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 13,
      color: i === 0 ? FK.AMBER_800 : FK.INK_MUTED,
      whiteSpace: 'nowrap'
    }
  }, FLOW.fmtMoney(d.amt))), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 5,
      background: FK.PAPER_SOFT,
      borderRadius: 999,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      width: d.amt / maxAmt * 100 + '%',
      background: i === 0 ? FK.AMBER : FK.INK_FAINT
    }
  })))));
}

// ── the producer page ───────────────────────────────────────────────
// ── bid build-up (Bidding tab) ─────────────────────────────────────
const BID_COLS = '1fr 34px 40px 70px 78px';
function BidLever({
  label,
  sub,
  pct,
  amount,
  onStep
}) {
  const btn = {
    width: 21,
    height: 21,
    borderRadius: 3,
    border: `1px solid ${LT.line2}`,
    background: FK.PAPER_SOFT,
    cursor: 'pointer',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    fontFamily: FK.MONO,
    fontSize: 14,
    color: FK.INK_MUTED,
    lineHeight: 1,
    userSelect: 'none'
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '10px 0',
      borderBottom: `1px solid ${LT.line}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 14,
      color: FK.INK
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      marginTop: 2
    }
  }, sub)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: btn,
    onClick: () => onStep(-1)
  }, '\u2212'), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 13,
      color: FK.AMBER_800,
      width: 44,
      textAlign: 'center'
    }
  }, pct, "%"), /*#__PURE__*/React.createElement("span", {
    style: btn,
    onClick: () => onStep(1)
  }, "+")), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 13,
      color: FK.INK,
      width: 82,
      textAlign: 'right'
    }
  }, FLOW.fmtMoney(amount))));
}
function BiddingView() {
  const bid = FRAMEWORK.BID;
  const [open, setOpen] = React.useState(bid.sections[0].code);
  const [markup, setMarkup] = React.useState(bid.markupPct);
  const [fee, setFee] = React.useState(bid.feePct);
  const clamp = v => Math.max(0, Math.min(50, v));
  const lineTotal = l => l.qty * l.days * l.rate;
  const secSub = s => s.lines.reduce((a, l) => a + lineTotal(l), 0);
  const direct = bid.sections.reduce((a, s) => a + secSub(s), 0);
  const mAmt = Math.round(direct * markup / 100);
  const fAmt = Math.round(direct * fee / 100);
  const iAmt = Math.round((direct + mAmt) * bid.insurancePct / 100);
  const grand = direct + mAmt + fAmt + iAmt;
  const diff = grand - bid.expected;
  const ok = Math.abs(diff) <= bid.expected * 0.02;
  const colR = {
    fontFamily: FK.MONO,
    fontSize: 9,
    letterSpacing: '0.08em',
    textTransform: 'uppercase',
    color: FK.INK_FAINT,
    textAlign: 'right'
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 24px 24px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      gap: 12,
      paddingBottom: 14,
      borderBottom: `1px solid ${LT.line}`,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 11,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 19,
      color: FK.INK
    }
  }, bid.client), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 11,
      color: FK.INK_FAINT
    }
  }, bid.agency, " \xB7 ", bid.project), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      color: FK.AMBER_800,
      padding: '2px 7px',
      borderRadius: 2,
      background: FK.AMBER_50,
      border: `1px solid ${FK.AMBER}`
    }
  }, bid.status)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: FK.INK_FAINT,
      whiteSpace: 'nowrap'
    }
  }, bid.system, " \xB7 ", bid.director)), bid.sections.map(s => {
    const sub = secSub(s),
      isOpen = open === s.code;
    return /*#__PURE__*/React.createElement("div", {
      key: s.code,
      style: {
        borderBottom: `1px solid ${LT.line}`
      }
    }, /*#__PURE__*/React.createElement("div", {
      onClick: () => setOpen(isOpen ? null : s.code),
      style: {
        display: 'grid',
        gridTemplateColumns: '24px 1fr auto 16px',
        gap: 11,
        alignItems: 'center',
        padding: '11px 4px',
        cursor: 'pointer'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 11,
        color: FK.AMBER
      }
    }, s.code), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.SERIF,
        fontStyle: 'italic',
        fontSize: 16,
        color: isOpen ? FK.AMBER_800 : FK.INK
      }
    }, s.name), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 13,
        color: FK.INK
      }
    }, FLOW.fmtMoney(sub)), /*#__PURE__*/React.createElement(FGlyph, {
      name: "arrow",
      size: 11,
      style: {
        color: FK.INK_FAINT,
        transform: isOpen ? 'rotate(90deg)' : 'rotate(0deg)',
        transition: 'transform 0.15s'
      }
    })), isOpen && /*#__PURE__*/React.createElement("div", {
      style: {
        paddingBottom: 12
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'grid',
        gridTemplateColumns: BID_COLS,
        gap: 8,
        padding: '0 4px 5px'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        ...colR,
        textAlign: 'left'
      }
    }, "Description"), /*#__PURE__*/React.createElement("span", {
      style: colR
    }, "Qty"), /*#__PURE__*/React.createElement("span", {
      style: colR
    }, "Days"), /*#__PURE__*/React.createElement("span", {
      style: colR
    }, "Rate"), /*#__PURE__*/React.createElement("span", {
      style: colR
    }, "Total")), s.lines.map((l, i) => /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        display: 'grid',
        gridTemplateColumns: BID_COLS,
        gap: 8,
        alignItems: 'baseline',
        padding: '5px 4px'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.SANS,
        fontSize: 12.5,
        color: FK.INK,
        minWidth: 0
      }
    }, l.desc, l.ctype ? /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 8,
        letterSpacing: '0.08em',
        color: FK.AMBER_800,
        marginLeft: 8,
        padding: '1px 5px',
        borderRadius: 2,
        background: FK.AMBER_50,
        border: `1px solid ${FK.AMBER}`
      }
    }, l.ctype) : /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 8.5,
        letterSpacing: '0.06em',
        color: FK.INK_FAINT,
        marginLeft: 7
      }
    }, l.unit)), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 12,
        color: FK.INK_MUTED,
        textAlign: 'right'
      }
    }, l.qty), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 12,
        color: FK.INK_MUTED,
        textAlign: 'right'
      }
    }, l.days), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 12,
        color: FK.INK_MUTED,
        textAlign: 'right'
      }
    }, FLOW.fmtMoney(l.rate)), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 12,
        color: FK.INK,
        textAlign: 'right'
      }
    }, FLOW.fmtMoney(lineTotal(l))))), s.travel && /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        padding: '8px 4px 2px',
        marginTop: 4,
        borderTop: `1px solid ${LT.line}`
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 9,
        letterSpacing: '0.08em',
        textTransform: 'uppercase',
        color: FK.INK_FAINT
      }
    }, "Travel handling \xB7 ", s.handlingPct, "% reduced"), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 10,
        color: FK.INK_FAINT
      }
    }, "incl. in subtotal"))));
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 18
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '0 0 8px'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT
    }
  }, "Bid calc \xB7 live"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.04em',
      color: FK.INK_FAINT
    }
  }, "Direct ", FLOW.fmtMoney(direct))), /*#__PURE__*/React.createElement(BidLever, {
    label: "Markup",
    sub: "on direct costs",
    pct: markup,
    amount: mAmt,
    onStep: d => setMarkup(v => clamp(v + d))
  }), /*#__PURE__*/React.createElement(BidLever, {
    label: "Production fee",
    sub: "on direct costs",
    pct: fee,
    amount: fAmt,
    onStep: d => setFee(v => clamp(v + d))
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '10px 0'
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 14,
      color: FK.INK
    }
  }, "Insurance"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      marginTop: 2
    }
  }, bid.insurancePct, "% on direct + markup")), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 13,
      color: FK.INK
    }
  }, FLOW.fmtMoney(iAmt)))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 14,
      padding: '15px 17px',
      background: FK.PAPER_SOFT,
      borderRadius: 4,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      marginBottom: 5
    }
  }, "Computed grand total"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: 30,
      lineHeight: 1,
      letterSpacing: '-0.02em',
      color: FK.INK
    }
  }, FLOW.fmtMoney(grand))), /*#__PURE__*/React.createElement("div", {
    style: {
      textAlign: 'right'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 11,
      color: FK.INK_MUTED,
      marginBottom: 7
    }
  }, "Expected ", FLOW.fmtMoney(bid.expected)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.04em',
      color: ok ? FIN_GOOD : FK.AMBER_800,
      padding: '3px 9px',
      borderRadius: 2,
      background: ok ? 'rgba(31,138,91,0.10)' : FK.AMBER_50,
      border: `1px solid ${ok ? 'rgba(31,138,91,0.4)' : FK.AMBER}`,
      whiteSpace: 'nowrap'
    }
  }, ok ? 'within tolerance' : `review · ${diff > 0 ? '+' : '\u2212'}${FLOW.fmtMoney(Math.abs(diff))}`))));
}

// ── script intake (Script tab — line-by-line + live breakdown) ─────
const SCRIPT_LINES = function () {
  const out = [];
  FLOW.FILM.parts.forEach(p => {
    out.push({
      kind: 'part',
      text: p.transition ? 'Transition' : 'Part ' + p.n + ' \u00b7 ' + p.label
    });
    p.blocks.forEach(b => out.push({
      kind: b.k,
      text: b.text
    }));
  });
  return out;
}();
// What the breakdown engine flags as it reads — each tagged to a bid line.
const SCRIPT_INSIGHTS = [{
  match: 'BOOMS UP',
  code: 'D',
  cat: 'Equipment',
  driver: 'Technocrane move',
  impact: 16000,
  src: 'Camera BOOMS UP the face of an 80th-floor building'
}, {
  match: '80th-floor',
  code: 'C',
  cat: 'Locations',
  driver: 'High-rise permit + freight',
  impact: 9000,
  src: 'an 80th-floor office building'
}, {
  match: 'frantic executives',
  code: 'A',
  cat: 'Talent',
  driver: 'Background crowd (~60)',
  impact: 12000,
  src: 'rows of frantic executives'
}, {
  match: 'thrown out a window',
  code: 'F',
  cat: 'Post / FX',
  driver: 'Window gag — rig + comp',
  impact: 18000,
  src: 'a man is thrown out a window'
}, {
  match: 'Sets constructed',
  code: 'E',
  cat: 'Production Design',
  driver: 'Period set build',
  impact: 16000,
  src: 'Sets constructed. Crew scrambling.'
}, {
  match: 'parking lot',
  code: 'C',
  cat: 'Locations',
  driver: 'Exterior unit + picture vehicles',
  impact: 10000,
  src: 'Cars pull up to a store parking lot'
}, {
  match: 'production set that matches',
  code: 'E',
  cat: 'Production Design',
  driver: 'Match-to-board build',
  impact: 8000,
  src: 'a real production set that matches the storyboard'
}];
function hlScript(text) {
  for (const ins of SCRIPT_INSIGHTS) {
    const idx = text.toLowerCase().indexOf(ins.match.toLowerCase());
    if (idx >= 0) return [text.slice(0, idx), /*#__PURE__*/React.createElement("span", {
      key: "m",
      style: {
        background: FK.AMBER_50,
        color: FK.AMBER_800,
        borderRadius: 2,
        padding: '0 2px',
        boxShadow: `inset 0 -1px 0 ${FK.AMBER}`
      }
    }, text.slice(idx, idx + ins.match.length)), text.slice(idx + ins.match.length)];
  }
  return [text];
}
function ScriptProcessing({
  step
}) {
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (ref.current) ref.current.scrollTop = ref.current.scrollHeight;
  }, [step]);
  const total = SCRIPT_LINES.length;
  const done = step >= total - 1;
  const cursor = /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-block',
      width: 7,
      height: 14,
      background: FK.AMBER,
      marginLeft: 4,
      verticalAlign: '-2px',
      animation: 'tenetBreathe 1s ease-in-out infinite'
    }
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      display: 'flex',
      flexDirection: 'column',
      minHeight: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '11px 22px',
      borderBottom: `1px solid ${LT.line}`,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: FK.AMBER
    }
  }, "Reading script"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: FK.INK_FAINT
    }
  }, FLOW.FILM.file, " \xB7 ", Math.min(step + 1, total), "/", total)), /*#__PURE__*/React.createElement("div", {
    ref: ref,
    style: {
      flex: 1,
      minHeight: 0,
      overflow: 'auto',
      padding: '16px 22px 24px'
    }
  }, SCRIPT_LINES.slice(0, step + 1).map((l, i) => {
    const active = i === step && !done;
    if (l.kind === 'part') return /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        margin: i === 0 ? '0 0 12px' : '20px 0 12px'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 10,
        letterSpacing: '0.2em',
        textTransform: 'uppercase',
        color: FK.AMBER
      }
    }, l.text), /*#__PURE__*/React.createElement("div", {
      style: {
        height: 1,
        background: LT.line,
        marginTop: 8
      }
    }));
    if (l.kind === 'card') return /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 12,
        margin: '14px 0'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        height: 1,
        background: LT.line2
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 10,
        letterSpacing: '0.18em',
        textTransform: 'uppercase',
        color: FK.INK_MUTED,
        whiteSpace: 'nowrap'
      }
    }, l.text), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        height: 1,
        background: LT.line2
      }
    }));
    if (l.kind === 'q') return /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        fontFamily: FK.SERIF,
        fontStyle: 'italic',
        fontSize: 15.5,
        lineHeight: 1.4,
        color: FK.INK,
        textAlign: 'center',
        margin: '0 8px 12px'
      }
    }, hlScript(l.text), active && cursor);
    const vo = l.kind === 'vo';
    return /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        display: 'flex',
        gap: 13,
        marginBottom: 11,
        padding: '3px 6px',
        borderRadius: 3,
        background: active ? FK.AMBER_50 : 'transparent'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        flex: '0 0 44px',
        textAlign: 'right',
        paddingTop: 3
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 9,
        letterSpacing: '0.12em',
        textTransform: 'uppercase',
        color: vo ? FK.AMBER : FK.INK_FAINT
      }
    }, vo ? 'VO' : 'Visual')), /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        minWidth: 0,
        borderLeft: vo ? `2px solid ${FK.AMBER}` : 'none',
        paddingLeft: vo ? 12 : 0
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.SCRIPT,
        fontSize: 13.5,
        lineHeight: 1.5,
        color: vo ? FK.INK : FK.INK_MUTED,
        fontWeight: vo ? 500 : 400
      }
    }, hlScript(l.text), active && cursor)));
  })));
}
function BidInsights({
  step,
  onOpenBid
}) {
  const seen = SCRIPT_LINES.slice(0, step + 1).map(l => l.text.toLowerCase()).join(' \u2063 ');
  const detected = SCRIPT_INSIGHTS.filter(ins => seen.includes(ins.match.toLowerCase()));
  const total = SCRIPT_INSIGHTS.length;
  const flagged = detected.reduce((a, d) => a + d.impact, 0);
  const done = step >= SCRIPT_LINES.length - 1;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 22px 22px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 15,
      color: FK.INK_MUTED,
      marginBottom: 14
    }
  }, done ? 'Breakdown ready — every driver tagged to a bid line.' : 'Reading the script for cost drivers\u2026'), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      padding: '10px 12px',
      marginBottom: 8,
      background: FK.PAPER_SOFT,
      borderRadius: 3
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: FK.INK_MUTED
    }
  }, detected.length, "/", total, " drivers"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 13,
      color: FK.AMBER_800
    }
  }, FLOW.fmtMoney(flagged), " flagged")), detected.map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      padding: '12px 6px',
      borderBottom: `1px solid ${LT.line}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      gap: 10,
      marginBottom: 4
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 9,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: FK.AMBER
    }
  }, d.code), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 16,
      color: FK.INK
    }
  }, d.cat)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 12,
      color: FK.AMBER_800,
      whiteSpace: 'nowrap'
    }
  }, "+", FLOW.fmtMoney(d.impact))), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontSize: 13,
      color: FK.INK,
      marginBottom: 3
    }
  }, d.driver), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 12.5,
      color: FK.INK_FAINT
    }
  }, "\u201C", d.src, "\u201D"))), detected.length === 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 11,
      color: FK.INK_FAINT,
      padding: '20px 6px',
      textAlign: 'center'
    }
  }, "Listening\u2026"), done && /*#__PURE__*/React.createElement("button", {
    onClick: onOpenBid,
    style: {
      marginTop: 16,
      width: '100%',
      display: 'inline-flex',
      alignItems: 'center',
      justifyContent: 'center',
      gap: 8,
      cursor: 'pointer',
      background: FK.AMBER,
      color: FK.PAPER,
      border: 'none',
      borderRadius: 3,
      padding: '10px 14px',
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.12em',
      textTransform: 'uppercase'
    }
  }, "Open the bid ", /*#__PURE__*/React.createElement(FGlyph, {
    name: "arrow",
    size: 13
  })));
}

// ── scenarios (Bidding tab — the "what if" engine) ─────────────────
function ScenariosPanel() {
  const bid = FRAMEWORK.BID;
  const [on, setOn] = React.useState({});
  const factor = 1 + (bid.markupPct + bid.feePct) / 100 + bid.insurancePct / 100 * (1 + bid.markupPct / 100);
  const baseDirect = bid.sections.reduce((a, s) => a + s.lines.reduce((b, l) => b + l.qty * l.days * l.rate, 0), 0);
  const active = FRAMEWORK.BID_SCENARIOS.filter(s => on[s.id]);
  const saveDirect = active.reduce((a, s) => a + s.save, 0);
  const baseGrand = Math.round(baseDirect * factor);
  const newGrand = Math.round((baseDirect - saveDirect) * factor);
  const dBudget = newGrand - baseGrand;
  const dDays = active.reduce((a, s) => a + s.days, 0);
  const dCrew = active.reduce((a, s) => a + s.crew, 0);
  const riskNet = active.reduce((a, s) => a + s.risk, 0);
  const risk = active.length === 0 ? {
    t: '\u2014',
    c: FK.INK_FAINT
  } : riskNet < 0 ? {
    t: 'Lower',
    c: FIN_GOOD
  } : riskNet > 0 ? {
    t: 'Higher',
    c: FK.AMBER_800
  } : {
    t: 'Neutral',
    c: FK.INK_MUTED
  };
  const stat = (label, value, color) => /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      marginBottom: 6
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: 22,
      lineHeight: 1,
      color: color || FK.INK
    }
  }, value));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 22px 22px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 15,
      color: FK.INK_MUTED,
      marginBottom: 16
    }
  }, "Toggle a lever \u2014 watch the bid move."), FRAMEWORK.BID_SCENARIOS.map(s => {
    const sel = !!on[s.id];
    return /*#__PURE__*/React.createElement("div", {
      key: s.id,
      onClick: () => setOn(o => Object.assign({}, o, {
        [s.id]: !o[s.id]
      })),
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 12,
        padding: '11px 10px',
        marginLeft: -10,
        marginRight: -10,
        cursor: 'pointer',
        borderBottom: `1px solid ${LT.line}`,
        background: sel ? FK.AMBER_50 : 'transparent'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        width: 18,
        height: 18,
        flexShrink: 0,
        borderRadius: 3,
        border: `1px solid ${sel ? FK.AMBER : LT.line2}`,
        background: sel ? FK.AMBER : LT.win,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: FK.PAPER,
        fontSize: 12,
        lineHeight: 1
      }
    }, sel ? '\u2713' : ''), /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        minWidth: 0
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: FK.SANS,
        fontSize: 14,
        color: sel ? FK.AMBER_800 : FK.INK
      }
    }, s.label), /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 9,
        letterSpacing: '0.04em',
        color: FK.INK_FAINT,
        marginTop: 2
      }
    }, s.note)), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 12,
        color: sel ? FK.AMBER_800 : FK.INK_MUTED,
        whiteSpace: 'nowrap'
      }
    }, '\u2212', FLOW.fmtMoney(s.save)));
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 20,
      padding: '16px 17px',
      background: FK.PAPER_SOFT,
      borderRadius: 4
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      marginBottom: 16,
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      marginBottom: 5,
      whiteSpace: 'nowrap'
    }
  }, "Budget impact \xB7 all-in"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: 30,
      lineHeight: 1,
      letterSpacing: '-0.02em',
      color: dBudget < 0 ? FIN_GOOD : FK.INK
    }
  }, dBudget === 0 ? '$0' : finSigned(dBudget))), /*#__PURE__*/React.createElement("div", {
    style: {
      textAlign: 'right'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      color: FK.INK_FAINT,
      marginBottom: 4,
      whiteSpace: 'nowrap'
    }
  }, FLOW.fmtMoney(baseGrand), " ", '\u2192'), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: 20,
      color: FK.INK
    }
  }, FLOW.fmtMoney(newGrand)))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 14,
      borderTop: `1px solid ${LT.line}`,
      paddingTop: 14
    }
  }, stat('Schedule', dDays === 0 ? '0' : dDays + 'd', dDays < 0 ? FIN_GOOD : dDays > 0 ? FK.AMBER_800 : FK.INK), stat('Crew', dCrew === 0 ? '0' : String(dCrew), dCrew < 0 ? FIN_GOOD : FK.INK), stat('Risk', risk.t, risk.c))));
}

// ── the producer page ───────────────────────────────────────────────
// ── viewing window (program monitor) ───────────────────
function ViewingWindow({
  state
}) {
  const len = FLOW.totalLength(state);
  return /*#__PURE__*/React.createElement(Win, {
    title: "Program",
    style: {
      flex: '0 0 auto'
    },
    right: /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 10,
        color: FK.INK_FAINT
      }
    }, "16\xD79")
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 14
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: '100%',
      aspectRatio: '16 / 9',
      background: '#000',
      border: `1px solid ${LT.line2}`,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: "assets/production-thumbnail.png",
    alt: "Program",
    style: {
      position: 'absolute',
      inset: 0,
      width: '100%',
      height: '100%',
      objectFit: 'cover',
      display: 'block'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 0,
      left: 0,
      right: 0,
      height: 38,
      background: 'linear-gradient(rgba(0,0,0,0.5), transparent)',
      pointerEvents: 'none'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      bottom: 0,
      left: 0,
      right: 0,
      height: 34,
      background: 'linear-gradient(transparent, rgba(0,0,0,0.5))',
      pointerEvents: 'none'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 10,
      left: 12,
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.16em',
      color: 'rgba(255,255,255,0.7)'
    }
  }, "PROGRAM \xB7 16\xD79"), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 12,
      bottom: 10,
      fontFamily: FK.MONO,
      fontSize: 9,
      color: 'rgba(255,255,255,0.75)'
    }
  }, "01:00:00:00"), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      right: 12,
      bottom: 10,
      fontFamily: FK.MONO,
      fontSize: 9,
      color: 'rgba(255,255,255,0.75)'
    }
  }, FLOW.fmtClock(len)))));
}

// ── bottom timeline (full width, compact) ─────────────────
function PWave() {
  const bars = [];
  for (let i = 0; i < 150; i++) {
    const x = i / 150;
    bars.push(Math.max(0.08, Math.abs(Math.sin(x * 22) * 0.5 + Math.sin(x * 7 + 1) * 0.35 + Math.sin(x * 51) * 0.16)) * 100);
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: '3px 0',
      display: 'flex',
      alignItems: 'center',
      gap: 1,
      padding: '0 6px'
    }
  }, bars.map((h, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      flex: 1,
      height: h + '%',
      background: '#b3bcc6',
      minWidth: 1
    }
  })));
}

// Cost heat ramp — neutral (cheap) → rust (expensive). Stays in the warm,
// amber-led brand family rather than a literal red/green map.
const HEAT_STOPS = ['#E7E2D8', '#F2DCB4', '#ECB659', '#DB8526', '#B4471C'];
const HEAT_CSS = 'linear-gradient(90deg, #E7E2D8, #F2DCB4, #ECB659, #DB8526, #B4471C)';
function heatColor(t) {
  const i = t >= 0.8 ? 4 : t >= 0.6 ? 3 : t >= 0.4 ? 2 : t >= 0.2 ? 1 : 0;
  return HEAT_STOPS[i];
}
function finTotal(scene) {
  return scene.fin ? scene.fin.lv.reduce((a, b) => a + b, 0) : 0;
}
function dollarColor(lv) {
  return lv <= 2 ? '#B58A4C' : lv <= 4 ? '#D8922F' : lv <= 5 ? '#C56A1E' : '#B4471C';
}

// Per-shot cost-profile popup.
function FinancePopup({
  block,
  state,
  anchor
}) {
  const {
    scene,
    num
  } = block;
  const loc = FLOW.LOCS.find(l => l.id === scene.locId);
  const align = anchor < 24 ? 'left' : anchor > 76 ? 'right' : 'center';
  const tx = align === 'left' ? '-10px' : align === 'right' ? 'calc(-100% + 10px)' : '-50%';
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      bottom: 'calc(100% + 12px)',
      left: anchor + '%',
      transform: `translateX(${tx})`,
      width: 304,
      background: LT.win,
      border: `1px solid ${LT.line2}`,
      borderRadius: 6,
      boxShadow: '0 14px 38px rgba(0,0,0,0.18)',
      padding: '14px 16px 15px',
      zIndex: 60,
      pointerEvents: 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      gap: 10,
      marginBottom: 3
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.18em',
      color: FK.AMBER,
      whiteSpace: 'nowrap',
      flexShrink: 0
    }
  }, "SHOT ", num), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      color: FK.INK_FAINT,
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      minWidth: 0,
      textAlign: 'right'
    }
  }, block.sh.dur.toFixed(1).replace('.0', ''), "s \xB7 ", block.sh.desc)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: FK.INK_MUTED,
      marginBottom: 11
    }
  }, loc.kind, ". ", loc.name, " \u2014 ", FLOW.todOf(scene, state)), FLOW.FIN_CATS.map((c, i) => {
    const lv = scene.fin.lv[i];
    return /*#__PURE__*/React.createElement("div", {
      key: c,
      style: {
        display: 'flex',
        alignItems: 'flex-end',
        marginBottom: 4
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 10.5,
        color: FK.INK,
        width: 96,
        flexShrink: 0
      }
    }, c), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        borderBottom: `1px dotted ${LT.line2}`,
        margin: '0 6px 4px'
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 12,
        letterSpacing: '1.5px',
        color: dollarColor(lv),
        whiteSpace: 'nowrap'
      }
    }, '$'.repeat(lv)));
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: `1px solid ${LT.line}`,
      margin: '11px 0 9px'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      marginBottom: 7
    }
  }, "Primary cost drivers"), scene.fin.drivers.map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      gap: 8,
      marginBottom: 4,
      fontFamily: FK.SANS,
      fontSize: 12.5,
      color: FK.INK,
      lineHeight: 1.3
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: FK.AMBER,
      flexShrink: 0
    }
  }, "\u2022"), d)));
}
function ProducerTimeline({
  state,
  flashId
}) {
  const [view, setView] = React.useState('timeline');
  const [hover, setHover] = React.useState(null);
  const scenes = FLOW.activeScenes(state);
  const total = FLOW.totalLength(state);
  // cut clips (timeline view)
  let acc = 0;
  const clips = scenes.map(s => {
    const dur = s.shots.reduce((a, sh) => a + sh.dur, 0);
    const c = {
      left: acc / total * 100,
      width: dur / total * 100,
      name: FLOW.locName(s.locId).toUpperCase(),
      night: FLOW.isNight(s, state),
      id: s.id
    };
    acc += dur;
    return c;
  });
  // shot heat blocks (finance view)
  let fa = 0;
  const fblocks = FLOW.allShots(state).map((sh, i) => {
    const scene = FLOW.SCENES.find(s => s.id === sh.sceneId);
    const tot = finTotal(scene);
    const left = fa / total * 100,
      width = sh.dur / total * 100;
    fa += sh.dur;
    return {
      sh,
      scene,
      tot,
      left,
      width,
      center: left + width / 2,
      num: String(i + 1).padStart(2, '0')
    };
  });
  const tots = fblocks.map(b => b.tot);
  const mn = Math.min(...tots),
    mx = Math.max(...tots);
  const heatT = tot => mx === mn ? 0.5 : (tot - mn) / (mx - mn);
  const hb = fblocks.find(b => b.num === hover);
  const ticks = [];
  for (let t = 0; t <= total; t += 5) ticks.push(t);
  const HEAD = 110,
    ROW = 30;
  const hdr = label => /*#__PURE__*/React.createElement("div", {
    style: {
      height: ROW,
      display: 'flex',
      alignItems: 'center',
      padding: '0 12px',
      borderBottom: `1px solid ${LT.line}`,
      fontFamily: FK.MONO,
      fontSize: 10,
      color: FK.INK_MUTED,
      letterSpacing: '0.08em'
    }
  }, label);
  const tab = (id, label) => /*#__PURE__*/React.createElement("div", {
    key: id,
    onClick: () => {
      setView(id);
      setHover(null);
    },
    style: {
      padding: '5px 13px',
      cursor: 'pointer',
      fontFamily: FK.MONO,
      fontSize: 11,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      borderLeft: id === 'finance' ? `1px solid ${LT.line2}` : 'none',
      background: view === id ? FK.AMBER : LT.win,
      color: view === id ? FK.PAPER : FK.INK_MUTED
    }
  }, label);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      background: LT.win,
      borderTop: `1px solid ${LT.line2}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '7px 16px',
      borderBottom: `1px solid ${LT.line}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: `1px solid ${LT.line2}`,
      borderRadius: 3,
      overflow: 'hidden'
    }
  }, tab('timeline', 'Timeline'), tab('finance', 'Finance')), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.SANS,
      fontWeight: 300,
      fontSize: 20,
      color: FK.INK
    }
  }, FLOW.fmtClock(total))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: HEAD,
      flexShrink: 0,
      borderRight: `1px solid ${LT.line2}`
    }
  }, view === 'timeline' ? /*#__PURE__*/React.createElement(React.Fragment, null, hdr('V1 · CUT'), hdr('A1 · MIX')) : /*#__PURE__*/React.createElement(React.Fragment, null, hdr('SHOTS · COST'), hdr('HEAT'))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      position: 'relative'
    }
  }, view === 'timeline' ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: {
      height: ROW,
      position: 'relative',
      borderBottom: `1px solid ${LT.line}`
    }
  }, clips.map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    title: c.name,
    style: {
      position: 'absolute',
      top: 3,
      bottom: 3,
      left: c.left + '%',
      width: c.width + '%',
      background: flashId === c.id ? FK.AMBER_50 : c.night ? '#EFE4D2' : '#dde6ef',
      border: `1px solid ${flashId === c.id ? FK.AMBER : LT.line2}`,
      borderRadius: 2,
      display: 'flex',
      alignItems: 'center',
      padding: '0 7px',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 9,
      color: FK.INK_MUTED,
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, c.name)))), /*#__PURE__*/React.createElement("div", {
    style: {
      height: ROW,
      position: 'relative',
      borderBottom: `1px solid ${LT.line}`,
      background: FK.PAPER_SOFT
    }
  }, /*#__PURE__*/React.createElement(PWave, null)), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 0,
      bottom: 0,
      left: '34%',
      width: 1,
      background: FK.AMBER
    }
  })) : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: {
      height: ROW,
      position: 'relative',
      borderBottom: `1px solid ${LT.line}`
    }
  }, fblocks.map(b => {
    const t = heatT(b.tot),
      active = hover === b.num;
    return /*#__PURE__*/React.createElement("div", {
      key: b.num,
      onMouseEnter: () => setHover(b.num),
      onMouseLeave: () => setHover(h => h === b.num ? null : h),
      onClick: () => setHover(h => h === b.num ? null : b.num),
      style: {
        position: 'absolute',
        top: 3,
        bottom: 3,
        left: b.left + '%',
        width: b.width + '%',
        background: heatColor(t),
        border: `1px solid ${active ? FK.INK : 'rgba(0,0,0,0.14)'}`,
        borderRadius: 2,
        cursor: 'pointer',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        overflow: 'hidden',
        boxShadow: active ? '0 0 0 1px ' + FK.INK : 'none'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 8.5,
        color: t > 0.55 ? 'rgba(255,255,255,0.92)' : FK.INK_MUTED
      }
    }, b.num));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      height: ROW,
      display: 'flex',
      alignItems: 'center',
      gap: 9,
      padding: '0 12px',
      borderBottom: `1px solid ${LT.line}`,
      background: FK.PAPER_SOFT
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 8.5,
      letterSpacing: '0.14em',
      color: FK.INK_FAINT
    }
  }, "LOW"), /*#__PURE__*/React.createElement("div", {
    style: {
      width: 130,
      height: 7,
      borderRadius: 999,
      background: HEAT_CSS,
      border: `1px solid ${LT.line2}`
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 8.5,
      letterSpacing: '0.14em',
      color: FK.INK_FAINT
    }
  }, "HIGH"), /*#__PURE__*/React.createElement("span", {
    style: {
      marginLeft: 'auto',
      fontFamily: FK.SERIF,
      fontStyle: 'italic',
      fontSize: 12,
      color: FK.INK_FAINT
    }
  }, "Hover a shot for its cost profile")), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 0,
      bottom: 0,
      left: '34%',
      width: 1,
      background: 'rgba(0,0,0,0.25)'
    }
  }), hb && /*#__PURE__*/React.createElement(FinancePopup, {
    block: hb,
    state: state,
    anchor: hb.center
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: HEAD,
      flexShrink: 0,
      borderRight: `1px solid ${LT.line2}`
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      position: 'relative',
      height: 22
    }
  }, ticks.map(t => /*#__PURE__*/React.createElement("span", {
    key: t,
    style: {
      position: 'absolute',
      left: t / total * 100 + '%',
      top: 5,
      fontFamily: FK.MONO,
      fontSize: 9,
      color: FK.INK_FAINT,
      transform: 'translateX(2px)'
    }
  }, `01:00:${String(t).padStart(2, '0')}`)))));
}

// ── Script intake cockpit — the production-lane landing ──────────────
// Program monitor + message-to-director (left) · live script reader (center) ·
// auto-tagged bidding insights (right) · cut/finance timeline (bottom).
// This is the unified lane's first view; nav lives in ProductionLane.
function ScriptCockpit({
  state,
  chat,
  flashId,
  onSend,
  goTab
}) {
  const [scriptStep, setScriptStep] = React.useState(0);
  const scriptDone = scriptStep >= SCRIPT_LINES.length - 1;
  React.useEffect(() => {
    if (scriptDone) return;
    const t = setTimeout(() => setScriptStep(s => Math.min(s + 1, SCRIPT_LINES.length - 1)), 430);
    return () => clearTimeout(t);
  }, [scriptStep, scriptDone]);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex',
      flexDirection: 'column',
      background: LT.canvas
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex',
      gap: 14,
      padding: 16,
      boxSizing: 'border-box'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 430px',
      display: 'flex',
      flexDirection: 'column',
      gap: 14,
      minHeight: 0
    }
  }, /*#__PURE__*/React.createElement(ViewingWindow, {
    state: state
  }), /*#__PURE__*/React.createElement(Win, {
    title: "Message \xB7 Director",
    style: {
      flex: 1
    }
  }, /*#__PURE__*/React.createElement(ChatDirector, {
    chat: chat,
    onSend: onSend
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      background: LT.win,
      border: `1px solid ${LT.line}`,
      borderRadius: 4,
      display: 'flex',
      flexDirection: 'column',
      minHeight: 0,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement(ScriptProcessing, {
    step: scriptStep
  })), /*#__PURE__*/React.createElement(Win, {
    title: "Bidding insights",
    style: {
      flex: '0 0 560px'
    },
    right: /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: FK.MONO,
        fontSize: 9,
        letterSpacing: '0.16em',
        textTransform: 'uppercase',
        color: FK.INK_FAINT,
        whiteSpace: 'nowrap'
      }
    }, "Auto-tagged")
  }, /*#__PURE__*/React.createElement(BidInsights, {
    step: scriptStep,
    onOpenBid: () => goTab('bid')
  }))), /*#__PURE__*/React.createElement(ProducerTimeline, {
    state: state,
    flashId: flashId
  }));
}
Object.assign(window, {
  ScriptCockpit,
  BiddingView
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/producer-dash.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/production-data.js
try { (() => {
/* ============================================================
   TENET — Production Portal data model.
   ------------------------------------------------------------
   The producer's operational brain for the one project:
   the :30 brand film "A Brief History of Selling Ideas".
   Reconciles to FRAMEWORK: budget from FIN_CENTERS, shots from
   FLOW.SCENES (fin.lv complexity), scenarios from BID_SCENARIOS.
   Every line answers: what costs money, why, and what if I change it.
   ============================================================ */
window.PROD = function () {
  var F = window.FLOW,
    FW = window.FRAMEWORK;
  var S0 = F.DEFAULT_STATE;
  var T = FW.finTotals(S0);
  function money(n) {
    return '$' + Math.round(Math.abs(n)).toLocaleString('en-US');
  }
  function signed(n) {
    return (n < 0 ? '\u2212' : '+') + money(n);
  }
  var project = {
    title: F.TITLE,
    logline: F.LOGLINE,
    format: ':30 Brand Film',
    agency: 'H&W',
    code: '#17433',
    director: 'D. Parker',
    today: 'Jun 5, 2026'
  };

  // ── Dashboard ───────────────────────────────────────────────
  var dashboard = {
    budget: {
      approved: T.approved,
      actual: T.actual,
      forecast: T.forecast,
      variance: T.variance,
      contingency: T.contingency,
      reserve: T.reserve,
      directBudget: T.directBudget
    },
    health: [{
      id: 'budget',
      label: 'Budget health',
      score: 84,
      state: 'Healthy',
      note: 'Forecast ' + money(T.forecast) + ' under ' + money(T.approved) + ' approved.'
    }, {
      id: 'schedule',
      label: 'Schedule health',
      score: 88,
      state: 'On Track',
      note: 'Shoot wrapped on schedule; post on plan.'
    }, {
      id: 'resource',
      label: 'Resource health',
      score: 72,
      state: 'Tight',
      note: 'Crew released; post houses at capacity.'
    }, {
      id: 'risk',
      label: 'Risk score',
      score: 68,
      state: 'Watch',
      note: 'Night overtime and the ending beat carry exposure.'
    }],
    alerts: [{
      kind: 'OT risk',
      sev: 'high',
      text: 'Night exterior is loading ' + money(FW.FIN_NIGHT_OT) + ' of crew overtime',
      tab: 'scenarios'
    }, {
      kind: 'Location risk',
      sev: 'med',
      text: 'Downtown closure ran two hours long \u2014 ' + money(4000) + ' over',
      tab: 'budget'
    }, {
      kind: 'Permit',
      sev: 'low',
      text: 'Beach permit window is tide-dependent',
      tab: 'schedule'
    }, {
      kind: 'Over budget',
      sev: 'ok',
      text: 'No category is forecast over its approved line',
      tab: 'budget'
    }, {
      kind: 'Vendor',
      sev: 'ok',
      text: 'All vendor POs current',
      tab: 'resources'
    }]
  };

  // ── Budget — level 1 + expandable level 2 ───────────────────
  var budgetCats = FW.FIN_CENTERS.map(function (c) {
    return {
      id: c.id,
      name: c.name,
      budget: c.budget,
      actual: c.actual,
      forecast: FW.finForecast(c, S0),
      variance: FW.finVariance(c, S0),
      why: c.why,
      drivers: FW.finDrivers(c, S0)
    };
  });
  // Contingency as its own line.
  var contingency = {
    id: 'cont',
    name: 'Contingency',
    budget: T.reserve,
    actual: T.drawn,
    forecast: T.drawn,
    variance: T.drawn - T.reserve,
    why: 'A night exterior and a location overage are drawing the reserve.',
    drivers: FW.finContingencyDrivers(S0)
  };

  // ── Shots — scenes as setups, fin.lv = complexity factors ───
  var FACTORS = F.FIN_CATS; // 10, aligns to scene.fin.lv
  var maxCost = Math.max.apply(null, F.SCENES.map(function (s) {
    return F.costOf(s, S0);
  }));
  var shots = F.SCENES.map(function (s) {
    var lv = s.fin.lv;
    var complexity = Math.round(lv.reduce(function (a, b) {
      return a + b;
    }, 0) / (FACTORS.length * 7) * 100);
    var cost = F.costOf(s, S0);
    var night = F.isNight(s, S0);
    return {
      id: s.id,
      n: s.n,
      loc: F.locName(s.locId),
      tod: F.todOf(s, S0),
      desc: s.action[0].text,
      complexity: complexity,
      cost: cost,
      costScore: Math.round(cost / maxCost * 100),
      status: 'Wrapped',
      night: night,
      factors: FACTORS.map(function (f, i) {
        return {
          name: f,
          lv: lv[i]
        };
      }),
      drivers: s.fin.drivers,
      shotCount: s.shots.length,
      scheduleRisk: lv[9]
    };
  });
  var topDrivers = [{
    label: 'Night exterior',
    amt: 18000
  }, {
    label: '75 extras \u2014 downtown',
    amt: 12000
  }, {
    label: 'Technocrane',
    amt: 16000
  }, {
    label: 'Picture vehicles',
    amt: 10000
  }, {
    label: 'Downtown closure',
    amt: 18000
  }];

  // ── Schedule ────────────────────────────────────────────────
  var schedule = {
    shootDays: 3,
    daysComplete: 3,
    daysRemaining: 0,
    forecastWrap: 'Apr 18',
    postPhase: 'Editorial',
    delivery: 'Jun 27',
    days: [{
      id: 'd1',
      label: 'Day 1',
      date: 'Apr 14',
      locs: ['Workshop', 'Studio'],
      pages: 2.4,
      shots: 6,
      crew: 17,
      hours: 11,
      risks: ['Complex setups'],
      state: 'done'
    }, {
      id: 'd2',
      label: 'Day 2',
      date: 'Apr 16',
      locs: ['Downtown', 'Apartment'],
      pages: 3.1,
      shots: 6,
      crew: 19,
      hours: 13,
      risks: ['Company moves', 'Night overtime'],
      state: 'done'
    }, {
      id: 'd3',
      label: 'Day 3',
      date: 'Apr 18',
      locs: ['Rooftop', 'Beach'],
      pages: 2.0,
      shots: 5,
      crew: 16,
      hours: 12,
      risks: ['Weather', 'Company moves'],
      state: 'done'
    }],
    riskFactors: [{
      label: 'Company moves',
      count: 3,
      note: 'Three multi-location days'
    }, {
      label: 'Weather',
      count: 1,
      note: 'Beach golden-hour window'
    }, {
      label: 'Talent',
      count: 1,
      note: 'Two principals on the night'
    }, {
      label: 'Complex setups',
      count: 2,
      note: 'Technocrane + macro rig'
    }, {
      label: 'Client reviews',
      count: 1,
      note: 'Picture lock gate ahead'
    }]
  };

  // ── Resources ───────────────────────────────────────────────
  var resources = [{
    group: 'People',
    items: [{
      name: 'Principal talent',
      detail: 'Mara \u00b7 Theo \u00b7 The Maker',
      count: 3,
      state: 'wrapped'
    }, {
      name: 'Extras',
      detail: 'Downtown background',
      count: 75,
      state: 'wrapped'
    }, {
      name: 'Crew',
      detail: 'Core + department heads',
      count: 19,
      state: 'wrapped'
    }, {
      name: 'Vendors',
      detail: 'Catering, transport, rentals',
      count: 6,
      state: 'active'
    }]
  }, {
    group: 'Locations',
    items: [{
      name: 'Stages',
      detail: 'Studio (product table)',
      count: 1,
      state: 'wrapped'
    }, {
      name: 'Practicals',
      detail: 'Rooftop \u00b7 Workshop \u00b7 Apartment \u00b7 Downtown',
      count: 4,
      state: 'wrapped'
    }, {
      name: 'Remote',
      detail: 'Beach \u2014 destination coast',
      count: 1,
      state: 'wrapped'
    }]
  }, {
    group: 'Equipment',
    items: [{
      name: 'Camera',
      detail: 'Package + lenses',
      count: 1,
      state: 'wrapped'
    }, {
      name: 'Grip & lighting',
      detail: 'Truck package',
      count: 1,
      state: 'wrapped'
    }, {
      name: 'Vehicles',
      detail: 'Picture cars',
      count: 3,
      state: 'wrapped'
    }, {
      name: 'Specialty',
      detail: 'Technocrane \u00b7 macro rig',
      count: 2,
      state: 'wrapped'
    }]
  }, {
    group: 'Post',
    items: [{
      name: 'Editorial',
      detail: 'Current cut for review',
      count: 1,
      state: 'active'
    }, {
      name: 'Color',
      detail: 'Scheduled Jun 16',
      count: 1,
      state: 'pending'
    }, {
      name: 'VFX',
      detail: 'Skyline cleanup',
      count: 1,
      state: 'active'
    }, {
      name: 'Sound',
      detail: 'Mix + music',
      count: 1,
      state: 'pending'
    }, {
      name: 'Delivery',
      detail: 'Masters + versions',
      count: 1,
      state: 'pending'
    }]
  }];

  // ── Risks ───────────────────────────────────────────────────
  var risks = [{
    id: 'pr1',
    type: 'Budget',
    title: 'Night exterior overtime',
    prob: 'Medium',
    exposure: money(18000),
    cost: 18000,
    days: 0,
    mitigation: 'Flip the apartment scene to day to clear the premium.'
  }, {
    id: 'pr2',
    type: 'Weather',
    title: 'Beach golden-hour window',
    prob: 'Medium',
    exposure: 'One half-day',
    cost: 18000,
    days: 1,
    mitigation: 'Hold a contingency day; watch the tide chart.'
  }, {
    id: 'pr3',
    type: 'Client',
    title: 'Picture lock waits on review',
    prob: 'Medium',
    exposure: '3-day slip',
    cost: 0,
    days: 3,
    mitigation: 'Drive the client to approve or note by Jun 9.'
  }, {
    id: 'pr4',
    type: 'Technical',
    title: 'VFX cleanup heavier than bid',
    prob: 'Low',
    exposure: money(8000),
    cost: 8000,
    days: 1,
    mitigation: 'Lock the skyline plate count at picture lock.'
  }, {
    id: 'pr5',
    type: 'Legal',
    title: 'Talent usage renews in 24 months',
    prob: 'Low',
    exposure: money(16000),
    cost: 16000,
    days: 0,
    mitigation: 'Renewal budgeted in licensing; flag at month 20.'
  }];

  // ── SCENARIOS — the what-if engine (toggle + stack) ─────────
  // Negative budget = savings. crew = resource delta. risk delta −2..+2.
  var scenarios = [{
    id: 'extras',
    label: 'Reduce extras',
    note: '75 \u2192 40 background',
    budget: -12000,
    days: 0,
    crew: 0,
    risk: -1,
    res: ['Extras']
  }, {
    id: 'crane',
    label: 'Remove the technocrane',
    note: 'Crane \u2192 dolly + jib',
    budget: -16000,
    days: 0,
    crew: -2,
    risk: -1,
    res: ['Specialty', 'Grip']
  }, {
    id: 'stage',
    label: 'Move the night to a stage',
    note: 'Drops the night exterior',
    budget: -22000,
    days: -1,
    crew: -8,
    risk: -2,
    res: ['Locations', 'Crew']
  }, {
    id: 'day',
    label: 'Drop a shoot day',
    note: '3 \u2192 2 shoot days',
    budget: -28000,
    days: -1,
    crew: -12,
    risk: 2,
    res: ['Crew', 'Schedule']
  }, {
    id: 'vfx',
    label: 'Convert practical to VFX',
    note: 'Plate + comp',
    budget: -9000,
    days: 0,
    crew: -4,
    risk: 1,
    res: ['VFX', 'Crew']
  }, {
    id: 'deliv',
    label: 'Reduce deliverables',
    note: 'Drop the :15 cutdown',
    budget: -6000,
    days: 0,
    crew: 0,
    risk: 0,
    res: ['Editorial']
  }];
  return {
    F: F,
    FW: FW,
    S0: S0,
    T: T,
    money: money,
    signed: signed,
    project: project,
    dashboard: dashboard,
    budgetCats: budgetCats,
    contingency: contingency,
    FACTORS: FACTORS,
    shots: shots,
    topDrivers: topDrivers,
    schedule: schedule,
    resources: resources,
    risks: risks,
    scenarios: scenarios,
    bidExpected: FW.BID.expected,
    NAV: [{
      id: 'script',
      label: 'Script',
      phase: 'bid'
    }, {
      id: 'dashboard',
      label: 'Dashboard',
      phase: 'budget'
    }, {
      id: 'budget',
      label: 'Budget',
      phase: 'budget'
    }, {
      id: 'bid',
      label: 'Bid',
      phase: 'bid'
    }, {
      id: 'schedule',
      label: 'Schedule',
      phase: 'budget'
    }, {
      id: 'scenarios',
      label: 'Changes',
      phase: 'budget'
    }]
  };
}();
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/production-data.js", error: String((e && e.message) || e) }); }

// flow_ctx/production-hero.jsx
try { (() => {
/* global React */
// TENET Production Portal — hero tabs: Dashboard, Budget (expandable), Scenarios.
const {
  FK: DFK,
  FGlyph: DG,
  PROD: DC
} = window;
const {
  CPanel: DP,
  CPStat: DStat,
  CPBar: DBar,
  CPChip: DChip,
  CPHead: DHead,
  useCountUp: DCount,
  cpKickerFaint: DKickF,
  RISK_COLOR: DRISK,
  SEV_COLOR: DSEV
} = window;
const DUse = React.useState;
const RES_BUDGET = {
  wrapped: {
    c: '#9A9A9A',
    t: 'Wrapped'
  },
  active: {
    c: '#BA7517',
    t: 'Active'
  },
  pending: {
    c: '#9A9A9A',
    t: 'Pending'
  }
};

/* ══════════════════════════════════════════════════════════
   DASHBOARD — producer mission control
   ══════════════════════════════════════════════════════════ */
function PDashboard({
  ctx
}) {
  const d = DC.dashboard;
  const b = d.budget;
  const projForecast = b.forecast + ctx.savings; // savings negative
  const hasScenarios = ctx.active.length > 0;
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(DHead, {
    kicker: "Dashboard",
    lede: "The operational read \u2014 money, schedule, resources, and what\u2019s flashing red."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(5, 1fr)',
      gap: 14,
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement(DStat, {
    label: "Approved budget",
    value: DC.money(b.approved)
  }), /*#__PURE__*/React.createElement(DStat, {
    label: "Actual spend",
    value: DC.money(b.actual),
    sub: "Committed to date"
  }), /*#__PURE__*/React.createElement(DStat, {
    label: "Forecast final",
    value: DC.money(projForecast),
    tone: hasScenarios ? '#1F8A5B' : DFK.INK,
    sub: hasScenarios ? DC.signed(ctx.savings) + ' w/ scenarios' : 'On plan'
  }), /*#__PURE__*/React.createElement(DStat, {
    label: "Variance",
    value: (b.variance < 0 ? '\u2212' : '+') + DC.money(b.variance),
    tone: b.variance > 0 ? '#B23A2E' : '#1F8A5B',
    sub: b.variance < 0 ? 'Under approved' : 'Over approved'
  }), /*#__PURE__*/React.createElement(DStat, {
    label: "Contingency",
    value: DC.money(b.contingency),
    sub: 'of ' + DC.money(b.reserve) + ' reserve'
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1.4fr 1fr',
      gap: 16,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement(DP, {
    label: "Project health",
    title: "Status across the board"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 18
    }
  }, d.health.map(h => /*#__PURE__*/React.createElement("div", {
    key: h.id
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline',
      marginBottom: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.SANS,
      fontSize: 14,
      color: DFK.INK
    }
  }, h.label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 10,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: h.score >= 80 ? '#1F8A5B' : h.score >= 70 ? DFK.AMBER : '#B23A2E'
    }
  }, h.state)), /*#__PURE__*/React.createElement(DBar, {
    pct: h.score,
    color: h.score >= 80 ? '#1F8A5B' : h.score >= 70 ? DFK.AMBER : '#B23A2E'
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 8,
      fontFamily: DFK.SANS,
      fontSize: 12.5,
      color: DFK.INK_MUTED,
      lineHeight: 1.4
    }
  }, h.note))))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(DP, {
    label: "Alerts",
    title: null,
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, d.alerts.map((a, i) => /*#__PURE__*/React.createElement("button", {
    key: i,
    onClick: () => ctx.goTab(a.tab),
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      gap: 12,
      textAlign: 'left',
      background: 'none',
      border: 0,
      borderTop: i ? `1px solid ${DFK.BORDER}` : 'none',
      padding: '13px 0',
      cursor: 'pointer',
      width: '100%'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 7,
      height: 7,
      borderRadius: 999,
      background: DSEV[a.sev],
      marginTop: 5,
      flexShrink: 0
    }
  }), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: DSEV[a.sev],
      marginBottom: 3
    }
  }, a.kind), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontSize: 13.5,
      color: DFK.INK,
      lineHeight: 1.4
    }
  }, a.text)))))), /*#__PURE__*/React.createElement("button", {
    onClick: () => ctx.goTab('scenarios'),
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      cursor: 'pointer',
      padding: '18px 22px',
      borderRadius: 4,
      border: 'none',
      background: DFK.INK,
      color: DFK.PAPER
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      textAlign: 'left'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: DFK.AMBER_200
    }
  }, "Changes"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontWeight: 300,
      fontSize: 18,
      marginTop: 6
    }
  }, "Model a change")), /*#__PURE__*/React.createElement(DG, {
    name: "arrow",
    size: 18,
    style: {
      color: DFK.PAPER
    }
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 28
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, DKickF, {
      marginBottom: 14
    })
  }, "Risks \xB7 priced exposure"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 16
    }
  }, DC.risks.map(r => /*#__PURE__*/React.createElement("div", {
    key: r.id,
    style: {
      border: `1px solid ${DFK.BORDER}`,
      borderRadius: 6,
      background: DFK.PAPER,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 22px 16px',
      borderBottom: `1px solid ${DFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: DFK.AMBER,
      whiteSpace: 'nowrap'
    }
  }, r.type, " risk"), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 7
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: DFK.INK_FAINT
    }
  }, "Prob"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.SANS,
      fontSize: 13,
      color: DRISK[r.prob]
    }
  }, r.prob))), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontWeight: 300,
      fontSize: 19,
      lineHeight: 1.2,
      color: DFK.INK
    }
  }, r.title)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr 1fr',
      gap: 1,
      background: DFK.BORDER
    }
  }, [['Exposure', r.exposure, DFK.INK], ['Cost impact', r.cost ? '+' + DC.money(r.cost) : '\u2014', r.cost ? '#B23A2E' : DFK.INK_FAINT], ['Schedule', r.days ? '+' + r.days + 'd' : '\u2014', r.days ? DFK.AMBER : DFK.INK_FAINT]].map(([k, v, c], i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      background: DFK.PAPER,
      padding: '14px 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: DFK.INK_FAINT,
      marginBottom: 6
    }
  }, k), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontSize: 15,
      color: c,
      fontVariantNumeric: 'tabular-nums'
    }
  }, v)))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 22px',
      borderTop: `1px solid ${DFK.BORDER}`,
      background: DFK.PAPER_SOFT
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, DKickF, {
      marginBottom: 8
    })
  }, "Mitigation"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontSize: 13.5,
      color: DFK.INK,
      lineHeight: 1.5
    }
  }, r.mitigation)))))));
}

/* ══════════════════════════════════════════════════════════
   BUDGET — level 1 + expandable level 2 (drivers)
   ══════════════════════════════════════════════════════════ */
function PBudget({
  ctx
}) {
  const [open, setOpen] = DUse('crew');
  const [shotsOpen, setShotsOpen] = DUse(false);
  const [resOpen, setResOpen] = DUse(false);
  const cats = DC.budgetCats.concat([DC.contingency]);
  const b = DC.dashboard.budget;
  const maxDriver = Math.max.apply(null, DC.topDrivers.map(d => d.amt));
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(DHead, {
    kicker: "Budget",
    lede: "Open any line to see the drivers \u2014 the system already knows why."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(3, 1fr)',
      gap: 14,
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement(DStat, {
    label: "Approved budget",
    value: DC.money(b.approved)
  }), /*#__PURE__*/React.createElement(DStat, {
    label: "Forecast final",
    value: DC.money(b.forecast),
    sub: "On plan"
  }), /*#__PURE__*/React.createElement(DStat, {
    label: "Variance",
    value: (b.variance < 0 ? '\u2212' : '+') + DC.money(b.variance),
    tone: b.variance > 0 ? '#B23A2E' : '#1F8A5B',
    sub: b.variance < 0 ? 'Under approved' : 'Over approved'
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${DFK.BORDER}`,
      borderRadius: 6,
      background: DFK.PAPER,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '2fr 1fr 1fr 1fr 1fr 28px',
      gap: 16,
      padding: '14px 22px',
      borderBottom: `1px solid ${DFK.BORDER}`,
      alignItems: 'center'
    }
  }, ['Category', 'Budget', 'Actual', 'Forecast', 'Variance', ''].map((h, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      fontFamily: DFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: DFK.INK_FAINT,
      textAlign: i === 0 ? 'left' : i === 5 ? 'center' : 'right'
    }
  }, h))), cats.map((c, idx) => {
    const isOpen = open === c.id;
    const over = c.variance > 0;
    const isCont = c.id === 'cont';
    return /*#__PURE__*/React.createElement("div", {
      key: c.id,
      style: {
        borderTop: idx ? `1px solid ${DFK.BORDER}` : 'none',
        background: isOpen ? DFK.PAPER_SOFT : DFK.PAPER
      }
    }, /*#__PURE__*/React.createElement("button", {
      onClick: () => setOpen(isOpen ? null : c.id),
      style: {
        width: '100%',
        display: 'grid',
        gridTemplateColumns: '2fr 1fr 1fr 1fr 1fr 28px',
        gap: 16,
        padding: '15px 22px',
        alignItems: 'center',
        background: 'none',
        border: 0,
        cursor: 'pointer',
        textAlign: 'left'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: DFK.SANS,
        fontSize: 15,
        color: DFK.INK,
        display: 'flex',
        alignItems: 'center',
        gap: 10
      }
    }, isCont && /*#__PURE__*/React.createElement("span", {
      style: {
        width: 6,
        height: 6,
        borderRadius: 999,
        background: DFK.AMBER
      }
    }), c.name), /*#__PURE__*/React.createElement("span", {
      style: numCell(DFK.INK_MUTED)
    }, DC.money(c.budget)), /*#__PURE__*/React.createElement("span", {
      style: numCell(DFK.INK_MUTED)
    }, DC.money(c.actual)), /*#__PURE__*/React.createElement("span", {
      style: numCell(DFK.INK)
    }, DC.money(c.forecast)), /*#__PURE__*/React.createElement("span", {
      style: numCell(over ? '#B23A2E' : '#1F8A5B')
    }, c.variance === 0 ? '\u2014' : (over ? '+' : '\u2212') + DC.money(c.variance)), /*#__PURE__*/React.createElement("span", {
      style: {
        textAlign: 'center',
        color: DFK.INK_FAINT,
        transition: 'transform 200ms ease',
        transform: isOpen ? 'rotate(90deg)' : 'none'
      }
    }, /*#__PURE__*/React.createElement(DG, {
      name: "arrow",
      size: 14
    }))), isOpen && /*#__PURE__*/React.createElement("div", {
      style: {
        padding: '4px 22px 22px'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: DFK.SERIF,
        fontStyle: 'italic',
        fontSize: 14,
        color: DFK.INK_MUTED,
        marginBottom: 16,
        maxWidth: 620
      }
    }, c.why), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'grid',
        gridTemplateColumns: '1fr 1fr',
        gap: '10px 32px'
      }
    }, c.drivers.map((dr, i) => {
      const dmax = Math.max.apply(null, c.drivers.map(x => x.amt));
      return /*#__PURE__*/React.createElement("div", {
        key: i
      }, /*#__PURE__*/React.createElement("div", {
        style: {
          display: 'flex',
          justifyContent: 'space-between',
          alignItems: 'baseline',
          marginBottom: 6
        }
      }, /*#__PURE__*/React.createElement("span", {
        style: {
          fontFamily: DFK.SANS,
          fontSize: 13,
          color: dr.night ? DFK.AMBER_800 : DFK.INK
        }
      }, dr.label, dr.night && ' \u00b7 night'), /*#__PURE__*/React.createElement("span", {
        style: {
          fontFamily: DFK.MONO,
          fontSize: 12,
          color: DFK.INK,
          fontVariantNumeric: 'tabular-nums'
        }
      }, DC.money(dr.amt))), /*#__PURE__*/React.createElement(DBar, {
        pct: dr.amt / dmax * 100,
        color: dr.night ? DFK.AMBER : DFK.INK_FAINT,
        h: 3
      }));
    }))));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 16,
      border: `1px solid ${DFK.BORDER}`,
      borderRadius: 6,
      background: DFK.PAPER,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: () => setShotsOpen(o => !o),
    style: {
      width: '100%',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '16px 22px',
      background: shotsOpen ? DFK.PAPER_SOFT : DFK.PAPER,
      border: 0,
      cursor: 'pointer',
      textAlign: 'left'
    }
  }, /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      fontFamily: DFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: DFK.AMBER
    }
  }, "Shot breakdown"), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      fontFamily: DFK.SANS,
      fontWeight: 300,
      fontSize: 18,
      color: DFK.INK,
      marginTop: 4
    }
  }, "Cost and complexity, shot by shot")), /*#__PURE__*/React.createElement("span", {
    style: {
      color: DFK.INK_FAINT,
      transition: 'transform 200ms ease',
      transform: shotsOpen ? 'rotate(90deg)' : 'none'
    }
  }, /*#__PURE__*/React.createElement(DG, {
    name: "arrow",
    size: 16
  }))), shotsOpen && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '0 22px 22px',
      borderTop: `1px solid ${DFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(5, 1fr)',
      gap: 18,
      padding: '18px 0'
    }
  }, DC.topDrivers.map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: i
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 13,
      color: DFK.INK,
      fontVariantNumeric: 'tabular-nums'
    }
  }, DC.money(d.amt)), /*#__PURE__*/React.createElement("div", {
    style: {
      margin: '7px 0'
    }
  }, /*#__PURE__*/React.createElement(DBar, {
    pct: d.amt / maxDriver * 100,
    color: DFK.AMBER_200
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontSize: 12,
      color: DFK.INK_MUTED,
      lineHeight: 1.3
    }
  }, d.label)))), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${DFK.BORDER}`,
      borderRadius: 6,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '40px 1fr 120px 110px',
      gap: 12,
      padding: '12px 18px',
      borderBottom: `1px solid ${DFK.BORDER}`
    }
  }, ['#', 'Setup', 'Complexity', 'Est. cost'].map((h, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      fontFamily: DFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: DFK.INK_FAINT,
      textAlign: i >= 2 ? 'right' : 'left'
    }
  }, h))), DC.shots.map((sh, i) => /*#__PURE__*/React.createElement("div", {
    key: sh.id,
    style: {
      display: 'grid',
      gridTemplateColumns: '40px 1fr 120px 110px',
      gap: 12,
      padding: '13px 18px',
      alignItems: 'center',
      borderTop: i ? `1px solid ${DFK.BORDER}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 12,
      color: DFK.INK_FAINT
    }
  }, String(sh.n).padStart(2, '0')), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.SANS,
      fontSize: 14,
      color: DFK.INK,
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, sh.loc, sh.night && /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 8.5,
      letterSpacing: '0.1em',
      color: DFK.AMBER,
      border: `1px solid ${DFK.AMBER}`,
      borderRadius: 2,
      padding: '1px 4px'
    }
  }, "NIGHT")), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      justifyContent: 'flex-end'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 50
    }
  }, /*#__PURE__*/React.createElement(DBar, {
    pct: sh.complexity,
    color: sh.complexity > 55 ? '#B23A2E' : DFK.INK,
    h: 4
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 11,
      color: DFK.INK,
      width: 22,
      textAlign: 'right'
    }
  }, sh.complexity)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 12,
      color: DFK.INK,
      textAlign: 'right',
      fontVariantNumeric: 'tabular-nums'
    }
  }, DC.money(sh.cost))))))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 16,
      border: `1px solid ${DFK.BORDER}`,
      borderRadius: 6,
      background: DFK.PAPER,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: () => setResOpen(o => !o),
    style: {
      width: '100%',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '16px 22px',
      background: resOpen ? DFK.PAPER_SOFT : DFK.PAPER,
      border: 0,
      cursor: 'pointer',
      textAlign: 'left'
    }
  }, /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      fontFamily: DFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: DFK.AMBER
    }
  }, "Resources"), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'block',
      fontFamily: DFK.SANS,
      fontWeight: 300,
      fontSize: 18,
      color: DFK.INK,
      marginTop: 4
    }
  }, "People, locations, gear, and post")), /*#__PURE__*/React.createElement("span", {
    style: {
      color: DFK.INK_FAINT,
      transition: 'transform 200ms ease',
      transform: resOpen ? 'rotate(90deg)' : 'none'
    }
  }, /*#__PURE__*/React.createElement(DG, {
    name: "arrow",
    size: 16
  }))), resOpen && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 22px 22px',
      borderTop: `1px solid ${DFK.BORDER}`,
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 16
    }
  }, DC.resources.map(g => /*#__PURE__*/React.createElement("div", {
    key: g.group,
    style: {
      border: `1px solid ${DFK.BORDER}`,
      borderRadius: 6,
      background: DFK.PAPER,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '14px 18px 0',
      fontFamily: DFK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: DFK.AMBER
    }
  }, g.group), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '6px 18px 14px'
    }
  }, g.items.map((it, i) => {
    const st = RES_BUDGET[it.state] || RES_BUDGET.pending;
    return /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        gap: 12,
        padding: '12px 0',
        borderTop: i ? `1px solid ${DFK.BORDER}` : 'none'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        minWidth: 0
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: DFK.SANS,
        fontSize: 14,
        color: DFK.INK
      }
    }, it.name), /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: DFK.SANS,
        fontSize: 12.5,
        color: DFK.INK_MUTED,
        marginTop: 1
      }
    }, it.detail)), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 12,
        flexShrink: 0
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: DFK.SANS,
        fontWeight: 300,
        fontSize: 20,
        color: DFK.INK
      }
    }, it.count), /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'inline-flex',
        alignItems: 'center',
        gap: 6,
        fontFamily: DFK.MONO,
        fontSize: 9,
        letterSpacing: '0.12em',
        textTransform: 'uppercase',
        color: st.c
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        width: 5,
        height: 5,
        borderRadius: 999,
        background: st.c
      }
    }), st.t)));
  })))))));
}
function numCell(color) {
  return {
    fontFamily: DFK.MONO,
    fontSize: 13,
    color: color,
    textAlign: 'right',
    fontVariantNumeric: 'tabular-nums'
  };
}

/* ══════════════════════════════════════════════════════════
   SCENARIOS — the producer superpower (toggle + stack)
   ══════════════════════════════════════════════════════════ */
function PScenarios({
  ctx
}) {
  const approved = DC.dashboard.budget.approved;
  const projected = DC.dashboard.budget.forecast + ctx.savings;
  const saved = Math.abs(ctx.savings);
  const animSaved = saved; // render the live value directly (count-up didn't track target)

  function toggle(id) {
    ctx.setActive(prev => prev.indexOf(id) >= 0 ? prev.filter(x => x !== id) : prev.concat([id]));
  }
  function clearAll() {
    ctx.setActive([]);
  }
  const activeRes = {};
  DC.scenarios.filter(s => ctx.active.indexOf(s.id) >= 0).forEach(s => s.res.forEach(r => {
    activeRes[r] = true;
  }));
  const riskWord = ctx.riskDelta < 0 ? 'Lower' : ctx.riskDelta > 0 ? 'Higher' : 'Neutral';
  const riskColor = ctx.riskDelta < 0 ? '#1F8A5B' : ctx.riskDelta > 0 ? '#B23A2E' : DFK.INK_MUTED;
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1.3fr 1fr 1fr 1fr',
      gap: 16,
      marginBottom: 28
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${saved ? '#1F8A5B' : DFK.BORDER}`,
      background: saved ? 'rgba(31,138,91,0.06)' : DFK.PAPER,
      borderRadius: 4,
      padding: '16px 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: DKickF
  }, "Projected / approved"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontWeight: 300,
      fontSize: 24,
      marginTop: 8,
      color: DFK.INK,
      fontVariantNumeric: 'tabular-nums'
    }
  }, DC.money(projected), " ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: DFK.INK_FAINT,
      fontSize: 16
    }
  }, "/ ", DC.money(approved))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement(DBar, {
    pct: projected / approved * 100,
    color: saved ? '#1F8A5B' : DFK.AMBER
  }))), /*#__PURE__*/React.createElement(StripStat, {
    label: "Total saved",
    value: DC.money(animSaved),
    tone: saved ? '#1F8A5B' : DFK.INK,
    sub: ctx.active.length + ' active'
  }), /*#__PURE__*/React.createElement(StripStat, {
    label: "Schedule",
    value: ctx.daysDelta ? ctx.daysDelta + ' days' : 'No change',
    tone: ctx.daysDelta < 0 ? '#1F8A5B' : DFK.INK,
    sub: ctx.daysDelta < 0 ? 'Shorter' : 'Same'
  }), /*#__PURE__*/React.createElement(StripStat, {
    label: "Risk",
    value: riskWord,
    tone: riskColor,
    sub: ctx.crewDelta ? ctx.crewDelta + ' crew' : 'No crew change'
  })), /*#__PURE__*/React.createElement(DHead, {
    kicker: "Changes",
    lede: "Stack changes and watch the savings, the schedule, and the risk move together. Nothing is committed."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 360px',
      gap: 24,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 10
    }
  }, DC.scenarios.map(s => {
    const on = ctx.active.indexOf(s.id) >= 0;
    return /*#__PURE__*/React.createElement("button", {
      key: s.id,
      onClick: () => toggle(s.id),
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 16,
        textAlign: 'left',
        cursor: 'pointer',
        padding: '16px 18px',
        borderRadius: 4,
        border: `1px solid ${on ? '#1F8A5B' : DFK.BORDER}`,
        background: on ? 'rgba(31,138,91,0.05)' : DFK.PAPER,
        transition: 'all 160ms ease'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        width: 22,
        height: 22,
        borderRadius: 5,
        flexShrink: 0,
        border: `1.5px solid ${on ? '#1F8A5B' : DFK.BORDER_STRONG}`,
        background: on ? '#1F8A5B' : 'transparent',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: '#fff'
      }
    }, on && /*#__PURE__*/React.createElement(DG, {
      name: "check",
      size: 13
    })), /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        minWidth: 0
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: DFK.SANS,
        fontSize: 15.5,
        color: DFK.INK
      }
    }, s.label), /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: DFK.SERIF,
        fontStyle: 'italic',
        fontSize: 13,
        color: DFK.INK_MUTED,
        marginTop: 2
      }
    }, s.note)), /*#__PURE__*/React.createElement("div", {
      style: {
        textAlign: 'right',
        flexShrink: 0
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: DFK.MONO,
        fontSize: 14,
        color: '#1F8A5B',
        fontVariantNumeric: 'tabular-nums'
      }
    }, DC.signed(s.budget)), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 5,
        justifyContent: 'flex-end',
        marginTop: 5
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        width: 6,
        height: 6,
        borderRadius: 999,
        background: s.risk < 0 ? '#1F8A5B' : s.risk > 0 ? '#B23A2E' : DFK.INK_FAINT
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: DFK.MONO,
        fontSize: 9.5,
        letterSpacing: '0.1em',
        textTransform: 'uppercase',
        color: DFK.INK_FAINT
      }
    }, s.risk < 0 ? 'lowers risk' : s.risk > 0 ? 'raises risk' : 'neutral'))));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${DFK.BORDER_STRONG}`,
      borderRadius: 6,
      background: DFK.PAPER,
      overflow: 'hidden',
      position: 'sticky',
      top: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '22px 24px',
      borderBottom: `1px solid ${DFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: DKickF
  }, "How much can I save?"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontWeight: 300,
      fontSize: 44,
      lineHeight: 1,
      color: saved ? '#1F8A5B' : DFK.INK,
      marginTop: 10,
      fontVariantNumeric: 'tabular-nums'
    }
  }, DC.money(animSaved)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontSize: 13,
      color: DFK.INK_MUTED,
      marginTop: 8
    }
  }, "Brings the bid to ", /*#__PURE__*/React.createElement("b", {
    style: {
      color: DFK.INK,
      fontWeight: 400
    }
  }, DC.money(projected)))), /*#__PURE__*/React.createElement(OutCell, {
    label: "Schedule impact",
    value: ctx.daysDelta ? ctx.daysDelta + ' days' : 'No change',
    tone: ctx.daysDelta < 0 ? '#1F8A5B' : DFK.INK
  }), /*#__PURE__*/React.createElement(OutCell, {
    label: "Resource impact",
    value: ctx.crewDelta ? ctx.crewDelta + ' crew' : 'No change',
    tone: DFK.INK
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 7,
      marginTop: 12
    }
  }, ['Crew', 'Extras', 'Specialty', 'Grip', 'Locations', 'VFX', 'Editorial', 'Schedule'].map(r => /*#__PURE__*/React.createElement(DChip, {
    key: r,
    on: !!activeRes[r],
    dot: true
  }, r)))), /*#__PURE__*/React.createElement(OutCell, {
    label: "Risk impact",
    value: riskWord,
    tone: riskColor
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 12,
      display: 'flex',
      gap: 6
    }
  }, [-2, -1, 0, 1, 2].map(v => {
    const mid = 2;
    const filled = ctx.riskDelta < 0 ? v < 0 && v >= ctx.riskDelta : ctx.riskDelta > 0 ? v > 0 && v <= ctx.riskDelta : false;
    return /*#__PURE__*/React.createElement("div", {
      key: v,
      style: {
        flex: 1,
        height: 8,
        borderRadius: 2,
        background: v === 0 ? DFK.BORDER_STRONG : filled ? riskColor : DFK.BORDER
      }
    });
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 24px',
      borderTop: `1px solid ${DFK.BORDER}`,
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 10,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: DFK.INK_FAINT
    }
  }, ctx.active.length, " scenario", ctx.active.length === 1 ? '' : 's', " stacked"), ctx.active.length > 0 && /*#__PURE__*/React.createElement("button", {
    onClick: clearAll,
    style: {
      background: 'none',
      border: 0,
      padding: 0,
      cursor: 'pointer',
      fontFamily: DFK.MONO,
      fontSize: 10,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: DFK.INK_MUTED,
      borderBottom: `1px solid ${DFK.BORDER}`
    }
  }, "Reset")))));
}
function StripStat({
  label,
  value,
  sub,
  tone
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${DFK.BORDER}`,
      background: DFK.PAPER,
      borderRadius: 4,
      padding: '16px 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: DKickF
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.SANS,
      fontWeight: 300,
      fontSize: 24,
      marginTop: 8,
      color: tone || DFK.INK
    }
  }, value), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: DFK.MONO,
      fontSize: 11,
      color: DFK.INK_FAINT,
      marginTop: 8
    }
  }, sub));
}
function OutCell({
  label,
  value,
  tone,
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 24px',
      borderTop: `1px solid ${DFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: DKickF
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: DFK.SANS,
      fontSize: 17,
      color: tone || DFK.INK
    }
  }, value)), children);
}
Object.assign(window, {
  PDashboard,
  PBudget,
  PScenarios
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/production-hero.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/production-shell.jsx
try { (() => {
/* global React */
// TENET Production lane — ONE cockpit, ONE nav. The script intake is the
// landing (tab 1); every other view is a projection over the same money object.
// Nav is lifecycle-ordered: THE BID (Script · Bid) → THE BUDGET (the rest),
// straight from the schema — "the bid is the budget before the project starts."
const {
  FK: PFK,
  FGlyph: PG,
  PROD: PC
} = window;
const {
  CPHead: PHead,
  cpKickerFaint: PKickF,
  cpSerif: PSerif
} = window;
const PUse = React.useState,
  PRef = React.useRef;
const SEV_COLOR = {
  high: '#B23A2E',
  med: PFK.AMBER,
  low: PFK.INK_MUTED,
  ok: '#1F8A5B'
};

// Message · Director — persistent on every production tab (the script cockpit
// embeds its own copy of this same conversation).
const PROD_MSG_DRAFT = 'Schedule won\u2019t let us shoot this night scene. Can it be changed?';
function ProductionMessagePanel({
  chat,
  onSend
}) {
  const bub = mine => ({
    alignSelf: mine ? 'flex-end' : 'flex-start',
    maxWidth: '90%',
    background: mine ? PFK.INK : PFK.PAPER,
    color: mine ? PFK.PAPER : PFK.INK,
    border: mine ? 'none' : `1px solid ${PFK.BORDER}`,
    borderRadius: 8,
    padding: '8px 11px',
    fontFamily: PFK.SANS,
    fontSize: 12.5,
    lineHeight: 1.4,
    marginBottom: 8
  });
  const who = mine => ({
    fontFamily: PFK.MONO,
    fontSize: 8.5,
    letterSpacing: '0.12em',
    textTransform: 'uppercase',
    color: mine ? 'rgba(255,255,255,0.6)' : PFK.INK_FAINT,
    marginBottom: 3
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      padding: '14px 16px',
      borderBottom: `1px solid ${PFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement(PG, {
    name: "chat",
    size: 13,
    style: {
      color: PFK.AMBER
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.MONO,
      fontSize: 10,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: PFK.AMBER
    }
  }, "Message \xB7 Director")), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      overflowY: 'auto',
      padding: '14px 16px',
      display: 'flex',
      flexDirection: 'column'
    }
  }, chat.messages.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: bub(m.from === 'producer')
  }, /*#__PURE__*/React.createElement("div", {
    style: who(m.from === 'producer')
  }, m.from === 'producer' ? 'You · Producer' : 'D. Parker · Director'), m.text)), chat.typing && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: PFK.SERIF,
      fontStyle: 'italic',
      fontSize: 12.5,
      color: PFK.INK_FAINT,
      marginBottom: 7
    }
  }, "Director is editing the script\u2026"), chat.messages.length === 0 && !chat.sent && /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: PFK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: PFK.INK_FAINT,
      lineHeight: 1.5,
      marginBottom: 'auto'
    }
  }, "A direct line to the director. Send a question and watch the cut respond.")), !chat.sent && /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      borderTop: `1px solid ${PFK.BORDER}`,
      padding: '12px 16px',
      display: 'flex',
      flexDirection: 'column',
      gap: 9
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: PFK.SANS,
      fontSize: 12.5,
      color: PFK.INK,
      lineHeight: 1.4,
      padding: '9px 11px',
      background: PFK.PAPER_SOFT,
      border: `1px solid ${PFK.BORDER_STRONG}`,
      borderRadius: 8
    }
  }, PROD_MSG_DRAFT), /*#__PURE__*/React.createElement("button", {
    onClick: onSend,
    style: {
      alignSelf: 'flex-end',
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      cursor: 'pointer',
      background: PFK.AMBER,
      color: PFK.PAPER,
      border: 'none',
      borderRadius: 2,
      padding: '8px 15px',
      fontFamily: PFK.MONO,
      fontSize: 11,
      letterSpacing: '0.12em',
      textTransform: 'uppercase'
    }
  }, /*#__PURE__*/React.createElement(PG, {
    name: "send",
    size: 13
  }), " Send")));
}
function ProductionLane(props) {
  const {
    state,
    chat,
    flashId,
    onSend,
    onMode,
    onReset
  } = props;
  const [tab, setTab] = PUse('script');
  const [active, setActive] = PUse([]); // active scenario ids
  const [alertsOpen, setAlertsOpen] = PUse(false);
  const scrollRef = PRef(null);
  const act = PC.scenarios.filter(s => active.indexOf(s.id) >= 0);
  const savings = act.reduce((a, s) => a + s.budget, 0); // negative
  const daysDelta = act.reduce((a, s) => a + s.days, 0);
  const crewDelta = act.reduce((a, s) => a + s.crew, 0);
  const riskDelta = act.reduce((a, s) => a + s.risk, 0);
  function goTab(t) {
    setTab(t);
    setAlertsOpen(false);
    if (scrollRef.current) scrollRef.current.scrollTop = 0;
  }
  const ctx = {
    active,
    setActive,
    savings,
    daysDelta,
    crewDelta,
    riskDelta,
    goTab
  };

  // Pulled from window at render — these modules load after this one.
  const ScriptCockpit = window.ScriptCockpit;
  const BiddingView = window.BiddingView;
  const VIEWS = {
    dashboard: window.PDashboard,
    budget: window.PBudget,
    schedule: window.PSchedule,
    scenarios: window.PScenarios
  };
  const View = VIEWS[tab] || function () {
    return null;
  };
  const liveAlerts = PC.dashboard.alerts.filter(a => a.sev !== 'ok');
  const onScript = tab === 'script';

  // Lifecycle phase of the current tab → status read in the header.
  const curPhase = (PC.NAV.find(n => n.id === tab) || {}).phase;
  const phaseLabel = curPhase === 'bid' ? 'Bidding' : 'In production';
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      background: PFK.PAPER_SOFT,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      height: 60,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '0 32px',
      borderBottom: `1px solid ${PFK.BORDER}`,
      background: PFK.PAPER,
      position: 'relative',
      zIndex: 20,
      boxShadow: '0 1px 0 rgba(38,28,12,0.035)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.SERIF,
      fontStyle: 'italic',
      fontSize: 22,
      color: PFK.INK,
      letterSpacing: '0.01em'
    }
  }, "tenet"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.24em',
      textTransform: 'uppercase',
      color: PFK.AMBER,
      whiteSpace: 'nowrap'
    }
  }, "Production"), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 1,
      height: 17,
      background: PFK.BORDER_STRONG,
      display: 'inline-block',
      alignSelf: 'center'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: Object.assign({}, PSerif, {
      fontSize: 15,
      color: PFK.INK,
      whiteSpace: 'nowrap'
    })
  }, PC.project.title), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: PFK.INK_FAINT
    }
  }, PC.project.format, " \xB7 ", PC.project.code)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 22
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 7,
      fontFamily: PFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: PFK.INK_MUTED
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 6,
      height: 6,
      borderRadius: 999,
      background: curPhase === 'bid' ? PFK.AMBER : '#1F8A5B'
    }
  }), phaseLabel), onScript && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: `1px solid ${PFK.BORDER_STRONG}`,
      borderRadius: 3,
      overflow: 'hidden'
    }
  }, [30, 15].map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: m,
    onClick: () => onMode && onMode(m),
    style: {
      padding: '5px 12px',
      cursor: 'pointer',
      fontFamily: PFK.MONO,
      fontSize: 11,
      borderLeft: i ? `1px solid ${PFK.BORDER_STRONG}` : 'none',
      background: state.mode === m ? PFK.AMBER : PFK.PAPER,
      color: state.mode === m ? PFK.PAPER : PFK.INK_MUTED
    }
  }, ":", m))), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative'
    }
  }, /*#__PURE__*/React.createElement("button", {
    onClick: () => setAlertsOpen(o => !o),
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      background: 'none',
      border: 0,
      cursor: 'pointer',
      padding: 0,
      color: alertsOpen ? PFK.AMBER : PFK.INK_MUTED
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 6,
      height: 6,
      borderRadius: 999,
      background: '#B23A2E'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.MONO,
      fontSize: 11,
      letterSpacing: '0.08em',
      color: 'inherit'
    }
  }, liveAlerts.length, " alerts")), alertsOpen && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 30,
      right: 0,
      width: 340,
      background: PFK.PAPER,
      border: `1px solid ${PFK.BORDER_STRONG}`,
      borderRadius: 6,
      boxShadow: '0 18px 48px rgba(0,0,0,0.14)',
      padding: 8,
      zIndex: 40
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, PKickF, {
      padding: '8px 10px 10px'
    })
  }, "Active alerts"), liveAlerts.map((a, i) => /*#__PURE__*/React.createElement("button", {
    key: i,
    onClick: () => goTab(a.tab),
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 12,
      width: '100%',
      textAlign: 'left',
      background: 'none',
      border: 0,
      borderTop: `1px solid ${PFK.BORDER}`,
      padding: '12px 10px',
      cursor: 'pointer'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: SEV_COLOR[a.sev],
      flex: '0 0 70px'
    }
  }, a.kind), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.SANS,
      fontSize: 13,
      color: PFK.INK,
      lineHeight: 1.4
    }
  }, a.text))))), /*#__PURE__*/React.createElement("button", {
    className: "reset-btn",
    onClick: onReset
  }, /*#__PURE__*/React.createElement(PG, {
    name: "arrow",
    size: 13,
    style: {
      transform: 'scaleX(-1)'
    }
  }), " Change lane"))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      height: 52,
      display: 'flex',
      alignItems: 'stretch',
      padding: '0 30px',
      borderBottom: `1px solid ${PFK.BORDER}`,
      background: PFK.PAPER,
      position: 'relative',
      zIndex: 10
    }
  }, PC.NAV.map((n, idx) => {
    const on = n.id === tab;
    const prev = PC.NAV[idx - 1];
    const newPhase = prev && prev.phase !== n.phase;
    return /*#__PURE__*/React.createElement(React.Fragment, {
      key: n.id
    }, /*#__PURE__*/React.createElement("button", {
      onClick: () => goTab(n.id),
      style: {
        position: 'relative',
        display: 'flex',
        alignItems: 'center',
        gap: 8,
        background: 'none',
        border: 0,
        borderBottom: on ? `2px solid ${PFK.AMBER}` : '2px solid transparent',
        cursor: 'pointer',
        padding: '0 15px',
        marginBottom: -1,
        fontFamily: PFK.MONO,
        fontSize: 11,
        letterSpacing: '0.13em',
        textTransform: 'uppercase',
        color: on ? PFK.INK : PFK.INK_FAINT
      }
    }, n.label, n.id === 'scenarios' && active.length > 0 && /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: PFK.MONO,
        fontSize: 9,
        color: PFK.PAPER,
        background: PFK.AMBER,
        borderRadius: 999,
        padding: '1px 6px'
      }
    }, active.length)));
  })), onScript ? ScriptCockpit ? /*#__PURE__*/React.createElement(ScriptCockpit, {
    state: state,
    chat: chat,
    flashId: flashId,
    onSend: onSend,
    goTab: goTab
  }) : null : /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      display: 'flex'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 340px',
      minWidth: 0,
      minHeight: 0,
      borderRight: `1px solid ${PFK.BORDER}`,
      background: PFK.PAPER,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement(ProductionMessagePanel, {
    chat: chat,
    onSend: onSend
  })), /*#__PURE__*/React.createElement("div", {
    ref: scrollRef,
    style: {
      flex: 1,
      minHeight: 0,
      overflowY: 'auto',
      overflowX: 'hidden',
      background: PFK.PAPER_SOFT
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 1080,
      margin: '0 auto',
      padding: '36px 44px 64px'
    }
  }, tab === 'bid' ? /*#__PURE__*/React.createElement(BidView, {
    BiddingView: BiddingView
  }) : /*#__PURE__*/React.createElement(View, {
    ctx: ctx
  })))));
}

// Bid tab — approved bids on top; toggle the working bid below.
function BidView({
  BiddingView
}) {
  const [mode, setMode] = PUse('post'); // 'post' | 'overages' | 'new'
  const segBtn = (id, label) => /*#__PURE__*/React.createElement("div", {
    key: id,
    onClick: () => setMode(id),
    style: {
      padding: '7px 15px',
      cursor: 'pointer',
      whiteSpace: 'nowrap',
      fontFamily: PFK.MONO,
      fontSize: 11,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      borderLeft: id !== 'post' ? `1px solid ${PFK.BORDER_STRONG}` : 'none',
      background: mode === id ? PFK.AMBER : PFK.PAPER,
      color: mode === id ? PFK.PAPER : PFK.INK_MUTED
    }
  }, label);
  const empty = (title, line) => /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px dashed ${PFK.BORDER_STRONG}`,
      borderRadius: 6,
      background: PFK.PAPER,
      padding: '64px 32px',
      textAlign: 'center'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: PFK.MONO,
      fontSize: 10,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: PFK.INK_FAINT,
      marginBottom: 12
    }
  }, title), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: PFK.SERIF,
      fontStyle: 'italic',
      fontSize: 18,
      color: PFK.INK_MUTED,
      maxWidth: 460,
      margin: '0 auto',
      lineHeight: 1.5
    }
  }, line));
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(PHead, {
    kicker: "Bid",
    lede: "Every flagged driver lands on a line. Slide the deal terms and the grand total moves \u2014 this is the bid before the project starts."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 22
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, PKickF, {
      marginBottom: 12
    })
  }, "Approved bids"), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${PFK.BORDER}`,
      borderRadius: 6,
      background: PFK.PAPER,
      padding: '16px 20px',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 12,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: Object.assign({}, PSerif, {
      fontSize: 17,
      color: PFK.INK
    })
  }, "Master bid"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.MONO,
      fontSize: 10.5,
      color: PFK.INK_FAINT
    }
  }, PC.project.agency, " \xB7 ", PC.project.code), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.MONO,
      fontSize: 9,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: '#1F8A5B',
      border: '1px solid rgba(31,138,91,0.4)',
      background: 'rgba(31,138,91,0.08)',
      borderRadius: 2,
      padding: '2px 8px'
    }
  }, "Approved")), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: PFK.SANS,
      fontWeight: 300,
      fontSize: 24,
      color: PFK.INK,
      fontVariantNumeric: 'tabular-nums'
    }
  }, PC.money(PC.dashboard.budget.approved)))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      marginBottom: 18,
      flexWrap: 'wrap'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: `1px solid ${PFK.BORDER_STRONG}`,
      borderRadius: 3,
      overflow: 'hidden'
    }
  }, segBtn('post', 'Post bid'), segBtn('overages', 'Overages'), segBtn('new', 'New bid')), ['Running overage', 'Running overage'].map((t, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      padding: '7px 14px',
      borderRadius: 3,
      border: `1px dashed ${PFK.BORDER}`,
      fontFamily: PFK.MONO,
      fontSize: 10.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: PFK.INK_FAINT,
      opacity: 0.5,
      cursor: 'not-allowed',
      whiteSpace: 'nowrap'
    }
  }, t))), mode === 'post' ? /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${PFK.BORDER}`,
      borderRadius: 6,
      background: PFK.PAPER,
      overflow: 'hidden'
    }
  }, BiddingView ? /*#__PURE__*/React.createElement(BiddingView, null) : null) : mode === 'overages' ? empty('Overages', 'No overages logged yet. Once the project is in production, every line that runs over its approved figure surfaces here.') : empty('New bid', 'Start a fresh bid from this project\u2019s scope — or branch the approved master to model a different cut.'));
}

// Kept for back-compat with any external reference.
const ProductionPortal = ProductionLane;
Object.assign(window, {
  ProductionLane,
  ProductionPortal,
  SEV_COLOR
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/production-shell.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/production-tabs.jsx
try { (() => {
/* global React */
// TENET Production Portal — Shots, Schedule, Resources, Risks.
const {
  FK: XFK,
  FGlyph: XG,
  PROD: XC
} = window;
const {
  CPanel: XP,
  CPStat: XStat,
  CPBar: XBar,
  CPChip: XChip,
  CPHead: XHead,
  cpKickerFaint: XKickF,
  RISK_COLOR: XRISK
} = window;
const XUse = React.useState;
const RES_STATE = {
  wrapped: {
    c: '#9A9A9A',
    t: 'Wrapped'
  },
  active: {
    c: '#BA7517',
    t: 'Active'
  },
  pending: {
    c: '#9A9A9A',
    t: 'Pending'
  }
};

/* ══════════════════════════════════════════════════════════
   SHOTS — cost & complexity, scene by scene
   ══════════════════════════════════════════════════════════ */
function PShots({
  ctx
}) {
  const [sel, setSel] = XUse(XC.shots[0].id);
  const s = XC.shots.find(x => x.id === sel);
  const maxDriver = Math.max.apply(null, XC.topDrivers.map(d => d.amt));
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(XHead, {
    kicker: "Shots",
    title: "Cost and complexity, shot by shot",
    lede: "Every setup carries a complexity and a cost score \u2014 and the drivers behind both."
  }), /*#__PURE__*/React.createElement(XP, {
    label: "Primary cost drivers",
    title: "Across the production",
    style: {
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(5, 1fr)',
      gap: 18
    }
  }, XC.topDrivers.map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: i
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 14,
      color: XFK.INK,
      fontVariantNumeric: 'tabular-nums'
    }
  }, XC.money(d.amt)), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 8,
      marginBottom: 8
    }
  }, /*#__PURE__*/React.createElement(XBar, {
    pct: d.amt / maxDriver * 100,
    color: XFK.AMBER_200
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SANS,
      fontSize: 12.5,
      color: XFK.INK_MUTED,
      lineHeight: 1.3
    }
  }, d.label))))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 360px',
      gap: 24,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${XFK.BORDER}`,
      borderRadius: 6,
      overflow: 'hidden',
      background: XFK.PAPER
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '40px 1fr 92px 92px',
      gap: 12,
      padding: '13px 18px',
      borderBottom: `1px solid ${XFK.BORDER}`
    }
  }, ['#', 'Setup', 'Complexity', 'Cost'].map((h, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      fontFamily: XFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: XFK.INK_FAINT,
      textAlign: i >= 2 ? 'right' : 'left'
    }
  }, h))), XC.shots.map((sh, i) => {
    const on = sh.id === sel;
    return /*#__PURE__*/React.createElement("button", {
      key: sh.id,
      onClick: () => setSel(sh.id),
      style: {
        width: '100%',
        display: 'grid',
        gridTemplateColumns: '40px 1fr 92px 92px',
        gap: 12,
        padding: '14px 18px',
        alignItems: 'center',
        cursor: 'pointer',
        textAlign: 'left',
        background: on ? XFK.PAPER_SOFT : XFK.PAPER,
        border: 0,
        borderTop: i ? `1px solid ${XFK.BORDER}` : 'none',
        borderLeft: `2px solid ${on ? XFK.AMBER : 'transparent'}`
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: XFK.MONO,
        fontSize: 12,
        color: XFK.INK_FAINT
      }
    }, String(sh.n).padStart(2, '0')), /*#__PURE__*/React.createElement("span", {
      style: {
        minWidth: 0
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: XFK.SANS,
        fontSize: 14,
        color: XFK.INK,
        display: 'flex',
        alignItems: 'center',
        gap: 8
      }
    }, sh.loc, sh.night && /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: XFK.MONO,
        fontSize: 8.5,
        letterSpacing: '0.1em',
        color: XFK.AMBER,
        border: `1px solid ${XFK.AMBER}`,
        borderRadius: 2,
        padding: '1px 4px'
      }
    }, "NIGHT")), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: XFK.MONO,
        fontSize: 10,
        letterSpacing: '0.08em',
        color: XFK.INK_FAINT
      }
    }, sh.tod, " \xB7 ", sh.shotCount, " shots")), /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 6,
        justifyContent: 'flex-end'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        width: 34
      }
    }, /*#__PURE__*/React.createElement(XBar, {
      pct: sh.complexity,
      color: sh.complexity > 55 ? '#B23A2E' : XFK.INK,
      h: 4
    })), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: XFK.MONO,
        fontSize: 11,
        color: XFK.INK,
        width: 22,
        textAlign: 'right'
      }
    }, sh.complexity)), /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 6,
        justifyContent: 'flex-end'
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        width: 34
      }
    }, /*#__PURE__*/React.createElement(XBar, {
      pct: sh.costScore,
      color: XFK.AMBER,
      h: 4
    })), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: XFK.MONO,
        fontSize: 11,
        color: XFK.INK,
        width: 22,
        textAlign: 'right'
      }
    }, sh.costScore)));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      border: `1px solid ${XFK.BORDER_STRONG}`,
      borderRadius: 6,
      background: XFK.PAPER,
      overflow: 'hidden',
      position: 'sticky',
      top: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '20px 22px',
      borderBottom: `1px solid ${XFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: XFK.AMBER
    }
  }, "Setup ", String(s.n).padStart(2, '0')), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 10,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: XFK.INK_FAINT
    }
  }, s.status)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SANS,
      fontWeight: 300,
      fontSize: 22,
      marginTop: 8,
      color: XFK.INK
    }
  }, s.loc), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SERIF,
      fontStyle: 'italic',
      fontSize: 13.5,
      color: XFK.INK_MUTED,
      marginTop: 4,
      lineHeight: 1.4
    }
  }, s.desc)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 1,
      background: XFK.BORDER
    }
  }, /*#__PURE__*/React.createElement(MiniStat, {
    label: "Est. cost",
    value: XC.money(s.cost)
  }), /*#__PURE__*/React.createElement(MiniStat, {
    label: "Complexity",
    value: s.complexity + ' / 100',
    tone: s.complexity > 55 ? '#B23A2E' : XFK.INK
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 22px',
      borderTop: `1px solid ${XFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, XKickF, {
      marginBottom: 14
    })
  }, "Complexity factors"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 9
    }
  }, s.factors.map((f, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'grid',
      gridTemplateColumns: '92px 1fr 18px',
      gap: 10,
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.SANS,
      fontSize: 11.5,
      color: XFK.INK_MUTED,
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, f.name), /*#__PURE__*/React.createElement(XBar, {
    pct: f.lv / 7 * 100,
    color: f.lv >= 5 ? XFK.AMBER : XFK.INK_FAINT,
    h: 5
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 10,
      color: XFK.INK_FAINT,
      textAlign: 'right'
    }
  }, f.lv))))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 22px',
      borderTop: `1px solid ${XFK.BORDER}`,
      background: XFK.PAPER_SOFT
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, XKickF, {
      marginBottom: 10
    })
  }, "Primary cost drivers"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 7
    }
  }, s.drivers.map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 9
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 5,
      height: 5,
      borderRadius: 999,
      background: XFK.AMBER,
      flexShrink: 0
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.SANS,
      fontSize: 13,
      color: XFK.INK
    }
  }, d))))))));
}
function MiniStat({
  label,
  value,
  tone
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      background: XFK.PAPER,
      padding: '16px 22px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: XKickF
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SANS,
      fontWeight: 300,
      fontSize: 24,
      marginTop: 6,
      color: tone || XFK.INK,
      fontVariantNumeric: 'tabular-nums'
    }
  }, value));
}

/* ══════════════════════════════════════════════════════════
   SCHEDULE
   ══════════════════════════════════════════════════════════ */
function PSchedule({
  ctx
}) {
  const sc = XC.schedule;
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(XHead, {
    kicker: "Schedule",
    lede: "The shoot wrapped on plan. Here is how the days ran, and what carried the risk."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(4, 1fr)',
      gap: 16,
      marginBottom: 28
    }
  }, /*#__PURE__*/React.createElement(XStat, {
    label: "Shoot days",
    value: sc.shootDays,
    sub: "Principal photography"
  }), /*#__PURE__*/React.createElement(XStat, {
    label: "Days complete",
    value: sc.daysComplete + ' / ' + sc.shootDays,
    sub: "Wrapped",
    tone: "#1F8A5B"
  }), /*#__PURE__*/React.createElement(XStat, {
    label: "Days remaining",
    value: sc.daysRemaining,
    sub: "In post now"
  }), /*#__PURE__*/React.createElement(XStat, {
    label: "Forecast wrap",
    value: sc.forecastWrap,
    sub: "Hit on schedule"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1.5fr 1fr',
      gap: 16,
      alignItems: 'start'
    }
  }, /*#__PURE__*/React.createElement(XP, {
    label: "Day view",
    title: "How the days ran"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, sc.days.map(d => /*#__PURE__*/React.createElement("div", {
    key: d.id,
    style: {
      border: `1px solid ${XFK.BORDER}`,
      borderRadius: 4,
      padding: '16px 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      justifyContent: 'space-between',
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.SANS,
      fontWeight: 300,
      fontSize: 20,
      color: XFK.INK
    }
  }, d.label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 11,
      color: XFK.INK_FAINT
    }
  }, d.date), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: XFK.INK_MUTED
    }
  }, d.locs.join(' \u2192 '))), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: '#1F8A5B'
    }
  }, "Complete")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(4, 1fr)',
      gap: 12,
      marginBottom: d.risks.length ? 12 : 0
    }
  }, [['Pages', d.pages], ['Shots', d.shots], ['Crew', d.crew], ['Hours', d.hours]].map(([k, v]) => /*#__PURE__*/React.createElement("div", {
    key: k
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 9,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      color: XFK.INK_FAINT
    }
  }, k), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SANS,
      fontSize: 17,
      color: XFK.INK,
      marginTop: 3
    }
  }, v)))), d.risks.length > 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 7
    }
  }, d.risks.map((r, i) => /*#__PURE__*/React.createElement(XChip, {
    key: i,
    on: true,
    tone: XFK.AMBER,
    dot: true
  }, r))))))), /*#__PURE__*/React.createElement(XP, {
    label: "Risk factors",
    title: "What stressed the days",
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, sc.riskFactors.map((r, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 12,
      padding: '13px 0',
      borderTop: i ? `1px solid ${XFK.BORDER}` : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SANS,
      fontSize: 14,
      color: XFK.INK
    }
  }, r.label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SANS,
      fontSize: 12.5,
      color: XFK.INK_MUTED,
      marginTop: 1
    }
  }, r.note)), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 16,
      color: XFK.AMBER,
      flexShrink: 0
    }
  }, r.count)))))));
}

/* ══════════════════════════════════════════════════════════
   RESOURCES
   ══════════════════════════════════════════════════════════ */
function PResources({
  ctx
}) {
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(XHead, {
    kicker: "Resources",
    lede: "Every person, place, and piece of gear the project switched on \u2014 and where it stands."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 16
    }
  }, XC.resources.map(g => /*#__PURE__*/React.createElement(XP, {
    key: g.group,
    label: g.group,
    title: null,
    pad: 22
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, g.items.map((it, i) => {
    const st = RES_STATE[it.state];
    return /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        gap: 12,
        padding: '13px 0',
        borderTop: i ? `1px solid ${XFK.BORDER}` : 'none'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        minWidth: 0
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: XFK.SANS,
        fontSize: 14,
        color: XFK.INK
      }
    }, it.name), /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: XFK.SANS,
        fontSize: 12.5,
        color: XFK.INK_MUTED,
        marginTop: 1
      }
    }, it.detail)), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 12,
        flexShrink: 0
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: XFK.SANS,
        fontWeight: 300,
        fontSize: 20,
        color: XFK.INK
      }
    }, it.count), /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'inline-flex',
        alignItems: 'center',
        gap: 6,
        fontFamily: XFK.MONO,
        fontSize: 9,
        letterSpacing: '0.12em',
        textTransform: 'uppercase',
        color: st.c
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        width: 5,
        height: 5,
        borderRadius: 999,
        background: st.c
      }
    }), st.t)));
  }))))));
}

/* ══════════════════════════════════════════════════════════
   RISKS
   ══════════════════════════════════════════════════════════ */
function PRisks({
  ctx
}) {
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(XHead, {
    kicker: "Risks",
    title: "Threats before they happen",
    lede: "Each risk priced in dollars and days \u2014 the producer reads exposure, not vibes."
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr',
      gap: 16
    }
  }, XC.risks.map(r => /*#__PURE__*/React.createElement("div", {
    key: r.id,
    style: {
      border: `1px solid ${XFK.BORDER}`,
      borderRadius: 6,
      background: XFK.PAPER,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '18px 22px 16px',
      borderBottom: `1px solid ${XFK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: XFK.AMBER,
      whiteSpace: 'nowrap'
    }
  }, r.type, " risk"), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 7
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 9.5,
      letterSpacing: '0.1em',
      textTransform: 'uppercase',
      color: XFK.INK_FAINT
    }
  }, "Prob"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: XFK.SANS,
      fontSize: 13,
      color: XRISK[r.prob]
    }
  }, r.prob))), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SANS,
      fontWeight: 300,
      fontSize: 19,
      lineHeight: 1.2,
      color: XFK.INK
    }
  }, r.title)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: '1fr 1fr 1fr',
      gap: 1,
      background: XFK.BORDER
    }
  }, [['Exposure', r.exposure, XFK.INK], ['Cost impact', r.cost ? '+' + XC.money(r.cost) : '\u2014', r.cost ? '#B23A2E' : XFK.INK_FAINT], ['Schedule', r.days ? '+' + r.days + 'd' : '\u2014', r.days ? XFK.AMBER : XFK.INK_FAINT]].map(([k, v, c], i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      background: XFK.PAPER,
      padding: '14px 18px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.MONO,
      fontSize: 9,
      letterSpacing: '0.12em',
      textTransform: 'uppercase',
      color: XFK.INK_FAINT,
      marginBottom: 6
    }
  }, k), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SANS,
      fontSize: 15,
      color: c,
      fontVariantNumeric: 'tabular-nums'
    }
  }, v)))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 22px',
      borderTop: `1px solid ${XFK.BORDER}`,
      background: XFK.PAPER_SOFT
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: Object.assign({}, XKickF, {
      marginBottom: 8
    })
  }, "Mitigation"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: XFK.SANS,
      fontSize: 13.5,
      color: XFK.INK,
      lineHeight: 1.5
    }
  }, r.mitigation))))));
}
Object.assign(window, {
  PShots,
  PSchedule,
  PResources,
  PRisks
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/production-tabs.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/scaffolds.jsx
try { (() => {
/* global React, FK, FGlyph, FLOW, FRAMEWORK, useFTween */
// The left panel — each lane's TOP 10. Producer carries dollars + the chat.

const dScaff = {
  root: {
    height: '100%',
    display: 'flex',
    flexDirection: 'column',
    background: FK.PAPER,
    borderRight: `1px solid ${FK.BORDER}`,
    boxSizing: 'border-box',
    overflow: 'hidden'
  },
  head: {
    padding: '22px 30px 16px',
    borderBottom: `1px solid ${FK.BORDER}`
  },
  kicker: {
    fontFamily: FK.MONO,
    fontSize: 10,
    letterSpacing: '0.2em',
    textTransform: 'uppercase',
    color: FK.AMBER,
    whiteSpace: 'nowrap'
  },
  title: {
    fontFamily: FK.SERIF,
    fontStyle: 'italic',
    fontSize: 22,
    color: FK.INK,
    lineHeight: 1.1,
    marginTop: 8
  },
  sub: {
    fontFamily: FK.MONO,
    fontSize: 10,
    letterSpacing: '0.16em',
    textTransform: 'uppercase',
    color: FK.INK_FAINT,
    marginTop: 8
  },
  list: {
    flex: 1,
    overflow: 'auto',
    padding: '8px 0'
  },
  row: on => ({
    display: 'flex',
    alignItems: 'center',
    gap: 14,
    padding: '11px 30px',
    cursor: 'pointer',
    background: on ? FK.AMBER_50 : 'transparent',
    boxShadow: on ? `inset 3px 0 0 ${FK.AMBER}` : 'none'
  }),
  n: on => ({
    fontFamily: FK.MONO,
    fontSize: 11,
    color: on ? FK.AMBER : FK.INK_FAINT,
    width: 20
  }),
  name: on => ({
    fontFamily: FK.SERIF,
    fontStyle: 'italic',
    fontSize: 18,
    color: on ? FK.AMBER_800 : FK.INK,
    flex: 1
  }),
  count: {
    fontFamily: FK.MONO,
    fontSize: 10,
    color: FK.INK_FAINT
  }
};
function ScaffoldDirector({
  cat,
  onCat
}) {
  const S = dScaff;
  return /*#__PURE__*/React.createElement("div", {
    style: S.root
  }, /*#__PURE__*/React.createElement("div", {
    style: S.head
  }, /*#__PURE__*/React.createElement("div", {
    style: S.kicker
  }, "Director \xB7 top 10"), /*#__PURE__*/React.createElement("div", {
    style: S.title
  }, "Creative variables"), /*#__PURE__*/React.createElement("div", {
    style: S.sub
  }, "The ten that decide whether it lands")), /*#__PURE__*/React.createElement("div", {
    style: S.list
  }, FRAMEWORK.DIRECTOR.map(c => {
    const on = cat === c.id;
    return /*#__PURE__*/React.createElement("div", {
      key: c.id,
      style: S.row(on),
      onClick: () => onCat(c.id)
    }, /*#__PURE__*/React.createElement("span", {
      style: S.n(on)
    }, String(c.n).padStart(2, '0')), /*#__PURE__*/React.createElement("span", {
      style: S.name(on)
    }, c.name), /*#__PURE__*/React.createElement("span", {
      style: S.count
    }, on ? '6 subs ›' : '6'));
  })));
}
const pScaff = {
  root: {
    height: '100%',
    display: 'flex',
    flexDirection: 'column',
    background: FK.PAPER,
    borderRight: `1px solid ${FK.BORDER}`,
    boxSizing: 'border-box',
    overflow: 'hidden'
  },
  top: {
    padding: '20px 30px 16px',
    borderBottom: `1px solid ${FK.BORDER}`
  },
  kicker: {
    fontFamily: FK.MONO,
    fontSize: 10,
    letterSpacing: '0.2em',
    textTransform: 'uppercase',
    color: FK.AMBER,
    whiteSpace: 'nowrap'
  },
  totalLabel: {
    fontFamily: FK.MONO,
    fontSize: 10,
    letterSpacing: '0.18em',
    textTransform: 'uppercase',
    color: FK.INK_FAINT,
    marginTop: 12
  },
  total: {
    fontFamily: FK.SANS,
    fontWeight: 300,
    fontSize: 44,
    color: FK.INK,
    lineHeight: 1,
    letterSpacing: '-0.02em',
    marginTop: 4
  },
  list: {
    flex: 1,
    overflow: 'auto'
  },
  row: (on, flash) => ({
    display: 'flex',
    alignItems: 'center',
    gap: 12,
    padding: '9px 30px',
    cursor: 'pointer',
    borderBottom: `1px solid ${FK.BORDER}`,
    background: on || flash ? FK.AMBER_50 : 'transparent',
    boxShadow: on ? `inset 3px 0 0 ${FK.AMBER}` : 'none'
  }),
  n: {
    fontFamily: FK.MONO,
    fontSize: 10,
    color: FK.INK_FAINT,
    width: 18
  },
  name: on => ({
    fontFamily: FK.SERIF,
    fontStyle: 'italic',
    fontSize: 16,
    color: on ? FK.AMBER_800 : FK.INK,
    flex: 1
  }),
  ot: {
    fontFamily: FK.MONO,
    fontSize: 9,
    letterSpacing: '0.1em',
    textTransform: 'uppercase',
    color: FK.AMBER_800,
    padding: '2px 6px',
    borderRadius: 2,
    background: FK.AMBER_50,
    border: `1px solid ${FK.AMBER}`
  },
  cost: {
    fontFamily: FK.MONO,
    fontSize: 12,
    color: FK.INK,
    width: 74,
    textAlign: 'right'
  },
  chat: {
    borderTop: `1px solid ${FK.BORDER}`,
    background: FK.PAPER_SOFT,
    padding: '14px 24px 18px',
    flexShrink: 0
  },
  chatHead: {
    fontFamily: FK.MONO,
    fontSize: 10,
    letterSpacing: '0.18em',
    textTransform: 'uppercase',
    color: FK.AMBER,
    marginBottom: 10,
    display: 'flex',
    alignItems: 'center',
    gap: 8,
    whiteSpace: 'nowrap'
  },
  bubble: mine => ({
    alignSelf: mine ? 'flex-end' : 'flex-start',
    maxWidth: '88%',
    background: mine ? FK.INK : FK.PAPER,
    color: mine ? FK.PAPER : FK.INK,
    border: mine ? 'none' : `1px solid ${FK.BORDER}`,
    borderRadius: 8,
    padding: '8px 11px',
    fontFamily: FK.SANS,
    fontSize: 12.5,
    lineHeight: 1.4,
    marginBottom: 7
  }),
  who: mine => ({
    fontFamily: FK.MONO,
    fontSize: 9,
    letterSpacing: '0.12em',
    textTransform: 'uppercase',
    color: mine ? 'rgba(255,255,255,0.6)' : FK.INK_FAINT,
    marginBottom: 3
  }),
  composer: {
    display: 'flex',
    flexDirection: 'column',
    gap: 9
  },
  draft: {
    fontFamily: FK.SANS,
    fontSize: 12.5,
    color: FK.INK,
    lineHeight: 1.4,
    padding: '9px 11px',
    background: FK.PAPER,
    border: `1px solid ${FK.BORDER_STRONG}`,
    borderRadius: 8
  },
  send: {
    alignSelf: 'flex-end',
    display: 'inline-flex',
    alignItems: 'center',
    gap: 8,
    cursor: 'pointer',
    background: FK.AMBER,
    color: FK.PAPER,
    border: 'none',
    borderRadius: 2,
    padding: '8px 15px',
    fontFamily: FK.MONO,
    fontSize: 11,
    letterSpacing: '0.12em',
    textTransform: 'uppercase'
  },
  typing: {
    fontFamily: FK.SERIF,
    fontStyle: 'italic',
    fontSize: 12.5,
    color: FK.INK_FAINT,
    marginBottom: 7
  }
};
const DRAFT_MSG = 'Schedule won\u2019t let us shoot this night scene. Can it be changed?';
function ScaffoldProducer({
  state,
  cat,
  onCat,
  chat,
  onSend,
  catFlash
}) {
  const S = pScaff;
  const total = useFTween(FRAMEWORK.producerTotal(state), 700);
  return /*#__PURE__*/React.createElement("div", {
    style: S.root
  }, /*#__PURE__*/React.createElement("div", {
    style: S.top
  }, /*#__PURE__*/React.createElement("div", {
    style: S.kicker
  }, "Producer \xB7 top 10"), /*#__PURE__*/React.createElement("div", {
    style: S.totalLabel
  }, "Total cost"), /*#__PURE__*/React.createElement("div", {
    style: S.total
  }, FLOW.fmtMoney(total))), /*#__PURE__*/React.createElement("div", {
    style: S.list
  }, FRAMEWORK.PRODUCER.map(c => {
    const on = cat === c.id;
    const cost = FRAMEWORK.categoryCost(c, state);
    const ot = c.nightVar && FRAMEWORK.nightActive(state);
    return /*#__PURE__*/React.createElement("div", {
      key: c.id,
      style: S.row(on, catFlash === c.id),
      onClick: () => onCat(c.id)
    }, /*#__PURE__*/React.createElement("span", {
      style: S.n
    }, String(c.n).padStart(2, '0')), /*#__PURE__*/React.createElement("span", {
      style: S.name(on)
    }, c.name), ot && /*#__PURE__*/React.createElement("span", {
      style: S.ot
    }, "+OT"), /*#__PURE__*/React.createElement("span", {
      style: S.cost
    }, FLOW.fmtMoney(cost)));
  })), /*#__PURE__*/React.createElement("div", {
    style: S.chat
  }, /*#__PURE__*/React.createElement("div", {
    style: S.chatHead
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "chat",
    size: 14
  }), " Message \xB7 Director"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column'
    }
  }, chat.messages.map((m, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: S.bubble(m.from === 'producer')
  }, /*#__PURE__*/React.createElement("div", {
    style: S.who(m.from === 'producer')
  }, m.from === 'producer' ? 'You · Producer' : 'D. Parker · Director'), m.text)), chat.typing && /*#__PURE__*/React.createElement("div", {
    style: S.typing
  }, "Director is editing the script\\u2026")), !chat.sent && /*#__PURE__*/React.createElement("div", {
    style: S.composer
  }, /*#__PURE__*/React.createElement("div", {
    style: S.draft
  }, DRAFT_MSG), /*#__PURE__*/React.createElement("button", {
    style: S.send,
    onClick: onSend
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "send",
    size: 13
  }), " Send"))));
}
window.ScaffoldDirector = ScaffoldDirector;
window.ScaffoldProducer = ScaffoldProducer;
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/scaffolds.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/screens.jsx
try { (() => {
/* global React, FK, FGlyph */
// Entry screens — lane select + upload gate, in the First Experience register.

const laneStyles = {
  wrap: {
    position: 'absolute',
    inset: 0,
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'center',
    background: FK.PAPER
  },
  kicker: {
    fontFamily: FK.MONO,
    fontSize: 12,
    letterSpacing: '0.28em',
    textTransform: 'uppercase',
    color: FK.AMBER,
    marginBottom: 64,
    whiteSpace: 'nowrap'
  },
  row: {
    display: 'flex',
    alignItems: 'stretch',
    gap: 0
  },
  opt: hover => ({
    width: 340,
    padding: '8px 40px',
    textAlign: 'center',
    cursor: 'pointer',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center'
  }),
  divider: {
    width: 1,
    background: FK.BORDER,
    alignSelf: 'stretch',
    margin: '6px 0'
  },
  label: hover => ({
    fontFamily: FK.SANS,
    fontWeight: 300,
    fontSize: 42,
    letterSpacing: '0.04em',
    color: hover ? FK.AMBER : FK.INK,
    lineHeight: 1,
    whiteSpace: 'nowrap'
  }),
  rule: hover => ({
    width: 40,
    height: 1,
    background: hover ? FK.AMBER : FK.BORDER_STRONG,
    margin: '22px 0'
  }),
  exp: {
    fontFamily: FK.SERIF,
    fontStyle: 'italic',
    fontSize: 18,
    color: FK.INK_MUTED,
    lineHeight: 1.4,
    maxWidth: 240
  },
  brief: {
    fontFamily: FK.SERIF,
    fontStyle: 'italic',
    fontSize: 15,
    color: FK.INK_FAINT,
    marginTop: 64,
    whiteSpace: 'nowrap'
  }
};
function LaneSelect({
  onPick
}) {
  const S = laneStyles;
  const [hover, setHover] = React.useState(null);
  const opts = [{
    id: 'director',
    label: 'Creative',
    exp: 'Shape the work. Script, shots, and the craft of the cut.'
  }, {
    id: 'producer',
    label: 'Production',
    exp: 'Hold the line. Cost, schedule, and every change in dollars.'
  }, {
    id: 'client',
    label: 'Client',
    exp: 'Watch it take shape. Approve before a single frame is shot.'
  }];
  return /*#__PURE__*/React.createElement("div", {
    style: S.wrap
  }, /*#__PURE__*/React.createElement("div", {
    style: S.kicker
  }, "Pick a lane"), /*#__PURE__*/React.createElement("div", {
    style: S.row
  }, opts.map((o, i) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: o.id
  }, i > 0 && /*#__PURE__*/React.createElement("div", {
    style: S.divider
  }), /*#__PURE__*/React.createElement("div", {
    style: S.opt(hover === o.id),
    onMouseEnter: () => setHover(o.id),
    onMouseLeave: () => setHover(null),
    onClick: () => onPick(o.id)
  }, /*#__PURE__*/React.createElement("div", {
    style: S.label(hover === o.id)
  }, o.label), /*#__PURE__*/React.createElement("div", {
    style: S.rule(hover === o.id)
  }), /*#__PURE__*/React.createElement("div", {
    style: S.exp
  }, o.exp))))), /*#__PURE__*/React.createElement("div", {
    style: S.brief
  }, "Three seats, one project. Choose where you sit."));
}
const upStyles = {
  wrap: {
    position: 'absolute',
    inset: 0,
    background: FK.PAPER
  },
  center: {
    position: 'absolute',
    left: '50%',
    top: '46%',
    transform: 'translate(-50%,-50%)',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center'
  },
  icons: {
    display: 'flex',
    alignItems: 'flex-start',
    gap: 92,
    marginBottom: 44
  },
  iconCol: active => ({
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    gap: 16,
    cursor: active ? 'pointer' : 'default',
    opacity: active ? 1 : 0.32
  }),
  iconLabel: active => ({
    fontFamily: FK.MONO,
    fontSize: 10,
    letterSpacing: '0.18em',
    textTransform: 'uppercase',
    color: active ? FK.AMBER : FK.INK_FAINT
  }),
  prompt: {
    fontFamily: FK.SERIF,
    fontStyle: 'italic',
    fontSize: 19,
    color: FK.INK_MUTED
  },
  ring: active => ({
    width: 92,
    height: 92,
    borderRadius: '50%',
    border: `1px solid ${active ? FK.AMBER : FK.BORDER}`,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    color: active ? FK.AMBER : FK.INK_FAINT,
    background: active ? FK.AMBER_50 : FK.PAPER
  }),
  fileRow: {
    display: 'flex',
    alignItems: 'center',
    gap: 12,
    marginTop: 6,
    fontFamily: FK.MONO,
    fontSize: 13,
    color: FK.INK
  },
  bar: {
    width: 320,
    height: 2,
    background: FK.BORDER,
    marginTop: 22,
    position: 'relative'
  },
  barFill: p => ({
    position: 'absolute',
    left: 0,
    top: 0,
    height: 2,
    width: p + '%',
    background: FK.AMBER
  })
};
function UploadGate({
  lane,
  onUploaded
}) {
  const S = upStyles;
  const [phase, setPhase] = React.useState('idle'); // idle | uploading
  const [prog, setProg] = React.useState(0);
  const isClient = lane === 'client';
  function startUpload() {
    if (phase !== 'idle') return;
    setPhase('uploading');
    let p = 0;
    const id = setInterval(() => {
      p = Math.min(100, p + 12);
      setProg(p);
      if (p >= 100) clearInterval(id);
    }, 90);
    // Completion is one timer, not dependent on the bar's many ticks.
    setTimeout(onUploaded, 1400);
  }

  // Client seats don't upload a script — they review the cut. The active
  // action becomes Review (straight through to the cut); Upload is greyed.
  const tools = isClient ? [{
    id: 'mic',
    label: 'Record',
    active: false
  }, {
    id: 'phone',
    label: 'Call',
    active: false
  }, {
    id: 'upload',
    label: 'Upload',
    active: false
  }, {
    id: 'review',
    label: 'Review',
    active: true,
    action: onUploaded
  }] : [{
    id: 'mic',
    label: 'Record',
    active: false
  }, {
    id: 'phone',
    label: 'Call',
    active: false
  }, {
    id: 'upload',
    label: 'Upload',
    active: true,
    action: startUpload
  }, {
    id: 'video',
    label: 'Video call',
    active: false
  }];
  return /*#__PURE__*/React.createElement("div", {
    style: S.wrap
  }, /*#__PURE__*/React.createElement("div", {
    style: S.center
  }, /*#__PURE__*/React.createElement("div", {
    style: S.icons
  }, tools.map(t => /*#__PURE__*/React.createElement("div", {
    key: t.id,
    style: S.iconCol(t.active),
    onClick: t.active ? t.action : undefined
  }, /*#__PURE__*/React.createElement("div", {
    style: S.ring(t.active)
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: t.id,
    size: 32
  })), /*#__PURE__*/React.createElement("div", {
    style: S.iconLabel(t.active)
  }, t.label)))), phase === 'idle' && /*#__PURE__*/React.createElement("div", {
    style: S.prompt
  }, isClient ? 'Open the latest cut to review.' : 'Upload a script to begin.'), phase === 'uploading' && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: S.fileRow
  }, /*#__PURE__*/React.createElement(FGlyph, {
    name: "script",
    size: 16,
    style: {
      color: FK.AMBER
    }
  }), "A_Brief_History_of_Selling_Ideas_v3.fdx"), /*#__PURE__*/React.createElement("div", {
    style: S.bar
  }, /*#__PURE__*/React.createElement("div", {
    style: S.barFill(prog)
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: FK.MONO,
      fontSize: 10,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: FK.INK_FAINT,
      marginTop: 12
    }
  }, prog < 100 ? 'Reading scenes\u2026' : 'Building scaffold\u2026'))));
}
window.LaneSelect = LaneSelect;
window.UploadGate = UploadGate;
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/screens.jsx", error: String((e && e.message) || e) }); }

// flow_ctx/ui.jsx
try { (() => {
/* global React */
// TENET flow — shared UI: First Experience tokens, line icons, tween, scaling.

const FK = {
  INK: '#1A1A1A',
  INK_MUTED: '#6B6B6B',
  INK_FAINT: '#9A9A9A',
  PAPER: '#FFFFFF',
  PAPER_RAISED: '#FBFBFA',
  PAPER_SOFT: '#F4F4F2',
  PAPER_DIM: '#ECECEA',
  AMBER: '#BA7517',
  AMBER_50: '#FAEEDA',
  AMBER_200: '#EF9F27',
  AMBER_800: '#854F0B',
  BORDER: 'rgba(0,0,0,0.10)',
  BORDER_STRONG: 'rgba(0,0,0,0.18)',
  SHADOW_LIFT: '0 1px 0 rgba(20,20,22,0.02), 0 12px 34px -16px rgba(20,20,30,0.20)',
  SHADOW_POP: '0 28px 64px -22px rgba(20,20,30,0.28)',
  OK: '#1F8A5B',
  DANGER: '#B23A2E',
  SANS: '"Brown","Avenir Next","Helvetica Neue",system-ui,sans-serif',
  SERIF: '"Source Serif 4","Source Serif Pro",Georgia,serif',
  MONO: '"JetBrains Mono","SF Mono",Menlo,monospace',
  SCRIPT: '"Times New Roman",Times,serif'
};
function FGlyph({
  name,
  size = 24,
  sw = 1.5,
  style
}) {
  const base = {
    width: size,
    height: size,
    viewBox: '0 0 32 32',
    fill: 'none',
    stroke: 'currentColor',
    strokeWidth: sw,
    strokeLinecap: 'round',
    strokeLinejoin: 'round',
    style
  };
  const P = {
    mic: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("rect", {
      x: "12",
      y: "4",
      width: "8",
      height: "16",
      rx: "4"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M7 14a9 9 0 0 0 18 0"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M16 23v5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M12 28h8"
    })),
    phone: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M7 6.5C7 5.7 7.7 5 8.5 5h3.2c.7 0 1.3.5 1.4 1.2l.7 4c.1.6-.2 1.2-.7 1.5l-2 1.2a14 14 0 0 0 7 7l1.2-2c.3-.5.9-.8 1.5-.7l4 .7c.7.1 1.2.7 1.2 1.4v3.2c0 .8-.7 1.5-1.5 1.5A18.5 18.5 0 0 1 7 6.5z"
    })),
    upload: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M16 4v16M10 10l6-6 6 6"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M5 22v4a2 2 0 0 0 2 2h18a2 2 0 0 0 2-2v-4"
    })),
    script: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M8 4h12l5 5v18a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2z"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M20 4v5h5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M11 15h10M11 19h10M11 23h6"
    })),
    chat: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M27 19a2 2 0 0 1-2 2H11l-5 5V8a2 2 0 0 1 2-2h17a2 2 0 0 1 2 2z"
    })),
    send: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M28 4 14 18M28 4l-9 24-5-10-10-5 24-9z"
    })),
    pin: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M25 13c0 7-9 15-9 15s-9-8-9-15a9 9 0 0 1 18 0Z"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "16",
      cy: "13",
      r: "3.4"
    })),
    user: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "16",
      cy: "11",
      r: "5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M6 27a10 10 0 0 1 20 0"
    })),
    clock: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "16",
      cy: "16",
      r: "12"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M16 8v8l5 3"
    })),
    arrow: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M6 16h20M18 8l8 8-8 8"
    })),
    check: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M27 8 12 23l-7-7"
    })),
    x: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M8 8l16 16M24 8 8 24"
    })),
    plus: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M16 6v20M6 16h20"
    })),
    film: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("rect", {
      x: "4",
      y: "7",
      width: "24",
      height: "18",
      rx: "2"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M10 7v18M22 7v18M4 13h6M22 13h6M4 19h6M22 19h6"
    })),
    video: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("rect", {
      x: "4",
      y: "9",
      width: "17",
      height: "14",
      rx: "2.5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M21 13.5l7-4v13l-7-4z"
    })),
    review: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M2 16s5-9 14-9 14 9 14 9-5 9-14 9S2 16 2 16z"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "16",
      cy: "16",
      r: "4.2"
    }))
  };
  return /*#__PURE__*/React.createElement("svg", base, P[name] || null);
}

// Interruptible count tween — guarantees the destination even if rAF is paused.
function useFTween(value, dur = 600) {
  const [disp, setDisp] = React.useState(value);
  const ref = React.useRef(value);
  const raf = React.useRef();
  React.useEffect(() => {
    const from = ref.current,
      to = value;
    if (Math.abs(from - to) < 0.001) {
      ref.current = to;
      setDisp(to);
      return;
    }
    const start = performance.now();
    cancelAnimationFrame(raf.current);
    let done = false;
    const finish = () => {
      if (done) return;
      done = true;
      ref.current = to;
      setDisp(to);
    };
    const tick = now => {
      const p = Math.min(1, (now - start) / dur);
      const e = 1 - Math.pow(1 - p, 3);
      ref.current = from + (to - from) * e;
      setDisp(ref.current);
      if (p < 1) raf.current = requestAnimationFrame(tick);else finish();
    };
    raf.current = requestAnimationFrame(tick);
    const fb = setTimeout(finish, dur + 80);
    return () => {
      cancelAnimationFrame(raf.current);
      clearTimeout(fb);
    };
  }, [value, dur]);
  return disp;
}
function fitFlowStage() {
  const st = document.getElementById('stage');
  if (!st) return;
  const vw = window.innerWidth || document.documentElement.clientWidth || 0;
  const vh = window.innerHeight || document.documentElement.clientHeight || 0;
  const s = Math.min(vw / 1920, vh / 1080);
  // Dimensions not ready yet (can be 0 on first paint in some embeds) —
  // never apply scale(0); reschedule until the frame has real size.
  if (!(s > 0) || !isFinite(s)) {
    setTimeout(fitFlowStage, 60);
    return;
  }
  st.style.transform = `translate(-50%, -50%) scale(${s})`;
}
window.addEventListener('resize', fitFlowStage);
if (typeof ResizeObserver !== 'undefined') {
  try {
    new ResizeObserver(() => fitFlowStage()).observe(document.documentElement);
  } catch (e) {}
}
Object.assign(window, {
  FK,
  FGlyph,
  useFTween,
  fitFlowStage
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "flow_ctx/ui.jsx", error: String((e && e.message) || e) }); }

// lobby_deck/deck-stage.js
try { (() => {
/**
 * <deck-stage> — reusable web component for HTML decks.
 *
 * Handles:
 *  (a) speaker notes — reads <script type="application/json" id="speaker-notes">
 *      and posts {slideIndexChanged: N} to the parent window on nav.
 *  (b) keyboard navigation — ←/→, PgUp/PgDn, Space, Home/End, number keys.
 *      On touch devices, tapping the left/right half of the stage goes
 *      prev/next — taps on links, buttons and other interactive slide
 *      content are left alone.
 *  (c) press R to reset to slide 0 (with a tasteful keyboard hint).
 *  (d) bottom-center overlay showing slide count + hints, fades out on idle.
 *  (e) auto-scaling — inner canvas is a fixed design size (default 1920×1080)
 *      scaled with `transform: scale()` to fit the viewport, letterboxed.
 *      Set the `noscale` attribute to render at authored size (1:1) — the
 *      PPTX exporter sets this so its DOM capture sees unscaled geometry.
 *  (f) print — `@media print` lays every slide out as its own page at the
 *      design size, so the browser's Print → Save as PDF produces a clean
 *      one-page-per-slide PDF with no extra setup.
 *  (g) thumbnail rail — resizable left-hand column of per-slide thumbnails
 *      (static clones). Click to navigate; ↑/↓ with a thumbnail focused to
 *      step between slides; drag to reorder; right-click for
 *      Skip / Move up / Move down / Delete (opens a Cancel/Delete confirm
 *      dialog). Drag the rail's right edge to resize; width persists to
 *      localStorage. Skipped slides carry `data-deck-skip`, are dimmed in
 *      the rail, omitted from prev/next navigation, and hidden at print.
 *      The rail is suppressed in presenting mode, in the host's Preview
 *      mode (ViewerMode='none'), on `noscale`, on narrow viewports
 *      (≤640px), and via the `no-rail` attribute. Rail mutations dispatch
 *      a `deckchange`
 *      CustomEvent on the element: detail = {action, from, to, slide}.
 *
 * Slides are HIDDEN, not unmounted. Non-active slides stay in the DOM with
 * `visibility: hidden` + `opacity: 0`, so their state (videos, iframes,
 * form inputs, React trees) is preserved across navigation.
 *
 * Lifecycle event — the component dispatches a `slidechange` CustomEvent on
 * itself whenever the active slide changes (including the initial mount).
 * The event bubbles and composes out of shadow DOM, so you can listen on
 * the <deck-stage> element or on document:
 *
 *   document.querySelector('deck-stage').addEventListener('slidechange', (e) => {
 *     e.detail.index         // new 0-based index
 *     e.detail.previousIndex // previous index, or -1 on init
 *     e.detail.total         // total slide count
 *     e.detail.slide         // the new active slide element
 *     e.detail.previousSlide // the prior slide element, or null on init
 *     e.detail.reason        // 'init' | 'keyboard' | 'click' | 'tap' | 'api'
 *   });
 *
 * Persistence: none at the deck level. The host app keeps the current slide
 * in its own URL (?slide=) and re-delivers it via location.hash on load, so a
 * bare load with no hash always starts at slide 1.
 *
 * Usage:
 *   <style>deck-stage:not(:defined){visibility:hidden}</style>
 *   <deck-stage width="1920" height="1080">
 *     <section data-label="Title">...</section>
 *     <section data-label="Agenda">...</section>
 *   </deck-stage>
 *   <script src="deck-stage.js"></script>
 *
 * The :not(:defined) rule prevents a flash of the first slide at its
 * authored styles before this script runs and attaches the shadow root.
 *
 * Slides are the direct element children of <deck-stage>. Each slide is
 * automatically tagged with:
 *   - data-screen-label="NN Label"   (1-indexed, for comment flow)
 *   - data-om-validate="no_overflowing_text,no_overlapping_text,slide_sized_text"
 */

(() => {
  const DESIGN_W_DEFAULT = 1920;
  const DESIGN_H_DEFAULT = 1080;
  const OVERLAY_HIDE_MS = 1800;
  const VALIDATE_ATTR = 'no_overflowing_text,no_overlapping_text,slide_sized_text';
  const FINE_POINTER_MQ = matchMedia('(hover: hover) and (pointer: fine)');
  const NARROW_MQ = matchMedia('(max-width: 640px)');
  // Slide-authored controls that should keep a tap instead of it navigating.
  const INTERACTIVE_SEL = 'a[href], button, input, select, textarea, summary, label, video[controls], audio[controls], [role="button"], [onclick], [tabindex]:not([tabindex^="-"]), [contenteditable]:not([contenteditable="false" i])';
  const pad2 = n => String(n).padStart(2, '0');

  // Label precedence: data-label → data-screen-label (number stripped) → first heading → "Slide".
  const getSlideLabel = el => {
    const explicit = el.getAttribute('data-label');
    if (explicit) return explicit;
    const existing = el.getAttribute('data-screen-label');
    if (existing) return existing.replace(/^\s*\d+\s*/, '').trim() || existing;
    const h = el.querySelector('h1, h2, h3, [data-title]');
    const t = h && (h.textContent || '').trim().slice(0, 40);
    if (t) return t;
    return 'Slide';
  };
  const stylesheet = `
    :host {
      position: fixed;
      inset: 0;
      display: block;
      background: #000;
      color: #fff;
      font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", Helvetica, Arial, sans-serif;
      overflow: hidden;
      -webkit-tap-highlight-color: transparent;
    }
    /* connectedCallback holds this until document.fonts.ready (capped 2s) so
     * the first visible paint has the deck's real typography + final rail
     * layout. opacity (not visibility) so the active slide can't un-hide
     * itself via the ::slotted([data-deck-active]) visibility:visible rule.
     * Only the stage/rail hide — the black :host background stays, so the
     * iframe doesn't flash the page's default white. */
    :host([data-fonts-pending]) .stage,
    :host([data-fonts-pending]) .rail { opacity: 0; pointer-events: none; }

    .stage {
      position: absolute;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .canvas {
      position: relative;
      transform-origin: center center;
      flex-shrink: 0;
      background: #fff;
      will-change: transform;
    }

    /* Slides live in light DOM (via <slot>) so authored CSS still applies.
       We absolutely position each slotted child to stack them. */
    ::slotted(*) {
      position: absolute !important;
      inset: 0 !important;
      width: 100% !important;
      height: 100% !important;
      box-sizing: border-box !important;
      overflow: hidden;
      opacity: 0;
      pointer-events: none;
      visibility: hidden;
    }
    ::slotted([data-deck-active]) {
      opacity: 1;
      pointer-events: auto;
      visibility: visible;
    }

    .overlay {
      position: fixed;
      left: 50%;
      bottom: 22px;
      transform: translate(-50%, 6px) scale(0.92);
      filter: blur(6px);
      display: flex;
      align-items: center;
      gap: 4px;
      padding: 4px;
      background: #000;
      color: #fff;
      border-radius: 999px;
      font-size: 12px;
      font-feature-settings: "tnum" 1;
      letter-spacing: 0.01em;
      opacity: 0;
      pointer-events: none;
      transition: opacity 260ms ease, transform 260ms cubic-bezier(.2,.8,.2,1), filter 260ms ease;
      transform-origin: center bottom;
      z-index: 2147483000;
      user-select: none;
    }
    .overlay[data-visible] {
      opacity: 1;
      pointer-events: auto;
      transform: translate(-50%, 0) scale(1);
      filter: blur(0);
    }

    .btn {
      appearance: none;
      -webkit-appearance: none;
      background: transparent;
      border: 0;
      margin: 0;
      padding: 0;
      color: inherit;
      font: inherit;
      cursor: default;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      height: 28px;
      min-width: 28px;
      border-radius: 999px;
      color: rgba(255,255,255,0.72);
      transition: background 140ms ease, color 140ms ease;
      -webkit-tap-highlight-color: transparent;
    }
    .btn:hover { background: rgba(255,255,255,0.12); color: #fff; }
    .btn:active { background: rgba(255,255,255,0.18); }
    .btn:focus { outline: none; }
    .btn:focus-visible { outline: none; }
    .btn::-moz-focus-inner { border: 0; }
    .btn svg { width: 14px; height: 14px; display: block; }
    .btn.reset {
      font-size: 11px;
      font-weight: 500;
      letter-spacing: 0.02em;
      padding: 0 10px 0 12px;
      gap: 6px;
      color: rgba(255,255,255,0.72);
    }
    .btn.reset .kbd {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-width: 16px;
      height: 16px;
      padding: 0 4px;
      font-family: ui-monospace, "SF Mono", Menlo, Consolas, monospace;
      font-size: 10px;
      line-height: 1;
      color: rgba(255,255,255,0.88);
      background: rgba(255,255,255,0.12);
      border-radius: 4px;
    }

    .count {
      font-variant-numeric: tabular-nums;
      color: #fff;
      font-weight: 500;
      padding: 0 8px;
      min-width: 42px;
      text-align: center;
      font-size: 12px;
    }
    .count .sep { color: rgba(255,255,255,0.45); margin: 0 3px; font-weight: 400; }
    .count .total { color: rgba(255,255,255,0.55); }

    .divider {
      width: 1px;
      height: 14px;
      background: rgba(255,255,255,0.18);
      margin: 0 2px;
    }

    /* ── Thumbnail rail ──────────────────────────────────────────────────
       Fixed column on the left; each thumbnail is a static deep-clone of
       the light-DOM slide scaled into a 16:9 (or design-aspect) frame. The
       stage re-fits around it (see _fit); hidden during present / noscale
       / print so capture geometry and fullscreen output are unchanged. */
    .rail {
      position: fixed;
      left: 0;
      top: 0;
      bottom: 0;
      width: var(--deck-rail-w, 188px);
      background: #141414;
      border-right: 1px solid rgba(255,255,255,0.08);
      overflow-y: auto;
      overflow-x: hidden;
      padding: 12px 10px;
      box-sizing: border-box;
      display: flex;
      flex-direction: column;
      gap: 12px;
      z-index: 2147482500;
      scrollbar-width: thin;
      scrollbar-color: rgba(255,255,255,0.18) transparent;
    }
    .rail::-webkit-scrollbar { width: 8px; }
    .rail::-webkit-scrollbar-track { background: transparent; margin: 2px; }
    .rail::-webkit-scrollbar-thumb {
      background: rgba(255,255,255,0.18);
      border-radius: 4px;
      border: 2px solid transparent;
      background-clip: content-box;
    }
    .rail::-webkit-scrollbar-thumb:hover {
      background: rgba(255,255,255,0.28);
      border: 2px solid transparent;
      background-clip: content-box;
    }
    :host([no-rail]) .rail,
    :host([noscale]) .rail { display: none; }
    .rail[data-presenting] { display: none; }
    @media (max-width: 640px) {
      .rail, .rail-resize { display: none; }
    }
    /* User-driven show/hide (the TweaksPanel toggle) slides instead of
       popping. Transitions are gated on :host([data-rail-anim]) — set only
       for the 200ms around the toggle — so window-resize and rail-width
       drag (which also call _fit) don't lag behind the cursor. */
    .rail[data-user-hidden] { transform: translateX(-100%); }
    :host([data-rail-anim]) .rail { transition: transform 200ms cubic-bezier(.3,.7,.4,1); }
    :host([data-rail-anim]) .stage { transition: left 200ms cubic-bezier(.3,.7,.4,1); }
    :host([data-rail-anim]) .canvas { transition: transform 200ms cubic-bezier(.3,.7,.4,1); }
    /* transition shorthand replaces rather than merges — repeat the base
       .overlay opacity/transform/filter transitions so visibility changes
       during the 200ms toggle window still fade instead of popping. */
    :host([data-rail-anim]) .overlay {
      transition: margin-left 200ms cubic-bezier(.3,.7,.4,1),
                  opacity 260ms ease,
                  transform 260ms cubic-bezier(.2,.8,.2,1),
                  filter 260ms ease;
    }

    .thumb {
      position: relative;
      display: flex;
      align-items: flex-start;
      gap: 8px;
      cursor: pointer;
      user-select: none;
    }
    .thumb .num {
      width: 16px;
      flex-shrink: 0;
      font-size: 11px;
      font-weight: 500;
      text-align: right;
      color: rgba(255,255,255,0.55);
      padding-top: 2px;
      font-variant-numeric: tabular-nums;
    }
    .thumb .frame {
      position: relative;
      flex: 1;
      min-width: 0;
      aspect-ratio: var(--deck-aspect);
      background: #fff;
      border-radius: 4px;
      outline: 2px solid transparent;
      outline-offset: 0;
      overflow: hidden;
      transition: outline-color 120ms ease;
    }
    .thumb:hover .frame { outline-color: rgba(255,255,255,0.25); }
    .thumb { outline: none; }
    .thumb:focus-visible .frame { outline-color: rgba(255,255,255,0.5); }
    .thumb[data-current] .num { color: #fff; }
    .thumb[data-current] .frame { outline-color: #D97757; }
    .thumb[data-dragging] { opacity: 0.35; }
    .thumb::before {
      content: '';
      position: absolute;
      left: 24px;
      right: 0;
      height: 3px;
      border-radius: 2px;
      background: #D97757;
      opacity: 0;
      pointer-events: none;
    }
    .thumb[data-drop="before"]::before { top: -8px; opacity: 1; }
    .thumb[data-drop="after"]::before { bottom: -8px; opacity: 1; }
    .thumb[data-skip] .frame { opacity: 0.35; }
    .thumb[data-skip] .frame::after {
      content: 'Skipped';
      position: absolute;
      inset: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(0,0,0,0.45);
      color: #fff;
      font-size: 10px;
      font-weight: 500;
      letter-spacing: 0.04em;
    }

    .ctxmenu {
      position: fixed;
      min-width: 150px;
      padding: 4px;
      background: #242424;
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 7px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.45);
      z-index: 2147483100;
      display: none;
      font-size: 12px;
    }
    .ctxmenu[data-open] { display: block; }
    .ctxmenu button {
      display: block;
      width: 100%;
      appearance: none;
      border: 0;
      background: transparent;
      color: #e8e8e8;
      font: inherit;
      text-align: left;
      padding: 6px 10px;
      border-radius: 4px;
      cursor: pointer;
    }
    .ctxmenu button:hover:not(:disabled) { background: rgba(255,255,255,0.08); }
    .ctxmenu button:disabled { opacity: 0.35; cursor: default; }
    .ctxmenu hr {
      border: 0;
      border-top: 1px solid rgba(255,255,255,0.1);
      margin: 4px 2px;
    }

    .rail-resize {
      position: fixed;
      left: calc(var(--deck-rail-w, 188px) - 3px);
      top: 0;
      bottom: 0;
      width: 6px;
      cursor: col-resize;
      z-index: 2147482600;
      touch-action: none;
    }
    .rail-resize:hover,
    .rail-resize[data-dragging] { background: rgba(255,255,255,0.12); }
    :host([no-rail]) .rail-resize,
    :host([noscale]) .rail-resize,
    .rail[data-presenting] + .rail-resize,
    .rail[data-user-hidden] + .rail-resize { display: none; }

    /* Delete-confirm popup — matches the SPA's ConfirmDialog layout
       (title + message body, depressed footer with Cancel / Delete). */
    .confirm-backdrop {
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.45);
      z-index: 2147483200;
      display: none;
      align-items: center;
      justify-content: center;
    }
    .confirm-backdrop[data-open] { display: flex; }
    .confirm {
      width: 320px;
      max-width: calc(100vw - 32px);
      background: #2a2a2a;
      color: #e8e8e8;
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 12px;
      box-shadow: 0 12px 32px rgba(0,0,0,0.5);
      overflow: hidden;
      font-family: inherit;
      animation: deck-confirm-in 0.18s ease;
    }
    @keyframes deck-confirm-in {
      from { opacity: 0; transform: scale(0.96); }
      to { opacity: 1; transform: scale(1); }
    }
    .confirm .body { padding: 20px 20px 16px; }
    .confirm .title { font-size: 14px; font-weight: 600; margin-bottom: 4px; }
    .confirm .msg { font-size: 13px; line-height: 1.5; color: rgba(255,255,255,0.65); }
    .confirm .footer {
      padding: 14px 20px;
      background: #1f1f1f;
      border-top: 1px solid rgba(255,255,255,0.08);
      display: flex;
      justify-content: flex-end;
      gap: 8px;
    }
    .confirm button {
      appearance: none;
      font: inherit;
      font-size: 13px;
      font-weight: 500;
      padding: 8px 16px;
      border-radius: 8px;
      cursor: pointer;
    }
    .confirm .cancel {
      background: transparent;
      border: 0;
      color: rgba(255,255,255,0.8);
    }
    .confirm .cancel:hover { background: rgba(255,255,255,0.08); }
    .confirm .danger {
      background: #c96442;
      border: 1px solid rgba(0,0,0,0.15);
      color: #fff;
      box-shadow: 0 1px 3px rgba(166,50,68,0.3), 0 2px 6px rgba(166,50,68,0.18);
    }
    .confirm .danger:hover { background: #b5563a; }

    /* ── Print: one page per slide, no chrome ────────────────────────────
       The screen layout stacks every slide at inset:0 inside a scaled
       canvas; for print we want them in document flow at the authored
       design size so the browser paginates one slide per sheet. The
       @page size is set from the width/height attributes via the inline
       <style id="deck-stage-print-page"> that connectedCallback injects
       into <head> (the @page at-rule has no effect inside shadow DOM). */
    @media print {
      :host {
        position: static;
        inset: auto;
        background: none;
        overflow: visible;
        color: inherit;
      }
      .stage { position: static; display: block; }
      .canvas {
        transform: none !important;
        width: auto !important;
        height: auto !important;
        background: none;
        will-change: auto;
      }
      ::slotted(*) {
        position: relative !important;
        inset: auto !important;
        width: var(--deck-design-w) !important;
        height: var(--deck-design-h) !important;
        box-sizing: border-box !important;
        opacity: 1 !important;
        visibility: visible !important;
        pointer-events: auto;
        break-after: page;
        page-break-after: always;
        break-inside: avoid;
        overflow: hidden;
      }
      /* :last-child alone isn't enough once data-deck-skip hides the
         trailing slide(s) — the last *visible* slide still carries
         break-after:page and prints a blank sheet. _markLastVisible()
         maintains data-deck-last-visible on the last non-skipped slide. */
      ::slotted(*:last-child),
      ::slotted([data-deck-last-visible]) {
        break-after: auto;
        page-break-after: auto;
      }
      ::slotted([data-deck-skip]) { display: none !important; }
      .overlay, .rail, .rail-resize, .ctxmenu, .confirm-backdrop { display: none !important; }
    }
  `;
  class DeckStage extends HTMLElement {
    static get observedAttributes() {
      return ['width', 'height', 'noscale', 'no-rail'];
    }
    constructor() {
      super();
      this._root = this.attachShadow({
        mode: 'open'
      });
      this._index = 0;
      this._slides = [];
      this._notes = [];
      this._hideTimer = null;
      this._mouseIdleTimer = null;
      this._menuIndex = -1;
      this._onKey = this._onKey.bind(this);
      this._onResize = this._onResize.bind(this);
      this._onSlotChange = this._onSlotChange.bind(this);
      this._onMouseMove = this._onMouseMove.bind(this);
      this._onTap = this._onTap.bind(this);
      this._onMessage = this._onMessage.bind(this);
      // Capture-phase close so a click anywhere dismisses the menu, but
      // ignore clicks that land inside the menu itself — otherwise the
      // capture handler runs before the menu's own (bubble) handler and
      // clears _menuIndex out from under it.
      this._onDocClick = e => {
        if (this._menu && e.composedPath && e.composedPath().includes(this._menu)) return;
        this._closeMenu();
      };
    }
    get designWidth() {
      return parseInt(this.getAttribute('width'), 10) || DESIGN_W_DEFAULT;
    }
    get designHeight() {
      return parseInt(this.getAttribute('height'), 10) || DESIGN_H_DEFAULT;
    }
    connectedCallback() {
      // Presenter-view popup loads deckUrl?_snthumb=...#N for its prev/cur/
      // next thumbnails — the rail has no business rendering inside those
      // (wrong scale, and it offsets the stage so the thumb shows a gutter).
      if (/[?&]_snthumb=/.test(location.search)) this.setAttribute('no-rail', '');
      this._render();
      this._loadNotes();
      this._syncPrintPageRule();
      window.addEventListener('keydown', this._onKey);
      window.addEventListener('resize', this._onResize);
      window.addEventListener('mousemove', this._onMouseMove, {
        passive: true
      });
      window.addEventListener('message', this._onMessage);
      window.addEventListener('click', this._onDocClick, true);
      this.addEventListener('click', this._onTap);
      // Initial collection + layout happens via slotchange, which fires on mount.
      this._enableRail();
      // Hold the stage hidden until webfonts are ready so the first visible
      // paint has the deck's real typography — the :not(:defined) guard in
      // the page HTML only covers custom-element upgrade, not font load.
      // Capped so a 404'd font URL can't blank the deck indefinitely.
      this.setAttribute('data-fonts-pending', '');
      const reveal = () => this.removeAttribute('data-fonts-pending');
      // rAF first: fonts.ready is a pre-resolved promise until layout has
      // resolved the slotted text's font-family and pushed a FontFace into
      // 'loading'. Reading it here in connectedCallback (parse-time) would
      // settle the race in a microtask before any font fetch starts.
      requestAnimationFrame(() => {
        Promise.race([document.fonts ? document.fonts.ready : Promise.resolve(), new Promise(r => setTimeout(r, 2000))]).then(reveal, reveal);
      });
    }
    _enableRail() {
      // Idempotent — older host builds still post __omelette_rail_enabled.
      // no-rail guard keeps the observers/stylesheet walk off the cheap path
      // for presenter-popup thumbnail iframes (up to 9 per view).
      if (this._railEnabled || this.hasAttribute('no-rail')) return;
      this._railEnabled = true;
      // Per-viewer preference — restored alongside rail width. Default on;
      // only a stored '0' (from the TweaksPanel toggle) hides it.
      this._railVisible = true;
      try {
        if (localStorage.getItem('deck-stage.railVisible') === '0') this._railVisible = false;
      } catch (e) {}
      // Live thumbnail updates: watch the light-DOM slides for content
      // edits and re-clone just the affected thumb(s), debounced. Ignore
      // the data-deck-* / data-screen-label / data-om-validate attributes
      // this component itself writes so nav and skip don't trigger
      // spurious refreshes.
      const OWN_ATTRS = /^data-(deck-|screen-label$|om-validate$)/;
      this._liveDirty = new Set();
      this._liveObserver = new MutationObserver(records => {
        for (const r of records) {
          if (r.type === 'attributes' && OWN_ATTRS.test(r.attributeName || '')) continue;
          let n = r.target;
          while (n && n.parentElement !== this) n = n.parentElement;
          if (n && this._slideSet && this._slideSet.has(n)) this._liveDirty.add(n);
        }
        if (this._liveDirty.size && !this._liveTimer) {
          this._liveTimer = setTimeout(() => {
            this._liveTimer = null;
            this._liveDirty.forEach(s => this._refreshThumb(s));
            this._liveDirty.clear();
          }, 200);
        }
      });
      this._liveObserver.observe(this, {
        subtree: true,
        childList: true,
        characterData: true,
        attributes: true
      });
      // Lazy thumbnail materialization — clone the slide only when its
      // frame scrolls into (or near) the rail viewport. rootMargin gives
      // ~4 thumbs of pre-load so fast scrolling doesn't flash blanks.
      this._railObserver = new IntersectionObserver(entries => {
        entries.forEach(e => {
          if (e.isIntersecting && e.target.__deckThumb) {
            this._materialize(e.target.__deckThumb);
          }
        });
      }, {
        root: this._rail,
        rootMargin: '400px 0px'
      });
      // Tweaks typically change CSS vars / attrs OUTSIDE <deck-stage>
      // (on <html>, <body>, a wrapper div, or a <style> tag), which
      // _liveObserver can't see. Re-snapshot author CSS (constructable
      // sheet is shared by reference, so one replaceSync updates every
      // thumb shadow root) and re-sync each thumb host's attrs + custom
      // properties. In-slide DOM mutations are _liveObserver's job.
      // Debounced so slider drags don't thrash.
      this._onTweakChange = () => {
        clearTimeout(this._tweakTimer);
        this._tweakTimer = setTimeout(() => {
          this._snapshotAuthorCss();
          // One getComputedStyle for the whole batch — each
          // getPropertyValue read below reuses the same computed style
          // as long as nothing invalidates layout between thumbs.
          const cs = getComputedStyle(this);
          (this._thumbs || []).forEach(t => {
            if (t.host) this._syncThumbHostAttrs(t.host, cs);
          });
        }, 120);
      };
      window.addEventListener('tweakchange', this._onTweakChange);
      this._snapshotAuthorCss();
      // Build the rail now that it's enabled — slotchange already fired,
      // so _renderRail's early-return skipped the initial build.
      this._syncRailHidden();
      this._renderRail();
      this._fit();
    }

    /** Snapshot document stylesheets into a constructable sheet that each
     *  thumbnail's nested shadow root adopts — so author CSS styles the
     *  cloned slide content without touching this component's chrome.
     *  Cross-origin sheets throw on .cssRules — skip them. Re-callable:
     *  the existing constructable sheet is reused via replaceSync so every
     *  already-adopted shadow root picks up the fresh CSS without re-adopt. */
    _snapshotAuthorCss() {
      // :root in an adopted sheet inside a shadow root matches nothing
      // (only the document root qualifies), so author rules like
      // `:root[data-voice="modern"] .serif` never reach the clones.
      // Rewrite :root → :host and mirror <html>'s data-*/class/lang onto
      // each thumb host (see _syncThumbHostAttrs) so the same selectors
      // match inside the thumbnail's shadow tree.
      const authorCss = Array.from(document.styleSheets).map(sh => {
        try {
          return Array.from(sh.cssRules).map(r => r.cssText).join('\n');
        } catch (e) {
          return '';
        }
      }).join('\n')
      // The shadow host is featureless outside the functional :host(...)
      // form, so any compound on :root — [attr], .class, #id, :pseudo —
      // must become :host(<compound>) not :host<compound>. Same for the
      // html type selector (Tailwind class-strategy dark mode emits
      // html.dark; Pico uses html[data-theme]), which has nothing to
      // match inside the thumb's shadow tree.
      .replace(/:root((?:\[[^\]]*\]|[.#][-\w]+|:[-\w]+(?:\([^)]*\))?)+)/g, ':host($1)').replace(/:root\b/g, ':host').replace(/(^|[\s,>~+(}])html((?:\[[^\]]*\]|[.#][-\w]+|:[-\w]+(?:\([^)]*\))?)+)(?![-\w])/g, '$1:host($2)').replace(/(^|[\s,>~+(}])html(?![-\w])/g, '$1:host');
      // Every custom property the author references. _syncThumbHostAttrs
      // mirrors each one's *computed* value at <deck-stage> onto the
      // thumb host so the live value wins over the :host default above
      // regardless of which ancestor the tweak wrote to (<html>, <body>,
      // a wrapper div, or the deck-stage element itself all inherit
      // down to getComputedStyle(this)).
      this._authorVars = new Set(authorCss.match(/--[\w-]+/g) || []);
      try {
        if (!this._adoptedSheet) this._adoptedSheet = new CSSStyleSheet();
        this._adoptedSheet.replaceSync(authorCss);
      } catch (e) {
        this._adoptedSheet = null;
        this._authorCss = authorCss;
      }
    }
    _syncThumbHostAttrs(host, cs) {
      const de = document.documentElement;
      // setAttribute overwrites but can't delete — an attr removed from
      // <html> (toggleAttribute off, classList emptied) would linger on
      // the host and :host([data-*]) / :host(.foo) rules would keep
      // matching. Remove stale mirrored attrs first; iterate backward
      // because removeAttribute mutates the live NamedNodeMap.
      for (let i = host.attributes.length - 1; i >= 0; i--) {
        const n = host.attributes[i].name;
        if ((n.startsWith('data-') || n === 'class' || n === 'lang') && !de.hasAttribute(n)) {
          host.removeAttribute(n);
        }
      }
      for (const a of de.attributes) {
        if (a.name.startsWith('data-') || a.name === 'class' || a.name === 'lang') {
          host.setAttribute(a.name, a.value);
        }
      }
      // The :root→:host rewrite in _snapshotAuthorCss pins each custom
      // property to its stylesheet default on the thumb host, shadowing
      // the live value that would otherwise inherit. Tweaks can write the
      // live value on any ancestor — <html>, <body>, a wrapper div, the
      // deck-stage element — so read it as the *computed* value at
      // <deck-stage> (which sees the whole inheritance chain) rather than
      // trying to guess which element the author wrote to. Inline on the
      // host beats the :host{} rule. remove-stale covers vars dropped
      // from the stylesheet between snapshots.
      const vars = this._authorVars || new Set();
      for (let i = host.style.length - 1; i >= 0; i--) {
        const p = host.style[i];
        if (p.startsWith('--') && !vars.has(p)) host.style.removeProperty(p);
      }
      const live = cs || getComputedStyle(this);
      vars.forEach(p => {
        const v = live.getPropertyValue(p);
        if (v) host.style.setProperty(p, v.trim());else host.style.removeProperty(p);
      });
    }
    disconnectedCallback() {
      window.removeEventListener('keydown', this._onKey);
      window.removeEventListener('resize', this._onResize);
      window.removeEventListener('mousemove', this._onMouseMove);
      window.removeEventListener('message', this._onMessage);
      window.removeEventListener('click', this._onDocClick, true);
      this.removeEventListener('click', this._onTap);
      if (this._hideTimer) clearTimeout(this._hideTimer);
      if (this._mouseIdleTimer) clearTimeout(this._mouseIdleTimer);
      if (this._liveTimer) clearTimeout(this._liveTimer);
      if (this._tweakTimer) clearTimeout(this._tweakTimer);
      if (this._railAnimTimer) clearTimeout(this._railAnimTimer);
      if (this._scaleRaf) cancelAnimationFrame(this._scaleRaf);
      if (this._liveObserver) this._liveObserver.disconnect();
      if (this._railObserver) this._railObserver.disconnect();
      if (this._onTweakChange) window.removeEventListener('tweakchange', this._onTweakChange);
    }
    attributeChangedCallback() {
      if (this._canvas) {
        this._canvas.style.width = this.designWidth + 'px';
        this._canvas.style.height = this.designHeight + 'px';
        this._canvas.style.setProperty('--deck-design-w', this.designWidth + 'px');
        this._canvas.style.setProperty('--deck-design-h', this.designHeight + 'px');
        if (this._rail) {
          this._rail.style.setProperty('--deck-aspect', this.designWidth + '/' + this.designHeight);
        }
        this._fit();
        this._scaleThumbs();
        this._syncPrintPageRule();
      }
    }
    _render() {
      const style = document.createElement('style');
      style.textContent = stylesheet;
      const stage = document.createElement('div');
      stage.className = 'stage';
      const canvas = document.createElement('div');
      canvas.className = 'canvas';
      canvas.style.width = this.designWidth + 'px';
      canvas.style.height = this.designHeight + 'px';
      canvas.style.setProperty('--deck-design-w', this.designWidth + 'px');
      canvas.style.setProperty('--deck-design-h', this.designHeight + 'px');
      const slot = document.createElement('slot');
      slot.addEventListener('slotchange', this._onSlotChange);
      canvas.appendChild(slot);
      stage.appendChild(canvas);

      // Overlay: compact, solid black, with clickable controls.
      const overlay = document.createElement('div');
      overlay.className = 'overlay export-hidden';
      overlay.setAttribute('role', 'toolbar');
      overlay.setAttribute('aria-label', 'Deck controls');
      overlay.setAttribute('data-omelette-chrome', '');
      overlay.innerHTML = `
        <button class="btn prev" type="button" aria-label="Previous slide" title="Previous (←)">
          <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M10 3L5 8l5 5"/></svg>
        </button>
        <span class="count" aria-live="polite"><span class="current">1</span><span class="sep">/</span><span class="total">1</span></span>
        <button class="btn next" type="button" aria-label="Next slide" title="Next (→)">
          <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M6 3l5 5-5 5"/></svg>
        </button>
        <span class="divider"></span>
        <button class="btn reset" type="button" aria-label="Reset to first slide" title="Reset (R)">Reset<span class="kbd">R</span></button>
      `;
      overlay.querySelector('.prev').addEventListener('click', () => this._advance(-1, 'click'));
      overlay.querySelector('.next').addEventListener('click', () => this._advance(1, 'click'));
      overlay.querySelector('.reset').addEventListener('click', () => this._go(0, 'click'));

      // Thumbnail rail + context menu. Thumbnails are populated in
      // _renderRail() after _collectSlides().
      const rail = document.createElement('div');
      rail.className = 'rail export-hidden';
      rail.setAttribute('data-omelette-chrome', '');
      rail.style.setProperty('--deck-aspect', this.designWidth + '/' + this.designHeight);
      // Edge auto-scroll while dragging a thumb near the rail's top/bottom
      // so off-screen drop targets are reachable. Native dragover fires
      // continuously while the pointer is stationary, so a per-event nudge
      // (ramped by edge proximity) is enough — no rAF loop needed.
      rail.addEventListener('dragover', e => {
        if (this._dragFrom == null) return;
        const r = rail.getBoundingClientRect();
        const EDGE = 40;
        const dt = e.clientY - r.top;
        const db = r.bottom - e.clientY;
        if (dt < EDGE) rail.scrollTop -= Math.ceil((EDGE - dt) / 3);else if (db < EDGE) rail.scrollTop += Math.ceil((EDGE - db) / 3);
      });
      const menu = document.createElement('div');
      menu.className = 'ctxmenu export-hidden';
      menu.setAttribute('data-omelette-chrome', '');
      menu.innerHTML = `
        <button type="button" data-act="skip">Skip slide</button>
        <button type="button" data-act="up">Move up</button>
        <button type="button" data-act="down">Move down</button>
        <hr>
        <button type="button" data-act="delete">Delete slide</button>
      `;
      menu.addEventListener('click', e => {
        const act = e.target && e.target.getAttribute && e.target.getAttribute('data-act');
        if (!act) return;
        const i = this._menuIndex;
        this._closeMenu();
        if (act === 'skip') this._toggleSkip(i);else if (act === 'up') this._moveSlide(i, i - 1);else if (act === 'down') this._moveSlide(i, i + 1);else if (act === 'delete') this._openConfirm(i);
      });
      menu.addEventListener('contextmenu', e => e.preventDefault());

      // Rail resize handle — drag to set --deck-rail-w, persisted to
      // localStorage so the width survives reloads.
      const resize = document.createElement('div');
      resize.className = 'rail-resize export-hidden';
      resize.setAttribute('data-omelette-chrome', '');
      resize.addEventListener('pointerdown', e => {
        e.preventDefault();
        resize.setPointerCapture(e.pointerId);
        resize.setAttribute('data-dragging', '');
        const move = ev => this._setRailWidth(ev.clientX);
        const up = () => {
          resize.removeEventListener('pointermove', move);
          resize.removeEventListener('pointerup', up);
          resize.removeEventListener('pointercancel', up);
          resize.removeAttribute('data-dragging');
          try {
            localStorage.setItem('deck-stage.railWidth', String(this._railPx));
          } catch (err) {}
        };
        resize.addEventListener('pointermove', move);
        resize.addEventListener('pointerup', up);
        resize.addEventListener('pointercancel', up);
      });

      // Delete-confirm dialog — mirrors the SPA's ConfirmDialog layout.
      const confirm = document.createElement('div');
      confirm.className = 'confirm-backdrop export-hidden';
      confirm.setAttribute('data-omelette-chrome', '');
      confirm.innerHTML = `
        <div class="confirm" role="dialog" aria-modal="true">
          <div class="body">
            <div class="title">Delete slide?</div>
            <div class="msg">This slide will be removed from the deck.</div>
          </div>
          <div class="footer">
            <button type="button" class="cancel">Cancel</button>
            <button type="button" class="danger">Delete</button>
          </div>
        </div>
      `;
      confirm.addEventListener('click', e => {
        if (e.target === confirm) this._closeConfirm();
      });
      confirm.querySelector('.cancel').addEventListener('click', () => this._closeConfirm());
      confirm.querySelector('.danger').addEventListener('click', () => {
        const i = this._confirmIndex;
        this._closeConfirm();
        this._deleteSlide(i);
      });
      this._root.append(style, rail, resize, stage, overlay, menu, confirm);
      this._canvas = canvas;
      this._stage = stage;
      this._slot = slot;
      this._overlay = overlay;
      this._rail = rail;
      this._resize = resize;
      this._menu = menu;
      this._confirm = confirm;
      this._countEl = overlay.querySelector('.current');
      this._totalEl = overlay.querySelector('.total');

      // Restore persisted rail width.
      let rw = 188;
      try {
        const s = localStorage.getItem('deck-stage.railWidth');
        if (s) rw = parseInt(s, 10) || rw;
      } catch (err) {}
      this._setRailWidth(rw);
      this._syncRailHidden();
    }
    _setRailWidth(px) {
      const w = Math.max(120, Math.min(360, Math.round(px)));
      this._railPx = w;
      this.style.setProperty('--deck-rail-w', w + 'px');
      this._fit();
      // _scaleThumbs forces a sync layout (frame.offsetWidth) then writes
      // N transforms. During a resize drag this runs per-pointermove;
      // coalesce to one per frame.
      if (!this._scaleRaf) {
        this._scaleRaf = requestAnimationFrame(() => {
          this._scaleRaf = null;
          this._scaleThumbs();
        });
      }
    }

    /** @page must live in the document stylesheet — it's a no-op inside
     *  shadow DOM. Inject/update a single <head> style tag so the print
     *  sheet matches the design size and Save-as-PDF yields one slide per
     *  page with no margins. */
    _syncPrintPageRule() {
      const id = 'deck-stage-print-page';
      let tag = document.getElementById(id);
      if (!tag) {
        tag = document.createElement('style');
        tag.id = id;
        document.head.appendChild(tag);
      }
      tag.textContent = '@page { size: ' + this.designWidth + 'px ' + this.designHeight + 'px; margin: 0; } ' + '@media print { html, body { margin: 0 !important; padding: 0 !important; background: none !important; overflow: visible !important; height: auto !important; } ' + '* { -webkit-print-color-adjust: exact; print-color-adjust: exact; } }';
    }
    _onSlotChange() {
      // Rail mutations (delete/move) already reconcile synchronously and
      // emit slidechange with reason 'api'; skip the async slotchange that
      // would otherwise re-broadcast with reason 'init'.
      if (this._squelchSlotChange) {
        this._squelchSlotChange = false;
        return;
      }
      this._collectSlides();
      this._restoreIndex();
      this._applyIndex({
        showOverlay: false,
        broadcast: true,
        reason: 'init'
      });
      this._fit();
    }
    _collectSlides() {
      const assigned = this._slot.assignedElements({
        flatten: true
      });
      this._slides = assigned.filter(el => {
        // Skip template/style/script nodes even if someone slots them.
        const tag = el.tagName;
        return tag !== 'TEMPLATE' && tag !== 'SCRIPT' && tag !== 'STYLE';
      });
      this._slideSet = new Set(this._slides);
      this._slides.forEach((slide, i) => {
        const n = i + 1;
        slide.setAttribute('data-screen-label', `${pad2(n)} ${getSlideLabel(slide)}`);

        // Validation attribute for comment flow / auto-checks.
        if (!slide.hasAttribute('data-om-validate')) {
          slide.setAttribute('data-om-validate', VALIDATE_ATTR);
        }
        slide.setAttribute('data-deck-slide', String(i));
      });
      if (this._totalEl) this._totalEl.textContent = String(this._slides.length || 1);
      if (this._index >= this._slides.length) this._index = Math.max(0, this._slides.length - 1);
      this._markLastVisible();
      this._renderRail();
    }

    /** Tag the last non-skipped slide so print CSS can drop its
     *  break-after (see the @media print comment above — :last-child
     *  alone matches a hidden skipped slide). */
    _markLastVisible() {
      let last = null;
      this._slides.forEach(s => {
        s.removeAttribute('data-deck-last-visible');
        if (!s.hasAttribute('data-deck-skip')) last = s;
      });
      if (last) last.setAttribute('data-deck-last-visible', '');
    }
    _loadNotes() {
      const tag = document.getElementById('speaker-notes');
      if (!tag) {
        this._notes = [];
        return;
      }
      try {
        const parsed = JSON.parse(tag.textContent || '[]');
        if (Array.isArray(parsed)) this._notes = parsed;
      } catch (e) {
        console.warn('[deck-stage] Failed to parse #speaker-notes JSON:', e);
        this._notes = [];
      }
    }
    _restoreIndex() {
      // The host's ?slide= param is delivered as a #<int> hash (1-indexed) on
      // the iframe src. No hash → slide 1; the deck itself keeps no position
      // state across loads.
      const h = (location.hash || '').match(/^#(\d+)$/);
      if (h) {
        const n = parseInt(h[1], 10) - 1;
        if (n >= 0 && n < this._slides.length) this._index = n;
      }
    }
    _applyIndex({
      showOverlay = true,
      broadcast = true,
      reason = 'init'
    } = {}) {
      if (!this._slides.length) return;
      const prev = this._prevIndex == null ? -1 : this._prevIndex;
      const curr = this._index;
      // Keep the iframe's own hash in sync so an in-iframe location.reload()
      // (reload banner path in viewer-handle.ts) lands on the current slide,
      // not the stale deep-link hash from initial load.
      try {
        history.replaceState(null, '', '#' + (curr + 1));
      } catch (e) {}
      this._slides.forEach((s, i) => {
        if (i === curr) s.setAttribute('data-deck-active', '');else s.removeAttribute('data-deck-active');
      });
      if (this._countEl) this._countEl.textContent = String(curr + 1);
      // Follow-scroll on every navigation (init deep-link, keyboard, click,
      // tap, external goTo) — the only time we *don't* want the rail to
      // track current is after a rail-internal mutation, where _renderRail
      // has already restored the user's scroll position and yanking back to
      // current would undo it.
      this._syncRail(reason !== 'mutation');
      if (broadcast) {
        // (1) Legacy: host-window postMessage for speaker-notes renderers.
        try {
          window.postMessage({
            slideIndexChanged: curr,
            deckTotal: this._slides.length,
            deckSkipped: this._skippedIndices()
          }, '*');
        } catch (e) {}

        // (2) In-page CustomEvent on the <deck-stage> element itself.
        //     Bubbles and composes out of shadow DOM so slide code can listen:
        //       document.querySelector('deck-stage').addEventListener('slidechange', e => {
        //         e.detail.index, e.detail.previousIndex, e.detail.total, e.detail.slide, e.detail.reason
        //       });
        const detail = {
          index: curr,
          previousIndex: prev,
          total: this._slides.length,
          slide: this._slides[curr] || null,
          previousSlide: prev >= 0 ? this._slides[prev] || null : null,
          reason: reason // 'init' | 'keyboard' | 'click' | 'tap' | 'api'
        };
        this.dispatchEvent(new CustomEvent('slidechange', {
          detail,
          bubbles: true,
          composed: true
        }));
      }
      this._prevIndex = curr;
      if (showOverlay) this._flashOverlay();
    }
    _flashOverlay() {
      // Host posts __omelette_presenting while in fullscreen/tab presentation
      // mode — suppress the nav footer entirely (both hover and slide-change
      // flash) so the audience sees clean slides.
      if (!this._overlay || this._presenting) return;
      this._overlay.setAttribute('data-visible', '');
      if (this._hideTimer) clearTimeout(this._hideTimer);
      this._hideTimer = setTimeout(() => {
        this._overlay.removeAttribute('data-visible');
      }, OVERLAY_HIDE_MS);
    }
    _railWidth() {
      // State-based, no offsetWidth: the first _fit() can run before the
      // rail has had layout on some load paths, and a 0 there paints the
      // slide full-width for one frame before the post-slotchange _fit()
      // corrects it.
      if (!this._railEnabled || !this._railVisible || this.hasAttribute('no-rail') || this.hasAttribute('noscale') || this._presenting || this._previewMode || NARROW_MQ.matches) return 0;
      return this._railPx || 0;
    }
    _fit() {
      if (!this._canvas) return;
      const stage = this._canvas.parentElement;
      // PPTX export sets noscale so the DOM capture sees authored-size
      // geometry — the scaled canvas is in shadow DOM, so the exporter's
      // resetTransformSelector can't reach .canvas.style.transform directly.
      if (this.hasAttribute('noscale')) {
        this._canvas.style.transform = 'none';
        if (stage) stage.style.left = '0';
        if (this._overlay) this._overlay.style.marginLeft = '0';
        return;
      }
      const rw = this._railWidth();
      if (stage) stage.style.left = rw + 'px';
      // Overlay is centred on the viewport via left:50% + translate(-50%);
      // marginLeft shifts the centre by rw/2 so it lands in the middle of
      // the [rw, innerWidth] stage region.
      if (this._overlay) this._overlay.style.marginLeft = rw / 2 + 'px';
      const vw = window.innerWidth - rw;
      const vh = window.innerHeight;
      const s = Math.min(vw / this.designWidth, vh / this.designHeight);
      this._canvas.style.transform = `scale(${s})`;
    }
    _onResize() {
      this._fit();
      // Crossing the narrow-viewport breakpoint reveals the rail — rerun the
      // thumbnail scale the same way _setRailWidth does.
      if (!this._scaleRaf) {
        this._scaleRaf = requestAnimationFrame(() => {
          this._scaleRaf = null;
          this._scaleThumbs();
        });
      }
    }
    _onMouseMove() {
      // Keep overlay visible while mouse moves; hide after idle.
      this._flashOverlay();
    }
    _onMessage(e) {
      const d = e.data;
      if (d && typeof d.__omelette_presenting === 'boolean') {
        this._presenting = d.__omelette_presenting;
        if (this._presenting && this._overlay) {
          this._overlay.removeAttribute('data-visible');
          if (this._hideTimer) clearTimeout(this._hideTimer);
        }
        this._syncRailHidden();
        this._closeMenu();
        this._closeConfirm();
        this._fit();
        this._scaleThumbs();
      }
      // Host's Preview segment (ViewerMode='none'): the rail's drag-reorder /
      // right-click skip-delete affordances are editing chrome, so hide it
      // while the user is just looking at the deck. Same hard-hide path as
      // presenting; independent of the user's _railVisible preference so
      // returning to Edit restores whatever they had.
      if (d && typeof d.__omelette_preview_mode === 'boolean') {
        if (d.__omelette_preview_mode === this._previewMode) return;
        this._previewMode = d.__omelette_preview_mode;
        this._syncRailHidden();
        this._closeMenu();
        this._closeConfirm();
        this._fit();
        this._scaleThumbs();
      }
      // Per-viewer show/hide, driven by the TweaksPanel's auto-injected
      // "Thumbnail rail" toggle (or any author script). Independent of
      // whether the Tweaks panel itself is open — closing the panel
      // doesn't change rail visibility. Persists alongside rail width.
      if (d && d.type === '__deck_rail_visible' && typeof d.on === 'boolean') {
        if (d.on === this._railVisible) return;
        this._railVisible = d.on;
        try {
          localStorage.setItem('deck-stage.railVisible', d.on ? '1' : '0');
        } catch (e) {}
        // Arm the transition, commit it, then flip state — otherwise the
        // browser coalesces both writes and nothing animates on show.
        this.setAttribute('data-rail-anim', '');
        void (this._rail && this._rail.offsetHeight);
        this._syncRailHidden();
        this._fit();
        this._scaleThumbs();
        clearTimeout(this._railAnimTimer);
        this._railAnimTimer = setTimeout(() => this.removeAttribute('data-rail-anim'), 220);
      }
      if (d && d.type === '__omelette_rail_enabled') this._enableRail();
    }
    _syncRailHidden() {
      if (!this._rail) return;
      // data-presenting is the hard hide (display:none) for flag-off,
      // presentation mode, and the host's Preview segment — instant, no
      // transition. data-user-hidden is the soft hide (translateX(-100%))
      // for the viewer's rail toggle, so show/hide slides under
      // :host([data-rail-anim]).
      const hard = !this._railEnabled || this._presenting || this._previewMode;
      if (hard) this._rail.setAttribute('data-presenting', '');else this._rail.removeAttribute('data-presenting');
      if (!this._railVisible) this._rail.setAttribute('data-user-hidden', '');else this._rail.removeAttribute('data-user-hidden');
      // translateX hide leaves thumbs (tabIndex=0) in the tab order —
      // inert keeps them unfocusable while the rail is off-screen.
      this._rail.inert = hard || !this._railVisible;
    }
    _onTap(e) {
      // Touch-only — keyboard + the overlay toolbar cover nav on desktop.
      if (FINE_POINTER_MQ.matches) return;
      // Only taps that land on the stage (slide content or letterbox); the
      // overlay / rail / menus are siblings with their own click handlers.
      const path = e.composedPath();
      if (!this._stage || !path.includes(this._stage)) return;
      // Let interactive slide content keep the tap. composedPath (not
      // e.target.closest) so we see through open shadow roots — a <button>
      // inside a slide-authored custom element retargets e.target to the
      // host but still appears in the composed path.
      if (e.defaultPrevented) return;
      for (const n of path) {
        if (n === this._stage) break;
        if (n.matches && n.matches(INTERACTIVE_SEL)) return;
      }
      e.preventDefault();
      const rw = this._railWidth();
      const mid = rw + (window.innerWidth - rw) / 2;
      this._advance(e.clientX < mid ? -1 : 1, 'tap');
    }
    _onKey(e) {
      // Ignore when the user is typing.
      const t = e.target;
      if (t && (t.isContentEditable || /^(INPUT|TEXTAREA|SELECT)$/.test(t.tagName))) return;
      // Confirm dialog swallows nav keys while open; Escape cancels. Enter
      // is left to the focused button's native activation so Tab→Cancel
      // →Enter activates Cancel, not the window-level confirm path.
      if (this._confirm && this._confirm.hasAttribute('data-open')) {
        if (e.key === 'Escape') {
          this._closeConfirm();
          e.preventDefault();
        }
        return;
      }
      if (e.key === 'Escape' && this._menu && this._menu.hasAttribute('data-open')) {
        this._closeMenu();
        e.preventDefault();
        return;
      }
      if (e.metaKey || e.ctrlKey || e.altKey) return;
      const key = e.key;
      let handled = true;
      if (key === 'ArrowRight' || key === 'PageDown' || key === ' ' || key === 'Spacebar') {
        this._advance(1, 'keyboard');
      } else if (key === 'ArrowLeft' || key === 'PageUp') {
        this._advance(-1, 'keyboard');
      } else if (key === 'Home') {
        this._go(0, 'keyboard');
      } else if (key === 'End') {
        this._go(this._slides.length - 1, 'keyboard');
      } else if (key === 'r' || key === 'R') {
        this._go(0, 'keyboard');
      } else if (/^[0-9]$/.test(key)) {
        // 1..9 jump to that slide; 0 jumps to 10.
        const n = key === '0' ? 9 : parseInt(key, 10) - 1;
        if (n < this._slides.length) this._go(n, 'keyboard');
      } else {
        handled = false;
      }
      if (handled) {
        e.preventDefault();
        this._flashOverlay();
      }
    }
    _go(i, reason = 'api') {
      if (!this._slides.length) return;
      const clamped = Math.max(0, Math.min(this._slides.length - 1, i));
      if (clamped === this._index) {
        this._flashOverlay();
        return;
      }
      this._index = clamped;
      this._applyIndex({
        showOverlay: true,
        broadcast: true,
        reason
      });
    }

    /** Step forward/back skipping any slide marked data-deck-skip. Falls
     *  back to _go's clamp-at-ends behaviour (flash overlay) when there's
     *  nothing further in that direction. */
    _advance(dir, reason) {
      if (!this._slides.length) return;
      let i = this._index + dir;
      while (i >= 0 && i < this._slides.length && this._slides[i].hasAttribute('data-deck-skip')) {
        i += dir;
      }
      if (i < 0 || i >= this._slides.length) {
        this._flashOverlay();
        return;
      }
      this._go(i, reason);
    }

    // ── Thumbnail rail ────────────────────────────────────────────────────
    //
    // Thumbs are keyed by slide element and reused across _renderRail()
    // calls, so a reorder/delete is an O(changed) DOM shuffle instead of an
    // O(N) teardown-and-re-clone. Each thumb starts as a lightweight shell
    // (num + empty frame); the clone is materialized lazily by an
    // IntersectionObserver when the frame scrolls into (or near) view, so
    // only visible-ish slides pay the clone + image-decode cost.

    _renderRail() {
      if (!this._rail || !this._railEnabled) {
        this._thumbs = [];
        return;
      }
      // FLIP: record each *materialized* thumb's top before the reconcile.
      // Off-screen (non-materialized) thumbs don't need the animation and
      // skipping their getBoundingClientRect saves a forced layout per
      // off-screen thumb on large decks.
      const prevTops = new Map();
      (this._thumbs || []).forEach(({
        thumb,
        slide,
        host
      }) => {
        if (host) prevTops.set(slide, thumb.getBoundingClientRect().top);
      });
      const st = this._rail.scrollTop;

      // Reconcile: reuse thumbs that already exist for a slide, create
      // shells for new slides, drop thumbs for removed slides.
      const bySlide = new Map();
      (this._thumbs || []).forEach(t => bySlide.set(t.slide, t));
      const next = [];
      this._slides.forEach(slide => {
        let t = bySlide.get(slide);
        if (t) bySlide.delete(slide);else t = this._makeThumb(slide);
        next.push(t);
      });
      // Orphans — slides removed since last render.
      bySlide.forEach(t => {
        if (this._railObserver) this._railObserver.unobserve(t.frame);
        t.thumb.remove();
      });
      // Put thumbs into document order to match _slides. insertBefore on
      // an already-correctly-placed node is a no-op, so this is cheap
      // when nothing moved.
      next.forEach((t, i) => {
        const want = t.thumb;
        const at = this._rail.children[i];
        if (at !== want) this._rail.insertBefore(want, at || null);
        t.i = i;
        t.num.textContent = String(i + 1);
        if (t.slide.hasAttribute('data-deck-skip')) t.thumb.setAttribute('data-skip', '');else t.thumb.removeAttribute('data-skip');
      });
      this._thumbs = next;
      this._rail.scrollTop = st;
      if (prevTops.size) {
        const moved = [];
        this._thumbs.forEach(({
          thumb,
          slide
        }) => {
          const old = prevTops.get(slide);
          if (old == null) return;
          const dy = old - thumb.getBoundingClientRect().top;
          if (Math.abs(dy) < 1) return;
          thumb.style.transition = 'none';
          thumb.style.transform = `translateY(${dy}px)`;
          moved.push(thumb);
        });
        if (moved.length) {
          // Commit the inverted positions before flipping the transition
          // on — otherwise the browser coalesces both style writes and
          // nothing animates.
          void this._rail.offsetHeight;
          moved.forEach(t => {
            t.style.transition = 'transform 180ms cubic-bezier(.2,.7,.3,1)';
            t.style.transform = '';
          });
          setTimeout(() => moved.forEach(t => {
            t.style.transition = '';
          }), 220);
        }
      }
      requestAnimationFrame(() => this._scaleThumbs());
      this._syncRail(false);
    }

    /** Create a lightweight thumb shell for one slide. The clone is
     *  materialized later by the IntersectionObserver. Event handlers
     *  look up the thumb's *current* index (via _thumbs.indexOf) so the
     *  same element can be reused across reorders. */
    _makeThumb(slide) {
      const thumb = document.createElement('div');
      thumb.className = 'thumb';
      thumb.tabIndex = 0;
      const num = document.createElement('div');
      num.className = 'num';
      const frame = document.createElement('div');
      frame.className = 'frame';
      thumb.append(num, frame);
      const entry = {
        thumb,
        num,
        frame,
        slide,
        clone: null,
        host: null,
        i: -1
      };
      // entry.i is refreshed on every _renderRail reconcile pass, so
      // handlers read the thumb's current position without an O(N) scan.
      const idx = () => entry.i;
      thumb.addEventListener('click', () => this._go(idx(), 'click'));
      // ↑/↓ step through the rail when a thumb has focus. _go clamps at the
      // ends and _applyIndex→_syncRail scrolls the new current thumb into
      // view; we move focus to it (preventScroll — _syncRail already
      // scrolled) so a held key walks the whole list. stopPropagation keeps
      // this out of the window-level _onKey nav handler.
      thumb.addEventListener('keydown', e => {
        if (e.key !== 'ArrowUp' && e.key !== 'ArrowDown') return;
        if (e.metaKey || e.ctrlKey || e.altKey) return;
        e.preventDefault();
        e.stopPropagation();
        this._go(idx() + (e.key === 'ArrowDown' ? 1 : -1), 'keyboard');
        const cur = this._thumbs && this._thumbs[this._index];
        if (cur) cur.thumb.focus({
          preventScroll: true
        });
      });
      thumb.addEventListener('contextmenu', e => {
        e.preventDefault();
        this._openMenu(idx(), e.clientX, e.clientY);
      });
      thumb.draggable = true;
      thumb.addEventListener('dragstart', e => {
        this._dragFrom = idx();
        thumb.setAttribute('data-dragging', '');
        e.dataTransfer.effectAllowed = 'move';
        try {
          e.dataTransfer.setData('text/plain', String(this._dragFrom));
        } catch (err) {}
      });
      thumb.addEventListener('dragend', () => {
        thumb.removeAttribute('data-dragging');
        this._clearDrop();
        this._dragFrom = null;
      });
      thumb.addEventListener('dragover', e => {
        if (this._dragFrom == null) return;
        e.preventDefault();
        e.dataTransfer.dropEffect = 'move';
        const r = thumb.getBoundingClientRect();
        this._setDrop(idx(), e.clientY < r.top + r.height / 2 ? 'before' : 'after');
      });
      thumb.addEventListener('drop', e => {
        if (this._dragFrom == null) return;
        e.preventDefault();
        const i = idx();
        const r = thumb.getBoundingClientRect();
        let to = e.clientY >= r.top + r.height / 2 ? i + 1 : i;
        if (this._dragFrom < to) to--;
        const from = this._dragFrom;
        this._clearDrop();
        this._dragFrom = null;
        if (to !== from) this._moveSlide(from, to);
      });
      if (this._railObserver) this._railObserver.observe(frame);
      frame.__deckThumb = entry;
      return entry;
    }

    /** Lazily build the clone for a thumb that has scrolled into view. */
    _materialize(entry) {
      if (entry.host) return;
      const dw = this.designWidth,
        dh = this.designHeight;
      let clone = entry.slide.cloneNode(true);
      clone.removeAttribute('id');
      clone.removeAttribute('data-deck-active');
      clone.querySelectorAll('[id]').forEach(el => el.removeAttribute('id'));
      // Neuter heavy media; replace <video> with its poster so the box
      // keeps a visual. <iframe>/<audio> become empty placeholders.
      clone.querySelectorAll('iframe, audio, object, embed').forEach(el => {
        el.removeAttribute('src');
        el.removeAttribute('srcdoc');
        el.removeAttribute('data');
        el.innerHTML = '';
      });
      clone.querySelectorAll('video').forEach(el => {
        if (!el.poster) {
          el.removeAttribute('src');
          el.innerHTML = '';
          return;
        }
        const img = document.createElement('img');
        img.src = el.poster;
        img.alt = '';
        img.style.cssText = el.style.cssText + ';object-fit:cover;width:100%;height:100%;';
        img.className = el.className;
        el.replaceWith(img);
      });
      // Images: defer decode and let the browser pick the smallest
      // srcset candidate for the ~140px thumb. Same-URL clones reuse the
      // slide's decoded bitmap (URL-keyed cache), so the remaining cost
      // is paint/composite — lazy+async keeps that off the main thread.
      clone.querySelectorAll('img').forEach(el => {
        el.loading = 'lazy';
        el.decoding = 'async';
        if (el.srcset) el.sizes = (this._railPx || 188) + 'px';
      });
      // Custom elements inside the slide would have their
      // connectedCallback fire when the clone is appended. Replace them
      // with inert boxes so a component-heavy deck doesn't run N copies
      // of each component's mount logic in the rail. Children are
      // preserved so layout-wrapper elements (<my-column><h2>…</h2>)
      // still show their authored content; the querySelectorAll NodeList
      // is static, so nested custom elements in the moved subtree are
      // still visited on later iterations.
      const neuter = el => {
        const box = document.createElement('div');
        box.style.cssText = (el.getAttribute('style') || '') + ';background:rgba(0,0,0,0.06);border:1px dashed rgba(0,0,0,0.15);';
        box.className = el.className;
        // Preserve theming/i18n hooks so [data-*] / :lang() / [dir]
        // descendant selectors still match the neutered root.
        for (const a of el.attributes) {
          const n = a.name;
          if (n.startsWith('data-') || n.startsWith('aria-') || n === 'lang' || n === 'dir' || n === 'role' || n === 'title') {
            box.setAttribute(n, a.value);
          }
        }
        while (el.firstChild) box.appendChild(el.firstChild);
        return box;
      };
      // querySelectorAll('*') returns descendants only — a custom-element
      // slide root (<my-slide>…</my-slide>) would slip through and upgrade
      // on append. Swap the root first.
      if (clone.tagName.includes('-')) clone = neuter(clone);
      clone.querySelectorAll('*').forEach(el => {
        if (el.tagName.includes('-')) el.replaceWith(neuter(el));
      });
      clone.style.cssText += ';position:absolute;top:0;left:0;transform-origin:0 0;' + 'pointer-events:none;width:' + dw + 'px;height:' + dh + 'px;' + 'box-sizing:border-box;overflow:hidden;visibility:visible;opacity:1;';
      const host = document.createElement('div');
      host.style.cssText = 'position:absolute;inset:0;';
      this._syncThumbHostAttrs(host);
      const sr = host.attachShadow({
        mode: 'open'
      });
      if (this._adoptedSheet) sr.adoptedStyleSheets = [this._adoptedSheet];else {
        const st = document.createElement('style');
        st.textContent = this._authorCss || '';
        sr.appendChild(st);
      }
      sr.appendChild(clone);
      entry.frame.appendChild(host);
      entry.host = host;
      entry.clone = clone;
      if (this._thumbScale) clone.style.transform = 'scale(' + this._thumbScale + ')';
      // Once materialized the IO callback is a no-op early-return —
      // unobserve so scroll doesn't keep firing it.
      if (this._railObserver) this._railObserver.unobserve(entry.frame);
    }

    /** Re-clone a single thumb (live-update path). No-op if the thumb
     *  hasn't been materialized yet — it'll pick up current content when
     *  it scrolls into view. */
    _refreshThumb(slide) {
      const entry = (this._thumbs || []).find(t => t.slide === slide);
      if (!entry || !entry.host) return;
      entry.host.remove();
      entry.host = entry.clone = null;
      this._materialize(entry);
    }
    _scaleThumbs() {
      if (!this._thumbs || !this._thumbs.length) return;
      // Every frame is the same width; if it reads 0 the rail is
      // display:none (noscale / no-rail / presenting / print) — leave the
      // clones as-is and re-run when the rail is revealed.
      const fw = this._thumbs[0].frame.offsetWidth;
      if (!fw) return;
      this._thumbScale = fw / this.designWidth;
      this._thumbs.forEach(({
        clone
      }) => {
        if (clone) clone.style.transform = 'scale(' + this._thumbScale + ')';
      });
    }
    _setDrop(i, where) {
      // dragover fires at pointer-event rate; touch only the previous
      // and new target rather than sweeping all N thumbs.
      const t = this._thumbs && this._thumbs[i];
      if (this._dropOn && this._dropOn !== t) {
        this._dropOn.thumb.removeAttribute('data-drop');
      }
      if (t) t.thumb.setAttribute('data-drop', where);
      this._dropOn = t || null;
    }
    _clearDrop() {
      if (this._dropOn) this._dropOn.thumb.removeAttribute('data-drop');
      this._dropOn = null;
    }
    _syncRail(follow) {
      if (!this._thumbs) return;
      this._thumbs.forEach(({
        thumb
      }, i) => {
        if (i === this._index) {
          thumb.setAttribute('data-current', '');
          if (follow && typeof thumb.scrollIntoView === 'function') {
            thumb.scrollIntoView({
              block: 'nearest'
            });
          }
        } else {
          thumb.removeAttribute('data-current');
        }
      });
    }
    _openMenu(i, x, y) {
      if (!this._menu) return;
      this._menuIndex = i;
      const slide = this._slides[i];
      const skip = slide && slide.hasAttribute('data-deck-skip');
      this._menu.querySelector('[data-act="skip"]').textContent = skip ? 'Unskip slide' : 'Skip slide';
      this._menu.querySelector('[data-act="up"]').disabled = i <= 0;
      this._menu.querySelector('[data-act="down"]').disabled = i >= this._slides.length - 1;
      this._menu.querySelector('[data-act="delete"]').disabled = this._slides.length <= 1;
      // Place, then clamp to viewport after it's measurable.
      this._menu.style.left = x + 'px';
      this._menu.style.top = y + 'px';
      this._menu.setAttribute('data-open', '');
      const r = this._menu.getBoundingClientRect();
      const nx = Math.min(x, window.innerWidth - r.width - 4);
      const ny = Math.min(y, window.innerHeight - r.height - 4);
      this._menu.style.left = Math.max(4, nx) + 'px';
      this._menu.style.top = Math.max(4, ny) + 'px';
    }
    _closeMenu() {
      if (this._menu) this._menu.removeAttribute('data-open');
      this._menuIndex = -1;
    }
    _openConfirm(i) {
      if (!this._confirm) return;
      this._confirmIndex = i;
      this._confirm.querySelector('.title').textContent = 'Delete slide ' + (i + 1) + '?';
      this._confirm.setAttribute('data-open', '');
      const btn = this._confirm.querySelector('.danger');
      if (btn && btn.focus) btn.focus();
    }
    _closeConfirm() {
      if (this._confirm) this._confirm.removeAttribute('data-open');
      this._confirmIndex = -1;
    }
    _emitDeckChange(detail) {
      this.dispatchEvent(new CustomEvent('deckchange', {
        detail,
        bubbles: true,
        composed: true
      }));
    }
    _deleteSlide(i) {
      const slide = this._slides[i];
      if (!slide || this._slides.length <= 1) return;
      const wasCurrent = i === this._index;
      if (i < this._index || wasCurrent && i === this._slides.length - 1) this._index--;
      this._squelchSlotChange = true;
      slide.remove();
      this._emitDeckChange({
        action: 'delete',
        from: i,
        slide
      });
      this._collectSlides();
      this._applyIndex({
        showOverlay: true,
        broadcast: true,
        reason: 'mutation'
      });
    }
    _toggleSkip(i) {
      const slide = this._slides[i];
      if (!slide) return;
      const on = !slide.hasAttribute('data-deck-skip');
      if (on) slide.setAttribute('data-deck-skip', '');else slide.removeAttribute('data-deck-skip');
      if (this._thumbs && this._thumbs[i]) {
        if (on) this._thumbs[i].thumb.setAttribute('data-skip', '');else this._thumbs[i].thumb.removeAttribute('data-skip');
      }
      this._markLastVisible();
      this._emitDeckChange({
        action: on ? 'skip' : 'unskip',
        from: i,
        slide
      });
      // Re-broadcast so the presenter popup's prev/next thumbnails re-pick
      // the nearest non-skipped slide without waiting for a nav event.
      try {
        window.postMessage({
          slideIndexChanged: this._index,
          deckTotal: this._slides.length,
          deckSkipped: this._skippedIndices()
        }, '*');
      } catch (e) {}
    }
    _skippedIndices() {
      const out = [];
      for (let i = 0; i < this._slides.length; i++) {
        if (this._slides[i].hasAttribute('data-deck-skip')) out.push(i);
      }
      return out;
    }
    _moveSlide(i, j) {
      if (j < 0 || j >= this._slides.length || j === i) return;
      const slide = this._slides[i];
      const ref = j < i ? this._slides[j] : this._slides[j].nextSibling;
      // Track the active slide across the reorder so the same content
      // stays on screen.
      const cur = this._index;
      if (cur === i) this._index = j;else if (i < cur && j >= cur) this._index = cur - 1;else if (i > cur && j <= cur) this._index = cur + 1;
      this._squelchSlotChange = true;
      this.insertBefore(slide, ref);
      this._emitDeckChange({
        action: 'move',
        from: i,
        to: j,
        slide
      });
      this._collectSlides();
      this._applyIndex({
        showOverlay: false,
        broadcast: true,
        reason: 'mutation'
      });
    }

    // Public API ------------------------------------------------------------

    /** Current slide index (0-based). */
    get index() {
      return this._index;
    }
    /** Total slide count. */
    get length() {
      return this._slides.length;
    }
    /** Programmatically navigate. */
    goTo(i) {
      this._go(i, 'api');
    }
    next() {
      this._advance(1, 'api');
    }
    prev() {
      this._advance(-1, 'api');
    }
    reset() {
      this._go(0, 'api');
    }
  }
  if (!customElements.get('deck-stage')) {
    customElements.define('deck-stage', DeckStage);
  }
})();
})(); } catch (e) { __ds_ns.__errors.push({ path: "lobby_deck/deck-stage.js", error: String((e && e.message) || e) }); }

// scene.jsx
try { (() => {
/* global React, Stage, Sprite, useTime, useTimeline, Easing, interpolate, animate, clamp */
// Tenet — First Experience Animation
// Long-form dialogue piece. Opens with a calm "call started" sequence,
// streams the conversation in the right column while the script outline
// organizes itself in the left workspace, and resolves into a boat on rough sea.

const DIALOGUE = window.DIALOGUE;

// ── Tokens ─────────────────────────────────────────────────────────────────
const INK = '#1A1A1A';
const INK_MUTED = '#6B6B6B';
const INK_FAINT = '#9A9A9A';
const PAPER = '#FFFFFF';
const PAPER_SOFT = '#FAF8F4';
const AMBER = '#BA7517';
const BORDER = 'rgba(0,0,0,0.10)';
const SERIF = '"Source Serif 4", "Source Serif Pro", Georgia, serif';
const SANS = '"Brown", "Avenir Next", "Helvetica Neue", system-ui, sans-serif';
// Script document font — deliberately generic, like a real screenplay file.
const SCRIPT_FONT = '"Times New Roman", Times, serif';

// ── Layout constants ───────────────────────────────────────────────────────
const W = 1920;
const H = 1080;
const TOOLBAR_X = 80;
const TOOLBAR_ICON_SIZE = 34;
const TOOLBAR_Y = {
  mic: 104,
  phone: 154,
  upload: 204,
  script: 254
};

// Timing
const T_WORDMARK_IN = 0.4;
const T_ICONS_IN = 1.2;
const T_CURSOR_IN = 2.4;
const T_CLICK = 3.6;
const T_TOOLBAR_SETTLE = 4.4; // phone/upload reach their toolbar slots
const T_LISTENING = 4.4;
const T_WAVE_IN = 4.5;
const T_DIALOGUE_START = 5.2;
const T_SCRIPT_ICON = 'computed-from-half-dialogue';
// Color zone holds back until after the conversation has had time to breathe,
// so the bottom of the frame reads solid white through the opening minute.
const SEASCAPE_START = 15.0;
const SIGNATURE_LEAD = 2.5; // seconds of hold before signature

// ── Helpers ────────────────────────────────────────────────────────────────
function easeOutCubic(t) {
  return 1 - Math.pow(1 - t, 3);
}
function easeInOutCubic(t) {
  return t < 0.5 ? 4 * t * t * t : 1 - Math.pow(-2 * t + 2, 3) / 2;
}
function easeOutQuart(t) {
  return 1 - Math.pow(1 - t, 4);
}

// Build a dialogue timeline. Each line is assigned a start time based on word count.
// `pace` is a global multiplier (1 = normal, lower = faster).
function buildDialogueTimeline(dialogue, startAt, pace = 1) {
  let cursor = startAt;
  return dialogue.map(line => {
    const words = line.t.split(/\s+/).length;
    // baseline: ~5 words/sec read rate, with min and small punctuation pause
    const dur = Math.max(0.42, words * 0.16 * pace);
    const gap = (/[.!?…]$/.test(line.t) ? 0.18 : 0.10) * pace;
    const entry = {
      ...line,
      time: cursor,
      dur
    };
    cursor += dur + gap;
    return entry;
  });
}

// ── Wordmark ───────────────────────────────────────────────────────────────
function Wordmark() {
  const t = useTime();
  const op = clamp((t - T_WORDMARK_IN) / 0.8, 0, 1);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 56,
      left: TOOLBAR_X,
      fontFamily: SERIF,
      fontStyle: 'italic',
      fontWeight: 400,
      fontSize: 21,
      letterSpacing: '0.02em',
      color: INK_FAINT,
      opacity: op
    }
  }, "tenet");
}

// ── Icons (line-drawn, single weight) ──────────────────────────────────────
function MicIcon({
  size = 26,
  stroke = INK,
  sw = 1.5
}) {
  return /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    viewBox: "0 0 32 32",
    fill: "none"
  }, /*#__PURE__*/React.createElement("rect", {
    x: "12",
    y: "4",
    width: "8",
    height: "16",
    rx: "4",
    stroke: stroke,
    strokeWidth: sw
  }), /*#__PURE__*/React.createElement("path", {
    d: "M7 14a9 9 0 0 0 18 0",
    stroke: stroke,
    strokeWidth: sw,
    strokeLinecap: "round"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M16 23v5",
    stroke: stroke,
    strokeWidth: sw,
    strokeLinecap: "round"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M12 28h8",
    stroke: stroke,
    strokeWidth: sw,
    strokeLinecap: "round"
  }));
}
function PhoneIcon({
  size = 26,
  stroke = INK,
  sw = 1.5
}) {
  return /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    viewBox: "0 0 32 32",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M7 6.5C7 5.7 7.7 5 8.5 5h3.2c.7 0 1.3.5 1.4 1.2l.7 4c.1.6-.2 1.2-.7 1.5l-2 1.2a14 14 0 0 0 7 7l1.2-2c.3-.5.9-.8 1.5-.7l4 .7c.7.1 1.2.7 1.2 1.4v3.2c0 .8-.7 1.5-1.5 1.5A18.5 18.5 0 0 1 7 6.5z",
    stroke: stroke,
    strokeWidth: sw,
    strokeLinejoin: "round"
  }));
}
function UploadIcon({
  size = 26,
  stroke = INK,
  sw = 1.5
}) {
  return /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    viewBox: "0 0 32 32",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M16 4v16M10 10l6-6 6 6",
    stroke: stroke,
    strokeWidth: sw,
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M5 22v4a2 2 0 0 0 2 2h18a2 2 0 0 0 2-2v-4",
    stroke: stroke,
    strokeWidth: sw,
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }));
}
function ScriptIcon({
  size = 26,
  stroke = INK,
  sw = 1.5
}) {
  // line-drawn document with text rules
  return /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    viewBox: "0 0 32 32",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M8 4h12l5 5v18a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2z",
    stroke: stroke,
    strokeWidth: sw,
    strokeLinejoin: "round"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M20 4v5h5",
    stroke: stroke,
    strokeWidth: sw,
    strokeLinejoin: "round"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M11 15h10M11 19h10M11 23h6",
    stroke: stroke,
    strokeWidth: sw,
    strokeLinecap: "round"
  }));
}

// ── Action-bar icons for the script window ────────────────────
function PencilIcon({
  size = 18,
  stroke = INK_MUTED,
  sw = 1.5
}) {
  return /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    viewBox: "0 0 24 24",
    fill: "none",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M14 4l6 6L8 22H2v-6L14 4z",
    stroke: stroke,
    strokeWidth: sw
  }), /*#__PURE__*/React.createElement("path", {
    d: "M13 5l6 6",
    stroke: stroke,
    strokeWidth: sw
  }));
}
function ExpandIcon({
  size = 18,
  stroke = INK_MUTED,
  sw = 1.5
}) {
  return /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    viewBox: "0 0 24 24",
    fill: "none",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M4 9V4h5",
    stroke: stroke,
    strokeWidth: sw
  }), /*#__PURE__*/React.createElement("path", {
    d: "M20 9V4h-5",
    stroke: stroke,
    strokeWidth: sw
  }), /*#__PURE__*/React.createElement("path", {
    d: "M4 15v5h5",
    stroke: stroke,
    strokeWidth: sw
  }), /*#__PURE__*/React.createElement("path", {
    d: "M20 15v5h-5",
    stroke: stroke,
    strokeWidth: sw
  }));
}
function DriveIcon({
  size = 18,
  stroke = INK_MUTED,
  sw = 1.5
}) {
  // Simplified outline of the Google Drive prism — three folded faces.
  return /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    viewBox: "0 0 24 24",
    fill: "none",
    strokeLinejoin: "round",
    strokeLinecap: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M 8 3 L 16 3 L 19 9 L 5 9 Z",
    stroke: stroke,
    strokeWidth: sw
  }), /*#__PURE__*/React.createElement("path", {
    d: "M 5 9 L 12 9 L 8 20 L 2 16 Z",
    stroke: stroke,
    strokeWidth: sw
  }), /*#__PURE__*/React.createElement("path", {
    d: "M 12 9 L 19 9 L 22 16 L 14 20 L 8 20 Z",
    stroke: stroke,
    strokeWidth: sw
  }));
}
function DownloadGlyph({
  size = 14,
  stroke = PAPER,
  sw = 1.8
}) {
  return /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    viewBox: "0 0 24 24",
    fill: "none",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M12 4v12M6 12l6 6 6-6",
    stroke: stroke,
    strokeWidth: sw
  }), /*#__PURE__*/React.createElement("path", {
    d: "M4 20h16",
    stroke: stroke,
    strokeWidth: sw
  }));
}

// ── Avatar bubble ──────────────────────────────────────────────────────────
function Avatar({
  letter,
  opacity = 1
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: 33,
      height: 33,
      borderRadius: '50%',
      border: `1px solid ${BORDER}`,
      background: PAPER_SOFT,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      fontFamily: SANS,
      fontSize: 12,
      fontWeight: 400,
      color: INK_MUTED,
      letterSpacing: '0.04em',
      opacity
    }
  }, letter);
}

// ── Opening icon flight + caller strip ─────────────────────────────────────
// Three icons (mic, phone, upload) start centered. On "click" at T_CLICK,
// they each fly to their toolbar slot on the upper-left.
// After settle, caller strip (bubbles + LISTENING) appears next to mic.
function CallInitiation({
  initialA,
  initialB
}) {
  const t = useTime();

  // Original centered positions (before flight)
  const CENTER_X = W / 2;
  const CENTER_Y = H / 2 - 60;
  const ICON_GAP = 80 + 26; // 80px gap edge-to-edge for 26px icons
  const baseMic = {
    x: CENTER_X - ICON_GAP - 13,
    y: CENTER_Y
  };
  const basePhone = {
    x: CENTER_X - 13,
    y: CENTER_Y
  };
  const baseUpload = {
    x: CENTER_X + ICON_GAP - 13,
    y: CENTER_Y
  };

  // Target toolbar slots (upper-left)
  const tgt = {
    mic: {
      x: TOOLBAR_X,
      y: TOOLBAR_Y.mic
    },
    phone: {
      x: TOOLBAR_X,
      y: TOOLBAR_Y.phone
    },
    upload: {
      x: TOOLBAR_X,
      y: TOOLBAR_Y.upload
    }
  };

  // Icons fade in from t=T_ICONS_IN
  const iconsIn = clamp((t - T_ICONS_IN) / 0.7, 0, 1);

  // Flight phase: T_CLICK → T_TOOLBAR_SETTLE
  const fly = clamp((t - T_CLICK) / (T_TOOLBAR_SETTLE - T_CLICK), 0, 1);
  const flyE = easeInOutCubic(fly);
  function pos(base, target) {
    return {
      x: base.x + (target.x - base.x) * flyE,
      y: base.y + (target.y - base.y) * flyE
    };
  }
  const micP = pos(baseMic, tgt.mic);
  const phoneP = pos(basePhone, tgt.phone);
  const uploadP = pos(baseUpload, tgt.upload);

  // Scale: 1 at center → smaller toolbar size at slot
  const finalScale = TOOLBAR_ICON_SIZE / 32;
  const scale = 1 + (finalScale - 1) * flyE;

  // Cursor appearance + amber hover glow
  const cursorIn = t >= T_CURSOR_IN - 0.1 && t <= T_CLICK + 0.7;
  const hoverGlow = clamp((t - (T_CLICK - 0.6)) / 0.4, 0, 1) * (t < T_CLICK ? 1 : Math.max(0, 1 - (t - T_CLICK) * 2.5));
  // Cursor travel from off-screen lower-right to the mic
  const travel = clamp((t - T_CURSOR_IN) / (T_CLICK - T_CURSOR_IN), 0, 1);
  const cE = easeInOutCubic(travel);
  const cursorX = 1300 + (baseMic.x - 1300) * cE;
  const cursorY = 800 + (baseMic.y + 4 - 800) * cE;
  const cursorOp = t < T_CURSOR_IN ? 0 : t > T_CLICK + 0.4 ? Math.max(0, 1 - (t - T_CLICK - 0.4) / 0.4) : 1;
  const clickDip = t > T_CLICK - 0.05 && t < T_CLICK + 0.15 ? Math.sin((t - T_CLICK + 0.05) / 0.2 * Math.PI) * 2 : 0;

  // Ripple from click
  const rippleT = clamp((t - T_CLICK) / 0.7, 0, 1);
  const rippleVisible = rippleT > 0 && rippleT < 1;
  const rippleR = 14 + rippleT * 80;
  const rippleOp = (1 - rippleT) * 0.85;

  // BEGIN label fades out after click
  const beginOp = iconsIn * (1 - clamp((t - (T_CLICK - 0.4)) / 0.5, 0, 1));

  // Caller strip (bubbles + LISTENING) fades in after settle
  const listenOp = clamp((t - T_LISTENING) / 0.6, 0, 1);
  const breathing = 0.45 + 0.55 * (0.5 + 0.5 * Math.sin((t - T_LISTENING) * Math.PI * 0.7));
  return /*#__PURE__*/React.createElement(React.Fragment, null, t < T_CLICK + 0.6 && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: CENTER_X,
      top: CENTER_Y + 52,
      transform: 'translateX(-50%)',
      opacity: beginOp,
      fontFamily: SANS,
      fontSize: 11,
      fontWeight: 400,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: INK_FAINT
    }
  }, "Begin"), hoverGlow > 0.01 && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: baseMic.x + 13 - 36,
      top: baseMic.y + 13 - 36,
      width: 72,
      height: 72,
      borderRadius: '50%',
      background: `radial-gradient(circle, rgba(186,117,23,0.22) 0%, rgba(186,117,23,0) 60%)`,
      opacity: hoverGlow,
      pointerEvents: 'none'
    }
  }), rippleVisible && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: baseMic.x + 13 - rippleR,
      top: baseMic.y + 13 - rippleR,
      width: rippleR * 2,
      height: rippleR * 2,
      borderRadius: '50%',
      border: `1.5px solid ${AMBER}`,
      opacity: rippleOp,
      pointerEvents: 'none'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: micP.x,
      top: micP.y,
      opacity: iconsIn,
      transform: `scale(${scale})`,
      transformOrigin: 'top left'
    }
  }, /*#__PURE__*/React.createElement(MicIcon, null)), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: phoneP.x,
      top: phoneP.y,
      opacity: iconsIn,
      transform: `scale(${scale})`,
      transformOrigin: 'top left'
    }
  }, /*#__PURE__*/React.createElement(PhoneIcon, {
    stroke: INK_MUTED
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: uploadP.x,
      top: uploadP.y,
      opacity: iconsIn,
      transform: `scale(${scale})`,
      transformOrigin: 'top left'
    }
  }, /*#__PURE__*/React.createElement(UploadIcon, {
    stroke: INK_MUTED
  })), cursorIn && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: cursorX,
      top: cursorY + clickDip,
      opacity: cursorOp,
      pointerEvents: 'none',
      filter: 'drop-shadow(0 1px 2px rgba(0,0,0,0.12))'
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "18",
    height: "22",
    viewBox: "0 0 18 22",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M1 1 L1 18 L5.5 14 L8.5 20 L11 19 L8 13 L14 13 Z",
    fill: INK,
    stroke: PAPER,
    strokeWidth: "0.8",
    strokeLinejoin: "round"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: "1",
    cy: "1",
    r: "1.4",
    fill: AMBER,
    opacity: "0.85"
  }))), listenOp > 0.01 && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: TOOLBAR_X + TOOLBAR_ICON_SIZE + 14,
      top: TOOLBAR_Y.mic - 1,
      display: 'flex',
      alignItems: 'center',
      gap: 14,
      opacity: listenOp
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: -8,
      flexDirection: 'row'
    }
  }, /*#__PURE__*/React.createElement(Avatar, {
    letter: initialA
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      marginLeft: -8
    }
  }, /*#__PURE__*/React.createElement(Avatar, {
    letter: initialB
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      fontFamily: SANS,
      fontSize: 13,
      fontWeight: 400,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: AMBER
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: 6,
      height: 6,
      background: AMBER,
      borderRadius: '50%',
      opacity: breathing
    }
  }), "Listening")));
}

// ── Voice waveform: bars, sits inline beside the LISTENING strip ───────────
function VoiceWaveform() {
  const t = useTime();
  if (t < T_WAVE_IN - 0.1) return null;
  const fadeIn = clamp((t - T_WAVE_IN) / 1.2, 0, 1);
  const eased = easeOutCubic(fadeIn);

  // Half the previous size; bars are tighter and shorter.
  const N_BARS = 26;
  const BAR_W = 2;
  const GAP = 2;
  const TOTAL_W = N_BARS * BAR_W + (N_BARS - 1) * GAP;
  const HEIGHT = 18;

  // Anchored to the right of the LISTENING strip (upper-left workspace).
  // Strip lives at top=TOOLBAR_Y.mic; this sits centered on the same baseline.
  const LEFT = 322;
  const TOP = TOOLBAR_Y.mic + (TOOLBAR_ICON_SIZE - HEIGHT) / 2;
  const bars = [];
  for (let i = 0; i < N_BARS; i++) {
    const x = i / (N_BARS - 1);
    const env = 0.55 + 0.45 * Math.sin(x * Math.PI);
    const a = Math.abs(Math.sin(x * 9.0 + t * 5.2)) * 0.55 + Math.abs(Math.sin(x * 17.0 + t * 8.7)) * 0.28 + Math.abs(Math.sin(x * 3.7 + t * 2.8)) * 0.17;
    const h = Math.max(1.5, a * env * HEIGHT * (0.6 + 0.4 * eased));
    bars.push(h);
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: LEFT,
      top: TOP,
      width: TOTAL_W,
      height: HEIGHT,
      display: 'flex',
      alignItems: 'center',
      gap: GAP,
      opacity: fadeIn * 0.25,
      // 25% (50% of previous 50%)
      pointerEvents: 'none'
    }
  }, bars.map((h, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      width: BAR_W,
      height: h,
      background: INK,
      borderRadius: 1
    }
  })));
}

// ── Dialogue feed ──────────────────────────────────────────────────────────
// Auto-scrolling chat-style transcript on the right side of the frame.
// Speakers alternate L/R within a narrow column. Each new line lands at
// the same vertical "active" position; older lines scroll upward and fade.
function DialogueFeed({
  timeline,
  fontSize = 17
}) {
  const t = useTime();

  // active line index
  let active = -1;
  for (let i = 0; i < timeline.length; i++) {
    if (timeline[i].time <= t) active = i;else break;
  }
  if (active < 0) return null;
  const COL_LEFT = 900;
  const COL_WIDTH = 940; // right-side column
  // Top baseline aligns with the LISTENING strip / waveform row on the left.
  const VIEW_TOP = 104;
  const VIEW_BOTTOM = 920;
  const VIEW_H = VIEW_BOTTOM - VIEW_TOP;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: COL_LEFT,
      top: VIEW_TOP,
      width: COL_WIDTH,
      height: VIEW_H,
      pointerEvents: 'none',
      overflow: 'hidden'
      // Hard cut at the top — no fade. Bottom still fades to clear the seascape.
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      height: '100%',
      display: 'flex',
      flexDirection: 'column',
      justifyContent: 'flex-end',
      gap: 12,
      padding: '12px 24px 12px',
      boxSizing: 'border-box'
    }
  }, timeline.slice(0, active + 1).map((line, i) => {
    const since = t - line.time;
    const op = clamp(since / 0.18, 0, 1);
    const ty = (1 - op) * 6;
    const isA = line.s === 'A';
    return /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        alignSelf: isA ? 'flex-start' : 'flex-end',
        maxWidth: '78%',
        opacity: op,
        transform: `translateY(${ty}px)`,
        fontFamily: SERIF,
        fontStyle: 'italic',
        fontWeight: 400,
        fontSize,
        lineHeight: 1.45,
        color: isA ? INK : INK_MUTED,
        textAlign: isA ? 'left' : 'right',
        willChange: 'transform, opacity'
      }
    }, line.t);
  })));
}

// ── Script outline items (what the system "extracts" from the conversation) ─
// triggerLine = 0-based index into DIALOGUE. The item appears when t reaches
// that line's start time, OR when the script window first opens (whichever later).
const SCRIPT_ITEMS = [
// Title block (anchored by mention of "The Mourning Tide")
{
  kind: 'title',
  text: 'The Mourning Tide',
  triggerLine: 14
}, {
  kind: 'subtitle',
  text: 'A feature',
  triggerLine: 14
},
// Premise
{
  kind: 'section',
  label: 'Premise',
  triggerLine: 10
}, {
  kind: 'p',
  text: '2067. The seas have risen. Trade routes belong to private fleets.',
  triggerLine: 12
}, {
  kind: 'p',
  text: 'Piracy returns — for data, oxygen, AI server banks.',
  triggerLine: 12
},
// Characters
{
  kind: 'section',
  label: 'Characters',
  triggerLine: 20
}, {
  kind: 'char',
  name: 'Vale',
  desc: 'Captain. Former corp security. Hard, feared.',
  triggerLine: 20
}, {
  kind: 'char',
  name: 'Milo',
  desc: 'Engineer. First voyage. Idealist.',
  triggerLine: 22
}, {
  kind: 'char',
  name: 'The Ship AI',
  desc: 'Built for prison transport. Now: lonely.',
  triggerLine: 66
},
// Structure
{
  kind: 'section',
  label: 'Structure',
  triggerLine: 26
}, {
  kind: 'beat',
  num: 'I',
  text: 'Storm crossing into the dead zones.',
  triggerLine: 26
}, {
  kind: 'beat',
  num: 'II',
  text: 'The navigator. Locked room.',
  triggerLine: 36
}, {
  kind: 'beat',
  num: 'III',
  text: 'The cook. Frozen. “THE SHIP REMEMBERS.”',
  triggerLine: 88
}, {
  kind: 'beat',
  num: 'IV',
  text: 'The ship is the memory.',
  triggerLine: 104
},
// Theme
{
  kind: 'section',
  label: 'Theme',
  triggerLine: 116
}, {
  kind: 'p',
  text: 'Every escape carries its history aboard.',
  triggerLine: 116
}];

// ── Script icon + window ───────────────────────────────────────────────────
function ScriptWorkspace({
  scriptOpenAt,
  expandAt,
  dialogueTimeline
}) {
  const t = useTime();
  if (t < scriptOpenAt - 1.4) return null;

  // Icon fade-in
  const iconOp = clamp((t - scriptOpenAt) / 0.6, 0, 1);

  // Window expand: starts after icon visible (small lag)
  const winStart = scriptOpenAt + 0.4;
  const winP = clamp((t - winStart) / 0.9, 0, 1);
  const winE = easeOutQuart(winP);
  const WIN_X = TOOLBAR_X + TOOLBAR_ICON_SIZE + 14;
  // Window has two states:
  //   • small  — opens to the right of the toolbar, anchored just below the
  //              script icon.
  //   • large  — top rises to sit beside the phone icon, bottom drops down
  //              to share the baseline with the dialogue feed (y=920).
  const WIN_Y_SMALL = 252;
  const WIN_Y_LARGE = 152;

  // Two-stage sizing: opens "small", then the cursor returns later to drag
  // the bottom-right corner outward into the full workspace size.
  const W_SMALL = 530;
  const H_SMALL = 468;
  const W_LARGE = 760;
  const H_LARGE = 768; // bottom = WIN_Y_LARGE + H_LARGE = 920 (text baseline)
  const expandP = clamp((t - expandAt) / 1.0, 0, 1);
  const expandE = easeInOutCubic(expandP);
  const WIN_W = W_SMALL + (W_LARGE - W_SMALL) * expandE;
  const WIN_H = H_SMALL + (H_LARGE - H_SMALL) * expandE;
  const WIN_Y = WIN_Y_SMALL + (WIN_Y_LARGE - WIN_Y_SMALL) * expandE;

  // Item appear times
  const itemAppearTime = item => {
    const triggerT = dialogueTimeline[item.triggerLine]?.time ?? scriptOpenAt;
    return Math.max(triggerT, winStart);
  };
  const sorted = [...SCRIPT_ITEMS].map((item, i) => ({
    ...item,
    _i: i
  }));

  // Drafting indicator: pulse while there are still pending items.
  const anyPending = sorted.some(item => itemAppearTime(item) > t - 0.2);
  const dotPulse = 0.45 + 0.55 * (0.5 + 0.5 * Math.sin(t * 2.4));
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: TOOLBAR_X,
      top: TOOLBAR_Y.script,
      opacity: iconOp,
      transform: `scale(${TOOLBAR_ICON_SIZE / 32})`,
      transformOrigin: 'top left'
    }
  }, /*#__PURE__*/React.createElement(ScriptIcon, {
    stroke: INK
  })), /*#__PURE__*/React.createElement(ScriptCursor, {
    scriptOpenAt: scriptOpenAt,
    expandAt: expandAt,
    winX: WIN_X,
    winYSmall: WIN_Y_SMALL,
    winYLarge: WIN_Y_LARGE,
    smallW: W_SMALL,
    smallH: H_SMALL,
    largeW: W_LARGE,
    largeH: H_LARGE
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: WIN_X,
      top: WIN_Y,
      width: WIN_W * winE,
      height: WIN_H * winE,
      background: PAPER_SOFT,
      border: `1px solid ${BORDER}`,
      borderRadius: 4,
      boxShadow: '0 1px 2px rgba(0,0,0,0.03)',
      overflow: 'hidden',
      transformOrigin: 'top left'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      top: 0,
      width: WIN_W,
      height: WIN_H,
      display: 'flex',
      flexDirection: 'column',
      opacity: clamp((winP - 0.55) / 0.45, 0, 1)
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      padding: '22px 28px 12px',
      boxSizing: 'border-box',
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: SANS,
      fontSize: 12,
      fontWeight: 400,
      letterSpacing: '0.20em',
      textTransform: 'uppercase',
      color: AMBER
    }
  }, "Script"), anyPending && /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 7,
      fontFamily: SANS,
      fontSize: 11,
      fontWeight: 400,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: AMBER,
      opacity: 0.95
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: 5,
      height: 5,
      background: AMBER,
      borderRadius: '50%',
      opacity: dotPulse
    }
  }), "Drafting")), sorted.map((item, idx) => {
    const appear = itemAppearTime(item);
    const op = clamp((t - appear) / 0.5, 0, 1);
    if (op <= 0) return /*#__PURE__*/React.createElement(ScriptItemSkeleton, {
      key: idx,
      kind: item.kind
    });
    const ty = (1 - op) * 4;
    return /*#__PURE__*/React.createElement("div", {
      key: idx,
      style: {
        opacity: op,
        transform: `translateY(${ty}px)`,
        willChange: 'opacity, transform'
      }
    }, /*#__PURE__*/React.createElement(ScriptItemRender, {
      item: item
    }));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '12px 18px',
      borderTop: `1px solid ${BORDER}`,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      background: PAPER_SOFT
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 18
    }
  }, /*#__PURE__*/React.createElement(PencilIcon, null), /*#__PURE__*/React.createElement(ExpandIcon, null), /*#__PURE__*/React.createElement(DriveIcon, null)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      padding: '8px 14px',
      background: AMBER,
      color: PAPER,
      borderRadius: 2,
      fontFamily: SANS,
      fontSize: 11,
      fontWeight: 400,
      letterSpacing: '0.08em',
      textTransform: 'uppercase'
    }
  }, /*#__PURE__*/React.createElement(DownloadGlyph, null), "Download")))), (() => {
    const layout = computeReferenceLayout({
      dialogueTimeline,
      scriptOpenAt,
      winX: WIN_X,
      winY: WIN_Y,
      winW: WIN_W * winE,
      winH: WIN_H * winE,
      expandedE: expandE
    });
    return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(InlineReferences, {
      layout: layout
    }), /*#__PURE__*/React.createElement(ReferenceDropCursor, {
      layout: layout
    }));
  })());
}
function ScriptItemRender({
  item
}) {
  switch (item.kind) {
    case 'title':
      return /*#__PURE__*/React.createElement("div", {
        style: {
          fontFamily: SCRIPT_FONT,
          fontStyle: 'italic',
          fontWeight: 400,
          fontSize: 28,
          lineHeight: 1.15,
          color: INK,
          marginTop: 0
        }
      }, item.text);
    case 'subtitle':
      return /*#__PURE__*/React.createElement("div", {
        style: {
          fontFamily: SCRIPT_FONT,
          fontSize: 15,
          fontWeight: 400,
          color: INK_FAINT,
          marginTop: 4,
          marginBottom: 14
        }
      }, item.text);
    case 'section':
      return /*#__PURE__*/React.createElement("div", {
        style: {
          fontFamily: SANS,
          fontSize: 12,
          fontWeight: 400,
          letterSpacing: '0.20em',
          textTransform: 'uppercase',
          color: AMBER,
          marginTop: 14,
          marginBottom: 6
        }
      }, item.label);
    case 'p':
      return /*#__PURE__*/React.createElement("div", {
        style: {
          fontFamily: SCRIPT_FONT,
          fontSize: 16,
          fontWeight: 400,
          lineHeight: 1.45,
          color: INK,
          marginTop: 2,
          marginBottom: 4
        }
      }, item.text);
    case 'char':
      return /*#__PURE__*/React.createElement("div", {
        style: {
          display: 'flex',
          gap: 14,
          fontSize: 16,
          lineHeight: 1.45,
          color: INK,
          marginTop: 2,
          marginBottom: 3,
          fontFamily: SCRIPT_FONT
        }
      }, /*#__PURE__*/React.createElement("div", {
        style: {
          fontWeight: 400,
          minWidth: 98,
          color: INK
        }
      }, item.name), /*#__PURE__*/React.createElement("div", {
        style: {
          fontWeight: 400,
          color: INK_MUTED,
          flex: 1
        }
      }, item.desc));
    case 'beat':
      return /*#__PURE__*/React.createElement("div", {
        style: {
          display: 'flex',
          gap: 12,
          fontSize: 16,
          lineHeight: 1.45,
          marginTop: 2,
          marginBottom: 3,
          fontFamily: SCRIPT_FONT
        }
      }, /*#__PURE__*/React.createElement("div", {
        style: {
          fontStyle: 'italic',
          color: AMBER,
          minWidth: 34
        }
      }, item.num, "."), /*#__PURE__*/React.createElement("div", {
        style: {
          color: INK,
          flex: 1
        }
      }, item.text));
    default:
      return null;
  }
}
// Placeholder for unfilled item — keeps vertical rhythm consistent
function ScriptItemSkeleton({
  kind
}) {
  const h = kind === 'title' ? 33 : kind === 'subtitle' ? 22 : kind === 'section' ? 28 : kind === 'p' ? 23 : kind === 'char' ? 23 : kind === 'beat' ? 23 : 18;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      height: h,
      opacity: 0
    }
  });
}

// ── Reference moodboard ───────────────────────────────────────────────────
// Small thumbnail cards that surface as the conversation references things —
// some are images, some are YouTube clips. Pinned to the lower-left workspace
// area, below the script window.
const REFERENCE_CARDS = [{
  title: 'Galleon · silhouette',
  source: 'reference · hull shape',
  kind: 'image',
  triggerLine: 9,
  // "Imagine a pirate ship."
  src: 'TENET_product_deck/media/ref-ship-sun.jpg'
}, {
  title: 'The Mourning Tide',
  source: 'reference · storm mood',
  kind: 'image',
  triggerLine: 15,
  // "ship called The Mourning Tide"
  src: 'TENET_product_deck/media/ref-ship-storm.jpg'
}, {
  title: 'Crew · period dress',
  source: 'reference · costume',
  kind: 'image',
  triggerLine: 20,
  // "captain Vale" / crew intro
  src: 'TENET_product_deck/media/ref-pirates-shore.jpg'
}, {
  title: 'Locked-room interior',
  source: 'reference · scene mood',
  kind: 'image',
  triggerLine: 36,
  // navigator / locked room
  src: 'TENET_product_deck/media/ref-ship-cave.jpg'
}, {
  title: 'Prison transport',
  source: 'reference · ship AI lineage',
  kind: 'image',
  triggerLine: 66,
  // ship AI built for prison transport
  src: 'TENET_product_deck/media/ref-ship-navy.jpg'
}, {
  title: 'Hull classifications',
  source: 'reference · taxonomy',
  kind: 'image',
  triggerLine: 88,
  // "THE SHIP REMEMBERS"
  src: 'TENET_product_deck/media/ref-ship-types.jpg'
}];
function PlayBadge() {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: '50%',
      top: '50%',
      transform: 'translate(-50%, -50%)',
      width: 28,
      height: 28,
      borderRadius: '50%',
      background: 'rgba(0,0,0,0.55)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center'
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "10",
    height: "10",
    viewBox: "0 0 10 10",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M2.5 1 L8.5 5 L2.5 9 Z",
    fill: "#fff"
  })));
}
function YTBadge() {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 6,
      left: 6,
      fontFamily: SANS,
      fontSize: 8,
      fontWeight: 400,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: '#fff',
      background: 'rgba(192, 28, 28, 0.95)',
      padding: '2px 6px',
      borderRadius: 1
    }
  }, "YT");
}
function ReferenceMoodboard_DEPRECATED({
  dialogueTimeline
}) {
  const t = useTime();
  const sorted = [...REFERENCE_CARDS].sort((a, b) => a.triggerLine - b.triggerLine);
  const cards = sorted.map((c, i) => {
    const trig = dialogueTimeline[c.triggerLine]?.time ?? 1e9;
    const appear = trig + 0.5;
    const op = clamp((t - appear) / 0.6, 0, 1);
    return {
      ...c,
      _i: i,
      appear,
      op
    };
  }).filter(c => c.op > 0);
  if (cards.length === 0) return null;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 120,
      top: 782,
      // just below the script window
      display: 'flex',
      gap: 16,
      pointerEvents: 'none'
    }
  }, cards.map(c => /*#__PURE__*/React.createElement("div", {
    key: c._i,
    style: {
      width: 140,
      height: 112,
      background: PAPER_SOFT,
      border: `1px solid ${BORDER}`,
      borderRadius: 2,
      opacity: c.op,
      overflow: 'hidden',
      transform: `translateY(${(1 - c.op) * 8}px)`,
      willChange: 'transform, opacity',
      boxShadow: '0 1px 2px rgba(0,0,0,0.04)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'relative',
      width: '100%',
      height: 70,
      ...c.thumb
    }
  }, c.kind === 'video' && /*#__PURE__*/React.createElement(PlayBadge, null), c.kind === 'video' && /*#__PURE__*/React.createElement(YTBadge, null)), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '6px 10px'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: SANS,
      fontSize: 11,
      fontWeight: 400,
      color: INK,
      lineHeight: 1.2,
      marginBottom: 2,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, c.title), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: SANS,
      fontSize: 8,
      fontWeight: 400,
      color: INK_FAINT,
      letterSpacing: '0.14em',
      textTransform: 'uppercase',
      lineHeight: 1.2,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, c.source)))));
}

// Story beats where the seascape briefly comes into focus.
// Indices into DIALOGUE — the focus pulse fires shortly after the line lands.
const FOCUS_BEAT_LINES = [14, 26, 36, 88, 94, 104, 116];

// ── Cursor pointer (shared SVG) ────────────────────────────────────────────
function CursorPointer({
  x,
  y,
  opacity = 1,
  resize = false
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: x,
      top: y,
      opacity,
      pointerEvents: 'none',
      filter: 'drop-shadow(0 1px 2px rgba(0,0,0,0.16))',
      zIndex: 100
    }
  }, resize ? /*#__PURE__*/React.createElement("svg", {
    width: "20",
    height: "20",
    viewBox: "0 0 20 20",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M3 3 L17 17 M3 3 L7 3 M3 3 L3 7 M17 17 L13 17 M17 17 L17 13",
    stroke: INK,
    strokeWidth: "1.6",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style: {
      paintOrder: 'stroke'
    }
  }), /*#__PURE__*/React.createElement("path", {
    d: "M3 3 L17 17 M3 3 L7 3 M3 3 L3 7 M17 17 L13 17 M17 17 L17 13",
    stroke: PAPER,
    strokeWidth: "3",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style: {
      mixBlendMode: 'normal',
      opacity: 0.85
    }
  }), /*#__PURE__*/React.createElement("path", {
    d: "M3 3 L17 17 M3 3 L7 3 M3 3 L3 7 M17 17 L13 17 M17 17 L17 13",
    stroke: INK,
    strokeWidth: "1.6",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  })) : /*#__PURE__*/React.createElement("svg", {
    width: "18",
    height: "22",
    viewBox: "0 0 18 22",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M1 1 L1 18 L5.5 14 L8.5 20 L11 19 L8 13 L14 13 Z",
    fill: INK,
    stroke: PAPER,
    strokeWidth: "0.8",
    strokeLinejoin: "round"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: "1",
    cy: "1",
    r: "1.4",
    fill: AMBER,
    opacity: "0.85"
  })));
}

// ── Cursor choreography for the script window ──────────────────────────────
// Two visits:
//  1) just before `scriptOpenAt`, cursor flies to the script toolbar icon
//     and clicks — the window opens at its small size.
//  2) just before `expandAt`, cursor returns and grabs the bottom-right
//     corner of the small window, dragging it outward to the full size.
function ScriptCursor({
  scriptOpenAt,
  expandAt,
  winX,
  winYSmall,
  winYLarge,
  smallW,
  smallH,
  largeW,
  largeH
}) {
  const t = useTime();

  // ── Arrival 1 ────────────────────────────────────────────────────────────
  if (t >= scriptOpenAt - 1.2 && t <= scriptOpenAt + 0.6) {
    const startX = 1180,
      startY = 460;
    const endX = TOOLBAR_X + 8;
    const endY = TOOLBAR_Y.script + 4;
    const travel = clamp((t - (scriptOpenAt - 1.2)) / 1.05, 0, 1);
    const eT = easeInOutCubic(travel);
    const x = startX + (endX - startX) * eT;
    const y = startY + (endY - startY) * eT;
    const op = t < scriptOpenAt + 0.05 ? 1 : Math.max(0, 1 - (t - scriptOpenAt - 0.05) / 0.5);
    const dip = t > scriptOpenAt - 0.05 && t < scriptOpenAt + 0.18 ? Math.sin((t - scriptOpenAt + 0.05) / 0.23 * Math.PI) * 2 : 0;
    return /*#__PURE__*/React.createElement(CursorPointer, {
      x: x,
      y: y + dip,
      opacity: op
    });
  }

  // ── Arrival 2 ────────────────────────────────────────────────────────────
  if (t >= expandAt - 1.0 && t <= expandAt + 1.5) {
    const startX = 1020,
      startY = 540;
    const cornerX_small = winX + smallW - 6;
    const cornerY_small = winYSmall + smallH - 6;
    const cornerX_large = winX + largeW - 6;
    const cornerY_large = winYLarge + largeH - 6;
    let x,
      y,
      op = 1,
      dragging = false;
    if (t < expandAt) {
      const travel = clamp((t - (expandAt - 1.0)) / 1.0, 0, 1);
      const eT = easeInOutCubic(travel);
      x = startX + (cornerX_small - startX) * eT;
      y = startY + (cornerY_small - startY) * eT;
    } else if (t < expandAt + 1.0) {
      const drag = clamp((t - expandAt) / 1.0, 0, 1);
      const eD = easeInOutCubic(drag);
      x = cornerX_small + (cornerX_large - cornerX_small) * eD;
      y = cornerY_small + (cornerY_large - cornerY_small) * eD;
      dragging = true;
    } else {
      x = cornerX_large;
      y = cornerY_large;
      op = Math.max(0, 1 - (t - expandAt - 1.0) / 0.45);
    }
    return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(CursorPointer, {
      x: x,
      y: y,
      opacity: op,
      resize: true
    }), dragging && /*#__PURE__*/React.createElement("div", {
      style: {
        position: 'absolute',
        left: x - 5,
        top: y - 5,
        width: 10,
        height: 10,
        borderRadius: '50%',
        background: AMBER,
        opacity: 0.55 * op,
        pointerEvents: 'none',
        zIndex: 99
      }
    }));
  }
  return null;
}

// ── Reference layout / timing ─────────────────────────────────────────────
// Both InlineReferences and ReferenceDropCursor read from this so the
// cursor's drop target lines up with where the card actually lands.
//
// Wall-clock cadence (independent of dialogue pace):
//   60s   — script window opens, references header appears
//   65s   — first frame drops
//   70s   — second frame drops
//   71s   — third frame drops (concurrent with #2)
//  120s   — window expands to its larger size (top rises near phone icon,
//           bottom aligns with dialogue baseline)
//  125s   — fourth frame drops (row 2 begins)
//  130s   — fifth frame drops
//  135s   — sixth frame drops
//
// Cards are always 3-per-row, contained inside the script window. Row 1
// (cards 0-2) sits above row 2 (cards 3-5); row 1 visually slides upward
// as the window expands to make room for row 2.
const REFERENCE_DROP_TIMES = [65, 70, 71, 125, 130, 135];
const PER_ROW = 3;
const T_BOX_OPEN = 60;
const T_BOX_EXPAND = 120;
function computeReferenceLayout({
  scriptOpenAt,
  winX,
  winY,
  winW,
  winH,
  expandedE
}) {
  // Stable order — REFERENCE_CARDS is already in the order the user wants.
  const ordered = REFERENCE_CARDS.slice(0, 6);

  // Cards grow slightly when the script window expands.
  const cardW = 106 + 8 * expandedE;
  const cardH = 88 + 6 * expandedE;
  const thumbH = 52 + 4 * expandedE;
  const gap = 8;
  const rowGap = 6;

  // Strip is anchored to the script window's bottom-inside, padded.
  const padX = 22;
  const ACTION_BAR_H = 44;
  const HEADER_GAP = 22; // space for the "References" eyebrow above cards
  const stripLeft = winX + padX;
  const stripWidth = winW - padX * 2;

  // Row Y positions: row1 (cards 3-5) anchored to the bottom; row0 (cards 0-2)
  // sits above row1 *only* once the window has expanded. While the window is
  // still small, row0 occupies the row1 slot so the cards live at the bottom.
  const row1Top = winY + winH - ACTION_BAR_H - cardH - 12;
  const row0Top = row1Top - (cardH + rowGap) * expandedE;
  const headerTop = row0Top - HEADER_GAP;
  const cards = ordered.map((c, i) => {
    const row = Math.floor(i / PER_ROW);
    const col = i % PER_ROW;
    const dropAt = REFERENCE_DROP_TIMES[i] ?? 95 + (i - 5) * 5;
    const x = stripLeft + col * (cardW + gap);
    const y = row === 0 ? row0Top : row1Top;
    return {
      ...c,
      _i: i,
      row,
      col,
      dropAt,
      x,
      y
    };
  });
  return {
    cards,
    cardW,
    cardH,
    thumbH,
    stripLeft,
    stripWidth,
    headerTop,
    boxAppearAt: scriptOpenAt // header shows when script window opens
  };
}

// ── InlineReferences: the box itself — header appears with the script
// window at boxAppearAt, then cards land in as the cursor delivers them.
function InlineReferences({
  layout
}) {
  const t = useTime();
  const {
    cards,
    cardW,
    cardH,
    thumbH,
    stripLeft,
    headerTop,
    boxAppearAt
  } = layout;

  // Header fades in the moment the script window opens (T_BOX_OPEN).
  if (t < boxAppearAt - 0.05) return null;
  const headerOp = clamp((t - boxAppearAt) / 0.5, 0, 1);
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: stripLeft,
      top: headerTop,
      fontFamily: SANS,
      fontSize: 12,
      fontWeight: 400,
      letterSpacing: '0.20em',
      textTransform: 'uppercase',
      color: AMBER,
      opacity: headerOp,
      pointerEvents: 'none',
      zIndex: 5
    }
  }, "References"), cards.map(c => {
    // "Settle" animation: 0 = just dropped (slightly above + light), 1 = landed flat.
    const settle = clamp((t - c.dropAt) / 0.45, 0, 1);
    const eS = easeOutCubic(settle);
    if (settle <= 0) return null;
    return /*#__PURE__*/React.createElement("div", {
      key: c._i,
      style: {
        position: 'absolute',
        left: c.x,
        top: c.y,
        width: cardW,
        height: cardH,
        background: PAPER,
        border: `1px solid ${BORDER}`,
        borderRadius: 2,
        overflow: 'hidden',
        opacity: eS,
        transform: `translateY(${(1 - eS) * -10}px) scale(${0.96 + 0.04 * eS}) rotate(${(1 - eS) * -1.5}deg)`,
        transformOrigin: 'top left',
        boxShadow: `0 ${(1 - eS) * 12 + 1}px ${(1 - eS) * 22 + 2}px rgba(0,0,0,${0.05 + (1 - eS) * 0.12})`,
        willChange: 'transform, opacity',
        pointerEvents: 'none',
        zIndex: 5
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        position: 'relative',
        width: '100%',
        height: thumbH,
        backgroundImage: `url('${c.src}')`,
        backgroundSize: 'cover',
        backgroundPosition: 'center',
        backgroundColor: '#1a1a1a'
      }
    }, c.kind === 'video' && /*#__PURE__*/React.createElement(PlayBadge, null), c.kind === 'video' && /*#__PURE__*/React.createElement(YTBadge, null)), /*#__PURE__*/React.createElement("div", {
      style: {
        padding: '5px 8px'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: SCRIPT_FONT,
        fontSize: 13,
        fontWeight: 400,
        color: INK,
        lineHeight: 1.2,
        overflow: 'hidden',
        textOverflow: 'ellipsis',
        whiteSpace: 'nowrap'
      }
    }, c.title), /*#__PURE__*/React.createElement("div", {
      style: {
        fontFamily: SANS,
        fontSize: 8,
        fontWeight: 400,
        color: INK_FAINT,
        letterSpacing: '0.14em',
        textTransform: 'uppercase',
        lineHeight: 1.2,
        marginTop: 1,
        overflow: 'hidden',
        textOverflow: 'ellipsis',
        whiteSpace: 'nowrap'
      }
    }, c.source)));
  }));
}

// ── ReferenceDropCursor ───────────────────────────────────────────────────
// For each card: a cursor flies in from off-frame carrying a tilted thumbnail
// of the photo, drops it onto the references strip, then flicks away. Multiple
// cursors can be on screen simultaneously (drops 2 + 3 land within a second).
function ReferenceDropCursor({
  layout
}) {
  const t = useTime();
  const {
    cards,
    cardW,
    cardH,
    thumbH
  } = layout;

  // Each card has its own start-edge so the cursor doesn't feel mechanical.
  const startSides = [{
    x: W + 90,
    y: H * 0.18
  },
  // right-top
  {
    x: -90,
    y: H * 0.34
  },
  // left-mid
  {
    x: W + 90,
    y: H * 0.62
  },
  // right-low
  {
    x: W * 0.66,
    y: H + 90
  },
  // bottom
  {
    x: -90,
    y: H * 0.74
  },
  // left-low
  {
    x: W + 90,
    y: H * 0.42
  } // right-mid
  ];

  // Walk every card and collect each one currently mid-drop.
  const active = [];
  for (let i = 0; i < cards.length; i++) {
    const c = cards[i];
    const TRAVEL = 1.25;
    const RELEASE = 0.22;
    const FLICK = 0.55;
    const cursorStart = c.dropAt - TRAVEL - RELEASE;
    const cursorEnd = c.dropAt + FLICK;
    if (t < cursorStart || t > cursorEnd) continue;
    const start = startSides[i % startSides.length];
    const targetCx = c.x + cardW * 0.5;
    const targetCy = c.y + cardH * 0.5;
    let cx,
      cy,
      op = 1,
      carrying = true,
      rot = -3;
    if (t < c.dropAt - RELEASE) {
      // Travel phase: cursor flies from off-frame edge to the drop target.
      const p = clamp((t - cursorStart) / TRAVEL, 0, 1);
      const e = easeInOutCubic(p);
      cx = start.x + (targetCx - start.x) * e;
      cy = start.y + (targetCy - start.y) * e;
      // Slight arc — lift up at the peak of travel.
      cy -= Math.sin(p * Math.PI) * 22;
      // Card rotates from -8° at pickup to -3° just before drop.
      rot = -8 + 5 * e;
    } else if (t < c.dropAt) {
      // Release phase: small downward press, rotation straightens, image settles.
      const p = clamp((t - (c.dropAt - RELEASE)) / RELEASE, 0, 1);
      const e = easeOutCubic(p);
      cx = targetCx;
      cy = targetCy + 4 * e;
      rot = -3 + 3 * e; // straightens to 0
    } else {
      // Flick phase: cursor lifts and slides off; the InlineReferences card
      // is now visible underneath because its `settle` animation has started.
      const p = clamp((t - c.dropAt) / FLICK, 0, 1);
      const e = easeOutCubic(p);
      cx = targetCx + 70 * e;
      cy = targetCy - 38 * e;
      op = 1 - p;
      carrying = false;
    }
    const carryOp = carrying ? t < c.dropAt ? 1 : clamp(1 - (t - c.dropAt) / 0.18, 0, 1) : 0;
    active.push({
      c,
      i,
      cx,
      cy,
      op,
      carrying,
      rot,
      carryOp
    });
  }
  if (active.length === 0) return null;
  const imgW = cardW * 0.85;
  const imgH = cardH * 0.85;
  return /*#__PURE__*/React.createElement(React.Fragment, null, active.map(({
    c,
    i,
    cx,
    cy,
    op,
    rot,
    carryOp
  }) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: `drop-${i}`
  }, carryOp > 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: cx - imgW * 0.5 + 14,
      top: cy - imgH * 0.5 + 18,
      width: imgW,
      height: imgH,
      background: PAPER,
      border: `1px solid ${BORDER}`,
      borderRadius: 2,
      overflow: 'hidden',
      opacity: 0.96 * carryOp,
      transform: `rotate(${rot}deg)`,
      transformOrigin: 'top left',
      boxShadow: '0 18px 36px rgba(0,0,0,0.28), 0 4px 10px rgba(0,0,0,0.18)',
      pointerEvents: 'none',
      zIndex: 102
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      height: imgH * (thumbH / cardH),
      backgroundImage: `url('${c.src}')`,
      backgroundSize: 'cover',
      backgroundPosition: 'center',
      backgroundColor: '#1a1a1a'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '4px 8px',
      fontFamily: SCRIPT_FONT,
      fontSize: 10,
      color: INK,
      lineHeight: 1.2,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, c.title)), /*#__PURE__*/React.createElement(CursorPointer, {
    x: cx,
    y: cy,
    opacity: op
  }))));
}

// ── Timeline expander: horizontal line + pill button at the end ────────────
function TimelineExpander({
  appearAt
}) {
  const t = useTime();
  if (t < appearAt - 0.1) return null;
  const lineP = clamp((t - appearAt) / 0.9, 0, 1);
  const lineE = easeOutCubic(lineP);
  // line draws from center outward
  const maxW = W - 240;
  const lineW = maxW * lineE;
  const labelP = clamp((t - appearAt - 0.5) / 0.6, 0, 1);
  const labelOp = labelP;
  const labelTy = (1 - labelP) * 6;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: (W - lineW) / 2,
      top: 920,
      width: lineW,
      height: 1,
      background: 'rgba(255,255,255,0.65)',
      boxShadow: '0 0 8px rgba(0,0,0,0.18)',
      pointerEvents: 'none',
      zIndex: 8
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: '50%',
      top: 920,
      transform: `translate(-50%, calc(-50% + ${labelTy}px))`,
      opacity: labelOp,
      display: 'flex',
      alignItems: 'center',
      gap: 10,
      padding: '10px 20px',
      background: PAPER_SOFT,
      border: `1px solid ${BORDER}`,
      borderRadius: 999,
      fontFamily: SANS,
      fontSize: 11,
      fontWeight: 400,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: INK,
      boxShadow: '0 6px 20px rgba(0,0,0,0.10)',
      pointerEvents: 'none',
      zIndex: 9
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "13",
    height: "13",
    viewBox: "0 0 24 24",
    fill: "none"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M6 9l6 6 6-6",
    stroke: INK,
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  })), "Expand to show timeline"));
}

// ── Seascape: builds slowly throughout, resolves into ocean + boat at end ──
function Seascape({
  start,
  defStart,
  defEnd,
  intensity = 1,
  dialogueTimeline
}) {
  const t = useTime();
  if (t < start) return null;

  // Overall growth — height climbs slowly so the field stays below the
  // workspace line (~bottom 28% of canvas), never invading the script area.
  const buildP = clamp((t - start) / (defStart - start), 0, 1);
  const buildE = easeOutCubic(buildP);
  let heightPct = 4 + 18 * buildE; // max ~22% during build

  // Definition phase — abstract → seascape; height tops out at 28%.
  const defP = clamp((t - defStart) / (defEnd - defStart), 0, 1);
  const defE = easeInOutCubic(defP);
  heightPct = heightPct + (28 - heightPct) * defE;
  const fieldH = heightPct / 100 * H;
  const fieldY = H - fieldH;
  // Lighter overall — dawn sea, not midnight sea
  const fieldOpacity = (0.06 + 0.40 * buildE + 0.22 * defE) * intensity;

  // ── Focus / defocus ───────────────────────────────
  // Base blur is heavy so the sea stays out of focus during the brainstorm.
  // A slow LFO breathes it in and out; story-beat lines pull a sharper pulse.
  const baseBlur = 14;
  const lfo = Math.sin(t * 0.22 + 0.6) * 4;
  let beatPull = 0;
  if (dialogueTimeline) {
    for (const idx of FOCUS_BEAT_LINES) {
      const beat = dialogueTimeline[idx];
      if (!beat) continue;
      const center = beat.time + 0.6;
      const dt = t - center;
      // Bell-shaped pulse, ~1.6s wide, depth 8
      beatPull += 8 * Math.exp(-(dt * dt) / (1.6 * 1.6 * 0.5));
    }
  }
  // Definition phase tightens focus dramatically so the boat reads clean.
  const blurAmt = Math.max(0.4, (baseBlur + lfo - beatPull) * (1 - defE * 0.95));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      top: fieldY,
      width: W,
      height: fieldH,
      opacity: fieldOpacity,
      pointerEvents: 'none',
      overflow: 'hidden',
      WebkitMaskImage: 'linear-gradient(180deg, rgba(0,0,0,0) 0%, rgba(0,0,0,0.55) 8%, rgba(0,0,0,1) 22%, rgba(0,0,0,1) 100%)',
      maskImage: 'linear-gradient(180deg, rgba(0,0,0,0) 0%, rgba(0,0,0,0.55) 8%, rgba(0,0,0,1) 22%, rgba(0,0,0,1) 100%)',
      filter: `blur(${blurAmt.toFixed(2)}px)`
    }
  }, /*#__PURE__*/React.createElement(BlobField, {
    t: t,
    W: W,
    H: fieldH,
    fade: 1 - defE * 0.55
  }), defP > 0 && /*#__PURE__*/React.createElement(Ocean, {
    t: t,
    W: W,
    H: fieldH,
    alpha: defE
  }), defP > 0.45 && /*#__PURE__*/React.createElement(Boat, {
    t: t,
    W: W,
    fieldH: fieldH,
    alpha: clamp((defP - 0.45) / 0.45, 0, 1)
  }));
}
function BlobField({
  t,
  W,
  H,
  fade
}) {
  // Dawn-sea palette — lighter slates and warm peaches, no near-black.
  const blobs = [{
    color: 'rgba(95, 120, 138, 1)',
    bx: 0.20,
    by: 0.55,
    r: 620,
    px: 0.7,
    py: 0.4,
    sx: 1.0,
    sy: 0.6
  }, {
    color: 'rgba(220, 165, 110, 1)',
    bx: 0.78,
    by: 0.55,
    r: 580,
    px: 1.2,
    py: 0.7,
    sx: 0.9,
    sy: 0.8
  }, {
    color: 'rgba(115, 140, 138, 1)',
    bx: 0.50,
    by: 0.30,
    r: 700,
    px: 2.0,
    py: 1.1,
    sx: 1.1,
    sy: 0.5
  }, {
    color: 'rgba(200, 165, 130, 1)',
    bx: 0.62,
    by: 0.70,
    r: 480,
    px: 3.0,
    py: 1.8,
    sx: 1.4,
    sy: 0.9
  }, {
    color: 'rgba(80, 100, 118, 1)',
    bx: 0.10,
    by: 0.85,
    r: 540,
    px: 0.4,
    py: 2.3,
    sx: 0.7,
    sy: 1.1
  }];
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      filter: 'blur(80px)',
      opacity: fade
    }
  }, blobs.map((b, i) => {
    const driftX = Math.sin(t * 0.12 * b.sx + b.px) * 0.10;
    const driftY = Math.cos(t * 0.10 * b.sy + b.py) * 0.06;
    const cx = (b.bx + driftX) * W;
    const cy = (b.by + driftY) * H;
    const r = b.r * (0.95 + 0.08 * Math.sin(t * 0.18 + b.px));
    return /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        position: 'absolute',
        left: cx - r,
        top: cy - r,
        width: r * 2,
        height: r * 2,
        borderRadius: '50%',
        background: `radial-gradient(circle, ${b.color} 0%, ${b.color.replace(/, 1\)$/, ', 0.0)')} 65%)`
      }
    });
  }));
}

// Sky + multiple wave layers
function Ocean({
  t,
  W,
  H,
  alpha
}) {
  const horizonY = H * 0.34;

  // Build wave path: bottom-filled polygon with sine top
  const wave = (yBase, amp, freq, phase, fillColor, key) => {
    const pts = [];
    const N = 160;
    for (let i = 0; i <= N; i++) {
      const x = i / N * W;
      const y = yBase + Math.sin(x * freq + phase + t * 0.6) * amp + Math.sin(x * freq * 2.3 + phase * 1.5 + t * 0.9) * amp * 0.4 + Math.sin(x * freq * 5.0 + phase * 2.0 + t * 1.3) * amp * 0.18;
      pts.push([x, y]);
    }
    let d = `M0 ${H} L0 ${pts[0][1]} `;
    pts.forEach(p => {
      d += `L${p[0].toFixed(1)} ${p[1].toFixed(1)} `;
    });
    d += `L${W} ${H} Z`;
    return /*#__PURE__*/React.createElement("path", {
      key: key,
      d: d,
      fill: fillColor
    });
  };
  return /*#__PURE__*/React.createElement("svg", {
    width: W,
    height: H,
    style: {
      position: 'absolute',
      inset: 0,
      opacity: alpha
    }
  }, /*#__PURE__*/React.createElement("defs", null, /*#__PURE__*/React.createElement("linearGradient", {
    id: "tn-sky",
    x1: "0",
    y1: "0",
    x2: "0",
    y2: "1"
  }, /*#__PURE__*/React.createElement("stop", {
    offset: "0%",
    stopColor: "rgba(110, 130, 150, 0.55)"
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "55%",
    stopColor: "rgba(170, 165, 160, 0.55)"
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "100%",
    stopColor: "rgba(220, 175, 125, 0.65)"
  })), /*#__PURE__*/React.createElement("linearGradient", {
    id: "tn-ocean",
    x1: "0",
    y1: "0",
    x2: "0",
    y2: "1"
  }, /*#__PURE__*/React.createElement("stop", {
    offset: "0%",
    stopColor: "rgba(100, 120, 135, 0.55)"
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "100%",
    stopColor: "rgba(55, 75, 90, 0.85)"
  })), /*#__PURE__*/React.createElement("linearGradient", {
    id: "tn-glow",
    x1: "0",
    y1: "0",
    x2: "0",
    y2: "1"
  }, /*#__PURE__*/React.createElement("stop", {
    offset: "0%",
    stopColor: "rgba(235, 190, 130, 0)"
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "50%",
    stopColor: "rgba(235, 190, 130, 0.65)"
  }), /*#__PURE__*/React.createElement("stop", {
    offset: "100%",
    stopColor: "rgba(235, 190, 130, 0)"
  }))), /*#__PURE__*/React.createElement("rect", {
    x: "0",
    y: "0",
    width: W,
    height: horizonY + 2,
    fill: "url(#tn-sky)"
  }), /*#__PURE__*/React.createElement("rect", {
    x: "0",
    y: horizonY - 14,
    width: W,
    height: 28,
    fill: "url(#tn-glow)"
  }), /*#__PURE__*/React.createElement("rect", {
    x: "0",
    y: horizonY,
    width: W,
    height: H - horizonY,
    fill: "url(#tn-ocean)"
  }), wave(horizonY + 4, 3, 0.040, 0.0, 'rgba(115, 135, 150, 0.50)', 'w1'), wave(horizonY + 32, 7, 0.026, 1.2, 'rgba(85, 105, 120, 0.65)', 'w2'), wave(horizonY + 78, 12, 0.018, 2.4, 'rgba(65, 85, 100, 0.78)', 'w3'), wave(horizonY + 170, 20, 0.013, 3.6, 'rgba(48, 65, 80, 0.88)', 'w4'), wave(horizonY + 300, 32, 0.009, 4.8, 'rgba(35, 50, 65, 0.95)', 'w5'), /*#__PURE__*/React.createElement("rect", {
    x: "0",
    y: horizonY - 6,
    width: W,
    height: 40,
    fill: "rgba(255,255,255,0.04)"
  }));
}

// Tall sailing ship silhouette — three masts, square-rigged sails.
// Sits at the bottom of the frame (canvas y ≈ 990), no higher than the
// initial seascape band, regardless of how much the color field has grown.
function Boat({
  t,
  W,
  fieldH,
  alpha
}) {
  // Subtle pitch (degrees), driven by long-period sine to suggest swell
  const tilt = Math.sin(t * 0.5) * 1.6;
  const bob = Math.sin(t * 0.4 + 1.1) * 3;
  const boatW = 300;
  const boatH = 170; // hull + masts + sails
  const cx = W * 0.46;
  // Anchor to BOTTOM of field so the ship stays low in frame as field grows.
  // The field bottom == canvas bottom (1080), so this is canvas y ≈ 990.
  const cy = fieldH - 60;
  const HULL_C = 'rgba(22, 30, 38, 0.95)';
  const RIG_C = 'rgba(22, 30, 38, 0.90)';
  const SAIL_C = 'rgba(38, 50, 62, 0.88)';
  const mid = boatW / 2;
  const HY = boatH; // hull top reference

  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: cx - boatW / 2,
      top: cy - boatH + bob,
      width: boatW,
      height: boatH + 30,
      opacity: alpha,
      transform: `rotate(${tilt}deg)`,
      transformOrigin: '50% 92%',
      pointerEvents: 'none'
    }
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: `0 0 ${boatW} ${boatH + 30}`,
    width: boatW,
    height: boatH + 30
  }, /*#__PURE__*/React.createElement("path", {
    d: `
          M 22 ${HY - 6}
          Q 32 ${HY + 18}, 70 ${HY + 22}
          L ${boatW - 70} ${HY + 22}
          Q ${boatW - 30} ${HY + 18}, ${boatW - 18} ${HY - 6}
          L ${boatW - 26} ${HY - 14}
          L ${boatW - 46} ${HY - 14}
          L ${boatW - 50} ${HY - 6}
          L 50 ${HY - 6}
          L 46 ${HY - 14}
          L 26 ${HY - 14}
          Z
        `,
    fill: HULL_C
  }), /*#__PURE__*/React.createElement("line", {
    x1: "14",
    y1: HY - 12,
    x2: "58",
    y2: HY - 22,
    stroke: RIG_C,
    strokeWidth: "2",
    strokeLinecap: "round"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "74",
    y1: HY - 6,
    x2: "74",
    y2: "24",
    stroke: RIG_C,
    strokeWidth: "2.2",
    strokeLinecap: "round"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "44",
    y1: "38",
    x2: "104",
    y2: "38",
    stroke: RIG_C,
    strokeWidth: "1.6"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "50",
    y1: "72",
    x2: "98",
    y2: "72",
    stroke: RIG_C,
    strokeWidth: "1.6"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "54",
    y1: "104",
    x2: "94",
    y2: "104",
    stroke: RIG_C,
    strokeWidth: "1.6"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M 50 38 L 98 38 L 94 72 L 54 72 Z",
    fill: SAIL_C
  }), /*#__PURE__*/React.createElement("path", {
    d: "M 56 72 L 92 72 L 88 104 L 60 104 Z",
    fill: SAIL_C
  }), /*#__PURE__*/React.createElement("path", {
    d: `M 62 104 L 86 104 L 82 ${HY - 8} L 66 ${HY - 8} Z`,
    fill: SAIL_C
  }), /*#__PURE__*/React.createElement("line", {
    x1: mid,
    y1: HY - 6,
    x2: mid,
    y2: "8",
    stroke: RIG_C,
    strokeWidth: "2.6",
    strokeLinecap: "round"
  }), /*#__PURE__*/React.createElement("line", {
    x1: mid - 42,
    y1: "26",
    x2: mid + 42,
    y2: "26",
    stroke: RIG_C,
    strokeWidth: "1.8"
  }), /*#__PURE__*/React.createElement("line", {
    x1: mid - 50,
    y1: "62",
    x2: mid + 50,
    y2: "62",
    stroke: RIG_C,
    strokeWidth: "1.8"
  }), /*#__PURE__*/React.createElement("line", {
    x1: mid - 44,
    y1: "98",
    x2: mid + 44,
    y2: "98",
    stroke: RIG_C,
    strokeWidth: "1.8"
  }), /*#__PURE__*/React.createElement("path", {
    d: `M ${mid - 38} 26 L ${mid + 38} 26 L ${mid + 46} 62 L ${mid - 46} 62 Z`,
    fill: SAIL_C
  }), /*#__PURE__*/React.createElement("path", {
    d: `M ${mid - 42} 62 L ${mid + 42} 62 L ${mid + 40} 98 L ${mid - 40} 98 Z`,
    fill: SAIL_C
  }), /*#__PURE__*/React.createElement("path", {
    d: `M ${mid - 36} 98 L ${mid + 36} 98 L ${mid + 30} ${HY - 8} L ${mid - 30} ${HY - 8} Z`,
    fill: SAIL_C
  }), /*#__PURE__*/React.createElement("line", {
    x1: boatW - 74,
    y1: HY - 6,
    x2: boatW - 74,
    y2: "28",
    stroke: RIG_C,
    strokeWidth: "2.2",
    strokeLinecap: "round"
  }), /*#__PURE__*/React.createElement("line", {
    x1: boatW - 104,
    y1: "44",
    x2: boatW - 44,
    y2: "44",
    stroke: RIG_C,
    strokeWidth: "1.6"
  }), /*#__PURE__*/React.createElement("line", {
    x1: boatW - 98,
    y1: "78",
    x2: boatW - 50,
    y2: "78",
    stroke: RIG_C,
    strokeWidth: "1.6"
  }), /*#__PURE__*/React.createElement("path", {
    d: `M ${boatW - 100} 44 L ${boatW - 48} 44 L ${boatW - 52} 78 L ${boatW - 96} 78 Z`,
    fill: SAIL_C
  }), /*#__PURE__*/React.createElement("path", {
    d: `M ${boatW - 94}  78 L ${boatW - 54} 78 L ${boatW - 58} ${HY - 8} L ${boatW - 90} ${HY - 8} Z`,
    fill: SAIL_C
  }), /*#__PURE__*/React.createElement("path", {
    d: `M ${mid} 8 L ${mid + 12} 12 L ${mid} 16 Z`,
    fill: RIG_C
  }), /*#__PURE__*/React.createElement("line", {
    x1: "34",
    y1: HY + 26,
    x2: boatW - 30,
    y2: HY + 26,
    stroke: "rgba(255,255,255,0.06)",
    strokeWidth: "1"
  })));
}

// ── Signature line ─────────────────────────────────────────────────────────
function SignatureLine({
  appearAt
}) {
  const t = useTime();
  if (t < appearAt - 0.3) return null;
  const op = clamp((t - appearAt) / 1.4, 0, 1) * 0.78;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      left: 0,
      right: 0,
      // Sits in the wide bottom margin under everything
      bottom: 56,
      textAlign: 'center',
      fontFamily: SERIF,
      fontStyle: 'italic',
      fontWeight: 300,
      fontSize: 22,
      letterSpacing: '0.02em',
      color: PAPER,
      opacity: op,
      transform: `translateY(${(1 - clamp((t - appearAt) / 1.4, 0, 1)) * 6}px)`,
      zIndex: 10,
      textShadow: '0 0 24px rgba(0,0,0,0.5)'
    }
  }, "Before the script. Before the shot. Tenet.");
}

// ── Composition ────────────────────────────────────────────────────────────
function TenetFirstExperience({
  pace = 1.0,
  intensity = 1.0,
  showSignature = true,
  initialA = 'A',
  initialB = 'B',
  fontSize = 17
}) {
  // Build dialogue timeline
  const timeline = React.useMemo(() => buildDialogueTimeline(DIALOGUE, T_DIALOGUE_START, pace), [pace]);

  // Total dialogue end
  const last = timeline[timeline.length - 1];
  const dialogueEnd = last.time + last.dur;

  // Script icon: opens at a fixed wall-clock time so the user can plan around
  // the box appearing at exactly the 1-minute mark.
  const scriptOpenAt = T_BOX_OPEN;
  // Cursor returns later and drags the window's bottom-right corner to expand.
  const expandAt = T_BOX_EXPAND;

  // Seascape phases
  const defStart = dialogueEnd - 16;
  const defEnd = dialogueEnd + 1;

  // Signature appears after definition completes
  const signatureAt = defEnd + SIGNATURE_LEAD;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(Wordmark, null), /*#__PURE__*/React.createElement(Seascape, {
    start: SEASCAPE_START,
    defStart: defStart,
    defEnd: defEnd,
    intensity: intensity,
    dialogueTimeline: timeline
  }), /*#__PURE__*/React.createElement(CallInitiation, {
    initialA: initialA,
    initialB: initialB
  }), /*#__PURE__*/React.createElement(VoiceWaveform, null), /*#__PURE__*/React.createElement(DialogueFeed, {
    timeline: timeline,
    fontSize: fontSize
  }), /*#__PURE__*/React.createElement(ScriptWorkspace, {
    scriptOpenAt: scriptOpenAt,
    expandAt: expandAt,
    dialogueTimeline: timeline
  }), /*#__PURE__*/React.createElement(TimelineExpander, {
    appearAt: defEnd + 1.0
  }), showSignature && /*#__PURE__*/React.createElement(SignatureLine, {
    appearAt: signatureAt
  }));
}

// expose composition + total duration helper for host
window.TenetFirstExperience = TenetFirstExperience;
window.computeTenetDuration = function (pace = 1.0) {
  const timeline = buildDialogueTimeline(DIALOGUE, T_DIALOGUE_START, pace);
  const last = timeline[timeline.length - 1];
  const dialogueEnd = last.time + last.dur;
  return dialogueEnd + 1 + SIGNATURE_LEAD + 3; // + hold for signature
};
})(); } catch (e) { __ds_ns.__errors.push({ path: "scene.jsx", error: String((e && e.message) || e) }); }

// tenet-demo/App.jsx
try { (() => {
/* global React, ReactDOM, TK, Glyph, BreathingDot, ClientSeat, DirectorSeat */
// One source of truth, two reactive views, one translation wire between them.

const {
  useState,
  useMemo,
  useRef,
  useEffect
} = React;

// ── The translation band — the wire between the two languages ───────
function MathChip({
  k,
  v
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 7,
      padding: '6px 11px',
      borderRadius: 2,
      background: TK.PAPER_SOFT,
      border: `1px solid ${TK.BORDER}`,
      fontFamily: TK.MONO,
      fontSize: 12,
      color: TK.INK,
      whiteSpace: 'nowrap'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: TK.INK_FAINT,
      fontSize: 10,
      letterSpacing: '0.1em',
      textTransform: 'uppercase'
    }
  }, k === 'cost' ? 'Cost' : 'Date'), v);
}
function RecvChip({
  children
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      padding: '6px 11px',
      borderRadius: 2,
      background: TK.PAPER,
      border: `1px solid ${TK.BORDER}`,
      fontFamily: TK.SANS,
      fontSize: 13,
      color: TK.INK_MUTED,
      whiteSpace: 'nowrap'
    }
  }, children);
}
function Translation({
  tr
}) {
  return /*#__PURE__*/React.createElement("div", {
    key: tr.id,
    style: {
      display: 'flex',
      alignItems: 'center',
      width: '100%',
      gap: 28
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      minWidth: 0,
      maxWidth: 380
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.MONO,
      fontSize: 10,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: TK.INK_FAINT,
      marginBottom: 6
    }
  }, tr.fromSeat, " said"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.SERIF,
      fontStyle: 'italic',
      fontSize: 20,
      color: TK.INK,
      lineHeight: 1.1,
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: TK.AMBER
    }
  }, tr.spokeLabel), " \xB7 ", tr.spoke)), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10,
      color: TK.AMBER
    }
  }, /*#__PURE__*/React.createElement(BreathingDot, null), /*#__PURE__*/React.createElement(Glyph, {
    name: "arrow",
    size: 26
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.MONO,
      fontSize: 9,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: TK.INK_FAINT
    }
  }, tr.dir === 'up' ? 'Upstream' : 'Downstream')), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.MONO,
      fontSize: 10,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: TK.INK_FAINT,
      marginBottom: 6
    }
  }, tr.toSeat, " received"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 8
    }
  }, tr.received.length ? tr.received.map((r, i) => /*#__PURE__*/React.createElement(RecvChip, {
    key: i
  }, r)) : /*#__PURE__*/React.createElement(RecvChip, null, "no change"))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: '0 0 auto',
      display: 'flex',
      gap: 8
    }
  }, tr.chips.map((c, i) => /*#__PURE__*/React.createElement(MathChip, {
    key: i,
    k: c.k,
    v: c.v
  }))));
}
function IdleBand() {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      justifyContent: 'center',
      width: '100%',
      gap: 7
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement(BreathingDot, null), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: TK.MONO,
      fontSize: 10,
      letterSpacing: '0.22em',
      textTransform: 'uppercase',
      color: TK.AMBER
    }
  }, "The coordination layer \xB7 bidirectional")), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: TK.SERIF,
      fontStyle: 'italic',
      fontSize: 20,
      color: TK.INK
    }
  }, "Opacity, replaced with mathematics. Turn a dial \u2014 one event, two languages."));
}

// ── The app ─────────────────────────────────────────────────────────
function App() {
  const M = window.TENET;
  const [state, setState] = useState(M.DEFAULT_STATE);
  const [revealed, setRevealed] = useState(false);
  const [translation, setTranslation] = useState(null);
  const trId = useRef(0);
  const derived = useMemo(() => M.recompute(state), [state]);
  useEffect(() => {
    fitStage();
    const t1 = setTimeout(fitStage, 50);
    const t2 = setTimeout(fitStage, 200);
    return () => {
      clearTimeout(t1);
      clearTimeout(t2);
    };
  }, []);
  function commit(nextState, action) {
    const next = M.recompute(nextState);
    const tr = M.translate(action, derived, next);
    tr.id = ++trId.current;
    setState(nextState);
    setTranslation(tr);
    setRevealed(true);
  }
  function toggleScene(id) {
    const scene = M.SCENES.find(s => s.id === id);
    const wasCut = !!state.cuts[id];
    const cuts = Object.assign({}, state.cuts);
    if (wasCut) delete cuts[id];else cuts[id] = true;
    const nextState = Object.assign({}, state, {
      cuts
    });
    const next = M.recompute(nextState);
    const dropLoc = !wasCut && derived.locations.includes(scene.location) && !next.locations.includes(scene.location);
    const dropPrin = !wasCut && scene.principals.find(p => derived.principals.includes(p) && !next.principals.includes(p));
    const addPrin = wasCut && scene.principals.find(p => !derived.principals.includes(p) && next.principals.includes(p));
    commit(nextState, {
      dir: 'down',
      type: wasCut ? 'restore' : 'cut',
      scene,
      dropLoc,
      dropPrin,
      addPrin
    });
  }
  function setHero(len) {
    if (len === state.heroLen) return;
    commit(Object.assign({}, state, {
      heroLen: len
    }), {
      dir: 'down',
      type: 'hero',
      len
    });
  }
  function toggleTokyo() {
    const on = !state.addTokyo;
    commit(Object.assign({}, state, {
      addTokyo: on
    }), {
      dir: 'down',
      type: 'tokyo',
      on
    });
  }
  function toggleFlag() {
    const on = !state.flag;
    commit(Object.assign({}, state, {
      flag: on
    }), {
      dir: 'up',
      on
    });
  }
  function reset() {
    setState(M.DEFAULT_STATE);
    setTranslation(null);
    setRevealed(false);
  }
  return /*#__PURE__*/React.createElement("div", {
    className: "stage",
    id: "stage"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: 'absolute',
      top: 30,
      left: 70,
      zIndex: 20,
      fontFamily: TK.SERIF,
      fontStyle: 'italic',
      fontSize: 22,
      color: TK.INK_FAINT,
      letterSpacing: '0.02em'
    }
  }, "tenet"), revealed && /*#__PURE__*/React.createElement("button", {
    onClick: reset,
    title: "Reset the demo",
    style: {
      position: 'absolute',
      top: 26,
      right: 64,
      zIndex: 20,
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      background: 'transparent',
      border: `1px solid ${TK.BORDER}`,
      borderRadius: 2,
      padding: '7px 13px',
      cursor: 'pointer',
      color: TK.INK_MUTED,
      fontFamily: TK.MONO,
      fontSize: 11,
      letterSpacing: '0.14em',
      textTransform: 'uppercase'
    }
  }, /*#__PURE__*/React.createElement(Glyph, {
    name: "reset",
    size: 14
  }), " Reset"), /*#__PURE__*/React.createElement("div", {
    className: 'panels' + (revealed ? ' revealed' : '')
  }, /*#__PURE__*/React.createElement("div", {
    className: "panel panel-client"
  }, /*#__PURE__*/React.createElement("div", {
    className: "client-wrap"
  }, /*#__PURE__*/React.createElement(ClientSeat, {
    state: state,
    derived: derived,
    onToggleScene: toggleScene,
    onHero: setHero,
    onTokyo: toggleTokyo
  }))), /*#__PURE__*/React.createElement("div", {
    className: "panel panel-director"
  }, /*#__PURE__*/React.createElement("div", {
    className: "director-inner"
  }, /*#__PURE__*/React.createElement(DirectorSeat, {
    state: state,
    derived: derived,
    onFlag: toggleFlag
  })))), /*#__PURE__*/React.createElement("div", {
    className: "seam-band"
  }, translation ? /*#__PURE__*/React.createElement(Translation, {
    tr: translation
  }) : /*#__PURE__*/React.createElement(IdleBand, null)));
}

// ── Scale the fixed 1920×1080 stage to fit the viewport ─────────────
function fitStage() {
  const stage = document.getElementById('stage');
  if (!stage) return;
  const s = Math.min(window.innerWidth / 1920, window.innerHeight / 1080);
  stage.style.transform = `translate(-50%, -50%) scale(${s})`;
}
window.addEventListener('resize', fitStage);
ReactDOM.createRoot(document.getElementById('root')).render(/*#__PURE__*/React.createElement(App, null));
})(); } catch (e) { __ds_ns.__errors.push({ path: "tenet-demo/App.jsx", error: String((e && e.message) || e) }); }

// tenet-demo/ClientSeat.jsx
try { (() => {
/* global React, TK, Glyph, useTween, Switch, Segmented, BreathingDot */
// The client seat — the analytics view. Production made legible:
// total cost and delivery date dominate; everything else is quiet display.
// The dials are the only things that say "touch me."

function useChangeFlash(key) {
  const [flash, setFlash] = React.useState(0);
  const prev = React.useRef(key);
  React.useEffect(() => {
    if (prev.current !== key) {
      prev.current = key;
      setFlash(1);
      const id = setTimeout(() => setFlash(0), 700);
      return () => clearTimeout(id);
    }
  }, [key]);
  return flash;
}
function HeroFigure({
  label,
  children,
  flashKey,
  accentWhenFlash
}) {
  const flash = useChangeFlash(flashKey);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.MONO,
      fontSize: 11,
      letterSpacing: '0.18em',
      textTransform: 'uppercase',
      color: TK.INK_FAINT,
      marginBottom: 14
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.SANS,
      fontWeight: 300,
      fontSize: 76,
      lineHeight: 0.95,
      letterSpacing: '-0.02em',
      color: flash && accentWhenFlash ? TK.AMBER : TK.INK,
      transition: 'color .7s ease-out'
    }
  }, children));
}
function SecondaryStat({
  label,
  value,
  flashKey
}) {
  const flash = useChangeFlash(flashKey);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.SANS,
      fontWeight: 300,
      fontSize: 34,
      lineHeight: 1,
      color: flash ? TK.AMBER : TK.INK,
      transition: 'color .7s ease-out'
    }
  }, value), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: TK.INK_FAINT,
      marginTop: 8
    }
  }, label));
}
function DialRow({
  children,
  last
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 16,
      padding: '11px 0',
      borderBottom: last ? 'none' : `1px solid ${TK.BORDER}`
    }
  }, children);
}
function ClientSeat({
  state,
  derived,
  onToggleScene,
  onHero,
  onTokyo
}) {
  const cost = useTween(derived.cost, 700);
  const runtime = useTween(derived.runtime, 700);
  const shots = useTween(derived.shots, 700);
  const dp = window.TENET.fmtDateParts(derived.delivery);
  const cuttable = window.TENET.SCENES.filter(s => s.cuttable);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      boxSizing: 'border-box',
      padding: '64px 70px 56px',
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      marginBottom: 4
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: TK.MONO,
      fontSize: 11,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: TK.AMBER
    }
  }, "Client"), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 26,
      height: 1,
      background: TK.BORDER_STRONG
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: TK.MONO,
      fontSize: 11,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: TK.INK_FAINT
    }
  }, "Analytics")), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.SERIF,
      fontStyle: 'italic',
      fontWeight: 400,
      fontSize: 38,
      lineHeight: 1.05,
      color: TK.INK,
      marginTop: 6
    }
  }, "Meridian \u2014 A Day Held"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.SANS,
      fontSize: 14,
      color: TK.INK_MUTED,
      marginTop: 8
    }
  }, "Brand film \xB7 Meridian Watches \xB7 the room you steer, not the one you wait on"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 40,
      marginTop: 44
    }
  }, /*#__PURE__*/React.createElement(HeroFigure, {
    label: "Total cost",
    flashKey: derived.cost,
    accentWhenFlash: true
  }, window.TENET.fmtMoney(cost)), /*#__PURE__*/React.createElement(HeroFigure, {
    label: "Delivery",
    flashKey: derived.days,
    accentWhenFlash: true
  }, dp.mon, " ", dp.day, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 26,
      color: TK.INK_FAINT,
      marginLeft: 12
    }
  }, dp.year))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 24,
      marginTop: 46,
      paddingTop: 26,
      borderTop: `1px solid ${TK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement(SecondaryStat, {
    label: "Runtime",
    value: window.TENET.fmtRuntime(Math.round(runtime)),
    flashKey: derived.runtime
  }), /*#__PURE__*/React.createElement(SecondaryStat, {
    label: "Shots",
    value: Math.round(shots),
    flashKey: derived.shots
  }), /*#__PURE__*/React.createElement(SecondaryStat, {
    label: "Locations",
    value: derived.locations.length,
    flashKey: derived.locations.length
  }), /*#__PURE__*/React.createElement(SecondaryStat, {
    label: "Principal cast",
    value: derived.principals.length,
    flashKey: derived.principals.length
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'auto',
      paddingTop: 40
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 10,
      marginBottom: 6
    }
  }, /*#__PURE__*/React.createElement(BreathingDot, null), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: TK.MONO,
      fontSize: 11,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: TK.AMBER,
      whiteSpace: 'nowrap'
    }
  }, "Adjust the cut"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: TK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: TK.INK_FAINT,
      whiteSpace: 'nowrap',
      overflow: 'hidden',
      textOverflow: 'ellipsis'
    }
  }, "\u2014 one change a client makes on a call")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 48,
      marginTop: 14
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1.2
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: TK.INK_FAINT,
      marginBottom: 4
    }
  }, "Scenes in the cut"), cuttable.map((s, i) => {
    const cut = !!state.cuts[s.id];
    return /*#__PURE__*/React.createElement(DialRow, {
      key: s.id,
      last: i === cuttable.length - 1
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: TK.MONO,
        fontSize: 11,
        color: TK.INK_FAINT,
        width: 30
      }
    }, "SC", s.n), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        fontFamily: TK.SERIF,
        fontStyle: 'italic',
        fontSize: 17,
        color: cut ? TK.INK_FAINT : TK.INK,
        textDecorationLine: cut ? 'line-through' : 'none',
        textDecorationColor: TK.AMBER,
        transition: 'color .3s ease-out'
      }
    }, s.name), /*#__PURE__*/React.createElement(Switch, {
      on: !cut,
      onClick: () => onToggleScene(s.id)
    }));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      display: 'flex',
      flexDirection: 'column',
      gap: 30
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: TK.INK_FAINT,
      marginBottom: 12
    }
  }, "Hero macro length"), /*#__PURE__*/React.createElement(Segmented, {
    options: [{
      value: 15,
      label: '15s'
    }, {
      value: 10,
      label: '10s'
    }, {
      value: 6,
      label: '6s'
    }],
    value: state.heroLen,
    onChange: onHero
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: TK.INK_FAINT,
      marginTop: 12,
      maxWidth: 280
    }
  }, "The product shot. Shorten it and the comp work changes downstream.")), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.MONO,
      fontSize: 10,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: TK.INK_FAINT,
      marginBottom: 12
    }
  }, "Second location"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 14
    }
  }, /*#__PURE__*/React.createElement(Switch, {
    on: state.addTokyo,
    onClick: onTokyo
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: TK.SERIF,
      fontStyle: 'italic',
      fontSize: 17,
      color: state.addTokyo ? TK.INK : TK.INK_FAINT,
      transition: 'color .3s ease-out'
    }
  }, "Add Tokyo street")))))));
}
window.ClientSeat = ClientSeat;
})(); } catch (e) { __ds_ns.__errors.push({ path: "tenet-demo/ClientSeat.jsx", error: String((e && e.message) || e) }); }

// tenet-demo/DirectorSeat.jsx
try { (() => {
/* global React, TK, Glyph, useTween, Switch, DeptChip, BreathingDot */
// The director seat — the shot view. The client's budget-language change
// arrives here as the thing the director actually needs: shots struck,
// a location dropped, downstream tasks that evaporate.

// Full superset of scenes (Tokyo always in the DOM so it can collapse in/out).
function allScenesInOrder() {
  const out = [];
  window.TENET.SCENES.forEach(s => {
    out.push(s);
    if (s.id === 's3') out.push(window.TENET.TOKYO);
  });
  return out;
}

// A task chip that collapses horizontally when it evaporates.
function TaskSlot({
  present,
  dept,
  label,
  atRisk
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      overflow: 'hidden',
      whiteSpace: 'nowrap',
      maxWidth: present ? 160 : 0,
      opacity: present ? 1 : 0,
      marginRight: present ? 8 : 0,
      transform: present ? 'translateX(0)' : 'translateX(-6px)',
      transition: 'max-width .42s cubic-bezier(.4,0,.2,1), opacity .34s ease-out, margin-right .42s ease-out, transform .42s ease-out'
    }
  }, /*#__PURE__*/React.createElement(DeptChip, {
    dept: dept,
    label: label,
    atRisk: atRisk
  }));
}
function SceneRow({
  scene,
  present,
  tasks
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      maxHeight: present ? 96 : 0,
      opacity: present ? 1 : 0,
      overflow: 'hidden',
      transition: 'max-height .45s cubic-bezier(.4,0,.2,1), opacity .34s ease-out'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 18,
      padding: '15px 0',
      borderBottom: `1px solid ${TK.BORDER}`
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: TK.MONO,
      fontSize: 11,
      color: TK.INK_FAINT,
      width: 34,
      flexShrink: 0
    }
  }, "SC", scene.n), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.SERIF,
      fontStyle: 'italic',
      fontSize: 18,
      color: TK.INK,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap'
    }
  }, scene.name), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.SANS,
      fontSize: 12,
      color: TK.INK_FAINT,
      marginTop: 3,
      display: 'flex',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("span", null, scene.detail), /*#__PURE__*/React.createElement("span", {
    style: {
      color: TK.BORDER_STRONG
    }
  }, "\xB7"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: TK.MONO
    }
  }, scene.location))), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: TK.MONO,
      fontSize: 12,
      color: TK.INK_MUTED,
      width: 70,
      flexShrink: 0,
      textAlign: 'right'
    }
  }, scene.shots, " shots"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      width: 230,
      flexShrink: 0,
      justifyContent: 'flex-end'
    }
  }, tasks.map((t, i) => /*#__PURE__*/React.createElement(TaskSlot, {
    key: t.label + i,
    present: t.present,
    dept: t.dept,
    label: t.label,
    atRisk: t.atRisk
  })))));
}
function DirectorSeat({
  state,
  derived,
  onFlag
}) {
  const shots = useTween(derived.shots, 700);
  const tasks = useTween(derived.taskCount, 700);
  const scenes = allScenesInOrder();
  const heroPresent = !state.cuts['s5'];
  const heroHasComp = state.heroLen !== 6;
  const flagAvailable = heroPresent && heroHasComp;

  // Resolve each scene's task presence (superset, so chips can collapse).
  function tasksFor(s) {
    if (s.isHero) {
      const comp = {
        dept: 'VFX',
        label: 'Product comp',
        present: heroHasComp && !state.cuts[s.id],
        atRisk: state.flag
      };
      return [comp, {
        dept: 'Color',
        label: 'Grade',
        present: !state.cuts[s.id]
      }, {
        dept: 'Sound',
        label: 'Mix',
        present: !state.cuts[s.id]
      }];
    }
    const present = s.id === 'sT' ? state.addTokyo : !state.cuts[s.id];
    return s.tasks.map(t => ({
      dept: t.dept,
      label: t.label,
      present
    }));
  }
  function presentOf(s) {
    return s.id === 'sT' ? state.addTokyo : !state.cuts[s.id];
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      boxSizing: 'border-box',
      padding: '64px 70px 56px',
      display: 'flex',
      flexDirection: 'column',
      background: TK.PAPER_SOFT
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      marginBottom: 4
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: TK.MONO,
      fontSize: 11,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: TK.AMBER
    }
  }, "Director"), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 26,
      height: 1,
      background: TK.BORDER_STRONG
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: TK.MONO,
      fontSize: 11,
      letterSpacing: '0.16em',
      textTransform: 'uppercase',
      color: TK.INK_FAINT,
      whiteSpace: 'nowrap'
    }
  }, "Shot breakdown")), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.SERIF,
      fontStyle: 'italic',
      fontWeight: 400,
      fontSize: 38,
      lineHeight: 1.05,
      color: TK.INK,
      marginTop: 6
    }
  }, "The work, as shots."), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.SANS,
      fontSize: 14,
      color: TK.INK_MUTED,
      marginTop: 8,
      display: 'flex',
      gap: 18
    }
  }, /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("b", {
    style: {
      fontWeight: 400,
      color: TK.INK
    }
  }, Math.round(shots)), " shots"), /*#__PURE__*/React.createElement("span", {
    style: {
      color: TK.BORDER_STRONG
    }
  }, "\xB7"), /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("b", {
    style: {
      fontWeight: 400,
      color: TK.INK
    }
  }, derived.scenes.length), " scenes"), /*#__PURE__*/React.createElement("span", {
    style: {
      color: TK.BORDER_STRONG
    }
  }, "\xB7"), /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("b", {
    style: {
      fontWeight: 400,
      color: TK.INK
    }
  }, Math.round(tasks)), " downstream tasks")), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 26
    }
  }, scenes.map(s => /*#__PURE__*/React.createElement(SceneRow, {
    key: s.id,
    scene: s,
    present: presentOf(s),
    tasks: tasksFor(s)
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 'auto',
      paddingTop: 30
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 10,
      marginBottom: 14
    }
  }, /*#__PURE__*/React.createElement(BreathingDot, null), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: TK.MONO,
      fontSize: 11,
      letterSpacing: '0.2em',
      textTransform: 'uppercase',
      color: TK.AMBER,
      whiteSpace: 'nowrap'
    }
  }, "Schedule check"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: TK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: TK.INK_FAINT,
      whiteSpace: 'nowrap'
    }
  }, "\u2014 run the moat the other way")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 18,
      padding: '18px 22px',
      background: TK.PAPER,
      border: `1px solid ${state.flag ? TK.AMBER : TK.BORDER}`,
      borderRadius: 4,
      transition: 'border-color .3s ease-out',
      opacity: flagAvailable ? 1 : 0.45
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: state.flag ? TK.AMBER : TK.INK_MUTED,
      display: 'flex',
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement(Glyph, {
    name: "alert",
    size: 22
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.SANS,
      fontSize: 15,
      color: TK.INK
    }
  }, "SC5 \xB7 Product comp \u2014 280 render-hours over the window"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: TK.SERIF,
      fontStyle: 'italic',
      fontSize: 13,
      color: TK.INK_MUTED,
      marginTop: 3
    }
  }, flagAvailable ? state.flag ? 'Flagged. A second vendor is on it — and the client already knows the cost.' : 'This VFX shot can\u2019t land in the timeline. Flag it and watch it ripple up.' : heroPresent ? 'The 6s cut has no product comp — nothing to flag.' : 'SC5 is cut — nothing to flag.')), /*#__PURE__*/React.createElement(Switch, {
    on: state.flag,
    onClick: onFlag,
    disabled: !flagAvailable
  }))));
}
window.DirectorSeat = DirectorSeat;
})(); } catch (e) { __ds_ns.__errors.push({ path: "tenet-demo/DirectorSeat.jsx", error: String((e && e.message) || e) }); }

// tenet-demo/model.js
try { (() => {
/* ============================================================
   TENET — Live Demo · the one project, modeled deep.
   ------------------------------------------------------------
   The credibility lives in the recompute. We hardcode the INPUT
   data (scenes) and derive every total live, so the numbers can
   never drift and survive an unscripted poke.

   Mechanism decisions (reported back to the room):
   • Translation: pre-authored, deterministic phrasing assembled
     from the live diff. No network call. "The production system
     generates these with AI; for the demo they're pre-validated
     on this one project so it's deterministic in the room."
   • Recompute: client-side arithmetic. Instant, identical every
     time, incapable of an empty or wrong number.
   ============================================================ */
window.TENET = function () {
  // ── The project — a brand spot the founder knows cold ──────────────
  // A 60-second film, six scenes. Totals are NEVER stored; always derived.
  const SCENES = [{
    id: 's1',
    n: 1,
    name: 'Dawn rooftop',
    detail: 'EXT · Dawn · the open',
    location: 'Rooftop',
    shots: 4,
    runtime: 12,
    cost: 96000,
    principals: ['Mara Lyne'],
    cuttable: true,
    tasks: [{
      dept: 'VFX',
      label: 'Sky cleanup'
    }, {
      dept: 'Color',
      label: 'Grade'
    }, {
      dept: 'Sound',
      label: 'Design'
    }]
  }, {
    id: 's2',
    n: 2,
    name: 'The workshop',
    detail: 'INT · Day · the craft',
    location: 'Workshop',
    shots: 5,
    runtime: 10,
    cost: 72000,
    principals: [],
    cuttable: true,
    tasks: [{
      dept: 'Color',
      label: 'Grade'
    }, {
      dept: 'Sound',
      label: 'Foley'
    }]
  }, {
    id: 's3',
    n: 3,
    name: 'Street, in motion',
    detail: 'EXT · Day · Steadicam',
    location: 'Downtown',
    shots: 6,
    runtime: 8,
    cost: 84000,
    principals: ['Mara Lyne'],
    cuttable: true,
    tasks: [{
      dept: 'VFX',
      label: 'Plate cleanup'
    }, {
      dept: 'Color',
      label: 'Grade'
    }, {
      dept: 'Sound',
      label: 'Design'
    }]
  }, {
    id: 's4',
    n: 4,
    name: 'Interior, the gift',
    detail: 'INT · Night · two-hander',
    location: 'Apartment',
    shots: 3,
    runtime: 9,
    cost: 58000,
    principals: ['Mara Lyne', 'Theo Ross'],
    cuttable: true,
    tasks: [{
      dept: 'Color',
      label: 'Grade'
    }, {
      dept: 'Sound',
      label: 'Dialogue'
    }]
  }, {
    id: 's5',
    n: 5,
    name: 'Product macro',
    detail: 'INT · Studio · the hero',
    location: 'Studio',
    shots: 4,
    runtime: 15,
    cost: 110000,
    principals: [],
    cuttable: false,
    isHero: true,
    tasks: [{
      dept: 'VFX',
      label: 'Product comp',
      id: 'hero-vfx'
    }, {
      dept: 'Color',
      label: 'Grade'
    }, {
      dept: 'Sound',
      label: 'Mix'
    }]
  }, {
    id: 's6',
    n: 6,
    name: 'Beach, resolve',
    detail: 'EXT · Sunset · drone + ground',
    location: 'Beach',
    shots: 3,
    runtime: 6,
    cost: 60000,
    principals: ['Mara Lyne'],
    cuttable: true,
    tasks: [{
      dept: 'VFX',
      label: 'Drone stitch'
    }, {
      dept: 'Color',
      label: 'Grade'
    }, {
      dept: 'Sound',
      label: 'Design'
    }]
  }];

  // Optional scene — the "add a location" dial brings this in after s3.
  const TOKYO = {
    id: 'sT',
    n: 7,
    name: 'Second city',
    detail: 'EXT · Dusk · Tokyo street',
    location: 'Tokyo',
    shots: 5,
    runtime: 8,
    cost: 90000,
    principals: ['Mara Lyne'],
    cuttable: true,
    added: true,
    tasks: [{
      dept: 'VFX',
      label: 'Plate cleanup'
    }, {
      dept: 'Color',
      label: 'Grade'
    }, {
      dept: 'Sound',
      label: 'Design'
    }]
  };

  // Hero-macro length dial — three validated stops.
  const HERO = {
    15: {
      runtime: 15,
      cost: 110000,
      heroVfx: true
    },
    10: {
      runtime: 10,
      cost: 82000,
      heroVfx: true
    },
    6: {
      runtime: 6,
      cost: 58000,
      heroVfx: false
    }
  };

  // Director → client constraint: a VFX shot that won't land in schedule.
  // Resolving it (a second vendor) costs money and time — felt by the client.
  const SURCHARGE = {
    cost: 28000,
    days: 5
  };

  // Schedule anchor — picture wraps; delivery is the post deadline.
  const WRAP = new Date(2026, 4, 30); // 30 May 2026

  const DEFAULT_STATE = {
    cuts: {},
    heroLen: 15,
    addTokyo: false,
    flag: false
  };

  // ── Build the active scene list from state ─────────────────────────
  function buildScenes(state) {
    const out = [];
    SCENES.forEach(s => {
      if (state.cuts[s.id]) return;
      let sc = Object.assign({}, s, {
        tasks: s.tasks.slice()
      });
      if (s.isHero) {
        const h = HERO[state.heroLen];
        sc.runtime = h.runtime;
        sc.cost = h.cost;
        sc.tasks = sc.tasks.filter(t => t.id !== 'hero-vfx' || h.heroVfx);
        // The flagged constraint lives on the hero comp.
        if (state.flag) sc.tasks = sc.tasks.map(t => t.id === 'hero-vfx' ? Object.assign({}, t, {
          atRisk: true
        }) : t);
      }
      out.push(sc);
      if (s.id === 's3' && state.addTokyo) out.push(Object.assign({}, TOKYO, {
        tasks: TOKYO.tasks.slice()
      }));
    });
    return out;
  }

  // ── The recompute — totals derived, never stored ───────────────────
  function recompute(state) {
    const scenes = buildScenes(state);
    let cost = 0,
      runtime = 0,
      shots = 0,
      taskCount = 0,
      vfxCount = 0;
    const locSet = [],
      prinSet = [];
    scenes.forEach(s => {
      cost += s.cost;
      runtime += s.runtime;
      shots += s.shots;
      taskCount += s.tasks.length;
      vfxCount += s.tasks.filter(t => t.dept === 'VFX').length;
      if (locSet.indexOf(s.location) < 0) locSet.push(s.location);
      s.principals.forEach(p => {
        if (prinSet.indexOf(p) < 0) prinSet.push(p);
      });
    });
    if (state.flag) cost += SURCHARGE.cost;
    const days = 14 + scenes.length * 2 + vfxCount * 2 + (state.flag ? SURCHARGE.days : 0);
    const delivery = addDays(WRAP, days);
    return {
      scenes,
      cost,
      runtime,
      shots,
      taskCount,
      vfxCount,
      locations: locSet,
      principals: prinSet,
      days,
      delivery
    };
  }

  // ── Formatters ─────────────────────────────────────────────────────
  function addDays(d, n) {
    const x = new Date(d);
    x.setDate(x.getDate() + n);
    return x;
  }
  const MON = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
  function fmtMoney(n) {
    return '$' + Math.round(n).toLocaleString('en-US');
  }
  function fmtDateParts(d) {
    return {
      mon: MON[d.getMonth()],
      day: d.getDate(),
      year: d.getFullYear()
    };
  }
  function fmtRuntime(s) {
    const m = Math.floor(s / 60),
      r = s % 60;
    return m ? `${m}:${String(r).padStart(2, '0')}` : `0:${String(r).padStart(2, '0')}`;
  }
  function signMoney(n) {
    return (n >= 0 ? '+' : '−') + fmtMoney(Math.abs(n)).replace('$', '');
  }
  function signMoney$(n) {
    return (n >= 0 ? '+$' : '−$') + Math.abs(n).toLocaleString('en-US');
  }
  function signDays(n) {
    return (n >= 0 ? '+' : '−') + Math.abs(n) + (Math.abs(n) === 1 ? ' day' : ' days');
  }

  // ── The translation layer — one specialist's change, said in the
  //    language the next specialist speaks. Assembled from the diff. ──
  function translate(action, prev, next) {
    const dShots = prev.shots - next.shots;
    const dLoc = prev.locations.length - next.locations.length;
    const dTasks = prev.taskCount - next.taskCount;
    const dVfx = prev.vfxCount - next.vfxCount;
    const dPrin = prev.principals.length - next.principals.length;
    const dRun = prev.runtime - next.runtime;
    const dCost = next.cost - prev.cost;
    const dDays = next.days - prev.days;

    // Director-language items (shots, locations, tasks, cast)
    function directorItems(opts) {
      opts = opts || {};
      const items = [];
      if (dShots > 0) items.push(`${dShots} shots struck`);else if (dShots < 0) items.push(`${-dShots} shots added`);
      if (opts.runtime && dRun) items.push(`runtime ${dRun > 0 ? '−' : '+'}${Math.abs(dRun)}s`);
      if (dLoc > 0) items.push(`${opts.location || 'a location'} dropped`);else if (dLoc < 0) items.push(`${opts.location || 'a location'} added`);
      if (dVfx > 0) items.push(`${dVfx} VFX ${dVfx === 1 ? 'task' : 'tasks'} cleared`);else if (dVfx < 0) items.push(`${-dVfx} VFX ${-dVfx === 1 ? 'task' : 'tasks'} added`);
      const dOther = dTasks - dVfx;
      if (dOther > 0) items.push(`${dOther} color/sound ${dOther === 1 ? 'task' : 'tasks'} cleared`);else if (dOther < 0) items.push(`${-dOther} color/sound ${-dOther === 1 ? 'task' : 'tasks'} added`);
      if (dPrin > 0) items.push(`${opts.principal || 'a principal'} released`);else if (dPrin < 0) items.push(`${opts.principal || 'a principal'} added`);
      return items;
    }

    // Client-language items (cost, date)
    function clientItems() {
      const items = [];
      if (dDays) items.push(`delivery ${signDays(dDays)}`);
      if (dCost) items.push(`${signMoney$(dCost)} · second VFX vendor`);
      return items;
    }
    if (action.dir === 'up') {
      // Director hit a constraint; it travels up as cost & date.
      return {
        dir: 'up',
        fromSeat: 'Director',
        toSeat: 'Client',
        spokeLabel: 'Constraint',
        spoke: action.on ? 'Product comp won\u2019t land' : 'Product comp back in schedule',
        received: clientItems(),
        chips: [{
          k: 'date',
          v: signDays(dDays)
        }, {
          k: 'cost',
          v: signMoney$(dCost)
        }]
      };
    }

    // Down — client spoke in budget/runtime; director receives shots/tasks.
    let spokeLabel = 'Change',
      spoke = '',
      opts = {};
    if (action.type === 'cut') {
      spokeLabel = 'Cut';
      spoke = action.scene.name;
      opts = {
        location: action.dropLoc ? action.scene.location : null,
        principal: action.dropPrin
      };
    } else if (action.type === 'restore') {
      spokeLabel = 'Restore';
      spoke = action.scene.name;
      opts = {
        location: dLoc < 0 ? action.scene.location : null,
        principal: action.addPrin
      };
    } else if (action.type === 'hero') {
      spokeLabel = 'Hero macro';
      spoke = `${action.len}s cut`;
      opts = {
        runtime: true
      };
    } else if (action.type === 'tokyo') {
      spokeLabel = action.on ? 'Add location' : 'Remove location';
      spoke = 'Tokyo';
      opts = {
        location: 'Tokyo',
        runtime: true
      };
    }
    return {
      dir: 'down',
      fromSeat: 'Client',
      toSeat: 'Director',
      spokeLabel,
      spoke,
      received: directorItems(opts),
      chips: [{
        k: 'cost',
        v: signMoney$(dCost)
      }, {
        k: 'date',
        v: signDays(dDays)
      }]
    };
  }
  return {
    SCENES,
    TOKYO,
    HERO,
    SURCHARGE,
    WRAP,
    DEFAULT_STATE,
    buildScenes,
    recompute,
    translate,
    fmtMoney,
    fmtDateParts,
    fmtRuntime,
    signMoney$,
    signDays
  };
}();
})(); } catch (e) { __ds_ns.__errors.push({ path: "tenet-demo/model.js", error: String((e && e.message) || e) }); }

// tenet-demo/ui.jsx
try { (() => {
/* global React */
// TENET Live Demo — shared UI: tokens, line icons, tween hook, dials.

const TK = {
  INK: '#1A1A1A',
  INK_MUTED: '#6B6B6B',
  INK_FAINT: '#9A9A9A',
  PAPER: '#FFFFFF',
  PAPER_SOFT: '#FAF8F4',
  AMBER: '#BA7517',
  AMBER_50: '#FAEEDA',
  AMBER_800: '#854F0B',
  BORDER: 'rgba(0,0,0,0.10)',
  BORDER_STRONG: 'rgba(0,0,0,0.18)',
  SANS: '"Brown","Avenir Next","Helvetica Neue",system-ui,sans-serif',
  SERIF: '"Source Serif 4","Source Serif Pro",Georgia,serif',
  MONO: '"JetBrains Mono","SF Mono",Menlo,monospace'
};

// ── Line icons — single weight, currentColor (matches the wordmark) ──
function Glyph({
  name,
  size = 20,
  sw = 1.5,
  style
}) {
  const base = {
    width: size,
    height: size,
    viewBox: '0 0 24 24',
    fill: 'none',
    stroke: 'currentColor',
    strokeWidth: sw,
    strokeLinecap: 'round',
    strokeLinejoin: 'round',
    style
  };
  const P = {
    scissors: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "6",
      cy: "6",
      r: "3"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "6",
      cy: "18",
      r: "3"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M20 4 8.12 15.88M14.47 14.48 20 20M8.12 8.12 12 12"
    })),
    pin: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0Z"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "12",
      cy: "10",
      r: "3"
    })),
    ruler: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M3 9h18M3 9v6h18V9M7 9v3M11 9v3M15 9v3M19 9v3"
    })),
    alert: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M10.3 3.9 1.8 18a2 2 0 0 0 1.7 3h17a2 2 0 0 0 1.7-3L13.7 3.9a2 2 0 0 0-3.4 0Z"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M12 9v4"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M12 17h.01"
    })),
    arrow: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M5 12h14M13 6l6 6-6 6"
    })),
    check: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M20 6 9 17l-5-5"
    })),
    plus: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M12 5v14M5 12h14"
    })),
    clapper: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M3 9h18v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V9Z"
    }), /*#__PURE__*/React.createElement("path", {
      d: "m3 9 2.5-4 4 .8M9.5 5.8 13 5l-2 4M13 5l4 .8-2 3.2M17 5.8 20.5 5 21 9"
    })),
    layers: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "m12 2 9 5-9 5-9-5 9-5Z"
    }), /*#__PURE__*/React.createElement("path", {
      d: "m3 12 9 5 9-5M3 17l9 5 9-5"
    })),
    user: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "12",
      cy: "8",
      r: "4"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M4 21a8 8 0 0 1 16 0"
    })),
    reset: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M3 12a9 9 0 1 0 3-6.7L3 8"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M3 3v5h5"
    }))
  };
  return /*#__PURE__*/React.createElement("svg", base, P[name] || null);
}

// ── Tween hook — interruptible count animation ──────────────────────
function useTween(value, dur = 650) {
  const [disp, setDisp] = React.useState(value);
  const dispRef = React.useRef(value);
  const raf = React.useRef();
  React.useEffect(() => {
    const from = dispRef.current,
      to = value;
    if (Math.abs(from - to) < 0.001) {
      dispRef.current = to;
      setDisp(to);
      return;
    }
    const start = performance.now();
    cancelAnimationFrame(raf.current);
    let done = false;
    const finish = () => {
      if (done) return;
      done = true;
      dispRef.current = to;
      setDisp(to);
    };
    const tick = now => {
      const p = Math.min(1, (now - start) / dur);
      const e = 1 - Math.pow(1 - p, 3);
      dispRef.current = from + (to - from) * e;
      setDisp(dispRef.current);
      if (p < 1) raf.current = requestAnimationFrame(tick);else finish();
    };
    raf.current = requestAnimationFrame(tick);
    // Guarantee the destination even where rAF is throttled/paused (the resting
    // value can never depend on frames actually painting).
    const fb = setTimeout(finish, dur + 80);
    return () => {
      cancelAnimationFrame(raf.current);
      clearTimeout(fb);
    };
  }, [value, dur]);
  return disp;
}

// ── Switch — a calm, unmistakably-live toggle ───────────────────────
function Switch({
  on,
  onClick,
  disabled
}) {
  return /*#__PURE__*/React.createElement("button", {
    onClick: disabled ? undefined : onClick,
    disabled: disabled,
    style: {
      width: 44,
      height: 24,
      borderRadius: 999,
      position: 'relative',
      flexShrink: 0,
      border: `1px solid ${on ? TK.AMBER : TK.BORDER_STRONG}`,
      background: on ? TK.AMBER : TK.PAPER,
      cursor: disabled ? 'default' : 'pointer',
      padding: 0,
      opacity: disabled ? 0.35 : 1
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      top: 2,
      left: on ? 22 : 2,
      width: 18,
      height: 18,
      borderRadius: 999,
      background: on ? TK.PAPER : TK.INK_FAINT
    }
  }));
}

// ── Segmented control — amber selection ─────────────────────────────
function Segmented({
  options,
  value,
  onChange
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      border: `1px solid ${TK.BORDER_STRONG}`,
      borderRadius: 2,
      overflow: 'hidden'
    }
  }, options.map((o, i) => {
    const active = o.value === value;
    return /*#__PURE__*/React.createElement("button", {
      key: o.value,
      onClick: () => onChange(o.value),
      style: {
        padding: '7px 16px',
        fontFamily: TK.MONO,
        fontSize: 13,
        fontWeight: 500,
        letterSpacing: '0.02em',
        cursor: 'pointer',
        border: 'none',
        borderLeft: i ? `1px solid ${TK.BORDER}` : 'none',
        background: active ? TK.AMBER : TK.PAPER,
        color: active ? TK.PAPER : TK.INK_MUTED
      }
    }, o.label);
  }));
}

// ── Department chip — the downstream task tag ───────────────────────
function DeptChip({
  dept,
  label,
  atRisk
}) {
  const muted = TK.INK_MUTED;
  return /*#__PURE__*/React.createElement("span", {
    title: label,
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6,
      padding: '4px 9px',
      borderRadius: 2,
      fontFamily: TK.MONO,
      fontSize: 11,
      letterSpacing: '0.04em',
      border: `1px solid ${atRisk ? TK.AMBER : TK.BORDER}`,
      background: atRisk ? TK.AMBER_50 : TK.PAPER,
      color: atRisk ? TK.AMBER_800 : muted,
      whiteSpace: 'nowrap'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 5,
      height: 5,
      borderRadius: 999,
      background: atRisk ? TK.AMBER : dept === 'VFX' ? TK.INK : TK.INK_FAINT
    }
  }), dept);
}

// ── Breathing amber dot — the "live" / "translating" pulse ──────────
function BreathingDot({
  size = 6,
  color = TK.AMBER
}) {
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-block',
      width: size,
      height: size,
      borderRadius: 999,
      background: color,
      animation: 'tenetBreathe 1.6s ease-in-out infinite'
    }
  });
}
Object.assign(window, {
  TK,
  Glyph,
  useTween,
  Switch,
  Segmented,
  DeptChip,
  BreathingDot
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "tenet-demo/ui.jsx", error: String((e && e.message) || e) }); }

// tweaks-panel.jsx
try { (() => {
// @ds-adherence-ignore -- omelette starter scaffold (raw elements/hex/px by design)

/* BEGIN USAGE */
// tweaks-panel.jsx
// Reusable Tweaks shell + form-control helpers.
// Exports (to window): useTweaks, TweaksPanel, TweakSection, TweakRow, TweakSlider,
//   TweakToggle, TweakRadio, TweakSelect, TweakText, TweakNumber, TweakColor, TweakButton.
//
// Owns the host protocol (listens for __activate_edit_mode / __deactivate_edit_mode,
// posts __edit_mode_available / __edit_mode_set_keys / __edit_mode_dismissed) so
// individual prototypes don't re-roll it. Ships a consistent set of controls so you
// don't hand-draw <input type="range">, segmented radios, steppers, etc.
//
// Usage (in an HTML file that loads React + Babel):
//
//   const TWEAK_DEFAULTS = /*EDITMODE-BEGIN*/{
//     "primaryColor": "#D97757",
//     "palette": ["#D97757", "#29261b", "#f6f4ef"],
//     "fontSize": 16,
//     "density": "regular",
//     "dark": false
//   }/*EDITMODE-END*/;
//
//   function App() {
//     const [t, setTweak] = useTweaks(TWEAK_DEFAULTS);
//     return (
//       <div style={{ fontSize: t.fontSize, color: t.primaryColor }}>
//         Hello
//         <TweaksPanel>
//           <TweakSection label="Typography" />
//           <TweakSlider label="Font size" value={t.fontSize} min={10} max={32} unit="px"
//                        onChange={(v) => setTweak('fontSize', v)} />
//           <TweakRadio  label="Density" value={t.density}
//                        options={['compact', 'regular', 'comfy']}
//                        onChange={(v) => setTweak('density', v)} />
//           <TweakSection label="Theme" />
//           <TweakColor  label="Primary" value={t.primaryColor}
//                        options={['#D97757', '#2A6FDB', '#1F8A5B', '#7A5AE0']}
//                        onChange={(v) => setTweak('primaryColor', v)} />
//           <TweakColor  label="Palette" value={t.palette}
//                        options={[['#D97757', '#29261b', '#f6f4ef'],
//                                  ['#475569', '#0f172a', '#f1f5f9']]}
//                        onChange={(v) => setTweak('palette', v)} />
//           <TweakToggle label="Dark mode" value={t.dark}
//                        onChange={(v) => setTweak('dark', v)} />
//         </TweaksPanel>
//       </div>
//     );
//   }
//
// TweakRadio is the segmented control for 2–3 short options (auto-falls-back to
// TweakSelect past ~16/~10 chars per label); reach for TweakSelect directly when
// options are many or long. For color tweaks always curate 3-4 options rather than
// a free picker; an option can also be a whole 2–5 color palette (the stored value
// is the array). The Tweak* controls are a floor, not a ceiling — build custom
// controls inside the panel if a tweak calls for UI they don't cover.
/* END USAGE */
// ─────────────────────────────────────────────────────────────────────────────

const __TWEAKS_STYLE = `
  .twk-panel{position:fixed;right:16px;bottom:16px;z-index:2147483646;width:280px;
    max-height:calc(100vh - 32px);display:flex;flex-direction:column;
    transform:scale(var(--dc-inv-zoom,1));transform-origin:bottom right;
    background:rgba(250,249,247,.78);color:#29261b;
    -webkit-backdrop-filter:blur(24px) saturate(160%);backdrop-filter:blur(24px) saturate(160%);
    border:.5px solid rgba(255,255,255,.6);border-radius:14px;
    box-shadow:0 1px 0 rgba(255,255,255,.5) inset,0 12px 40px rgba(0,0,0,.18);
    font:11.5px/1.4 ui-sans-serif,system-ui,-apple-system,sans-serif;overflow:hidden}
  .twk-hd{display:flex;align-items:center;justify-content:space-between;
    padding:10px 8px 10px 14px;cursor:move;user-select:none}
  .twk-hd b{font-size:12px;font-weight:600;letter-spacing:.01em}
  .twk-x{appearance:none;border:0;background:transparent;color:rgba(41,38,27,.55);
    width:22px;height:22px;border-radius:6px;cursor:default;font-size:13px;line-height:1}
  .twk-x:hover{background:rgba(0,0,0,.06);color:#29261b}
  .twk-body{padding:2px 14px 14px;display:flex;flex-direction:column;gap:10px;
    overflow-y:auto;overflow-x:hidden;min-height:0;
    scrollbar-width:thin;scrollbar-color:rgba(0,0,0,.15) transparent}
  .twk-body::-webkit-scrollbar{width:8px}
  .twk-body::-webkit-scrollbar-track{background:transparent;margin:2px}
  .twk-body::-webkit-scrollbar-thumb{background:rgba(0,0,0,.15);border-radius:4px;
    border:2px solid transparent;background-clip:content-box}
  .twk-body::-webkit-scrollbar-thumb:hover{background:rgba(0,0,0,.25);
    border:2px solid transparent;background-clip:content-box}
  .twk-row{display:flex;flex-direction:column;gap:5px}
  .twk-row-h{flex-direction:row;align-items:center;justify-content:space-between;gap:10px}
  .twk-lbl{display:flex;justify-content:space-between;align-items:baseline;
    color:rgba(41,38,27,.72)}
  .twk-lbl>span:first-child{font-weight:500}
  .twk-val{color:rgba(41,38,27,.5);font-variant-numeric:tabular-nums}

  .twk-sect{font-size:10px;font-weight:600;letter-spacing:.06em;text-transform:uppercase;
    color:rgba(41,38,27,.45);padding:10px 0 0}
  .twk-sect:first-child{padding-top:0}

  .twk-field{appearance:none;box-sizing:border-box;width:100%;min-width:0;height:26px;padding:0 8px;
    border:.5px solid rgba(0,0,0,.1);border-radius:7px;
    background:rgba(255,255,255,.6);color:inherit;font:inherit;outline:none}
  .twk-field:focus{border-color:rgba(0,0,0,.25);background:rgba(255,255,255,.85)}
  select.twk-field{padding-right:22px;
    background-image:url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='10' height='6' viewBox='0 0 10 6'><path fill='rgba(0,0,0,.5)' d='M0 0h10L5 6z'/></svg>");
    background-repeat:no-repeat;background-position:right 8px center}

  .twk-slider{appearance:none;-webkit-appearance:none;width:100%;height:4px;margin:6px 0;
    border-radius:999px;background:rgba(0,0,0,.12);outline:none}
  .twk-slider::-webkit-slider-thumb{-webkit-appearance:none;appearance:none;
    width:14px;height:14px;border-radius:50%;background:#fff;
    border:.5px solid rgba(0,0,0,.12);box-shadow:0 1px 3px rgba(0,0,0,.2);cursor:default}
  .twk-slider::-moz-range-thumb{width:14px;height:14px;border-radius:50%;
    background:#fff;border:.5px solid rgba(0,0,0,.12);box-shadow:0 1px 3px rgba(0,0,0,.2);cursor:default}

  .twk-seg{position:relative;display:flex;padding:2px;border-radius:8px;
    background:rgba(0,0,0,.06);user-select:none}
  .twk-seg-thumb{position:absolute;top:2px;bottom:2px;border-radius:6px;
    background:rgba(255,255,255,.9);box-shadow:0 1px 2px rgba(0,0,0,.12);
    transition:left .15s cubic-bezier(.3,.7,.4,1),width .15s}
  .twk-seg.dragging .twk-seg-thumb{transition:none}
  .twk-seg button{appearance:none;position:relative;z-index:1;flex:1;border:0;
    background:transparent;color:inherit;font:inherit;font-weight:500;min-height:22px;
    border-radius:6px;cursor:default;padding:4px 6px;line-height:1.2;
    overflow-wrap:anywhere}

  .twk-toggle{position:relative;width:32px;height:18px;border:0;border-radius:999px;
    background:rgba(0,0,0,.15);transition:background .15s;cursor:default;padding:0}
  .twk-toggle[data-on="1"]{background:#34c759}
  .twk-toggle i{position:absolute;top:2px;left:2px;width:14px;height:14px;border-radius:50%;
    background:#fff;box-shadow:0 1px 2px rgba(0,0,0,.25);transition:transform .15s}
  .twk-toggle[data-on="1"] i{transform:translateX(14px)}

  .twk-num{display:flex;align-items:center;box-sizing:border-box;min-width:0;height:26px;padding:0 0 0 8px;
    border:.5px solid rgba(0,0,0,.1);border-radius:7px;background:rgba(255,255,255,.6)}
  .twk-num-lbl{font-weight:500;color:rgba(41,38,27,.6);cursor:ew-resize;
    user-select:none;padding-right:8px}
  .twk-num input{flex:1;min-width:0;height:100%;border:0;background:transparent;
    font:inherit;font-variant-numeric:tabular-nums;text-align:right;padding:0 8px 0 0;
    outline:none;color:inherit;-moz-appearance:textfield}
  .twk-num input::-webkit-inner-spin-button,.twk-num input::-webkit-outer-spin-button{
    -webkit-appearance:none;margin:0}
  .twk-num-unit{padding-right:8px;color:rgba(41,38,27,.45)}

  .twk-btn{appearance:none;height:26px;padding:0 12px;border:0;border-radius:7px;
    background:rgba(0,0,0,.78);color:#fff;font:inherit;font-weight:500;cursor:default}
  .twk-btn:hover{background:rgba(0,0,0,.88)}
  .twk-btn.secondary{background:rgba(0,0,0,.06);color:inherit}
  .twk-btn.secondary:hover{background:rgba(0,0,0,.1)}

  .twk-swatch{appearance:none;-webkit-appearance:none;width:56px;height:22px;
    border:.5px solid rgba(0,0,0,.1);border-radius:6px;padding:0;cursor:default;
    background:transparent;flex-shrink:0}
  .twk-swatch::-webkit-color-swatch-wrapper{padding:0}
  .twk-swatch::-webkit-color-swatch{border:0;border-radius:5.5px}
  .twk-swatch::-moz-color-swatch{border:0;border-radius:5.5px}

  .twk-chips{display:flex;gap:6px}
  .twk-chip{position:relative;appearance:none;flex:1;min-width:0;height:46px;
    padding:0;border:0;border-radius:6px;overflow:hidden;cursor:default;
    box-shadow:0 0 0 .5px rgba(0,0,0,.12),0 1px 2px rgba(0,0,0,.06);
    transition:transform .12s cubic-bezier(.3,.7,.4,1),box-shadow .12s}
  .twk-chip:hover{transform:translateY(-1px);
    box-shadow:0 0 0 .5px rgba(0,0,0,.18),0 4px 10px rgba(0,0,0,.12)}
  .twk-chip[data-on="1"]{box-shadow:0 0 0 1.5px rgba(0,0,0,.85),
    0 2px 6px rgba(0,0,0,.15)}
  .twk-chip>span{position:absolute;top:0;bottom:0;right:0;width:34%;
    display:flex;flex-direction:column;box-shadow:-1px 0 0 rgba(0,0,0,.1)}
  .twk-chip>span>i{flex:1;box-shadow:0 -1px 0 rgba(0,0,0,.1)}
  .twk-chip>span>i:first-child{box-shadow:none}
  .twk-chip svg{position:absolute;top:6px;left:6px;width:13px;height:13px;
    filter:drop-shadow(0 1px 1px rgba(0,0,0,.3))}
`;

// ── useTweaks ───────────────────────────────────────────────────────────────
// Single source of truth for tweak values. setTweak persists via the host
// (__edit_mode_set_keys → host rewrites the EDITMODE block on disk).
function useTweaks(defaults) {
  const [values, setValues] = React.useState(defaults);
  // Accepts either setTweak('key', value) or setTweak({ key: value, ... }) so a
  // useState-style call doesn't write a "[object Object]" key into the persisted
  // JSON block.
  const setTweak = React.useCallback((keyOrEdits, val) => {
    const edits = typeof keyOrEdits === 'object' && keyOrEdits !== null ? keyOrEdits : {
      [keyOrEdits]: val
    };
    setValues(prev => ({
      ...prev,
      ...edits
    }));
    window.parent.postMessage({
      type: '__edit_mode_set_keys',
      edits
    }, '*');
    // Same-window signal so in-page listeners (deck-stage rail thumbnails)
    // can react — the parent message only reaches the host, not peers.
    window.dispatchEvent(new CustomEvent('tweakchange', {
      detail: edits
    }));
  }, []);
  return [values, setTweak];
}

// ── TweaksPanel ─────────────────────────────────────────────────────────────
// Floating shell. Registers the protocol listener BEFORE announcing
// availability — if the announce ran first, the host's activate could land
// before our handler exists and the toolbar toggle would silently no-op.
// The close button posts __edit_mode_dismissed so the host's toolbar toggle
// flips off in lockstep; the host echoes __deactivate_edit_mode back which
// is what actually hides the panel.
function TweaksPanel({
  title = 'Tweaks',
  children
}) {
  const [open, setOpen] = React.useState(false);
  const dragRef = React.useRef(null);
  const offsetRef = React.useRef({
    x: 16,
    y: 16
  });
  const PAD = 16;
  const clampToViewport = React.useCallback(() => {
    const panel = dragRef.current;
    if (!panel) return;
    const w = panel.offsetWidth,
      h = panel.offsetHeight;
    const maxRight = Math.max(PAD, window.innerWidth - w - PAD);
    const maxBottom = Math.max(PAD, window.innerHeight - h - PAD);
    offsetRef.current = {
      x: Math.min(maxRight, Math.max(PAD, offsetRef.current.x)),
      y: Math.min(maxBottom, Math.max(PAD, offsetRef.current.y))
    };
    panel.style.right = offsetRef.current.x + 'px';
    panel.style.bottom = offsetRef.current.y + 'px';
  }, []);
  React.useEffect(() => {
    if (!open) return;
    clampToViewport();
    if (typeof ResizeObserver === 'undefined') {
      window.addEventListener('resize', clampToViewport);
      return () => window.removeEventListener('resize', clampToViewport);
    }
    const ro = new ResizeObserver(clampToViewport);
    ro.observe(document.documentElement);
    return () => ro.disconnect();
  }, [open, clampToViewport]);
  React.useEffect(() => {
    const onMsg = e => {
      const t = e?.data?.type;
      if (t === '__activate_edit_mode') setOpen(true);else if (t === '__deactivate_edit_mode') setOpen(false);
    };
    window.addEventListener('message', onMsg);
    window.parent.postMessage({
      type: '__edit_mode_available'
    }, '*');
    return () => window.removeEventListener('message', onMsg);
  }, []);
  const dismiss = () => {
    setOpen(false);
    window.parent.postMessage({
      type: '__edit_mode_dismissed'
    }, '*');
  };
  const onDragStart = e => {
    const panel = dragRef.current;
    if (!panel) return;
    const r = panel.getBoundingClientRect();
    const sx = e.clientX,
      sy = e.clientY;
    const startRight = window.innerWidth - r.right;
    const startBottom = window.innerHeight - r.bottom;
    const move = ev => {
      offsetRef.current = {
        x: startRight - (ev.clientX - sx),
        y: startBottom - (ev.clientY - sy)
      };
      clampToViewport();
    };
    const up = () => {
      window.removeEventListener('mousemove', move);
      window.removeEventListener('mouseup', up);
    };
    window.addEventListener('mousemove', move);
    window.addEventListener('mouseup', up);
  };
  if (!open) return null;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("style", null, __TWEAKS_STYLE), /*#__PURE__*/React.createElement("div", {
    ref: dragRef,
    className: "twk-panel",
    "data-omelette-chrome": "",
    style: {
      right: offsetRef.current.x,
      bottom: offsetRef.current.y
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-hd",
    onMouseDown: onDragStart
  }, /*#__PURE__*/React.createElement("b", null, title), /*#__PURE__*/React.createElement("button", {
    className: "twk-x",
    "aria-label": "Close tweaks",
    onMouseDown: e => e.stopPropagation(),
    onClick: dismiss
  }, "\u2715")), /*#__PURE__*/React.createElement("div", {
    className: "twk-body"
  }, children)));
}

// ── Layout helpers ──────────────────────────────────────────────────────────

function TweakSection({
  label,
  children
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "twk-sect"
  }, label), children);
}
function TweakRow({
  label,
  value,
  children,
  inline = false
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: inline ? 'twk-row twk-row-h' : 'twk-row'
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-lbl"
  }, /*#__PURE__*/React.createElement("span", null, label), value != null && /*#__PURE__*/React.createElement("span", {
    className: "twk-val"
  }, value)), children);
}

// ── Controls ────────────────────────────────────────────────────────────────

function TweakSlider({
  label,
  value,
  min = 0,
  max = 100,
  step = 1,
  unit = '',
  onChange
}) {
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label,
    value: `${value}${unit}`
  }, /*#__PURE__*/React.createElement("input", {
    type: "range",
    className: "twk-slider",
    min: min,
    max: max,
    step: step,
    value: value,
    onChange: e => onChange(Number(e.target.value))
  }));
}
function TweakToggle({
  label,
  value,
  onChange
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: "twk-row twk-row-h"
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-lbl"
  }, /*#__PURE__*/React.createElement("span", null, label)), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "twk-toggle",
    "data-on": value ? '1' : '0',
    role: "switch",
    "aria-checked": !!value,
    onClick: () => onChange(!value)
  }, /*#__PURE__*/React.createElement("i", null)));
}
function TweakRadio({
  label,
  value,
  options,
  onChange
}) {
  const trackRef = React.useRef(null);
  const [dragging, setDragging] = React.useState(false);
  // The active value is read by pointer-move handlers attached for the lifetime
  // of a drag — ref it so a stale closure doesn't fire onChange for every move.
  const valueRef = React.useRef(value);
  valueRef.current = value;

  // Segments wrap mid-word once per-segment width runs out. The track is
  // ~248px (280 panel − 28 body pad − 4 seg pad), each button loses 12px
  // to its own padding, and 11.5px system-ui averages ~6.3px/char — so 2
  // options fit ~16 chars each, 3 fit ~10. Past that (or >3 options), fall
  // back to a dropdown rather than wrap.
  const labelLen = o => String(typeof o === 'object' ? o.label : o).length;
  const maxLen = options.reduce((m, o) => Math.max(m, labelLen(o)), 0);
  const fitsAsSegments = maxLen <= ({
    2: 16,
    3: 10
  }[options.length] ?? 0);
  if (!fitsAsSegments) {
    // <select> emits strings — map back to the original option value so the
    // fallback stays type-preserving (numbers, booleans) like the segment path.
    const resolve = s => {
      const m = options.find(o => String(typeof o === 'object' ? o.value : o) === s);
      return m === undefined ? s : typeof m === 'object' ? m.value : m;
    };
    return /*#__PURE__*/React.createElement(TweakSelect, {
      label: label,
      value: value,
      options: options,
      onChange: s => onChange(resolve(s))
    });
  }
  const opts = options.map(o => typeof o === 'object' ? o : {
    value: o,
    label: o
  });
  const idx = Math.max(0, opts.findIndex(o => o.value === value));
  const n = opts.length;
  const segAt = clientX => {
    const r = trackRef.current.getBoundingClientRect();
    const inner = r.width - 4;
    const i = Math.floor((clientX - r.left - 2) / inner * n);
    return opts[Math.max(0, Math.min(n - 1, i))].value;
  };
  const onPointerDown = e => {
    setDragging(true);
    const v0 = segAt(e.clientX);
    if (v0 !== valueRef.current) onChange(v0);
    const move = ev => {
      if (!trackRef.current) return;
      const v = segAt(ev.clientX);
      if (v !== valueRef.current) onChange(v);
    };
    const up = () => {
      setDragging(false);
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("div", {
    ref: trackRef,
    role: "radiogroup",
    onPointerDown: onPointerDown,
    className: dragging ? 'twk-seg dragging' : 'twk-seg'
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-seg-thumb",
    style: {
      left: `calc(2px + ${idx} * (100% - 4px) / ${n})`,
      width: `calc((100% - 4px) / ${n})`
    }
  }), opts.map(o => /*#__PURE__*/React.createElement("button", {
    key: o.value,
    type: "button",
    role: "radio",
    "aria-checked": o.value === value
  }, o.label))));
}
function TweakSelect({
  label,
  value,
  options,
  onChange
}) {
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("select", {
    className: "twk-field",
    value: value,
    onChange: e => onChange(e.target.value)
  }, options.map(o => {
    const v = typeof o === 'object' ? o.value : o;
    const l = typeof o === 'object' ? o.label : o;
    return /*#__PURE__*/React.createElement("option", {
      key: v,
      value: v
    }, l);
  })));
}
function TweakText({
  label,
  value,
  placeholder,
  onChange
}) {
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("input", {
    className: "twk-field",
    type: "text",
    value: value,
    placeholder: placeholder,
    onChange: e => onChange(e.target.value)
  }));
}
function TweakNumber({
  label,
  value,
  min,
  max,
  step = 1,
  unit = '',
  onChange
}) {
  const clamp = n => {
    if (min != null && n < min) return min;
    if (max != null && n > max) return max;
    return n;
  };
  const startRef = React.useRef({
    x: 0,
    val: 0
  });
  const onScrubStart = e => {
    e.preventDefault();
    startRef.current = {
      x: e.clientX,
      val: value
    };
    const decimals = (String(step).split('.')[1] || '').length;
    const move = ev => {
      const dx = ev.clientX - startRef.current.x;
      const raw = startRef.current.val + dx * step;
      const snapped = Math.round(raw / step) * step;
      onChange(clamp(Number(snapped.toFixed(decimals))));
    };
    const up = () => {
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return /*#__PURE__*/React.createElement("div", {
    className: "twk-num"
  }, /*#__PURE__*/React.createElement("span", {
    className: "twk-num-lbl",
    onPointerDown: onScrubStart
  }, label), /*#__PURE__*/React.createElement("input", {
    type: "number",
    value: value,
    min: min,
    max: max,
    step: step,
    onChange: e => onChange(clamp(Number(e.target.value)))
  }), unit && /*#__PURE__*/React.createElement("span", {
    className: "twk-num-unit"
  }, unit));
}

// Relative-luminance contrast pick — checkmarks drawn over a swatch need to
// read on both #111 and #fafafa without per-option configuration. Hex input
// only (#rgb / #rrggbb); named or rgb()/hsl() colors fall through to "light".
function __twkIsLight(hex) {
  const h = String(hex).replace('#', '');
  const x = h.length === 3 ? h.replace(/./g, c => c + c) : h.padEnd(6, '0');
  const n = parseInt(x.slice(0, 6), 16);
  if (Number.isNaN(n)) return true;
  const r = n >> 16 & 255,
    g = n >> 8 & 255,
    b = n & 255;
  return r * 299 + g * 587 + b * 114 > 148000;
}
const __TwkCheck = ({
  light
}) => /*#__PURE__*/React.createElement("svg", {
  viewBox: "0 0 14 14",
  "aria-hidden": "true"
}, /*#__PURE__*/React.createElement("path", {
  d: "M3 7.2 5.8 10 11 4.2",
  fill: "none",
  strokeWidth: "2.2",
  strokeLinecap: "round",
  strokeLinejoin: "round",
  stroke: light ? 'rgba(0,0,0,.78)' : '#fff'
}));

// TweakColor — curated color/palette picker. Each option is either a single
// hex string or an array of 1-5 hex strings; the card adapts — a lone color
// renders solid, a palette renders colors[0] as the hero (left ~2/3) with the
// rest stacked in a sharp column on the right. onChange emits the
// option in the shape it was passed (string stays string, array stays array).
// Without options it falls back to the native color input for back-compat.
function TweakColor({
  label,
  value,
  options,
  onChange
}) {
  if (!options || !options.length) {
    return /*#__PURE__*/React.createElement("div", {
      className: "twk-row twk-row-h"
    }, /*#__PURE__*/React.createElement("div", {
      className: "twk-lbl"
    }, /*#__PURE__*/React.createElement("span", null, label)), /*#__PURE__*/React.createElement("input", {
      type: "color",
      className: "twk-swatch",
      value: value,
      onChange: e => onChange(e.target.value)
    }));
  }
  // Native <input type=color> emits lowercase hex per the HTML spec, so
  // compare case-insensitively. String() guards JSON.stringify(undefined),
  // which returns the primitive undefined (no .toLowerCase).
  const key = o => String(JSON.stringify(o)).toLowerCase();
  const cur = key(value);
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-chips",
    role: "radiogroup"
  }, options.map((o, i) => {
    const colors = Array.isArray(o) ? o : [o];
    const [hero, ...rest] = colors;
    const sup = rest.slice(0, 4);
    const on = key(o) === cur;
    return /*#__PURE__*/React.createElement("button", {
      key: i,
      type: "button",
      className: "twk-chip",
      role: "radio",
      "aria-checked": on,
      "data-on": on ? '1' : '0',
      "aria-label": colors.join(', '),
      title: colors.join(' · '),
      style: {
        background: hero
      },
      onClick: () => onChange(o)
    }, sup.length > 0 && /*#__PURE__*/React.createElement("span", null, sup.map((c, j) => /*#__PURE__*/React.createElement("i", {
      key: j,
      style: {
        background: c
      }
    }))), on && /*#__PURE__*/React.createElement(__TwkCheck, {
      light: __twkIsLight(hero)
    }));
  })));
}
function TweakButton({
  label,
  onClick,
  secondary = false
}) {
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: secondary ? 'twk-btn secondary' : 'twk-btn',
    onClick: onClick
  }, label);
}
Object.assign(window, {
  useTweaks,
  TweaksPanel,
  TweakSection,
  TweakRow,
  TweakSlider,
  TweakToggle,
  TweakRadio,
  TweakSelect,
  TweakText,
  TweakNumber,
  TweakColor,
  TweakButton
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "tweaks-panel.jsx", error: String((e && e.message) || e) }); }

// ui_kits/tenet/Icon.jsx
try { (() => {
// Lucide-style stroke icons. 1.75 stroke, currentColor.
// Keep additions to this file — never use emoji, never use Unicode glyphs.
const Icon = ({
  name,
  size = 18,
  strokeWidth = 1.75,
  style
}) => {
  const base = {
    width: size,
    height: size,
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth,
    strokeLinecap: "round",
    strokeLinejoin: "round",
    style
  };
  const P = {
    script: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M14 2v6h6"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M16 13H8"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M16 17H8"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M10 9H8"
    })),
    shots: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("rect", {
      x: "3",
      y: "3",
      width: "18",
      height: "18",
      rx: "2"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M7 3v18M17 3v18M3 7h4M17 7h4M3 17h4M17 17h4M3 12h18"
    })),
    dailies: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("rect", {
      x: "2",
      y: "6",
      width: "14",
      height: "12",
      rx: "2"
    }), /*#__PURE__*/React.createElement("path", {
      d: "m22 8-6 4 6 4V8Z"
    })),
    cuts: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "6",
      cy: "6",
      r: "3"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "6",
      cy: "18",
      r: "3"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M20 4 8.12 15.88M14.47 14.48 20 20M8.12 8.12 12 12"
    })),
    color: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "13.5",
      cy: "6.5",
      r: "2.5"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "19",
      cy: "13",
      r: "2.5"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "6",
      cy: "12",
      r: "2.5"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "10",
      cy: "20",
      r: "2.5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M12 2a10 10 0 1 0 0 20"
    })),
    sound: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M2 10v3"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M6 6v11"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M10 3v18"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M14 8v7"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M18 5v13"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M22 10v3"
    })),
    crew: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "9",
      cy: "7",
      r: "4"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M22 21v-2a4 4 0 0 0-3-3.87"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M16 3.13a4 4 0 0 1 0 7.75"
    })),
    search: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "11",
      cy: "11",
      r: "8"
    }), /*#__PURE__*/React.createElement("path", {
      d: "m21 21-4.3-4.3"
    })),
    share: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M4 12v8a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-8"
    }), /*#__PURE__*/React.createElement("polyline", {
      points: "16 6 12 2 8 6"
    }), /*#__PURE__*/React.createElement("line", {
      x1: "12",
      y1: "2",
      x2: "12",
      y2: "15"
    })),
    plus: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M12 5v14M5 12h14"
    })),
    chevron: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("polyline", {
      points: "6 9 12 15 18 9"
    })),
    chevronRight: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("polyline", {
      points: "9 6 15 12 9 18"
    })),
    lock: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("rect", {
      x: "3",
      y: "11",
      width: "18",
      height: "11",
      rx: "2"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M7 11V7a5 5 0 0 1 10 0v4"
    })),
    comment: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"
    })),
    download: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"
    }), /*#__PURE__*/React.createElement("polyline", {
      points: "7 10 12 15 17 10"
    }), /*#__PURE__*/React.createElement("line", {
      x1: "12",
      y1: "15",
      x2: "12",
      y2: "3"
    })),
    play: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("polygon", {
      points: "6 3 20 12 6 21 6 3"
    })),
    more: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "5",
      cy: "12",
      r: "1"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "12",
      cy: "12",
      r: "1"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "19",
      cy: "12",
      r: "1"
    })),
    bell: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M6 8a6 6 0 0 1 12 0c0 7 3 9 3 9H3s3-2 3-9"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M10.3 21a1.94 1.94 0 0 0 3.4 0"
    })),
    folder: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"
    })),
    timecode: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "12",
      cy: "12",
      r: "10"
    }), /*#__PURE__*/React.createElement("polyline", {
      points: "12 6 12 12 16 14"
    }))
  };
  return /*#__PURE__*/React.createElement("svg", base, P[name] || null);
};
window.Icon = Icon;
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/tenet/Icon.jsx", error: String((e && e.message) || e) }); }

// ui_kits/tenet/ShotDetail.jsx
try { (() => {
const tenetShotDetailStyles = {
  root: {
    width: 380,
    flexShrink: 0,
    borderLeft: "1px solid var(--border)",
    background: "var(--paper)",
    display: "flex",
    flexDirection: "column",
    height: "100%",
    overflow: "auto"
  },
  head: {
    padding: "20px 24px 16px",
    borderBottom: "1px solid var(--border)"
  },
  amberRule: {
    width: 32,
    height: 1,
    background: "var(--anona-orange)",
    marginBottom: 10
  },
  kicker: {
    fontSize: 10,
    letterSpacing: "0.18em",
    textTransform: "uppercase",
    color: "var(--anona-orange)",
    fontWeight: 400,
    fontFamily: "var(--font-mono)"
  },
  title: {
    fontFamily: "var(--font-display)",
    fontWeight: 400,
    fontSize: 22,
    lineHeight: 1.2,
    color: "var(--ink)",
    marginTop: 6
  },
  sub: {
    fontSize: 12,
    color: "var(--ink-muted)",
    marginTop: 4
  },
  player: {
    aspectRatio: "16 / 9",
    background: "var(--ink)",
    margin: "16px 24px 0",
    position: "relative",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    color: "#fff"
  },
  playerTimecode: {
    position: "absolute",
    bottom: 10,
    left: 12,
    fontFamily: "var(--font-mono)",
    fontSize: 11,
    color: "#fff",
    opacity: 0.7
  },
  playerScene: {
    position: "absolute",
    top: 10,
    left: 12,
    fontFamily: "var(--font-mono)",
    fontSize: 11,
    color: "#fff",
    opacity: 0.7,
    letterSpacing: "0.1em"
  },
  playBtn: {
    width: 56,
    height: 56,
    borderRadius: 999,
    border: "1px solid rgba(255,255,255,0.4)",
    display: "flex",
    alignItems: "center",
    justifyContent: "center"
  },
  section: {
    padding: "20px 24px",
    borderBottom: "1px solid var(--border)"
  },
  sectionLabel: {
    fontSize: 10,
    letterSpacing: "0.18em",
    textTransform: "uppercase",
    color: "var(--ink-muted)",
    fontWeight: 400,
    marginBottom: 12
  },
  metaRow: {
    display: "grid",
    gridTemplateColumns: "1fr 1fr",
    gap: "12px 24px"
  },
  metaItem: {},
  metaLabel: {
    fontSize: 10,
    color: "var(--ink-faint)",
    letterSpacing: "0.04em",
    textTransform: "uppercase",
    marginBottom: 2
  },
  metaValue: {
    fontSize: 13,
    color: "var(--ink)"
  },
  comment: {
    padding: "14px 0",
    borderBottom: "1px solid var(--border)"
  },
  commentHead: {
    display: "flex",
    alignItems: "center",
    gap: 8,
    marginBottom: 6
  },
  avatar: color => ({
    width: 22,
    height: 22,
    borderRadius: 999,
    background: color,
    color: "#fff",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    fontSize: 10,
    fontWeight: 400
  }),
  commentName: {
    fontSize: 12,
    color: "var(--ink)",
    fontWeight: 400
  },
  commentTime: {
    fontSize: 10,
    color: "var(--ink-faint)",
    fontFamily: "var(--font-mono)",
    marginLeft: "auto"
  },
  commentBody: {
    fontSize: 12,
    color: "var(--ink-muted)",
    lineHeight: 1.55
  },
  commentTC: {
    fontFamily: "var(--font-mono)",
    fontSize: 10,
    color: "var(--anona-orange)",
    marginRight: 6
  },
  version: {
    display: "flex",
    alignItems: "center",
    gap: 10,
    padding: "8px 0"
  },
  versionDot: active => ({
    width: 8,
    height: 8,
    borderRadius: 999,
    background: active ? "var(--anona-orange)" : "var(--ink-faint)",
    flexShrink: 0
  }),
  versionMeta: {
    fontSize: 11,
    color: "var(--ink-muted)",
    fontFamily: "var(--font-mono)"
  },
  versionLabel: {
    fontSize: 12,
    color: "var(--ink)"
  },
  actions: {
    padding: 24,
    display: "flex",
    gap: 10,
    borderTop: "1px solid var(--border)",
    marginTop: "auto"
  },
  btn: {
    flex: 1,
    padding: "10px 12px",
    borderRadius: 2,
    fontFamily: "var(--font-sans)",
    fontWeight: 400,
    fontSize: 12,
    border: "1px solid var(--ink)",
    background: "transparent",
    color: "var(--ink)",
    cursor: "pointer"
  },
  btnAmber: {
    background: "var(--anona-orange)",
    color: "#fff",
    border: "none"
  }
};
const ShotDetail = ({
  shot
}) => {
  const S = tenetShotDetailStyles;
  if (!shot) {
    return /*#__PURE__*/React.createElement("aside", {
      style: S.root
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        padding: 40,
        color: "var(--ink-faint)",
        fontSize: 13
      }
    }, "Select a shot."));
  }
  const comments = [{
    who: "A. Vega",
    role: "DP",
    color: "#1A1A1A",
    tc: "00:00:18:04",
    body: "Focus drift on B-cam from 18 onward. Worth a re-take if we have the time.",
    time: "2h"
  }, {
    who: "L. Mendez",
    role: "Editor",
    color: "#854F0B",
    tc: "00:00:31:11",
    body: "Beautiful match cut to the diner. Holding this in for now.",
    time: "1d"
  }, {
    who: "D. Parker",
    role: "Director",
    color: "var(--anona-orange)",
    tc: null,
    body: "Send to color when picture's locked.",
    time: "1d"
  }];
  const versions = [{
    label: "v17 · WIP",
    meta: "today · 14:22",
    active: true
  }, {
    label: "v16 · review",
    meta: "yesterday",
    active: false
  }, {
    label: "v15 · color v1",
    meta: "Aug 14",
    active: false
  }, {
    label: "v14 · picture",
    meta: "Aug 11",
    active: false
  }];
  return /*#__PURE__*/React.createElement("aside", {
    style: S.root
  }, /*#__PURE__*/React.createElement("div", {
    style: S.head
  }, /*#__PURE__*/React.createElement("div", {
    style: S.amberRule
  }), /*#__PURE__*/React.createElement("div", {
    style: S.kicker
  }, shot.id, " \xB7 SC ", shot.scene, " \xB7 T", shot.take), /*#__PURE__*/React.createElement("div", {
    style: S.title
  }, shot.title), /*#__PURE__*/React.createElement("div", {
    style: S.sub
  }, shot.sub), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 12
    }
  }, /*#__PURE__*/React.createElement(StatusChip, {
    state: shot.status
  }))), /*#__PURE__*/React.createElement("div", {
    style: S.player
  }, /*#__PURE__*/React.createElement("div", {
    style: S.playerScene
  }, "SC ", shot.scene, " \xB7 T", shot.take), /*#__PURE__*/React.createElement("div", {
    style: S.playBtn
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "play",
    size: 20
  })), /*#__PURE__*/React.createElement("div", {
    style: S.playerTimecode
  }, "00:00:00:00 / ", shot.duration)), /*#__PURE__*/React.createElement("div", {
    style: S.section
  }, /*#__PURE__*/React.createElement("div", {
    style: S.sectionLabel
  }, "Specs"), /*#__PURE__*/React.createElement("div", {
    style: S.metaRow
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: S.metaLabel
  }, "Duration"), /*#__PURE__*/React.createElement("div", {
    style: S.metaValue
  }, shot.duration)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: S.metaLabel
  }, "Owner"), /*#__PURE__*/React.createElement("div", {
    style: S.metaValue
  }, shot.owner)), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: S.metaLabel
  }, "Day"), /*#__PURE__*/React.createElement("div", {
    style: S.metaValue
  }, "Day 12")), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: S.metaLabel
  }, "Format"), /*#__PURE__*/React.createElement("div", {
    style: S.metaValue
  }, "ARRI \xB7 4.6K")))), /*#__PURE__*/React.createElement("div", {
    style: S.section
  }, /*#__PURE__*/React.createElement("div", {
    style: S.sectionLabel
  }, "Notes"), comments.map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      ...S.comment,
      borderBottom: i === comments.length - 1 ? "none" : "1px solid var(--border)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: S.commentHead
  }, /*#__PURE__*/React.createElement("div", {
    style: S.avatar(c.color)
  }, c.who.split(" ").map(s => s[0]).join("")), /*#__PURE__*/React.createElement("span", {
    style: S.commentName
  }, c.who), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      color: "var(--ink-faint)"
    }
  }, "\xB7 ", c.role), /*#__PURE__*/React.createElement("span", {
    style: S.commentTime
  }, c.time)), /*#__PURE__*/React.createElement("div", {
    style: S.commentBody
  }, c.tc && /*#__PURE__*/React.createElement("span", {
    style: S.commentTC
  }, c.tc), c.body)))), /*#__PURE__*/React.createElement("div", {
    style: S.section
  }, /*#__PURE__*/React.createElement("div", {
    style: S.sectionLabel
  }, "Versions"), versions.map((v, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: S.version
  }, /*#__PURE__*/React.createElement("div", {
    style: S.versionDot(v.active)
  }), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      ...S.versionLabel,
      fontWeight: v.active ? 500 : 400
    }
  }, v.label), /*#__PURE__*/React.createElement("div", {
    style: S.versionMeta
  }, v.meta))))), /*#__PURE__*/React.createElement("div", {
    style: S.actions
  }, /*#__PURE__*/React.createElement("button", {
    style: S.btn
  }, "Add note"), /*#__PURE__*/React.createElement("button", {
    style: {
      ...S.btn,
      ...S.btnAmber
    }
  }, "Send to color")));
};
window.ShotDetail = ShotDetail;
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/tenet/ShotDetail.jsx", error: String((e && e.message) || e) }); }

// ui_kits/tenet/ShotList.jsx
try { (() => {
const tenetShotListStyles = {
  root: {
    flex: 1,
    minWidth: 0,
    padding: "32px 32px 48px",
    overflow: "auto"
  },
  head: {
    display: "flex",
    alignItems: "flex-end",
    justifyContent: "space-between",
    marginBottom: 28
  },
  amberRule: {
    width: 48,
    height: 1,
    background: "var(--anona-orange)",
    marginBottom: 12
  },
  kicker: {
    fontSize: 11,
    letterSpacing: "0.18em",
    textTransform: "uppercase",
    color: "var(--anona-orange)",
    fontWeight: 400
  },
  title: {
    fontFamily: "var(--font-display)",
    fontWeight: 300,
    fontSize: 44,
    lineHeight: 1.05,
    letterSpacing: "-0.01em",
    color: "var(--ink)",
    marginTop: 6
  },
  sub: {
    fontSize: 13,
    color: "var(--ink-muted)",
    marginTop: 8,
    maxWidth: 520
  },
  controls: {
    display: "flex",
    gap: 8,
    alignItems: "center"
  },
  filterBtn: {
    padding: "6px 12px",
    border: "1px solid var(--border)",
    borderRadius: 2,
    fontSize: 12,
    color: "var(--ink-muted)",
    cursor: "pointer",
    background: "var(--paper)"
  },
  filterBtnActive: {
    color: "var(--ink)",
    borderColor: "var(--ink)"
  },
  table: {
    width: "100%",
    borderCollapse: "collapse"
  },
  th: {
    textAlign: "left",
    padding: "10px 12px",
    fontSize: 10,
    letterSpacing: "0.18em",
    textTransform: "uppercase",
    color: "var(--ink-muted)",
    fontWeight: 400,
    borderBottom: "1px solid var(--ink)"
  },
  thNum: {
    width: 70
  },
  thStatus: {
    width: 130
  },
  thDept: {
    width: 200
  },
  thOwner: {
    width: 140
  },
  row: active => ({
    cursor: "pointer",
    background: active ? "var(--paper-soft)" : "transparent",
    boxShadow: active ? "inset 2px 0 0 var(--anona-orange)" : "none",
    transition: "background .15s ease-out"
  }),
  td: {
    padding: "14px 12px",
    fontSize: 13,
    color: "var(--ink)",
    borderBottom: "1px solid var(--border)",
    verticalAlign: "middle"
  },
  tdMono: {
    fontFamily: "var(--font-mono)",
    fontSize: 11,
    color: "var(--ink-muted)"
  },
  shotTitle: {
    fontWeight: 400,
    color: "var(--ink)"
  },
  shotSub: {
    fontSize: 11,
    color: "var(--ink-faint)",
    marginTop: 2
  },
  deptDots: {
    display: "flex",
    gap: 4
  },
  dept: state => ({
    fontSize: 10,
    fontFamily: "var(--font-mono)",
    padding: "2px 6px",
    borderRadius: 2,
    background: state === "lock" ? "var(--ink)" : state === "review" ? "var(--amber-50)" : "transparent",
    color: state === "lock" ? "#fff" : state === "review" ? "var(--amber-800)" : "var(--ink-faint)",
    border: state === "open" ? "1px solid var(--border)" : "none"
  })
};
const shots = [{
  id: "001A",
  scene: 4,
  take: 3,
  title: "Rooftop chase, golden hour",
  sub: "EXT · Day · A-cam + B-cam",
  status: "review",
  duration: "00:48",
  owner: "A. Vega",
  depts: {
    pic: "review",
    snd: "lock",
    vfx: "open",
    post: "open"
  }
}, {
  id: "002B",
  scene: 7,
  take: 1,
  title: "Diner interior, dialogue",
  sub: "INT · Night · single-camera",
  status: "locked",
  duration: "02:14",
  owner: "D. Parker",
  depts: {
    pic: "lock",
    snd: "lock",
    vfx: "lock",
    post: "review"
  }
}, {
  id: "003A",
  scene: 9,
  take: 5,
  title: "Highway driving plate",
  sub: "EXT · Day · plate",
  status: "delivered",
  duration: "01:22",
  owner: "M. Cho",
  depts: {
    pic: "lock",
    snd: "lock",
    vfx: "lock",
    post: "lock"
  }
}, {
  id: "004C",
  scene: 12,
  take: 2,
  title: "Hospital corridor, walk-and-talk",
  sub: "INT · Night · Steadicam",
  status: "review",
  duration: "01:36",
  owner: "R. Halberg",
  depts: {
    pic: "review",
    snd: "review",
    vfx: "open",
    post: "open"
  }
}, {
  id: "005A",
  scene: 14,
  take: 7,
  title: "Phone call, motel exterior",
  sub: "EXT · Night · long lens",
  status: "pending",
  duration: "00:54",
  owner: "S. Idrissi",
  depts: {
    pic: "open",
    snd: "open",
    vfx: "open",
    post: "open"
  }
}, {
  id: "006B",
  scene: 18,
  take: 1,
  title: "Beach climax, master",
  sub: "EXT · Sunset · drone + ground",
  status: "review",
  duration: "03:08",
  owner: "A. Vega",
  depts: {
    pic: "review",
    snd: "open",
    vfx: "review",
    post: "open"
  }
}, {
  id: "007A",
  scene: 21,
  take: 4,
  title: "Reveal — wide single",
  sub: "EXT · Day · 100mm",
  status: "locked",
  duration: "01:02",
  owner: "D. Parker",
  depts: {
    pic: "lock",
    snd: "lock",
    vfx: "open",
    post: "open"
  }
}];
const ShotList = ({
  activeId,
  onSelect
}) => {
  const S = tenetShotListStyles;
  const [filter, setFilter] = React.useState("all");
  const filtered = shots.filter(s => filter === "all" ? true : s.status === filter);
  return /*#__PURE__*/React.createElement("main", {
    style: S.root
  }, /*#__PURE__*/React.createElement("div", {
    style: S.head
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: S.amberRule
  }), /*#__PURE__*/React.createElement("span", {
    style: S.kicker
  }, "Production \xB7 Shots"), /*#__PURE__*/React.createElement("div", {
    style: S.title
  }, "The work, listed."), /*#__PURE__*/React.createElement("div", {
    style: S.sub
  }, shots.length, " shots across 9 scenes. ", shots.filter(s => s.status === "locked" || s.status === "delivered").length, " locked, the rest in flight.")), /*#__PURE__*/React.createElement("div", {
    style: S.controls
  }, ["all", "review", "locked", "delivered", "pending"].map(f => /*#__PURE__*/React.createElement("button", {
    key: f,
    style: {
      ...S.filterBtn,
      ...(filter === f ? S.filterBtnActive : {})
    },
    onClick: () => setFilter(f)
  }, f === "all" ? "All" : f[0].toUpperCase() + f.slice(1))))), /*#__PURE__*/React.createElement("table", {
    style: S.table
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", {
    style: {
      ...S.th,
      ...S.thNum
    }
  }, "Shot"), /*#__PURE__*/React.createElement("th", {
    style: S.th
  }, "Description"), /*#__PURE__*/React.createElement("th", {
    style: {
      ...S.th,
      ...S.thStatus
    }
  }, "Status"), /*#__PURE__*/React.createElement("th", {
    style: {
      ...S.th,
      ...S.thDept
    }
  }, "Departments"), /*#__PURE__*/React.createElement("th", {
    style: {
      ...S.th,
      ...S.thOwner
    }
  }, "Owner"))), /*#__PURE__*/React.createElement("tbody", null, filtered.map(s => /*#__PURE__*/React.createElement("tr", {
    key: s.id,
    style: S.row(s.id === activeId),
    onClick: () => onSelect(s.id)
  }, /*#__PURE__*/React.createElement("td", {
    style: {
      ...S.td,
      ...S.tdMono
    }
  }, s.id, /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 2
    }
  }, s.duration)), /*#__PURE__*/React.createElement("td", {
    style: S.td
  }, /*#__PURE__*/React.createElement("div", {
    style: S.shotTitle
  }, s.title), /*#__PURE__*/React.createElement("div", {
    style: S.shotSub
  }, "Sc ", s.scene, " \xB7 T", s.take, " \xB7 ", s.sub)), /*#__PURE__*/React.createElement("td", {
    style: S.td
  }, /*#__PURE__*/React.createElement(StatusChip, {
    state: s.status
  })), /*#__PURE__*/React.createElement("td", {
    style: S.td
  }, /*#__PURE__*/React.createElement("div", {
    style: S.deptDots
  }, /*#__PURE__*/React.createElement("span", {
    style: S.dept(s.depts.pic)
  }, "PIC"), /*#__PURE__*/React.createElement("span", {
    style: S.dept(s.depts.snd)
  }, "SND"), /*#__PURE__*/React.createElement("span", {
    style: S.dept(s.depts.vfx)
  }, "VFX"), /*#__PURE__*/React.createElement("span", {
    style: S.dept(s.depts.post)
  }, "POST"))), /*#__PURE__*/React.createElement("td", {
    style: S.td
  }, s.owner))))));
};
window.ShotList = ShotList;
window.__tenetShots = shots;
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/tenet/ShotList.jsx", error: String((e && e.message) || e) }); }

// ui_kits/tenet/Sidebar.jsx
try { (() => {
const tenetSidebarStyles = {
  root: {
    width: 240,
    flexShrink: 0,
    background: "var(--paper-soft)",
    borderRight: "1px solid var(--border)",
    display: "flex",
    flexDirection: "column",
    height: "100vh",
    position: "sticky",
    top: 0,
    fontFamily: "var(--font-sans)"
  },
  brand: {
    padding: "20px 22px 18px",
    borderBottom: "1px solid var(--border)",
    display: "flex",
    alignItems: "center",
    gap: 10
  },
  wordmark: {
    fontFamily: "var(--font-display)",
    fontWeight: 300,
    fontSize: 20,
    color: "var(--anona-orange)",
    letterSpacing: "0.06em"
  },
  project: {
    padding: "16px 22px",
    borderBottom: "1px solid var(--border)",
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    cursor: "pointer"
  },
  projectMeta: {
    fontSize: 10,
    letterSpacing: "0.18em",
    textTransform: "uppercase",
    color: "var(--ink-muted)",
    fontWeight: 400
  },
  projectName: {
    fontSize: 14,
    color: "var(--ink)",
    fontWeight: 400,
    marginTop: 2
  },
  navSection: {
    padding: "14px 12px 6px"
  },
  navLabel: {
    fontSize: 10,
    letterSpacing: "0.18em",
    textTransform: "uppercase",
    color: "var(--ink-faint)",
    fontWeight: 400,
    padding: "0 10px 8px"
  },
  navItem: active => ({
    display: "flex",
    alignItems: "center",
    gap: 12,
    padding: "8px 10px",
    borderRadius: 2,
    cursor: "pointer",
    color: active ? "var(--ink)" : "var(--ink-muted)",
    background: active ? "var(--paper)" : "transparent",
    boxShadow: active ? "inset 2px 0 0 var(--anona-orange)" : "none",
    fontSize: 13,
    fontWeight: active ? 500 : 400,
    transition: "color .15s ease-out, background .15s ease-out"
  }),
  navCount: {
    marginLeft: "auto",
    fontSize: 11,
    color: "var(--ink-faint)",
    fontFamily: "var(--font-mono)"
  },
  footer: {
    marginTop: "auto",
    padding: "14px 22px",
    borderTop: "1px solid var(--border)",
    display: "flex",
    alignItems: "center",
    gap: 10
  },
  avatar: {
    width: 28,
    height: 28,
    borderRadius: 999,
    background: "var(--ink)",
    color: "#fff",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    fontSize: 11,
    fontWeight: 400
  },
  userName: {
    fontSize: 12,
    color: "var(--ink)",
    fontWeight: 400
  },
  userRole: {
    fontSize: 10,
    color: "var(--ink-faint)",
    letterSpacing: "0.04em"
  }
};
const Sidebar = ({
  active = "shots",
  onChange = () => {}
}) => {
  const S = tenetSidebarStyles;
  const items = [{
    id: "script",
    label: "Script",
    icon: "script",
    count: "2 drafts"
  }, {
    id: "shots",
    label: "Shots",
    icon: "shots",
    count: 124
  }, {
    id: "dailies",
    label: "Dailies",
    icon: "dailies",
    count: 18
  }, {
    id: "cuts",
    label: "Cuts",
    icon: "cuts",
    count: "v17"
  }, {
    id: "color",
    label: "Color",
    icon: "color",
    count: null
  }, {
    id: "sound",
    label: "Sound",
    icon: "sound",
    count: null
  }];
  const depts = [{
    id: "picture",
    label: "Picture"
  }, {
    id: "sound2",
    label: "Sound"
  }, {
    id: "vfx",
    label: "VFX"
  }, {
    id: "post",
    label: "Post"
  }];
  return /*#__PURE__*/React.createElement("aside", {
    style: S.root
  }, /*#__PURE__*/React.createElement("div", {
    style: S.brand
  }, /*#__PURE__*/React.createElement("span", {
    style: S.wordmark
  }, "TENET")), /*#__PURE__*/React.createElement("div", {
    style: S.project
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: S.projectMeta
  }, "Project"), /*#__PURE__*/React.createElement("div", {
    style: S.projectName
  }, "The Last Reel")), /*#__PURE__*/React.createElement(Icon, {
    name: "chevron",
    size: 14,
    style: {
      color: "var(--ink-muted)"
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: S.navSection
  }, /*#__PURE__*/React.createElement("div", {
    style: S.navLabel
  }, "Production"), items.map(it => /*#__PURE__*/React.createElement("div", {
    key: it.id,
    style: S.navItem(active === it.id),
    onClick: () => onChange(it.id)
  }, /*#__PURE__*/React.createElement(Icon, {
    name: it.icon,
    size: 16
  }), /*#__PURE__*/React.createElement("span", null, it.label), it.count != null && /*#__PURE__*/React.createElement("span", {
    style: S.navCount
  }, it.count)))), /*#__PURE__*/React.createElement("div", {
    style: S.navSection
  }, /*#__PURE__*/React.createElement("div", {
    style: S.navLabel
  }, "Departments"), depts.map(d => /*#__PURE__*/React.createElement("div", {
    key: d.id,
    style: S.navItem(false)
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 6,
      height: 6,
      borderRadius: 999,
      background: "var(--ink-faint)",
      marginLeft: 5,
      marginRight: 1
    }
  }), /*#__PURE__*/React.createElement("span", null, d.label)))), /*#__PURE__*/React.createElement("div", {
    style: S.footer
  }, /*#__PURE__*/React.createElement("div", {
    style: S.avatar
  }, "DP"), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: S.userName
  }, "David Parker"), /*#__PURE__*/React.createElement("div", {
    style: S.userRole
  }, "Director \xB7 Founder"))));
};
window.Sidebar = Sidebar;
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/tenet/Sidebar.jsx", error: String((e && e.message) || e) }); }

// ui_kits/tenet/StatusChip.jsx
try { (() => {
// Status chip — the only pill in the Tenet system.
// 4 workflow states: pending → in-review → locked → delivered.
const StatusChip = ({
  state = "pending",
  size = "md"
}) => {
  const map = {
    pending: {
      label: "Pending",
      bg: "transparent",
      border: "1px solid var(--border)",
      color: "var(--ink)",
      dot: "var(--ink-faint)"
    },
    review: {
      label: "In Review",
      bg: "var(--amber-50)",
      border: "none",
      color: "var(--amber-800)",
      dot: "var(--anona-orange)"
    },
    locked: {
      label: "Locked",
      bg: "var(--ink)",
      border: "none",
      color: "#fff",
      dot: "#fff"
    },
    delivered: {
      label: "Delivered",
      bg: "transparent",
      border: "1px solid var(--border)",
      color: "var(--ink-muted)",
      dot: "var(--ink)"
    }
  };
  const s = map[state] || map.pending;
  const pad = size === "sm" ? "3px 8px" : "4px 10px";
  const fs = size === "sm" ? 10 : 11;
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: "inline-flex",
      alignItems: "center",
      gap: 6,
      padding: pad,
      borderRadius: 999,
      fontSize: fs,
      fontWeight: 400,
      letterSpacing: "0.02em",
      background: s.bg,
      border: s.border,
      color: s.color,
      whiteSpace: "nowrap"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 6,
      height: 6,
      borderRadius: 999,
      background: s.dot
    }
  }), s.label);
};
window.StatusChip = StatusChip;
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/tenet/StatusChip.jsx", error: String((e && e.message) || e) }); }

// ui_kits/tenet/TopBar.jsx
try { (() => {
const tenetTopBarStyles = {
  root: {
    height: 56,
    flexShrink: 0,
    borderBottom: "1px solid var(--border)",
    background: "rgba(255,255,255,0.85)",
    backdropFilter: "blur(16px)",
    display: "flex",
    alignItems: "center",
    padding: "0 24px",
    gap: 18,
    position: "sticky",
    top: 0,
    zIndex: 5
  },
  crumbs: {
    display: "flex",
    alignItems: "center",
    gap: 10,
    color: "var(--ink-muted)",
    fontSize: 13
  },
  crumbsCurrent: {
    color: "var(--ink)",
    fontWeight: 400
  },
  sep: {
    color: "var(--ink-faint)"
  },
  version: {
    marginLeft: 14,
    display: "inline-flex",
    alignItems: "center",
    gap: 6,
    padding: "4px 10px",
    borderRadius: 2,
    border: "1px solid var(--border)",
    fontSize: 11,
    fontFamily: "var(--font-mono)",
    color: "var(--ink)",
    cursor: "pointer"
  },
  spacer: {
    flex: 1
  },
  search: {
    display: "flex",
    alignItems: "center",
    gap: 8,
    padding: "7px 12px",
    border: "1px solid var(--border)",
    borderRadius: 2,
    color: "var(--ink-faint)",
    fontSize: 12,
    width: 220,
    background: "var(--paper)"
  },
  iconBtn: {
    width: 32,
    height: 32,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    color: "var(--ink-muted)",
    cursor: "pointer",
    borderRadius: 2
  },
  btn: {
    display: "inline-flex",
    alignItems: "center",
    gap: 8,
    padding: "8px 14px",
    borderRadius: 2,
    fontFamily: "var(--font-sans)",
    fontSize: 13,
    fontWeight: 400,
    background: "var(--ink)",
    color: "#fff",
    cursor: "pointer",
    border: "none"
  },
  btnAmber: {
    background: "var(--anona-orange)"
  }
};
const TopBar = ({
  section = "Shots"
}) => {
  const S = tenetTopBarStyles;
  return /*#__PURE__*/React.createElement("header", {
    style: S.root
  }, /*#__PURE__*/React.createElement("div", {
    style: S.crumbs
  }, /*#__PURE__*/React.createElement("span", null, "The Last Reel"), /*#__PURE__*/React.createElement("span", {
    style: S.sep
  }, "/"), /*#__PURE__*/React.createElement("span", {
    style: S.crumbsCurrent
  }, section)), /*#__PURE__*/React.createElement("div", {
    style: S.version
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 6,
      height: 6,
      borderRadius: 999,
      background: "var(--anona-orange)"
    }
  }), "v17 \xB7 WIP", /*#__PURE__*/React.createElement(Icon, {
    name: "chevron",
    size: 12
  })), /*#__PURE__*/React.createElement("div", {
    style: S.spacer
  }), /*#__PURE__*/React.createElement("div", {
    style: S.search
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "search",
    size: 14
  }), /*#__PURE__*/React.createElement("span", null, "Search shots, takes, notes\u2026")), /*#__PURE__*/React.createElement("div", {
    style: S.iconBtn
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "bell",
    size: 18
  })), /*#__PURE__*/React.createElement("div", {
    style: S.iconBtn
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "share",
    size: 18
  })), /*#__PURE__*/React.createElement("button", {
    style: {
      ...S.btn,
      ...S.btnAmber
    }
  }, /*#__PURE__*/React.createElement(Icon, {
    name: "lock",
    size: 14
  }), "Lock the cut"));
};
window.TopBar = TopBar;
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/tenet/TopBar.jsx", error: String((e && e.message) || e) }); }

})();
